- (decision) the thread design document goes back to spec.md, written "the spec", with the skills renamed back to spec and review-spec and the glossary entry for spec superseding its "leaves the vocabulary" row, because "change" collides with ordinary English everywhere and the risk the rework renamed against — agents reading the document as standing truth through its thread-local ids and restated behavior — is now prevented structurally by the removal of FR/AC ids and by standing behavior living only in delta/; keeping "change" with tightened wording was rejected because it leaves the term fighting its surroundings
- (decision) the rule that another thread is history and is read only when the user or this thread's seed names it goes as one line in the shared instruction read-and-cite-the-project-layer.md, so every skill carries it, because the risk of reading an old spec as standing truth sits mostly in the spec-authoring, plan and implement skills; a line in discussion alone, as the seed proposed, was rejected because it leaves those skills unguarded
- (decision) the "another thread is history; read it only when the user or this thread's seed names it" rule is stated in the ## Inputs of every skill that gathers thread inputs, with the line in read-and-cite-the-project-layer.md kept as the canonical wording, superseding the earlier placement in the instruction alone, because each skill reads its own inline inputs at run start and opens that instruction only for citation forms, and seven skills, discussion among them, do not carry it; discussion alone was rejected because the plan, implement and spec skills are where an old spec does damage, and a pointer from every skill to the instruction was rejected because it adds indirection for one sentence the skill body should state itself
