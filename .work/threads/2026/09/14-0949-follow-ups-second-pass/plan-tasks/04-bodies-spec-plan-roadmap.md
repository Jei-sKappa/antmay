### Task 4: Sweep the `spec`, `plan`, and `roadmap` bodies

**Objective:** Bring `spec`, `plan-brief`, `plan-strict`, `check-plan`, and `roadmap` to the prefixed, project-layer-inputs, one-shape, directive-pointer form, with every refusal, block, and completion exit going through the terminal-outcome instruction and no procedure change.

**Input / context:** `spec.md` § "The `<skill_path>` prefix", § "Project-layer inputs", § "One shape for Inputs items", § "Reference pointers as directives", § "Every exit points at the terminal-outcome instruction"; AC-1.1, AC-1.4, AC-2.1 … AC-2.4, AC-3.1, AC-3.2, AC-3.4 (`spec`'s "whole of what the spec rests on" sentence stays), AC-4.1, AC-4.2, AC-4.4, AC-5.1 … AC-5.3, AC-9.2. Settled decisions: `decisions.md DR1`, `DR2`, `DR3`, `DR4`, `DR5`, `DR9`. All five bodies are completion-oriented declarers of `emit-terminal-outcome.md`, so rule E applies. Starts from the tree tasks 1–3 left; the synced copies under these skills are already prefixed, and `emit-terminal-outcome.md` already says it is followed at whichever exit the run reaches (task 2). Today these bodies spell the literal `Outcome:` line 21 times: `spec` 4, `plan-brief` 5, `plan-strict` 4, `check-plan` 4, `roadmap` 4.

**The rules applied to every body in this task.** The letters are for this brief only; none of them, and no `DR<N>` identifier, is written into a body.

- **A. Prefix.** Every pointer to a file inside the skill's own folder is written with the literal prefix `<skill_path>/` — `<skill_path>/references/formats/adr.md`, `<skill_path>/references/instructions/append-log-line.md`, `<skill_path>/references/worked-example.md`. Project paths (`docs/adr/`, `docs/glossary.md`, `.wip/roadmaps/`, thread files such as `spec.md`, `plans/<folder>/`) stay bare.
- **B. Project-layer inputs.** The first two items of `## Inputs` become, with the target clause adapted to the skill (the per-body clause is given below):

  ```markdown
  - `docs/adr/`, read via `/consult-adrs` — the project decisions bearing on <the target>.
  - `docs/glossary.md`, read via `/consult-glossary` — the project's fixed terms, to be used in everything you write.
  ```

  Where the body invokes the conflict rule, the skill is a procedure, never a document: "the conflict rule `/consult-adrs` carries" becomes "as `/consult-adrs` instructs" (or an equivalent that reads as an instruction to classify). The folder is `docs/adr/`, singular, everywhere.
- **C. One shape for Inputs items.** Every item reads `- <path or source><, presence clause when it may be absent> — <what it is and what it is for>.` Remove every trailing tag (`; authoritative.`, `. Authoritative.`, `. Material.`, `. Material: …`, `Authoritative for intent.`, `Authoritative within the thread.`), and fold the inline forms (`, authoritative within the thread;`, `Material either way`) into the clause. Fold what carried information — the thread's `adr/` and `glossary.md` items say the delta takes precedence over the project records inside the thread; a `Material: <what to read>` tag becomes the clause's own sentence — and drop what was decoration. The presence clause is exactly one of `when present`, `when the seed carries one`, `when the file exists`: rewrite every other form (`when the thread holds one`, `whenever the thread holds one`, `when the invocation carries one`, `when the invocation names an entry`, `when it names one`, `when the thread's owner authored one`, `when a plan folder is the form`) to the nearest of the three. An item with no clause is always present or is the primary input; a primary input keeps its accepted-forms text inside its clause, in as many sentences as it needs. After the sweep the words "authoritative" and "material" survive in an item only where they state a ranking the body's procedure uses (for example the primary-input item of the plan skills: the reference "carries no authority over a `spec.md` the thread holds"). Where the body ranks its sources in prose (the review skills' authority-anchor section, `spec`'s "these are the whole of what the spec rests on"), that prose stays and nothing is added to compensate for a removed tag.
- **D. Pointers are directives.** A pointer to an instruction file is the step: "Follow `<skill_path>/references/instructions/<name>.md`" (or "follow … with <parameters>", "by following …"). A pointer to a format file is what a write or read conforms to: "following `<skill_path>/references/formats/<name>.md`" or "in the shape `<skill_path>/references/formats/<name>.md` defines". Other verbs are fine where they read better, provided the intent — open this file and do what it says, or conform to it — stays plain. Strict: the word `per` never directly precedes a `<skill_path>/` pointer; and no text around a pointer restates what the pointed file holds. Delete every sentence or clause that can be deleted because the reference carries it (for `emit-pending-decisions.md`: "one point per open decision", what each point states; for `emit-pending-review.md`: "as a single bundle"; for `append-log-line.md`: the line's shape, the seven types, the transient-framing rule; for `write-implementation-report.md`: the list of what the report folds in; for a format: its section list). Keep only the skill-specific parameters the file leaves open — producer or reviewer name, target, what the line states, which folder, the token and its reason — and any rule the body needs for its own judgment at that step, written as the skill's rule and not as a paraphrase with a citation. Same-body section pointers written "per `## <Section>`" or "per `### <Section>`" are untouched in text and count.
- **E. Every exit through the terminal-outcome instruction** (completion-oriented bodies only). Every sentence that spells a literal `Outcome:` line — refusal, block, or completion — becomes a directive to the instruction with the token and the reason as parameters: "follow `<skill_path>/references/instructions/emit-terminal-outcome.md` with `REFUSED`, naming the dirty paths and how to re-invoke", "… with `DONE` and `Spec written: spec.md`", "… with `BLOCKED` and `pending decisions at <bundle path>`". Existing "End per `references/instructions/emit-terminal-outcome.md` with `Outcome: DONE — …`" sentences become "Follow `<skill_path>/references/instructions/emit-terminal-outcome.md` with `DONE` and `…`". The literal string `Outcome:` appears nowhere in a `SKILL.md`. A body names one token as the parameter at an exit; it never lists `DONE`, `BLOCKED`, and `REFUSED` together as a vocabulary. Skill-local reply tokens and lane verdicts are a different vocabulary and are left alone.

Everything else in each body — posture, write boundary, order of acts, the judgment between them, section headings, numbered-step count, same-body pointers — is unchanged. Preserve each file's existing line-wrapping style (`open-ticket` wraps at 80 columns; the others do not).

**Steps:**

1. `suite/skills/spec/spec/SKILL.md`:
   1. Rules B and C on `## Inputs`. Target clause: "the thread's subject". Primary input: drop the trailing "Material."; `seed.md`: drop the tag; `adr/` and `glossary.md`: "— the thread's delta of the project layer, which inside the thread takes precedence over the project records. The spec cites each record by stem where it is operative and does not restate it." Keep the paragraph "These are the whole of what the spec rests on: …" unchanged.
   2. Inputs closing paragraph (preflight): "…write nothing, and end with `Outcome: REFUSED — <the gap and how to re-invoke>`." → "…write nothing, and follow `<skill_path>/references/instructions/emit-terminal-outcome.md` with `REFUSED`, naming the gap and how to re-invoke."
   3. `## Amendment pass`, the paragraph beginning "Amending in place under these rules": "with one line appended to `log.md` per `references/instructions/append-log-line.md` recording the change" → "with one line appended to `log.md` by following `<skill_path>/references/instructions/append-log-line.md`, recording the change".
   4. Procedure step 1: "A preflight failure writes nothing and ends `Outcome: REFUSED — <reason and how to re-invoke>`, never a pending bundle" → "A preflight failure writes nothing and follows `<skill_path>/references/instructions/emit-terminal-outcome.md` with `REFUSED`, naming the reason and how to re-invoke — never a pending bundle".
   5. Procedure step 6: "Append exactly one `event` line to the thread's `log.md` per `references/instructions/append-log-line.md`, stating that…" → "Follow `<skill_path>/references/instructions/append-log-line.md` with exactly one `event` line stating that the spec was authored or amended and from which form of the primary input." Keep the next sentence about the amendment position.
   6. Procedure step 7: "End per `references/instructions/emit-terminal-outcome.md` with `Outcome: DONE — Spec written: spec.md` after authoring, or `Outcome: DONE — Spec amended: spec.md` after an amendment, and nothing before it" → "Follow `<skill_path>/references/instructions/emit-terminal-outcome.md` with `DONE` and `Spec written: spec.md` after authoring, or `DONE` and `Spec amended: spec.md` after an amendment, and nothing before it".
   7. `## Blocked`: "classify it by the conflict rule `/consult-adrs` carries" → "classify it as `/consult-adrs` instructs"; "queue the open decision(s) per `references/instructions/emit-pending-decisions.md`, naming yourself as the producer, `spec.md` as the target, the originating user request, and one point per open decision. Then stop with a concise notification of where the bundle was written, whose final line is `Outcome: BLOCKED — pending decisions at <bundle path>`." → "follow `<skill_path>/references/instructions/emit-pending-decisions.md` with yourself as the producer, `spec.md` as the target, and the originating user request. Then stop with a concise notification of where the bundle was written and follow `<skill_path>/references/instructions/emit-terminal-outcome.md` with `BLOCKED` and `pending decisions at <bundle path>`."
2. `suite/skills/plan/plan-brief/SKILL.md`:
   1. Rules B and C. Target clause: "the design". `seed.md`: drop the tag; `adr/` and `glossary.md`: fold ("…of the project layer, which inside the thread takes precedence over the project records. The plan cites a record by its stem where a step rests on it, rather than restating it."). The primary-input item keeps its "it is material, and it carries no authority over a `spec.md` the thread holds" clause — that is a ranking the procedure uses.
   2. Rule E on the four refusal/completion sentences: the Inputs closing paragraph (`REFUSED`, naming the ambiguous reference and how to re-invoke); the escalation paragraph ("Tell the user that the work warrants `plan-strict`, write nothing, and follow `<skill_path>/references/instructions/emit-terminal-outcome.md` with `REFUSED` and `work warrants plan-strict; no brief plan written`, rather than emitting an over-inflated brief plan."); Procedure step 1 (preflight, as in `spec`); Procedure step 5 ("Follow `<skill_path>/references/instructions/emit-terminal-outcome.md` with `DONE` and `Plan written: plans/<folder>/plan.md`, and nothing before it — …").
   3. `## Blocked`: conflict-rule phrase → "as `/consult-adrs` instructs"; the pending-decisions and `BLOCKED` sentences as in `spec` step 1.7, with the plan folder as the target.
3. `suite/skills/plan/plan-strict/SKILL.md`: the same edits as `plan-brief` (Inputs; Inputs closing paragraph; Procedure step 1; Procedure step 6 with `DONE` and `Plan written: plans/<folder>/plan.md`; `## Blocked` with the conflict-rule phrase and the pending-decisions and `BLOCKED` sentences), plus Procedure step 3: "look at `references/worked-example.md`" → "look at `<skill_path>/references/worked-example.md`" ("look at" is a plain directive to open the file; keep it).
4. `suite/skills/plan/check-plan/SKILL.md`:
   1. Rules B and C. Target clause: "the plan". The `spec.md` item keeps its "Required: the check does not run on a thread that holds no spec." sentence (the skill's own rule). `adr/` and `glossary.md`: fold as above.
   2. Rule E: the Inputs closing paragraph (`REFUSED`, naming what is missing and how to supply it); Procedure step 1 (preflight); Procedure step 7 ("Follow `<skill_path>/references/instructions/emit-terminal-outcome.md` with `DONE` and `Plan checked: plans/<folder>/`. That is the outcome whether you corrected many faults or none.").
   3. `## Blocked`: conflict-rule phrase; "Apply every derivable correction first, then follow `<skill_path>/references/instructions/emit-pending-decisions.md` with yourself as the producer, the plan folder as the target, and the originating user request. Then stop with a concise notification of where the bundle was written and follow `<skill_path>/references/instructions/emit-terminal-outcome.md` with `BLOCKED` and `pending decisions at <bundle path>`."
5. `suite/skills/roadmap/roadmap/SKILL.md`:
   1. Rules B and C. Target clause: "the direction". `seed.md`: drop the tag; `spec.md`, `when the file exists` (was "when the thread's owner authored one"), drop the tag; `adr/` and `glossary.md`: "— the direction's settled constraints and terms, which inside the thread take precedence over the project records. These records are the constraints that bind the threads opened from the index's entries, …" (keep that second sentence unchanged).
   2. Rule E: the preflight paragraph ("Every preflight failure writes nothing, emits no bundle, and follows `<skill_path>/references/instructions/emit-terminal-outcome.md` with `REFUSED`, naming the reason and how to re-invoke."); the existing-file bullet ("write nothing and follow `<skill_path>/references/instructions/emit-terminal-outcome.md` with `REFUSED` and the existing path: an index is authored once …"); the blocked paragraph ("Stop with a concise notification of where the bundle was written and follow … with `BLOCKED` and `pending decisions at <bundle path>`."); the report paragraph ("Follow `<skill_path>/references/instructions/emit-terminal-outcome.md` with `DONE` and `Roadmap index written: .wip/roadmaps/<file>`. No preamble, no closing remark.").
   3. Rule A on "in the shape `references/formats/roadmap-index.md` defines" (already a directive; prefix only). Rule D on "Queue the open decision(s) as one bundle per `references/instructions/emit-pending-decisions.md`, naming yourself as the producer, the index path you were authoring as the target, the originating user request, and one point per open decision." → "Queue the open decision(s): follow `<skill_path>/references/instructions/emit-pending-decisions.md` with yourself as the producer, the index path you were authoring as the target, and the originating user request."
6. Read each of the five bodies once end to end and apply rules A, D, and E to anything the list above missed; in particular confirm that no sentence enumerates `DONE`, `BLOCKED`, and `REFUSED` together.

**Files modified:** `suite/skills/spec/spec/SKILL.md`, `suite/skills/plan/plan-brief/SKILL.md`, `suite/skills/plan/plan-strict/SKILL.md`, `suite/skills/plan/check-plan/SKILL.md`, `suite/skills/roadmap/roadmap/SKILL.md`.

**Verification:** run from `suite/` (`$BASE` is the commit the run started from):

```sh
BODIES="skills/spec/spec/SKILL.md skills/plan/plan-brief/SKILL.md skills/plan/plan-strict/SKILL.md skills/plan/check-plan/SKILL.md skills/roadmap/roadmap/SKILL.md"
FILES="$BODIES"
grep -n 'references/' $FILES | grep -v '<skill_path>/references/'       # → no output (AC-1.1, scoped)
grep -n '<skill_path>/docs\|<skill_path>/\.wip\|<skill_path>/plans\|<skill_path>/spec\.md' $FILES   # → no output (AC-1.4)
grep -n 'per `<skill_path>' $FILES                                       # → no output (AC-4.1)
grep -n 'Outcome:' $FILES                                                # → no output (AC-5.1)
grep -nE '(; authoritative\.|\. Authoritative\.|\. Material\.|\. Material:|Authoritative for intent\.|Authoritative within the thread\.)' $FILES   # → no output (AC-3.1)
grep -niE 'authoritative within the thread|material either way' $FILES   # → no output (inline tag forms folded)
grep -n "write the project's fixed terms" $FILES                         # → no output (AC-2.2)
grep -n 'consult-adrs` carries\|docs/adrs' $FILES                        # → no output (AC-2.3, AC-2.4)
grep -nE '\bDR[0-9]+\b' $FILES                                           # → no output (register)
grep -nE 'when (the thread holds one|the invocation (carries one|names an entry)|it names one|the thread.s owner authored one|a plan folder is the form)|whenever the thread holds one' $FILES   # → no output (AC-3.2 presence vocabulary)
for f in $BODIES; do
  awk -v F="$f" '/^## Inputs/{p=1;next} p&&/^## /{exit} p&&/^- /{c++; if(c==1&&$0!~/^- `docs\/adr\/`, read via `\/consult-adrs` — /)print F": item 1 wrong"; if(c==2){if($0!~/^- `docs\/glossary\.md`, read via `\/consult-glossary` — /)print F": item 2 wrong"; exit}}' $f
done                                                                     # → no output (AC-2.1)
for f in $FILES; do
  a=$(git show $BASE:suite/$f | grep -o 'per `#' | wc -l); b=$(grep -o 'per `#' $f | wc -l); [ "$a" = "$b" ] || echo "$f: same-body per count $a -> $b"
  diff <(git show $BASE:suite/$f | grep -E '^#{1,4} ') <(grep -E '^#{1,4} ' $f) >/dev/null || echo "$f: heading set changed"
  a=$(git show $BASE:suite/$f | grep -cE '^ {0,3}[0-9]+\. '); b=$(grep -cE '^ {0,3}[0-9]+\. ' $f); [ "$a" = "$b" ] || echo "$f: numbered-step count $a -> $b"
done                                                                     # → no output (AC-4.2, AC-9.2)
git status --porcelain -- ../cli                                        # → no output (AC-9.1)
node scripts/check-marketplace-skills.mjs                                # → exit 0
for f in $FILES; do n=$(grep -c '<skill_path>/references/instructions/emit-terminal-outcome.md' $f); echo "$f exits via the instruction: $n"; done
#  → spec 4, plan-brief 5, plan-strict 4, check-plan 4, roadmap 4 — one per former Outcome: literal (AC-5.2)
grep -n 'with `DONE` and `' $FILES | wc -l                                # → 5 lines (spec names both DONE reasons on one line)
grep -nE '`DONE`.*`BLOCKED`.*`REFUSED`|`REFUSED`.*`BLOCKED`.*`DONE`' $FILES   # → no output (AC-5.3)
grep -c 'whole of what the spec rests on' skills/spec/spec/SKILL.md      # → 1 (AC-3.4)
grep -c '<skill_path>/references/worked-example.md' skills/plan/plan-strict/SKILL.md   # → 1
grep -n 'one point per open decision' $FILES                              # → no output (instruction content no longer restated)
```

**Acceptance criteria:**
- Every skill-local pointer in the five bodies carries `<skill_path>/`; no project path carries it (AC-1.1, AC-1.4, scoped).
- Each `## Inputs` opens with the two project-layer items; no body says "write the project's fixed terms" or "`/consult-adrs` carries" (AC-2.1 … AC-2.3).
- No Inputs item ends in a tag and every presence clause is one of the three; `spec` keeps its "whole of what the spec rests on" sentence (AC-3.1, AC-3.2, AC-3.4).
- No `per` before a `<skill_path>/` pointer; no text around an instruction pointer restates the instruction (AC-4.1, AC-4.4).
- `Outcome:` appears in none of the five bodies; every refusal, block, and completion exit points at `<skill_path>/references/instructions/emit-terminal-outcome.md` with its token; no body lists the three tokens as a vocabulary (AC-5.1 … AC-5.3).
- Same-body `per` count, heading set, and numbered-step count unchanged in every body (AC-4.2, AC-9.2); marketplace check exits 0; `cli/` has no diff.

**Consumes:** `suite/shared/references/instructions/emit-terminal-outcome.md` carrying the every-exit sentence (task 2), synced under each of these skills; the `discussion` step 5 wording (task 3) as the model for `spec`'s log-line steps.

**Produces:** the exit wording "follow `<skill_path>/references/instructions/emit-terminal-outcome.md` with `<TOKEN>` and `<reason>`" as used in these bodies — tasks 5 and 6 use the same phrasing so the twelve declarers read alike.
