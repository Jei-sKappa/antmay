- (direction) the thread addresses only the durable recording of rejected alternatives and leaves the capture of decision rationale alone, because the reason clause is already mandatory in the current log-line format and the gap the ticket describes was read off an older version of the skill
- (decision) an alternative is recorded as considered and rejected exactly when the settling exchange produced an argument against it — a reason it does not work, from either side — and never when it was merely presented, compared, or left unselected, because the record exists to answer a later reader who reaches for that option and there is nothing to preserve where no one had to be talked out of it
- (constraint) option letters and any other framing from the options menu stay out of the log line, as the append-log-line instruction already requires, because a reader of log.md has no menu left to resolve them against
- (constraint) an argument against an option never by itself settles a point or triggers a log line — only the user settling the point does, as the discussion procedure already requires — so the rejection trigger governs what a settled decision's line carries and never when a line is written
- (decision) the shape of a decision line that carries a rejection goes in the log-line format and the trigger for including one goes in the append-log-line instruction, because a format states what the artifact is while an instruction carries the judgment made during the act, and putting the rule in those two shared references reaches resolve-pending-decisions as well as discussion
- (decision) the inline dissent template in the discussion skill's peer framing is removed in favour of the general rejection rule, because it is the one-directional special case of that rule and its trailing wording cannot generalise to the case where the agent's argument prevails
- (constraint) the peer-framing mandate to flag a point the agent believes is wrong stays as it is and the conversational behaviour it produces is unchanged, because the thread changes only what a settled point is recorded as and never how the discussion is conducted
- (decision) the rejection rule reaches any alternative that was argued against whatever its provenance — a lettered option, a lone practical proposal, or an approach raised mid-conversation — because what triggers the record is the argument and most points settle with no options menu in play
- (decision) the project term "rejected alternative" is fixed for an option someone argued against before a point settled, because the rule turns entirely on the distinction between argued against and merely mentioned and that distinction has to survive restatement in the log-line format, the append-log-line instruction, and the discussion skill body
