### Task 15: Maintainer documents and final sweeps

**Objective:** Bring the maintainer documents — `docs/documentation-rules.md`, both `AGENTS.md` files and the two remaining authoring documents — to the reworked model, run the whole-change sweeps the spec's acceptance requires, and list the CLI drift for the implementation report.

**Input / context:** `spec.md` `## This repository's adoption` (`docs/documentation-rules.md` edited only where its citation paragraph changes, its three-kinds section gains `docs/product/` and `docs/architecture/`, its `AGENTS.md` paragraph untouched; the root `AGENTS.md` governing-documents table points at the new files), `### Citation and read order` (the citation rule by direction), `## Acceptance criteria` FR-9 to FR-11, `## Constraints` (CLI drift recorded in the report), `## Inferences` (`suite/authoring/` touched only where it names retired skills or the inputs opening pair). The index's interpretation note on `suite/authoring/side-effects.md`. Current files: `docs/documentation-rules.md`, `AGENTS.md` (root), `suite/AGENTS.md`, `suite/authoring/interaction-posture.md`, `suite/authoring/side-effects.md`. Files from task 14 (`docs/product/method.md`, `docs/architecture/suite.md`). This is the last task; it also carries the whole-change gates.

**Steps:**

1. Edit `docs/documentation-rules.md`. In `## Three kinds of document, one audience each`, the maintainer-documentation paragraph replaces "`docs/working-with-threads.md` covers how the repository uses the suite on itself" with: `docs/product/method.md` describes the method as this repository runs it and `docs/architecture/suite.md` describes how the suite is put together — both maintainer documentation of this repository held in the shapes the suite's product-behavior and architecture-description formats define. In `## Document only durable, properly scoped information`, replace the paragraph beginning "Thread-local material stays in its thread." with the citation rule stated by direction: nothing under `.work/` — a thread's log entries, change document, delta documents, plans or implementation folders, or a roadmap entry — is cited from `docs/` or from code, and a thread path appears outside its thread only as commit provenance; a decision record, ADR or PDR, is cited by its stem for the reason behind a choice and never for what the system does; a description is cited by path and heading; a roadmap entry is cited by index path and entry slug from thread artifacts only; code, comments, test names and migrations carry no thread reference and a test is named for the behavior it proves; no citation is ever required. Leave the `AGENTS.md` paragraph byte-identical and every other paragraph unchanged.
2. Edit the root `AGENTS.md`. `## Layout`: `docs/glossary.md the project's terms` gains sibling lines for `docs/adr/` and `docs/pdr/` (decision records, created lazily), `docs/product/` (product behavior, one document per capability) and `docs/architecture/` (architecture description, one document per module); the `docs/working-with-threads.md` line becomes `docs/product/method.md`. `## The documents that govern the work` table: the `docs/working-with-threads.md` row becomes `docs/product/method.md` — "How the method works and how this repository runs it, which skill to reach for"; add a row `docs/architecture/suite.md` — "How the suite is put together: shared references, distribution, gates". `## Before anything else`: the `/consult-adrs` bullet becomes "Invoke `/consult-decisions` to read the project decisions bearing on what you are about to do — `docs/adr/` and `docs/pdr/` — and `/consult-descriptions` for the product behavior and architecture description the work touches; both are authoritative and carry the rule for a contradiction."; the `/consult-glossary` bullet becomes "Read `docs/glossary.md` before writing; it is this repository's naming authority: one meaning per term across the suite, the CLI, and these documents."; the terms bullet becomes "Terms, decisions and descriptions are settled through threads — a thread drafts them as delta documents under its `delta/`, and `close-thread` lands them at close; do not edit the project layer by hand outside that landing." Nothing else in the file changes (the CLI-on-hold sections stand).
3. Edit `suite/AGENTS.md` `## Layout`: `model-invoked/ consult-decisions, consult-descriptions`; `spec/ spec` becomes `change/ change` in alphabetical position; `review/ review-code, review-implementation, review-change`; the `shared/references/` tree stands. Nothing else changes unless a sentence names a retired skill.
4. Edit `suite/authoring/interaction-posture.md`: in the completion-oriented bullet, `spec` becomes `change`; the example `Outcome: DONE — Spec written: spec.md` becomes `Outcome: DONE — Change document written: change.md`. Nothing else.
5. Edit `suite/authoring/side-effects.md` `## The suite-wide write boundaries` only: the `discussion`/`resolve-pending-decisions`/`spec`/`close-thread` log bullet names `change` in place of `spec`; the bullet saying `discussion` and `resolve-pending-decisions` write `adr/` drafts and `glossary.md` is removed; the `spec` bullet becomes "`change` writes the thread's `change.md` and its delta documents under `delta/`, and amends both in place"; the implement bullet adds "and nothing in the project layer"; the `close-thread` bullet becomes "`close-thread` alone writes the project layer by landing the thread's delta documents — `docs/adr/`, `docs/pdr/`, `docs/product/`, `docs/architecture/`, `docs/glossary.md`, and the `superseded/` folders records move into — plus the closing line beneath one roadmap entry; it also appends the thread's closing event and leaves the thread folder where it is."; the paragraph below the map replaces "reads its spec and its plan" with "reads its change document and its plan" and "a contradiction of a thread record or of a spec decision" with "a contradiction of a delta document or of a change document decision". Touch no other section of the document.
6. Whole-change sweeps, each fixed at its source when it fails: (a) `grep -rn -E 'spec\.md|review-spec|consult-adrs|consult-glossary|binding test' suite/ README.md AGENTS.md CONTRIBUTING.md docs/documentation-rules.md docs/product docs/architecture .claude-plugin .vscode .github` returns nothing (the phrase "Spec Driven Development" is allowed and is not matched by these patterns; `docs/glossary.md` is outside the sweep because no task edits it — its **spec**, **binding test** and `consult-adrs` rows are retired by this thread's `glossary.md` rows landing at close); (b) a grep for the retired thread files — the backticked `adr/` folder and the thread's or thread-root `glossary.md` — over `suite/`, `README.md`, `AGENTS.md` and `docs/documentation-rules.md` returns nothing (the exact command is in the verification block); (c) `grep -rln -E 'FR-[0-9]|AC-[0-9]' suite/` returns nothing (the change-document format and the plan skills name the forbidden identifiers only as the thing not to write; keep those mentions, and confirm every hit is such a mention); (d) no format under `suite/shared/references/formats/` defines a `status`, `lifecycle` or `kind` frontmatter key for a project-layer file, and no skill or format states a count threshold for records or reports a record count: `grep -rn -E '^(status|lifecycle|kind):' suite/shared/references/formats/` returns nothing and `grep -rn -i -E 'more than [a-z0-9]+ (records|ADRs|decisions)|record count' suite/skills suite/shared` returns nothing; (e) `git diff --stat fe83a4f -- suite/authoring/` lists only `body-structure.md`, `skill-roles.md`, `interaction-posture.md` and `side-effects.md`, and every hunk in the four touches a retired-skill reference, the inputs opening block, or the write-boundary map lines named above; (f) `git diff --quiet fe83a4f -- cli/` and `git diff --quiet fe83a4f -- docs/glossary.md`; (g) the paragraph beginning "`AGENTS.md` files are durable working memory" in `docs/documentation-rules.md` is byte-identical to the baseline.
7. Record the CLI drift for the implementation report's `## Follow-ups` (this run's report is written by the implement skill; carry these lines into it): the stage catalog and stage ids name `spec` and `review-spec` (`cli/src/pipeline/catalog.ts`, `cli/src/pipeline/stage-id.ts` and their tests); the thread paths `spec.md` and `adr/` in target resolution and composition (`cli/src/pipeline/targets.ts`, `cli/src/pipeline/composition.ts` and tests); the stage-support table and the artifact-state prose in `cli/README.md` naming `spec.md`; `cli/src/pipeline/documentation.test.ts`, which reads this repository's `README.md` skill list and now fails, so the `cli check` CI job fails until the `[contract]` realignment thread; and that the executor's Git boundary selectors still allow `spec.md` rather than `change.md` and `delta/`.
8. Run the suite checks one last time from `suite/`.

**Files modified:** `docs/documentation-rules.md`, `AGENTS.md`, `suite/AGENTS.md`, `suite/authoring/interaction-posture.md`, `suite/authoring/side-effects.md`.

**Verification:**

```sh
grep -q 'docs/product/method.md' docs/documentation-rules.md && grep -q 'docs/architecture/suite.md' docs/documentation-rules.md && grep -q 'PDR' docs/documentation-rules.md && grep -q 'path and heading' docs/documentation-rules.md && grep -q 'commit provenance' docs/documentation-rules.md
diff <(git show fe83a4f:docs/documentation-rules.md | sed -n '/^`AGENTS.md` files are durable working memory/,/^$/p') <(sed -n '/^`AGENTS.md` files are durable working memory/,/^$/p' docs/documentation-rules.md) && echo agents-paragraph-identical
grep -q 'docs/product/method.md' AGENTS.md && grep -q 'docs/architecture/suite.md' AGENTS.md && grep -q '/consult-decisions' AGENTS.md && ! grep -q -E 'consult-adrs|consult-glossary|working-with-threads' AGENTS.md
grep -q 'consult-decisions, consult-descriptions' suite/AGENTS.md && grep -q 'change/' suite/AGENTS.md && ! grep -q -E 'consult-adrs|spec/ +spec|review-spec' suite/AGENTS.md
grep -q 'Change document written: change.md' suite/authoring/interaction-posture.md && ! grep -q 'Spec written' suite/authoring/interaction-posture.md
grep -q 'delta/' suite/authoring/side-effects.md && ! grep -n -E 'spec\.md|`spec`|adr/` drafts' suite/authoring/side-effects.md
git diff --stat fe83a4f -- suite/authoring/ | grep -v -E 'body-structure.md|skill-roles.md|interaction-posture.md|side-effects.md|files? changed' ; echo "(nothing above this line but the four files)"
! grep -rn -E 'spec\.md|review-spec|consult-adrs|consult-glossary|binding test' suite/ README.md AGENTS.md CONTRIBUTING.md docs/documentation-rules.md docs/product docs/architecture .claude-plugin .vscode .github   # docs/glossary.md is never edited; its rows land at close
! grep -rn -E -e '`adr/`' -e 'thread.s `glossary.md`' -e 'thread-root `glossary.md`' suite/ README.md AGENTS.md docs/documentation-rules.md
! grep -rn -E '^(status|lifecycle|kind):' suite/shared/references/formats/
! grep -rn -i -E 'more than [a-z0-9]+ (records|ADRs|decisions)|record count' suite/skills suite/shared
git diff --quiet fe83a4f -- cli/ && git diff --quiet fe83a4f -- docs/glossary.md && echo cli-and-glossary-untouched
(cd suite && node scripts/sync-shared-references.mjs && node scripts/check-marketplace-skills.mjs && node scripts/check-skill-text.mjs)
git status --porcelain | grep -v '^.M\|^A \|^D \|^R \|^M \|^??' ; git diff --name-only fe83a4f | grep -c .   # the whole change, for the report's ## Changes
```

**Acceptance criteria:**

- `docs/documentation-rules.md` states the citation rule by direction, names PDR, path-and-heading citation and the commit provenance exception, lists `docs/product/` and `docs/architecture/` as maintainer documentation, and its `AGENTS.md` paragraph is byte-identical to the baseline.
- The root `AGENTS.md` governing-documents table names `docs/product/method.md` and `docs/architecture/suite.md`, and its before-anything bullets name `/consult-decisions`, `/consult-descriptions` and the direct read of `docs/glossary.md`; `suite/AGENTS.md` lists the current skill folders.
- `suite/authoring/` differs from the baseline only in `body-structure.md`, `skill-roles.md`, `interaction-posture.md` and `side-effects.md`, each only where it named a retired skill, the inputs opening pair, or the write-boundary map.
- No file under `suite/`, `docs/` other than the unedited `docs/glossary.md`, `README.md`, `AGENTS.md`, `CONTRIBUTING.md`, `.claude-plugin/`, `.vscode/` or `.github/` names `spec.md`, `review-spec`, `consult-adrs`, `consult-glossary` or the binding test; no format defines a status, lifecycle or kind key on a project-layer file; no skill or format states a record-count threshold.
- No file under `cli/` differs from the baseline, `docs/glossary.md` is unchanged, and the implementation report's `## Follow-ups` lists the CLI drift named in step 7.
- The sync script and both suite checks exit 0.

**Consumes:** `docs/product/method.md` and `docs/architecture/suite.md` (task 14); every renamed skill and rewritten body from tasks 2–13 (the sweeps run over the finished tree).

**Produces:** none
