### Task 2: Consult skills

**Objective:** Leave the suite with exactly two model-invoked skills — `consult-decisions` for `docs/adr/` and `docs/pdr/`, and `consult-descriptions` for `docs/product/` and `docs/architecture/` — each carrying its listing command, its citation form and how its kind binds, with `consult-adrs` renamed into the first and `consult-glossary` retired.

**Input / context:** `spec.md` `### Citation and read order` (the two remaining skills and what each carries), `### Admission rules for descriptions` (the conflict rule), `## Skills whose roles change` (last row), `## Degrees of freedom` (the listing commands are free). Thread glossary rows **consult-decisions**, **consult-descriptions**, **consult-glossary**, **consult-adrs**, **description**. `suite/authoring/skill-roles.md` (model-invoked role: no `disable-model-invocation`, `agents/openai.yaml` with the `interface` block only; the description is what the model routes on; the name is the act an agent performs) and `suite/AGENTS.md` `## Before anything else` (registration in the manifest, `marketplace.json`, `README.md`, `.vscode/settings.json`). Current bodies: `suite/skills/model-invoked/consult-adrs/SKILL.md`, `suite/skills/model-invoked/consult-glossary/SKILL.md`. Starts from task 1's formats.

**Steps:**

1. `git mv suite/skills/model-invoked/consult-adrs suite/skills/model-invoked/consult-decisions`.
2. Rewrite `suite/skills/model-invoked/consult-decisions/SKILL.md`: frontmatter `name: consult-decisions`, `version: 0.0.0`, a routing description saying to read the project's decision records — `docs/adr/` and `docs/pdr/` — before acting on any work that could rest on a project decision or contradict one, and not to invoke it when neither folder holds a `.md` file. The body: one paragraph stating that the two folders are the whole authoritative surface for project decisions, ADRs for how the system is structured or built and PDRs for what the product does or for whom, in the shape `<skill_path>/references/formats/decision-record.md` defines; `## Print the catalog` with one shell command that prints stem, name and description for every record in both folders without loading bodies (adapt the existing `awk` loop to iterate `docs/adr/*.md` and `docs/pdr/*.md`, tolerating an absent folder); `## Open what bears on the work` (open every record whose subject touches the work, read in full, authoritative until superseded); `## How a record is cited and binds` (cite by stem, for the reason behind a choice, never for what the system does; a record binds until a later record naming it under `supersedes` lands and moves it to `superseded/`); `## When the work contradicts a record`, rewritten around the thread's delta: a contradiction is intentional when the thread's `delta/` holds a `create` delta document naming that record under `supersedes`, an `edit` or `delete` delta document targeting it, or an `edit` delta document for `docs/glossary.md` that redefines the term — writing that delta document is how the user's confirmation was recorded; every other contradiction is an unnoticed conflict raised rather than settled (an interactive agent puts it to the user, a completion-oriented run queues it as a pending decision), never resolved by overriding the project record.
3. Update `suite/skills/model-invoked/consult-decisions/agents/openai.yaml`: `display_name: Consult Decisions`, a fresh 4–7-word `short_description`, no `policy` block.
4. `git rm -r suite/skills/model-invoked/consult-glossary`.
5. Create `suite/skills/model-invoked/consult-descriptions/SKILL.md`: frontmatter `name: consult-descriptions`, `metadata.author: https://github.com/Jei-sKappa`, `version: 0.0.0`, no `disable-model-invocation`; a routing description saying to open the product behavior and architecture description for what the work touches before designing, planning, implementing, reviewing or closing, and not to invoke it when neither `docs/product/` nor `docs/architecture/` holds a `.md` file. The body: one paragraph naming the two kinds and their shapes (`<skill_path>/references/formats/product-behavior.md`, `<skill_path>/references/formats/architecture-description.md`); `## List what exists` with one shell command that prints each document's path and its `#`/`##` headings without loading every body; `## Open what the work touches` (the product behavior of each touched capability and the architecture description of each touched module, read in full); `## How a description is cited and read` — cite by path and heading; every statement is built-only and present tense, describing what exists now, so behavior that is settled but not built is found in the roadmap entry that will build it and never here; when a description and the code disagree, identify which of three it is — a bug, a stale description, or an authorised change — and never assume the code or the prose is right; a description changes only through a thread's delta documents landed at close.
6. Create `suite/skills/model-invoked/consult-descriptions/agents/openai.yaml` with `display_name: Consult Descriptions` and a fresh short description, interface block only.
7. In `suite/shared/manifest.yaml`: rename the key `skills/model-invoked/consult-adrs` to `skills/model-invoked/consult-decisions` (declaring `formats/decision-record.md`); delete the `skills/model-invoked/consult-glossary` key and its list; add `skills/model-invoked/consult-descriptions` declaring `formats/product-behavior.md` and `formats/architecture-description.md`.
8. In `.claude-plugin/marketplace.json`, replace `./skills/model-invoked/consult-adrs` with `./skills/model-invoked/consult-decisions` and `./skills/model-invoked/consult-glossary` with `./skills/model-invoked/consult-descriptions`, keeping the array sorted.
9. In `.vscode/settings.json` `conventionalCommits.scopes`, replace `consult-adrs` and `consult-glossary` with `consult-decisions` and `consult-descriptions`, keeping the array sorted.
10. In `README.md` `## Model-invoked skills`, replace the two entries with `consult-decisions` and `consult-descriptions`, each in the existing "Expects … ; leaves nothing on disk — …" register with its install snippet and its `./suite/skills/model-invoked/<name>/SKILL.md` link; leave the rest of the README for tasks 14 and 15.
11. In every `suite/skills/*/*/SKILL.md` and in `suite/authoring/body-structure.md`, replace each `/consult-adrs` with `/consult-decisions` (the `## Inputs` opening lines and every "classify it as `/consult-adrs` instructs" sentence), and replace each `## Inputs` line that reads `docs/glossary.md`, read via `/consult-glossary` with the line below; in `suite/authoring/skill-roles.md` replace the naming example `consult-adrs` with `consult-decisions`. Task 3 reshapes these opening lines further; this step only removes every reference to a skill that no longer exists.

```markdown
- `docs/glossary.md`, when the file exists — the project's fixed terms, to be used in everything you write.
```
12. From `suite/`, run the sync script and both checks.

**Files modified:** `suite/skills/model-invoked/consult-adrs/` → `suite/skills/model-invoked/consult-decisions/` (renamed: `SKILL.md`, `agents/openai.yaml`, `references/formats/decision-record.md`), `suite/skills/model-invoked/consult-glossary/` (DELETED, all three files), `suite/skills/model-invoked/consult-descriptions/SKILL.md` (NEW), `suite/skills/model-invoked/consult-descriptions/agents/openai.yaml` (NEW), `suite/skills/model-invoked/consult-descriptions/references/formats/product-behavior.md` (NEW, synced), `suite/skills/model-invoked/consult-descriptions/references/formats/architecture-description.md` (NEW, synced), `suite/shared/manifest.yaml`, `.claude-plugin/marketplace.json`, `.vscode/settings.json`, `README.md`, `suite/authoring/body-structure.md`, `suite/authoring/skill-roles.md`, and every `suite/skills/*/*/SKILL.md` that named `/consult-adrs` or `/consult-glossary` (all sixteen entry-point skills).

**Verification:**

```sh
test -d suite/skills/model-invoked/consult-decisions && test -d suite/skills/model-invoked/consult-descriptions && ! test -e suite/skills/model-invoked/consult-adrs && ! test -e suite/skills/model-invoked/consult-glossary
grep -q '^name: consult-decisions$' suite/skills/model-invoked/consult-decisions/SKILL.md && grep -q '^name: consult-descriptions$' suite/skills/model-invoked/consult-descriptions/SKILL.md
! grep -q 'disable-model-invocation' suite/skills/model-invoked/consult-decisions/SKILL.md suite/skills/model-invoked/consult-descriptions/SKILL.md
! grep -q 'policy' suite/skills/model-invoked/consult-decisions/agents/openai.yaml suite/skills/model-invoked/consult-descriptions/agents/openai.yaml
grep -q 'docs/pdr/' suite/skills/model-invoked/consult-decisions/SKILL.md && grep -q 'docs/product/' suite/skills/model-invoked/consult-descriptions/SKILL.md && grep -q 'docs/architecture/' suite/skills/model-invoked/consult-descriptions/SKILL.md
grep -c '```sh' suite/skills/model-invoked/consult-decisions/SKILL.md suite/skills/model-invoked/consult-descriptions/SKILL.md   # at least 1 each: the listing command
grep -q 'delta/' suite/skills/model-invoked/consult-decisions/SKILL.md
! grep -rn -E 'consult-adrs|consult-glossary' suite/ README.md .claude-plugin .vscode
grep -q 'consult-decisions' README.md && grep -q 'consult-descriptions' README.md
(cd suite && node scripts/sync-shared-references.mjs && node scripts/check-marketplace-skills.mjs && node scripts/check-skill-text.mjs)
git diff --quiet fe83a4f -- cli/ && git diff --quiet fe83a4f -- docs/glossary.md
```

Run the two listing commands against this repository (`docs/adr/` and `docs/pdr/` are absent here; `docs/product/` and `docs/architecture/` are absent until task 14): each must exit without error and print nothing rather than fail on a missing folder.

**Acceptance criteria:**

- `suite/skills/model-invoked/` holds exactly `consult-decisions` and `consult-descriptions`, each with a `SKILL.md`, an `agents/openai.yaml` carrying only the interface block, and its synced format copies.
- `consult-decisions` lists `docs/adr/` and `docs/pdr/` without loading every record, states stem citation for the reason behind a choice and never for what the system does, states that a record binds until superseded, and classifies a contradiction as intentional only through the thread's `delta/`.
- `consult-descriptions` lists `docs/product/` and `docs/architecture/` without loading every file, states path-and-heading citation, the built-only present-tense reading, the bug / stale description / authorised change rule, and that descriptions change only through delta documents landed at close.
- No file under `suite/`, `README.md`, `.claude-plugin/` or `.vscode/` names `consult-adrs` or `consult-glossary`; `README.md` lists the two remaining model-invoked skills; the manifest, `marketplace.json` and the scopes array register both.
- The sync script and both suite checks exit 0; nothing under `cli/` or in `docs/glossary.md` differs from the baseline.

**Consumes:** `suite/shared/references/formats/decision-record.md`, `formats/product-behavior.md`, `formats/architecture-description.md` from task 1.

**Produces:** the skill invocations `/consult-decisions` and `/consult-descriptions`, which task 3 writes into every entry-point skill's `## Inputs`.
