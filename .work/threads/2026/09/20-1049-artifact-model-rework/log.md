- (event) the five Leitspace reports were copied unchanged into reports/ and researches/audit-synthesis.md was written from them, the suite at HEAD 7d3d60d, and the 05-1339 thread, as the coverage base for the discussion
- (decision) the method owns two living kinds beside ADR, glossary and roadmap index, namely product requirements (standing FR and NFR, one document per capability) and architecture description (what exists, one document per module), because both audits found exactly these two kinds pressed into ADR shape at Leitspace; keeping three containers and only raising the ADR bar was rejected because both audits show it moves the dumping ground to AGENTS.md, and the homes for rules, guidelines, product decisions and release scope are settled as their own points
- (decision) product requirements describe built behaviour only, one concise present-tense statement per behaviour, admitted only when it is not obvious from the code, and planned behaviour has exactly one home, the roadmap entry that will build it, because the roadmap index already is the living artifact whose entries close and whose file is deleted at the destination, so planned behaviour gets a sunset for free and the product kind cannot become a shadow spec; holding planned behaviour inside the product document with a marker, an owner and a sunset was rejected because it duplicates what roadmap entries already do and reproduces the pre-mortem failure where the dumping ground moves one folder over
- (decision) a thread stages its changes to product requirements and architecture description as delta files made of literal add, replace and remove operations, each carrying the exact text it adds, replaces or removes and recording the git blob hash of its target file at drafting time, landed only by close-thread, which treats an operation whose new text is already present as done and stops to ask the user whenever the hash or the quoted text no longer matches, because literal operations keep the landing mechanical where the semantic glossary merge lost content and the hash makes an intervening edit detectable; editing the canonical documents within implementation scope was rejected because the project layer has close-thread as its single writer, whole-heading replacement was rejected because it duplicates content unrelated to the thread, and instruction-style operations were rejected because applying them is the semantic merge that loses content
- (decision) the implement skills write nothing inside the project layer, which now includes product requirements and architecture description, so their boundary is stated as a closed deny-list rather than as living documentation within scope, because that phrase covered both the method-owned living kinds and the user-owned documentation the implementer may still edit
- (decision) the spec authoring drafts the living delta files as it writes the spec, and the spec cites those files instead of restating them, because the spec is already the projection of the whole settled design and the delta then inherits its amend-in-place lifecycle; drafting per point inside the discussion was rejected because it turns the interview into filing and a statement usually spans several points, and drafting at close from what was built was rejected because a document nobody can review until close is a document nobody reviews
- (decision) the spec is a change document: a sentence describing standing behaviour of the product or standing structure of the system is written in a delta file and cited from the spec, never written in the spec body, which keeps only what is thread-only (goal, context, scope, constraints, the shape of the change, acceptance guidance for everything built including the obvious parts, freedoms, inferences), because that one rule stops the spec from becoming a shadow product document that dies at close; keeping the full behaviour narrative in the spec beside the deltas was rejected because it writes the same behaviour twice in two registers, the duplication Leitspace produced by design
- (decision) the thread design document is renamed from spec.md to change.md, written as "the change document" in prose and never as bare "change", because the common reading of "spec" is the document code is checked against now, which is the reading that made agents treat thread-local ids as authoritative, while the glossary already defines the unit of work as a single coherent change; keeping the name with a rewritten glossary row was rejected because the name keeps saying standing truth to every reader who has not read the glossary, and the qualified form "change spec" was rejected because it is shortened back to "spec" in use; Spec Driven Development stays the name of the practice, and the skill names follow the term as an inference
- (decision) the product kind is named "product behavior", one document per capability, because the kind is built-only description of what the product does and the name should carry its nature; "product requirements" was rejected because a requirement names intended behavior and the word pulls unbuilt must-statements back into the document, "product contract" was rejected because contract is a reserved word that must be qualified, and "product description" was rejected because it reads as prose about the product rather than as behavior statements a test can be written from; the sibling kind keeps the name "architecture description" as an inference
- (decision) everything a thread drafts for the project layer is a delta document naming one target file and one of three types, create (body is the whole new file), edit (body is literal add, replace and remove operations against the recorded blob hash) or delete (target and hash only), so a draft ADR is a create targeting docs/adr/ and a glossary change is an edit, because one primitive gives close-thread one landing algorithm and one baseline check for every kind; the primitives carry no kind rule, so an agent may draft an edit or delete against a landed ADR, with the method adding only a short note that this is generally poor practice, because a user who feels limited bypasses the method entirely and a typo fix must not need a workaround; an agent-facing rule that a landed ADR is never edited and a change of stance is a new superseding record was argued for, because Leitspace edited all 46 records in one day to track status, and rejected by the user on the freedom ground
- (decision) the two living kinds live at fixed paths, docs/product/<capability>.md and docs/architecture/<module>.md, created lazily like the rest of the project layer, and a thread stores its delta documents under delta/ mirroring the target path (delta/docs/product/feed.md, delta/docs/adr/<stem>.md), one delta document per target per thread, because every mechanism settled so far (the deny-list, delta targets, the read order) then works with no configuration; roles mapped per project through a registry file were rejected for now under YAGNI, since a later thread can add the mapping without breaking anything
- (decision) a settled point becomes an ADR only when three clauses all hold: a real alternative was argued against and is named, the reason for the choice cannot be read off the code, the product behavior or the architecture description, and a later thread could build against it incorrectly if not told, because the old two-clause test selected for durable and non-obvious without asking whether a decision was made, and with the living kinds in place the discussion only has to answer ADR-or-not per point; the seven-item and ten-item checklists of the audits were rejected because several items are now enforced by the kinds themselves and "costly to reverse" is a judgment no two agents make alike, with the user free to tighten the test later
- (decision) the ADR format makes a rejected-alternatives section mandatory, one line per alternative with the reason it lost, and keeps consequences optional, because a record with no alternative records no choice while a required consequences section teaches agents to fill templates; the record stays concise and limited to what a new reader needs, since the current instructions were found to produce verbose records with no gain; making both sections mandatory as Nygard and MADR do was rejected for the filler it produces on small records
- (decision) decisions get two containers: ADRs at docs/adr/ for how the system is structured or built, and product decision records (PDRs) at docs/pdr/ for what the product does or for whom, each with its own superseded/ folder, the same format and the same three-clause test, filed by the question the decision answers and never by how it is enforced, with a mixed point split into a product stance as PDR and a structural choice as ADR only where the latter passes the test alone, because the user judges two folders the right shape and merging them later is a move while splitting one later is a classification pass; one container for any subject was argued for, because a subject sort per point adds friction and mixed decisions have one home, and rejected on that reversibility ground; docs/product/decisions/ was rejected as the PDR path because it collides with a capability named decisions and breaks the symmetry with docs/adr/
- (decision) the change authoring drafts every delta document, ADRs and PDRs included, and the discussion writes only the thread log, superseding the earlier clause that kept ADR drafting in the discussion, because the log line already carries what a record needs and one drafter gives one review moment; keeping record drafting in the discussion for live confirmation of the text was rejected because it keeps two drafting moments and a filing step per record in the interview
- (decision) an ADR or PDR records a choice in the present tense and carries no implementation state, whether built is read from the code and the roadmap and whether current is read from the folder, because a record that tracks reality is a record that gets edited, which is what happened to all 46 Leitspace records; a lifecycle field on the record (proposed, accepted, superseded) was rejected because a field that must be updated is a field that goes stale, and the premise that a record describes what currently exists was dropped since what exists now has its own homes in architecture description and product behavior
- (decision) the change document carries no FR or AC identifiers: acceptance criteria are a flat checklist of behavior statements, and a plan task, a report row or a review finding references a criterion by quoting it verbatim, because an identifier is a handle that gets used wherever it is handy and a thread-local id read outside the thread looks like a permanent requirement, while a test named after the quoted behavior is the test name both audits want; keeping the ids with a thread-local banner and a write boundary was rejected because the banner is a sentence agents skim and the two-level FR-to-AC hierarchy was mostly repetition, with the verbatim-quoting cost on large changes accepted
- (decision) the implementation report carries a mandatory acceptance section with one row per criterion, the criterion quoted verbatim, the verification method (automated test, manual check or code review) and the evidence (test name and file, or what was walked through and observed), and a criterion without a row fails the review, because traceability had no named home and the ids went into the nearest durable surface, the test names; leaving verification to prose with an optional table was rejected because somewhere in the report is the same nowhere that produced the leak
- (decision) a commit message explains the change concisely in its own words, may name the thread path once as provenance, and never carries a task number, a criterion, a progress block or a thread artifact as the explanation, because a commit is read for archaeology, which is what a thread is for, while code comments and test names are read as current; a total ban on thread references in commits was rejected because git already stores the thread folder and the ban would protect nothing, and the current verbose progress block in commit bodies is dropped because it propagated task vocabulary into history with no gain
- (decision) review-implementation searches the delivered code, comments, test names and migrations for thread paths and references to thread artifacts and reports each hit as a finding, close-thread runs the same search over the repository as a pre-write check and asks on a hit, and the method publishes the one-line search recipe for a project that wants it as a hard gate, with the pattern fixed in a shared instruction rather than improvised, because the check belongs where the reading already happens and the hard gate is the project's choice; conventions alone were rejected because the Leitspace cleanup would recur by hand, and a shipped six-check tool was rejected because half its checks target metadata the method no longer has
- (decision) this thread leaves the method silent on what a project AGENTS.md holds and gives guidelines no method-owned home, deferring rules, guidelines and the marking of normative strength to a thread of their own once this rework has been implemented, because the user wants this thread bounded; a method-owned handbook kind at docs/handbook/<module>.md with AGENTS.md reduced to rules and routing was argued for on the ground that every sentence outliving a thread would then have one kind, one path, one drafter and one landing, and was deferred rather than rejected
- (decision) every entry-point skill lists its project-layer reads by path in its own Inputs, in the fixed order (the project AGENTS.md, docs/glossary.md, the architecture description of the touched module, the product behavior of the touched capability, the decision folders opening what touches the work, the roadmap entry when the thread was opened from one, then the thread), and two model-invoked skills remain, consult-decisions for docs/adr/ and docs/pdr/ and consult-descriptions for docs/product/ and docs/architecture/, each carrying the listing command that avoids loading every file plus the citation form and how the kind binds, because a folder must be listed rather than read whole while a single file needs no skill; consult-glossary is retired because the agent simply reads docs/glossary.md, consult-adrs is replaced by consult-decisions, and one routing skill printing the whole order was rejected because a long printout gets skimmed and a single routing skill is a single point of failure
- (decision) the roadmap index stays under .work/roadmaps/, because .work/ holds work, current while open and historical or gone after, and a roadmap is planned work exactly as an open thread is, while docs/ holds standing truth about the system; the citation rule is phrased by direction, nothing under .work/ is cited from docs/ or from code, a decision is cited by stem for the reason behind a choice and never for what the system does, a product behavior or architecture description by path and heading, a roadmap entry by index path and entry slug from thread artifacts only, and a thread path appears outside its thread only as commit provenance; moving the index to docs/roadmaps/ was argued for on the ground that authority belongs under docs/ and rejected because authority is not the property that decides the folder
- (decision) the method carries no signal about decision-record volume, no per-thread flag and no portfolio count, because every project is different and how many records it holds is the user's judgment; count-based flags (more than three drafts in a thread, records against closed roadmap entries) were rejected because a threshold becomes a target and the structural bounds settled in this thread act on the cause rather than the symptom
- (decision) this repository adopts the reworked method as written, with the admission rules applied honestly: docs/adr/ and docs/pdr/ are created lazily by the first thread whose decision passes the test, sorted here as "how the suite is built" for ADR and "what the method does for its users" for PDR; docs/product/ holds one method-level overview (how the method works, how the skills relate, what each produces, the thread shape cited from its format) absorbing the method-level content of docs/working-with-threads.md and README.md, which keeps install and index; docs/architecture/ appears only for what no single file makes readable, the shared-reference manifest and sync being the one expected candidate; suite/authoring/ is untouched because its files are rules and guidelines for skill authors and belong to the deferred rules thread, and docs/documentation-rules.md stays a maintainer document edited only where its citation rule changed, because the reference project must not leave its own rationale in historical logs while manufacturing description that the suite bodies already are; not adopting was rejected because it keeps the method violating its own citation rule, and relocating suite/authoring into the descriptive kinds was rejected because rules filed as behavior pre-empt the deferred decision
- (event) spec authored from the live conversation of the discussion session
- (decision) the write-boundary map in suite/authoring/side-effects.md is brought to the reworked model in this thread, including its lines that name no retired skill (the discussion and resolve-pending-decisions drafts bullet, the implement bullet, the close-thread bullet and the paragraph below the map), because the map describes what the skills write rather than stating a rule for skill authors, so correcting it does not pre-empt the deferred rules thread while leaving it would ship a map contradicting the skill bodies that body-structure.md says it fixes; holding suite/authoring/ to the letter of "untouched" and recording the stale lines as a follow-up was rejected because the suite would then describe its own skills falsely until that thread runs
- (event) thread closed; delta: landed
