# Seed: Modular Agentic Workflow V2

External: none (workflow-infrastructure work, no tracker ticket)

Two consultation sessions held inside the `appaltiav2` workspace (2026-06-12) produced
a critique of the V1 thread layout and a broader workflow review, building on the
maintainer's accumulated TODO list (`temp/my-workflow-skills/TODO.md` in that
workspace) and on the live evidence of the `appaltiav2` v2-design thread — the
largest real V1-adjacent thread to date, where several V1 rules were knowingly and
correctly violated (in-place spec revisions, a `reviews/` folder, no inbox).

This thread exists to carry the resulting proposal for Workflow V2 through review,
discussion, and adoption.

> Note: this seed and this thread's layout already follow the V2 conventions the
> proposal defines (dogfooding). If the proposal is rejected, the thread is
> trivially re-shaped to V1.
