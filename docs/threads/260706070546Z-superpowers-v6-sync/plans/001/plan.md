# Plan: Superpowers v6 sync — multi-file strict plans and hardened plan execution

## Goal

Implement `specs/001/spec.md` (approved): make `plan-strict` emit a multi-file plan artifact (index + one file per task), teach `review-plan` the new format plus its mechanical checks, harden `implement-plan-with-subagents` (merged reviewer dispatch, deterministic scratch, durable run ledger, mechanical pre-flight, reviewer-freedom rules), carry the applicable subset into `implement-plan`, deprecate `plan-loose` and `adjust-plan-granularity`, and update all registrations and V2 workflow docs.

Upstream input: `specs/001/spec.md` (spec, `status.approved: 260706142220Z`). The spec compiles the decision logs at `seed/discussions/260706072854Z-superpowers-v6-adoption-decision-log.md` and `specs/001/discussions/260706141308Z-review-findings-disposition-decision-log.md`; tasks below cite the spec's sections (§) and acceptance criteria (AC) as the operative references.

## Plan-level constraints (apply to every task)

Copied from `specs/001/spec.md ## Constraints`:

- **Skill self-containment:** no skill may reference another skill by name, command, or path; natural-language awareness only.
- **Harness-agnosticism:** no model names, no model-selection guidance, no assumption that a per-dispatch model parameter exists — in any touched file.
- **Lossless behavior surface:** everything in the current skills not named in the spec stays behaviorally unchanged (four-state protocol, claim-vs-verified gating, commit policy incl. no-history-rewriting, dirty-worktree handling, immutability rules, implementation-report shape and follow-up routing, no-worktree rule, tier awareness). Where this clause and an enumerated spec change conflict, the enumerated change wins.
- **Version bumps:** the four modified skills get a major version bump (`3.0.0` → `4.0.0`); deprecated skills' versions are untouched.
- **Frontmatter descriptions** stay one sentence (what + when).
- **No commits by this implementation as a spec requirement** — the executing skill's own commit cadence applies; nothing in these tasks requires an extra commit step.
- The decision logs, the seed, and the thread ledger are records — never edited.

Repo-wide invariant used by several verification blocks: the spec's shorthand `plans-NNN` denotes the scratch folder mirroring the **full** lineage folder name (`plans/001-cli/` → `plans-001-cli`).

## Tasks

### Task 1: Rewrite `plan-strict` to emit the multi-file plan format

**Objective:** Make `plan-strict` produce a plan lineage of `plan.md` (index) plus `tasks/NN-<kebab-slug>.md` task files that are directly dispatchable briefs, with the Task Right-Sizing rubric folded into self-review and all loose-granularity references removed.

**Input / context:** `specs/001/spec.md` §1 (artifact format), §2 (`plan-strict` changes), AC-1.1–AC-1.6. Current file: `skills/workflow/plan/plan-strict/SKILL.md` (182 lines, version 3.0.0). Loose references currently sit in the Inputs form-2 sentence, the Plan Artifact Contract closing sentence, the Strict Plan Body Shape intro, and self-review check 2. Degrees of freedom granted by the spec: all exact prose/section naming (DoF-1), Consumes/Produces placement within the task-file field order and index section ordering (DoF-3), index task-list entry formatting (DoF-4), kebab slugs (DoF-5), granularity-fit remedy rewording (DoF-6).

**Steps:**
1. In the frontmatter, bump `version` to `4.0.0`. Keep the description one sentence; update it to reflect the multi-file output (e.g. "…into a strict-granularity multi-file plan (an index plus one dispatchable brief per task)…") without naming any other skill.
2. Rewrite `## Lineage Folder and Filename` to define the lineage layout: `plans/NNN[-<desc>]/plan.md` (the index) plus `plans/NNN[-<desc>]/tasks/NN-<kebab-slug>.md` — two-digit zero-padded ordinal, one file per task, kebab slug chosen at authoring time. State that the whole folder (index + task files) is the plan artifact, created on-demand.
3. Replace the single-body `## Strict Plan Body Shape` with two subsections. **Index (`plan.md`)** must mandate: (a) plan-level objective and context; (b) a `Source:` line with exactly four legal value forms — a thread-relative pointer to the upstream artifact, a repo-relative path for a cross-thread artifact, an issue URL, or `none — raw prompt`; (c) a **Global Constraints** block whose entries are copied **verbatim** from the source artifact's stated constraints, one line each, with an explicit "the source states no constraints" line (never an omitted block) when the source states none or there is no source; (d) an **ordered task list** carrying number, title, one-line objective, and a relative pointer to each task file.
4. In the same section, define **task files**: each carries the six existing mandatory fields (Objective; Input / context; Steps / substeps; Files modified; Verification; Acceptance criteria) **plus** two hand-off lines — `Consumes:` (exact things this task uses from earlier tasks) and `Produces:` (exact things later tasks rely on) — with `none` an explicit legal value for either, no "Interfaces" umbrella heading, and the **precision-not-notation** rule stated with notation-native examples: a function in the target language's own signature style, a CLI invocation as the literal command line, an HTTP endpoint as method + path, a file as path + format; natural-language entries are fine as long as a later implementer could use the named thing without guessing its name or shape.
5. State the invariants: no YAML frontmatter and no status markers anywhere in the plan folder (execution state lives in commits and the implementation report; the index never needs updating mid-run); the index is authoritative for task count and order and consumers must flag index↔folder mismatches; task ordering is implicit in the numbering; alive-in-place editing and git-as-history apply to the whole folder; the no-parallelization contract is unchanged and applies to index and task files alike.
6. Rewrite `### Worked Example` to show one complete task file (the eight labeled elements including `Consumes:`/`Produces:`) and a short index excerpt (`Source:` line, Global Constraints block, one task-list entry). Keep the example a negative test of the no-parallelization rule.
7. In `## Self-Review`, fold the **Task Right-Sizing** rubric into checks 3 and 4: a task is the smallest unit that carries its own test cycle and is worth a fresh reviewer's gate; setup, configuration, scaffolding, and documentation steps fold into the task whose deliverable needs them; split only where a reviewer could meaningfully reject one task while approving its neighbor. Reword check 2 (granularity fit) so its remedy no longer recommends a loose plan — e.g. when the input is too thin to earn the per-task structure, the remedy is to merge tasks, shrink the plan, or push back on planning at all — introducing no new mechanism.
8. Update `## Workflow` steps 5–7 to draft the index plus one task file per task, run self-review across the whole folder, and write `plan.md` and `tasks/` together; the confirmation line stays index-path-shaped (`plans/NNN/plan.md`).
9. Remove every remaining loose-granularity reference (Inputs form 2, Plan Artifact Contract, body-shape intro) — the strict format is now the only plan format this skill knows.

**Files modified:** `skills/workflow/plan/plan-strict/SKILL.md`

**Verification:** `grep -ri "loose" skills/workflow/plan/plan-strict/` returns nothing; `grep -c "version: 4.0.0" skills/workflow/plan/plan-strict/SKILL.md` returns 1; `grep -n "Source:" skills/workflow/plan/plan-strict/SKILL.md`, `grep -n "Consumes:" …`, `grep -n "Produces:" …`, `grep -n "Global Constraints" …`, and `grep -n "tasks/" …` all return hits; `grep -n "Interfaces" skills/workflow/plan/plan-strict/SKILL.md` returns hits only in the prose forbidding the umbrella heading (or none); `grep -in "wave\|depends_on\|fork/join" skills/workflow/plan/plan-strict/SKILL.md` returns only the No Parallelization prohibition text.

**Acceptance criteria:**
- AC-1.1: lineage layout specified as index + `tasks/NN-<kebab-slug>.md`, two-digit ordinals, one file per task.
- AC-1.2: index mandates objective/context, `Source:` with the four legal forms, verbatim Global Constraints block with explicit-none statement, ordered task list with number/title/objective/pointer.
- AC-1.3: task-file spec mandates six fields + `Consumes:`/`Produces:` with `none` legal, no "Interfaces" heading, precision-not-notation rule with notation-native examples.
- AC-1.4: no-frontmatter/no-status-markers, index-authoritative, alive-in-place-whole-folder, and no-parallelization invariants stated.
- AC-1.5: no loose-granularity references remain in the skill folder.
- AC-1.6: self-review contains the Task Right-Sizing rubric.
- Frontmatter carries `version: 4.0.0` and a one-sentence description.

### Task 2: Rewrite `review-plan` for the multi-file format and new mechanical checks

**Objective:** Teach `review-plan` to read the index first and task files one at a time, verify the structural and cross-task invariants of the new format, apply the Task Right-Sizing rubric, and check the Global Constraints block verbatim against the `Source:` artifact — with all loose-granularity branches removed.

**Input / context:** `specs/001/spec.md` §3, AC-2.1–AC-2.7. Current file: `skills/workflow/review/review-plan/SKILL.md` (200 lines, version 3.0.0). Task 1 has already redefined the format this skill checks (`skills/workflow/plan/plan-strict/SKILL.md` describes the emitted shape); this task must define the same shape independently in its own words (skills are self-contained — no cross-skill reference). The adherence-review contract (four outcomes, auto-fix plan faults, route spec faults to the human, review-record shape, disposition frontmatter) stays unchanged per the spec's lossless clause.

**Steps:**
1. Bump `version` to `4.0.0`; keep the description one sentence (it needs no substantive change — the adherence contract is unchanged).
2. Update `## Inputs` to describe the plan artifact as the lineage folder holding `plan.md` (the index) plus `tasks/NN-<kebab-slug>.md` task files; accepting the folder path or the index path resolves to the same artifact. Remove the loose-plans-accepted sentence.
3. Delete `## Loose vs Strict Detection` entirely. Replace it with the valid-task definition: a valid strict task file carries the six mandatory fields plus the `Consumes:`/`Produces:` hand-off lines (the spec's "seven-element shape", counting the hand-off pair together); a task file lacking either `Consumes:` or `Produces:` is flagged as a finding.
4. Add the reading order to the workflow: read the index first, then task files one at a time.
5. Add the **structural check** (mechanical): every index task-list entry resolves to an existing task file; every file under `tasks/` is listed in the index; ordinals are contiguous and match the filenames. Classify failures as plan-authoring faults (outcome 2, auto-fix), consistent with the existing mixed-body precedent.
6. Add the **cross-task check** (mechanical): every non-`none` `Consumes:` entry must correspond to some earlier task's `Produces:` entry (a dangling `Consumes:` is a finding); a `Produces:` entry nothing later consumes is flagged as a smell, not an error — a final task legitimately produces for the world outside the plan.
7. Add the **Global Constraints check**: resolve the artifact named by the index's `Source:` line and verify the Global Constraints block's entries match the source's stated constraints verbatim; flag drift (stale, missing, or altered entries). When `Source:` is `none — raw prompt`, the block must carry the explicit no-constraints statement.
8. Add the **Task Right-Sizing rubric** as a checking rubric, worded as in Task 1 step 7 (smallest unit with its own test cycle, worth a fresh reviewer's gate; setup/scaffolding/docs fold into the deliverable's task; split only where a reviewer could reject one task while approving its neighbor).
9. Make the per-task ambiguity check unconditionally mandatory (drop the granularity-conditional framing and the loose branch of `### Per-task ambiguity`); extend its field list with the two hand-off lines (each entry precise enough to use without guessing).
10. Sweep the rest of the body (four outcomes, auto-fix loop, record sections, workflow steps) for residual single-file or loose-granularity phrasing and align it with the index/tasks framing.

**Files modified:** `skills/workflow/review/review-plan/SKILL.md`

**Verification:** `grep -ri "loose" skills/workflow/review/review-plan/` returns nothing; `grep -c "version: 4.0.0" skills/workflow/review/review-plan/SKILL.md` returns 1; `grep -n "Consumes:" skills/workflow/review/review-plan/SKILL.md` and `grep -n "Produces:" …` return hits; `grep -n "Global Constraints" …` returns hits; `grep -in "index" …` shows the index-first reading order and the structural check; `grep -n "Right-Sizing\|right-sizing" …` returns hits.

**Acceptance criteria:**
- AC-2.1: index-first, then task files one at a time.
- AC-2.2: the three structural checks mandated.
- AC-2.3: cross-task `Consumes:`/`Produces:` check with dangling-`Produces:`-is-a-smell rule.
- AC-2.4: Task Right-Sizing rubric present as a checking rubric.
- AC-2.5: no loose-granularity branches remain.
- AC-2.6: valid-task definition is six fields + both hand-off lines; a missing `Consumes:` or `Produces:` is a flagged finding.
- AC-2.7: Global Constraints block verified verbatim against the `Source:` artifact, drift flagged.
- Four-outcome adherence contract, auto-fix loop, and review-record shape behaviorally unchanged.

### Task 3: Rewrite `implement-plan-with-subagents` orchestration

**Objective:** Convert the orchestrator to the multi-file plan format with a mechanical pre-flight and `Source:` read, replace the two sequential review passes with one merged reviewer dispatch returning two lane verdicts, move scratch to deterministic `.wip/implement/plans-NNN/` paths with a durable `progress.md` run ledger, add the reply cap and brief-construction rules, and reword the deviation-policy passages — removing all loose-granularity handling.

**Input / context:** `specs/001/spec.md` §4, AC-3.1–AC-3.5, AC-4.1–AC-4.7, AC-5.1–AC-5.3, AC-6.1–AC-6.5. Current file: `skills/workflow/implement/implement-plan-with-subagents/SKILL.md` (263 lines, version 3.0.0). The two deviation passages to reword sit at the current SKILL.md:236 and :242. The method files under `references/` are rewritten in Task 4; this task only changes how the SKILL.md briefs load and dispatch them (both paths in ONE reviewer brief). Preserved unchanged per the lossless clause: subagent-capability precondition, no-worktree rule, dirty-worktree handling, four-state claim-vs-verified protocol, empty-diff and terminal-claim gating, commit policy (cadence, baseline gate, failed commit, no history rewriting), implementation-report shape and follow-up routing.

**Steps:**
1. Bump `version` to `4.0.0`. Update the one-sentence description: "dual-reviewer subagent loop" becomes the merged single-reviewer-two-lane-verdicts model (e.g. "…through an implementer and a merged two-lane reviewer subagent loop…").
2. Update the intro paragraph and `## Subagent Capability Precondition` for the two subagent roles (implementer; merged reviewer) — remove "dual-reviewer separation" phrasing in favor of the two-lane framing.
3. Rewrite `## Inputs` for the multi-file plan: the lineage folder holds `plan.md` (the index — authoritative for task count and order) plus `tasks/NN-<kebab-slug>.md` files carrying the per-task fields including `Consumes:`/`Produces:`. Remove the loose/strict tolerance paragraph. A plan not matching this shape fails the pre-flight (step 4 below) — the remedy is recompiling the plan from its source, not tolerating the old shape.
4. In `## Workflow`, after plan resolution and before any dispatch, insert the **mechanical pre-flight**: (a) every index entry resolves to an existing task file, (b) every task file is listed in the index, (c) ordinals contiguous and matching filenames. On failure, halt with a clear malformed-artifact message *before any dispatch* — this is not reported as a `BLOCKED` task.
5. Replace workflow step 4 (read the whole plan) with the **source read**: the orchestrator reads the index AND the artifact named by its `Source:` line when one exists (the orchestrator holds the intent; subagents hold the mechanics). Task files are read lazily — a task's `Files modified` list is read at staging time; a judgment call may open a task file on demand; no step requires reading all task files up front.
6. Rewrite the orchestration cycle: **implementer dispatch** passes the task file path + the index path (+ the findings-file path on fix-loop respawns) and states that nothing else *from the plan* is to be read; the brief affirmatively grants on-demand reading of the index's `Source:` artifact when the task file and index leave a question open.
7. Replace review steps b–e with the **merged review dispatch**: ONE reviewer subagent per review pass, whose brief loads BOTH method-file paths (`references/plan-compliance-reviewer.md` and `references/code-quality-reviewer.md`, resolved relative to this skill's directory) and passes the task file path + index path. The reviewer inspects the diff itself and returns BOTH verdicts separately — plan-compliance and code-quality, each named per lane, each one of `PASS`/`ISSUES`/`BLOCKED`/`NEEDS_CONTEXT`, each judged strictly within its own lane; a report missing either verdict is not accepted. A task passes only when both verdicts are clean.
8. Rewrite the **fix loop**: if either verdict is `ISSUES`, respawn a fresh implementer with the findings file, then a fresh merged reviewer returning both verdicts (the diff changed). Fix iterations are tracked **per verdict lane**. Convergence and `BLOCKED` rules unchanged. In `## Commit Policy`, the baseline-gate fix re-runs the **merged reviewer** (remove "re-run the code-quality review pass (step e)").
9. In `## Commit Policy` staging, make the task's `Files modified` list unconditionally authoritative; delete the loose-plan "track touched files" fallback (both occurrences — Workflow step f and the Commit Policy cadence bullet).
10. Replace the scratch-path convention everywhere (briefs, workflow, immutability): scratch lives at `docs/threads/<thread>/.wip/implement/plans-NNN[-<desc>]/` (the folder name mirrors the FULL lineage folder name, e.g. `plans/001-cli/` → `plans-001-cli`), containing `progress.md` plus `task-NN/` folders (NN = the plan's task ordinal, matching `tasks/NN-<slug>.md`) holding `SS-implementer-outcome.md` / `SS-review.md` files. `SS` is a two-digit dispatch ordinal within the task, shared across roles, assigned by the orchestrator at dispatch time so every brief carries exact pre-computed paths; on a resumed run the next `SS` is max-existing + 1; gaps are normal (files are conditional) — never renumber existing files. Every dispatch writes to a NEW file, write-once — never overwrite or append a prior dispatch's file. Files remain conditional: the implementer writes its outcome file only when there is diff-blind content; the reviewer writes ONE `SS-review.md` per review dispatch (containing both lanes' sections) only when there is content; a no-findings clean pass writes no file. Remove the old `<UTC>-task-<N>-…` stamp-named scratch convention.
11. Add the **reply cap**: implementer and reviewer replies use a fixed short return shape — status/verdict token(s), files touched, one-line verification result, concerns flag, scratch-file path if one was written — under a hard ceiling of 15 lines stated in the skill.
12. Add the **run progress ledger** (cycle-gated, not commit-gated — spec disposition log P7): append that task's report block to `.wip/implement/plans-NNN/progress.md` — append-only, one block per **attempted** task; a cycle that reaches a verified outcome appends exactly one block: committed cycles immediately after the commit, empty-diff `DONE` / `BLOCKED` / `NEEDS_CONTEXT` cycles with `Commit: none` before advancing or stopping (a pre-flight halt appends nothing — no cycle started). Block content: claimed status vs verified verdict, dispatch counts, fix iterations per verdict lane, deviations, unresolved concerns verbatim (resolved findings counted, never restated), the commit SHA + subject for committed cycles (else `Commit: none`), and brief notes. The commit message body keeps carrying the block for committed cycles (omitting the SHA of the commit itself). Chat shrinks to one line per task (e.g. `Task 04: verified DONE, commit abc1234`). The implementation report is folded from the ledger file re-read from disk at the end; name the ledger + `git log` as the compaction-recovery resume state, never conversation recollection. Update the task-report-block shape in `## Four-State Status Protocol` to the merged-reviewer audit (one reviewer line, fix iterations per lane) and reconcile it with the ledger block.
13. Add to `## Subagent Briefs` the **brief-construction rules**: a reviewer brief must never pre-rate a finding's severity, exclude categories of findings, or declare a question settled — listing the red-flag phrasings as examples ("do not flag…", "at most minor…", "already decided / the plan chose…") — and cite the existing unclassified assumption-injection as the pattern to follow. Also state that because `.wip/` is disposable, no durable artifact (commit message, implementation report) may point into it — anything a durable record needs is copied into that record.
14. Reword the two deviation passages (currently SKILL.md:236 and :242): plan revision happens upstream — a spec fault means fix the spec and recompile the plan; a plan fault means revise the living plan by re-running planning or editing it in place under the Plan Artifact Contract. Remove "the user re-shapes the plan in a separate plan-adjustment pass"; name no skill.
15. Add the **awareness sentence** in or near `## Implementation Report`: per-task reviews are deliberately task-scoped gates and the implementation as a whole is expected to receive a broader review afterward — naming no skill.
16. Update the thread folder-set tree in `## Immutability` (plans lineage shows `plan.md` + `tasks/`; `.wip/` shows the `implement/plans-NNN/` layout) and sweep the whole body for residual loose-granularity or single-file-plan phrasing.

**Files modified:** `skills/workflow/implement/implement-plan-with-subagents/SKILL.md`

**Verification:** `grep -ni "loose" skills/workflow/implement/implement-plan-with-subagents/SKILL.md` returns nothing; `grep -n "plan-adjustment\|re-shapes the plan" …` returns nothing; `grep -n "progress.md" …`, `grep -n "plans-NNN" …`, and `grep -in "pre-flight" …` return hits; `grep -in "FIRST review pass\|SECOND review pass" …` returns nothing; `grep -c "version: 4.0.0" …` returns 1; `grep -ri "model" skills/workflow/implement/implement-plan-with-subagents/SKILL.md` — adjudicate every hit against AC-6.4 (no model-selection content; innocuous uses pass); `grep -n "15" …` shows the reply-cap ceiling.

**Acceptance criteria:**
- AC-3.1–AC-3.5: one merged reviewer dispatch per pass loading both method files; both named lane verdicts required; commit only when both clean with per-lane fix tracking; baseline-gate fix re-runs the merged reviewer; awareness sentence present naming no skill.
- AC-4.1–AC-4.7: brief contents (task file + index + findings-on-fix; nothing else from the plan; `Source:` grant); scratch paths per the mirrored `plans-NNN[-<desc>]/task-NN/SS-*` scheme with orchestrator-assigned shared `SS` and max+1 resume; write-once conditional files with one two-lane review file; fixed reply shape under a stated 15-line ceiling; no-durable-pointer-into-`.wip/` rule; brief-construction prohibitions with red-flag phrasings citing the unclassified-injection pattern.
- AC-5.1–AC-5.3: append-only `progress.md` block per attempted task with the enumerated content (cycle-gated, not commit-gated — committed cycles after the commit, empty-diff `DONE`/`BLOCKED`/`NEEDS_CONTEXT` cycles with `Commit: none`; spec disposition log P7); commit body carries the block for committed cycles; one chat line per task; report folded from the ledger re-read from disk; ledger + `git log` named as compaction recovery.
- AC-6.1–AC-6.5: pre-flight halts before dispatch with malformed-artifact (not `BLOCKED`) semantics; index + `Source:` read at startup with lazy task files; deviation passages reworded; no model-selection content; no loose-granularity handling and `Files modified` unconditionally authoritative.
- Frontmatter carries `version: 4.0.0`; preserved behaviors listed in Input / context are textually intact.

### Task 4: Rewrite the two reviewer method files for the merged dispatch

**Objective:** Reframe both method files from sequential passes to one dispatch producing two independent lane verdicts, and add the read-permission, unverified-concern, positive-focus (out-of-task ban dropped), and plan-mandated-label rules — with output templates matching the single two-lane `SS-review.md`.

**Input / context:** `specs/001/spec.md` §5, AC-7.1–AC-7.6. Current files: `skills/workflow/implement/implement-plan-with-subagents/references/plan-compliance-reviewer.md` (130 lines) and `references/code-quality-reviewer.md` (127 lines). Both stay separate files, both loaded by the single merged reviewer whose dispatch Task 3 defined. Sequential-pass framing to remove: plan-compliance "You are the FIRST review pass"; code-quality "You are the SECOND review pass — the plan-compliance reviewer ran first and … has already PASSED", "The first pass already passed; you trust that verdict", and the boundary-finding paragraphs resting on that ordering. Out-of-task bans to replace: plan-compliance Hard Constraints "DO NOT make findings outside the current plan task"; code-quality Hard Constraints "DO NOT make findings outside the current diff". Method files carry no version frontmatter — no bump applies.

**Steps:**
1. In both files, rewrite the header note and `## Focus Area` for the merged model: one reviewer dispatch loads both method files and produces two lane verdicts — this file defines ONE lane; each lane is judged independently, strictly within its own scope, with no cross-lane trust; a diff may fail plan-compliance and still receive code-quality findings in the same report. Remove all first/second-pass ordering and trust-the-other-verdict language (including code-quality's "What Code-Quality Is NOT" bullet resting on "the first pass already passed" and plan-compliance's "second pass" pointers — lane-scope statements stay, re-anchored to the lane split rather than dispatch order).
2. In both files, add the **read permission**: the diff is the review *target*; the repo is readable *context* — reading unchanged code to verify a criterion is in-scope and expected. Reconcile the existing "DO NOT run tests beyond the prescribed verification" constraint (plan-compliance) with this rule — reading is unrestricted; test execution stays bounded as today.
3. In both files, define the **unverified-concern rule**: a criterion unverifiable from within the run (external config, runtime-only behavior, credentials nobody has) is recorded as a named "unverified" concern — non-blocking by default, riding into `DONE_WITH_CONCERNS` — with `NEEDS_CONTEXT` reserved for when proceeding without the answer would be reckless (reviewer's judgment).
4. In both files, drop the out-of-task ban and replace it with **positive framing**: a plan is being implemented in task batches and this lane's focus is the current task's diff, with no prohibition on reporting other discoveries. Out-of-task observations go in a distinct section of the findings file, never drive the current task's fix loop or blocking classification (blocking stays defined against the current task); the orchestrator routes them through the existing concern/follow-up mechanisms.
5. In both files, define the **plan-mandated** finding label: a finding that traces to the plan's own text (the diff faithfully implements something defective the plan mandated) is labeled *plan-mandated*; the orchestrator routes it through the existing plan-fault path (deviation policy — `DONE_WITH_CONCERNS`/`NEEDS_CONTEXT` per severity), never silently accepting it and never patching the plan mid-run.
6. Remove loose-granularity inference branches (plan-compliance: the loose alternatives in "What Plan-Compliance Is" and Process step 4; sweep code-quality for any) and update plan references to the multi-file shape: the reviewer reads the task file (six fields + `Consumes:`/`Produces:`) and the index (Global Constraints, `Source:`) named in its brief, both READ-ONLY.
7. Update both `## Output Template` sections for the merged single review file: ONE `SS-review.md` per review dispatch, containing one section per lane; each file's template becomes its lane's section (lane-named verdict line, findings/concerns with the blocking/non-blocking and *plan-mandated* labels, the distinct out-of-task observations section, references). Keep the write-only-when-content rule: both lanes clean → no file.
8. Update both `## Verdict` and reply-contract passages: the merged reviewer's reply carries BOTH lane verdicts on every dispatch; each lane's verdict vocabulary stays `PASS`/`ISSUES`/`BLOCKED`/`NEEDS_CONTEXT` with the existing blocking-by-default classification and can't-assess escape semantics.

**Files modified:** `skills/workflow/implement/implement-plan-with-subagents/references/plan-compliance-reviewer.md`, `skills/workflow/implement/implement-plan-with-subagents/references/code-quality-reviewer.md`

**Verification:** `grep -rin "first pass\|second pass\|FIRST review\|SECOND review\|already passed\|trust that verdict\|ran first" skills/workflow/implement/implement-plan-with-subagents/references/` returns nothing; `grep -rin "loose" skills/workflow/implement/implement-plan-with-subagents/references/` returns nothing; `grep -rn "DO NOT make findings outside" …/references/` returns nothing; `grep -rln "plan-mandated" …/references/` lists both files; `grep -rlin "unverified" …/references/` lists both files; `grep -rn "out-of-task\|Out-of-task" …/references/` returns hits in both files' templates/framing.

**Acceptance criteria:**
- AC-7.1: both files state diff-is-target / repo-is-readable-context.
- AC-7.2: both define the "unverified" concern with `DONE_WITH_CONCERNS` default and reckless-only `NEEDS_CONTEXT`.
- AC-7.3: no out-of-task ban in either file; task-batch focus framing plus a distinct out-of-task template section with the never-drives-fix-loop/blocking rule.
- AC-7.4: both define the *plan-mandated* label and its deviation-policy routing.
- AC-7.5: no loose branches; templates match the single merged review file with two lane sections.
- AC-7.6: no sequential-pass or cross-lane-trust framing; one dispatch, two independently judged lane verdicts.

### Task 5: Carry the applicable changes into `implement-plan` (single-agent)

**Objective:** Give the single-agent implement skill the multi-file plan handling, mechanical pre-flight, `Source:` read, run ledger, deviation-policy rewording, adapted self-review rules, and awareness sentence — without any subagent machinery — and remove all loose-granularity handling.

**Input / context:** `specs/001/spec.md` §6, AC-8.1–AC-8.5. Current file: `skills/workflow/implement/implement-plan/SKILL.md` (163 lines, version 3.0.0). The deviation passages to reword sit at the current SKILL.md:155 and :161. Excluded per spec: merged-reviewer machinery, dispatch scratch files (only `progress.md` lives under the `.wip/implement/plans-NNN/` layout), brief-construction rules, model-selection content. Preserved unchanged per the lossless clause: single-agent topology, four-state protocol, dirty-worktree handling, commit policy, implementation-report shape and follow-up routing.

**Steps:**
1. Bump `version` to `4.0.0`; keep the description one sentence (no granularity phrasing exists in it; adjust only if the multi-file mention improves the trigger).
2. Rewrite `## Inputs` for the multi-file plan (index authoritative for task count/order; `tasks/NN-<kebab-slug>.md` per task with the six fields + `Consumes:`/`Produces:`); remove both loose/strict tolerance paragraphs. A plan not matching this shape fails the pre-flight; the remedy is recompiling the plan from its source.
3. In `## Workflow`, insert the **mechanical pre-flight** after plan resolution (same three checks as the orchestrator skill: index entries resolve, all task files listed, ordinals contiguous and matching filenames; halt with a malformed-artifact message — not a `BLOCKED` task report). Then the **startup read**: read the index and the artifact named by its `Source:` line when not `none`. The session itself reads each task file as it reaches that task (it is the implementer — no dispatch indirection).
4. Update step 5a (implement) to execute the task file's substeps literally; remove the infer-loose branch. Update the commit staging rule: the task's `Files modified` list is unconditionally authoritative; delete the loose track-touched-files fallback.
5. Extend step 5b (self-review) with the adapted reviewer rules: classify a criterion unverifiable from within the run (external config, runtime-only behavior, credentials nobody has) as a named "unverified" concern — non-blocking by default, riding into `DONE_WITH_CONCERNS`, with `NEEDS_CONTEXT` only when proceeding would be reckless; frame review focus positively — the current task's diff is the focus, discoveries outside it are recorded (task report / implementation report follow-ups) without driving this task's status, with no out-of-task prohibition.
6. Add the **run ledger** (cycle-gated, not commit-gated — spec disposition log P7): append the task's report block to `docs/threads/<thread>/.wip/implement/plans-NNN[-<desc>]/progress.md` (folder name mirrors the full lineage folder name) — append-only, one block per **attempted** task; a cycle that reaches a verified outcome appends exactly one block: committed cycles after the commit, empty-diff `DONE` / `BLOCKED` / `NEEDS_CONTEXT` cycles with `Commit: none` before advancing or stopping (a pre-flight halt appends nothing). Block: status (claimed = verified in this topology, so a single verified status line), deviations, unresolved concerns verbatim, the commit SHA + subject for committed cycles (else `Commit: none`), brief notes — WITHOUT subagent-audit lines (nothing is dispatched). Commit body keeps carrying the block for committed cycles; chat shrinks to one line per task; the implementation report is folded from the ledger re-read from disk; name the ledger + `git log` as compaction-recovery state. State that no durable artifact may point into `.wip/`.
7. Reword the two deviation passages (SKILL.md:155 and :161) identically in substance to Task 3 step 14: spec fault → fix the spec and recompile the plan; plan fault → revise the living plan by re-running planning or editing it in place; no "plan-adjustment pass", no skill named.
8. Add the **awareness sentence** in or near `## Implementation Report`: the per-task self-review is a task-scoped gate; the implementation as a whole is expected to receive a broader review afterward — naming no skill.
9. Sweep the body for residual loose or single-file-plan phrasing (Four-State `DONE` definition, report block, immutability section).

**Files modified:** `skills/workflow/implement/implement-plan/SKILL.md`

**Verification:** `grep -ni "loose" skills/workflow/implement/implement-plan/SKILL.md` returns nothing; `grep -n "plan-adjustment\|re-shapes the plan" …` returns nothing; `grep -n "progress.md" …` and `grep -in "pre-flight" …` and `grep -n "Source:" …` return hits; `grep -in "unverified" …` returns hits; `grep -c "version: 4.0.0" …` returns 1; `grep -in "subagent" …` returns hits only in the existing no-subagents-are-spawned topology statements.

**Acceptance criteria:**
- AC-8.1: index parsing, three-check pre-flight with halt semantics, `Source:` read at startup, task files read by the session itself.
- AC-8.2: deviation passages reworded identically in substance to AC-6.3.
- AC-8.3: unverified-concern classification and positive focus framing in self-review; awareness sentence present.
- AC-8.4: `progress.md` ledger appended cycle-gated, not commit-gated (one block per attempted task: committed cycles after the commit, empty-diff `DONE`/`BLOCKED`/`NEEDS_CONTEXT` cycles with `Commit: none`; spec disposition log P7) without subagent-audit lines; one chat line per task; report folded from the ledger re-read from disk.
- AC-8.5: no loose-granularity handling; no merged-reviewer or brief-construction machinery added.
- Frontmatter carries `version: 4.0.0`.

### Task 6: Deprecate `plan-loose` and `adjust-plan-granularity`; update registrations

**Objective:** Move the two retired skills to `skills/deprecated/` as pure renames and update every registration surface (marketplace plugin arrays, README, AGENTS.md Layout) — including the README blurbs that still describe the pre-v6 behavior of the three updated pipeline skills.

**Input / context:** `specs/001/spec.md` §7, AC-9.1–AC-9.4, plus README consistency with Tasks 1, 3, 5 (the plan-strict blurb at README.md:118-124, the implement-plan blurb at :144-150, and the implement-plan-with-subagents blurb at :152-158 still describe single-file plans, loose-or-strict input, and the two-pass dual-reviewer loop). README's existing convention for retired skills is the dated "Retired skills" list (README.md:314-318). `.vscode/settings.json` commit scopes stay unchanged — the leaf folder names still exist on disk. Depends on Tasks 1–5 being in place so the new blurb wording describes actual behavior.

**Steps:**
1. `git mv skills/workflow/plan/plan-loose skills/deprecated/plan-loose` and `git mv skills/workflow/plan/adjust-plan-granularity skills/deprecated/adjust-plan-granularity` — bodies, frontmatter, and versions untouched (pure renames).
2. In `.claude-plugin/marketplace.json`: remove `./skills/workflow/plan/plan-loose` and `./skills/workflow/plan/adjust-plan-granularity` from the `JeisKappa-plan` plugin's `skills` array (leaving only `./skills/workflow/plan/plan-strict`), and add `./skills/deprecated/plan-loose` and `./skills/deprecated/adjust-plan-granularity` to the `JeisKappa-deprecated` plugin's `skills` array. Plugin ordering is already compliant (alphabetical with deprecated last) — do not reorder plugins.
3. In `README.md`: delete the `plan-loose` and `adjust-plan-granularity` sections (currently lines 110-132) from the Plan module, leaving `plan-strict` as its only entry.
4. In `README.md`, rewrite the "Refining a plan" recipe row (currently line 41) without `adjust-plan-granularity` — e.g. `plan-strict → review-plan → implement-plan` — keeping the table shape intact.
5. In `README.md`, add two dated entries to "Retired skills" following the existing convention (retired 2026-07-06): `plan-loose` (the strict multi-file plan is now the single plan format; new installs pick `plan-strict`) and `adjust-plan-granularity` (with one granularity there is nothing to adjust; no successor — plans are edited alive in place or recompiled from their spec). Note the legacy folders remain on disk so existing installs do not break.
6. In `README.md`, update the three stale blurbs to match the new behavior: `plan-strict` (multi-file artifact: index `plans/NNN/plan.md` + one dispatchable task file per task under `tasks/`), `implement-plan` (multi-file plan input, mechanical pre-flight, durable run ledger; drop "loose or strict granularity, produced by any of the `plan-*` skills"), and `implement-plan-with-subagents` (merged single-reviewer dispatch returning two lane verdicts replacing the first-pass/second-pass description; durable run ledger). Keep each blurb's length and register consistent with its neighbors.
7. In `AGENTS.md` Layout section: change the `plan/` line to list only `plan-strict`, and extend the `deprecated/` line with `plan-loose` and `adjust-plan-granularity`.
8. Leave `.vscode/settings.json` untouched; confirm both scope entries still match on-disk leaf folder names.

**Files modified:** `skills/workflow/plan/plan-loose/` → `skills/deprecated/plan-loose/` (MOVED), `skills/workflow/plan/adjust-plan-granularity/` → `skills/deprecated/adjust-plan-granularity/` (MOVED), `.claude-plugin/marketplace.json`, `README.md`, `AGENTS.md`

**Verification:** `test -f skills/deprecated/plan-loose/SKILL.md && test -f skills/deprecated/adjust-plan-granularity/SKILL.md && test ! -e skills/workflow/plan/plan-loose && test ! -e skills/workflow/plan/adjust-plan-granularity` succeeds; `git show HEAD:skills/workflow/plan/plan-loose/SKILL.md | diff - skills/deprecated/plan-loose/SKILL.md` and the same for `adjust-plan-granularity` produce no output (pure renames); `jq -r '.plugins[] | select(.name=="JeisKappa-deprecated").skills[]' .claude-plugin/marketplace.json` lists both new paths; `jq -r '.plugins[] | select(.name=="JeisKappa-plan").skills[]' .claude-plugin/marketplace.json` lists only plan-strict; `jq -r '.plugins[-1].name' .claude-plugin/marketplace.json` returns `JeisKappa-deprecated`; `grep -n "plan-loose\|adjust-plan-granularity" README.md` returns hits only in the Retired skills section; `grep -n "plan-loose\|adjust-plan-granularity" AGENTS.md` returns hits only on the `deprecated/` Layout line.

**Acceptance criteria:**
- AC-9.1: both skills exist under `skills/deprecated/` with unmodified bodies; old paths gone; git reports pure renames.
- AC-9.2: marketplace paths moved to `JeisKappa-deprecated` and present in no other plugin; plugin ordering rules hold.
- AC-9.3: README no longer lists either skill under the plan group; no recipe row references them; Retired-skills entries added per convention.
- AC-9.4: AGENTS.md Layout shows both under `deprecated/` and only `plan-strict` under `plan/`.
- README blurbs for `plan-strict`, `implement-plan`, and `implement-plan-with-subagents` describe the multi-file / merged-reviewer behavior.

### Task 7: Documentation ripples and repo-wide consistency sweep

**Objective:** Update the V2 workflow reference docs for the multi-file plan folder, and run the spec's repo-wide consistency checks (deprecated-skill references, single-file-plan assumptions, version bumps) as the closing whole-change gate.

**Input / context:** `specs/001/spec.md` §8, AC-10.1–AC-10.4, DoF-8. Files: `docs/workflow/v2/thread-layout.md` (the plans lineage tree at lines 42-45 currently shows only `plan.md` + `reviews/`) and `docs/workflow/v2/filename-grammar.md`. On the grammar doc: its two-form system (versioned `<type>.md`; record `<UTC>-<desc>-<type>.md`) leaves a task file `tasks/NN-<kebab-slug>.md` ungrammatical — it has no UTC stamp and no type token — so a strict reader would forbid it; per DoF-8 this qualifies as "current rules would forbid task files" and the doc gets a minimal addition. Depends on Tasks 1–6 (this task verifies their combined output). V1 docs are never edited.

**Steps:**
1. In `docs/workflow/v2/thread-layout.md`, update the plans lineage subtree in the Folder Set diagram to show `plan.md` (the index), `tasks/NN-<kebab-slug>.md` task files, and `reviews/`, with a one-line annotation that the strict plan is a multi-file artifact whose index is authoritative for task count and order.
2. In `docs/workflow/v2/filename-grammar.md`, add a short subsection (near the Versioned Form) defining the plan task-file form: `tasks/NN-<kebab-slug>.md` inside a plan lineage folder — two-digit zero-padded ordinal + kebab slug, no UTC stamp, no `v<N>`, no artifact-type token — a plan-local form carried by the plan artifact (the index at `plan.md` remains the versioned `<type>.md` file). Keep the addition minimal; do not touch the record form or the token vocabulary list.
3. Run `grep -rn "plan-loose\|adjust-plan-granularity" README.md AGENTS.md .claude-plugin/ skills/workflow/ docs/workflow/v2/` and fix any residual STALE hit. Expected and allowed (do NOT remove): hits under `skills/deprecated/`, `docs/threads/`, and `.vscode/settings.json`, AND the intentional deprecation registrations mandated by AC-9.2–9.4 — the marketplace `JeisKappa-deprecated` entries, the README Retired-skills entries, and the AGENTS.md `deprecated/` Layout line (spec disposition log P8). A hit that treats either skill as ACTIVE (a plan-group listing, a live recipe/workflow row, a `skills/workflow/` occurrence) is fixed here; a hit in a file the spec puts out of scope is surfaced as a concern, not silently fixed.
4. Run the single-file-assumption sweep over the four updated skills and two method files: `grep -rn "the plan file\|plan artifact path\|parse the numbered task list\|plan body" skills/workflow/plan/plan-strict/ skills/workflow/review/review-plan/ skills/workflow/implement/implement-plan/ skills/workflow/implement/implement-plan-with-subagents/` — adjudicate each hit against AC-10.3 (phrasing that assumes a single-file plan without the index/tasks framing is fixed; phrasing already anchored to the index/tasks shape passes).
5. Verify version bumps: `grep -n "version:" skills/workflow/plan/plan-strict/SKILL.md skills/workflow/review/review-plan/SKILL.md skills/workflow/implement/implement-plan/SKILL.md skills/workflow/implement/implement-plan-with-subagents/SKILL.md` all show `4.0.0`; `grep -n "version:" skills/deprecated/plan-loose/SKILL.md skills/deprecated/adjust-plan-granularity/SKILL.md` show their pre-move values unchanged.
6. Re-run every earlier task's Verification block as a final regression pass; fix any drift a later task introduced into an earlier task's file.

**Files modified:** `docs/workflow/v2/thread-layout.md`, `docs/workflow/v2/filename-grammar.md`. Steps 3 and 6 are a consistency/regression sweep that may **contingently** edit any file where a residual reference or drift is found — `README.md`, `AGENTS.md`, `.claude-plugin/marketplace.json`, and the four updated skills (`skills/workflow/plan/plan-strict/SKILL.md`, `skills/workflow/review/review-plan/SKILL.md`, `skills/workflow/implement/implement-plan/SKILL.md`, `skills/workflow/implement/implement-plan-with-subagents/SKILL.md`) plus the two method files under `.../implement-plan-with-subagents/references/`; a clean final state touches only the two docs above.

**Verification:** `grep -n "tasks/NN" docs/workflow/v2/thread-layout.md` returns a hit inside the plans subtree; `grep -n "tasks/NN" docs/workflow/v2/filename-grammar.md` returns a hit in the new subsection; `grep -rn "plan-loose\|adjust-plan-granularity" README.md AGENTS.md .claude-plugin/ skills/workflow/ docs/workflow/v2/` returns only the mandated deprecation registrations (marketplace `JeisKappa-deprecated`, README Retired-skills entries, AGENTS.md `deprecated/` Layout line) and no active reference — `skills/workflow/` and `docs/workflow/v2/` clean; the step-4 sweep yields no unadjudicated single-file assumptions; the step-5 version greps match; every prior task's Verification block passes when re-run.

**Acceptance criteria:**
- AC-10.1: the deprecated-skill grep over README, AGENTS.md, `.claude-plugin/`, and `skills/workflow/` returns only the AC-9.2–9.4 deprecation registrations (marketplace `JeisKappa-deprecated`, README Retired-skills entries, AGENTS.md `deprecated/` Layout line) and no reference treating either skill as active (spec disposition log P8).
- AC-10.2: `thread-layout.md` shows the plans lineage folder containing `plan.md` + `tasks/NN-<kebab-slug>.md` (+ `reviews/`).
- AC-10.3: no single-file-plan assumptions remain in the four updated skills or two method files.
- AC-10.4: the four updated skills carry `version: 4.0.0`; the two deprecated skills' versions are unchanged.
- `filename-grammar.md` defines the task-file form without altering the record form or token vocabulary.

## Notes

- Task ordering front-loads the artifact format (Task 1) so every consumer task (2–5) describes the same shape; registrations (Task 6) and docs (Task 7) close the change. Between task boundaries the repo may transiently mix old and new descriptions across files — Task 7's regression pass is the whole-change gate that confirms the final state is consistent.
- This implementation deliberately writes no Raycast changes (`assets/skills.json` is generated), no V1 doc edits, no migration of existing plan artifacts, and no changes to `implement`, `spec`, `review-implementation`, `review-code`, or any other skill not named — all explicitly out of scope per `specs/001/spec.md ## Scope`.
- The reply-cap ceiling (15 lines) and the filename-grammar addition are this plan's resolutions of spec Degrees-of-freedom 2 and 8 respectively; all other DoF items remain free implementer choices at the prose level.
