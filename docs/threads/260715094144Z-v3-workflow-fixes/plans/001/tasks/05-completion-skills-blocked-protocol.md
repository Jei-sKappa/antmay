# Task 5: Unify the blocked protocol across the remaining completion-oriented skills

**Objective:** Remove every attended/AFK dual branch, dead decision-recording section, and blind "ASK the user" from the completion-oriented skills outside the implement trio and spec, and extract plan-strict's worked example.

**Input / context:** Settled decisions per `seed/discussions/260715102305Z-v3-skill-review-notes-decision-log.md` P1 (unified protocol), P5/P7 (remove "Recording elicited decisions" sections), P12 (worked-example extraction), P18 (input-resolution rule), P19 (terminal outcome). Primary files (known to carry dual-branch or recording sections): `skills/workflow/plan/plan-brief/SKILL.md`, `skills/workflow/plan/plan-strict/SKILL.md`, `skills/workflow/propose/propose/SKILL.md`, `skills/workflow/merge/merge-artifacts/SKILL.md`, and the four reconciles under `skills/workflow/reconcile/`. Audit files (completion-oriented, may carry stray ask-language): the four reviews under `skills/workflow/review/`, `skills/workflow/roadmap/roadmap/SKILL.md`, `skills/workflow/roadmap/materialize-roadmap-threads/SKILL.md`, `skills/workflow/documentation/take-snapshot/SKILL.md`, `skills/workflow/capture-discussion/open-ticket/SKILL.md`, `skills/workflow/research/afk-exploration/SKILL.md`. Dialogue-driven and handshake skills are OUT of scope: `discussion`, `resolve-pending-decisions`, `open-thread`, `whats-next`, `finish`, `archive-thread`, the three handoffs, `meta-prompting`, `the-librarian`.

**Steps:**

1. For each of the eight primary files, run `grep -n "AFK\|Attended\|attended\|ASK\|Recording elicited\|decisions.md" <file>` to locate the touchpoints, then apply the same transformation given to `spec` in Task 4:
   - Replace the `## Blocked under an AFK invocation` section (or attended/AFK branch pair) with a `## Blocked` section: finish everything safely derivable → `/emit-pending-decisions` (producer = the skill, target = its output artifact, evidence, the originating user request, suggested follow-up) → concise terminal notification → stop. No attended branch, no AFK detection.
   - Delete any `## Recording elicited decisions` section (present at minimum in `plan-brief`, `plan-strict`, `propose`) and `merge-artifacts`' attended/AFK divergence-settling branch plus its inline `D<N>` record format; in `merge-artifacts`, divergences that need human intent route to the `## Blocked` section.
   - Rewrite ask-language per the P18 input-resolution rule: a chat refusal (ending `Outcome: REFUSED — <reason>`, nothing written) only where a bundle is physically impossible — no thread exists, or which-thread is ambiguous; which-artifact and which-lineage ambiguity inside the resolved thread, and garbled inputs, route to `## Blocked` as clarification bundles; mid-run missing intent → `## Blocked`.
   - Apply the P19 terminal-outcome line to each edited skill's terminal paths: `Outcome: DONE — <the skill's exact confirmation>` on success, `Outcome: BLOCKED — pending decisions at <bundle path>` from `## Blocked`, `Outcome: REFUSED — <reason>` on refusal. The line composes with — never replaces — each skill's specified confirmation text.
2. Run the same grep over the nine audit files and apply the P18 rewrite wherever a completion-oriented body instructs waiting on a chat answer, plus the P19 terminal-outcome line on their terminal paths. Reviews emit `.pending-reviews/` bundles as today — a review run that wrote its findings bundle ends `Outcome: DONE`; only bundle-impossible input failures become `REFUSED`. Leave dialogue-driven skills untouched.
3. In `plan-strict/SKILL.md`, hoist anything normative stated only inside `### Worked Example` into the body (per P12 nothing is expected to need hoisting — the sequential-tasks note restates existing rules; verify), then move the example to `skills/workflow/plan/plan-strict/references/worked-example.md` (NEW) and replace the section with a naturally-worded pointer at the drafting step (e.g. "before writing the first task file, look at `references/worked-example.md` for the complete shape of a task file and index").
4. Re-read each edited file end-to-end for internal consistency (no dangling references to deleted sections, no leftover dual-branch vocabulary).

**Files modified:** `skills/workflow/plan/plan-brief/SKILL.md`, `skills/workflow/plan/plan-strict/SKILL.md`, `skills/workflow/plan/plan-strict/references/worked-example.md` (NEW), `skills/workflow/propose/propose/SKILL.md`, `skills/workflow/merge/merge-artifacts/SKILL.md`, `skills/workflow/reconcile/reconcile-spec/SKILL.md`, `skills/workflow/reconcile/reconcile-proposal/SKILL.md`, `skills/workflow/reconcile/reconcile-plan/SKILL.md`, `skills/workflow/reconcile/reconcile-roadmap/SKILL.md`, plus whichever of the nine audit files the grep shows needing the P18/P19 rewrite (expected: some of the four reviews, `roadmap`, `materialize-roadmap-threads`, `take-snapshot`, `open-ticket`)

**Verification:** `grep -rn "Blocked under an AFK\|Attended run\|explicitly AFK\|Recording elicited" skills/workflow --include=SKILL.md` returns nothing; `grep -rn "ASK the user" skills/workflow --include=SKILL.md` matches only dialogue-driven/handshake skills named out of scope above; `grep -L "Outcome:" <the eight primary SKILL.md files>` returns nothing; `test -f skills/workflow/plan/plan-strict/references/worked-example.md` succeeds; `grep -n "Worked Example" skills/workflow/plan/plan-strict/SKILL.md` returns nothing.

**Acceptance criteria:**
- No completion-oriented skill carries an attended branch, AFK detection, or a decision-recording section.
- Every blocked path in the primary files goes through `/emit-pending-decisions` and stops; refusal happens only where a bundle is physically impossible (P18), and every terminal path ends with the standard `Outcome:` line (P19).
- Plan-strict's worked example lives in `references/worked-example.md` with a natural pointer at the drafting step and no orphaned normative content.

**Consumes:** the unified-protocol canonical text from Task 1; the bundle shape (originating request) from Task 2; the transformation pattern established in Task 4.

**Produces:** `skills/workflow/plan/plan-strict/references/worked-example.md`; the rewritten completion-skill bodies in the file set Task 7 sweeps.
