# Seed: V3 workflow fixes and followups
External: none — this exploration began as a local idea without a tracker ticket

The V3 workflow cutover (docs/threads/260711150919Z-v3-workflow-kinds/) landed all 39 planned tasks, but its own implementation report flagged loose ends: per-task review checked each diff against its own task rather than the full spec's acceptance criteria, three skills (`discussion`, `review-spec`, `reconcile-roadmap`) carry minor duplicated-guidance nits deferred as future-polish, and a broader spec-acceptance-criteria pass was named as the expected next step. This thread addresses those loose ends and any other issues surfacing now that V3 is the active ruleset.
