# Task 14: `antmay afk resume`

**Objective:** Implement `resume <run-id>`: full checkpoint revalidation, per-waiting-kind resume behavior — including declared queue-resolution `advance`/`rerun`, harness-free Git-boundary finalization, and abandoned-`executing` recovery — then continuation through the snapshotted stages.

**Input / context:** `spec.md` §"Resuming a run" plus the resume-relevant parts of §"Git boundaries and executor commits" (finalization without another harness invocation) and the exit-code table; `decisions.md DR36` (revalidation list, snapshot-only, no overrides, preflight leaves checkpoint unchanged), `DR53` (`advance`/`rerun` after DONE-finalized queue pauses; non-DONE queue pauses retry same stage), `DR50` (Git-kind finalization; cross-pause HEAD warning), `DR54` (clean-worktree rule and its two exempt kinds), `DR16`/`DR26` (executing → manual lock removal → `interrupted` + new attempt), `DR9` (recorded repo path must resolve; never search), `DR48` (probe only the current stage's harness), `DR56` (re-print unrestricted warning). Reuses the runner: after the kind-specific entry action, continuation is just `executeRun` from the (possibly advanced) cursor.

**Steps:**

1. Create `cli/src/commands/resume.ts` exporting `resumeCommand(args: { runId: string }, deps: RunDeps): Promise<number>`.
2. Implement preflight, in order, every failure exiting `1` with the checkpoint unchanged: locate `runDirectoryFor(stateRoot, runId)` (absent runs directory or run dir → unknown run); `readCheckpoint` + `validateCheckpoint` (malformed → clear error); a `completed` run reports completion and exits `1`; verify the recorded `repoRoot` still exists and `git rev-parse --show-toplevel` from it equals it, and the recorded `threadRelPath` is an existing direct child of its active `docs/threads/` (a non-resolving recorded path fails clearly, never searched — AC-15.2); revalidate non-empty `seed.md`/`decisions.md`; probe **only** the current stage's snapshotted harness via `probeHarnessExecutables([currentProfile.harness], repoRoot)` (keep the version line for new attempt logs); resolve `resolveCurrentCheckoutWorkspace(repoRoot)` and require its canonical path to equal the checkpoint's `workspace.path`; enforce the clean-worktree rule via `isWorktreeClean` — required for every waiting kind except `git-policy-violation` and `commit-error`, and for `ready` and `executing` conditions; a dirty tree exits `1` with a commit-what-you-want-to-keep-or-revert message (AC-15.1).
3. Handle the `executing` condition before locking: an `executing` checkpoint whose lock file still exists refuses with the lock metadata (manual stale-lock removal required); once the lock is gone, atomically mark the abandoned attempt `interrupted` (with `diagnostics.origin: "manual-recovery"`), persist, and treat the run as retry-at-same-stage.
4. Acquire the lock; re-scan both queues under it. Print the startup summary (re-print the prominent unrestricted warning when the persisted flag is set). Then branch on the stored state:
   - waiting + non-empty queues: print the remaining files, release the lock, exit `2`, checkpoint unchanged;
   - waiting `pending-queues` whose finished attempt is `done` (DONE-finalized): apply the stage's snapshotted `queueResolution` — `advance` increments `stageIndex` and persists `ready` without rerunning; `rerun` starts a fresh attempt at the same stage;
   - waiting `pending-queues` whose attempt is `waiting` (non-DONE): new attempt at the same stage;
   - waiting `git-policy-violation`/`commit-error`: perform boundary finalization **without a harness invocation** — recheck the policy via `collectBoundaryStatus` + `evaluateBoundary` + `finalizeBoundary`; on success (committed or intended diff now empty) finish per the stage's normal advance path (then, if this pause had listed pending files, they were already required empty by the queue recheck above); on continued violation, refresh the waiting object, release, exit `2`;
   - every other waiting kind, and `ready`: new attempt at the stored stage.
   Implement the entry actions by adjusting the checkpoint, then delegate to `executeRun` for continuation through subsequent stages until pause or completion; map results to exit codes as in `run` and release the lock in `finally`. Warn (never pause) when `HEAD` differs from the stored stage-entry baseline across the pause (AC-12.7).
5. Wire the real handler in `cli/src/main.ts` (replace the `resume` placeholder). `resume` accepts no execution overrides and never rereads settings or recipe definitions — it must not import `loadSettings` or `builtInRecipes` for resolution (grammar already blocks the flags).
6. Add `cli/src/commands/resume.test.ts` on the repo fixture with the fake harness, seeding state by running `runCommand` with scripted pauses: unknown/malformed/completed runs exit `1` (AC-15.2); dirty-worktree refusal for a `outcome-blocked` pause vs. acceptance for a `commit-error` pause (AC-15.1); waiting + still-pending files → unchanged checkpoint, files printed, exit `2`; DONE-finalized `pending-queues` with `advance` continues to the next stage without rerunning, with `rerun` re-attempts the same stage; non-DONE `pending-queues` re-attempts; `git-policy-violation` finalization commits the preserved diff without any invoker call (assert zero fake-harness invocations before the next stage launches) and the became-empty diff advances; `executing` + present lock refused, after manual unlink the abandoned attempt is `interrupted` and a new attempt runs (AC-15.3); a settings edit between pause and resume changes nothing (AC-15.4, AC-20.2); stages already advanced past never rerun (AC-15.4); cross-pause HEAD movement warns without pausing (AC-12.7); unrestricted-flag runs re-print the warning (AC-18.1).

**Files modified:** `cli/src/commands/resume.ts` (NEW), `cli/src/main.ts`, `cli/src/commands/resume.test.ts` (NEW)

**Verification:** `npm --prefix cli run check` exits 0; `npm --prefix cli run test -- src/commands/resume.test.ts` exits 0.

**Acceptance criteria:**

- The full AC-15.1 revalidation sequence runs in order with checkpoint-preserving failures; only the current stage's harness is probed.
- Every AC-15.3 branch behaves as specified, including `advance`/`rerun`, harness-free Git finalization, and `executing` recovery.
- AC-15.4 holds: no settings/recipe rereads, no overrides, no session resumption, no advancing past a stage lacking DONE finalization, continuation through the snapshot after success.
- Resume of a completed run and a vanished repository fail per AC-15.2.

**Consumes:** `RunDeps` shape and allocation-free preflight helpers from Task 12; `executeRun`, fake harness (Task 10); `readCheckpoint`/`writeCheckpoint`/`validateCheckpoint`, `runDirectoryFor` (Task 5); lock and queue modules (Task 6); `probeHarnessExecutables` (Task 7); boundary engine (Task 9); `isWorktreeClean`, `readHead` (Task 9); display helpers (Task 11); `SignalInterruption` handling (Task 13 — resume installs the same signal handlers).

**Produces:** `resumeCommand(args, deps): Promise<number>` from `cli/src/commands/resume.ts`; the wired `resume` handler in `cli/src/main.ts`.
