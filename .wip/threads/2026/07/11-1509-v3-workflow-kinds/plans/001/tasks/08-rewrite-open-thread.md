# Task 8: Rewrite open-thread

**Objective:** Rewrite `open-thread` as the V3 thread-genesis entry point that interprets the user's input and delegates normalized creation to `/create-thread`.

**Input / context:** The cutover spec `specs/001/spec.md` § "Thread model" (Layout, Seed contract, External references) and AC-3.1/AC-3.2 (thread-relative to `docs/threads/260711150919Z-v3-workflow-kinds/`); decision log `seed/discussions/260711154147Z-v3-thread-kind-profiles-decision-log.md` P24 (verbatim expanded suggestion, no persisted workflow name), P25 (built-in name or complete custom suggestion; ask when neither; never infer), P26 (three seed requirements; sparse conditional metadata), P27 (passive external references), P51 (rewrite scope), P13 (completion-oriented posture). Current file: `skills/workflow/capture-discussion/open-thread/SKILL.md` (V2: ledger, tier, frozen seed folder — replace the body wholesale). The template copies at `references/shared/workflows/` are generated by a later task; this body references those paths without any note about how they are produced.

**Skill-body hygiene** — binding for the `SKILL.md` this task writes:

1. The body contains only what a fresh agent needs to execute the skill; every sentence must change the executing agent's behavior. No author commentary, design history, or explanations of how this repository is organized.
2. No repo-internal concepts in the body: no references to the cutover spec or its acceptance criteria, no `P<N>` decision-log citations, no V2/V3, migration, or supersession talk, nothing "we decided". An internal decision that matters at runtime is restated purely as behavior the agent must follow. (IDs that are part of the skill's own emitted artifact format — `D<N>`, `F<N>`, `C<N>` — are fine.)
3. Remove obsolete concepts by silence. Never write disclaimers such as "this skill does not use a ledger/tier/`.wip/`" — a "do not X" belongs in the body only when the skill's own instructions could plausibly lead an agent to do X.
4. State each rule once, in the section where it is operative; do not restate a guideline across sections. Repeat only a genuinely critical safety rule, and sparingly.

**Steps:**

1. Rewrite the frontmatter: keep `name: open-thread`; write a concise human-facing `description` (open a durable thread folder with a seed and decision log from an idea, a ticket, or a custom workflow suggestion); add `disable-model-invocation: true`; keep `metadata.author`; bump `metadata.version` (a major bump is appropriate; exact value is your choice).
2. Create `skills/workflow/capture-discussion/open-thread/agents/openai.yaml` containing exactly `policy:` / `  allow_implicit_invocation: false`.
3. Rewrite the body — inputs: a rough idea and/or an external ticket reference, PLUS either a built-in workflow template name (Quick, Standard, Roadmap) or a complete custom `## Suggested workflow`. A built-in name is invocation input only: resolve it against the skill's own `references/shared/workflows/{quick,standard,roadmap}.md` and use that file's `## Suggested workflow` section verbatim. When the user supplies a complete custom suggestion, use it as given. When neither is supplied, ASK — never infer a workflow from the task's subject or apparent complexity. Persist no workflow name, progress markers, or lifecycle values.
4. Body — composition: author the seed title and a self-contained genesis narrative from the user's input (and the ticket content when one is linked); assemble conditional metadata per the seed contract — `External:` only when a real tracker URL exists (never `none`); `Supersedes:` only when a known supersession relationship is useful; no owner, tier, disposition, or placeholder fields.
5. Body — delegation: invoke `/create-thread` with the full caller-authorization block (invoking operation `open-thread` plus every normalized field: slug/title, genesis narrative, verbatim suggestion text, conditional metadata). `decisions.md` is created eagerly by that delegation (header-only when no decisions exist).
6. Body — external passivity (P27): a linked ticket is read for context and its URL recorded; the skill performs NO tracker writes (no backlink comments, transitions, or closures) and never fails thread creation because tracker access is missing. When a supplied ticket must be read to build a self-contained seed and read access is unavailable, ask the user to provide the relevant content.
7. Body — report the created thread path; allow a short correction window; this is a completion-oriented operation, not a dialogue.

**Files modified:** `skills/workflow/capture-discussion/open-thread/SKILL.md` (rewritten), `skills/workflow/capture-discussion/open-thread/agents/openai.yaml` (NEW).

**Verification:**

```sh
grep -n 'disable-model-invocation: true' skills/workflow/capture-discussion/open-thread/SKILL.md
grep -n 'allow_implicit_invocation: false' skills/workflow/capture-discussion/open-thread/agents/openai.yaml
grep -n 'references/shared/workflows' skills/workflow/capture-discussion/open-thread/SKILL.md
grep -n 'create-thread' skills/workflow/capture-discussion/open-thread/SKILL.md
grep -riE '\.wip|ledger\.md|status\.approved|status\.implemented|record-verdict' skills/workflow/capture-discussion/open-thread/SKILL.md skills/workflow/capture-discussion/open-thread/agents/ ; \
grep -riwE 'tier|ledger' skills/workflow/capture-discussion/open-thread/SKILL.md skills/workflow/capture-discussion/open-thread/agents/   # both must return nothing
```

**Acceptance criteria:**

- The body instructs: accept a built-in template name or a complete custom suggestion; resolve names against its local `references/shared/workflows/` copies; copy the `## Suggested workflow` verbatim into `seed.md`; persist no workflow name; ask when neither input is supplied and never infer from subject or complexity; create `decisions.md` eagerly (header-only allowed); write `External:` only when a real source exists (spec AC-3.1).
- The body performs no tracker writes, does not fail on missing tracker access, and asks the user for ticket content when a needed ticket cannot be read; no tier or ledger behavior remains (spec AC-3.2).
- Frontmatter/metadata: `disable-model-invocation: true` present, `agents/openai.yaml` present with `allow_implicit_invocation: false`, concise human-facing description, semver bump (spec AC-2.1/2.2).
- Hygiene: the greps above return nothing; no `P<N>` citations or cutover/spec/migration references in the body; no disclaimers naming retired concepts (the old ledger/tier machinery disappears silently); no rule stated twice.

**Consumes:** `/create-thread` contract (Task 5); the moved tree (Task 1).

**Produces:** the V3 `open-thread` entry point; the `references/shared/workflows/` path convention that Task 24's manifest declares for this skill.
