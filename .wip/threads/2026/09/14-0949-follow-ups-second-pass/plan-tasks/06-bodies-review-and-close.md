### Task 6: Sweep the `review` and `close` bodies

**Objective:** Bring `review-spec`, `review-implementation`, `review-code`, and `close-thread` to the prefixed, project-layer-inputs, one-shape, directive-pointer, every-exit form, keeping the review skills' authority-anchor ranking in prose and changing no procedure.

**Input / context:** `spec.md` § "The `<skill_path>` prefix", § "Project-layer inputs", § "One shape for Inputs items", § "Reference pointers as directives", § "Every exit points at the terminal-outcome instruction"; AC-1.1, AC-1.4, AC-2.1 … AC-2.3, AC-3.1, AC-3.2, AC-3.4 (the three review bodies keep their authority-anchor ranking in prose), AC-4.1, AC-4.2, AC-4.4, AC-5.1 … AC-5.3, AC-9.2. Settled decisions: `decisions.md DR1`, `DR2`, `DR3`, `DR4`, `DR5`, `DR9`. All four bodies are completion-oriented declarers of `emit-terminal-outcome.md`; today they spell the literal `Outcome:` line 12 times (`review-spec` 2, `review-implementation` 3, `review-code` 3, `close-thread` 4). The review bodies' `## The authority anchor` sections already rank their sources in prose; the per-item "Authoritative for what the code was meant to do" tags that duplicate that ranking are dropped, not folded. Starts from the tree tasks 1–5 left; this is the last body group.

**The rules applied to every body in this task.** The letters are for this brief only; none of them, and no `DR<N>` identifier, is written into a body.

- **A. Prefix.** Every pointer to a file inside the skill's own folder is written with the literal prefix `<skill_path>/` — `<skill_path>/references/formats/adr.md`, `<skill_path>/references/instructions/append-log-line.md`, `<skill_path>/references/worked-example.md`. Project paths (`docs/adr/`, `docs/glossary.md`, `.wip/roadmaps/`, thread files such as `spec.md`, `plans/<folder>/`) stay bare.
- **B. Project-layer inputs.** The first two items of `## Inputs` become, with the target clause adapted to the skill (the per-body clause is given below):

  ```markdown
  - `docs/adr/`, read via `/consult-adrs` — the project decisions bearing on <the target>.
  - `docs/glossary.md`, read via `/consult-glossary` — the project's fixed terms, to be used in everything you write.
  ```

  Where the body invokes the conflict rule, the skill is a procedure, never a document: "the conflict rule `/consult-adrs` carries" becomes "as `/consult-adrs` instructs" (or an equivalent that reads as an instruction to classify). The folder is `docs/adr/`, singular, everywhere.
- **C. One shape for Inputs items.** Every item reads `- <path or source><, presence clause when it may be absent> — <what it is and what it is for>.` Remove every trailing tag (`; authoritative.`, `. Authoritative.`, `. Material.`, `. Material: …`, `Authoritative for intent.`, `Authoritative within the thread.`), and fold the inline forms (`, authoritative within the thread;`, `Material either way`) into the clause. Fold what carried information — the thread's `adr/` and `glossary.md` items say the delta takes precedence over the project records inside the thread; a `Material: <what to read>` tag becomes the clause's own sentence — and drop what was decoration. The presence clause is exactly one of `when present`, `when the seed carries one`, `when the file exists`: rewrite every other form (`when the thread holds one`, `whenever the thread holds one`, `when the invocation carries one`, `when the invocation names an entry`, `when it names one`, `when the thread's owner authored one`, `when a plan folder is the form`) to the nearest of the three. An item with no clause is always present or is the primary input; a primary input keeps its accepted-forms text inside its clause, in as many sentences as it needs. After the sweep the words "authoritative" and "material" survive in an item only where they state a ranking the body's procedure uses (for example the primary-input item of the plan skills: the reference "carries no authority over a `spec.md` the thread holds"). Where the body ranks its sources in prose (the review skills' authority-anchor section, `spec`'s "these are the whole of what the spec rests on"), that prose stays and nothing is added to compensate for a removed tag.
- **D. Pointers are directives.** A pointer to an instruction file is the step: "Follow `<skill_path>/references/instructions/<name>.md`" (or "follow … with <parameters>", "by following …"). A pointer to a format file is what a write or read conforms to: "following `<skill_path>/references/formats/<name>.md`" or "in the shape `<skill_path>/references/formats/<name>.md` defines". Other verbs are fine where they read better, provided the intent — open this file and do what it says, or conform to it — stays plain. Strict: the word `per` never directly precedes a `<skill_path>/` pointer; and no text around a pointer restates what the pointed file holds. Delete every sentence or clause that can be deleted because the reference carries it (for `emit-pending-decisions.md`: "one point per open decision", what each point states; for `emit-pending-review.md`: "as a single bundle"; for `append-log-line.md`: the line's shape, the seven types, the transient-framing rule; for `write-implementation-report.md`: the list of what the report folds in; for a format: its section list). Keep only the skill-specific parameters the file leaves open — producer or reviewer name, target, what the line states, which folder, the token and its reason — and any rule the body needs for its own judgment at that step, written as the skill's rule and not as a paraphrase with a citation. Same-body section pointers written "per `## <Section>`" or "per `### <Section>`" are untouched in text and count.
- **E. Every exit through the terminal-outcome instruction** (completion-oriented bodies only). Every sentence that spells a literal `Outcome:` line — refusal, block, or completion — becomes a directive to the instruction with the token and the reason as parameters: "follow `<skill_path>/references/instructions/emit-terminal-outcome.md` with `REFUSED`, naming the dirty paths and how to re-invoke", "… with `DONE` and `Spec written: spec.md`", "… with `BLOCKED` and `pending decisions at <bundle path>`". Existing "End per `references/instructions/emit-terminal-outcome.md` with `Outcome: DONE — …`" sentences become "Follow `<skill_path>/references/instructions/emit-terminal-outcome.md` with `DONE` and `…`". The literal string `Outcome:` appears nowhere in a `SKILL.md`. A body names one token as the parameter at an exit; it never lists `DONE`, `BLOCKED`, and `REFUSED` together as a vocabulary. Skill-local reply tokens and lane verdicts are a different vocabulary and are left alone.

Everything else in each body — posture, write boundary, order of acts, the judgment between them, section headings, numbered-step count, same-body pointers — is unchanged. Preserve each file's existing line-wrapping style (`open-ticket` wraps at 80 columns; the others do not).

**Steps:**

1. `suite/skills/review/review-spec/SKILL.md`:
   1. Rules B and C on `## Inputs`. Target clause: "the spec's subject". `spec.md`: drop "Material for the judgment." (keep the read-it-end-to-end sentence — the skill's own rule); `seed.md`: drop the tag; `adr/` and `glossary.md`: "— the thread's delta of the project layer and the constraint sources a spec must not contradict; inside the thread they take precedence over the project records."
   2. Rule E: Procedure step 1 ("…tell the user there is nothing to review, write nothing, and follow `<skill_path>/references/instructions/emit-terminal-outcome.md` with `REFUSED` and `no spec.md to review`."); Procedure step 4 ("Follow `<skill_path>/references/instructions/emit-terminal-outcome.md` with `DONE` and the readiness judgment for a clean review, or `DONE` and `findings at <bundle path>` when a bundle was written. No preamble, no closing remark.").
   3. `## Recording findings`: "record them as a single bundle per `references/instructions/emit-pending-review.md`, naming yourself as the reviewer and `spec.md` as the target." → "record them by following `<skill_path>/references/instructions/emit-pending-review.md`, with yourself as the reviewer and `spec.md` as the target."
   4. The obvious-contradiction paragraph: "classify it by the conflict rule `/consult-adrs` carries" → "classify it as `/consult-adrs` instructs".
2. `suite/skills/review/review-implementation/SKILL.md`:
   1. Rules B and C. Target clause: "the reviewed work". The folder item: drop "Material."; the report item: "in the shape `<skill_path>/references/formats/implementation-report.md` defines." (drop "Material."); the plan item: "The plan folder the report's `Plan:` line names, when present — `plans/<folder>/plan.md`, together with the `plan-tasks/` briefs the index points at for a strict plan." (drop "Authoritative for what this implementation set out to build." — `## The authority anchor` carries the ranking); `spec.md`, `when present`: drop the tag; `adr/` and `glossary.md`: fold as in review-spec ("…the constraint sources delivered work must not contradict; inside the thread they take precedence over the project records."); `seed.md`: drop the tag; the delivered-code item: drop "Material." (keep the read-only rule sentence).
   2. Rule E: Procedure step 1 (`REFUSED`, naming the reason); Procedure step 2 (`REFUSED`, naming the ambiguity; keep "never pick by recency or sort order"); Procedure step 4 (`DONE` and the fidelity judgment, or `DONE` and `findings at <bundle path>`).
   3. `## Recording findings`: the pending-review directive as in review-spec, with the implementation folder `implementations/<folder>/` as the target; keep the `## Context` sentence about a coarse anchor (skill-specific parameter).
   4. `## The authority anchor`: "What makes such a contradiction intentional rather than a finding is the conflict rule `/consult-adrs` carries." → "Whether such a contradiction is intentional or a finding is classified as `/consult-adrs` instructs."
3. `suite/skills/review/review-code/SKILL.md`: the same edits as `review-implementation` — Inputs (target clause "the code under review"; the report item keeps its "its `## Changes` locates the code and its `## Deviations` says which departures were deliberate" clause, drops "Material."); rule E on Procedure steps 1, 2, 4 (`DONE` and the quality judgment); `## Recording findings` directive with the implementation folder as the target and the `## Context` sentence kept; the authority-anchor conflict-rule sentence.
4. `suite/skills/close/close-thread/SKILL.md`:
   1. Rules B and C. Target clause: "the thread's drafts" — for example "the project decisions bearing on the thread's drafts, which land beside them." The thread item: "laid out per `references/formats/thread.md`" → "in the shape `<skill_path>/references/formats/thread.md` defines"; `seed.md`: drop the tag, keep the `Roadmap:`/`Entry:` sentence; `spec.md`, `when the file exists` (was "when the thread holds one"), drop the tag; `adr/`: "in the shape `<skill_path>/references/formats/adr.md` defines" and drop the tag; `glossary.md`, `when the file exists`, "in the shape `<skill_path>/references/formats/glossary.md` defines", drop the tag; the reports item: "in the shape `<skill_path>/references/formats/implementation-report.md` defines; its `## Deviations` entries and the delivered changes it describes are what the currency check reads."; the roadmap item, `when the seed carries one`: "— a project-level file under `.wip/roadmaps/`, in the shape `<skill_path>/references/formats/roadmap-index.md` defines; the heading whose text is the seed's `Entry:` slug is where the closing line goes."; the workspaces item: unchanged.
   2. Landing preflight (Procedure step 2): "What makes a contradiction intentional rather than an unnoticed conflict is the conflict rule `/consult-adrs` carries." → "Whether a contradiction is intentional or an unnoticed conflict is classified as `/consult-adrs` instructs."
   3. `## Blocked`: "Queue the open decision(s) per `references/instructions/emit-pending-decisions.md`, naming yourself as the producer, the thread root as the target, the originating user request, and one point per open decision — each stating what the decision blocks, why you could not settle it from the gathered inputs, and the evidence you weighed." → "Queue the open decision(s): follow `<skill_path>/references/instructions/emit-pending-decisions.md` with yourself as the producer, the thread root as the target, and the originating user request." Then rule E: "Then stop with a concise notification of where the bundle was written and follow `<skill_path>/references/instructions/emit-terminal-outcome.md` with `BLOCKED` and `pending decisions at <bundle path>`. The thread is left untouched …"; and "Name the bundles that are open and follow … with `BLOCKED` and `open pending decisions: <bundle names>`."
   4. Merge step (Procedure step 2 of `## Closing`, "Merge the glossary"): rule A only — "in the shape `<skill_path>/references/formats/glossary.md` defines".
   5. Report step: "End per `references/instructions/emit-terminal-outcome.md` with `Outcome: DONE — Thread closed: <thread path relative to .wip/threads/>`." → "Follow `<skill_path>/references/instructions/emit-terminal-outcome.md` with `DONE` and `Thread closed: <thread path relative to .wip/threads/>`."
   6. `## Refusals`: "Refuse before any write, naming what is wrong and how to re-invoke, and end with `Outcome: REFUSED — <the reason and how to re-invoke>`:" → "Refuse before any write, naming what is wrong and how to re-invoke, and follow `<skill_path>/references/instructions/emit-terminal-outcome.md` with `REFUSED`:" (the list that follows is unchanged).
5. Read each of the four bodies once end to end and apply rules A, D, and E to anything the list above missed; confirm the three `## The authority anchor` sections still rank `spec.md`, the plan folder, and `seed.md` in prose.

**Files modified:** `suite/skills/review/review-spec/SKILL.md`, `suite/skills/review/review-implementation/SKILL.md`, `suite/skills/review/review-code/SKILL.md`, `suite/skills/close/close-thread/SKILL.md`.

**Verification:** run from `suite/` (`$BASE` is the commit the run started from):

```sh
BODIES="skills/review/review-spec/SKILL.md skills/review/review-implementation/SKILL.md skills/review/review-code/SKILL.md skills/close/close-thread/SKILL.md"
FILES="$BODIES"
grep -n 'references/' $FILES | grep -v '<skill_path>/references/'       # → no output (AC-1.1, scoped)
grep -n '<skill_path>/docs\|<skill_path>/\.wip\|<skill_path>/plans\|<skill_path>/spec\.md' $FILES   # → no output (AC-1.4)
grep -n 'per `<skill_path>' $FILES                                       # → no output (AC-4.1)
grep -n 'Outcome:' $FILES                                                # → no output (AC-5.1)
grep -nE '(; authoritative\.|\. Authoritative\.|\. Material\.|\. Material:|Authoritative for intent\.|Authoritative within the thread\.)' $FILES   # → no output (AC-3.1)
grep -niE 'authoritative within the thread|material either way' $FILES   # → no output (inline tag forms folded)
grep -n "write the project's fixed terms" $FILES                         # → no output (AC-2.2)
grep -n 'consult-adrs` carries\|docs/adrs' $FILES                        # → no output (AC-2.3, AC-2.4)
grep -nE '\bDR[0-9]+\b' $FILES                                           # → no output (register)
grep -nE 'when (the thread holds one|the invocation (carries one|names an entry)|it names one|the thread.s owner authored one|a plan folder is the form)|whenever the thread holds one' $FILES   # → no output (AC-3.2 presence vocabulary)
for f in $BODIES; do
  awk -v F="$f" '/^## Inputs/{p=1;next} p&&/^## /{exit} p&&/^- /{c++; if(c==1&&$0!~/^- `docs\/adr\/`, read via `\/consult-adrs` — /)print F": item 1 wrong"; if(c==2){if($0!~/^- `docs\/glossary\.md`, read via `\/consult-glossary` — /)print F": item 2 wrong"; exit}}' $f
done                                                                     # → no output (AC-2.1)
for f in $FILES; do
  a=$(git show $BASE:suite/$f | grep -o 'per `#' | wc -l); b=$(grep -o 'per `#' $f | wc -l); [ "$a" = "$b" ] || echo "$f: same-body per count $a -> $b"
  diff <(git show $BASE:suite/$f | grep -E '^#{1,4} ') <(grep -E '^#{1,4} ' $f) >/dev/null || echo "$f: heading set changed"
  a=$(git show $BASE:suite/$f | grep -cE '^ {0,3}[0-9]+\. '); b=$(grep -cE '^ {0,3}[0-9]+\. ' $f); [ "$a" = "$b" ] || echo "$f: numbered-step count $a -> $b"
done                                                                     # → no output (AC-4.2, AC-9.2)
git status --porcelain -- ../cli                                        # → no output (AC-9.1)
node scripts/check-marketplace-skills.mjs                                # → exit 0
for f in $FILES; do n=$(grep -c '<skill_path>/references/instructions/emit-terminal-outcome.md' $f); echo "$f exits via the instruction: $n"; done
#  → review-spec 2, review-implementation 3, review-code 3, close-thread 4 — one per former Outcome: literal (AC-5.2)
grep -nE '`DONE`.*`BLOCKED`.*`REFUSED`|`REFUSED`.*`BLOCKED`.*`DONE`' $FILES   # → no output (AC-5.3)
for f in skills/review/review-implementation/SKILL.md skills/review/review-code/SKILL.md; do diff <(git show $BASE:suite/$f | awk '/^## The authority anchor/,/^## Procedure/' | grep -E '^[0-9]\. ') <(awk '/^## The authority anchor/,/^## Procedure/' $f | grep -E '^[0-9]\. ') && echo "$f: anchor ranking intact"; done   # → two confirmations (AC-3.4)
grep -n 'as a single bundle per\|one point per open decision' $FILES        # → no output
grep -c '<skill_path>/references/formats/' skills/close/close-thread/SKILL.md   # → 6 lines (thread, adr, glossary, implementation-report, roadmap-index in Inputs; glossary in the merge step)
# The whole-suite checks now hold for every body:
grep -rn 'Outcome:' skills                                                 # → no output (AC-5.1)
grep -rn 'references/' skills shared/references --include='*.md' | grep -v '<skill_path>/references/'   # → no output (AC-1.1)
grep -rn 'per `<skill_path>' skills shared/references                      # → no output (AC-4.1)
```

**Acceptance criteria:**
- Every skill-local pointer in the four bodies carries `<skill_path>/`; no project path carries it (AC-1.1, AC-1.4).
- Each `## Inputs` opens with the two project-layer items; "write the project's fixed terms" and "`/consult-adrs` carries" appear in no body (AC-2.1 … AC-2.3).
- No Inputs item ends in a tag; every presence clause is one of the three; the three review bodies still rank their sources in `## The authority anchor` (AC-3.1, AC-3.2, AC-3.4).
- No `per` before a `<skill_path>/` pointer; no text around an instruction pointer restates the instruction (AC-4.1, AC-4.4).
- `Outcome:` appears in none of the four bodies — and, this being the last body group, in no `SKILL.md` at all; every exit points at the terminal-outcome instruction with its token; no body lists the three tokens together (AC-5.1 … AC-5.3).
- Same-body `per` count, heading set, and numbered-step count unchanged in every body (AC-4.2, AC-9.2); marketplace check exits 0; `cli/` has no diff.

**Consumes:** the exit wording from task 4; the synced, prefixed `emit-pending-review.md`, `emit-pending-decisions.md`, and format files (tasks 1–2) under each of these skills.

**Produces:** the fully swept `suite/skills/` tree — every body prefixed, one Inputs shape, directive pointers, no `Outcome:` literal — which task 8's check must find clean and whose text the authoring documents in task 7 describe.
