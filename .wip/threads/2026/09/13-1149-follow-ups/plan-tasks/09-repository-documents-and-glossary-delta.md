### Task 9: Rewrite the repository documents and draft the glossary delta

**Objective:** Split the repository's documents by audience — `README.md` for the external user, `docs/documentation-rules.md` and `docs/working-with-threads.md` for maintainers, both `AGENTS.md` files reduced to pointers plus essentials, `CONTRIBUTING.md` repointed — and write this thread's `glossary.md` so `close-thread` can merge the vocabulary this thread changes.

**Input / context:** `spec.md` § "Documents" (`README.md`; the two new `docs/` documents; `AGENTS.md`, root and suite) and § "Glossary delta"; AC-7.2, AC-8.2 (for these files), AC-9.2 … AC-9.5, AC-11.1. Settled decisions: `decisions.md DR11` (three document kinds; what each `AGENTS.md` holds), `DR9` (README overview and expects/leaves lines; no lifecycle document), `DR14` (recipes, `finish`, `whats-next` gone from the README; the CLI lags), `DR8` (README model-invoked section; root `AGENTS.md` points at `/consult-adrs`), `DR10`, `DR12`, `DR13` (paths and no archive), `DR18` (this thread migrates too — but this task writes the paths as the suite defines them, `.wip/threads/` and `.wip/roadmaps/`, before the move in task 10). Material: current `README.md`, root `AGENTS.md`, `suite/AGENTS.md`, `CONTRIBUTING.md`, `docs/glossary.md` (read only — its rows are what the delta redefines or retires), `suite/shared/references/formats/glossary.md` (task 1), the five `suite/authoring/` files (task 8), and the final bodies (tasks 4–7) for each skill's expects/leaves line. The thread root in this task is still `docs/threads/260913114934Z-follow-ups/`; write the glossary at `glossary.md` in that root.

**Steps:**

1. Rewrite `README.md`:
   - Intro paragraphs: threads live under `.wip/threads/`, the project layer under `docs/adr/` and `docs/glossary.md`; drop "one of three built-in recipes" and "automates the automatable core of a recipe" — the CLI paragraph says a pipeline runs a sequence of skills unattended against one thread (keep the `cli/README.md` link).
   - Delete `## Recipes` entirely. Keep `## Installation` unchanged.
   - Add a short overview section (for example `## Threads and the project layer`): a thread is a folder `.wip/threads/yyyy/mm/dd-hhmm-slug/` holding `seed.md`, `log.md`, `spec.md`, `adr/`, `glossary.md`, `plans/`, `implementations/`; the project layer is `docs/adr/` and `docs/glossary.md`, plus roadmap indexes under `.wip/roadmaps/`; closing a thread lands its decisions and terms into the project layer and leaves the folder in place. Overview level only — no format rules restated.
   - Keep `## Terminal outcomes`; rewrite its last sentence so it names no primitive (dialogue-driven skills such as `discussion` and `open-thread` emit no terminal outcome; the model-invoked skills below emit none either).
   - `## Skills`: drop the sentence pointing at Primitives; every entry keeps its heading link (rewritten to the new nested path where it moved: `./suite/skills/close/close-thread/SKILL.md`) and its install snippet, and its prose becomes one short line saying what the skill expects and what it leaves behind, opening with the word `Expects` and containing the word `leaves` (for example, for `spec`: "Expects a thread whose discussion has settled — the live conversation or `log.md`; leaves `spec.md` at the thread root and one `event` line in the log."). Delete the `finish` and `whats-next` entries. Rename `### Finish & Navigate` to `### Close`.
   - Replace `## Primitives` with `## Model-invoked skills`: one paragraph saying these are skills the model may invoke on its own whenever they help, whether or not another skill is running, and two entries (`consult-adrs`, `consult-glossary`) with heading link, one line each, install snippet.
   - Add `## Contributing` linking `CONTRIBUTING.md`, `docs/documentation-rules.md`, `docs/working-with-threads.md`, and `suite/authoring/`. Delete the closing "For the method … see `suite/method.md`" line.
2. Create `docs/documentation-rules.md` (NEW), maintainer register: the three document kinds with the audience and register of each (shipped content — the invoked agent in any project, never cited as this repository's documentation; `README.md` — the external user; maintainer documentation — whoever works on this repository, referenced from the `AGENTS.md` files; the same fact may appear in two kinds, never twice within one); "Describe the current state, never the diff" with its test, moved verbatim in substance from the root `AGENTS.md`; "Document only durable, properly scoped information" (its four paragraphs — durable prose only, narrowest scope, printed catalogs are not documentation, thread-local material stays in its thread and ADRs are cited by stem, README is user-facing, AGENTS.md files are working memory and not inventories), moved from the root `AGENTS.md`.
3. Create `docs/working-with-threads.md` (NEW), maintainer register: this repository runs on the suite it ships; threads live under `.wip/threads/yyyy/mm/dd-hhmm-slug/` (year, month, day-time-slug), roadmap indexes under `.wip/roadmaps/`, the project layer under `docs/adr/` and `docs/glossary.md`; `.wip/` is a dot-folder so searches skip it unless asked (`rg --hidden`, or `grep` with the path named); `/consult-adrs` and `/consult-glossary` are how an agent reads the project's decisions and terms — never a catalog command in a document; which skills to reach for, stated by situation (to open a thread; to think a topic through; to write the spec; to plan; to check a plan; to implement; to review; to settle queued decisions; to write a roadmap; to close) and pointing at the README's skill index for the list — not as a numbered sequence; the `[contract]` note that the CLI lags the suite is not repeated here (the root `AGENTS.md` carries it).
4. Rewrite the root `AGENTS.md` to under 80 lines: the update rule (unchanged, with the `CLAUDE.md` symlink note); one paragraph on what the repository is (the method, the suite, the CLI); the layout tree (`suite/`, `cli/`, `docs/` with `adr/`, `glossary.md`, `documentation-rules.md`, `working-with-threads.md`; `.wip/` with `threads/` and `roadmaps/`; `.claude-plugin/`, `.github/`, `assets/`, `README.md`); one pointer line per maintainer document (`CONTRIBUTING.md`, `docs/documentation-rules.md`, `docs/working-with-threads.md`, `suite/authoring/`, `suite/AGENTS.md`, `cli/AGENTS.md`); the authoritative-documents table listing `README.md`, `suite/authoring/`, `cli/README.md`, `CONTRIBUTING.md`, `docs/documentation-rules.md`, `docs/working-with-threads.md`, and the three `AGENTS.md` files; the before-anything-else rules only — never commit unless asked; Conventional Commits with the scope rule in two lines; the "Keep the CLI stage support reference current" rule as written today; one line stating that the CLI lags the suite (paths, deleted skills, stage table) and is realigned in a follow-up `[contract]` thread, so it is not fixed piecemeal; `/consult-adrs` and `/consult-glossary` as the way to read the project layer, in one line each; `docs/glossary.md` as the naming authority, settled through threads and merged by `close-thread`, in two lines. Everything else (issue classification, the two documentation rules, the method section's catalog command, the vocabulary section's prose) leaves — it lives in `CONTRIBUTING.md` or the two `docs/` documents.
5. Rewrite `suite/AGENTS.md` to under 80 lines: the update rule; one paragraph on what the suite is and how it is installed; the layout tree showing the eight groups with their skills, `shared/references/{formats,instructions,trackers,repository-conventions.md}`, `shared/manifest.yaml`, `scripts/`; one pointer line per file under `suite/authoring/`; the before-anything-else rules only — the marketplace-array warning and `node scripts/check-marketplace-skills.mjs` after any add/remove/rename/move; edit canonical sources under `shared/references/` and run the sync script, never a generated copy; the leaf folder name equals the frontmatter `name`; new skills start at `version: 0.0.0` and register in `.claude-plugin/marketplace.json`, `README.md`, and `.vscode/settings.json` scopes. The `## SKILL.md format`, `## Invocation roles`, `## Skill composition`, `## Inputs convention`, `## Write boundaries`, `## Shared references`, and the eight-step `## When adding a new skill` sections leave — their rules now live in `suite/authoring/` (the adding-a-skill checklist may survive as a five-line list if it stays under the line budget).
6. Edit `CONTRIBUTING.md` § "Working in the repository": replace the sentence pointing at `suite/method.md` with pointers to `docs/working-with-threads.md` (how the repository uses its own threads) and `suite/authoring/` (skill conventions); leave the rest unchanged.
7. Write this thread's `glossary.md` (NEW) per `suite/shared/references/formats/glossary.md`: a table with these rows —
   - additions: **instruction** (a shared reference holding a self-contained procedure for one act, written to whoever performs it, naming no skill); **model-invoked skill** (a skill the model may invoke at its own discretion because it is useful in any situation, whether or not an entry point is running);
   - redefinitions of existing project rows whose current text names an old path or a retired concept: **thread** (a folder `.wip/threads/yyyy/mm/dd-hhmm-slug/`, identified by that path relative to `.wip/threads/`); **roadmap index** (`.wip/roadmaps/<yymmddhhmm>-<slug>.md`, …); **project layer** (`docs/adr/`, `docs/glossary.md`, and the roadmap indexes under `.wip/roadmaps/`); **closing** (check, land the delta into the project layer, write the closing line beneath the roadmap entry; the folder stays in place); **final deliverable** (the code, the landed records, and the thread's artifacts in place); **shared reference** (formats, instructions, trackers); **entry point** (a user-invoked skill …); **thread log** (appended to by `discussion`, `resolve-pending-decisions`, and `spec`); **preflight** (validates the invocation input, artifacts, and tooling — no thread resolution); **skill** (drop "never inspects which recipe it is running under"); **the Antmay method** (same meaning, without the `suite/method.md` link); **terminal outcome** (same meaning, pointing at `suite/authoring/interaction-posture.md`); **write authority** (same meaning, pointing at `suite/authoring/side-effects.md`); **workflow** (same meaning, without the "**recipe** for one of the three named paths" clause); the heading row **`## Workflow`** (never a section heading in a skill body; `## Recipe` is not named);
   - retirements, each a row whose meaning says the term leaves the vocabulary and what to write instead: **primitive** (write "model-invoked skill" or "instruction" as the case is), **caller**, **caller-authorization block**, **recipe**, **Quick / Standard / Roadmap**, **step**, **process shape**, the **recipe vs pipeline** contrast (the paragraph, not a row — state that no such contrast is drawn), the **stage vs step** row (retired with **step**), **archive** (a closed thread stays in place).
   Do not touch **pipeline** or **stage** — the CLI follow-up thread redefines them.
8. Search every file this task wrote for `docs/threads`, `docs/roadmaps`, `method.md`, `skill-authoring.md`, "recipe", "primitive", "archive", `finish`, `whats-next`, and fix every hit that is not a retirement row in the glossary delta.

**Files modified:** `README.md`, `AGENTS.md` (root), `suite/AGENTS.md`, `CONTRIBUTING.md`, `docs/documentation-rules.md` (NEW), `docs/working-with-threads.md` (NEW), `docs/threads/260913114934Z-follow-ups/glossary.md` (NEW — this thread's root).

**Verification:**

```sh
TH=docs/threads/260913114934Z-follow-ups
grep -n -i 'recipe\|primitive' README.md AGENTS.md suite/AGENTS.md CONTRIBUTING.md docs/documentation-rules.md docs/working-with-threads.md    # nothing (AC-7.2)
grep -n '^## Recipes\|^## Primitives' README.md; grep -c '^## Model-invoked skills' README.md    # nothing; 1
grep -n 'method.md\|skill-authoring.md' README.md AGENTS.md suite/AGENTS.md CONTRIBUTING.md docs/*.md    # nothing (AC-9.5 for these files)
grep -n 'docs/threads\|docs/roadmaps\|archiv\|finish\b\|whats-next' README.md AGENTS.md suite/AGENTS.md CONTRIBUTING.md docs/documentation-rules.md docs/working-with-threads.md   # nothing
# AC-9.3: every skill link resolves; every skill entry has an expects/leaves line
grep -oE '\./suite/skills/[a-z-]+/[a-z-]+/SKILL.md' README.md | sort -u | while read p; do test -f "$p" || echo "DEAD LINK $p"; done
grep -oE '\./suite/skills/[a-z-]+/[a-z-]+/SKILL.md' README.md | sort -u | wc -l       # 20
grep -c '^Expects .*leaves' README.md     # 20 (the twenty user-invoked and model-invoked entries; model-invoked lines may instead say what they read — then expect 18)
# AC-9.2
test -f docs/documentation-rules.md && test -f docs/working-with-threads.md
grep -c 'docs/documentation-rules.md' AGENTS.md; grep -c 'docs/working-with-threads.md' AGENTS.md     # >= 1 each
for f in skill-roles interaction-posture body-structure shared-references side-effects; do grep -q "authoring/$f.md" suite/AGENTS.md || echo "suite/AGENTS.md misses $f"; done
# AC-9.4
wc -l AGENTS.md suite/AGENTS.md      # each < 80
grep -c 'Keep the CLI stage support reference current' AGENTS.md    # 1
grep -c -i 'lags\|follow-up' AGENTS.md                              # >= 1
grep -c 'consult-adrs' AGENTS.md docs/working-with-threads.md       # >= 1 each
# AC-11.1
test -f $TH/glossary.md && head -3 $TH/glossary.md
for t in instruction 'model-invoked skill' thread 'roadmap index' primitive caller 'caller-authorization block' recipe 'Quick / Standard / Roadmap' step 'process shape' archive; do grep -q -- "\*\*$t\*\*" $TH/glossary.md || echo "glossary misses $t"; done
grep -n '\*\*pipeline\*\*\|\*\*stage\*\*' $TH/glossary.md    # nothing
```

**Acceptance criteria:**

- `README.md` has no `Recipes` or `Primitives` heading, has `## Model-invoked skills`, a `### Close` group, a threads/project-layer overview, a contributing section, exactly twenty skill links that all resolve, and one expects/leaves line per skill.
- `docs/documentation-rules.md` and `docs/working-with-threads.md` exist, in the maintainer register, and are linked from the root `AGENTS.md`; `suite/AGENTS.md` links each of the five `suite/authoring/` files.
- Both `AGENTS.md` files are under 80 lines, hold a purpose paragraph, a layout tree, pointer lines, and only before-anything-else rules; the root file keeps the CLI-stage-table rule verbatim and states in one line that the CLI lags and is realigned in a follow-up thread.
- `CONTRIBUTING.md` no longer points at `suite/method.md`.
- This thread's `glossary.md` follows `formats/glossary.md` and carries every addition, redefinition, and retirement in step 7, leaving `pipeline` and `stage` untouched.

**Consumes:** `suite/shared/references/formats/glossary.md` (task 1); the five `suite/authoring/*.md` paths (task 8); the skill tree and the two model-invoked skills (task 3); the final bodies (tasks 4–7) for the expects/leaves lines.

**Produces:** `README.md`, `AGENTS.md`, `suite/AGENTS.md`, `CONTRIBUTING.md` with no `docs/threads` reference left for task 10 to rewrite outside `.gitignore`; the thread's `glossary.md`, which task 10 moves with the thread.
