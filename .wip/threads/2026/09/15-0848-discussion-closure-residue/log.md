# Thread log
- (decision) the points the discussion agent holds back at closure are not written to the log, because the user waving them through means "not a fork" rather than agreement with the answer, so they are not settled points; a fresh-session spec re-derives them from scratch, cost accepted
- (decision) the discussion closure message lists every held-back point in full, never a sample, and states that the answers are non-binding and that a point whose specific answer matters to the user is a fork to promote now
- (decision) one term, inference, names an agent-settled point whether it appears in the discussion closure list or in the spec, because they are the same object at two moments; the spec section listing them carries both the ones the discussion listed and the ones the spec agent finds while writing, undistinguished
- (constraint) the vocabulary is six terms with one meaning each: decision and assumption (user-settled, log types), inference (agent-settled, spec-pinned), degree of freedom (spec-granted open how), judgment call (implementer pick where the spec pins nothing), deviation (implementer departure from something pinned, intent preserved); the spec audit label "assumption" becomes "inference"
- (question) whether folding implementer judgment calls into the report's deviations section should be corrected, since a deviation must name what it departs from; noted as outside this thread
- (decision) the closure list is bullet points, each labelled either as a choice the agent would settle one way or as one to leave to the implementer, because the user is present to correct a misfiled item and absent when the spec applies its own bar; the spec still re-applies that bar
- (constraint) the closure list is everything the agent holds back at that moment and not a sample, but it is not the full set of inferences the spec will write; the spec section normally holds more, and the discussion does not try to be exhaustive
- (decision) the spec marks each inference inline at the point of use and also lists every inference as a bullet in a dedicated Inferences section present in every spec, because the inline label keeps local context and the section is the unit review-spec and the user scan; the amendment pass keeps the two in step
- (decision) the closure message stops at the labelled inference list and the promote-or-stop question, and names no next skill, because the discussion does not hand off and the list already says where the material goes; Finish reports as it does today
- (decision) the closure step replaces the generic keep-going-or-finish prompt: the discussion works through undecided forks without asking whether to continue, and asks only once, when no real fork remains, which settles #57 together with #2
- (event) spec authored from the live conversation
