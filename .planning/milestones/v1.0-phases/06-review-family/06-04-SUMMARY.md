---
phase: 06-review-family
plan: 04
subsystem: skills
tags: [review-family, implementation-review, code-vs-original-intent-fidelity, v1-verification-role, no-separate-verify, anti-sycophancy, v1-skill-pair, decision-log, review-finding, inbox-open, two-input-contract, d85-verification-coverage, d89-ask-and-test, d92-d95-conditional-inbox]

requires:
  - phase: 01-foundations
    provides: V1 thread layout, record-form + versioned-form filename grammar, immutability + ambiguous-reference rules
  - phase: 02-capture-discussion
    provides: discussion + seeded-discussion anti-sycophancy stance (4 markers verbatim), capture-inbox for scope-drift parking, lazy decision-log creation pattern
  - phase: 05-implement-family
    provides: implement-auto + implement-interactive + implement-plan-auto + implement-plan-interactive + implement-plan-with-subagents-auto + implement-plan-with-subagents-interactive — emit the implementation outcomes this skill reviews; D75 per-plan-task commit cadence; D74 four-state status protocol (DONE / DONE_WITH_CONCERNS / BLOCKED / NEEDS_CONTEXT); the 4–5 source-artifact input forms (spec / proposal / plan / GitHub issue / Inbox item)
  - plan: 06-01
    provides: V1 review-pair body shape (auto + interactive); 6-section findings-first report shape (Verdict / Findings / Evidence / References / Open Questions / Next Actions); per-finding walk loop with D89 ASK-AND-TEST do-not-just-accept phrasing; 4-marker anti-sycophancy carry-over + review-stance amplifier pattern; D92/D95 conditional inbox-dump rule ("no Inbox file when nothing remains")
  - plan: 06-02
    provides: Per-element walk grain choice in interactive review siblings; severity mapping (`blocker | issue | nit`) reusable shape; handoff-grade-amplifier framing reusable for the implementation-stage stakes amplifier
  - plan: 06-03
    provides: Multi-axis (D83 four-axis) review skill body shape with per-axis severity heuristics tethered to downstream impact; optional source-artifact input with explicit skip-axis behavior (skipped axis with `## Open Questions` note rather than silently inventing or picking by recency); reuse pattern for the implementation-target additional checks (per-plan-task commit cadence + four-state status alignment when source is a plan)

provides:
  - skills/review-implementation-auto/SKILL.md (autonomous V1 implementation review against five D85 code-vs-original-intent fidelity axes — acceptance/outcome coverage + constraint adherence + scope adherence + behavior fidelity + test coverage; plan-driven additional checks per D75 commit cadence + D74 four-state status alignment; 6-section findings-first report emitted to inbox/open/<UTC>-<kebab-desc>-review-finding.md with TWO citations per finding (diff + source); two-input contract (implementation reference + source artifact))
  - skills/review-implementation-interactive/SKILL.md (collaborative V1 implementation review with per-finding OR per-source-acceptance-item OR per-plan-task walk against the same five fidelity axes; 4 anti-sycophancy markers verbatim + review-stance amplifier + implementation-stage stakes amplifier; D89 ASK-AND-TEST against BOTH the diff AND the source (do-not-just-accept); decision log at discussions/<UTC>-<kebab-desc>-decision-log.md + conditional inbox dump at inbox/open/<UTC>-<kebab-desc>-review-finding.md per D92/D95)
  - V1 Verification Role explicit statement in both skill bodies (D85, REVW-09): "covers V1 verification" + "no separate verify-* skill" — the verification question is subsumed by implementation review
  - SHORT (1-line) verification-coverage blockquote in README.md inserted above the new review-implementation-* entries naming review-implementation-* as the V1 verification answer

affects: [phase-06-plan-05-review-code, phase-07-merge-finish-navigation]

tech-stack:
  added: []
  patterns:
    - "Five-axis implementation review per D85 — acceptance/outcome coverage + constraint adherence + scope adherence + behavior fidelity + test coverage. Each axis carries explicit severity heuristics tethered to downstream impact (what does the next reviewer / next implementer / downstream caller find missing, mis-implemented, exceeding source-scope, or not testable?)."
    - "TWO-INPUT contract — the distinguishing trait of the implementation review target. Input 1: an implementation reference (a git ref / commit range / branch name / saved diff file / inline diff text). Input 2: a source artifact (one of 5 forms matching Phase 5's implement-* inputs — spec / proposal / plan / GitHub issue / Inbox item). The review's core question is 'does the IMPLEMENTATION deliver what the SOURCE ARTIFACT said it would?'. Both inputs are MANDATORY; without the source artifact the user is redirected to review-code-*'s general-purpose code review territory."
    - "V1 Verification Role explicit in both skill bodies — covers V1 verification (no separate verify-* skill). The verification question is the same as implementation-review's question; splitting them would duplicate logic and confuse users. Both skill bodies carry an explicit ## V1 Verification Role subsection naming the coverage fact. The literal phrase 'covers V1 verification' AND 'no separate verify-* skill' both appear verbatim."
    - "Plan-driven implementations get additional checks beyond the five fidelity axes — per-plan-task commit cadence per D75 (each plan task should have at least one commit; no commit spans multiple plan tasks; no plan task split across multiple commits) and four-state status alignment per D74 (DONE / DONE_WITH_CONCERNS / BLOCKED / NEEDS_CONTEXT tokens are matched against the diff state). When the source is NOT a plan (less-structured input from implement-* / implement-interactive*) these checks do NOT apply."
    - "Two-citation Evidence requirement — every fidelity finding cites BOTH the diff (file:line) AND the source-artifact section. The two-citation requirement is the implementation-review-specific shape (a fidelity finding without both citations is unverifiable). Carried in both skill bodies' ## Evidence section description and in the per-iteration walk format step for the interactive sibling."
    - "Implementation-stage stakes amplifier in review-implementation-interactive's anti-sycophancy stance: 'the cheap moment to push back is during the walk, before the implementation is merged or released, before downstream callers depend on the not-quite-right shape; V1 implementation skills do not rewind the commits per D77'. Implementation-stage variant of review-spec-interactive's handoff-grade amplifier and review-plan-interactive's plan-stage stakes amplifier; the asymmetry here is sharpest because the code is already written — every unflagged fidelity gap becomes a future 'but the source said X' conversation with no clean way to recover."
    - "Per-finding OR per-source-acceptance-item OR per-plan-task walk grain choice in review-implementation-interactive — executor's discretion. Per-finding default when findings cluster across the five fidelity axes (e.g., a few coverage gaps PLUS a constraint violation PLUS a scope drift); per-source-acceptance-item default when the source has many discrete acceptance items (the spec-style walk pattern); per-plan-task default when the source is a plan (each plan task surfaces alongside its matching commits / diff hunks). Same flexible-grain pattern established by review-spec-interactive (Plan 06-02) and review-plan-interactive (Plan 06-03); only the recommended-default rationale differs."
    - "Read-only-against-implementation discipline — both skill bodies state explicitly: this skill does NOT check out branches, does NOT run tests, does NOT modify the working tree, and does NOT mutate any git state (no git checkout / git reset / git stash / git rebase). Code modifications are an implement-* skill's job per Phase 5. The review is read-only against the diff text."
    - "README verification-coverage blockquote (≤3 lines) inserted ABOVE the new review-implementation-* entries — a SHORT one-line note stating that V1 verification of implementations is covered by review-implementation-* and there is no separate verify-* skill in V1 (per D85, REVW-09). Positioned as a blockquote rather than an italicized inline to be findable by a user scanning the README for 'verify'. The full README hybrid (toolbox model, layered map, recommended paths) is still Phase 7; this is the minimal documentation note REVW-09 requires."

key-files:
  created:
    - skills/review-implementation-auto/SKILL.md
    - skills/review-implementation-interactive/SKILL.md
  modified:
    - .claude-plugin/marketplace.json (JeisKappa-workflow.skills 25 → 27; both new skills inserted alphabetically between propose-interactive and review-plan-auto; JeisKappa-skills unchanged at 7)
    - .vscode/settings.json (conventionalCommits.scopes 32 → 34; review-implementation-auto + review-implementation-interactive inserted alphabetically between report-to-the-owner and review-plan-auto; array remains alphabetically sorted)
    - README.md (Available skills 32 → 34; both new entries inserted between propose-interactive and review-plan-auto heading, above ## Retired skills; SHORT 1-line verification-coverage blockquote added directly above the new entries naming review-implementation-* as the V1 verification answer per D85, REVW-09)

key-decisions:
  - "Five-axis fidelity review per D85 — acceptance/outcome coverage + constraint adherence + scope adherence + behavior fidelity + test coverage. The five axes were elaborated from CONTEXT.md's specifics block: each axis carries its own severity heuristics (total gap = blocker; partial gap or drift = issue; misaligned coverage that LOOKS like the source's acceptance = issue; safety constraint violation = blocker; out-of-scope refactor = issue; etc.). The five-axis enumeration is the structurally richest review target in Phase 6 so far — appropriate because the implementation target's risk surface is wider than spec / proposal / plan (it carries everything the source promised PLUS everything the implementer chose to add)."
  - "TWO mandatory inputs — implementation reference + source artifact. Without the source artifact, there are no fidelity axes to check against; the user is redirected to review-code-* for source-independent code review. This is the key distinction from the other four review targets (which take ONE primary input — the artifact being reviewed). The implementation review NEEDS both inputs because the question is fidelity (input vs. source), not standalone quality."
  - "V1 Verification Role explicit in both bodies (D85, REVW-09) — covers V1 verification; no separate verify-* skill. The literal phrase 'covers V1 verification' AND 'no separate verify-* skill' both appear verbatim in both skill bodies under dedicated ## V1 Verification Role subsections. This is the key documentation contribution of Plan 06-04 — without the explicit statement, a user searching for a 'verify-*' skill would assume verification was forgotten in V1; with the statement, they know review-implementation-* IS the verification answer. The README verification-coverage blockquote backs the in-skill statement up at the README level."
  - "Plan-driven additional checks — per-plan-task commit cadence (D75) + four-state status alignment (D74). Both checks ONLY apply when the source artifact is a plan under docs/threads/<thread>/plans/. When the source is less-structured input (spec / proposal / issue / Inbox item passed to implement-auto / implement-interactive), the plan-driven checks do NOT apply (the cadence is per implicit task, not per plan task; the four-state status reports are still produced but are not matched against plan tasks). The skill body distinguishes these cases explicitly so the executor doesn't run plan-driven checks on non-plan sources."
  - "Two-citation Evidence requirement — every fidelity finding in the ## Evidence section cites BOTH the diff (file:line) AND the source-artifact section. The two-citation requirement is the implementation-review-specific shape — a fidelity finding without both citations is unverifiable (one without the other tells the next reviewer 'something is wrong somewhere'). Carried in both skill bodies; for the interactive sibling, the per-iteration walk format step 2 also requires both citations during the conversation."
  - "Severity vocabulary unchanged: `blocker | issue | nit`. Same shape as Plans 06-01 / 06-02 / 06-03. Per-target meaning of severity differs (e.g., blocker here is 'the source's contract is unmet — the implementation has not delivered what was promised'; in 06-03 it was 'plan cannot reasonably reach implementation in this shape'; in 06-02 it was 'spec cannot be handed downstream')."
  - "Settlement vocabulary unchanged: `resolved | rejected | accepted | deferred | parked`. Same shape as Plans 06-01 / 06-02 / 06-03. Resolved+rejected stay in the decision log; accepted+deferred+parked dump to inbox/open/ at end-of-session per D92/D95."
  - "Implementation-stage stakes amplifier wording — adopted: 'the cheap moment to push back is during the walk, before the implementation is merged or released, before downstream callers depend on the not-quite-right shape; the implementation-stage stakes are particularly sharp because the code is already written — every unflagged fidelity gap becomes a future *but the source said X* conversation with no clean way to recover; V1 implementation skills do not rewind the commits per D77'. Implementation-stage variant of review-spec-interactive's handoff-grade amplifier (Plan 06-02) and review-plan-interactive's plan-stage stakes amplifier (Plan 06-03); the asymmetry here is sharpest because the code is already written, which compounds the cost of unflagged findings differently than the plan or spec stages."
  - "Walk grain flexibility — per-finding OR per-source-acceptance-item OR per-plan-task walk in review-implementation-interactive, executor's discretion. Per-finding default when findings cluster across axes; per-source-acceptance-item default when the source has many discrete acceptance items (the spec-style walk pattern); per-plan-task default when the source is a plan (each plan task surfaces alongside its matching commits / diff hunks). Same flexible-grain pattern as review-spec-interactive (per-element or per-finding) and review-plan-interactive (per-finding or per-task); only the third grain (per-source-acceptance-item) is newly enumerated here because the implementation target's source artifact can come in 5 forms with different acceptance shapes."
  - "Read-only-against-implementation discipline — explicit in both skill bodies. The skill does NOT check out branches, does NOT run tests, does NOT modify the working tree, and does NOT mutate any git state. Code modifications are an implement-* skill's job per Phase 5. The review is read-only against the diff text — if the review surfaces findings that require code changes, the surrounding session invokes the appropriate implement-* skill on a fresh invocation. This discipline keeps the review skill's territory tight: review is review; code is implement-*'s job. (Also explicit in the ## Immutability section.)"
  - "README verification-coverage blockquote placement — directly above the two new review-implementation-* entries as a 1-line `> **Note:**` blockquote. Executor discretion per CONTEXT.md was {italicized line above, blockquote above, inline parenthetical}; chose blockquote for visibility (a user scanning the README for 'verify' will find the keyword in the blockquote text). Per CONTEXT.md the note MUST be ≤3 lines and findable by a user scanning for 'verify' — both requirements satisfied. The full README hybrid is still Phase 7; this is the minimal documentation note REVW-09 requires."
  - "`<kebab-desc>` slug for decision log + review-finding — recommended `<source-slug>-implementation-review` (capturing which source artifact the implementation was checked against) or `<commit-sha-short>-vs-<source-slug>-review` (capturing the implementation reference plus the source). The interactive sibling confirms slug with the user before first settlement; the auto sibling treats the source-artifact slug plus the implementation-reference summary as authoritative."
  - "No `## The Fool Delegation` section — per CONTEXT.md, the the-fool delegation hint is recommended only for review-proposal-* (Plan 06-01) and review-spec-* (Plan 06-02) where adversarial pressure is most useful at the early shape-defining stages. review-implementation-* does not carry the delegation note in the skill body — the README-level adversarial-delegation note is Plan 06-05's territory. Neither skill body in this plan has a `## The Fool Delegation` section."

patterns-established:
  - "review-implementation-* body shape: opening clarifier naming sibling + Phase 5 implement-* family (all 6 implement-* skills by absolute path) + Phase 6 review siblings (review-spec-* upstream, review-plan-* upstream, review-code-* downstream-of-source-independent); ## V1 Verification Role explicit subsection naming the coverage fact (covers V1 verification; no separate verify-* skill); ## Inputs with TWO REQUIRED inputs (implementation reference with 3 forms — git ref / commit range / diff text; source artifact with 5 forms — spec / proposal / plan / GitHub issue / Inbox item); ambiguity fallback per docs/workflow/v1/immutability.md; ## What This Skill Reviews enumerating the five D85 fidelity axes with per-axis severity heuristics + plan-driven additional checks (per-task commit cadence + four-state status alignment); ## Findings Report Shape with 6 required sections (Verdict / Findings / Evidence with TWO citations / References / Open Questions / Next Actions); ## Output Artifact naming inbox/open/<UTC>-<kebab-desc>-review-finding.md per Phase 1 record-form grammar; ## Workflow with READ-ONLY-source-read + READ-ONLY-diff-inspection + 5-axis sequence + plan-driven checks + UTC-stamp + write; ## Commit Policy NEVER auto-commits + does-not-modify-implementation; ## Immutability citing docs/workflow/v1/immutability.md by absolute path + naming both source and implementation as read-only. Interactive sibling adds Anti-Sycophancy Stance (4 markers verbatim + review-stance amplifier + implementation-stage stakes amplifier) + Walk Format (per-finding OR per-source-acceptance-item OR per-plan-task, executor's discretion; per-iteration loop with TWO citations + D89 ASK-AND-TEST against BOTH inputs) + Output Artifacts (always decision-log; conditional review-finding per D92/D95) + Decision Log Lazy Creation + Scope Drift section. Pattern reusable verbatim by Plan 06-05 (review-code-*) with the target-specific axis enumeration (review-code-* drops the source-artifact requirement per D86 and uses its own quality/safety/idiomatic-fit/regression-risk axes)."
  - "Three-grain walk pattern continuity — review-implementation-interactive offers per-finding OR per-source-acceptance-item OR per-plan-task walk grain (the THIRD grain — per-plan-task — is unique to this skill because the source artifact CAN be a plan, in which case the plan-task structure provides a natural walk order). The first two grains carry over from Plan 06-02 (per-element / per-finding for spec review) and Plan 06-03 (per-finding / per-task for plan review). Three grains is the upper bound for V1 walk-format flexibility; Plan 06-05's review-code-* will likely have only one grain (per-finding) because there is no source-artifact structure to walk."
  - "TWO-input + TWO-citation pattern unique to the implementation review target — every other review target (proposal, spec, plan, code) takes ONE primary input. The implementation review takes TWO (the implementation + the source it was supposed to deliver). The two-input pattern drives the two-citation Evidence requirement (every fidelity finding cites BOTH the diff and the source) and the ASK-AND-TEST-against-BOTH walk discipline. Plan 06-05 (review-code-*) will revert to the one-input pattern because review-code-* takes no source artifact per D86."
  - "Verification-coverage documentation pattern — for V1 decisions that subsume what users might expect to find as a separate skill (D85: review-implementation-* covers verification; no separate verify-*; D88: review-* delegates adversarial pressure to the external the-fool skill; no native V1 adversarial review), the documentation contribution is a SHORT in-skill statement (under ## V1 Verification Role or similar) PLUS a SHORT README note (≤3 lines, findable by scanning for the keyword the user would search for — 'verify' here, 'adversarial' in Plan 06-05's territory). Pattern reusable for Plan 06-05's adversarial-delegation README note."

requirements-completed: [REVW-04, REVW-06, REVW-07, REVW-08, REVW-09]

duration: 8min
completed: 2026-05-21
---

# Phase 6 Plan 04: Review-Implementation Pair Summary

**`review-implementation-auto` (autonomous five-axis code-vs-original-intent fidelity review per D85 — acceptance/outcome coverage + constraint adherence + scope adherence + behavior fidelity + test coverage; plan-driven additional checks per D75/D74; 6-section findings-first report to inbox/open/ with TWO citations per finding (diff + source); two-input contract — implementation reference + source artifact from 5 forms) + `review-implementation-interactive` (per-finding OR per-source-acceptance-item OR per-plan-task ASK-and-TEST-against-BOTH walk with 4 anti-sycophancy markers verbatim + review-stance amplifier + implementation-stage stakes amplifier; decision log + conditional inbox dump per D92/D95) + V1 Verification Role explicit (no separate verify-* skill, per D85 / REVW-09) shipped as the fourth paired-review plan in Phase 6; SHORT README verification-coverage blockquote added directly above the new entries to satisfy REVW-09 visibility; pattern continuity with Plans 06-01/06-02/06-03 codified; the TWO-input + TWO-citation contract is the unique twist for the implementation target.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-05-21T13:45:16Z
- **Completed:** 2026-05-21T13:53:56Z
- **Tasks:** 3
- **Files modified:** 5 (2 created, 3 modified)

## Accomplishments

- `review-implementation-auto/SKILL.md` ships the autonomous V1 implementation review per D85: runs the five code-vs-original-intent fidelity axes (acceptance/outcome coverage / constraint adherence / scope adherence / behavior fidelity / test coverage) against the TWO mandatory inputs (an implementation reference — git ref / commit range / branch / saved diff path / inline diff body — PLUS a source artifact from 5 forms matching Phase 5's implement-* inputs: spec / proposal / plan / GitHub issue / Inbox item). For plan-driven implementations, additionally checks per-plan-task commit cadence (D75) and four-state status alignment (D74). Emits a 6-section findings-first report (Verdict / Findings with severity tags `blocker | issue | nit` / Evidence with TWO citations per finding — both file:line in the diff AND the source-artifact section / References / Open Questions / Next Actions) to `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md`. Carries an explicit `## V1 Verification Role` subsection stating this skill covers V1 verification — there is no separate `verify-*` skill in V1.
- `review-implementation-interactive/SKILL.md` ships the collaborative V1 implementation review: per-finding OR per-source-acceptance-item OR per-plan-task walk (executor's discretion — three grains because the source artifact can come in 5 forms with different acceptance shapes); ASKs the user for their view AND TESTs the user's explanation against BOTH the diff AND the source artifact (D89; explicit "do not just accept" phrasing); five-way settlement (resolved / rejected / accepted / deferred / parked); 4 anti-sycophancy markers carried verbatim from `skills/discussion/SKILL.md` + review-stance amplifier ("a review is most valuable when it disagrees with the author") + implementation-stage stakes amplifier ("the cheap moment to push back is during the walk, before the implementation is merged or released ... V1 implementation skills do not rewind the commits"); decision log at `docs/threads/<thread>/discussions/<UTC>-<kebab-desc>-decision-log.md` (lazy creation at first settlement); conditional inbox dump at `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md` per D92/D95 ("no Inbox file when nothing remains"). Carries the same `## V1 Verification Role` subsection as the auto sibling.
- Both new skills cite `skills/implement-plan-auto/SKILL.md` AND `skills/implement-auto/SKILL.md` AND the other four `implement-*` skills (implement-interactive, implement-plan-interactive, implement-plan-with-subagents-auto, implement-plan-with-subagents-interactive) by absolute path as the sources of the implementation outcomes this skill reviews and the 5 source-artifact input shapes. The plan-driven additional checks (per-plan-task commit cadence per D75 + four-state status alignment per D74) are described in dedicated subsections in both bodies with explicit guidance that these checks ONLY apply when the source is a plan.
- Both new skills enforce the TWO-CITATION Evidence requirement — every fidelity finding cites BOTH the diff (file:line) AND the source-artifact section. The interactive sibling's per-iteration walk format step 2 also requires both citations during the conversation. This is the implementation-review-specific shape that distinguishes Plan 06-04 from Plans 06-01/06-02/06-03 (which had ONE primary input and ONE citation requirement).
- Both new skills are explicitly READ-ONLY against the implementation — they do NOT check out branches, do NOT run tests, do NOT modify the working tree, and do NOT mutate any git state. Code modifications are an `implement-*` skill's job per Phase 5; the review's role is review only. This discipline is stated in both the `## Inputs` section (under Input 1) AND the `## Immutability` section AND the `## Commit Policy` section.
- All three V1 registration touchpoints updated atomically:
  - `marketplace.json`: `JeisKappa-workflow.skills` 25 → 27 (`review-implementation-auto` + `review-implementation-interactive` inserted alphabetically between `./skills/propose-interactive` and `./skills/review-plan-auto`); `JeisKappa-skills` unchanged at 7. JSON validity preserved.
  - `.vscode/settings.json`: `conventionalCommits.scopes` 32 → 34 (`review-implementation-auto` + `review-implementation-interactive` added between `report-to-the-owner` and `review-plan-auto`; full array remains alphabetically sorted).
  - `README.md`: Available skills 32 → 34 (both new entries inserted between `propose-interactive` and `review-plan-auto` heading, above the `## Retired skills` subsection); each entry has heading + description paragraph (with explicit interaction mode per DIST-05; mentions Phase 5 implement-* family by name; mentions the five fidelity axes per D85; mentions sibling review-* targets; explicit V1-verification-coverage clause) + fenced `npx skills add` install snippet. **Plus** a SHORT (1-line) `> **Note:**` blockquote inserted directly above the new entries stating: "V1 verification of implementations is covered by `review-implementation-auto` and `review-implementation-interactive` — there is no separate `verify-*` skill in V1 (per D85, REVW-09)." Placed as a blockquote so a user scanning the README for "verify" will find it.
- Pattern continuity: the V1 review-pair body shape codified by Plans 06-01, 06-02, and 06-03 is reused. The deltas for this plan are (a) the five-axis fidelity enumeration replaces the four-axis plan review and the 8 D50 elements of spec review and the lightweight gaps/risks/ambiguities of proposal review, (b) the TWO-input contract is unique to this target (implementation + source artifact), (c) the TWO-citation Evidence requirement is unique to this target (every finding cites both the diff and the source), (d) the V1 Verification Role explicit statement is the documentation contribution of Plan 06-04 (no separate verify-* skill — review-implementation-* IS the verification answer), and (e) the SHORT README verification-coverage blockquote satisfies REVW-09's documentation requirement.

## Task Commits

Each task was committed atomically:

1. **Task 1: Author skills/review-implementation-auto/SKILL.md** — `5568a3c` (feat)
2. **Task 2: Author skills/review-implementation-interactive/SKILL.md** — `b2264e6` (feat)
3. **Task 3: Register review-implementation-* in 3 touchpoints + SHORT README verification-coverage note** — `ff98d33` (chore)

## Files Created/Modified

- `skills/review-implementation-auto/SKILL.md` — autonomous V1 implementation review skill; runs five D85 code-vs-original-intent fidelity axes; two-input contract (implementation reference + source artifact); plan-driven additional checks (per-task commit cadence per D75 + four-state status alignment per D74); emits 6-section findings-first report to `inbox/open/<UTC>-<kebab-desc>-review-finding.md` with TWO citations per finding; covers V1 verification (no separate verify-* skill).
- `skills/review-implementation-interactive/SKILL.md` — collaborative V1 implementation review skill; per-finding OR per-source-acceptance-item OR per-plan-task ASK-and-TEST-against-BOTH walk; emits decision log at `discussions/` + conditional inbox dump at `inbox/open/`; carries the 4 anti-sycophancy markers verbatim from `discussion` + review-stance amplifier + implementation-stage stakes amplifier; covers V1 verification (no separate verify-* skill).
- `.claude-plugin/marketplace.json` — JeisKappa-workflow.skills 25 → 27 (review-implementation-* added alphabetically); JeisKappa-skills unchanged at 7.
- `.vscode/settings.json` — conventionalCommits.scopes 32 → 34 (review-implementation-* added alphabetically; full array remains alphabetical).
- `README.md` — Available skills 32 → 34 (review-implementation-* inserted between `propose-interactive` and `review-plan-auto` heading, above the `## Retired skills` subsection); SHORT (1-line) `> **Note:**` verification-coverage blockquote added directly above the new entries naming review-implementation-* as the V1 verification answer per D85, REVW-09.

## Decisions Made

- **Five-axis fidelity review per D85** — acceptance/outcome coverage + constraint adherence + scope adherence + behavior fidelity + test coverage. Each axis carries explicit severity heuristics tethered to downstream impact. The five-axis enumeration is the structurally richest review target in Phase 6 so far — appropriate because the implementation target's risk surface is wider than spec / proposal / plan (it carries everything the source promised PLUS everything the implementer chose to add). Both skill bodies enumerate the five axes by name in a `## What This Skill Reviews` section with per-axis severity heuristics.
- **TWO mandatory inputs** — implementation reference + source artifact. Without the source artifact, there are no fidelity axes to check against; the user is redirected to `review-code-*` for source-independent code review. This is the key distinction from the other four review targets (which take ONE primary input — the artifact being reviewed). Implementation review NEEDS both inputs because the question is fidelity (input vs. source), not standalone quality.
- **V1 Verification Role explicit in both bodies (D85, REVW-09)** — covers V1 verification; no separate verify-* skill. The literal phrase "covers V1 verification" AND "no separate verify-* skill" both appear verbatim in both skill bodies under dedicated `## V1 Verification Role` subsections. This is the key documentation contribution of Plan 06-04 — without the explicit statement, a user searching for a `verify-*` skill would assume verification was forgotten in V1; with the statement, they know review-implementation-* IS the verification answer. The README verification-coverage blockquote backs the in-skill statement up at the README level.
- **Plan-driven additional checks** — per-plan-task commit cadence (D75) + four-state status alignment (D74). Both checks ONLY apply when the source artifact is a plan under `docs/threads/<thread>/plans/`. When the source is less-structured input (spec / proposal / issue / Inbox item passed to `implement-auto` / `implement-interactive`), the plan-driven checks do NOT apply (the cadence is per implicit task, not per plan task; the four-state status reports are still produced but are not matched against plan tasks). The skill body distinguishes these cases explicitly so the executor doesn't run plan-driven checks on non-plan sources.
- **Two-citation Evidence requirement** — every fidelity finding in the `## Evidence` section cites BOTH the diff (file:line) AND the source-artifact section. The two-citation requirement is the implementation-review-specific shape — a fidelity finding without both citations is unverifiable (one without the other tells the next reviewer "something is wrong somewhere"). Carried in both skill bodies; for the interactive sibling, the per-iteration walk format step 2 also requires both citations during the conversation.
- **Severity vocabulary `blocker | issue | nit`** — adopted verbatim from Plans 06-01 / 06-02 / 06-03 (V1 review-family standard). Per-target meaning of severity differs (e.g., blocker here is "the source's contract is unmet — the implementation has not delivered what was promised"; in 06-03 it was "plan cannot reasonably reach implementation in this shape"; in 06-02 it was "spec cannot be handed downstream").
- **Settlement vocabulary `resolved | rejected | accepted | deferred | parked`** — adopted verbatim from Plans 06-01 / 06-02 / 06-03. Resolved+rejected stay in the decision log; accepted+deferred+parked dump to inbox/open/ at end-of-session per D92/D95.
- **Implementation-stage stakes amplifier wording** — adopted: "the cheap moment to push back is during the walk, before the implementation is merged or released, before downstream callers depend on the not-quite-right shape; the implementation-stage stakes are particularly sharp because the code is already written — every unflagged fidelity gap becomes a future *but the source said X* conversation with no clean way to recover; V1 implementation skills do not rewind the commits per D77". Implementation-stage variant of review-spec-interactive's handoff-grade amplifier (Plan 06-02) and review-plan-interactive's plan-stage stakes amplifier (Plan 06-03); the asymmetry here is sharpest because the code is already written, which compounds the cost of unflagged findings differently than the plan or spec stages.
- **Walk grain flexibility — per-finding OR per-source-acceptance-item OR per-plan-task** — both grain choices documented in `review-implementation-interactive`'s `## Walk Format` section. Per-finding default when findings cluster across axes; per-source-acceptance-item default when the source has many discrete acceptance items (the spec-style walk pattern); per-plan-task default when the source is a plan (each plan task surfaces alongside its matching commits / diff hunks). Three grains because the source artifact can come in 5 forms with different acceptance shapes — this is the upper bound for V1 walk-format flexibility.
- **Read-only-against-implementation discipline** — explicit in both skill bodies. The skill does NOT check out branches, does NOT run tests, does NOT modify the working tree, and does NOT mutate any git state. Code modifications are an `implement-*` skill's job per Phase 5. The review is read-only against the diff text — if the review surfaces findings that require code changes, the surrounding session invokes the appropriate `implement-*` skill on a fresh invocation. This discipline keeps the review skill's territory tight: review is review; code is implement-*'s job.
- **README verification-coverage blockquote placement** — directly above the two new review-implementation-* entries as a 1-line `> **Note:**` blockquote. Executor discretion per CONTEXT.md was {italicized line above, blockquote above, inline parenthetical}; chose blockquote for visibility (a user scanning the README for "verify" will find the keyword in the blockquote text). Per CONTEXT.md the note MUST be ≤3 lines and findable by a user scanning for "verify" — both requirements satisfied. The full README hybrid is still Phase 7; this is the minimal documentation note REVW-09 requires.
- **`<kebab-desc>` slug for decision log + review-finding** — recommended `<source-slug>-implementation-review` (capturing which source artifact the implementation was checked against) or `<commit-sha-short>-vs-<source-slug>-review` (capturing the implementation reference plus the source). The interactive sibling confirms slug with the user before first settlement; the auto sibling treats the source-artifact slug plus the implementation-reference summary as authoritative.
- **No `## The Fool Delegation` section** — per CONTEXT.md, the the-fool delegation hint is recommended only for `review-proposal-*` (Plan 06-01) and `review-spec-*` (Plan 06-02) where adversarial pressure is most useful at the early shape-defining stages. `review-implementation-*` does not carry the delegation note in the skill body — the README-level adversarial-delegation note is Plan 06-05's territory. Neither skill body in this plan has a `## The Fool Delegation` section.

## Deviations from Plan

None — both new skill authoring tasks landed cleanly on the first verify pass; the registration task's three atomic edits across `marketplace.json` + `.vscode/settings.json` + `README.md` all passed every automated verify check (workflow plugin count = 27, skills plugin count = 7, scopes count = 34, scopes alphabetically sorted, README heading counts, install-snippet counts, verification-coverage-note presence, position-before-Retired ordering) on the first run.

The Phase 4 regression guard observed in Plan 06-03 is moot for Plan 06-04 — neither new skill body needs to enforce the D60 no-parallelization contract (that is a plan-content review concern owned by `review-plan-*`), so no descriptive-prose-vs-literal-token concerns arose. The executor's structural-notation negative grep was not triggered.

The TWO-input + TWO-citation pattern is the unique twist of Plan 06-04 relative to Plans 06-01/06-02/06-03 — and it landed cleanly without any rework: both skill bodies carry the two-input contract in `## Inputs`, the two-citation requirement in `## Evidence`, and the read-only-against-both-inputs discipline in `## Immutability`. The interactive sibling additionally carries the ASK-AND-TEST-against-BOTH discipline in `## Walk Format` step 4.

The V1-verification-coverage statement landed in both skill bodies as planned — under a dedicated `## V1 Verification Role` subsection in each. The README blockquote was placed directly above the new entries, satisfying REVW-09's visibility requirement.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Plan 06-05 (review-code-* pair + README adversarial-delegation note) is unblocked. The V1 review-pair body shape codified by Plans 06-01, 06-02, 06-03, and 06-04 is now solidly reusable; the deltas for Plan 06-05 are heavier than the previous transitions but still tractable: (a) the target-specific axis enumeration changes (review-code-* will use its own quality/safety/idiomatic-fit/regression-risk axes per D86, no fidelity-to-source axes), (b) the source-artifact requirement is DROPPED per D86 — review-code-* is general-purpose code review with no source contract (revert to ONE-input pattern; the user is ASKED for specific areas of concern instead), (c) the citations point at the codebase rather than at any Phase 5 implement-* skill (although references to implement-* may be useful in `## Next Actions` for fix recommendations), and (d) the SHORT README adversarial-delegation note replaces this plan's verification-coverage note (different content; same SHORT-blockquote placement pattern as established here). The pattern of {open-clarifier + axis enumeration + 6-section report + conditional inbox dump + decision-log + anti-sycophancy stance (interactive only)} transfers verbatim.
- After this plan: JeisKappa-workflow plugin sits at 27 entries (Phase 6 target 29 — review-code-* still to ship across Plan 06-05); JeisKappa-skills plugin sits at 7 entries (Phase 6 target 7 — final, no further changes to this plugin in Phase 6); .vscode scopes at 34 (Phase 6 target 36 = 27 + 10 new - 1 retired); README Available skills at 34 (same target); README Retired skills subsection unchanged with 2 entries (discussion-loop from Phase 2 + review-decision-document from Plan 06-02); README documentation notes now has 1 entry (the verification-coverage note added by this plan; Plan 06-05 will add the adversarial-delegation note for a total of 2 in Phase 6's final state).
- REVW-09 partial — the verification-coverage README note is COMPLETE; the adversarial-delegation README note is still Plan 06-05's territory. REVW-09 will be fully complete after Plan 06-05.

---
*Phase: 06-review-family*
*Completed: 2026-05-21*

## Self-Check: PASSED

- Files created: skills/review-implementation-auto/SKILL.md, skills/review-implementation-interactive/SKILL.md — both FOUND on disk.
- Files modified: .claude-plugin/marketplace.json, .vscode/settings.json, README.md, .planning/phases/06-review-family/06-04-SUMMARY.md — all FOUND on disk.
- Commits 5568a3c (feat: review-implementation-auto), b2264e6 (feat: review-implementation-interactive), ff98d33 (chore: register 3 touchpoints + README verification-coverage note) — all FOUND in git log.
