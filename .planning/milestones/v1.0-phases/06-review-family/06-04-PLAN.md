---
phase: 06-review-family
plan: 04
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - skills/review-implementation-auto/SKILL.md
  - skills/review-implementation-interactive/SKILL.md
  - .claude-plugin/marketplace.json
  - .vscode/settings.json
  - README.md
autonomous: true
requirements:
  - REVW-04
  - REVW-06
  - REVW-07
  - REVW-08
  - REVW-09

must_haves:
  truths:
    - "User can invoke `review-implementation-auto` with an implementation artifact reference (typically a git ref, a diff, or a commit range — i.e., 'the implementation' produced by one of the Phase 5 implement-* skills) PLUS the source artifact it was supposed to implement (spec / proposal / plan / GitHub issue / Inbox item — the 4–5 input forms named in Phase 5's implement-* skills) and receive a findings-first report at `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md` checking code-vs-original-intent fidelity per D85 (REVW-04). This is V1's verification role — there is no separate `verify-*` skill."
    - "User can invoke `review-implementation-interactive` with the same inputs and walk one finding (or one diff hunk / one source acceptance item) at a time with ASK-and-TEST against both the diff AND the source artifact per D89; resolved/rejected findings stay in `discussions/<UTC>-<kebab-desc>-decision-log.md`; unresolved actionable findings dump to `inbox/open/` only if any remain per D92/D95."
    - "User reading either review-implementation skill body sees explicit citation of the Phase 5 implement-* skills by absolute path (at minimum `skills/implement-plan-auto/SKILL.md` AND `skills/implement-auto/SKILL.md`) as the source of the implementation-shape this skill reviews."
    - "User reading either review-implementation skill body sees an explicit statement that this skill covers V1 verification (no separate `verify-*` skill) per D85 / REVW-09. The literal phrase 'covers V1 verification' or 'V1 verification' + 'no separate verify' (or near-verbatim) MUST appear in both bodies."
    - "User reading `review-implementation-interactive` finds the 4 anti-sycophancy markers verbatim + review-stance amplifier."
    - "User reading `README.md` finds a SHORT (≤3 lines) documentation note near the review-implementation-* entries stating that V1 verification of implementations is covered by `review-implementation-*` and there is no separate `verify-*` skill (per D85, REVW-09)."
    - "User running `npx skills add` finds both new skills installable via JeisKappa-workflow (25 -> 27)."
  artifacts:
    - path: "skills/review-implementation-auto/SKILL.md"
      provides: "Autonomous implementation review — code-vs-original-intent fidelity check (covers V1 verification per D85)"
      contains: "frontmatter with name=review-implementation-auto, version=1.0.0; ## Inputs naming both the implementation reference AND the source artifact (4–5 forms matching Phase 5's implement-* inputs); ## What This Skill Reviews enumerating code-vs-original-intent fidelity; ## V1 Verification Role explicit note (no separate verify-* skill); ## Findings Report Shape with 6 sections; ## Output Artifact naming inbox/open/<UTC>-<kebab-desc>-review-finding.md; citations to Phase 5 implement-* skills + Phase 1 docs"
    - path: "skills/review-implementation-interactive/SKILL.md"
      provides: "Collaborative implementation review — per-finding walk + 4-marker anti-sycophancy + decision log + opt-in inbox dump"
      contains: "frontmatter with name=review-implementation-interactive, version=1.0.0; ## Anti-Sycophancy Stance with 4 verbatim markers + review-stance amplifier; ## Inputs naming both implementation + source; ## V1 Verification Role explicit note; ## Walk Format with per-finding loop; ## Output Artifacts (decision log + conditional inbox dump); D89 ASK-AND-TEST phrasing; D92/D95 conditional-inbox phrasing; citations to Phase 5 implement-* skills"
    - path: ".claude-plugin/marketplace.json"
      provides: "JeisKappa-workflow grows by 2"
      contains: "./skills/review-implementation-auto + ./skills/review-implementation-interactive ADDED (length 25 -> 27)"
    - path: ".vscode/settings.json"
      provides: "Scopes grow by 2; alphabetical"
      contains: "review-implementation-auto + review-implementation-interactive added to conventionalCommits.scopes (length 32 -> 34)"
    - path: "README.md"
      provides: "Available skills grows by 2; new SHORT verification-coverage note added near the review-implementation-* entries"
      contains: "### [`review-implementation-auto`](./skills/review-implementation-auto/SKILL.md) + ### [`review-implementation-interactive`](./skills/review-implementation-interactive/SKILL.md) headings ADDED; SHORT (≤3 lines) verification-coverage note stating that V1 verification of implementations is covered by review-implementation-* and there is no separate verify-* skill (D85, REVW-09)"
  key_links:
    - from: "skills/review-implementation-auto/SKILL.md"
      to: "skills/implement-plan-auto/SKILL.md and skills/implement-auto/SKILL.md (and other Phase 5 implement-* skills)"
      via: "citation by absolute path — defines the implementation outcomes this skill reviews"
      pattern: "skills/implement-(auto|interactive|plan-auto|plan-interactive|plan-with-subagents-auto|plan-with-subagents-interactive)/SKILL.md"
    - from: "skills/review-implementation-interactive/SKILL.md"
      to: "skills/discussion/SKILL.md"
      via: "anti-sycophancy stance carried verbatim"
      pattern: "Disagree when you disagree|Do not treat pushback as correctness"
    - from: "README.md"
      to: "skills/review-implementation-auto/SKILL.md and skills/review-implementation-interactive/SKILL.md"
      via: "SHORT verification-coverage note pointing the reader to these skills as V1's verification answer"
      pattern: "review-implementation"
---

<objective>
Ship the fourth review pair of Phase 6: `review-implementation-auto` and `review-implementation-interactive`, both targeting an implementation artifact (a git ref / diff / commit range — typically the output of one of the Phase 5 implement-* skills) PLUS the source artifact it was supposed to implement (spec / proposal / plan / GitHub issue / Inbox item — the 4–5 input forms named in Phase 5's implement-* skills). Per D85 (REVW-04), this pair performs the CODE-vs-ORIGINAL-INTENT fidelity check — and explicitly covers V1's verification role (there is no separate `verify-*` skill in V1 per the same decision). Add a SHORT documentation note to README.md near the review-implementation-* entries stating this verification-coverage fact (REVW-09).

Purpose: This is the fourth paired review plan. The implementation target's distinguishing trait is that the input is a CODE DIFF / commit range rather than a markdown artifact — but the review still checks against an upstream markdown artifact (the source the implementation was supposed to deliver). The "covers V1 verification" framing is the key documentation contribution this plan makes — without the README note, a user looking for `verify-*` skills will find nothing and assume verification was forgotten; with the note, they know `review-implementation-*` IS the verification answer.

Output: Two new skill folders + three registration files updated + one SHORT README documentation note. After this plan: workflow plugin 25 -> 27; .vscode scopes 32 -> 34; README "Available skills" 32 -> 34 with a new verification-coverage note added inline/near the review-implementation-* block.

Depends on Plan 06-03 because shared marketplace/scopes/README files (sequential serialization) and the paired-review body shape established in Plans 06-01..06-03 is reused.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-review-family/06-CONTEXT.md
@AGENTS.md

@docs/workflow/v1/README.md
@docs/workflow/v1/thread-layout.md
@docs/workflow/v1/filename-grammar.md
@docs/workflow/v1/immutability.md

<!-- Phase 5 implement-* skills — produce the implementation outcomes this skill reviews; also enumerate the source-artifact forms -->
@skills/implement-auto/SKILL.md
@skills/implement-interactive/SKILL.md
@skills/implement-plan-auto/SKILL.md
@skills/implement-plan-interactive/SKILL.md
@skills/implement-plan-with-subagents-auto/SKILL.md
@skills/implement-plan-with-subagents-interactive/SKILL.md

<!-- Anti-sycophancy + walk-pattern source -->
@skills/discussion/SKILL.md
@skills/seeded-discussion/SKILL.md

<!-- Plans 06-01..06-03 — established the review-skill body shape this plan reuses -->
@skills/review-proposal-auto/SKILL.md
@skills/review-spec-auto/SKILL.md
@skills/review-plan-auto/SKILL.md
@skills/review-proposal-interactive/SKILL.md
@skills/review-spec-interactive/SKILL.md
@skills/review-plan-interactive/SKILL.md

<!-- Registration files being edited -->
@.claude-plugin/marketplace.json
@.vscode/settings.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author skills/review-implementation-auto/SKILL.md</name>
  <files>skills/review-implementation-auto/SKILL.md</files>
  <read_first>
    - skills/review-implementation-auto/SKILL.md (the file being created — empty / non-existent)
    - .planning/phases/06-review-family/06-CONTEXT.md (LOCKED: D81 / D85 review-implementation-* covers V1 verification; specifics: skill must cite the 4–5 source-artifact forms from Phase 5's implement-*; review's job is "does the diff implement what THIS source artifact said it would?")
    - skills/implement-auto/SKILL.md (Phase 5 — enumerates the 7 input forms for less-structured-input implementation; this skill should mirror the 4–5 source-artifact forms naming)
    - skills/implement-plan-auto/SKILL.md (Phase 5 — plan-driven implementation; the diff-vs-plan check is a key review path)
    - skills/implement-plan-with-subagents-auto/SKILL.md (Phase 5 — subagent variant; this skill consumes the same implementation outcomes regardless of which Phase 5 skill produced them)
    - skills/review-spec-auto/SKILL.md (Plan 06-02 — closest analog with a "specific contract to check against" character; the spec contract analog here is the source artifact)
    - skills/review-plan-auto/SKILL.md (Plan 06-03 — sibling with "input + optional source" structure; this skill follows the same pattern with source MANDATORY)
    - docs/workflow/v1/filename-grammar.md
    - docs/workflow/v1/thread-layout.md
    - docs/workflow/v1/immutability.md
  </read_first>
  <action>
    Create the file with YAML frontmatter and a markdown body. Frontmatter required fields:
    - `name: review-implementation-auto`
    - `description:` ONE sentence with "Use when..." trigger AND explicit interaction mode (autonomous; checks code-vs-original-intent fidelity; covers V1 verification — no separate verify-* skill; emits findings-first report; no clarifying questions). Mention sibling `review-implementation-interactive`. Mention the Phase 5 implement-* family.
    - `metadata.author: https://github.com/Jei-sKappa`
    - `metadata.version: 1.0.0`

    Body sections (executor discretion on exact section names; behavioral tokens below MUST appear):

    1. **Opening clarifier paragraph** naming the sibling `review-implementation-interactive` and at least one other review target. State that this skill performs CODE-vs-ORIGINAL-INTENT fidelity check per D85.

    2. **V1 Verification Role** subsection or short paragraph. MUST state explicitly: this skill covers V1 verification of implementations. There is NO separate `verify-*` skill in V1; verification is subsumed by implementation review per D85 (REVW-04 / REVW-09). The literal phrase "covers V1 verification" or "V1 verification" + "no separate verify" (executor MAY rephrase but the verification-coverage fact MUST be unambiguous) MUST appear.

    3. **Inputs** section. MUST require TWO inputs:
       - **An implementation reference** — typically a git ref (commit SHA, branch name, or commit range like `main..feature-branch`) or a diff (unified diff text passed inline or via a path). Acceptable forms: git ref, diff text, path to a saved diff file. This is "the implementation" that was produced — typically by one of the Phase 5 implement-* skills.
       - **A source artifact** — the markdown artifact the implementation was supposed to deliver. MUST enumerate the 4–5 input forms matching Phase 5's implement-* inputs (per CONTEXT.md specifics):
         1. A spec artifact path under `docs/threads/<thread>/specs/`
         2. A proposal artifact path under `docs/threads/<thread>/proposals/`
         3. A plan artifact path under `docs/threads/<thread>/plans/`
         4. A GitHub issue URL or `owner/repo#NNN` identifier
         5. An Inbox item path under `docs/threads/<thread>/inbox/open/` or `inbox/processed/`

       Ambiguity fallback per `docs/workflow/v1/immutability.md` ("Ambiguous Reference Resolution") — ASK if multiple plausible candidates for EITHER input. Do not pick by recency.

    4. **What This Skill Reviews** section. MUST enumerate the code-vs-original-intent fidelity axes (executor discretion on exact subdivision; the core question is "does the implementation deliver what THIS source artifact said it would?"):
       - Acceptance / outcome coverage — does every acceptance item (spec / plan task / issue acceptance criterion / Inbox item resolution) have a corresponding change in the diff?
       - Constraint adherence — does the implementation respect the source's constraints (tech / repo / safety / API contracts)?
       - Scope adherence — is the implementation INSIDE the source's stated scope? Out-of-scope changes (scope drift) are findings.
       - Behavior fidelity — does the implementation exhibit the EXPECTED BEHAVIORS the source named? Missing behaviors are findings; unexpected new behaviors are findings.
       - Test coverage — does the implementation add tests or update existing ones consistent with the source's acceptance guidance? (When applicable — e.g., a doc-only Inbox item resolution may not require tests.)

       For PLAN-driven implementations specifically (input source = a plan artifact under `plans/`): the review SHOULD additionally check that each plan task has a corresponding commit / diff hunk (per the Phase 5 D75 "per plan task" commit cadence) and that the four-state status protocol (D74) from the implementation's task reports matches the diff state.

       NOTE: this skill does NOT perform general-purpose code review independent of source artifacts — that is `review-code-*`'s territory (Plan 06-05). If the source artifact is unavailable or the user wants spec-independent code review, point at `review-code-auto` / `review-code-interactive` instead.

    5. **Findings Report Shape** section. Same 6 sections as Plans 06-01..06-03: `## Verdict` / `## Findings` (severity tags) / `## Evidence` (cite both file:line in the diff AND the source-artifact section) / `## References` (the diff/ref + the source artifact path + any related decision logs by absolute path + `D<N>`) / `## Open Questions` / `## Next Actions`. List all 6 by literal name.

    6. **Output Artifact** section. Emit to `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md`; `review-finding` artifact-type token MANDATORY per `docs/workflow/v1/filename-grammar.md`. ONE record per review run.

    7. **Workflow** section with numbered steps:
       - Resolve thread + the TWO inputs (implementation reference + source artifact). ASK on ambiguity for either input.
       - Read the source artifact READ-ONLY per `docs/workflow/v1/immutability.md`.
       - Inspect the implementation: read the diff or check out the git ref (READ-ONLY — do not check out and run anything destructive; the review is READ-ONLY against the diff).
       - Map source acceptance items to diff hunks.
       - Run the 5 fidelity axes (acceptance coverage / constraint adherence / scope adherence / behavior fidelity / test coverage).
       - For plan-driven implementations: also check per-plan-task commit cadence and four-state status alignment.
       - Draft the findings-first report covering all 6 sections.
       - Capture the UTC stamp per `docs/workflow/v1/filename-grammar.md`.
       - Write the artifact to `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md`.
       - Confirm: `Review written: <relative-path-to-the-file>`. No closing remark.

    8. **Commit Policy** section. NEVER auto-commits the review artifact.

    9. **Immutability** section. Emitted review-finding immutable; the source artifact is READ-ONLY; the implementation diff is READ-ONLY (this skill does NOT modify source code — code modifications are an implement-* skill's job per Phase 5). Cite `docs/workflow/v1/immutability.md` by absolute path.

    Voice and discipline:
    - Dense, declarative, citation-first.
    - Cite Phase 1 docs by absolute path.
    - Cite AT LEAST `skills/implement-plan-auto/SKILL.md` AND `skills/implement-auto/SKILL.md` by absolute path (the most common implementation-producing skills).
    - NO `## Anti-Sycophancy Stance` section (this is the AUTO half).
    - NO decision-log output (interactive sibling's territory).
    - The V1-verification-coverage statement MUST be unambiguous.
    - NO claim to perform general-purpose code review independent of a source artifact — that is review-code-*'s territory.
  </action>
  <verify>
    <automated>test -f skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qxF 'name: review-implementation-auto' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qxF '  version: 1.0.0' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qxF '  author: https://github.com/Jei-sKappa' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qF 'Use when' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qF 'review-implementation-interactive' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/inbox/open/' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qF 'review-finding' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/filename-grammar.md' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/thread-layout.md' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/immutability.md' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qF 'skills/implement-plan-auto/SKILL.md' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qF 'skills/implement-auto/SKILL.md' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qiE 'V1 verification|covers.*verification|covers V1 verification' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qiE 'no separate verify|no separate.*verify-\*|there is no separate verify' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qiE 'code.{0,5}vs.{0,5}original|original intent|original-intent|code-vs-original' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/specs/' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/proposals/' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/plans/' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qiE 'GitHub issue|github.com|owner/repo#' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qiF 'verdict' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qiF 'findings' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qiF 'evidence' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qiF 'references' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qiF 'open questions' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qiF 'next actions' skills/review-implementation-auto/SKILL.md &amp;&amp; grep -qiE 'NEVER (commit|auto-commit)|never auto-commit|never commit' skills/review-implementation-auto/SKILL.md &amp;&amp; ! grep -qxF '## Anti-Sycophancy Stance' skills/review-implementation-auto/SKILL.md</automated>
  </verify>
  <done>skills/review-implementation-auto/SKILL.md exists with frontmatter (name=review-implementation-auto, version=1.0.0) and a body covering: opening clarifier with sibling reference; V1 Verification Role explicit statement (covers V1 verification; no separate verify-* skill); Inputs naming TWO required inputs (implementation reference + source artifact from 5 forms); What This Skill Reviews enumerating code-vs-original-intent fidelity axes (acceptance coverage / constraint adherence / scope adherence / behavior fidelity / test coverage); Findings Report Shape with 6 sections; Output Artifact at inbox/open/<UTC>-<kebab-desc>-review-finding.md; Workflow with mandatory two-input resolution + per-axis check + write-review steps; Commit Policy NEVER auto-commits; Immutability. Cites Phase 5 implement-plan-auto AND implement-auto by absolute path. NO Anti-Sycophancy section.</done>
</task>

<task type="auto">
  <name>Task 2: Author skills/review-implementation-interactive/SKILL.md</name>
  <files>skills/review-implementation-interactive/SKILL.md</files>
  <read_first>
    - skills/review-implementation-interactive/SKILL.md (the file being created — empty / non-existent)
    - skills/review-implementation-auto/SKILL.md (Task 1 output — sibling consistency)
    - .planning/phases/06-review-family/06-CONTEXT.md
    - skills/discussion/SKILL.md (anti-sycophancy 4 markers verbatim)
    - skills/seeded-discussion/SKILL.md (per-finding walk shape)
    - skills/implement-interactive/SKILL.md (Phase 5 — interactive sibling for less-structured-input implementation; the execution-time stakes amplifier reference)
    - skills/implement-plan-interactive/SKILL.md (Phase 5 — interactive sibling for plan-driven; per-task / per-commit walk pattern source)
    - skills/review-proposal-interactive/SKILL.md (Plan 06-01)
    - skills/review-spec-interactive/SKILL.md (Plan 06-02)
    - skills/review-plan-interactive/SKILL.md (Plan 06-03)
    - docs/workflow/v1/filename-grammar.md
    - docs/workflow/v1/thread-layout.md
    - docs/workflow/v1/immutability.md
  </read_first>
  <action>
    Create the file with YAML frontmatter and a markdown body. Frontmatter required fields:
    - `name: review-implementation-interactive`
    - `description:` ONE sentence with "Use when..." trigger + explicit interaction mode (collaborative; walks one finding / one diff hunk / one source acceptance item at a time; ASKS user's view AND TESTS against both the diff AND the source artifact; writes decision log; opt-in inbox dump). Mention sibling `review-implementation-auto`. Mention Phase 5 implement-* family.
    - `metadata.author: https://github.com/Jei-sKappa`
    - `metadata.version: 1.0.0`

    Body sections (executor discretion on names; behavioral tokens below MUST appear):

    1. **Opening clarifier paragraph** naming the sibling `review-implementation-auto` + at least one other review target.

    2. **Anti-Sycophancy Stance** section. Carry the FOUR markers from `skills/discussion/SKILL.md` VERBATIM:
       - "Disagree when you disagree"
       - "Push back on weak or incomplete reasoning"
       - "Do not treat pushback as correctness"
       - "Refuse to log" (clause)

       PLUS the REVIEW-STANCE AMPLIFIER: "A review is most valuable when it disagrees with the author" or near-verbatim.

    3. **V1 Verification Role** subsection or short paragraph. Same explicit statement as `review-implementation-auto`: covers V1 verification; no separate verify-* skill (D85, REVW-09).

    4. **Inputs** section. Same TWO required inputs as `review-implementation-auto`: implementation reference + source artifact (5 forms). Ambiguity fallback.

    5. **What This Skill Reviews** section. Same code-vs-original-intent fidelity axes as `review-implementation-auto`.

    6. **Walk Format** section. MUST document the per-finding OR per-source-acceptance-item walk:
       - Either walk per finding (impact-ordered) or per source acceptance item (map each source acceptance item to the diff hunks that implement it). Executor discretion.
       - For each step:
         1. Surface the finding (or the source item + matching diff hunks).
         2. Cite evidence — file:line in the diff AND the source-artifact section.
         3. ASK the user for their view.
         4. TEST the user's explanation against BOTH the diff AND the source. Do NOT just accept. (D89 phrase with `ASK` + `TEST` + `do not just accept` MUST appear.)
         5. Settle: resolved / rejected / accepted / deferred / parked.
         6. Append a record to the decision log with the outcome.

    7. **Output Artifacts** section (TWO conditional outputs per D92/D95):
       - **Decision log** (ALWAYS when ≥1 finding settled): `docs/threads/<thread>/discussions/<UTC>-<kebab-desc>-decision-log.md`; `decision-log` artifact-type MANDATORY.
       - **Unresolved-findings inbox dump** (CONDITIONAL): `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md`. Literal "no Inbox file when nothing remains" phrasing MUST appear.

    8. **Workflow** section with numbered steps:
       - Resolve thread + the two inputs.
       - Read source READ-ONLY; inspect diff READ-ONLY.
       - Identify candidate findings (or source acceptance walk order).
       - Confirm with user before starting.
       - Per-finding loop from `## Walk Format`.
       - At end: write inbox-dump IF unresolved actionable findings remain; else NO inbox file.
       - Final message: cite decision log path; cite inbox-dump path if written, else "no unresolved findings — no inbox file written".

    9. **Decision Log** lazy-creation rule.

    10. **Scope Drift** section. PARK / SPLIT / DEFER.

    11. **Commit Policy** section. NEVER auto-commits.

    12. **Immutability** section. Source artifact READ-ONLY; diff READ-ONLY; emitted artifacts immutable; cite `docs/workflow/v1/immutability.md` by absolute path. (Code modifications go through implement-* skills, not this skill.)

    Voice and discipline:
    - 4 anti-sycophancy markers verbatim; review-stance amplifier present.
    - D89 ASK-AND-TEST phrasing present.
    - D92/D95 conditional-inbox phrasing present.
    - V1-verification-coverage statement present.
    - Cite Phase 1 + Phase 5 docs by absolute path.
    - NO general-purpose code review claim — that is review-code-*'s territory.
  </action>
  <verify>
    <automated>test -f skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qxF 'name: review-implementation-interactive' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qxF '  version: 1.0.0' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qxF '  author: https://github.com/Jei-sKappa' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qF 'Use when' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qF 'review-implementation-auto' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qiF 'anti-sycophancy' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qF 'Disagree when you disagree' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qF 'Push back on weak or incomplete reasoning' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qF 'Do not treat pushback as correctness' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qF 'Refuse to log' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qiE 'review is most valuable when it disagrees|disagrees with the author|push back hard' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/discussions/' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/inbox/open/' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/specs/' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/proposals/' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/plans/' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qF 'decision-log' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qF 'review-finding' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/filename-grammar.md' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/thread-layout.md' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/immutability.md' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qF 'skills/implement-plan-auto/SKILL.md' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qF 'skills/implement-auto/SKILL.md' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qiE 'V1 verification|covers.*verification' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qiE 'no separate verify|there is no separate verify' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qiE 'code.{0,5}vs.{0,5}original|original intent|original-intent' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qiE 'ASK.*TEST|TEST.*ASK' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qiE 'do not just accept|don.t just accept' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qiE 'no Inbox file|no inbox dump|nothing remains|no inbox file when nothing remains|all findings resolved' skills/review-implementation-interactive/SKILL.md &amp;&amp; grep -qiE 'NEVER (commit|auto-commit)|never auto-commit|never commit' skills/review-implementation-interactive/SKILL.md</automated>
  </verify>
  <done>skills/review-implementation-interactive/SKILL.md exists with frontmatter (name=review-implementation-interactive, version=1.0.0) and body covering: opening clarifier; Anti-Sycophancy Stance with 4 verbatim markers + review-stance amplifier; V1 Verification Role explicit (covers V1 verification; no separate verify-*); Inputs (implementation ref + source artifact from 5 forms); What This Skill Reviews (code-vs-original-intent fidelity axes); Walk Format with per-finding/per-source-acceptance loop + D89 ASK-AND-TEST + do-not-just-accept; Output Artifacts (decision log + conditional inbox dump with D92/D95 phrasing); Workflow; lazy decision-log; Scope Drift; Commit Policy NEVER auto-commits; Immutability. Cites implement-plan-auto AND implement-auto by absolute path.</done>
</task>

<task type="auto">
  <name>Task 3: Register review-implementation-* in marketplace.json, .vscode/settings.json, README.md + add SHORT verification-coverage README note</name>
  <files>.claude-plugin/marketplace.json, .vscode/settings.json, README.md</files>
  <read_first>
    - .claude-plugin/marketplace.json (state AFTER Plan 06-03: JeisKappa-workflow has 25 entries; JeisKappa-skills has 7)
    - .vscode/settings.json (state AFTER Plan 06-03: 32 alphabetically-sorted scopes)
    - README.md (state AFTER Plan 06-03: 32 "Available skills" entries; Retired skills subsection contains discussion-loop + review-decision-document bullets)
    - AGENTS.md (4-touchpoint rule)
    - .planning/phases/06-review-family/06-CONTEXT.md (LOCKED: README documentation note for verification per REVW-09, D85; SHORT note ≤3 lines)
    - skills/review-implementation-auto/SKILL.md (Task 1 output)
    - skills/review-implementation-interactive/SKILL.md (Task 2 output)
  </read_first>
  <action>
    Update all three registration files atomically:

    1. **`.claude-plugin/marketplace.json`** — add `"./skills/review-implementation-auto"` and `"./skills/review-implementation-interactive"` to `JeisKappa-workflow.skills`. Alphabetically these fall before `review-plan-auto`. So insert BETWEEN whatever comes before `review-plan-auto` (after Plan 06-03: `./skills/propose-interactive`) and `./skills/review-plan-auto`. After: JeisKappa-workflow array has exactly 27 entries (25 + 2). JeisKappa-skills unchanged at 7.

    2. **`.vscode/settings.json`** — add `"review-implementation-auto"` and `"review-implementation-interactive"` to `."conventionalCommits.scopes"` in alphabetical position (between `review-decision-document` if it had remained, or whatever precedes `review-plan-auto` — note that `review-decision-document` was removed in Plan 06-02). Net length 34 (32 + 2). Keep alphabetical. The jq accessor MUST use the quoted form `."conventionalCommits.scopes"`.

    3. **`README.md`** — TWO changes:
       a. ADD two new "Available skills" entries (`### [\`review-implementation-auto\`](./skills/review-implementation-auto/SKILL.md)` + `### [\`review-implementation-interactive\`](./skills/review-implementation-interactive/SKILL.md)`), each with description paragraph + fenced `npx skills add Jei-sKappa/skills --skill <name>` block. Insert AFTER any preceding alphabetical entry (likely `propose-interactive` or whatever sits alphabetically before `review-implementation-` — `review-decision-document` was removed in Plan 06-02 so the position is now after `propose-interactive`/`report-to-the-owner`) and BEFORE `review-plan-auto`. Position both new entries together (auto then interactive). Description sentences MUST state explicit interaction mode per DIST-05 AND mention that this skill covers V1 verification (no separate `verify-*`).
       b. ADD a SHORT (≤3 lines) documentation note inline with or directly above the new `review-implementation-*` entries stating that V1 verification of implementations is covered by `review-implementation-*` — there is no separate `verify-*` skill (per D85, REVW-09). Suggested text (executor may rephrase; the verification-coverage fact MUST be unambiguous and the note MUST be ≤3 lines):

          > **Note:** V1 verification of implementations is covered by `review-implementation-auto` and `review-implementation-interactive` — there is no separate `verify-*` skill in V1.

       Place the note either as an italicized line ABOVE the two review-implementation-* entries OR as a blockquote DIRECTLY above them OR as a parenthetical inline with the first review-implementation-* entry. Executor discretion provided REVW-09 visibility is satisfied (the note MUST be findable by a user scanning the README for `verify`).

    Do NOT touch the `## Retired skills` subsection.
    Do NOT add the the-fool delegation README note (that is Plan 06-05's job).
    Do NOT touch any unrelated entry.
    Do NOT commit.
  </action>
  <verify>
    <automated>jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length == 27' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/review-implementation-auto")' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/review-implementation-interactive")' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '.plugins[] | select(.name=="JeisKappa-skills") | .skills | length == 7' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" | length == 34' .vscode/settings.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" | index("review-implementation-auto")' .vscode/settings.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" | index("review-implementation-interactive")' .vscode/settings.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" == (."conventionalCommits.scopes" | sort)' .vscode/settings.json &gt;/dev/null &amp;&amp; [ "$(grep -c '^### \[`review-implementation-auto`\]' README.md)" = "1" ] &amp;&amp; [ "$(grep -c '^### \[`review-implementation-interactive`\]' README.md)" = "1" ] &amp;&amp; [ "$(grep -c 'npx skills add Jei-sKappa/skills --skill review-implementation-auto' README.md)" = "1" ] &amp;&amp; [ "$(grep -c 'npx skills add Jei-sKappa/skills --skill review-implementation-interactive' README.md)" = "1" ] &amp;&amp; grep -qiE 'no separate.*verify|verification.{0,30}review-implementation|review-implementation.{0,30}verification' README.md &amp;&amp; A=$(grep -n '^### \[`review-implementation-auto`\]' README.md | cut -d: -f1) &amp;&amp; B=$(grep -n '^### \[`review-implementation-interactive`\]' README.md | cut -d: -f1) &amp;&amp; R=$(grep -n '^## Retired skills' README.md | cut -d: -f1) &amp;&amp; [ "$A" -lt "$R" ] &amp;&amp; [ "$B" -lt "$R" ] &amp;&amp; echo OK</automated>
  </verify>
  <done>marketplace.json JeisKappa-workflow array has 27 entries (was 25, +2). .vscode/settings.json scopes has 34 entries (32 + 2), alphabetical. README.md has 2 new "Available skills" entries inserted before `## Retired skills` AND a SHORT verification-coverage note linking review-implementation-* to V1's verification role (no separate verify-* skill). No git commit created.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

1. **Skill files exist:**
   ```bash
   ls -la skills/review-implementation-auto/SKILL.md skills/review-implementation-interactive/SKILL.md
   ```

2. **Auto skill structural tokens:**
   ```bash
   for TOKEN in "skills/implement-plan-auto/SKILL.md" "skills/implement-auto/SKILL.md" "review-finding" "docs/threads/<thread>/inbox/open/" "docs/threads/<thread>/specs/" "docs/threads/<thread>/proposals/" "docs/threads/<thread>/plans/"; do
     grep -qF "$TOKEN" skills/review-implementation-auto/SKILL.md || echo "FAIL auto: $TOKEN"
   done
   grep -qiE 'V1 verification|covers.*verification' skills/review-implementation-auto/SKILL.md || echo "FAIL V1 verification statement"
   grep -qiE 'no separate verify' skills/review-implementation-auto/SKILL.md || echo "FAIL no-separate-verify statement"
   ! grep -qxF '## Anti-Sycophancy Stance' skills/review-implementation-auto/SKILL.md || echo "FAIL auto must NOT contain Anti-Sycophancy section"
   ```

3. **Interactive skill anti-sycophancy + D89 + D92/D95 + V1-verification:**
   ```bash
   for TOKEN in "Disagree when you disagree" "Push back on weak or incomplete reasoning" "Do not treat pushback as correctness" "Refuse to log"; do
     grep -qF "$TOKEN" skills/review-implementation-interactive/SKILL.md || echo "FAIL interactive: $TOKEN"
   done
   grep -qiE 'ASK.*TEST|TEST.*ASK' skills/review-implementation-interactive/SKILL.md || echo "FAIL D89"
   grep -qiE 'no Inbox file|no inbox dump|nothing remains' skills/review-implementation-interactive/SKILL.md || echo "FAIL D92/D95"
   grep -qiE 'V1 verification|covers.*verification' skills/review-implementation-interactive/SKILL.md || echo "FAIL V1 verification statement"
   ```

4. **Four-touchpoint registration + README verification note:**
   ```bash
   jq '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length' .claude-plugin/marketplace.json    # expect 27
   jq '."conventionalCommits.scopes" | length' .vscode/settings.json                                              # expect 34
   grep -cE '^### \[`review-implementation-(auto|interactive)`\]' README.md                                       # expect 2
   grep -qiE 'no separate.*verify|verification.{0,30}review-implementation|review-implementation.{0,30}verification' README.md || echo "FAIL README verification note (REVW-09)"
   ```
</verification>

<success_criteria>
- skills/review-implementation-auto/SKILL.md and skills/review-implementation-interactive/SKILL.md exist with correct V1 frontmatter and bodies.
- Both skills enumerate TWO inputs (implementation reference + source artifact from 5 forms).
- Both skills check code-vs-original-intent fidelity per D85 (REVW-04).
- Both skills explicitly state they cover V1 verification (no separate verify-* skill) per D85 / REVW-09.
- Both skills cite at least implement-plan-auto AND implement-auto by absolute path.
- review-implementation-interactive carries all 4 anti-sycophancy markers verbatim + review-stance amplifier.
- README.md gains a SHORT (≤3 lines) documentation note linking review-implementation-* to V1's verification role.
- marketplace.json JeisKappa-workflow array has exactly 27 entries (25 -> 27).
- .vscode/settings.json conventionalCommits.scopes has exactly 34 entries (32 -> 34), alphabetically sorted.
- README.md has 2 new entries inserted before `## Retired skills`.
- No commit created.
</success_criteria>

<output>
Create `.planning/phases/06-review-family/06-04-SUMMARY.md` when done. Record:
- Files created: skills/review-implementation-auto/SKILL.md, skills/review-implementation-interactive/SKILL.md
- Files modified: .claude-plugin/marketplace.json (workflow 25 -> 27), .vscode/settings.json (scopes 32 -> 34), README.md (Available skills 32 -> 34 + new verification-coverage note added near review-implementation-* entries per REVW-09)
- Requirements addressed: REVW-04 (code-vs-original-intent fidelity check per D85), REVW-06/07/08 (review-shape contract applied to the implementation target), REVW-09 partially via the README verification-coverage note (Plan 06-05 will add the README the-fool delegation note to finish REVW-09)
- V1 verification role established: no separate verify-* skill; review-implementation-* IS the verification answer
</output>
