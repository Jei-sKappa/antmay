---
phase: 06-review-family
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - skills/review-plan-auto/SKILL.md
  - skills/review-plan-interactive/SKILL.md
  - .claude-plugin/marketplace.json
  - .vscode/settings.json
  - README.md
autonomous: true
requirements:
  - REVW-03
  - REVW-06
  - REVW-07
  - REVW-08

must_haves:
  truths:
    - "User can invoke `review-plan-auto` against an emitted V1 plan artifact path under `docs/threads/<thread>/plans/` (loose OR strict granularity from any Phase 4 plan-* skill) and receive a findings-first report at `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md` covering source-spec adherence + project conventions + granularity-fit + (for strict plans) per-task ambiguity check per D83."
    - "User can invoke `review-plan-interactive` against the same plan path and walk one finding (or one task) at a time with ASK-and-TEST against the artifact (D89); resolved/rejected findings stay in `discussions/<UTC>-<kebab-desc>-decision-log.md`; unresolved actionable findings dump to `inbox/open/<UTC>-<kebab-desc>-review-finding.md` only if any remain (D92/D95)."
    - "User reading either review-plan skill body sees a loose-vs-strict detection step — the skill explicitly checks whether the input plan is loose-granularity (1–3 sentence tasks) or strict-granularity (six-field tasks: objective / input / steps / files modified / verification / acceptance) and applies the appropriate ambiguity check. The strict-plan ambiguity check is mandatory; the loose-plan ambiguity check is a granularity-fit signal (i.e., 'this should be strict')."
    - "User reading either review-plan skill body sees explicit citation of `skills/plan-loose-auto/SKILL.md` AND `skills/plan-strict-auto/SKILL.md` by absolute path as the source of the loose-vs-strict plan shape and the D59 contract (sequential / isolated / independently implementable)."
    - "User reading either review-plan skill body sees the D60 no-parallelization contract acknowledged — V1 plans MUST NOT contain wave numbers, dependency arrays, or task-graph notation; the review SHOULD flag any such constructs as findings."
    - "User reading `review-plan-interactive` finds the 4 anti-sycophancy markers verbatim + the review-stance amplifier."
    - "User running `npx skills add` finds both new skills installable via JeisKappa-workflow plugin after registration (23 -> 25)."
  artifacts:
    - path: "skills/review-plan-auto/SKILL.md"
      provides: "Autonomous plan review skill — source-adherence + project conventions + granularity-fit + strict-plan ambiguity check; emits findings-first report to inbox/open/"
      contains: "frontmatter with name=review-plan-auto, version=1.0.0; ## Inputs naming plans/ path (loose OR strict); ## Loose vs Strict Detection step; ## What This Skill Reviews enumerating 4 review axes (source adherence / project conventions / granularity fit / per-task ambiguity for strict); ## Findings Report Shape with 6 sections; ## Output Artifact naming inbox/open/<UTC>-<kebab-desc>-review-finding.md; D59 sequential-isolated-independent contract acknowledged; D60 no-parallelization contract acknowledged; citations to skills/plan-loose-auto/SKILL.md + skills/plan-strict-auto/SKILL.md + Phase 1 docs"
    - path: "skills/review-plan-interactive/SKILL.md"
      provides: "Collaborative plan review — per-finding or per-task walk + 4-marker anti-sycophancy + decision log + opt-in inbox dump"
      contains: "frontmatter with name=review-plan-interactive, version=1.0.0; ## Anti-Sycophancy Stance with 4 verbatim markers + review-stance amplifier; ## Loose vs Strict Detection; ## Walk Format (per-finding or per-task); ## Output Artifacts (decision log + conditional inbox dump); D89 ASK-AND-TEST phrasing; D92/D95 conditional-inbox phrasing; citations to plan-* skills + Phase 1 docs"
    - path: ".claude-plugin/marketplace.json"
      provides: "JeisKappa-workflow grows by 2"
      contains: "./skills/review-plan-auto + ./skills/review-plan-interactive ADDED (length 23 -> 25)"
    - path: ".vscode/settings.json"
      provides: "Scopes grow by 2; alphabetical"
      contains: "review-plan-auto + review-plan-interactive added to conventionalCommits.scopes (length 30 -> 32)"
    - path: "README.md"
      provides: "Available skills grows by 2"
      contains: "### [`review-plan-auto`](./skills/review-plan-auto/SKILL.md) + ### [`review-plan-interactive`](./skills/review-plan-interactive/SKILL.md) headings ADDED before `## Retired skills`"
  key_links:
    - from: "skills/review-plan-auto/SKILL.md"
      to: "skills/plan-loose-auto/SKILL.md and skills/plan-strict-auto/SKILL.md"
      via: "citation by absolute path — defines the loose-vs-strict plan shape this skill reviews against"
      pattern: "skills/plan-(loose|strict)-auto/SKILL.md"
    - from: "skills/review-plan-interactive/SKILL.md"
      to: "skills/discussion/SKILL.md"
      via: "anti-sycophancy stance carried verbatim"
      pattern: "Disagree when you disagree|Do not treat pushback as correctness"
    - from: ".claude-plugin/marketplace.json"
      to: "skills/review-plan-auto/ and skills/review-plan-interactive/"
      via: "JeisKappa-workflow.skills entries"
      pattern: "./skills/review-plan-(auto|interactive)"
---

<objective>
Ship the third review pair of Phase 6: `review-plan-auto` and `review-plan-interactive`, both targeting V1 plan artifacts (loose OR strict granularity from any Phase 4 plan-* skill). Per D83, both check the plan against four axes: source-spec adherence (does the plan deliver what the source artifact promised?), project conventions (does the plan follow the project's coding / testing / structural patterns?), granularity-fit (is loose appropriate for THIS input + THIS implementer? is strict appropriate?), and per-task ambiguity — the LAST axis is MANDATORY for strict plans (where the six-field per-task block must leave no inference required) and is a granularity-fit signal for loose plans (a loose task that hides too much inference is a loose-vs-strict mis-fit). Both honor the D59 sequential-isolated-independent contract and the D60 no-parallelization rule as review criteria. Register both skills in the three V1 touchpoints.

Purpose: This is the third paired review plan, reusing the body shape established in Plans 06-01 and 06-02. The plan target is more structurally rich than the proposal target (D81 lightweight) and matches the spec target's "specific contract to check against" character (D82) — but the contract here is plan-loose vs plan-strict from Phase 4, not the 8 D50 elements from Phase 3. The loose-vs-strict detection step is the unique twist for this pair; everything else follows the established pattern.

Output: Two new skill folders + three registration files updated. After this plan: workflow plugin 23 -> 25; .vscode scopes 30 -> 32; README "Available skills" 30 -> 32.

Depends on Plan 06-02 because (a) the marketplace.json + .vscode/settings.json + README.md files are shared (sequential serialization), and (b) the paired-review body shape established in Plans 06-01 and 06-02 is reused here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-review-family/06-CONTEXT.md
@AGENTS.md

@docs/workflow/v1/README.md
@docs/workflow/v1/thread-layout.md
@docs/workflow/v1/filename-grammar.md
@docs/workflow/v1/immutability.md

<!-- Phase 4 plan skills — define the loose-vs-strict shapes this skill reviews against -->
@skills/plan-loose-auto/SKILL.md
@skills/plan-loose-interactive/SKILL.md
@skills/plan-strict-auto/SKILL.md
@skills/plan-strict-interactive/SKILL.md
@skills/adjust-plan-granularity-auto/SKILL.md

<!-- Anti-sycophancy + walk-pattern source -->
@skills/discussion/SKILL.md
@skills/seeded-discussion/SKILL.md

<!-- Plans 06-01 and 06-02 — established the review-skill body shape this plan reuses -->
@skills/review-proposal-auto/SKILL.md
@skills/review-proposal-interactive/SKILL.md
@skills/review-spec-auto/SKILL.md
@skills/review-spec-interactive/SKILL.md

<!-- Registration files being edited -->
@.claude-plugin/marketplace.json
@.vscode/settings.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author skills/review-plan-auto/SKILL.md</name>
  <files>skills/review-plan-auto/SKILL.md</files>
  <read_first>
    - skills/review-plan-auto/SKILL.md (the file being created — empty / non-existent)
    - .planning/phases/06-review-family/06-CONTEXT.md (LOCKED: D81 / D83 review-plan-* checks source-spec adherence + project conventions + granularity-fit + (strict) ambiguity)
    - skills/plan-loose-auto/SKILL.md (Phase 4 — defines loose-granularity plan shape: 1–3 sentence tasks; D59 contract; D60 no-parallelization)
    - skills/plan-strict-auto/SKILL.md (Phase 4 — defines strict-granularity plan shape: six-field per-task block; D59; D60; D61 self-review)
    - skills/review-proposal-auto/SKILL.md (Plan 06-01 — sibling shape: Inputs / What This Skill Reviews / Findings Report Shape / Output Artifact / Workflow / Commit / Immutability)
    - skills/review-spec-auto/SKILL.md (Plan 06-02 — sibling shape with element-by-element check; extends the pattern)
    - docs/workflow/v1/filename-grammar.md
    - docs/workflow/v1/thread-layout.md
    - docs/workflow/v1/immutability.md
  </read_first>
  <action>
    Create the file with YAML frontmatter and a markdown body. Frontmatter required fields:
    - `name: review-plan-auto`
    - `description:` ONE sentence with "Use when..." trigger AND explicit interaction mode (autonomous; reviews loose OR strict V1 plan artifacts; checks source adherence + project conventions + granularity-fit + (for strict) per-task ambiguity; emits findings-first report; no clarifying questions). Mention sibling `review-plan-interactive`. Mention the Phase 4 plan-* family by name.
    - `metadata.author: https://github.com/Jei-sKappa`
    - `metadata.version: 1.0.0`

    Body sections (executor discretion on names; behavioral tokens below MUST appear):

    1. **Opening clarifier paragraph** naming the sibling `review-plan-interactive` and referencing at least one other Phase 6 review target (e.g., `review-spec-*` for upstream artifacts, `review-implementation-*` for downstream). State that this skill consumes BOTH loose-granularity AND strict-granularity plans from the Phase 4 family (`plan-loose-auto`, `plan-loose-interactive`, `plan-strict-auto`, `plan-strict-interactive`, `adjust-plan-granularity-*`).

    2. **Inputs** section. Input MUST be a V1 plan artifact path under `docs/threads/<thread>/plans/<UTC>-v<N>[-<descriptor>]-plan.md` (versioned-form per Phase 1 grammar). Optionally a source-artifact path (the spec / proposal / decision-log / GitHub issue / raw prompt the plan derives from) for the source-adherence axis. Ambiguity fallback per `docs/workflow/v1/immutability.md` ("Ambiguous Reference Resolution") — ASK if multiple plausible plans or sources.

    3. **Loose vs Strict Detection** section. MUST document the detection step: read the plan body and detect whether tasks carry 1–3 sentence descriptions (loose-granularity per `skills/plan-loose-auto/SKILL.md`) or six-field structured blocks with `Objective` / `Input` / `Steps` / `Files modified` / `Verification` / `Acceptance criteria` per `skills/plan-strict-auto/SKILL.md` (strict-granularity). The detection determines which ambiguity check applies in the next section. Cite BOTH plan-loose-auto AND plan-strict-auto by absolute path.

    4. **What This Skill Reviews** section. MUST enumerate FOUR review axes per D83:
       1. **Source-spec adherence** — does the plan, executed end-to-end, deliver what the source artifact promised? Map each task to the source's expected outcomes / acceptance items. Coverage gaps are findings.
       2. **Project conventions** — does the plan follow the project's coding / testing / structural patterns? Mismatches with established conventions are findings (e.g., the plan adds a new module at a path the project does not use; the plan uses a build tool the project does not use).
       3. **Granularity fit** — is loose appropriate for THIS input and THIS expected implementer (D58)? A loose plan handed to an agent-leaning implementer is a fit issue. A strict plan for a simple twenty-minute change is a fit issue. The skill MAY recommend `adjust-plan-granularity-auto` if a fit issue is found.
       4. **Per-task ambiguity** (MANDATORY for strict plans; granularity-fit signal for loose) — for strict plans: does each six-field block leave no inference required by an agent-leaning implementer? Vague substeps, missing verification commands, fuzzy acceptance criteria — these are MANDATORY findings on a strict plan. For loose plans, ambiguity that would force the implementer to ask follow-up questions is a granularity-fit signal that the plan should be strict (the skill flags this and may recommend `adjust-plan-granularity-auto`).

       MUST acknowledge the D59 contract — every task in a V1 plan must be sequential, isolated, independently implementable, and independently reviewable. A finding against the D59 contract (e.g., a task that requires shared state with the next task, a task that cannot be reviewed without running the previous task first) is a `blocker`.

       MUST acknowledge the D60 no-parallelization rule — V1 plans MUST NOT contain wave numbers, dependency arrays, or task-graph notation. ANY such construct in the plan body is a `blocker` finding. Use descriptive prose phrases when naming the forbidden constructs ("bracketed wave prefixes on tasks", "dependency arrays", "task-graph notation") — NOT literal `[W1]` / unquoted `depends_on:` strings — to keep verify-pass false-positives from triggering on this skill's own body (Phase 4 lesson).

    5. **Findings Report Shape** section. Same 6 sections as Plans 06-01 / 06-02: `## Verdict` / `## Findings` (severity tags) / `## Evidence` (cite plan section / task block) / `## References` (the plan path + the source artifact path if provided + any decision logs by absolute path + `D<N>`) / `## Open Questions` / `## Next Actions`. List all 6 by literal name.

    6. **Output Artifact** section. Emit to `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md`; `review-finding` artifact-type token MANDATORY per `docs/workflow/v1/filename-grammar.md`.

    7. **Workflow** section with numbered steps:
       - Resolve thread + plan artifact path + (optional) source artifact path. ASK if multiple plausible candidates.
       - Read the plan READ-ONLY per `docs/workflow/v1/immutability.md`.
       - Detect loose-vs-strict per `## Loose vs Strict Detection`.
       - If a source artifact was provided, read it READ-ONLY too.
       - Run the four review axes from `## What This Skill Reviews`.
       - Check D59 contract on every task and D60 no-parallelization across the plan body.
       - Draft the findings-first report covering all 6 sections.
       - Capture the UTC stamp per `docs/workflow/v1/filename-grammar.md`.
       - Write the artifact to `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md`.
       - Confirm: `Review written: <relative-path-to-the-file>`. No closing remark.

    8. **Commit Policy** section. NEVER auto-commits.

    9. **Immutability** section. Emitted review-finding immutable; plan under review READ-ONLY; if the plan needs revision the user invokes `adjust-plan-granularity-auto` / `adjust-plan-granularity-interactive`. Cite `docs/workflow/v1/immutability.md` by absolute path.

    Voice and discipline:
    - Dense, declarative, citation-first.
    - Cite Phase 1 docs by absolute path.
    - Cite plan-loose-auto AND plan-strict-auto by absolute path (mandatory — they own the loose-vs-strict shapes).
    - Cite at least one Phase 6 sibling review skill (review-spec-auto or review-proposal-auto) by absolute path for shape consistency.
    - NO `## Anti-Sycophancy Stance` section (this is the AUTO half).
    - NO decision-log output (interactive sibling's territory).
    - NO the-fool delegation note (per CONTEXT.md the the-fool hint is recommended for proposal/spec reviews where adversarial pressure is most useful; review-plan-* does NOT carry it in the body — only the README-level note in Plan 06-05 covers V1 adversarial-delegation globally).
    - When naming D60 forbidden constructs use DESCRIPTIVE PROSE PHRASES (e.g., "bracketed wave prefixes on tasks", "dependency arrays", "depends_on fields") — NOT literal token strings like `[W1]` or unquoted `depends_on:` — to avoid self-triggering the executor's structural-notation negative grep (Phase 4 regression).
  </action>
  <verify>
    <automated>test -f skills/review-plan-auto/SKILL.md &amp;&amp; grep -qxF 'name: review-plan-auto' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qxF '  version: 1.0.0' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qxF '  author: https://github.com/Jei-sKappa' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qF 'Use when' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qF 'review-plan-interactive' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/plans/' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/inbox/open/' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qF 'review-finding' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/filename-grammar.md' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/thread-layout.md' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/immutability.md' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qF 'skills/plan-loose-auto/SKILL.md' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qF 'skills/plan-strict-auto/SKILL.md' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qiE 'loose.{0,5}vs.{0,5}strict|loose-vs-strict|loose vs strict' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qiE 'source.{0,5}adherence|source-spec adherence|source adherence' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qiE 'project conventions' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qiE 'granularity.{0,5}fit|granularity fit|granularity-fit' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qiE 'ambiguity' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qiE 'sequential.{0,5}isolated|sequential, isolated' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qiE 'no.{0,5}parallelization|parallelization|wave number' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qiF 'verdict' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qiF 'findings' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qiF 'evidence' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qiF 'references' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qiF 'open questions' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qiF 'next actions' skills/review-plan-auto/SKILL.md &amp;&amp; grep -qiE 'NEVER (commit|auto-commit)|never auto-commit|never commit' skills/review-plan-auto/SKILL.md &amp;&amp; ! grep -qxF '## Anti-Sycophancy Stance' skills/review-plan-auto/SKILL.md &amp;&amp; ! grep -qE '\[W[0-9]+\]|^wave:|^depends_on:' skills/review-plan-auto/SKILL.md</automated>
  </verify>
  <done>skills/review-plan-auto/SKILL.md exists with frontmatter (name=review-plan-auto, version=1.0.0) and a body covering: opening clarifier with sibling reference + Phase 4 family acknowledgment; Inputs naming plans/ versioned-form path with optional source artifact + ambiguity fallback; Loose vs Strict Detection citing plan-loose-auto AND plan-strict-auto by absolute path; What This Skill Reviews enumerating 4 axes (source adherence / project conventions / granularity fit / per-task ambiguity) with D59 sequential-isolated-independent contract + D60 no-parallelization contract acknowledged via descriptive prose phrases (no literal [Wn] / depends_on: tokens); Findings Report Shape with 6 sections; Output Artifact; Workflow; Commit Policy NEVER auto-commits; Immutability. NO Anti-Sycophancy section. NO literal parallelization tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Author skills/review-plan-interactive/SKILL.md</name>
  <files>skills/review-plan-interactive/SKILL.md</files>
  <read_first>
    - skills/review-plan-interactive/SKILL.md (the file being created — empty / non-existent)
    - skills/review-plan-auto/SKILL.md (Task 1 output — sibling consistency)
    - .planning/phases/06-review-family/06-CONTEXT.md
    - skills/plan-loose-interactive/SKILL.md (Phase 4 interactive — per-task walk pattern + 4 anti-sycophancy markers carrying pattern + per-task push-back)
    - skills/plan-strict-interactive/SKILL.md (Phase 4 — per-field push-back model; six-field structure source)
    - skills/discussion/SKILL.md (anti-sycophancy 4 markers verbatim)
    - skills/seeded-discussion/SKILL.md (per-finding walk shape)
    - skills/review-proposal-interactive/SKILL.md (Plan 06-01 — established the interactive review-skill body shape; D89 / D92 / D95 phrasings)
    - skills/review-spec-interactive/SKILL.md (Plan 06-02 — handoff-grade variant; review-stance amplifier + heightened framing reference)
    - docs/workflow/v1/filename-grammar.md
    - docs/workflow/v1/thread-layout.md
    - docs/workflow/v1/immutability.md
  </read_first>
  <action>
    Create the file with YAML frontmatter and a markdown body. Frontmatter required fields:
    - `name: review-plan-interactive`
    - `description:` ONE sentence with "Use when..." trigger + explicit interaction mode (collaborative; walks one finding or one task at a time; ASKS user's view AND TESTS against the artifact; writes decision log; opt-in inbox dump). Mention sibling `review-plan-auto`. Mention Phase 4 plan-* family.
    - `metadata.author: https://github.com/Jei-sKappa`
    - `metadata.version: 1.0.0`

    Body sections (executor discretion on names; behavioral tokens below MUST appear):

    1. **Opening clarifier paragraph** naming the sibling `review-plan-auto` + at least one other review target.

    2. **Anti-Sycophancy Stance** section. Carry the FOUR markers from `skills/discussion/SKILL.md` VERBATIM:
       - "Disagree when you disagree"
       - "Push back on weak or incomplete reasoning"
       - "Do not treat pushback as correctness"
       - "Refuse to log" (clause)

       PLUS the REVIEW-STANCE AMPLIFIER: "A review is most valuable when it disagrees with the author" or near-verbatim.

    3. **Inputs** section. Same as `review-plan-auto`: plan artifact path (loose or strict); optional source artifact path; ambiguity fallback.

    4. **Loose vs Strict Detection** section. Same detection step as `review-plan-auto`. Cite plan-loose-auto AND plan-strict-auto by absolute path.

    5. **What This Skill Reviews** section. Same 4 axes as `review-plan-auto`: source adherence / project conventions / granularity fit / per-task ambiguity (mandatory strict; granularity-fit signal loose). Same D59 / D60 acknowledgments — use descriptive prose phrases for the D60 forbidden constructs (no literal `[W1]` / unquoted `depends_on:` strings).

    6. **Walk Format** section. MUST document the per-finding OR per-task walk:
       - Either walk per finding (impact-ordered) or per task (plan-order). Executor discretion.
       - For each step:
         1. Surface the finding/task with severity tag.
         2. Cite evidence — the plan's section/task block.
         3. ASK the user for their view.
         4. TEST the user's explanation against the plan AND the source artifact (if provided). Do NOT just accept. (D89 phrase with `ASK` + `TEST` + `do not just accept` MUST appear.)
         5. Settle: resolved / rejected / accepted / deferred / parked.
         6. Append a record to the decision log with the outcome.

    7. **Output Artifacts** section (TWO conditional outputs per D92/D95):
       - **Decision log** (ALWAYS when ≥1 finding/task settled): `docs/threads/<thread>/discussions/<UTC>-<kebab-desc>-decision-log.md`; `decision-log` artifact-type MANDATORY; sequential per-log `## D<N>: <Finding title>` headings.
       - **Unresolved-findings inbox dump** (CONDITIONAL when ≥1 unresolved actionable finding remains): `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md`. Literal "no Inbox file when nothing remains" phrasing MUST appear.

    8. **Workflow** section with numbered steps:
       - Resolve thread + plan artifact path + optional source.
       - Read plan READ-ONLY.
       - Detect loose-vs-strict.
       - Identify candidate findings list (or task walk order).
       - Confirm with user before starting.
       - Per-finding (or per-task) loop from `## Walk Format`.
       - At end: write inbox-dump IF ≥1 unresolved actionable finding remains; else NO inbox file.
       - Final message.

    9. **Decision Log** lazy-creation rule.

    10. **Scope Drift** section. PARK / SPLIT / DEFER.

    11. **Commit Policy** section. NEVER auto-commits.

    12. **Immutability** section. Cite `docs/workflow/v1/immutability.md` by absolute path. Reference `adjust-plan-granularity-*` as the plan-revision path.

    Voice and discipline:
    - 4 anti-sycophancy markers verbatim; review-stance amplifier present.
    - D89 ASK-AND-TEST phrasing present.
    - D92/D95 conditional-inbox phrasing present.
    - Cite Phase 1 + Phase 4 docs by absolute path.
    - NO literal parallelization tokens — use descriptive prose only.
  </action>
  <verify>
    <automated>test -f skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qxF 'name: review-plan-interactive' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qxF '  version: 1.0.0' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qxF '  author: https://github.com/Jei-sKappa' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qF 'Use when' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qF 'review-plan-auto' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qiF 'anti-sycophancy' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qF 'Disagree when you disagree' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qF 'Push back on weak or incomplete reasoning' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qF 'Do not treat pushback as correctness' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qF 'Refuse to log' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qiE 'review is most valuable when it disagrees|disagrees with the author|push back hard' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/plans/' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/discussions/' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/inbox/open/' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qF 'decision-log' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qF 'review-finding' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/filename-grammar.md' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/thread-layout.md' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/immutability.md' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qF 'skills/plan-loose-auto/SKILL.md' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qF 'skills/plan-strict-auto/SKILL.md' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qiE 'loose.{0,5}vs.{0,5}strict|loose-vs-strict|loose vs strict' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qiE 'source.{0,5}adherence|source adherence|source-spec adherence' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qiF 'project conventions' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qiE 'granularity.{0,5}fit|granularity fit|granularity-fit' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qiE 'ambiguity' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qiE 'ASK.*TEST|TEST.*ASK' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qiE 'do not just accept|don.t just accept' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qiE 'no Inbox file|no inbox dump|nothing remains|no inbox file when nothing remains|all findings resolved' skills/review-plan-interactive/SKILL.md &amp;&amp; grep -qiE 'NEVER (commit|auto-commit)|never auto-commit|never commit' skills/review-plan-interactive/SKILL.md &amp;&amp; ! grep -qE '\[W[0-9]+\]|^wave:|^depends_on:' skills/review-plan-interactive/SKILL.md</automated>
  </verify>
  <done>skills/review-plan-interactive/SKILL.md exists with frontmatter (name=review-plan-interactive, version=1.0.0) and body covering: opening clarifier; Anti-Sycophancy Stance with 4 verbatim markers + review-stance amplifier; Inputs (plan path + optional source); Loose vs Strict Detection citing plan-loose-auto + plan-strict-auto by absolute path; What This Skill Reviews with 4 axes + D59 / D60 acknowledged via descriptive prose; Walk Format with per-finding loop including D89 ASK-AND-TEST-do-not-just-accept phrasing; Output Artifacts (always decision-log + conditional review-finding with D92/D95 no-Inbox-when-nothing-remains); Workflow; lazy decision-log creation; Scope Drift; Commit Policy NEVER auto-commits; Immutability. NO literal parallelization tokens.</done>
</task>

<task type="auto">
  <name>Task 3: Register review-plan-auto + review-plan-interactive in marketplace.json, .vscode/settings.json, and README.md</name>
  <files>.claude-plugin/marketplace.json, .vscode/settings.json, README.md</files>
  <read_first>
    - .claude-plugin/marketplace.json (state AFTER Plan 06-02: JeisKappa-workflow has 23 entries; JeisKappa-skills has 7)
    - .vscode/settings.json (state AFTER Plan 06-02: 30 alphabetically-sorted scopes)
    - README.md (state AFTER Plan 06-02: 30 "Available skills" entries; "Retired skills" subsection has discussion-loop + review-decision-document bullets)
    - AGENTS.md (4-touchpoint rule; jq accessor convention)
    - .planning/phases/06-review-family/06-CONTEXT.md (registration delta for this pair)
    - skills/review-plan-auto/SKILL.md (Task 1 output — README description-style source)
    - skills/review-plan-interactive/SKILL.md (Task 2 output — README description-style source)
  </read_first>
  <action>
    Update all three registration files atomically:

    1. **`.claude-plugin/marketplace.json`** — add `"./skills/review-plan-auto"` and `"./skills/review-plan-interactive"` to `JeisKappa-workflow.skills` in alphabetical position (after `./skills/review-decision-document` if still present; after Plan 06-02 the alphabetical position is between `./skills/propose-interactive` (no — that is before review-*) and `./skills/review-proposal-auto`. Verify: alphabetical placement of `review-plan-auto` and `review-plan-interactive` is BEFORE `review-proposal-auto` and `review-proposal-interactive`. So they go between `propose-interactive` and `review-proposal-auto`). After: JeisKappa-workflow array has exactly 25 entries (23 + 2). JeisKappa-skills unchanged at 7.

    2. **`.vscode/settings.json`** — add `"review-plan-auto"` and `"review-plan-interactive"` to `."conventionalCommits.scopes"`. They fall alphabetically before `review-proposal-auto`. Total length now 32 (30 + 2). Keep alphabetical. The jq accessor MUST use the quoted form `."conventionalCommits.scopes"`.

    3. **`README.md`** — add TWO new "Available skills" entries (`### [\`review-plan-auto\`](./skills/review-plan-auto/SKILL.md)` + `### [\`review-plan-interactive\`](./skills/review-plan-interactive/SKILL.md)`), each with description paragraph + fenced `npx skills add Jei-sKappa/skills --skill <name>` block. Insert AFTER the `propose-interactive` entry (currently the last before the review-* group; Plan 06-01 inserted review-proposal-* and Plan 06-02 inserted review-spec-*; this plan inserts review-plan-* in alphabetical position BEFORE review-proposal-*). Specifically: insert AFTER `propose-interactive` and BEFORE `review-proposal-auto`. Position both new entries together (auto then interactive). Description sentences MUST state explicit interaction mode per DIST-05.

    Do NOT touch the `## Retired skills` subsection.
    Do NOT add the verification or the-fool README notes (those belong to Plans 06-04 and 06-05).
    Do NOT commit.
  </action>
  <verify>
    <automated>jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length == 25' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/review-plan-auto")' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/review-plan-interactive")' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '.plugins[] | select(.name=="JeisKappa-skills") | .skills | length == 7' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" | length == 32' .vscode/settings.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" | index("review-plan-auto")' .vscode/settings.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" | index("review-plan-interactive")' .vscode/settings.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" == (."conventionalCommits.scopes" | sort)' .vscode/settings.json &gt;/dev/null &amp;&amp; [ "$(grep -c '^### \[`review-plan-auto`\]' README.md)" = "1" ] &amp;&amp; [ "$(grep -c '^### \[`review-plan-interactive`\]' README.md)" = "1" ] &amp;&amp; [ "$(grep -c 'npx skills add Jei-sKappa/skills --skill review-plan-auto' README.md)" = "1" ] &amp;&amp; [ "$(grep -c 'npx skills add Jei-sKappa/skills --skill review-plan-interactive' README.md)" = "1" ] &amp;&amp; A=$(grep -n '^### \[`review-plan-auto`\]' README.md | cut -d: -f1) &amp;&amp; B=$(grep -n '^### \[`review-plan-interactive`\]' README.md | cut -d: -f1) &amp;&amp; R=$(grep -n '^## Retired skills' README.md | cut -d: -f1) &amp;&amp; [ "$A" -lt "$R" ] &amp;&amp; [ "$B" -lt "$R" ] &amp;&amp; echo OK</automated>
  </verify>
  <done>marketplace.json JeisKappa-workflow array has 25 entries (was 23, +2). .vscode/settings.json scopes has 32 entries (was 30, +2), alphabetical. README.md has 2 new "Available skills" entries inserted before `## Retired skills`. No git commit created.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run:

1. **Skill files exist:**
   ```bash
   ls -la skills/review-plan-auto/SKILL.md skills/review-plan-interactive/SKILL.md
   ```

2. **Auto skill structural tokens (loose-vs-strict + 4 axes + D59/D60):**
   ```bash
   for TOKEN in "skills/plan-loose-auto/SKILL.md" "skills/plan-strict-auto/SKILL.md" "review-finding" "docs/threads/<thread>/plans/" "docs/threads/<thread>/inbox/open/"; do
     grep -qF "$TOKEN" skills/review-plan-auto/SKILL.md || echo "FAIL auto: $TOKEN"
   done
   ! grep -qE '\[W[0-9]+\]|^wave:|^depends_on:' skills/review-plan-auto/SKILL.md || echo "FAIL parallelization tokens in body (Phase 4 lesson)"
   ! grep -qxF '## Anti-Sycophancy Stance' skills/review-plan-auto/SKILL.md || echo "FAIL auto must NOT contain Anti-Sycophancy section"
   ```

3. **Interactive skill anti-sycophancy + D89 + D92/D95:**
   ```bash
   for TOKEN in "Disagree when you disagree" "Push back on weak or incomplete reasoning" "Do not treat pushback as correctness" "Refuse to log"; do
     grep -qF "$TOKEN" skills/review-plan-interactive/SKILL.md || echo "FAIL interactive: $TOKEN"
   done
   grep -qiE 'ASK.*TEST|TEST.*ASK' skills/review-plan-interactive/SKILL.md || echo "FAIL D89"
   grep -qiE 'no Inbox file|no inbox dump|nothing remains' skills/review-plan-interactive/SKILL.md || echo "FAIL D92/D95"
   ```

4. **Four-touchpoint registration:**
   ```bash
   jq '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length' .claude-plugin/marketplace.json    # expect 25
   jq '."conventionalCommits.scopes" | length' .vscode/settings.json                                              # expect 32
   grep -cE '^### \[`review-plan-(auto|interactive)`\]' README.md                                                # expect 2
   ```
</verification>

<success_criteria>
- skills/review-plan-auto/SKILL.md and skills/review-plan-interactive/SKILL.md exist with correct V1 frontmatter and bodies.
- Both skills cite plan-loose-auto AND plan-strict-auto by absolute path.
- Both skills enumerate the 4 review axes per D83 (source adherence / project conventions / granularity fit / per-task ambiguity).
- Both skills acknowledge D59 sequential-isolated-independent contract and D60 no-parallelization rule using DESCRIPTIVE PROSE phrases for forbidden constructs (no literal `[W1]` or unquoted `depends_on:` tokens — Phase 4 regression prevention).
- review-plan-interactive carries all 4 anti-sycophancy markers verbatim + review-stance amplifier.
- review-plan-* honors REVW-03 (loose+strict + adherence + conventions + granularity + ambiguity) and REVW-06/07/08 (paired-review contract shape).
- marketplace.json JeisKappa-workflow array has exactly 25 entries (23 -> 25).
- .vscode/settings.json conventionalCommits.scopes has exactly 32 entries (30 -> 32), alphabetically sorted.
- README.md has 2 new entries inserted before `## Retired skills`.
- No commit created.
</success_criteria>

<output>
Create `.planning/phases/06-review-family/06-03-SUMMARY.md` when done. Record:
- Files created: skills/review-plan-auto/SKILL.md, skills/review-plan-interactive/SKILL.md
- Files modified: .claude-plugin/marketplace.json (workflow 23 -> 25), .vscode/settings.json (scopes 30 -> 32), README.md (Available skills 30 -> 32)
- Requirements addressed: REVW-03 (review-plan-* pair with the 4 axes per D83), REVW-06/07/08 (review-shape contract applied to the plan target)
- Pattern continuity: identical paired-skill body shape established in Plans 06-01/06-02; loose-vs-strict detection step added as the unique twist for the plan target
- Phase 4 regression guard: descriptive prose phrases used in place of literal `[W1]` / unquoted `depends_on:` tokens when naming D60 forbidden constructs
</output>
