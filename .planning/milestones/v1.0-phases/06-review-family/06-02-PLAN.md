---
phase: 06-review-family
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - skills/review-spec-auto/SKILL.md
  - skills/review-spec-interactive/SKILL.md
  - skills/review-decision-document/SKILL.md
  - .claude-plugin/marketplace.json
  - .vscode/settings.json
  - README.md
autonomous: true
requirements:
  - REVW-02
  - REVW-06
  - REVW-07
  - REVW-08
  - REVW-09

must_haves:
  truths:
    - "User can invoke `review-spec-auto` against an emitted V1 spec artifact path under `docs/threads/<thread>/specs/` and receive a findings-first report at `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md` that explicitly checks all EIGHT D50 semantic-contract elements (intended outcome / context / scope-non-scope / expected behavior / constraints / explicit decisions / unresolved questions / acceptance guidance) — this is the evolved handoff-grade-bar logic from legacy `review-decision-document` per D82."
    - "User can invoke `review-spec-interactive` against an emitted V1 spec artifact path and walk one D50 element at a time (or one finding at a time), with the skill ASKING for the user's view and TESTING that view against the artifact per D89; resolved/rejected findings stay in `discussions/<UTC>-<kebab-desc>-decision-log.md`; unresolved actionable findings dump to `inbox/open/<UTC>-<kebab-desc>-review-finding.md` ONLY IF any remain at the end (per D92/D95)."
    - "User reading `review-spec-interactive` finds the 4 anti-sycophancy markers from `skills/discussion/SKILL.md` verbatim + the review-stance amplifier."
    - "User reading either review-spec skill body sees explicit citation of `skills/spec-auto/SKILL.md` AND `skills/spec-interactive/SKILL.md` by absolute path as the source of the 8 D50 semantic-contract elements (handoff-grade contract)."
    - "User reading either review-spec skill body sees the optional the-fool delegation note (D88) — adversarial pressure on a spec is delegated to the external `the-fool` skill."
    - "User reading `skills/review-decision-document/SKILL.md` finds it has been REWRITTEN as a short deprecation notice (≤60 total lines including frontmatter) explicitly stating the skill is RETIRED, naming both replacements (`review-spec-auto`, `review-spec-interactive`) with install snippets, and noting that the handoff-grade-bar logic now lives in `review-spec-*` against Phase 3's locked spec contract; legacy folder stays on disk (soft retire)."
    - "User running `npx skills list` sees NO `./skills/review-decision-document` entry under `JeisKappa Skills` (the legacy plugin loses that entry: 8 -> 7). User running `npx skills list` sees the two new `review-spec-*` skills under `JeisKappa Workflow`."
    - "User checking `.vscode/settings.json` finds `review-decision-document` REMOVED from `conventionalCommits.scopes` and `review-spec-auto`, `review-spec-interactive` ADDED (27 + 2 - 1 = 28 entries; alphabetical)."
    - "User reading `README.md` sees the `review-decision-document` entry REMOVED from `## Available skills`, two new entries ADDED (review-spec-auto, review-spec-interactive), and the `## Retired skills` subsection now contains a new bullet for `review-decision-document` (dated 2026-05-21; points to the two replacements). Available-skills count 27 + 2 - 1 = 28."
  artifacts:
    - path: "skills/review-spec-auto/SKILL.md"
      provides: "Autonomous spec review skill — checks all 8 D50 elements; emits findings-first report to inbox/open/"
      contains: "frontmatter with name=review-spec-auto, version=1.0.0; ## Inputs naming specs/ path; ## Eight Semantic-Contract Elements section enumerating all 8 D50 elements by name; ## Findings Report Shape with 6 required sections (Verdict / Findings / Evidence / References / Open Questions / Next Actions); ## Output Artifact naming inbox/open/<UTC>-<kebab-desc>-review-finding.md; ## The Fool delegation note (D88); citations to skills/spec-auto/SKILL.md + skills/spec-interactive/SKILL.md + Phase 1 docs; explicit acknowledgment that this skill evolves the legacy review-decision-document handoff-grade-bar logic"
    - path: "skills/review-spec-interactive/SKILL.md"
      provides: "Collaborative spec review — element-by-element walk + 4-marker anti-sycophancy + decision log + opt-in inbox dump"
      contains: "frontmatter with name=review-spec-interactive, version=1.0.0; ## Anti-Sycophancy Stance with 4 verbatim markers + review-stance amplifier; ## Eight Semantic-Contract Elements; ## Walk Format (per-element or per-finding); ## Output Artifacts naming discussions/<UTC>-<kebab-desc>-decision-log.md AND conditional inbox/open/<UTC>-<kebab-desc>-review-finding.md; D89 ASK-AND-TEST phrasing; D92/D95 conditional-inbox phrasing; ## The Fool delegation note (D88); citations to spec-* skills + Phase 1 docs"
    - path: "skills/review-decision-document/SKILL.md"
      provides: "Short deprecation notice pointing legacy users to review-spec-auto + review-spec-interactive"
      contains: "frontmatter with name=review-decision-document UNCHANGED, description rewritten to a RETIRED sentence, metadata.version: 2.0.0 (MAJOR bump from 1.1.0); body ≤60 total lines; literal word `retired`; install snippets for review-spec-auto and review-spec-interactive; explicit note that the handoff-grade-bar logic now lives in review-spec-* against Phase 3's locked spec contract; NO Setup / Output / Discipline / Workflow sections from the legacy body"
    - path: ".claude-plugin/marketplace.json"
      provides: "JeisKappa-workflow grows by 2 (review-spec-*); JeisKappa-skills loses 1 (review-decision-document)"
      contains: "./skills/review-spec-auto + ./skills/review-spec-interactive ADDED to JeisKappa-workflow (21 -> 23); ./skills/review-decision-document REMOVED from JeisKappa-skills (8 -> 7)"
    - path: ".vscode/settings.json"
      provides: "Scopes grow by 2 (review-spec-*); review-decision-document REMOVED; alphabetical"
      contains: "review-spec-auto + review-spec-interactive in conventionalCommits.scopes; review-decision-document REMOVED; length 29 + 2 - 1 = 30; alphabetical"
    - path: "README.md"
      provides: "Available-skills 27 + 2 - 1 = 28 after Plan 06-01 already added 2 (so this plan: 29 + 2 - 1 = 30); Retired-skills subsection gains review-decision-document bullet"
      contains: "### [`review-spec-auto`](./skills/review-spec-auto/SKILL.md) and ### [`review-spec-interactive`](./skills/review-spec-interactive/SKILL.md) headings ADDED; `### [`review-decision-document`](./skills/review-decision-document/SKILL.md)` block REMOVED from `## Available skills`; new `## Retired skills` bullet ADDED naming review-decision-document with date 2026-05-21 and pointing to the replacements"
  key_links:
    - from: "skills/review-spec-auto/SKILL.md"
      to: "skills/spec-auto/SKILL.md and skills/spec-interactive/SKILL.md"
      via: "citation by absolute path — defines the 8 D50 elements the review checks against"
      pattern: "skills/spec-(auto|interactive)/SKILL.md"
    - from: "skills/review-spec-interactive/SKILL.md"
      to: "skills/discussion/SKILL.md"
      via: "anti-sycophancy stance carried verbatim (4 markers)"
      pattern: "Disagree when you disagree|Do not treat pushback as correctness"
    - from: "skills/review-decision-document/SKILL.md (retirement notice)"
      to: "skills/review-spec-auto/SKILL.md and skills/review-spec-interactive/SKILL.md"
      via: "named reference + install snippet in deprecation notice"
      pattern: "review-spec-(auto|interactive)"
    - from: "README.md `## Retired skills`"
      to: "skills/review-spec-auto/SKILL.md + skills/review-spec-interactive/SKILL.md"
      via: "named pointer in the retired-entry's description"
      pattern: "review-spec-(auto|interactive)"
    - from: ".claude-plugin/marketplace.json"
      to: "skills/review-spec-auto/ and skills/review-spec-interactive/ ADDED; skills/review-decision-document REMOVED"
      via: "JeisKappa-workflow.skills + JeisKappa-skills.skills array edits"
      pattern: "./skills/review-spec-(auto|interactive)|./skills/review-decision-document"
---

<objective>
Ship the second review pair of Phase 6: `review-spec-auto` and `review-spec-interactive`, both targeting V1 spec artifacts and explicitly checking all EIGHT D50 semantic-contract elements (the handoff-grade-bar per D82, REVW-02). Simultaneously RETIRE the legacy `review-decision-document` skill (soft retire): rewrite its SKILL.md as a short deprecation notice pointing to the two new replacements; remove its registration from `JeisKappa-skills.skills`, from `.vscode/settings.json` scopes, and from README "Available skills"; add a new bullet to the existing `## Retired skills` README subsection. Register the two new skills in the three V1 touchpoints. Both new interactive skills carry the 4 anti-sycophancy markers verbatim + the review-stance amplifier and honor the D89 ASK-AND-TEST + D92/D95 conditional-inbox-dump rules established in Plan 06-01.

Purpose: This is the largest plan of Phase 6 because it ships TWO new skills AND retires one legacy skill in a single atomic plan — the retirement is conceptually tied to the replacement (the new `review-spec-*` skills inherit the handoff-grade-bar logic from `review-decision-document` per D82). Splitting the retirement into a separate plan would leave the marketplace temporarily advertising both the legacy skill and its replacement, which is exactly what the soft-retire pattern from Phase 2's `discussion-loop` retirement avoids. The retirement ritual is identical to Phase 2's: SKILL.md rewrite + MAJOR semver bump + drop from marketplace plugin + drop from conventional-commit scopes + remove from README "Available skills" + add to README "Retired skills".

Output: Two new skill folders + one rewritten legacy SKILL.md + three registration files updated. All four CLAUDE.md touchpoints satisfied for the two new skills; all four REVERSE touchpoints satisfied for the retired skill. After this plan: workflow plugin 21 -> 23; JeisKappa-skills plugin 8 -> 7; .vscode scopes 29 + 2 - 1 = 30; README "Available skills" 29 + 2 - 1 = 30; README "Retired skills" gains a new bullet.

Depends on Plan 06-01 because (a) the marketplace.json + .vscode/settings.json + README.md files are shared (sequential serialization), and (b) the review-skill body shape (paired auto/interactive with findings-first auto + decision-log+conditional-inbox interactive) was established in Plan 06-01 and is reused here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-review-family/06-CONTEXT.md
@AGENTS.md

@docs/workflow/v1/README.md
@docs/workflow/v1/thread-layout.md
@docs/workflow/v1/filename-grammar.md
@docs/workflow/v1/immutability.md

<!-- Phase 3 spec skills — the source of the 8 D50 elements that review-spec-* checks against -->
@skills/spec-auto/SKILL.md
@skills/spec-interactive/SKILL.md

<!-- Anti-sycophancy + walk-pattern source -->
@skills/discussion/SKILL.md
@skills/seeded-discussion/SKILL.md

<!-- Plan 06-01 pair — established the review-skill body shape this plan reuses -->
@skills/review-proposal-auto/SKILL.md
@skills/review-proposal-interactive/SKILL.md

<!-- Legacy skill being retired — current contents drive the retirement notice rewrite -->
@skills/review-decision-document/SKILL.md

<!-- Phase 2 retirement precedent — identical soft-retire pattern -->
@skills/discussion-loop/SKILL.md
@.planning/phases/02-capture-and-discussion-infrastructure/02-03-PLAN.md

<!-- Registration files being edited -->
@.claude-plugin/marketplace.json
@.vscode/settings.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author skills/review-spec-auto/SKILL.md (handoff-grade-bar review against all 8 D50 elements)</name>
  <files>skills/review-spec-auto/SKILL.md</files>
  <read_first>
    - skills/review-spec-auto/SKILL.md (the file being created — empty / non-existent)
    - .planning/phases/06-review-family/06-CONTEXT.md (LOCKED: D81 / D82 review-spec evolves review-decision-document handoff-grade-bar; D88 the-fool delegation; D90/D91 findings-first report; specifics about severity tag mapping for the 8 D50 elements: missing element = blocker / partially-covered = issue / vague-but-present = nit)
    - skills/spec-auto/SKILL.md (Phase 3 — defines the 8 D50 semantic-contract elements; ## Semantic Contract section)
    - skills/spec-interactive/SKILL.md (Phase 3 — same 8 D50 elements; walk source)
    - skills/review-proposal-auto/SKILL.md (Plan 06-01 Task 1 output — sibling consistency for body shape: Inputs / What This Skill Reviews / Findings Report Shape / Output Artifact / The Fool / Workflow / Commit / Immutability)
    - skills/review-decision-document/SKILL.md (LEGACY — evolves into this skill per D82; review the "What to look for" / "Three layers" framing for the prose density transfer)
    - docs/workflow/v1/filename-grammar.md
    - docs/workflow/v1/thread-layout.md
    - docs/workflow/v1/immutability.md
  </read_first>
  <action>
    Create the file with YAML frontmatter and a markdown body. Frontmatter required fields:
    - `name: review-spec-auto`
    - `description:` ONE sentence including the "Use when..." trigger AND explicit interaction mode (autonomous; checks all 8 D50 elements; emits findings-first report; no clarifying questions). Mention sibling `review-spec-interactive`. Note that this evolves the legacy `review-decision-document` handoff-grade-bar logic against the Phase 3 spec contract. Mention adversarial delegation to `the-fool`.
    - `metadata.author: https://github.com/Jei-sKappa`
    - `metadata.version: 1.0.0`

    Body sections (executor discretion on exact section names; behavioral tokens below MUST appear):

    1. **Opening clarifier paragraph** naming the sibling `review-spec-interactive` and the broader review family (at minimum: name `review-proposal-*` for the LIGHTER review target above, `review-plan-*` for the granularity-and-ambiguity below). State that this skill applies the HANDOFF-GRADE BAR per D82 — the bar that a downstream implementer with no prior context can deliver the same work the author had in mind by reading the spec alone. State that this skill evolves the legacy `review-decision-document` (now retired — see `skills/review-decision-document/SKILL.md`) — the legacy "stress-tests against the bar that a recipient could deliver the same work the author had in mind" framing is preserved, but the bar is now the locked Phase 3 spec contract.

    2. **Inputs** section. Input MUST be a V1 spec artifact path under `docs/threads/<thread>/specs/<UTC>-v<N>[-<descriptor>]-spec.md` (versioned-form per Phase 1 grammar). Ambiguity fallback per `docs/workflow/v1/immutability.md` ("Ambiguous Reference Resolution") — ASK if multiple plausible specs share a version number; do not pick by recency.

    3. **Eight Semantic-Contract Elements** section. MUST enumerate all 8 D50 elements BY NAME, each on its own line or bullet:
       1. Intended outcome
       2. Context
       3. Scope / non-scope
       4. Expected behavior
       5. Constraints
       6. Explicit decisions
       7. Unresolved questions
       8. Acceptance guidance

       MUST cite `skills/spec-auto/SKILL.md` AND `skills/spec-interactive/SKILL.md` by ABSOLUTE PATH as the source of the contract (the spec contract is owned by Phase 3; this skill checks AGAINST it).

       Suggested severity mapping (executor MAY adopt; CONTEXT.md specifics): a MISSING element is a `blocker`; a PARTIALLY-COVERED element is an `issue`; a VAGUE-BUT-PRESENT element is a `nit`. The 3 severity tag tokens (`blocker`, `issue`, `nit`) SHOULD appear in the body explicitly.

    4. **What This Skill Reviews** section (or merge with the Eight-Elements section if you prefer — executor discretion, provided the 8 elements are enumerated). MUST emphasize: this is the HANDOFF-GRADE BAR — a downstream reader with no prior context can read the spec alone and know what to build. Cover gaps, ambiguities, contradictions, hidden assumptions, false precision ("robust", "scalable", "modern", "clean", "appropriate" — common red flags), unjustified absolutes (`must / never / only / always`) — this prose density echoes the legacy review-decision-document framing. Tether every finding to downstream impact ("a downstream implementer would have to guess whether X means A or B").

    5. **Findings Report Shape** section. MUST document the same six sections from Plan 06-01: `## Verdict` / `## Findings` (severity-tagged) / `## Evidence` (cite spec section/short quote) / `## References` (the spec path + any decision logs cited by absolute path + `D<N>`) / `## Open Questions` / `## Next Actions`. The six sections MUST be listed by literal name.

    6. **Output Artifact** section. Emit to `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md` (record-form per `docs/workflow/v1/filename-grammar.md`). `review-finding` artifact-type token is MANDATORY. ONE record per review run.

    7. **The Fool Delegation** subsection or short paragraph (D88, REVW-09). Adversarial pressure on a spec is delegated to the external `the-fool` skill. This skill performs the handoff-grade-bar check, NOT the adversarial pass.

    8. **Workflow** section with numbered steps:
       - Resolve thread + spec artifact path (ASK if multiple plausible).
       - Read the spec READ-ONLY per `docs/workflow/v1/immutability.md`.
       - Check each of the 8 D50 elements present + coherent (suggest severity per the mapping in step 3).
       - Apply the broader gaps/ambiguities/false-precision/unjustified-absolutes pass.
       - Draft the findings-first report covering all 6 sections.
       - Capture the UTC stamp per `docs/workflow/v1/filename-grammar.md`.
       - Write the artifact to `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md`.
       - Confirm: `Review written: <relative-path-to-the-file>`. No closing remark.

    9. **Commit Policy** section. NEVER auto-commits.

    10. **Immutability** section. Emitted review-finding artifacts are immutable; the spec under review is READ-ONLY per `docs/workflow/v1/immutability.md`.

    Voice and discipline:
    - Dense, declarative, citation-first. Match the prose density of legacy review-decision-document (which this evolves) AND the Phase 4/5 spine skill density.
    - Cite Phase 1 canonical docs by absolute path.
    - Cite Phase 3 spec-* skills by absolute path (mandatory — they own the contract).
    - NO `## Anti-Sycophancy Stance` section (this is the AUTO half).
    - NO decision-log output (interactive sibling's territory).
    - NO reimplementation of the-fool adversarial logic.
  </action>
  <verify>
    <automated>test -f skills/review-spec-auto/SKILL.md &amp;&amp; grep -qxF 'name: review-spec-auto' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qxF '  version: 1.0.0' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qxF '  author: https://github.com/Jei-sKappa' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qF 'Use when' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qF 'review-spec-interactive' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qF 'the-fool' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/specs/' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/inbox/open/' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qF 'review-finding' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/filename-grammar.md' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/thread-layout.md' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/immutability.md' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qF 'skills/spec-auto/SKILL.md' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qF 'skills/spec-interactive/SKILL.md' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qiF 'handoff-grade' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qiF 'intended outcome' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qiE 'scope.*non-scope|scope / non-scope|scope/non-scope' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qiF 'expected behavior' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qiF 'constraints' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qiF 'explicit decisions' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qiF 'unresolved questions' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qiF 'acceptance guidance' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qiF 'verdict' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qiF 'findings' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qiF 'evidence' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qiF 'references' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qiF 'open questions' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qiF 'next actions' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qiE 'review-decision-document|legacy' skills/review-spec-auto/SKILL.md &amp;&amp; grep -qiE 'NEVER (commit|auto-commit)|never auto-commit|never commit' skills/review-spec-auto/SKILL.md &amp;&amp; ! grep -qxF '## Anti-Sycophancy Stance' skills/review-spec-auto/SKILL.md</automated>
  </verify>
  <done>skills/review-spec-auto/SKILL.md exists with frontmatter (name=review-spec-auto, version=1.0.0) and a body covering: opening clarifier naming review-spec-interactive sibling + handoff-grade-bar framing + acknowledgment of legacy review-decision-document evolution per D82; Inputs naming specs/ versioned-form path with ambiguity fallback; Eight Semantic-Contract Elements section enumerating all 8 D50 elements by name with citations to skills/spec-auto/SKILL.md AND skills/spec-interactive/SKILL.md by absolute path; Findings Report Shape with 6 required sections; Output Artifact naming inbox/open/<UTC>-<kebab-desc>-review-finding.md; The Fool delegation note (D88); Workflow with read-only-spec + per-element-check + write-review steps; Commit Policy NEVER auto-commits; Immutability. NO Anti-Sycophancy section.</done>
</task>

<task type="auto">
  <name>Task 2: Author skills/review-spec-interactive/SKILL.md (handoff-grade-bar walk + 4-marker anti-sycophancy + decision log + opt-in inbox dump)</name>
  <files>skills/review-spec-interactive/SKILL.md</files>
  <read_first>
    - skills/review-spec-interactive/SKILL.md (the file being created — empty / non-existent)
    - skills/review-spec-auto/SKILL.md (Task 1 output — sibling consistency on Inputs / 8 D50 elements / Findings Report Shape / The Fool note)
    - .planning/phases/06-review-family/06-CONTEXT.md
    - skills/spec-auto/SKILL.md and skills/spec-interactive/SKILL.md (Phase 3 — the 8 D50 elements + the spec-interactive walk-element-at-a-time pattern this skill mirrors)
    - skills/discussion/SKILL.md (anti-sycophancy 4 markers verbatim)
    - skills/seeded-discussion/SKILL.md (per-element / per-finding walk shape)
    - skills/review-proposal-interactive/SKILL.md (Plan 06-01 Task 2 output — interactive sibling pattern; the D89 ASK-AND-TEST phrasing + the D92/D95 conditional-inbox phrasing established here are reused)
    - skills/spec-interactive/SKILL.md (Phase 3 walk pattern for the 8 D50 elements — this skill's walk format)
    - docs/workflow/v1/filename-grammar.md
    - docs/workflow/v1/thread-layout.md
    - docs/workflow/v1/immutability.md
  </read_first>
  <action>
    Create the file with YAML frontmatter and a markdown body. Frontmatter required fields:
    - `name: review-spec-interactive`
    - `description:` ONE sentence with "Use when..." trigger + explicit interaction mode (collaborative; walks one element or one finding at a time; ASKS for user's view AND TESTS against artifact; writes decision log; opt-in inbox dump). Mention sibling `review-spec-auto`, note legacy review-decision-document evolution, mention the-fool delegation.
    - `metadata.author: https://github.com/Jei-sKappa`
    - `metadata.version: 1.0.0`

    Body sections (executor discretion on names; behavioral tokens below MUST appear):

    1. **Opening clarifier paragraph** naming the sibling `review-spec-auto`, reference at least one other review target (e.g., `review-proposal-*`, `review-plan-*`), state HANDOFF-GRADE BAR per D82, note legacy review-decision-document evolution.

    2. **Anti-Sycophancy Stance** section. Carry the FOUR markers from `skills/discussion/SKILL.md` VERBATIM:
       - "Disagree when you disagree"
       - "Push back on weak or incomplete reasoning"
       - "Do not treat pushback as correctness"
       - "Refuse to log" (clause)

       PLUS the REVIEW-STANCE AMPLIFIER: "A review is most valuable when it disagrees with the author" or near-verbatim (push back hard on weak reasoning or hidden assumptions; do not soften findings just because the user pushes back). PLUS a HEIGHTENED FRAMING tied to the handoff-grade bar (executor discretion): bad design captured in the spec becomes expensive in implementation, so the cheap moment to push back is during the spec review — NOT after the implementer hits a contradiction.

    3. **Inputs** section. Same as `review-spec-auto`: spec artifact path; ambiguity fallback.

    4. **Eight Semantic-Contract Elements** section. Same as `review-spec-auto` — enumerate all 8 D50 elements by name and cite `skills/spec-auto/SKILL.md` + `skills/spec-interactive/SKILL.md` by absolute path. Suggested severity mapping (missing=blocker / partial=issue / vague=nit).

    5. **Walk Format** section. MUST document the per-element OR per-finding walk (executor's choice of grain — per CONTEXT.md "walk one finding/topic/component at a time"):
       - Either walk the 8 D50 elements in order (closer to spec-interactive's walk shape) OR walk the candidate findings list in impact order (closer to seeded-discussion's per-point walk). Executor discretion.
       - For each step in the walk:
         1. Surface the finding (or element) with severity tag.
         2. Cite evidence — the spec's section/short quote.
         3. ASK the user for their view.
         4. TEST the user's explanation against the spec — re-read the cited section. Do NOT just accept. (D89 literal phrase containing both `ASK` and `TEST` and `do not just accept` MUST appear.)
         5. Settle the finding/element: resolved / rejected / accepted / deferred / parked.
         6. Append a record to the decision log with the outcome.

    6. **Output Artifacts** section (TWO conditional outputs per D92/D95):
       - **Decision log** (ALWAYS when ≥1 finding settled): `docs/threads/<thread>/discussions/<UTC>-<kebab-desc>-decision-log.md`; `decision-log` artifact-type token MANDATORY; per-log sequential `## D<N>: <Finding title>` headings; `Decision:` and `Rationale:` lines.
       - **Unresolved-findings inbox dump** (CONDITIONAL — ONLY when ≥1 unresolved actionable finding remains): `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md`. The literal phrase "no Inbox file when nothing remains" or near-verbatim equivalent MUST appear.

    7. **The Fool Delegation** subsection (D88).

    8. **Workflow** section with numbered steps:
       - Resolve thread + spec artifact path.
       - Read spec READ-ONLY.
       - Identify candidate findings (per the 8 D50 elements + the broader gaps/ambiguities check).
       - Confirm findings list (or element walk order) with the user before starting.
       - Per-finding (or per-element) loop from `## Walk Format` — surface + cite + ASK + TEST + settle + log.
       - At end: write inbox-dump artifact if ≥1 unresolved actionable finding remains; else NO inbox file.
       - Final message: cite decision log path; cite inbox-dump path IF written, ELSE state "no unresolved findings — no inbox file written".

    9. **Decision Log** lazy-creation rule (no proactive creation; created on first settled finding).

    10. **Scope Drift** section. PARK / SPLIT / DEFER. Ask user.

    11. **Commit Policy** section. NEVER auto-commits.

    12. **Immutability** section. Emitted artifacts immutable; spec under review READ-ONLY. Cite `docs/workflow/v1/immutability.md` by absolute path.

    Voice and discipline:
    - Dense, declarative, citation-first.
    - All 4 anti-sycophancy markers verbatim; review-stance amplifier present; handoff-grade heightened framing present.
    - D89 ASK-AND-TEST phrasing present.
    - D92/D95 conditional-inbox phrasing present.
    - Cite Phase 1 + Phase 3 docs by absolute path.
    - No the-fool reimplementation.
  </action>
  <verify>
    <automated>test -f skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qxF 'name: review-spec-interactive' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qxF '  version: 1.0.0' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qxF '  author: https://github.com/Jei-sKappa' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qF 'Use when' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qF 'review-spec-auto' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qF 'the-fool' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qiF 'anti-sycophancy' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qF 'Disagree when you disagree' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qF 'Push back on weak or incomplete reasoning' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qF 'Do not treat pushback as correctness' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qF 'Refuse to log' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qiE 'review is most valuable when it disagrees|disagrees with the author|push back hard' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/specs/' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/discussions/' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/inbox/open/' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qF 'decision-log' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qF 'review-finding' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/filename-grammar.md' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/thread-layout.md' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/immutability.md' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qF 'skills/spec-auto/SKILL.md' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qF 'skills/spec-interactive/SKILL.md' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qiF 'handoff-grade' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qiF 'intended outcome' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qiE 'scope.*non-scope|scope / non-scope|scope/non-scope' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qiF 'expected behavior' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qiF 'constraints' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qiF 'explicit decisions' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qiF 'unresolved questions' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qiF 'acceptance guidance' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qiE 'ASK.*TEST|TEST.*ASK' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qiE 'do not just accept|don.t just accept' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qiE 'no Inbox file|no inbox dump|nothing remains|no inbox file when nothing remains|all findings resolved' skills/review-spec-interactive/SKILL.md &amp;&amp; grep -qiE 'NEVER (commit|auto-commit)|never auto-commit|never commit' skills/review-spec-interactive/SKILL.md</automated>
  </verify>
  <done>skills/review-spec-interactive/SKILL.md exists with frontmatter (name=review-spec-interactive, version=1.0.0) and a body covering: opening clarifier; Anti-Sycophancy Stance with 4 verbatim markers + review-stance amplifier + handoff-grade heightened framing; Inputs (spec versioned-form path); Eight Semantic-Contract Elements enumerated with citations to skills/spec-auto/SKILL.md AND skills/spec-interactive/SKILL.md by absolute path; Walk Format with per-finding (or per-element) loop including D89 ASK-AND-TEST-do-not-just-accept phrasing; Output Artifacts (always decision-log at discussions/; conditional review-finding at inbox/open/ with D92/D95 no-Inbox-when-nothing-remains phrasing); The Fool delegation (D88); Workflow; lazy decision-log creation; Scope Drift; Commit Policy NEVER auto-commits; Immutability.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite skills/review-decision-document/SKILL.md as a short deprecation notice</name>
  <files>skills/review-decision-document/SKILL.md</files>
  <read_first>
    - skills/review-decision-document/SKILL.md (CURRENT contents — captures the legacy 1.1.0 frontmatter and body structure for the rewrite)
    - .planning/phases/06-review-family/06-CONTEXT.md (LOCKED retirement decisions: SOFT retire; rewritten body ≤50 lines; frontmatter version bump 1.1.0 → 2.0.0; pre-existing review notes from legacy skill stay valid; handoff-grade-bar logic now lives in review-spec-*)
    - skills/review-spec-auto/SKILL.md (TASK 1 OUTPUT — verify name=review-spec-auto matches the reference)
    - skills/review-spec-interactive/SKILL.md (TASK 2 OUTPUT — verify name=review-spec-interactive matches the reference)
    - skills/discussion-loop/SKILL.md (Phase 2 PRECEDENT — identical soft-retire shape; mirror the structure exactly)
    - .planning/phases/02-capture-and-discussion-infrastructure/02-03-PLAN.md (the precedent plan that codified the soft-retire ritual)
    - AGENTS.md (## Version Bumping rule — MAJOR bump on behavioral removal)
  </read_first>
  <action>
    Rewrite `skills/review-decision-document/SKILL.md` ENTIRELY (overwrite with the Write tool — the new body replaces the legacy "What this skill is — and isn't" / "What to look for" / "Output" / "Discipline" / "Workflow" structure). Preserve the directory; only the file contents change.

    REQUIRED FRONTMATTER (modified from the existing 1.1.0):
    - `name: review-decision-document` (UNCHANGED — folder name still matches)
    - `description:` REWRITTEN to a single sentence stating the skill is RETIRED and naming both replacements. Example shape: "RETIRED — replaced by `review-spec-auto` (autonomous spec review against all 8 D50 semantic-contract elements) and `review-spec-interactive` (collaborative spec review with per-element / per-finding walk). Use when you find this skill in a legacy install and need to know what to install instead."
    - `metadata.author: https://github.com/Jei-sKappa` (UNCHANGED)
    - `metadata.version: 2.0.0` (MAJOR bump from 1.1.0 — behavioral removal; per AGENTS.md "bump on any meaningful behavioral change" and SemVer convention major bump for breaking change; matches the discussion-loop retirement bump precedent)

    REQUIRED BODY (target length: ≤50 body lines per CONTEXT.md; ≤60 lines total including frontmatter consistent with Phase 2's discussion-loop precedent):

    The body MUST contain (executor chooses the prose flow):

    1. A `#` heading or top paragraph explicitly stating the skill is RETIRED. The literal word `retired` MUST appear at least once.

    2. A short statement (1–3 sentences) explaining WHY: this skill was the V1 source of the handoff-grade-bar logic; that logic now lives in `review-spec-auto` and `review-spec-interactive` checking against the locked Phase 3 spec semantic contract (the 8 D50 elements: intended outcome / context / scope-non-scope / expected behavior / constraints / explicit decisions / unresolved questions / acceptance guidance).

    3. A pointer to BOTH replacements. The literal skill names `review-spec-auto` and `review-spec-interactive` MUST appear. Each replacement MUST include its install snippet inside an `sh` code fence:
       - `npx skills add Jei-sKappa/skills --skill review-spec-auto`
       - `npx skills add Jei-sKappa/skills --skill review-spec-interactive`
       Each pointer MUST include a one-line description of WHEN to pick that replacement: `review-spec-auto` for autonomous end-to-end spec review against the 8-element handoff-grade bar; `review-spec-interactive` for the collaborative element-by-element walk with anti-sycophancy push-back.

    4. A short note on pre-existing review outputs. MUST state: any review notes / documents previously produced against decision documents by the legacy skill remain VALID — they require NO migration. The new skills check against the Phase 3 spec contract specifically, but the legacy framing (a recipient could deliver the same work the author had in mind) is preserved in the new bar.

    5. (OPTIONAL but recommended) A short pointer at the END to the V1 reference doc set (e.g., "For V1 thread layout and filename grammar see `docs/workflow/v1/README.md`."). If included, the literal path MUST be accurate.

    PROHIBITED CONTENT:
    - NO `## What this skill is — and isn't` / `## What to look for` / `## Separate three layers in your findings` / `## Output` / `## Discipline` / `## Workflow` / `## When the document is genuinely solid` sections from the legacy body. The file is now a NOTICE — none of the legacy active-skill body sections survive.
    - NO instruction to follow the legacy review workflow.
    - NO instruction to MIGRATE pre-existing review outputs.
    - NO frontmatter `Supersedes:` / `Alternative to:` / `Deprecated by:` / `Forked from:` field — V1 forbids source-relation frontmatter on artifacts per D44 / THRD-07; SKILL.md frontmatter has its own schema (name / description / metadata) and adding new fields is prohibited per D4.
  </action>
  <verify>
    <automated>test -f skills/review-decision-document/SKILL.md &amp;&amp; grep -qxF 'name: review-decision-document' skills/review-decision-document/SKILL.md &amp;&amp; grep -qxF '  version: 2.0.0' skills/review-decision-document/SKILL.md &amp;&amp; grep -qxF '  author: https://github.com/Jei-sKappa' skills/review-decision-document/SKILL.md &amp;&amp; grep -qiF 'retired' skills/review-decision-document/SKILL.md &amp;&amp; grep -qF 'review-spec-auto' skills/review-decision-document/SKILL.md &amp;&amp; grep -qF 'review-spec-interactive' skills/review-decision-document/SKILL.md &amp;&amp; grep -qF 'npx skills add Jei-sKappa/skills --skill review-spec-auto' skills/review-decision-document/SKILL.md &amp;&amp; grep -qF 'npx skills add Jei-sKappa/skills --skill review-spec-interactive' skills/review-decision-document/SKILL.md &amp;&amp; ! grep -qxF '## What this skill is — and isn'"'"'t' skills/review-decision-document/SKILL.md &amp;&amp; ! grep -qxF '## What to look for' skills/review-decision-document/SKILL.md &amp;&amp; ! grep -qxF '## Output' skills/review-decision-document/SKILL.md &amp;&amp; ! grep -qxF '## Discipline' skills/review-decision-document/SKILL.md &amp;&amp; ! grep -qiE '(Supersedes:|Alternative to:|Deprecated by:|Forked from:)' skills/review-decision-document/SKILL.md &amp;&amp; [ "$(wc -l &lt; skills/review-decision-document/SKILL.md)" -le 60 ]</automated>
  </verify>
  <done>skills/review-decision-document/SKILL.md is rewritten: frontmatter has name=review-decision-document UNCHANGED, description rewritten to a RETIRED sentence naming both replacements, metadata.version: 2.0.0 (MAJOR bump). Body is a short deprecation notice (≤60 total lines including frontmatter) explicitly stating the skill is retired, naming review-spec-auto + review-spec-interactive with install snippets, noting that the handoff-grade-bar logic now lives in review-spec-* against the Phase 3 spec contract, and stating that pre-existing review outputs remain valid with no migration. The legacy ## What to look for / ## Output / ## Discipline / ## Workflow sections are gone. NO prohibited source-relation frontmatter fields.</done>
</task>

<task type="auto">
  <name>Task 4: Update marketplace.json, .vscode/settings.json, README.md — ADD review-spec-* and REMOVE review-decision-document</name>
  <files>.claude-plugin/marketplace.json, .vscode/settings.json, README.md</files>
  <read_first>
    - .claude-plugin/marketplace.json (state AFTER Plan 06-01: JeisKappa-workflow has 21 entries; JeisKappa-skills has 8)
    - .vscode/settings.json (state AFTER Plan 06-01: 29 alphabetically-sorted scopes)
    - README.md (state AFTER Plan 06-01: 29 "Available skills" entries; "Retired skills" subsection has discussion-loop entry only)
    - AGENTS.md (4-touchpoint rule; jq accessor `."conventionalCommits.scopes"` convention)
    - .planning/phases/06-review-family/06-CONTEXT.md (registration delta for review-spec-* AND review-decision-document retirement)
    - .planning/phases/02-capture-and-discussion-infrastructure/02-03-PLAN.md (Phase 2 discussion-loop retirement plan — precedent for the README "Retired skills" subsection edit shape)
    - skills/review-spec-auto/SKILL.md (Task 1 output — README description-style source)
    - skills/review-spec-interactive/SKILL.md (Task 2 output — README description-style source)
    - skills/review-decision-document/SKILL.md (Task 3 output — confirm retirement is in place before pulling the marketplace advertisement)
  </read_first>
  <action>
    Update all three registration files atomically. SIX edits total in this single task (across the three files):

    **`.claude-plugin/marketplace.json`** — TWO edits:
    1. ADD `"./skills/review-spec-auto"` and `"./skills/review-spec-interactive"` to `JeisKappa-workflow.skills` array in alphabetical position (after `./skills/review-proposal-interactive` from Plan 06-01 and before `./skills/seeded-discussion`). After: JeisKappa-workflow array has exactly 23 entries (21 + 2).
    2. REMOVE `"./skills/review-decision-document"` from `JeisKappa-skills.skills` array. After: JeisKappa-skills array has exactly 7 entries (8 − 1). JSON must remain valid (no dangling comma). Do NOT delete `skills/review-decision-document/` from disk — soft retire keeps the folder.

    **`.vscode/settings.json`** — TWO edits:
    3. ADD `"review-spec-auto"` and `"review-spec-interactive"` to `."conventionalCommits.scopes"` array. Alphabetically these fall after `review-proposal-interactive` (added by Plan 06-01) and before `seeded-discussion`.
    4. REMOVE `"review-decision-document"` from `."conventionalCommits.scopes"`.
       Net: 29 + 2 − 1 = 30 entries. Keep alphabetical. The jq accessor MUST use the quoted form `."conventionalCommits.scopes"`.

    **`README.md`** — THREE edits:
    5. ADD two new "Available skills" entries (`### [\`review-spec-auto\`](./skills/review-spec-auto/SKILL.md)` and `### [\`review-spec-interactive\`](./skills/review-spec-interactive/SKILL.md)`), each with description paragraph + fenced `npx skills add Jei-sKappa/skills --skill <name>` block. Insert AFTER the `review-proposal-interactive` entry (from Plan 06-01) and BEFORE the `spec-auto` entry. Each description sentence MUST state explicit interaction mode per DIST-05 AND mention the handoff-grade bar (the differentiator from review-proposal-*).
    6. REMOVE the existing `### [\`review-decision-document\`](./skills/review-decision-document/SKILL.md)` block from `## Available skills` — the full block (heading + description paragraph + fenced install snippet). Collapse to a single blank line between the now-adjacent entries (the previous entry was `review-code-interactive` or `report-to-the-owner` depending on alphabetical position — verify; the entry that comes AFTER `review-decision-document` in the current README is `the-librarian`). After this removal: Available-skills count is 29 + 2 − 1 = 30.
    7. ADD a new bullet to the existing `## Retired skills` subsection (which currently contains only the `discussion-loop` bullet from Phase 2). Insert the bullet AFTER the existing `discussion-loop` bullet. The new bullet MUST contain:
       - Literal date `2026-05-21`
       - Literal skill name `review-decision-document` (in inline backticks)
       - Literal replacement names `review-spec-auto` and `review-spec-interactive` (in inline backticks)
       - A one-sentence migration note stating that pre-existing review outputs remain valid as-is (no migration)
       - Mirror the structure of the existing `discussion-loop` bullet for style consistency

       Suggested bullet text (executor may rephrase, all literal requirements above MUST be met):
       ```markdown
       - **`review-decision-document`** — retired 2026-05-21. Evolved into `review-spec-auto` (autonomous) and `review-spec-interactive` (collaborative) to check against the locked Phase 3 spec semantic contract (the 8 D50 handoff-grade-bar elements). The legacy folder remains on disk so existing installs do not break; new installs should pick the relevant replacement skill. Pre-existing review outputs produced by the legacy skill remain valid as-is and require no migration.
       ```

    Critical:
    - The `### [\`review-decision-document\`](./skills/review-decision-document/SKILL.md)` HEADING (level 3) MUST NOT appear in the file after the edits.
    - The `npx skills add Jei-sKappa/skills --skill review-decision-document` install snippet MUST NOT appear anywhere after the edits.
    - The `## Retired skills` subsection MUST remain a level-2 heading.
    - The existing `discussion-loop` retired-skills bullet MUST remain untouched.
    - Two new entries (review-spec-auto, review-spec-interactive) MUST be POSITIONED BEFORE `## Retired skills`.
    - The 27 (now 29 after Plan 06-01) pre-existing entries from before this plan must remain present minus the one removed (review-decision-document).
    - Do NOT touch any other entry.
    - Do NOT add the verification note (Plan 06-04) or the-fool note (Plan 06-05) in this plan.
    - Do NOT commit.
  </action>
  <verify>
    <automated>jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length == 23' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/review-spec-auto")' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/review-spec-interactive")' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '.plugins[] | select(.name=="JeisKappa-skills") | .skills | length == 7' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '.plugins[] | select(.name=="JeisKappa-skills") | .skills | index("./skills/review-decision-document") == null' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" | length == 30' .vscode/settings.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" | index("review-spec-auto")' .vscode/settings.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" | index("review-spec-interactive")' .vscode/settings.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" | index("review-decision-document") == null' .vscode/settings.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" == (."conventionalCommits.scopes" | sort)' .vscode/settings.json &gt;/dev/null &amp;&amp; [ "$(grep -c '^### \[`review-spec-auto`\]' README.md)" = "1" ] &amp;&amp; [ "$(grep -c '^### \[`review-spec-interactive`\]' README.md)" = "1" ] &amp;&amp; [ "$(grep -c 'npx skills add Jei-sKappa/skills --skill review-spec-auto' README.md)" = "1" ] &amp;&amp; [ "$(grep -c 'npx skills add Jei-sKappa/skills --skill review-spec-interactive' README.md)" = "1" ] &amp;&amp; ! grep -qF '### [`review-decision-document`](./skills/review-decision-document/SKILL.md)' README.md &amp;&amp; ! grep -qF 'npx skills add Jei-sKappa/skills --skill review-decision-document' README.md &amp;&amp; grep -qxF '## Retired skills' README.md &amp;&amp; grep -qF '`review-decision-document`' README.md &amp;&amp; grep -qF '`review-spec-auto`' README.md &amp;&amp; grep -qF '`review-spec-interactive`' README.md &amp;&amp; grep -qF '2026-05-21' README.md &amp;&amp; grep -qF '`discussion-loop`' README.md &amp;&amp; A=$(grep -n '^### \[`review-spec-auto`\]' README.md | cut -d: -f1) &amp;&amp; B=$(grep -n '^### \[`review-spec-interactive`\]' README.md | cut -d: -f1) &amp;&amp; R=$(grep -n '^## Retired skills' README.md | cut -d: -f1) &amp;&amp; [ "$A" -lt "$R" ] &amp;&amp; [ "$B" -lt "$R" ] &amp;&amp; test -f skills/review-decision-document/SKILL.md &amp;&amp; echo OK</automated>
  </verify>
  <done>.claude-plugin/marketplace.json JeisKappa-workflow array has 23 entries (was 21, +2); JeisKappa-skills has 7 entries (was 8, -1 for review-decision-document). .vscode/settings.json conventionalCommits.scopes has 30 entries (29 + 2 - 1), alphabetically sorted. README.md has 2 new "Available skills" entries (review-spec-auto, review-spec-interactive) inserted before `## Retired skills`; the review-decision-document block is REMOVED from "Available skills"; the `## Retired skills` subsection has a NEW bullet for review-decision-document alongside the existing discussion-loop bullet. The skills/review-decision-document/ folder still exists on disk (soft retire). No git commit created.</done>
</task>

</tasks>

<verification>
After all 4 tasks complete, run:

1. **Skill files exist:**
   ```bash
   ls -la skills/review-spec-auto/SKILL.md skills/review-spec-interactive/SKILL.md skills/review-decision-document/SKILL.md
   ```

2. **Auto skill structural tokens (8 D50 elements + spec-* citations + handoff-grade-bar):**
   ```bash
   for TOKEN in "handoff-grade" "intended outcome" "expected behavior" "constraints" "explicit decisions" "unresolved questions" "acceptance guidance" "skills/spec-auto/SKILL.md" "skills/spec-interactive/SKILL.md" "review-finding" "docs/threads/<thread>/inbox/open/" "the-fool"; do
     grep -qiF "$TOKEN" skills/review-spec-auto/SKILL.md || echo "FAIL auto: $TOKEN"
   done
   ! grep -qxF '## Anti-Sycophancy Stance' skills/review-spec-auto/SKILL.md || echo "FAIL auto must NOT contain Anti-Sycophancy section"
   ```

3. **Interactive skill anti-sycophancy + 8 D50 elements + D89 + D92/D95:**
   ```bash
   for TOKEN in "Disagree when you disagree" "Push back on weak or incomplete reasoning" "Do not treat pushback as correctness" "Refuse to log" "handoff-grade" "skills/spec-auto/SKILL.md"; do
     grep -qF "$TOKEN" skills/review-spec-interactive/SKILL.md || echo "FAIL interactive: $TOKEN"
   done
   grep -qiE 'ASK.*TEST|TEST.*ASK' skills/review-spec-interactive/SKILL.md || echo "FAIL D89 ASK-AND-TEST"
   grep -qiE 'no Inbox file|no inbox dump|nothing remains' skills/review-spec-interactive/SKILL.md || echo "FAIL D92/D95"
   ```

4. **review-decision-document retirement:**
   ```bash
   grep -qxF '  version: 2.0.0' skills/review-decision-document/SKILL.md || echo "FAIL version bump"
   grep -qiF 'retired' skills/review-decision-document/SKILL.md || echo "FAIL retirement word"
   grep -qF 'review-spec-auto' skills/review-decision-document/SKILL.md || echo "FAIL replacement reference"
   grep -qF 'review-spec-interactive' skills/review-decision-document/SKILL.md || echo "FAIL replacement reference"
   [ "$(wc -l < skills/review-decision-document/SKILL.md)" -le 60 ] || echo "FAIL body length"
   test -d skills/review-decision-document/ || echo "FAIL folder deleted (must keep on disk per soft retire)"
   ```

5. **Four-touchpoint registration (additions AND removals):**
   ```bash
   jq '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length' .claude-plugin/marketplace.json    # expect 23
   jq '.plugins[] | select(.name=="JeisKappa-skills") | .skills | length' .claude-plugin/marketplace.json      # expect 7
   jq '."conventionalCommits.scopes" | length' .vscode/settings.json                                              # expect 30
   grep -cE '^### \[`review-spec-(auto|interactive)`\]' README.md                                                # expect 2
   grep -cF '### [`review-decision-document`](./skills/review-decision-document/SKILL.md)' README.md             # expect 0
   ```
</verification>

<success_criteria>
- skills/review-spec-auto/SKILL.md and skills/review-spec-interactive/SKILL.md exist with correct V1 frontmatter and bodies covering all 8 D50 elements + handoff-grade-bar framing + Phase 3 spec-* citations.
- review-spec-auto emits to inbox/open/<UTC>-<kebab-desc>-review-finding.md with 6-section findings-first report (REVW-02, REVW-06).
- review-spec-interactive walks element-by-element or finding-by-finding with ASK-and-TEST + decision log + opt-in inbox dump (REVW-02, REVW-07, REVW-08).
- review-spec-interactive carries all 4 anti-sycophancy markers + review-stance amplifier + handoff-grade heightened framing.
- Both review-spec-* skills include the optional the-fool delegation note (D88, REVW-09).
- review-decision-document is soft-retired: SKILL.md rewritten as a short deprecation notice (≤60 lines), version bumped 1.1.0 → 2.0.0, body explicitly names both replacements with install snippets; folder remains on disk.
- marketplace.json: JeisKappa-workflow grows 21 -> 23; JeisKappa-skills shrinks 8 -> 7.
- .vscode/settings.json scopes 29 -> 30 (29 + 2 - 1), alphabetical.
- README.md: 2 new Available-skills entries added before `## Retired skills`; review-decision-document block REMOVED from Available skills; new bullet added to `## Retired skills` subsection alongside the existing discussion-loop bullet.
- No commit created during this plan.
- Requirements REVW-02 fully addressed; REVW-06/07/08 addressed for the spec target; REVW-09 partially addressed via the the-fool note (the README-level note is added by Plan 06-05).
</success_criteria>

<output>
Create `.planning/phases/06-review-family/06-02-SUMMARY.md` when done. Record:
- Files created: skills/review-spec-auto/SKILL.md, skills/review-spec-interactive/SKILL.md
- Files rewritten: skills/review-decision-document/SKILL.md (legacy body replaced with short deprecation notice; version bumped 1.1.0 → 2.0.0)
- Files modified: .claude-plugin/marketplace.json (workflow plugin 21 -> 23; skills plugin 8 -> 7), .vscode/settings.json (scopes 29 -> 30, alphabetical), README.md (Available-skills 29 -> 30 with review-decision-document removed; new Retired-skills bullet added)
- Folders unchanged on disk: skills/review-decision-document/ still exists (soft retire — folder NOT deleted)
- Requirements addressed: REVW-02 (review-spec-* pair against the 8 D50 elements per the handoff-grade bar — D82), REVW-06/07/08 (review-shape contracts applied to the spec target), REVW-09 (the-fool delegation note in both spec-* bodies — README-level note coming in Plan 06-05)
- Retirement ritual completed for review-decision-document: SKILL.md rewrite + MAJOR semver bump + drop from marketplace + drop from .vscode scopes + remove from README Available skills + add to README Retired skills (per Phase 2 discussion-loop precedent)
</output>
