---
phase: 07-merge-finish-navigation-distribution
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - skills/finish/SKILL.md
  - skills/whats-next/SKILL.md
  - .claude-plugin/marketplace.json
  - .vscode/settings.json
  - README.md
autonomous: true
requirements:
  - FNSH-01
  - FNSH-02
  - FNSH-03
  - NAV-01
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "User can invoke `finish` (a SINGLE V1 skill with NO `-auto` / `-interactive` sibling — intentional EXCEPTION to the V1 mode-variant convention per D97, FNSH-01) and have it perform a lightweight thread check before asking the closure question."
    - "User can rely on the `finish` thread check surfacing four inspection items per D98, FNSH-02: (1) Final artifacts present (`proposals/`, `specs/`, `plans/`, `discussions/` have content?), (2) Open Inbox items (`inbox/open/` non-empty?), (3) Implementation commits/status (recent commits in current branch?), (4) Obvious unresolved workflow concerns (uncommitted changes? pending review-finding records?)."
    - "User can rely on `finish` asking the closure question with FOUR options per D98, FNSH-03: 'merge into main' / 'merge into other branch' / 'create PR' / 'leave as is'. The 4 options MUST be enumerated in the SKILL body."
    - "User can rely on `finish` NEVER force-pushing, NEVER rewriting history (no `--amend`, no rebase, no force-push, no `git reset --hard` against pushed history) — the SKILL body documents this explicitly."
    - "User reading `finish` finds the 4 anti-sycophancy markers from `skills/discussion/SKILL.md` verbatim PLUS the closure-stance amplifier ('Branch operations are hard to undo — push back if the user picks an option that would lose work' or near-verbatim)."
    - "User can invoke `whats-next` and receive a CHAT-FIRST answer per D105/NAV-01 — the skill inspects the current thread context + recent artifacts (proposals/, specs/, plans/, implementations, inbox/open/) and suggests 2–4 coherent next actions in chat. The skill NEVER writes an artifact by default."
    - "User can rely on `whats-next` writing an Inbox item ONLY on USER OPT-IN per D105, NAV-02 — if the user accepts a 'save this suggestion for later' prompt, the skill MAY route to `skills/capture-inbox/SKILL.md` to write the inbox-item. The skill does NOT auto-capture."
    - "User can rely on `whats-next` being allowed to be THIN per D33, NAV-03 — the V1 body MAY explicitly point the agent at the canonical README hybrid for the full workflow map (the README hybrid lands in Plan 07-03)."
    - "User reading `whats-next` finds the 4 anti-sycophancy markers verbatim (kept for V1 convention uniformity per 07-CONTEXT.md; NO stage-specific amplifier — the skill is advisory, not opinion-driven)."
    - "User running `npx skills add` finds both new skills installable via JeisKappa-workflow (31 -> 33). Phase 7 V1 target plugin count is reached at 33."
  artifacts:
    - path: "skills/finish/SKILL.md"
      provides: "Single V1 skill (no auto/interactive variants) that performs lightweight thread check + asks 4-option closure question (merge main / merge other / create PR / leave as is). Never force-pushes, never rewrites history."
      contains: "frontmatter with name=finish, version=1.0.0, description with 'Use when...' + explicit acknowledgement that this is the SINGLE V1 skill with no -auto/-interactive sibling (D97 exception); opening clarifier explaining the V1 mode-variant exception; Anti-Sycophancy Stance section with 4 verbatim markers + closure-stance amplifier; Thread Check section enumerating 4 inspection items; Closure Question section enumerating 4 options; Branch Operations section documenting per-option behaviors (with user-confirmation requirement before any git command); NEVER force-push / NEVER rewrite history statement; Workflow section; Commit Policy section; citations to all 4 Phase 1 docs by absolute path"
    - path: "skills/whats-next/SKILL.md"
      provides: "Advisory chat-first navigation skill that inspects current thread context and suggests 2–4 coherent next actions in chat. Never writes an artifact by default. Routes to capture-inbox ONLY on user opt-in. May be thin per D33."
      contains: "frontmatter with name=whats-next, version=1.0.0, description with 'Use when...' + explicit interaction mode (advisory; chat-first answer; no artifact by default; optional inbox capture on user opt-in); Anti-Sycophancy Stance section with 4 verbatim markers (NO stage-specific amplifier); Workflow / Thread Inspection section (proposals/ specs/ plans/ inbox/open/ + recent commits); Chat-First Answer rule (NO auto-write); Optional Inbox Capture section (only on user opt-in; route to capture-inbox); Thin-OK statement per D33 (may point to canonical README hybrid); citations to Phase 1 docs + skills/capture-inbox/SKILL.md by absolute path"
    - path: ".claude-plugin/marketplace.json"
      provides: "JeisKappa-workflow grows by 2 (31 -> 33; Phase 7 V1 target reached)."
      contains: "./skills/finish + ./skills/whats-next ADDED in alphabetical position. JeisKappa-skills unchanged at 7."
    - path: ".vscode/settings.json"
      provides: "Scopes grow by 2 (38 -> 40); alphabetical."
      contains: "finish + whats-next added to conventionalCommits.scopes alphabetically."
    - path: "README.md"
      provides: "Available skills grows by 2 (38 -> 40). NOTE: Plan 07-03 will subsequently REPLACE the entire 'Available skills' simple-list with the hybrid layout. This plan adds the 2 new entries to the existing simple-list structure. The verification-coverage and the-fool documentation notes added in Phase 6 + the merge-artifacts entries from Plan 07-01 MUST remain present and untouched."
      contains: "### [`finish`](./skills/finish/SKILL.md) + ### [`whats-next`](./skills/whats-next/SKILL.md) headings ADDED in alphabetical position (before `## Retired skills`); each entry has description + fenced npx install command; interaction mode explicitly stated in each description per DIST-05 (finish: SINGLE skill no variants D97 exception; whats-next: advisory chat-first with optional opt-in inbox capture)"
  key_links:
    - from: "skills/finish/SKILL.md"
      to: "skills/discussion/SKILL.md"
      via: "anti-sycophancy stance carried verbatim"
      pattern: "Disagree when you disagree|Do not treat pushback as correctness"
    - from: "skills/whats-next/SKILL.md"
      to: "skills/capture-inbox/SKILL.md"
      via: "optional inbox capture routes to capture-inbox on user opt-in"
      pattern: "skills/capture-inbox/SKILL.md"
    - from: "skills/finish/SKILL.md"
      to: "docs/workflow/v1/thread-layout.md"
      via: "thread check enumerates the V1 folder set (proposals/specs/plans/discussions/inbox/open)"
      pattern: "docs/workflow/v1/thread-layout.md"
---

<objective>
Ship the V1 FINISH + NAVIGATION closing skills: `finish` (SINGLE skill — intentional V1 exception to the auto/interactive variant convention per D97, FNSH-01) and `whats-next` (advisory chat-first navigation per NAV-01..03). `finish` performs a lightweight thread check (4 inspection items per D98, FNSH-02) and asks a 4-option closure question (merge main / merge other / create PR / leave as is per D98, FNSH-03). It NEVER force-pushes and NEVER rewrites history. `whats-next` inspects the current thread context + recent artifacts and suggests 2–4 next actions in chat; it writes NO artifact by default; on user opt-in it MAY route to `capture-inbox` to save a suggestion (per NAV-02). The V1 body MAY be thin per D33 and point to the canonical README hybrid (which lands in Plan 07-03).

`finish` is the ONLY V1 skill without `-auto` / `-interactive` siblings — this is an intentional exception to the V1 mode-variant convention because branch disposition is inherently user-directed (the closure question requires a human choice). Both `finish` and `whats-next` carry the 4 anti-sycophancy markers verbatim; `finish` additionally carries a closure-stance amplifier (branch ops are hard to undo); `whats-next` does NOT carry a stage-specific amplifier (it is advisory, not opinion-driven).

Purpose: This is the second of 3 Phase 7 plans. After this plan ships, JeisKappa-workflow grows from 31 to 33 — the Phase 7 V1 plugin target. The V1 catalog is now complete in terms of skill artifacts; only the README hybrid redesign (Plan 07-03) remains.

Output: Two new skill folders + three registration files updated. After this plan: workflow plugin 31 -> 33 (V1 final); .vscode scopes 38 -> 40; README "Available skills" 38 -> 40. Depends on Plan 07-01 because of shared marketplace.json, .vscode/settings.json, and README.md modifications (sequential serialization — no parallel work permitted under V1 parallelization=false).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-merge-finish-navigation-distribution/07-CONTEXT.md
@AGENTS.md

@docs/workflow/v1/README.md
@docs/workflow/v1/thread-layout.md
@docs/workflow/v1/filename-grammar.md
@docs/workflow/v1/immutability.md

<!-- Anti-sycophancy source — 4 markers verbatim -->
@skills/discussion/SKILL.md

<!-- capture-inbox is whats-next's optional opt-in output route -->
@skills/capture-inbox/SKILL.md

<!-- Phase 5 implementation skills — pattern source for git-discipline language (NEVER --amend / rebase / force-push) — finish reuses this discipline -->
@skills/implement-plan-auto/SKILL.md
@skills/implement-plan-interactive/SKILL.md

<!-- Phase 6 interactive review skill — pattern source for the 4-marker anti-sycophancy stance + amplifier format -->
@skills/review-spec-interactive/SKILL.md

<!-- Registration files being edited -->
@.claude-plugin/marketplace.json
@.vscode/settings.json
@README.md

<!-- Optional reference inspiration per D97 (CONTEXT.md: SKILL body MAY mention as inspiration; not required) -->
@.library/sources/obra_superpowers/skills/finishing-a-development-branch/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author skills/finish/SKILL.md (SINGLE skill — V1 exception to mode-variant convention)</name>
  <files>skills/finish/SKILL.md</files>
  <read_first>
    - skills/finish/SKILL.md (the file being created — empty / non-existent)
    - .planning/phases/07-merge-finish-navigation-distribution/07-CONTEXT.md (LOCKED: D97 single-skill exception; D98 thread check 4 items + closure question 4 options; no force-push / no history-rewrite)
    - skills/discussion/SKILL.md (anti-sycophancy 4 markers verbatim — REQUIRED phrases)
    - skills/review-spec-interactive/SKILL.md (Phase 6 — pattern source for 4-marker stance + amplifier format)
    - skills/implement-plan-auto/SKILL.md (Phase 5 — pattern source for git-discipline language: NEVER --amend / NEVER rebase / NEVER force-push)
    - skills/implement-plan-interactive/SKILL.md (Phase 5 — same git-discipline pattern in the interactive sibling)
    - docs/workflow/v1/thread-layout.md (V1 folder set — finish's thread check inspects this)
    - docs/workflow/v1/filename-grammar.md (used to recognize V1 artifacts during thread check)
    - docs/workflow/v1/immutability.md (finish reads artifacts READ-ONLY)
    - .library/sources/obra_superpowers/skills/finishing-a-development-branch/SKILL.md (optional inspiration per D97; SKILL body MAY reference)
  </read_first>
  <action>
    Create the file with YAML frontmatter and a markdown body. Frontmatter required fields:
    - `name: finish`
    - `description:` ONE sentence with "Use when..." trigger + EXPLICIT statement that this is the SINGLE V1 skill with NO `-auto` / `-interactive` sibling (intentional exception per D97 — branch disposition is inherently user-directed; the closure question requires a human choice). Mention: lightweight thread check, then asks closure question with 4 options (merge main / merge other / create PR / leave as is). Mention: NEVER force-pushes, NEVER rewrites history.
    - `metadata.author: https://github.com/Jei-sKappa`
    - `metadata.version: 1.0.0`

    Body sections (executor discretion on section names; behavioral tokens below MUST appear):

    1. **Opening clarifier paragraph**. State explicitly that `finish` is the ONLY V1 workflow skill WITHOUT `-auto` / `-interactive` siblings — this is an intentional V1 EXCEPTION to the mode-variant convention per D97. Explain WHY: branch disposition is inherently user-directed (the user MUST choose between merge / PR / leave-as-is — there is no autonomous default that would be safe across users / repos / branch contexts). Cite all 4 Phase 1 canonical docs by absolute path on first invocation. The body MAY mention `.library/sources/obra_superpowers/skills/finishing-a-development-branch/SKILL.md` as design inspiration (not required).

    2. **Anti-Sycophancy Stance** section. Carry the FOUR markers from `skills/discussion/SKILL.md` VERBATIM:
       - "Disagree when you disagree"
       - "Push back on weak or incomplete reasoning"
       - "Do not treat pushback as correctness"
       - "Refuse to log" (clause; verbatim phrasing from `skills/discussion/SKILL.md`)

       PLUS the CLOSURE-STANCE AMPLIFIER per 07-CONTEXT.md: "Branch operations are hard to undo — push back if the user picks an option that would lose work" (or near-verbatim — the amplifier MUST tie to the irreversibility of branch operations).

    3. **Thread Check** section. MUST enumerate the FOUR inspection items per D98, FNSH-02. Use a numbered or bulleted list with explicit per-item description:
       a. **Final artifacts present** — Inspect `docs/threads/<thread>/proposals/`, `specs/`, `plans/`, `discussions/` (and any other V1 folders) for content. Report which folders have artifacts and how many. Skip empty folders silently.
       b. **Open Inbox items** — Inspect `docs/threads/<thread>/inbox/open/` for non-empty state. Report any open inbox items by filename. Note that open items represent unaddressed findings or follow-ups.
       c. **Implementation commits / status** — Inspect recent commits on the current branch (e.g., `git log --oneline -20` or equivalent). Report whether commits have been made in this thread's working session and what the current branch state is.
       d. **Obvious unresolved workflow concerns** — Inspect for uncommitted changes (`git status`), pending review-finding records in `inbox/open/`, or other workflow signals that suggest closure may be premature. Report concerns.

       After surfacing the 4 inspection results, ASK the user the closure question (next section). The thread check is INFORMATIONAL — it does not block closure; the user decides whether the signals warrant closing now or pausing.

    4. **Closure Question** section. MUST document the FOUR options per D98, FNSH-03 (enumerate by literal option name):
       a. **merge into main** — checkout `main` (or repo default branch); merge the current branch (`git merge <current-branch>`); push (`git push`). The skill MUST ASK for user confirmation before running each git command.
       b. **merge into other branch** — same flow with a USER-SUPPLIED target branch name. ASK the user for the target before running.
       c. **create PR** — invoke `gh pr create` (or instruct the user manually if `gh` is unavailable in the runtime). The skill MAY offer suggested title and body based on the thread artifacts; the user reviews and confirms before submission.
       d. **leave as is** — Report the current branch state (branch name, commit count, file count) and STOP. No git operation runs. The skill exits cleanly.

       The 4 options MUST be enumerated with their literal option-name labels (merge into main / merge into other branch / create PR / leave as is — exact phrasing the user can choose from).

    5. **Branch Operations** section. MUST document for each non-leave option:
       - The git commands the skill will run (named explicitly).
       - The user-confirmation requirement BEFORE each command.
       - The behavior when a command fails (REPORT the failure; STOP; do NOT retry; do NOT attempt recovery).

       **NEVER force-push, NEVER rewrite history** — the skill MUST explicitly state this. Forbidden constructs (cite explicitly with NEVER language): `--amend`, `git rebase` (any flavor), `git push --force` / `--force-with-lease`, `git reset --hard` against pushed history, `git filter-branch`, `git filter-repo`. Mirror the git-discipline language from `skills/implement-plan-auto/SKILL.md` and `skills/implement-plan-interactive/SKILL.md`.

    6. **Workflow** section with numbered steps:
       - Resolve the active thread per `docs/workflow/v1/thread-layout.md` (ASK if ambiguous).
       - Run the Thread Check (4 inspection items from `## Thread Check`).
       - Surface the inspection results to the user.
       - ASK the closure question (4 options from `## Closure Question`).
       - Branch on the user's choice: execute the chosen flow per `## Branch Operations` with user confirmation before each git command.
       - Final message: report the outcome (e.g., "Merged into main and pushed.", "PR created: <url>", "Left as is. Current branch: <branch>"). No closing remark.

    7. **Commit Policy** section. `finish` does NOT auto-commit any artifact this skill itself produces (it does not produce artifacts). `finish` MAY run git commands that produce commits or merges WITHIN the user-chosen flow (e.g., `git merge` produces a merge commit), but the user MUST confirm each command before execution. `finish` does NOT stage, commit, or push without explicit user choice; it does NOT rewrite history.

    8. **Immutability** section. `finish` reads thread artifacts READ-ONLY per `docs/workflow/v1/immutability.md`. The skill does NOT edit any V1 artifact during the thread check. The skill does NOT add or remove inbox items. The skill does NOT modify the spec/plan/proposal/discussion bodies it inspects.

    Voice and discipline:
    - Dense, declarative, citation-first.
    - 4 anti-sycophancy markers verbatim from `skills/discussion/SKILL.md`.
    - Closure-stance amplifier present.
    - Git-discipline language mirrors Phase 5 implement-* skills (NEVER --amend / NEVER rebase / NEVER force-push).
    - All 4 Phase 1 docs cited by absolute path.
    - The single-skill V1 exception MUST be explicitly acknowledged in the opening clarifier AND in the description.
    - The 4 thread-check items + 4 closure-question options MUST be enumerated by literal token.
  </action>
  <verify>
    <automated>test -f skills/finish/SKILL.md &amp;&amp; grep -qxF 'name: finish' skills/finish/SKILL.md &amp;&amp; grep -qxF '  version: 1.0.0' skills/finish/SKILL.md &amp;&amp; grep -qxF '  author: https://github.com/Jei-sKappa' skills/finish/SKILL.md &amp;&amp; grep -qF 'Use when' skills/finish/SKILL.md &amp;&amp; grep -qiE 'single|sole|only V1 skill|no.*-auto.*-interactive|no auto/interactive' skills/finish/SKILL.md &amp;&amp; grep -qiE 'D97|exception' skills/finish/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/thread-layout.md' skills/finish/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/filename-grammar.md' skills/finish/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/immutability.md' skills/finish/SKILL.md &amp;&amp; grep -qF 'Disagree when you disagree' skills/finish/SKILL.md &amp;&amp; grep -qF 'Push back on weak or incomplete reasoning' skills/finish/SKILL.md &amp;&amp; grep -qF 'Do not treat pushback as correctness' skills/finish/SKILL.md &amp;&amp; grep -qF 'Refuse to log' skills/finish/SKILL.md &amp;&amp; grep -qiE 'branch operations are hard to undo|push back if the user picks|hard to undo' skills/finish/SKILL.md &amp;&amp; grep -qiE 'merge into main' skills/finish/SKILL.md &amp;&amp; grep -qiE 'merge into other|merge into another' skills/finish/SKILL.md &amp;&amp; grep -qiE 'create PR|create pull request|create a PR' skills/finish/SKILL.md &amp;&amp; grep -qiE 'leave as is|leave as-is' skills/finish/SKILL.md &amp;&amp; grep -qF 'inbox/open/' skills/finish/SKILL.md &amp;&amp; grep -qF 'proposals/' skills/finish/SKILL.md &amp;&amp; grep -qF 'specs/' skills/finish/SKILL.md &amp;&amp; grep -qF 'plans/' skills/finish/SKILL.md &amp;&amp; grep -qF 'discussions/' skills/finish/SKILL.md &amp;&amp; grep -qiE 'NEVER force.push|never force-push|no force.push|force.push.{0,30}forbidden' skills/finish/SKILL.md &amp;&amp; grep -qiE 'NEVER.{0,30}rewrite history|no rewrite|never rewrite|history.{0,30}NEVER|amend|rebase' skills/finish/SKILL.md &amp;&amp; grep -qiE '\-\-amend|--amend' skills/finish/SKILL.md &amp;&amp; grep -qiE 'rebase' skills/finish/SKILL.md</automated>
  </verify>
  <done>skills/finish/SKILL.md exists with frontmatter (name=finish, version=1.0.0) and body covering: opening clarifier explicitly stating single-skill V1 exception per D97; Anti-Sycophancy Stance with 4 verbatim markers + closure-stance amplifier; Thread Check section enumerating 4 inspection items (final artifacts / open inbox / commits / unresolved concerns); Closure Question section enumerating 4 options (merge main / merge other / create PR / leave as is); Branch Operations section with NEVER force-push + NEVER rewrite history + named-forbidden-constructs (--amend / rebase); Workflow; Commit Policy; Immutability. All 4 Phase 1 docs cited by absolute path.</done>
</task>

<task type="auto">
  <name>Task 2: Author skills/whats-next/SKILL.md</name>
  <files>skills/whats-next/SKILL.md</files>
  <read_first>
    - skills/whats-next/SKILL.md (the file being created — empty / non-existent)
    - .planning/phases/07-merge-finish-navigation-distribution/07-CONTEXT.md (LOCKED: D33 thin OK; D105 chat-first answer + optional inbox capture on opt-in; D106 literal whats-next kebab-case name; NAV-01..03)
    - skills/discussion/SKILL.md (anti-sycophancy 4 markers verbatim — REQUIRED phrases)
    - skills/capture-inbox/SKILL.md (whats-next's optional opt-in output route; cite by absolute path)
    - docs/workflow/v1/thread-layout.md (V1 folder set — whats-next inspects this for advisory)
    - docs/workflow/v1/filename-grammar.md (referenced by inbox-capture route when user opts in)
    - docs/workflow/v1/immutability.md (whats-next reads artifacts READ-ONLY)
  </read_first>
  <action>
    Create the file with YAML frontmatter and a markdown body. Frontmatter required fields:
    - `name: whats-next`
    - `description:` ONE sentence with "Use when..." trigger + explicit interaction mode (advisory; chat-first answer; suggests 2–4 coherent next actions based on current thread context + recent artifacts; NEVER writes an artifact by default; on user opt-in MAY route to capture-inbox to save a suggestion). State NO sibling auto/interactive variants exist (V1 single advisory skill).
    - `metadata.author: https://github.com/Jei-sKappa`
    - `metadata.version: 1.0.0`

    Body sections (executor discretion on section names; behavioral tokens below MUST appear):

    1. **Opening clarifier paragraph**. State this is the V1 advisory navigation skill — chat-first answer rooted in the current thread context. The skill inspects what artifacts exist in the active thread (proposals/, specs/, plans/, discussions/, inbox/open/) and recent commits on the current branch, then suggests 2–4 next actions in chat. The skill writes NO artifact by default. The V1 body MAY be THIN per D33, NAV-03 — the canonical workflow guidance lives in the project README hybrid (to be produced by Plan 07-03). Cite all 4 Phase 1 canonical docs by absolute path on first invocation.

    2. **Anti-Sycophancy Stance** section. Carry the FOUR markers from `skills/discussion/SKILL.md` VERBATIM:
       - "Disagree when you disagree"
       - "Push back on weak or incomplete reasoning"
       - "Do not treat pushback as correctness"
       - "Refuse to log" (clause; verbatim phrasing from `skills/discussion/SKILL.md`)

       NO stage-specific amplifier. The skill is advisory — it suggests next actions, not opinions on settled work. Per 07-CONTEXT.md: "whats-next is advisory, not opinion-driven. Standard V1 voice; the 4-marker stance is included to keep V1 convention uniform without a stage-specific amplifier."

    3. **Inputs** section. Document:
       - The skill accepts an OPTIONAL hint from the user (e.g., "I just finished the spec — what's next?" / "I have an open inbox item — should I address it?" / "Thread feels stuck — what should I look at?"). The hint shapes the advisory framing.
       - If no hint is provided, the skill runs the default thread-context pass.
       - Ambiguity fallback per `docs/workflow/v1/immutability.md`: if the active thread is ambiguous (multiple thread roots, none clearly active), ASK the user which thread to advise on. Do NOT pick by recency.

    4. **Thread Inspection** section. Document what the skill inspects:
       - Active thread root at `docs/threads/<YYMMDDHHMMSSZ-slug>/` per `docs/workflow/v1/thread-layout.md`.
       - V1 folders: `proposals/`, `specs/`, `plans/`, `discussions/`, `inbox/open/`. Report which folders have artifacts and which are empty.
       - Recent commits on the current branch (last 5–10 — executor discretion; the purpose is signal, not audit).
       - The skill READS artifacts READ-ONLY per `docs/workflow/v1/immutability.md` — does NOT edit anything during inspection.

    5. **Chat-First Answer** section. MUST document the chat-first rule:
       - The skill's PRIMARY output is a chat response with 2–4 suggested next actions, each tied to observable thread context (e.g., "You have an emitted spec at <path> and no plan yet — consider running `plan-loose-auto` or `plan-strict-auto`"; "Your `inbox/open/` has 3 unaddressed review findings — consider walking them with `seeded-discussion` or `review-spec-interactive`").
       - The chat answer is FREEFORM markdown — no rigid template, no required sections, no fixed length. Per NAV-01.
       - The skill NEVER writes an artifact by default. The 2–4 suggestions stay in the chat reply.
       - Each suggestion SHOULD cite the V1 skill that would execute the action (e.g., "use `skills/spec-auto/SKILL.md`", "invoke `merge-artifacts-interactive`") — discoverability is the value here.

    6. **Optional Inbox Capture** section. MUST document the opt-in capture rule per NAV-02, D105:
       - AFTER surfacing the 2–4 suggestions, the skill MAY ask the user: "Want to save any of these as Inbox items for later?" — phrasing at executor discretion.
       - If the user opts IN to saving a suggestion as an Inbox item, the skill ROUTES TO `skills/capture-inbox/SKILL.md` to write the item. The skill itself does NOT bypass capture-inbox.
       - If the user opts OUT (or says nothing about saving), NO inbox-item is written. The suggestions stay in chat only.
       - The opt-in MUST be EXPLICIT — the skill does NOT auto-capture, does NOT capture by default, does NOT capture without affirmative user consent.

    7. **Thin Body OK Per D33** section (short). State explicitly that the V1 body MAY be thin per D33, NAV-03 — the skill is allowed to point the agent at the canonical README hybrid (Plan 07-03 deliverable at `README.md` toolbox model + layered map + recommended paths + per-module catalog) and instruct the agent to read that guidance before advising. The body is not required to duplicate the README's workflow map. Cite D33 by literal D-ID. Mention the README hybrid as the canonical workflow guidance source.

    8. **Workflow** section with numbered steps:
       - Resolve the active thread (ASK if ambiguous).
       - Capture any user hint about what they're trying to figure out.
       - Inspect the V1 folders + recent commits per `## Thread Inspection`.
       - Compose 2–4 chat suggestions tied to observable signals, citing the V1 skill that would execute each action.
       - Present the suggestions in chat.
       - ASK the user (optional) whether to save any suggestion as an Inbox item. If YES, route to `skills/capture-inbox/SKILL.md`. If NO (or silent), stop.
       - No closing remark.

    9. **Commit Policy** section. `whats-next` writes no artifacts by default and so does not commit anything. If the user opts IN to capture, capture-inbox handles its own commit policy (which is also NEVER auto-commit per Phase 2 capture-inbox rules). `whats-next` itself does NOT stage, commit, push, or branch.

    10. **Immutability** section. `whats-next` reads thread artifacts READ-ONLY per `docs/workflow/v1/immutability.md`. The skill does NOT edit any V1 artifact during inspection. The chat reply is ephemeral — not a V1 artifact.

    Voice and discipline:
    - 4 anti-sycophancy markers verbatim from `skills/discussion/SKILL.md`.
    - NO stage-specific amplifier (per CONTEXT.md — whats-next is advisory).
    - Chat-first rule: the primary output is chat, NOT an artifact.
    - Opt-in capture rule: capture happens ONLY on explicit user consent, routed through `skills/capture-inbox/SKILL.md`.
    - Thin OK per D33: the body MAY point at the README hybrid for the canonical workflow map.
    - All 4 Phase 1 docs + `skills/capture-inbox/SKILL.md` cited by absolute path.
    - Default-OFF artifact creation MUST be unambiguous (the phrase "never writes an artifact by default" or near-verbatim equivalent MUST appear).
  </action>
  <verify>
    <automated>test -f skills/whats-next/SKILL.md &amp;&amp; grep -qxF 'name: whats-next' skills/whats-next/SKILL.md &amp;&amp; grep -qxF '  version: 1.0.0' skills/whats-next/SKILL.md &amp;&amp; grep -qxF '  author: https://github.com/Jei-sKappa' skills/whats-next/SKILL.md &amp;&amp; grep -qF 'Use when' skills/whats-next/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/thread-layout.md' skills/whats-next/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/immutability.md' skills/whats-next/SKILL.md &amp;&amp; grep -qF 'skills/capture-inbox/SKILL.md' skills/whats-next/SKILL.md &amp;&amp; grep -qF 'Disagree when you disagree' skills/whats-next/SKILL.md &amp;&amp; grep -qF 'Push back on weak or incomplete reasoning' skills/whats-next/SKILL.md &amp;&amp; grep -qF 'Do not treat pushback as correctness' skills/whats-next/SKILL.md &amp;&amp; grep -qF 'Refuse to log' skills/whats-next/SKILL.md &amp;&amp; grep -qiE 'chat.first|chat first|primary output is chat' skills/whats-next/SKILL.md &amp;&amp; grep -qiE 'never writes an artifact|no artifact by default|does not write an artifact|writes no artifact' skills/whats-next/SKILL.md &amp;&amp; grep -qiE 'opt.in|opts.in|user.{0,20}consent|affirmative.*consent|user opts' skills/whats-next/SKILL.md &amp;&amp; grep -qiE 'D33|thin|thin OK|thin body' skills/whats-next/SKILL.md &amp;&amp; grep -qF 'proposals/' skills/whats-next/SKILL.md &amp;&amp; grep -qF 'specs/' skills/whats-next/SKILL.md &amp;&amp; grep -qF 'plans/' skills/whats-next/SKILL.md &amp;&amp; grep -qF 'inbox/open/' skills/whats-next/SKILL.md &amp;&amp; grep -qiE 'READ.ONLY|read only' skills/whats-next/SKILL.md &amp;&amp; grep -qiE 'README|canonical.*workflow|workflow map|toolbox' skills/whats-next/SKILL.md</automated>
  </verify>
  <done>skills/whats-next/SKILL.md exists with frontmatter (name=whats-next, version=1.0.0) and body covering: opening clarifier (advisory; chat-first; thin OK per D33); Anti-Sycophancy Stance with 4 verbatim markers (NO stage-specific amplifier); Inputs (optional user hint); Thread Inspection (V1 folders + recent commits, READ-ONLY); Chat-First Answer rule (NEVER writes an artifact by default); Optional Inbox Capture (opt-in only, routes to capture-inbox); Thin Body OK Per D33 statement (may point at canonical README hybrid); Workflow; Commit Policy; Immutability. All 4 Phase 1 docs + capture-inbox cited by absolute path.</done>
</task>

<task type="auto">
  <name>Task 3: Register finish + whats-next in marketplace.json, .vscode/settings.json, README.md</name>
  <files>.claude-plugin/marketplace.json, .vscode/settings.json, README.md</files>
  <read_first>
    - .claude-plugin/marketplace.json (state AFTER Plan 07-01: JeisKappa-workflow has 31 entries; JeisKappa-skills has 7)
    - .vscode/settings.json (state AFTER Plan 07-01: 38 alphabetically-sorted scopes)
    - README.md (state AFTER Plan 07-01: 38 "Available skills" entries + verification-coverage note + the-fool delegation note + 2 Retired entries + merge-artifacts-* entries from 07-01)
    - AGENTS.md (4-touchpoint rule)
    - .planning/phases/07-merge-finish-navigation-distribution/07-CONTEXT.md
    - skills/finish/SKILL.md (Task 1 output)
    - skills/whats-next/SKILL.md (Task 2 output)
  </read_first>
  <action>
    Update all three registration files atomically:

    1. **`.claude-plugin/marketplace.json`** — add `"./skills/finish"` and `"./skills/whats-next"` to `JeisKappa-workflow.skills` in alphabetical position. Alphabetically:
       - `finish` falls between `discussion` (last existing `d*`) and `implement-auto` (first `i*`) — i.e., between `./skills/discussion` and `./skills/implement-auto`.
       - `whats-next` falls at the END of the alphabetical list (after `spec-interactive` — which is currently last).
       After: JeisKappa-workflow array has exactly 33 entries (31 + 2). **This is the Phase 7 V1 plugin target.** JeisKappa-skills unchanged at 7.

    2. **`.vscode/settings.json`** — add `"finish"` and `"whats-next"` to `."conventionalCommits.scopes"` alphabetically.
       - `finish` falls between `discussion` and `implement-auto`.
       - `whats-next` falls at the end of the alphabetical list (after `the-librarian`).
       Net length 40 (38 + 2). Keep alphabetical. The jq accessor MUST use the quoted form `."conventionalCommits.scopes"`. **Phase 7 V1 scopes target is 40.**

    3. **`README.md`** — Add TWO new "Available skills" entries:
       - `### [\`finish\`](./skills/finish/SKILL.md)` — description paragraph + fenced `npx skills add Jei-sKappa/skills --skill finish` block. Description MUST per DIST-05:
         - State explicit interaction mode: SINGLE V1 skill with NO `-auto` / `-interactive` siblings (intentional V1 exception per D97 — branch disposition is inherently user-directed).
         - Mention the lightweight thread check (4 inspection items).
         - Mention the 4 closure options (merge main / merge other / create PR / leave as is).
         - Mention NEVER force-pushes / NEVER rewrites history.
         - Mention the 4-marker anti-sycophancy stance + closure-stance amplifier.
       - `### [\`whats-next\`](./skills/whats-next/SKILL.md)` — description paragraph + fenced `npx skills add Jei-sKappa/skills --skill whats-next` block. Description MUST per DIST-05:
         - State explicit interaction mode: advisory; chat-first answer; NEVER writes an artifact by default.
         - Mention optional Inbox capture on user opt-in (routes to `capture-inbox`).
         - Mention that the V1 body MAY be thin per D33 and may point at the canonical README hybrid for the full workflow map.
         - Mention the 4-marker anti-sycophancy stance (no stage-specific amplifier).

       Insert position: alphabetical placement within the existing simple-list "Available skills" section, BEFORE `## Retired skills`. Each entry placed in its own alphabetical position (finish near the d/i boundary; whats-next near the end).

       NOTE: Plan 07-03 will REPLACE the entire "Available skills" section with a hybrid layout. This plan adds 2 entries to the existing simple-list structure.

       DO NOT touch:
       - The `## Retired skills` section (preserve the 2 existing bullets — discussion-loop + review-decision-document).
       - The verification-coverage note from Plan 06-04 (preserve as-is).
       - The the-fool delegation note from Plan 06-05 (preserve as-is).
       - The merge-artifacts-* entries added by Plan 07-01 (preserve as-is).

    Final state after this plan:
    - .claude-plugin/marketplace.json `JeisKappa-workflow.skills` length: **33** (31 -> 33; Phase 7 V1 plugin target reached)
    - .vscode/settings.json `."conventionalCommits.scopes"` length: **40** (38 -> 40; Phase 7 V1 scopes target reached)
    - README.md "Available skills" entries: **40** (38 -> 40) — count via `grep -c '^### \[`' README.md`
    - README.md verification-coverage note + the-fool delegation note + Retired skills subsection + merge-artifacts-* entries all UNTOUCHED.
    - Do NOT commit.
  </action>
  <verify>
    <automated>jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length == 33' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/finish")' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/whats-next")' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '.plugins[] | select(.name=="JeisKappa-skills") | .skills | length == 7' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" | length == 40' .vscode/settings.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" | index("finish")' .vscode/settings.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" | index("whats-next")' .vscode/settings.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" == (."conventionalCommits.scopes" | sort)' .vscode/settings.json &gt;/dev/null &amp;&amp; [ "$(grep -c '^### \[`finish`\]' README.md)" = "1" ] &amp;&amp; [ "$(grep -c '^### \[`whats-next`\]' README.md)" = "1" ] &amp;&amp; [ "$(grep -c 'npx skills add Jei-sKappa/skills --skill finish' README.md)" = "1" ] &amp;&amp; [ "$(grep -c 'npx skills add Jei-sKappa/skills --skill whats-next' README.md)" = "1" ] &amp;&amp; grep -qF 'the-fool' README.md &amp;&amp; grep -qiE 'no separate.*verify|verification.{0,30}review-implementation|review-implementation.{0,30}verification' README.md &amp;&amp; grep -qF 'discussion-loop' README.md &amp;&amp; grep -qF 'review-decision-document' README.md &amp;&amp; grep -qF 'merge-artifacts-auto' README.md &amp;&amp; grep -qF 'merge-artifacts-interactive' README.md &amp;&amp; F=$(grep -n '^### \[`finish`\]' README.md | cut -d: -f1) &amp;&amp; W=$(grep -n '^### \[`whats-next`\]' README.md | cut -d: -f1) &amp;&amp; R=$(grep -n '^## Retired skills' README.md | cut -d: -f1) &amp;&amp; [ "$F" -lt "$R" ] &amp;&amp; [ "$W" -lt "$R" ] &amp;&amp; [ "$(grep -c '^### \[`' README.md)" = "40" ] &amp;&amp; echo OK</automated>
  </verify>
  <done>marketplace.json JeisKappa-workflow array has 33 entries (31 -> 33; Phase 7 V1 plugin target reached). .vscode/settings.json scopes has 40 entries (38 -> 40; Phase 7 V1 scopes target reached), alphabetical (jq accessor uses `."conventionalCommits.scopes"` form). README.md has 2 new "Available skills" entries inserted before `## Retired skills`. Phase 6 documentation notes + Plan 07-01 merge-artifacts-* entries + Retired skills subsection all preserved untouched. Available skills count is 40. No git commit created.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

1. **Skill files exist:**
   ```bash
   ls -la skills/finish/SKILL.md skills/whats-next/SKILL.md
   ```

2. **finish behavioral tokens (single-skill V1 exception + 4 thread-check items + 4 closure options + no force-push):**
   ```bash
   for TOKEN in "Disagree when you disagree" "Push back on weak or incomplete reasoning" "Do not treat pushback as correctness" "Refuse to log"; do
     grep -qF "$TOKEN" skills/finish/SKILL.md || echo "FAIL finish: $TOKEN"
   done
   grep -qiE 'D97|exception' skills/finish/SKILL.md || echo "FAIL finish: D97 exception statement"
   grep -qiE 'merge into main' skills/finish/SKILL.md || echo "FAIL finish: merge into main option"
   grep -qiE 'merge into other|merge into another' skills/finish/SKILL.md || echo "FAIL finish: merge into other option"
   grep -qiE 'create PR' skills/finish/SKILL.md || echo "FAIL finish: create PR option"
   grep -qiE 'leave as is|leave as-is' skills/finish/SKILL.md || echo "FAIL finish: leave as is option"
   grep -qiE 'NEVER force.push|never force-push' skills/finish/SKILL.md || echo "FAIL finish: NEVER force-push"
   grep -qiE 'amend|rebase' skills/finish/SKILL.md || echo "FAIL finish: history-rewrite forbidden constructs"
   grep -qiE 'branch operations are hard to undo|hard to undo' skills/finish/SKILL.md || echo "FAIL finish: closure-stance amplifier"
   ```

3. **whats-next behavioral tokens (chat-first + opt-in inbox capture + thin OK + no amplifier):**
   ```bash
   for TOKEN in "Disagree when you disagree" "Push back on weak or incomplete reasoning" "Do not treat pushback as correctness" "Refuse to log"; do
     grep -qF "$TOKEN" skills/whats-next/SKILL.md || echo "FAIL whats-next: $TOKEN"
   done
   grep -qiE 'chat.first|chat first' skills/whats-next/SKILL.md || echo "FAIL whats-next: chat-first rule"
   grep -qiE 'never writes an artifact|no artifact by default|writes no artifact' skills/whats-next/SKILL.md || echo "FAIL whats-next: default-off artifact creation"
   grep -qiE 'opt.in|user opts' skills/whats-next/SKILL.md || echo "FAIL whats-next: opt-in capture"
   grep -qiE 'D33|thin' skills/whats-next/SKILL.md || echo "FAIL whats-next: D33 thin-OK"
   grep -qF 'skills/capture-inbox/SKILL.md' skills/whats-next/SKILL.md || echo "FAIL whats-next: capture-inbox citation"
   ```

4. **Registration counts (Phase 7 V1 targets):**
   ```bash
   jq '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length' .claude-plugin/marketplace.json    # expect 33 (Phase 7 V1 plugin target)
   jq '.plugins[] | select(.name=="JeisKappa-skills") | .skills | length' .claude-plugin/marketplace.json      # expect 7
   jq '."conventionalCommits.scopes" | length' .vscode/settings.json                                              # expect 40 (Phase 7 V1 scopes target)
   grep -c '^### \[`' README.md                                                                                    # expect 40
   ```

5. **Documentation preservation:**
   ```bash
   grep -qF 'the-fool' README.md || echo "FAIL: the-fool delegation note must remain"
   grep -qiE 'no separate.*verify|verification.{0,30}review-implementation' README.md || echo "FAIL: verification-coverage note must remain"
   grep -qF 'merge-artifacts-auto' README.md || echo "FAIL: Plan 07-01 merge-artifacts-auto entry must remain"
   grep -qF 'merge-artifacts-interactive' README.md || echo "FAIL: Plan 07-01 merge-artifacts-interactive entry must remain"
   grep -qF 'discussion-loop' README.md || echo "FAIL: Retired discussion-loop entry must remain"
   grep -qF 'review-decision-document' README.md || echo "FAIL: Retired review-decision-document entry must remain"
   ```
</verification>

<success_criteria>
- skills/finish/SKILL.md exists with frontmatter (name=finish, version=1.0.0) and body covering: single-skill V1 exception per D97; 4-marker anti-sycophancy stance + closure-stance amplifier; Thread Check enumerating 4 inspection items; Closure Question enumerating 4 options; Branch Operations with NEVER force-push + NEVER rewrite history + named-forbidden-constructs; Workflow; Commit Policy; Immutability.
- skills/whats-next/SKILL.md exists with frontmatter (name=whats-next, version=1.0.0) and body covering: chat-first advisory rule; 4-marker anti-sycophancy stance (NO amplifier); Thread Inspection (READ-ONLY); Chat-First Answer (NEVER writes by default); Optional Inbox Capture (opt-in only, routes to capture-inbox); Thin OK per D33; Workflow; Commit Policy; Immutability.
- All 4 Phase 1 canonical docs cited by absolute path in both skills.
- marketplace.json JeisKappa-workflow array has exactly 33 entries (31 -> 33; Phase 7 V1 plugin target reached).
- .vscode/settings.json conventionalCommits.scopes has exactly 40 entries (38 -> 40; Phase 7 V1 scopes target reached), alphabetical.
- README.md "Available skills" has 40 entries (38 -> 40); both finish + whats-next entries inserted before `## Retired skills`.
- README.md verification-coverage note, the-fool delegation note, merge-artifacts-* entries, and Retired skills subsection all preserved untouched.
- No git commit created.
- Plan 07-03 (README hybrid redesign) unblocked.
</success_criteria>

<output>
Create `.planning/phases/07-merge-finish-navigation-distribution/07-02-SUMMARY.md` when done. Record:
- Files created: skills/finish/SKILL.md, skills/whats-next/SKILL.md
- Files modified: .claude-plugin/marketplace.json (workflow 31 -> 33; Phase 7 V1 plugin target REACHED), .vscode/settings.json (scopes 38 -> 40; Phase 7 V1 scopes target REACHED), README.md (Available skills 38 -> 40; preserves verification-coverage + the-fool documentation notes + Retired skills subsection + Plan 07-01 merge-artifacts-* entries)
- Requirements addressed: FNSH-01 (single skill D97 exception), FNSH-02 (lightweight thread check 4 items D98), FNSH-03 (4-option closure question D98); NAV-01 (chat-first advisory D105/D106), NAV-02 (optional inbox capture on opt-in D105), NAV-03 (thin OK per D33)
- Phase 7 V1 SKILL ARTIFACTS COMPLETE: All 4 new V1 skills (merge-artifacts-auto, merge-artifacts-interactive, finish, whats-next) shipped; JeisKappa-workflow plugin at 33 (Phase 7 V1 target reached); .vscode scopes at 40 (Phase 7 V1 scopes target reached). Plan 07-03 (README hybrid redesign — DIST-01..03 + DIST-05) is the only remaining work in the milestone.
</output>
</content>
</invoke>