---
phase: 05-implementation-family
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/implement-auto/SKILL.md
  - skills/implement-interactive/SKILL.md
  - .claude-plugin/marketplace.json
  - .vscode/settings.json
  - README.md
autonomous: true
requirements:
  - IMPL-01
  - IMPL-02
  - IMPL-10
  - IMPL-11
  - IMPL-12
  - IMPL-13
  - IMPL-14

must_haves:
  truths:
    - "User can invoke `implement-auto` from less-structured input (spec / proposal / decision-log / GitHub issue / Inbox item / code context / raw prompt) and the agent derives implicit tasks if needed, executes end-to-end, self-reviews after each implicit task, and auto-commits per implicit task or per explicit Git instruction the user passes through."
    - "User can invoke `implement-interactive` and have the agent ASK before committing at each equivalent checkpoint (same boundary granularity as `implement-auto`)."
    - "User reading either skill body can verify the four-state status protocol is documented: `DONE`, `DONE_WITH_CONCERNS`, `BLOCKED`, `NEEDS_CONTEXT` (D74). Every task report carries one of these states + 1-3 sentence notes + suggested next action. State is in chat output and/or commit messages; not a separate artifact file."
    - "User reading either skill body can verify the failed-commit handling: failed commit reports `BLOCKED`, stops the flow, does NOT retry without explicit user instruction (D77)."
    - "User reading either skill body can verify the no-history-rewrite rule: NO `--amend`, NO rebase, NO force-push, NO history rewriting (general convention + D77 implication)."
    - "User reading either skill body can verify the dirty-worktree handling: check at start; if dirty, ASK 'continue/abort' (D79, single-agent variant)."
    - "User running `npx skills add` finds both new skills installable via the `JeisKappa-workflow` marketplace plugin after registration."
  artifacts:
    - path: "skills/implement-auto/SKILL.md"
      provides: "Autonomous less-structured-input implementation skill"
      contains: "frontmatter with name=implement-auto, version=1.0.0; ## Workflow section; four-state status protocol documented; literal 'DONE', 'DONE_WITH_CONCERNS', 'BLOCKED', 'NEEDS_CONTEXT' tokens; literal failed-commit → BLOCKED phrase; literal NO `--amend` / no rebase / no force-push prohibition; dirty-worktree check documented"
    - path: "skills/implement-interactive/SKILL.md"
      provides: "Collaborative less-structured-input implementation skill"
      contains: "frontmatter with name=implement-interactive, version=1.0.0; ## Anti-Sycophancy Stance with 4 verbatim markers from discussion/SKILL.md; ## Workflow section; four-state status protocol; failed-commit → BLOCKED phrase; no-history-rewrite prohibition; dirty-worktree check; explicit 'ASK before committing' clause at each checkpoint"
    - path: ".claude-plugin/marketplace.json"
      provides: "Marketplace registration for both new skills"
      contains: "./skills/implement-auto and ./skills/implement-interactive entries under JeisKappa-workflow (total array length now 15)"
    - path: ".vscode/settings.json"
      provides: "Conventional-commit scopes for both new skills"
      contains: "implement-auto and implement-interactive in conventionalCommits.scopes, alphabetically sorted (total length now 23)"
    - path: "README.md"
      provides: "Public install snippets for both new skills"
      contains: "### `implement-auto` and ### `implement-interactive` headings with `npx skills add` install commands"
  key_links:
    - from: "skills/implement-auto/SKILL.md"
      to: "docs/workflow/v1/immutability.md"
      via: "citation by absolute path (read plan / spec / proposal artifacts immutably)"
      pattern: "docs/workflow/v1/immutability.md"
    - from: "skills/implement-auto/SKILL.md"
      to: "docs/workflow/v1/thread-layout.md"
      via: "citation by absolute path"
      pattern: "docs/workflow/v1/thread-layout.md"
    - from: "skills/implement-interactive/SKILL.md"
      to: "skills/discussion/SKILL.md"
      via: "anti-sycophancy stance carried verbatim"
      pattern: "Disagree when you disagree|Do not treat pushback as correctness"
    - from: ".claude-plugin/marketplace.json"
      to: "skills/implement-auto/ and skills/implement-interactive/"
      via: "JeisKappa-workflow.skills array entries"
      pattern: "./skills/implement-auto|./skills/implement-interactive"
---

<objective>
Ship the less-structured-input implementation pair: `implement-auto` (autonomous end-to-end implementation from a spec / proposal / decision-log / GitHub issue / Inbox item / code context / raw prompt with auto-commits per implicit task or per explicit Git instruction) and `implement-interactive` (same input shape and execution flow but ASKS before committing at each equivalent checkpoint). Both skills run as single-agent (current session + self-review; no subagents). Both honor the V1 four-state status protocol (D74), the failed-commit → BLOCKED rule (D77), the no-history-rewrite prohibition, and the dirty-worktree check (D79). Register both skills in the three V1 touchpoints (`marketplace.json`, `.vscode/settings.json`, `README.md`).

Purpose: The less-structured-input pair is the first of three pairs in Phase 5. Plan-driven siblings land in Plan 05-02 (single-agent) and Plan 05-03 (multi-subagent). Per D66, only plan-driven input gets a `*-with-subagents-*` variant — the less-structured-input pair has no subagent variant in V1.

Output: Two new skill folders + three registration files updated. All four CLAUDE.md touchpoints satisfied for each skill.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-implementation-family/05-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@AGENTS.md

@docs/workflow/v1/README.md
@docs/workflow/v1/thread-layout.md
@docs/workflow/v1/filename-grammar.md
@docs/workflow/v1/immutability.md

<!-- Phase 4 body-shape and interactive-stance pattern sources -->
@skills/plan-loose-auto/SKILL.md
@skills/plan-strict-auto/SKILL.md
@skills/plan-loose-interactive/SKILL.md
@skills/plan-strict-interactive/SKILL.md
@skills/discussion/SKILL.md

@.claude-plugin/marketplace.json
@.vscode/settings.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author skills/implement-auto/SKILL.md</name>
  <read_first>
    - skills/implement-auto/SKILL.md (the file being created — empty / non-existent)
    - .planning/phases/05-implementation-family/05-CONTEXT.md (LOCKED decisions)
    - skills/plan-loose-auto/SKILL.md (Phase 4 closest body-shape analog — auto half pattern: voice, citation discipline, opening clarifier with sibling references)
    - skills/plan-strict-auto/SKILL.md (Phase 4 — citation-first density)
    - skills/discussion/SKILL.md (for the anti-sycophancy stance source — referenced here for cross-pair consistency, not used in this auto half)
    - docs/workflow/v1/thread-layout.md (folder set; implementation skills READ plans/specs/etc. and operate on source code)
    - docs/workflow/v1/immutability.md (plans/specs are immutable — implementation skills must not edit them)
    - docs/workflow/v1/filename-grammar.md (for reading versioned plan filenames as input)
  </read_first>
  <files>skills/implement-auto/SKILL.md</files>
  <action>
    Create the file with YAML frontmatter and a markdown body. Frontmatter required fields:
    - `name: implement-auto`
    - `description:` a single sentence including the "Use when..." trigger AND explicit interaction mode (autonomous, end-to-end). Mention the sibling `implement-interactive` and the plan-driven siblings (`implement-plan-auto`, `implement-plan-interactive`, `implement-plan-with-subagents-auto`, `implement-plan-with-subagents-interactive`). Make clear that this skill accepts LESS-STRUCTURED input (spec / proposal / decision-log / GitHub issue / Inbox item / code context / raw prompt — NOT a plan artifact; for plan-driven input, point at the plan-driven siblings).
    - `metadata.author: https://github.com/Jei-sKappa`
    - `metadata.version: 1.0.0`

    Body sections (executor discretion on section names; behavioral tokens below MUST appear):

    1. **Opening clarifier paragraph** naming the sibling `implement-interactive` and the four plan-driven siblings (`implement-plan-auto`, `implement-plan-interactive`, `implement-plan-with-subagents-auto`, `implement-plan-with-subagents-interactive`). State that the less-structured-input pair is the right pick when there is no plan artifact and the implementer must derive implicit tasks from the input itself; for a plan-driven flow, the user should pick a plan-driven sibling instead. State the V1 stance per CONTEXT.md decisions: this skill is single-agent (current session + self-review); no subagents are spawned.

    2. **Inputs** section listing the SEVEN accepted input forms (named explicitly per D64/D65 + CONTEXT.md):
       - A spec artifact path under `docs/threads/<thread>/specs/<UTC>-v<N>-spec.md`
       - A proposal artifact path under `docs/threads/<thread>/proposals/<UTC>-<kebab-desc>-proposal.md`
       - A decision-log artifact path under `docs/threads/<thread>/discussions/<UTC>-<kebab-desc>-decision-log.md`
       - A GitHub issue URL or `owner/repo#NNN` identifier
       - An Inbox item path under `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-inbox-item.md`
       - A code context reference (file path or directory)
       - A raw user prompt
       Document the ambiguity fallback: ASK the user which input is intended per `docs/workflow/v1/immutability.md` ("Ambiguous Reference Resolution"). No global "latest input" algorithm.

    3. **Four-State Status Protocol** section (D74, IMPL-10). MUST document the four states by NAME (`DONE`, `DONE_WITH_CONCERNS`, `BLOCKED`, `NEEDS_CONTEXT`) with a one-sentence definition for each. MUST describe the task-report block shape (approx 5-10 lines): state line + 1-3 sentence notes + suggested next action. State explicitly: the four-state status appears in chat output and/or commit messages; it is NOT written to a separate artifact file in V1. The skill body MAY suggest the format `Task <N> status: <STATE>` / `Notes: ...` / `Next: ...` but the exact wording is at executor discretion.

    4. **Dirty Worktree Handling** section (D79, IMPL-14). MUST document the check: at start, inspect the worktree (e.g., `git status --porcelain`); if dirty, ASK the user "Continue (and let the uncommitted changes be picked up by the next commit) / abort?". Single-agent variant — this skill itself owns the check (there is no orchestrator in this skill).

    5. **Workflow** section with numbered steps. MUST include:
       - Resolve thread + load input (cite `docs/workflow/v1/immutability.md` for ambiguity fallback).
       - Read the input. If the input requires deriving implicit tasks (e.g., a raw prompt; a spec with no plan; an Inbox item naming a fix), derive them in-session as an ordered list of implicit tasks. If the input is fully resolved (e.g., "the user said do X to file Y"), the implicit task list may be one task.
       - For each implicit task: implement → self-review → commit (commit cadence per `## Commit Policy`) → write task report (four-state status).
       - On failed commit: report `BLOCKED`, stop. Do not retry without explicit user instruction.
       - Final message: list each implicit task by its four-state status; cite the commits made (SHA + subject).

    6. **Commit Policy** section. MUST contain:
       - The cadence rule for `implement-auto` (D75): per implicit task IF the agent derives them, OR per explicit Git instruction the user passes through. Agent uses judgment.
       - Failed-commit → BLOCKED (D77). The literal phrase MUST appear: "If a commit fails, report `BLOCKED` and stop. Do not retry the commit without explicit user instruction." (or near-verbatim equivalent containing "BLOCKED" and "stop").
       - No history rewriting. The literal phrase MUST appear: "This skill does NOT rewrite history — no `--amend`, no rebase, no force-push." (or near-verbatim equivalent containing the three forbidden git operations by name).

    7. **Plan Deviation Policy** section (D80). MUST state: follow the plan or input; use judgment if the input is unclear, contradicts the observed code state, or omits an obvious step that blocks progress; surface deviations in the task report (`DONE_WITH_CONCERNS` if minor, `NEEDS_CONTEXT` if major). No mandatory pre-clearance, no blanket permission.

    8. **Immutability** section: input artifacts (specs, proposals, decision logs, Inbox items, plan artifacts the user passes through) are IMMUTABLE per `docs/workflow/v1/immutability.md` — the implementer reads them READ-ONLY. The implementer modifies SOURCE CODE; it does NOT modify the input artifacts.

    Voice and discipline:
    - Dense, declarative, citation-first. Match Phase 4 plan skills' density.
    - Cite Phase 1 canonical docs by ABSOLUTE PATH on first invocation.
    - Do NOT include a `## Anti-Sycophancy Stance` section (this is the AUTO half).
    - Do NOT embed code from full reviewer prompts.
  </action>
  <acceptance_criteria>
    Run these checks and ALL must pass:

    1. `test -f skills/implement-auto/SKILL.md`.
    2. `grep -c "^name: implement-auto$" skills/implement-auto/SKILL.md` — exactly 1.
    3. `grep -c "^  version: 1.0.0$" skills/implement-auto/SKILL.md` — at least 1.
    4. `grep -c "^  author: https://github.com/Jei-sKappa$" skills/implement-auto/SKILL.md` — at least 1.
    5. `grep -c "Use when" skills/implement-auto/SKILL.md` — at least 1.
    6. `grep -c "implement-interactive" skills/implement-auto/SKILL.md` — at least 1 (sibling reference).
    7. `grep -c "implement-plan" skills/implement-auto/SKILL.md` — at least 1 (plan-driven sibling reference).
    8. `grep -c "DONE_WITH_CONCERNS" skills/implement-auto/SKILL.md` — at least 1.
    9. `grep -c "BLOCKED" skills/implement-auto/SKILL.md` — at least 1.
    10. `grep -c "NEEDS_CONTEXT" skills/implement-auto/SKILL.md` — at least 1.
    11. `grep -cE "(^| )DONE($| |\\.|,|\`)" skills/implement-auto/SKILL.md` — at least 1 (the bare DONE state is named; the regex tolerates DONE as a token).
    12. `grep -c -i "amend" skills/implement-auto/SKILL.md` — at least 1 (no --amend is named-and-forbidden).
    13. `grep -c -i "force-push\|force push" skills/implement-auto/SKILL.md` — at least 1.
    14. `grep -c -i "rebase" skills/implement-auto/SKILL.md` — at least 1.
    15. `grep -c -i "dirty worktree\|worktree" skills/implement-auto/SKILL.md` — at least 1 (D79 dirty-worktree check documented).
    16. `grep -c -i "docs/workflow/v1/immutability.md" skills/implement-auto/SKILL.md` — at least 1.
    17. `grep -c -i "docs/workflow/v1/thread-layout.md" skills/implement-auto/SKILL.md` — at least 1.
    18. `grep -cE -i "failed commit|commit fail|commit fails" skills/implement-auto/SKILL.md` — at least 1.
    19. `grep -c -i "self-review" skills/implement-auto/SKILL.md` — at least 1.
    20. `grep -c -i "four-state\|four state" skills/implement-auto/SKILL.md` — at least 1.
    21. The body MUST NOT contain force-push instructions. `grep -E "git push --force|git push -f|git commit --amend|git rebase " skills/implement-auto/SKILL.md` returns nothing (the skill names these as forbidden, not as instructions; the negative grep matches a literal command that would be an instruction to do it).
    22. `grep -c -i "ASK" skills/implement-auto/SKILL.md` — at least 1 (dirty-worktree prompt uses ASK language).
  </acceptance_criteria>
  <verify>
    <automated>
      test -f skills/implement-auto/SKILL.md && \
      grep -q "^name: implement-auto$" skills/implement-auto/SKILL.md && \
      grep -q "DONE_WITH_CONCERNS" skills/implement-auto/SKILL.md && \
      grep -q "BLOCKED" skills/implement-auto/SKILL.md && \
      grep -q "NEEDS_CONTEXT" skills/implement-auto/SKILL.md && \
      grep -qi "amend" skills/implement-auto/SKILL.md && \
      grep -qi "force-push\|force push" skills/implement-auto/SKILL.md && \
      grep -qi "rebase" skills/implement-auto/SKILL.md && \
      grep -qi "worktree" skills/implement-auto/SKILL.md && \
      grep -q "docs/workflow/v1/immutability.md" skills/implement-auto/SKILL.md && \
      grep -qi "four-state\|four state" skills/implement-auto/SKILL.md && \
      ! grep -qE "git push --force|git push -f|git commit --amend|git rebase " skills/implement-auto/SKILL.md && \
      echo "implement-auto SKILL.md acceptance passes"
    </automated>
  </verify>
  <done>skills/implement-auto/SKILL.md exists with frontmatter + body covering: opening clarifier with all sibling references, 7-input acceptance with ambiguity fallback, four-state status protocol (DONE / DONE_WITH_CONCERNS / BLOCKED / NEEDS_CONTEXT) documented with task-report block shape, commit cadence per D75 (per implicit task or per explicit Git instruction), failed-commit → BLOCKED rule per D77, NO --amend / no rebase / no force-push prohibition, dirty-worktree check per D79 (single-agent variant — this skill owns the check), plan-deviation policy per D80, immutability of input artifacts per `docs/workflow/v1/immutability.md`, V1 doc citations by absolute path. NO anti-sycophancy section (auto half).</done>
</task>

<task type="auto">
  <name>Task 2: Author skills/implement-interactive/SKILL.md</name>
  <read_first>
    - skills/implement-interactive/SKILL.md (the file being created — empty / non-existent)
    - skills/implement-auto/SKILL.md (Task 1 output — sibling consistency)
    - .planning/phases/05-implementation-family/05-CONTEXT.md
    - skills/plan-loose-interactive/SKILL.md (Phase 4 interactive variant pattern — 4 anti-sycophancy markers verbatim, framing line)
    - skills/plan-strict-interactive/SKILL.md (Phase 4 — interactive pattern with stakes amplifier)
    - skills/discussion/SKILL.md (anti-sycophancy stance source — 4 marker phrases for verbatim carry-over)
    - docs/workflow/v1/thread-layout.md
    - docs/workflow/v1/immutability.md
    - docs/workflow/v1/filename-grammar.md
  </read_first>
  <files>skills/implement-interactive/SKILL.md</files>
  <action>
    Create the file with YAML frontmatter and a markdown body. Frontmatter required fields:
    - `name: implement-interactive`
    - `description:` a single sentence with the "Use when..." trigger AND explicit interaction mode (collaborative; agent ASKS before committing at each equivalent checkpoint). Mention the sibling `implement-auto` and the four plan-driven siblings.
    - `metadata.author: https://github.com/Jei-sKappa`
    - `metadata.version: 1.0.0`

    Body sections (executor discretion on section names; behavioral tokens below MUST appear):

    1. **Opening clarifier paragraph** naming the sibling `implement-auto` and the four plan-driven siblings, restating the single-agent topology.

    2. **Anti-Sycophancy Stance** section. Carry the FOUR anti-sycophancy markers from `skills/discussion/SKILL.md` VERBATIM (substring matches required by acceptance criteria):
       - "Disagree when you disagree" clause
       - "Push back on weak or incomplete reasoning" clause (regex tolerates "weak" or "incomplete")
       - "Do not treat pushback as correctness" clause
       - "Refuse to log a <task or commit> you believe is wrong without flagging it" clause (per Plan 04-01/04-02 pattern, the clause is adapted to "Refuse to log a [task / commit / implementation decision] you believe is wrong without flagging it" — the verbatim marker is the leading "Refuse to log" + the trailing "without flagging it" predicate)
       Plus the EXECUTION-TIME stakes amplifier (replacing the planning-stage one): "Bad commits become expensive to rewind — the cheap moment to push back is BEFORE the commit lands, because commit history is sacred (no `--amend`, no rebase, no force-push)." or near-verbatim equivalent containing "Bad commits become expensive to rewind".

    3. **Inputs** section listing the SEVEN accepted input forms (same as `implement-auto`).

    4. **Four-State Status Protocol** section (D74, IMPL-10). Same four states with definitions and task-report block shape.

    5. **Dirty Worktree Handling** section (D79, IMPL-14). Same single-agent check as `implement-auto`: this skill owns the check.

    6. **Workflow** section with numbered steps. MUST include:
       - Resolve thread + load input.
       - Read the input. Walk implicit tasks with the user (collaborative delta from auto sibling): present each implicit task, push back per the `## Anti-Sycophancy Stance` when warranted, adjust scope in conversation.
       - For each implicit task: implement → self-review → ASK the user before committing → on confirm, commit (commit cadence per `## Commit Policy`) → write task report (four-state status).
       - On failed commit: report `BLOCKED`, stop. Do not retry without explicit user instruction.
       - Final message: list each implicit task by its four-state status; cite commits made.

    7. **Commit Policy** section. MUST contain:
       - The cadence rule for `implement-interactive` (D76): same boundary granularity as `implement-auto` (per implicit task or per explicit Git instruction) but ASKS the user before each commit. The literal phrase "ASK the user before committing" (or near-verbatim with "ASK" + "before committing") MUST appear.
       - Failed-commit → BLOCKED (D77). Same literal phrase as `implement-auto`.
       - No history rewriting. Same literal phrase as `implement-auto`: NO `--amend`, no rebase, no force-push.

    8. **Plan Deviation Policy** section (D80). Same as `implement-auto`. Add a note that during the walk, deviations are surfaced LIVE to the user rather than only in the task report — push back per the anti-sycophancy stance.

    9. **Decision Log** section enforcing D93. Per D93, the default behavior is to NOT auto-write a separate decision log. Write a log ONLY when durable trade-offs / rejected alternatives emerge that cannot reasonably be captured in commit messages or the four-state task report. When written, use `docs/threads/<thread>/discussions/<UTC>-<kebab-desc>-decision-log.md` with `## D<N>:` sequential headings. Mirror Phase 4 interactive skills' pattern.

    10. **Scope Drift** section. PARK as Inbox item via `capture-inbox`, SPLIT into own task / plan / discussion, or DEFER. Ask the user which.

    11. **Immutability** section: same as `implement-auto` — input artifacts are READ-ONLY; the implementer modifies source code only.

    Voice and discipline:
    - Dense, declarative, citation-first.
    - All 4 anti-sycophancy marker phrases MUST be present verbatim or near-verbatim (substring matches required).
    - The execution-time stakes amplifier MUST appear ("Bad commits become expensive to rewind" or near-verbatim).
  </action>
  <acceptance_criteria>
    Run these checks and ALL must pass:

    1. `test -f skills/implement-interactive/SKILL.md`.
    2. `grep -c "^name: implement-interactive$" skills/implement-interactive/SKILL.md` — exactly 1.
    3. `grep -c "^  version: 1.0.0$" skills/implement-interactive/SKILL.md` — at least 1.
    4. `grep -c "^  author: https://github.com/Jei-sKappa$" skills/implement-interactive/SKILL.md` — at least 1.
    5. `grep -c "Use when" skills/implement-interactive/SKILL.md` — at least 1.
    6. `grep -c "implement-auto" skills/implement-interactive/SKILL.md` — at least 1 (sibling reference).
    7. `grep -c "implement-plan" skills/implement-interactive/SKILL.md` — at least 1 (plan-driven sibling reference).
    8. `grep -c -i "Anti-Sycophancy" skills/implement-interactive/SKILL.md` — at least 1.
    9. `grep -c "Disagree when you disagree" skills/implement-interactive/SKILL.md` — at least 1 (anti-sycophancy marker 1).
    10. `grep -cE "Push back on (weak|incomplete)" skills/implement-interactive/SKILL.md` — at least 1 (marker 2).
    11. `grep -c "Do not treat pushback as correctness" skills/implement-interactive/SKILL.md` — at least 1 (marker 3).
    12. `grep -cE "Refuse to log" skills/implement-interactive/SKILL.md` — at least 1 (marker 4).
    13. `grep -cE "expensive to rewind|expensive to undo" skills/implement-interactive/SKILL.md` — at least 1 (execution-time stakes amplifier).
    14. `grep -c "DONE_WITH_CONCERNS" skills/implement-interactive/SKILL.md` — at least 1.
    15. `grep -c "BLOCKED" skills/implement-interactive/SKILL.md` — at least 1.
    16. `grep -c "NEEDS_CONTEXT" skills/implement-interactive/SKILL.md` — at least 1.
    17. `grep -c -i "amend" skills/implement-interactive/SKILL.md` — at least 1.
    18. `grep -c -i "force-push\|force push" skills/implement-interactive/SKILL.md` — at least 1.
    19. `grep -c -i "rebase" skills/implement-interactive/SKILL.md` — at least 1.
    20. `grep -c -i "worktree" skills/implement-interactive/SKILL.md` — at least 1.
    21. `grep -c "docs/workflow/v1/immutability.md" skills/implement-interactive/SKILL.md` — at least 1.
    22. `grep -c "docs/workflow/v1/thread-layout.md" skills/implement-interactive/SKILL.md` — at least 1.
    23. `grep -cE -i "ASK.*before committing|before commit.*ASK" skills/implement-interactive/SKILL.md` — at least 1 (interactive ASK clause).
    24. `grep -cE -i "failed commit|commit fail|commit fails" skills/implement-interactive/SKILL.md` — at least 1.
    25. `grep -c -i "Decision Log\|decision log" skills/implement-interactive/SKILL.md` — at least 1 (D93 section present).
    26. `grep -c -i "four-state\|four state" skills/implement-interactive/SKILL.md` — at least 1.
    27. The body MUST NOT contain force-push instructions. `grep -E "git push --force|git push -f|git commit --amend|git rebase " skills/implement-interactive/SKILL.md` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>
      test -f skills/implement-interactive/SKILL.md && \
      grep -q "^name: implement-interactive$" skills/implement-interactive/SKILL.md && \
      grep -qi "Anti-Sycophancy" skills/implement-interactive/SKILL.md && \
      grep -q "Disagree when you disagree" skills/implement-interactive/SKILL.md && \
      grep -qE "Push back on (weak|incomplete)" skills/implement-interactive/SKILL.md && \
      grep -q "Do not treat pushback as correctness" skills/implement-interactive/SKILL.md && \
      grep -qE "Refuse to log" skills/implement-interactive/SKILL.md && \
      grep -qE "expensive to rewind|expensive to undo" skills/implement-interactive/SKILL.md && \
      grep -q "DONE_WITH_CONCERNS" skills/implement-interactive/SKILL.md && \
      grep -q "BLOCKED" skills/implement-interactive/SKILL.md && \
      grep -q "NEEDS_CONTEXT" skills/implement-interactive/SKILL.md && \
      grep -qi "amend" skills/implement-interactive/SKILL.md && \
      grep -qi "force-push\|force push" skills/implement-interactive/SKILL.md && \
      grep -qi "rebase" skills/implement-interactive/SKILL.md && \
      grep -qi "worktree" skills/implement-interactive/SKILL.md && \
      grep -qE -i "ASK.*before committing|before commit.*ASK" skills/implement-interactive/SKILL.md && \
      grep -qi "four-state\|four state" skills/implement-interactive/SKILL.md && \
      ! grep -qE "git push --force|git push -f|git commit --amend|git rebase " skills/implement-interactive/SKILL.md && \
      echo "implement-interactive SKILL.md acceptance passes"
    </automated>
  </verify>
  <done>skills/implement-interactive/SKILL.md exists with frontmatter + body covering: opening clarifier with all sibling references, anti-sycophancy stance with 4 verbatim markers + execution-time stakes amplifier, 7-input acceptance, four-state status protocol, dirty-worktree check (single-agent), workflow with collaborative walk + ASK-before-commit step, commit cadence per D76 (interactive variant of D75 boundaries), failed-commit → BLOCKED per D77, NO --amend / no rebase / no force-push prohibition, plan-deviation policy per D80 with live push-back amendment, D93 decision-log opt-in default, scope-drift handling, immutability of input artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Register implement-auto and implement-interactive in marketplace, scopes, and README</name>
  <read_first>
    - .claude-plugin/marketplace.json (current state: JeisKappa-workflow has 13 entries; JeisKappa-skills has 8)
    - .vscode/settings.json (current state: 21 alphabetically-sorted scopes)
    - README.md (current state: 21 "Available skills" entries; Retired skills subsection at bottom)
    - AGENTS.md (the 4-touchpoint checklist)
    - .planning/phases/05-implementation-family/05-CONTEXT.md
    - skills/plan-loose-auto/SKILL.md (README description style reference)
    - skills/plan-loose-interactive/SKILL.md (README description style reference for interactive sibling)
  </read_first>
  <files>.claude-plugin/marketplace.json, .vscode/settings.json, README.md</files>
  <action>
    Update all three registration files atomically:

    1. **`.claude-plugin/marketplace.json`** — add `"./skills/implement-auto"` and `"./skills/implement-interactive"` to the JeisKappa-workflow `skills` array in alphabetical position (between `discussion` and `plan-loose-auto`). After this change the JeisKappa-workflow array has exactly 15 entries. The JeisKappa-skills array stays unchanged at 8 entries.

    2. **`.vscode/settings.json`** — add `"implement-auto"` and `"implement-interactive"` to `conventionalCommits.scopes`. Total length now 23. Keep alphabetical order — the new entries fall between `discussion` and `meta-prompting`. Use the jq accessor form `."conventionalCommits.scopes"` (quoted with leading dot) for verification — the dotted key requires the quoted form.

    3. **`README.md`** — add two new "Available skills" entries (one per new skill), each following the established shape: `### [\`<name>\`](./skills/<name>/SKILL.md)` heading + paragraph description + fenced `npx skills add ...` block. Insert BEFORE the `## Retired skills` subsection. Position both entries together (auto then interactive) — placement within the catalog is presentation-order, not strict alphabetical (mirror the Phase 4 plan-loose / plan-strict / adjust-plan-granularity grouping pattern). Description sentences MUST state explicit interaction mode (autonomous vs collaborative) per DIST-05.

    Do NOT touch the `## Retired skills` subsection.

    Do NOT commit.
  </action>
  <acceptance_criteria>
    Run these checks and ALL must pass:

    1. `jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length == 15' .claude-plugin/marketplace.json` returns true.
    2. `jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/implement-auto")' .claude-plugin/marketplace.json` returns non-null.
    3. `jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/implement-interactive")' .claude-plugin/marketplace.json` returns non-null.
    4. `jq -e '.plugins[] | select(.name=="JeisKappa-skills") | .skills | length == 8' .claude-plugin/marketplace.json` returns true.
    5. `jq -e '."conventionalCommits.scopes" | length == 23' .vscode/settings.json` returns true.
    6. `jq -e '."conventionalCommits.scopes" | index("implement-auto")' .vscode/settings.json` returns non-null.
    7. `jq -e '."conventionalCommits.scopes" | index("implement-interactive")' .vscode/settings.json` returns non-null.
    8. `jq -e '."conventionalCommits.scopes" == (."conventionalCommits.scopes" | sort)' .vscode/settings.json` returns true (alphabetical).
    9. `grep -c "^### \[\`implement-auto\`\]" README.md` — exactly 1.
    10. `grep -c "^### \[\`implement-interactive\`\]" README.md` — exactly 1.
    11. `grep -c "npx skills add Jei-sKappa/skills --skill implement-auto" README.md` — exactly 1.
    12. `grep -c "npx skills add Jei-sKappa/skills --skill implement-interactive" README.md` — exactly 1.
    13. Both new README headings fall BEFORE `## Retired skills`. Verify:
        ```bash
        bash -c 'A=$(grep -n "^### \[\`implement-auto\`\]" README.md | cut -d: -f1); B=$(grep -n "^### \[\`implement-interactive\`\]" README.md | cut -d: -f1); R=$(grep -n "^## Retired skills" README.md | cut -d: -f1); [[ $A -lt $R && $B -lt $R ]] && echo OK'
        ```
        prints OK.
    14. No new git commit created.
  </acceptance_criteria>
  <verify>
    <automated>
      jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length == 15' .claude-plugin/marketplace.json >/dev/null && \
      jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/implement-auto")' .claude-plugin/marketplace.json >/dev/null && \
      jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/implement-interactive")' .claude-plugin/marketplace.json >/dev/null && \
      jq -e '."conventionalCommits.scopes" | length == 23' .vscode/settings.json >/dev/null && \
      jq -e '."conventionalCommits.scopes" | index("implement-auto")' .vscode/settings.json >/dev/null && \
      jq -e '."conventionalCommits.scopes" | index("implement-interactive")' .vscode/settings.json >/dev/null && \
      jq -e '."conventionalCommits.scopes" == (."conventionalCommits.scopes" | sort)' .vscode/settings.json >/dev/null && \
      [ "$(grep -c '^### \[`implement-auto`\]' README.md)" = "1" ] && \
      [ "$(grep -c '^### \[`implement-interactive`\]' README.md)" = "1" ] && \
      [ "$(grep -c 'npx skills add Jei-sKappa/skills --skill implement-auto' README.md)" = "1" ] && \
      [ "$(grep -c 'npx skills add Jei-sKappa/skills --skill implement-interactive' README.md)" = "1" ] && \
      echo "implement-* pair registration acceptance passes"
    </automated>
  </verify>
  <done>marketplace.json JeisKappa-workflow array has 15 entries, .vscode/settings.json scopes has 23 entries (alphabetically sorted), README.md has 2 new install blocks for the implement-* pair. No git commit created.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

1. **Skill files exist with correct frontmatter:**
   ```bash
   ls -la skills/implement-auto/SKILL.md skills/implement-interactive/SKILL.md
   ```

2. **D74 four-state status protocol + D77 failed-commit + commit-history sacredness:**
   ```bash
   for SKILL in skills/implement-auto/SKILL.md skills/implement-interactive/SKILL.md; do
     grep -q "DONE_WITH_CONCERNS" "$SKILL" || echo "FAIL D74 (DONE_WITH_CONCERNS) in $SKILL"
     grep -q "BLOCKED" "$SKILL" || echo "FAIL D74 (BLOCKED) in $SKILL"
     grep -q "NEEDS_CONTEXT" "$SKILL" || echo "FAIL D74 (NEEDS_CONTEXT) in $SKILL"
     grep -qi "amend" "$SKILL" || echo "FAIL --amend prohibition in $SKILL"
     grep -qi "force-push\|force push" "$SKILL" || echo "FAIL force-push prohibition in $SKILL"
     grep -qi "rebase" "$SKILL" || echo "FAIL rebase prohibition in $SKILL"
     grep -qi "worktree" "$SKILL" || echo "FAIL D79 dirty-worktree check in $SKILL"
   done
   ```

3. **Anti-sycophancy markers in interactive skill:**
   ```bash
   grep -q "Disagree when you disagree" skills/implement-interactive/SKILL.md
   grep -qE "Push back on (weak|incomplete)" skills/implement-interactive/SKILL.md
   grep -q "Do not treat pushback as correctness" skills/implement-interactive/SKILL.md
   grep -qE "Refuse to log" skills/implement-interactive/SKILL.md
   grep -qE "expensive to rewind|expensive to undo" skills/implement-interactive/SKILL.md
   ```

4. **Four-touchpoint registration:**
   ```bash
   jq '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length' .claude-plugin/marketplace.json    # expect 15
   jq '."conventionalCommits.scopes" | length' .vscode/settings.json                                              # expect 23
   grep -cE "^### \[\`implement-(auto|interactive)\`\]" README.md                                                  # expect 2
   ```
</verification>

<success_criteria>
- skills/implement-auto/SKILL.md and skills/implement-interactive/SKILL.md exist with correct V1 frontmatter.
- Both skills document the four-state status protocol per D74 (DONE / DONE_WITH_CONCERNS / BLOCKED / NEEDS_CONTEXT).
- Both skills document the commit cadence per D75/D76 (per implicit task or per explicit Git instruction; interactive ASKS before each commit).
- Both skills document the failed-commit → BLOCKED rule per D77 and forbid --amend / rebase / force-push.
- Both skills document the dirty-worktree check per D79 (single-agent variant — this skill owns the check).
- Both skills document the plan-deviation policy per D80 (use judgment; surface deviations in task report).
- Both skills cite docs/workflow/v1/immutability.md and docs/workflow/v1/thread-layout.md by absolute path.
- implement-interactive carries all 4 anti-sycophancy markers verbatim from discussion/SKILL.md, plus the execution-time stakes amplifier.
- implement-interactive enforces D93 (no auto-write decision log default).
- marketplace.json JeisKappa-workflow array has exactly 15 entries.
- .vscode/settings.json conventionalCommits.scopes has exactly 23 entries, alphabetically sorted.
- README.md has 2 new entries inserted before `## Retired skills`.
- No commit created during this plan.
</success_criteria>

<output>
Create `.planning/phases/05-implementation-family/05-01-SUMMARY.md` when done.
</output>
