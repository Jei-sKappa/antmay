---
phase: 05-implementation-family
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - skills/implement-plan-auto/SKILL.md
  - skills/implement-plan-interactive/SKILL.md
  - .claude-plugin/marketplace.json
  - .vscode/settings.json
  - README.md
autonomous: true
requirements:
  - IMPL-03
  - IMPL-04
  - IMPL-10
  - IMPL-11
  - IMPL-12
  - IMPL-13
  - IMPL-14

must_haves:
  truths:
    - "User can invoke `implement-plan-auto` against a V1 plan artifact path (loose or strict; produced by any of the Phase 4 plan-* skills) and have the current session execute each plan task in order with self-review, auto-committing per plan task (D75)."
    - "User can invoke `implement-plan-interactive` against a V1 plan artifact path and have the agent ASK before committing at each plan-task boundary (D76)."
    - "User can read either skill body and verify the four-state status protocol is documented and applied per plan task: DONE / DONE_WITH_CONCERNS / BLOCKED / NEEDS_CONTEXT (D74). Each task produces a task report; the four-state status appears in chat output and/or commit messages, not a separate artifact file."
    - "User reading either skill body can verify the failed-commit handling: failed commit reports `BLOCKED`, stops the flow, does NOT retry without explicit user instruction (D77)."
    - "User reading either skill body can verify the no-history-rewrite rule: NO `--amend`, NO rebase, NO force-push, NO history rewriting."
    - "User reading either skill body can verify the dirty-worktree handling (D79, single-agent variant — this skill owns the check)."
    - "User reading either skill body can verify single-agent topology: no subagents are spawned. Plan execution uses the current session + self-review (per IMPL-03/IMPL-04 + D67)."
    - "User running `npx skills add` finds both new skills installable via the `JeisKappa-workflow` marketplace plugin after registration."
  artifacts:
    - path: "skills/implement-plan-auto/SKILL.md"
      provides: "Autonomous plan-driven implementation skill (single-agent)"
      contains: "frontmatter with name=implement-plan-auto, version=1.0.0; ## Workflow section; four-state status protocol tokens DONE / DONE_WITH_CONCERNS / BLOCKED / NEEDS_CONTEXT; failed-commit → BLOCKED phrase; NO --amend / no rebase / no force-push; dirty-worktree check (single-agent); 'per plan task' commit cadence; explicit single-agent topology"
    - path: "skills/implement-plan-interactive/SKILL.md"
      provides: "Collaborative plan-driven implementation skill (single-agent)"
      contains: "frontmatter with name=implement-plan-interactive, version=1.0.0; ## Anti-Sycophancy Stance with 4 verbatim markers from discussion/SKILL.md; ## Workflow section; four-state status protocol; failed-commit → BLOCKED; no-history-rewrite prohibition; dirty-worktree check; explicit 'ASK before committing' clause at each plan-task boundary"
    - path: ".claude-plugin/marketplace.json"
      provides: "Marketplace registration for both new skills"
      contains: "./skills/implement-plan-auto and ./skills/implement-plan-interactive entries under JeisKappa-workflow (total array length now 17)"
    - path: ".vscode/settings.json"
      provides: "Conventional-commit scopes for both new skills"
      contains: "implement-plan-auto and implement-plan-interactive in conventionalCommits.scopes, alphabetically sorted (total length now 25)"
    - path: "README.md"
      provides: "Public install snippets for both new skills"
      contains: "### `implement-plan-auto` and ### `implement-plan-interactive` headings with `npx skills add` install commands"
  key_links:
    - from: "skills/implement-plan-auto/SKILL.md"
      to: "docs/workflow/v1/immutability.md"
      via: "citation by absolute path (plan artifacts are immutable; implementer reads READ-ONLY)"
      pattern: "docs/workflow/v1/immutability.md"
    - from: "skills/implement-plan-auto/SKILL.md"
      to: "docs/workflow/v1/thread-layout.md"
      via: "citation by absolute path"
      pattern: "docs/workflow/v1/thread-layout.md"
    - from: "skills/implement-plan-auto/SKILL.md"
      to: "skills/plan-loose-auto/SKILL.md or skills/plan-strict-auto/SKILL.md"
      via: "documents expected input shape (a V1 plan artifact produced by the Phase 4 plan-* family)"
      pattern: "plan-loose|plan-strict"
    - from: "skills/implement-plan-interactive/SKILL.md"
      to: "skills/discussion/SKILL.md"
      via: "anti-sycophancy stance carried verbatim"
      pattern: "Disagree when you disagree|Do not treat pushback as correctness"
    - from: ".claude-plugin/marketplace.json"
      to: "skills/implement-plan-auto/ and skills/implement-plan-interactive/"
      via: "JeisKappa-workflow.skills array entries"
      pattern: "./skills/implement-plan-auto|./skills/implement-plan-interactive"
---

<objective>
Ship the plan-driven single-agent implementation pair: `implement-plan-auto` (autonomous end-to-end execution of a V1 plan artifact in plan order; auto-commits per plan task) and `implement-plan-interactive` (same execution but ASKS before committing at each plan-task boundary). Both skills run as single-agent (current session + self-review; no subagents). Both honor the V1 four-state status protocol (D74), the failed-commit → BLOCKED rule (D77), the no-history-rewrite prohibition, and the dirty-worktree check (D79). Per IMPL-03/IMPL-04 + D67, plan execution uses the current agent + self-review with no subagents spawned. Register both skills in the three V1 touchpoints (`marketplace.json`, `.vscode/settings.json`, `README.md`).

Purpose: The plan-driven single-agent pair is the second of three pairs in Phase 5. Differs from the less-structured-input pair (Plan 05-01) in TWO ways: (1) input MUST be a V1 plan artifact path (not raw input), and (2) commit cadence is per-plan-task (D75) rather than per-implicit-task. Differs from the subagent pair (Plan 05-03) in topology: this pair runs in the current session; no implementer / reviewer subagents are spawned.

Output: Two new skill folders + three registration files updated. All four CLAUDE.md touchpoints satisfied for each skill.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-implementation-family/05-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@AGENTS.md

@docs/workflow/v1/README.md
@docs/workflow/v1/thread-layout.md
@docs/workflow/v1/filename-grammar.md
@docs/workflow/v1/immutability.md

<!-- Phase 4 plan family — produces the input artifacts this pair consumes -->
@skills/plan-loose-auto/SKILL.md
@skills/plan-loose-interactive/SKILL.md
@skills/plan-strict-auto/SKILL.md
@skills/plan-strict-interactive/SKILL.md
@skills/discussion/SKILL.md

<!-- Plan 05-01 siblings (less-structured-input pair) for cross-pair consistency. After 05-01 ships these exist. -->
@skills/implement-auto/SKILL.md
@skills/implement-interactive/SKILL.md

@.claude-plugin/marketplace.json
@.vscode/settings.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author skills/implement-plan-auto/SKILL.md</name>
  <read_first>
    - skills/implement-plan-auto/SKILL.md (the file being created — empty / non-existent)
    - .planning/phases/05-implementation-family/05-CONTEXT.md (LOCKED decisions)
    - skills/plan-loose-auto/SKILL.md (Phase 4 closest body-shape analog; defines what a loose plan looks like — the input shape this skill consumes)
    - skills/plan-strict-auto/SKILL.md (Phase 4 — defines what a strict plan looks like; the per-task fields the strict input carries)
    - skills/implement-auto/SKILL.md (Plan 05-01 Task 1 output — sibling consistency for four-state protocol + commit policy + dirty-worktree wording)
    - docs/workflow/v1/thread-layout.md (plans/ folder routing — implementation skills read plan artifacts from here)
    - docs/workflow/v1/immutability.md (plan artifacts are immutable; implementer reads READ-ONLY)
    - docs/workflow/v1/filename-grammar.md (versioned-form plan filenames the input may be specified by)
  </read_first>
  <files>skills/implement-plan-auto/SKILL.md</files>
  <action>
    Create the file with YAML frontmatter and a markdown body. Frontmatter required fields:
    - `name: implement-plan-auto`
    - `description:` a single sentence including the "Use when..." trigger AND explicit interaction mode (autonomous; executes plan tasks in order; single-agent — no subagents). Mention the sibling `implement-plan-interactive`, the subagent siblings (`implement-plan-with-subagents-auto`, `implement-plan-with-subagents-interactive`), and the less-structured-input siblings (`implement-auto`, `implement-interactive`).
    - `metadata.author: https://github.com/Jei-sKappa`
    - `metadata.version: 1.0.0`

    Body sections (executor discretion on names; behavioral tokens below MUST appear):

    1. **Opening clarifier paragraph** naming all five siblings (`implement-plan-interactive`, `implement-plan-with-subagents-auto`, `implement-plan-with-subagents-interactive`, `implement-auto`, `implement-interactive`). State the two axes per D64/D65/D66:
       - INPUT-SHAPE axis: plan-driven (this skill) vs. less-structured input.
       - TOPOLOGY axis: single-agent (this skill) vs. multi-subagent.
       Cross the axes per D66: only plan-driven input gets a `*-with-subagents-*` variant. Recommend the subagent siblings when the user wants the heavier review loop; recommend the less-structured-input siblings when there is no plan artifact.
       State the V1 stance: THIS skill is single-agent — the CURRENT session executes plan tasks + self-reviews; no subagents are spawned.

    2. **Inputs** section. MUST state the input is a V1 plan artifact path under `docs/threads/<thread>/plans/<UTC>-v<N>[-<descriptor>]-plan.md` (loose OR strict granularity — both are valid per Phase 4 plan family; both honor the D59 sequential-isolated-independent contract). Optionally a SPECIFIC plan task identifier (e.g., "task 3") can be passed alongside the plan path to execute only that task. Document the ambiguity fallback: if multiple plausible plan artifacts exist, ASK the user per `docs/workflow/v1/immutability.md` ("Ambiguous Reference Resolution"). Do not pick by recency.

    3. **Four-State Status Protocol** section (D74, IMPL-10). MUST document the four states by NAME (`DONE`, `DONE_WITH_CONCERNS`, `BLOCKED`, `NEEDS_CONTEXT`) with a one-sentence definition for each. MUST describe the per-task report block shape (state + 1-3 sentence notes + suggested next action). The state appears in chat output and/or commit messages; NOT a separate artifact file. Match the wording from `skills/implement-auto/SKILL.md` for sibling consistency (executor MAY paraphrase but the four state tokens are non-negotiable).

    4. **Dirty Worktree Handling** section (D79, IMPL-14). MUST document the check: at start, inspect the worktree; if dirty, ASK the user "Continue / abort?". Single-agent variant — THIS skill owns the check (there is no orchestrator role here; the current session IS the implementer).

    5. **Single-Agent Topology** subsection or paragraph. MUST state explicitly: this skill is SINGLE-AGENT. The current session reads the plan, executes each task in order, and self-reviews after each task. NO subagents are spawned. If the user wants subagent-driven execution (implementer + spec-compliance reviewer + code-quality reviewer; D70 / D71 / D72), they should pick `implement-plan-with-subagents-auto` or `implement-plan-with-subagents-interactive` instead.

    6. **Workflow** section with numbered steps. MUST include:
       - Resolve the active thread.
       - Resolve the plan artifact path. If multiple plausible plans exist, ASK the user.
       - Check the worktree (D79). If dirty, ASK the user "Continue / abort?".
       - Read the plan READ-ONLY per `docs/workflow/v1/immutability.md`. Parse the numbered task list.
       - For each plan task IN ORDER (sequential execution per the D59 contract — there are no waves; the implicit dependency is "previous task ran first"):
         a. Implement the task (apply substeps if strict-granularity; infer obvious substeps if loose-granularity).
         b. Self-review the implementation against the task's verification + acceptance criteria.
         c. Commit per the `## Commit Policy` (per plan task — D75).
         d. Write a task report with the four-state status.
       - On failed commit: report `BLOCKED`, stop. Do not retry without explicit user instruction.
       - Final message: list each plan task by its four-state status; cite the commits made (SHA + subject); reference the plan path.

    7. **Commit Policy** section. MUST contain:
       - The cadence rule for `implement-plan-auto` (D75): per plan task (one commit per successful task). The literal phrase "per plan task" (or near-verbatim) MUST appear.
       - Failed-commit → BLOCKED (D77). The literal phrase MUST contain "BLOCKED" + "stop". State "Do not retry the commit without explicit user instruction" or near-verbatim.
       - No history rewriting. MUST contain NO `--amend`, NO rebase, NO force-push (all three forbidden git operations named).

    8. **Plan Deviation Policy** section (D80). MUST state: follow the plan; use judgment if the plan is unclear, contradicts the observed code state, or omits an obvious step that blocks progress; surface deviations in the task report (`DONE_WITH_CONCERNS` if minor, `NEEDS_CONTEXT` if major). No mandatory pre-clearance, no blanket permission.

    9. **Immutability** section: plan artifacts are IMMUTABLE per `docs/workflow/v1/immutability.md`. The implementer reads them READ-ONLY; modifications go to SOURCE CODE, not to the plan. If the plan needs revision, the user invokes `adjust-plan-granularity-auto` or `adjust-plan-granularity-interactive` (Phase 4) to emit a new versioned plan.

    Voice and discipline:
    - Dense, declarative, citation-first. Match Phase 4 plan skills' density.
    - Cite Phase 1 canonical docs by ABSOLUTE PATH on first invocation.
    - Cite at least one Phase 4 plan-family skill (`plan-loose-auto` or `plan-strict-auto`) by absolute path to anchor "expected input shape".
    - Do NOT include a `## Anti-Sycophancy Stance` section (this is the AUTO half).
    - Do NOT include subagent loop language (D69 — that is the with-subagents skills' territory).
  </action>
  <acceptance_criteria>
    Run these checks and ALL must pass:

    1. `test -f skills/implement-plan-auto/SKILL.md`.
    2. `grep -c "^name: implement-plan-auto$" skills/implement-plan-auto/SKILL.md` — exactly 1.
    3. `grep -c "^  version: 1.0.0$" skills/implement-plan-auto/SKILL.md` — at least 1.
    4. `grep -c "^  author: https://github.com/Jei-sKappa$" skills/implement-plan-auto/SKILL.md` — at least 1.
    5. `grep -c "Use when" skills/implement-plan-auto/SKILL.md` — at least 1.
    6. `grep -c "implement-plan-interactive" skills/implement-plan-auto/SKILL.md` — at least 1 (sibling).
    7. `grep -c "implement-plan-with-subagents" skills/implement-plan-auto/SKILL.md` — at least 1 (subagent sibling).
    8. `grep -cE "implement-(auto|interactive)" skills/implement-plan-auto/SKILL.md` — at least 1 (less-structured siblings).
    9. `grep -cE "plan-loose|plan-strict" skills/implement-plan-auto/SKILL.md` — at least 1 (Phase 4 input-shape citation).
    10. `grep -c "DONE_WITH_CONCERNS" skills/implement-plan-auto/SKILL.md` — at least 1.
    11. `grep -c "BLOCKED" skills/implement-plan-auto/SKILL.md` — at least 1.
    12. `grep -c "NEEDS_CONTEXT" skills/implement-plan-auto/SKILL.md` — at least 1.
    13. `grep -c -i "amend" skills/implement-plan-auto/SKILL.md` — at least 1.
    14. `grep -c -i "force-push\|force push" skills/implement-plan-auto/SKILL.md` — at least 1.
    15. `grep -c -i "rebase" skills/implement-plan-auto/SKILL.md` — at least 1.
    16. `grep -c -i "worktree" skills/implement-plan-auto/SKILL.md` — at least 1.
    17. `grep -c "docs/workflow/v1/immutability.md" skills/implement-plan-auto/SKILL.md` — at least 1.
    18. `grep -c "docs/workflow/v1/thread-layout.md" skills/implement-plan-auto/SKILL.md` — at least 1.
    19. `grep -cE "per plan task|per-plan-task" skills/implement-plan-auto/SKILL.md` — at least 1 (D75 commit cadence phrase).
    20. `grep -cE -i "failed commit|commit fail|commit fails" skills/implement-plan-auto/SKILL.md` — at least 1.
    21. `grep -c -i "self-review" skills/implement-plan-auto/SKILL.md` — at least 1.
    22. `grep -c -i "single-agent\|single agent" skills/implement-plan-auto/SKILL.md` — at least 1 (topology declared).
    23. `grep -c -i "four-state\|four state" skills/implement-plan-auto/SKILL.md` — at least 1.
    24. `grep -c "docs/threads/<thread>/plans/" skills/implement-plan-auto/SKILL.md` — at least 1 (plans/ folder routing).
    25. The body MUST NOT contain force-push instructions. `grep -E "git push --force|git push -f|git commit --amend|git rebase " skills/implement-plan-auto/SKILL.md` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>
      test -f skills/implement-plan-auto/SKILL.md && \
      grep -q "^name: implement-plan-auto$" skills/implement-plan-auto/SKILL.md && \
      grep -q "DONE_WITH_CONCERNS" skills/implement-plan-auto/SKILL.md && \
      grep -q "BLOCKED" skills/implement-plan-auto/SKILL.md && \
      grep -q "NEEDS_CONTEXT" skills/implement-plan-auto/SKILL.md && \
      grep -qi "amend" skills/implement-plan-auto/SKILL.md && \
      grep -qi "force-push\|force push" skills/implement-plan-auto/SKILL.md && \
      grep -qi "rebase" skills/implement-plan-auto/SKILL.md && \
      grep -qi "worktree" skills/implement-plan-auto/SKILL.md && \
      grep -q "docs/workflow/v1/immutability.md" skills/implement-plan-auto/SKILL.md && \
      grep -qE "per plan task|per-plan-task" skills/implement-plan-auto/SKILL.md && \
      grep -qi "single-agent\|single agent" skills/implement-plan-auto/SKILL.md && \
      grep -qi "four-state\|four state" skills/implement-plan-auto/SKILL.md && \
      grep -q "docs/threads/<thread>/plans/" skills/implement-plan-auto/SKILL.md && \
      ! grep -qE "git push --force|git push -f|git commit --amend|git rebase " skills/implement-plan-auto/SKILL.md && \
      echo "implement-plan-auto SKILL.md acceptance passes"
    </automated>
  </verify>
  <done>skills/implement-plan-auto/SKILL.md exists with frontmatter + body covering: opening clarifier with all five sibling references (axis-aware) per D64/D65/D66, plan-artifact-path input with optional task identifier + ambiguity fallback, four-state status protocol per D74, dirty-worktree check per D79 (single-agent variant), single-agent topology explicit, workflow with per-task execute + self-review + commit + task-report flow, commit cadence per D75 (per plan task), failed-commit → BLOCKED per D77, NO --amend / no rebase / no force-push, plan-deviation policy per D80, immutability of plan artifacts citing `adjust-plan-granularity-*` as the revision path. NO subagent loop language. NO anti-sycophancy section.</done>
</task>

<task type="auto">
  <name>Task 2: Author skills/implement-plan-interactive/SKILL.md</name>
  <read_first>
    - skills/implement-plan-interactive/SKILL.md (the file being created — empty / non-existent)
    - skills/implement-plan-auto/SKILL.md (Task 1 output — sibling consistency)
    - .planning/phases/05-implementation-family/05-CONTEXT.md
    - skills/plan-loose-interactive/SKILL.md (Phase 4 interactive variant pattern — 4 anti-sycophancy markers verbatim, framing line, scope-drift handling)
    - skills/plan-strict-interactive/SKILL.md (Phase 4 — interactive pattern with stakes amplifier)
    - skills/discussion/SKILL.md (anti-sycophancy stance source — 4 marker phrases for verbatim carry-over)
    - skills/implement-interactive/SKILL.md (Plan 05-01 Task 2 output — sibling interactive variant; execution-time stakes amplifier wording reference)
    - docs/workflow/v1/thread-layout.md
    - docs/workflow/v1/immutability.md
    - docs/workflow/v1/filename-grammar.md
  </read_first>
  <files>skills/implement-plan-interactive/SKILL.md</files>
  <action>
    Create the file with YAML frontmatter and a markdown body. Frontmatter required fields:
    - `name: implement-plan-interactive`
    - `description:` a single sentence with the "Use when..." trigger AND explicit interaction mode (collaborative; ASKS before committing at each plan-task boundary; single-agent — no subagents). Mention the sibling `implement-plan-auto`, the subagent siblings, and the less-structured-input siblings.
    - `metadata.author: https://github.com/Jei-sKappa`
    - `metadata.version: 1.0.0`

    Body sections (executor discretion on section names; behavioral tokens below MUST appear):

    1. **Opening clarifier paragraph** naming all five siblings + the two axes (input-shape × topology) per D64/D65/D66.

    2. **Anti-Sycophancy Stance** section. Carry the FOUR anti-sycophancy markers from `skills/discussion/SKILL.md` VERBATIM (substring matches required):
       - "Disagree when you disagree" clause
       - "Push back on weak or incomplete reasoning" clause
       - "Do not treat pushback as correctness" clause
       - "Refuse to log a <task / commit> you believe is wrong without flagging it" clause
       Plus the EXECUTION-TIME stakes amplifier (same as `implement-interactive`): "Bad commits become expensive to rewind" or near-verbatim equivalent.

    3. **Inputs** section. Same as `implement-plan-auto`: a V1 plan artifact path; optionally a specific task identifier. Ambiguity fallback documented.

    4. **Four-State Status Protocol** section (D74, IMPL-10). Same four states.

    5. **Dirty Worktree Handling** section (D79, IMPL-14). Same single-agent check (this skill owns it).

    6. **Single-Agent Topology** subsection or paragraph. MUST state explicitly: this skill is SINGLE-AGENT.

    7. **Workflow** section with numbered steps. MUST include:
       - Resolve thread + plan artifact path (ASK if multiple plausible plans).
       - Check worktree; if dirty, ASK the user.
       - Read the plan READ-ONLY.
       - For each plan task IN ORDER:
         a. PRESENT the task to the user (objective + verification + acceptance — paraphrased from the plan). Push back per the `## Anti-Sycophancy Stance` if the task appears contradictory or under-specified for the current code state.
         b. Implement the task (apply substeps if strict; infer if loose).
         c. Self-review against the task's verification + acceptance.
         d. ASK the user "Commit task <N> now? (yes / no / show diff first)".
         e. On confirm, commit per `## Commit Policy`.
         f. Write a task report with the four-state status.
       - On failed commit: report `BLOCKED`, stop.
       - Final message: list tasks by four-state status; cite commits.

    8. **Commit Policy** section. MUST contain:
       - The cadence rule for `implement-plan-interactive` (D76): per plan task, ASKS before each commit. Literal phrase "ASK the user before committing" (or near-verbatim with "ASK" + "before committing") MUST appear.
       - Failed-commit → BLOCKED (D77).
       - No history rewriting (NO `--amend`, no rebase, no force-push).

    9. **Plan Deviation Policy** section (D80). Same as `implement-plan-auto`. Add: during the walk, deviations are surfaced LIVE to the user rather than only in the task report.

    10. **Decision Log** section enforcing D93. Default: no auto-write. Write a log ONLY when durable trade-offs / rejected alternatives emerge that cannot reasonably be captured in commit messages or the task report. When written, use `docs/threads/<thread>/discussions/<UTC>-<kebab-desc>-decision-log.md`.

    11. **Scope Drift** section. PARK as Inbox item via `capture-inbox`, SPLIT into own plan / discussion, or DEFER. Ask the user.

    12. **Immutability** section: same as `implement-plan-auto` — plan artifacts are READ-ONLY; revisions go via `adjust-plan-granularity-*`.

    Voice and discipline:
    - Dense, declarative, citation-first.
    - All 4 anti-sycophancy marker phrases MUST be present verbatim or near-verbatim.
    - The execution-time stakes amplifier MUST appear.
    - Do NOT include subagent loop language.
  </action>
  <acceptance_criteria>
    Run these checks and ALL must pass:

    1. `test -f skills/implement-plan-interactive/SKILL.md`.
    2. `grep -c "^name: implement-plan-interactive$" skills/implement-plan-interactive/SKILL.md` — exactly 1.
    3. `grep -c "^  version: 1.0.0$" skills/implement-plan-interactive/SKILL.md` — at least 1.
    4. `grep -c "^  author: https://github.com/Jei-sKappa$" skills/implement-plan-interactive/SKILL.md` — at least 1.
    5. `grep -c "Use when" skills/implement-plan-interactive/SKILL.md` — at least 1.
    6. `grep -c "implement-plan-auto" skills/implement-plan-interactive/SKILL.md` — at least 1.
    7. `grep -c "implement-plan-with-subagents" skills/implement-plan-interactive/SKILL.md` — at least 1.
    8. `grep -cE "implement-(auto|interactive)" skills/implement-plan-interactive/SKILL.md` — at least 1.
    9. `grep -c -i "Anti-Sycophancy" skills/implement-plan-interactive/SKILL.md` — at least 1.
    10. `grep -c "Disagree when you disagree" skills/implement-plan-interactive/SKILL.md` — at least 1.
    11. `grep -cE "Push back on (weak|incomplete)" skills/implement-plan-interactive/SKILL.md` — at least 1.
    12. `grep -c "Do not treat pushback as correctness" skills/implement-plan-interactive/SKILL.md` — at least 1.
    13. `grep -cE "Refuse to log" skills/implement-plan-interactive/SKILL.md` — at least 1.
    14. `grep -cE "expensive to rewind|expensive to undo" skills/implement-plan-interactive/SKILL.md` — at least 1.
    15. `grep -c "DONE_WITH_CONCERNS" skills/implement-plan-interactive/SKILL.md` — at least 1.
    16. `grep -c "BLOCKED" skills/implement-plan-interactive/SKILL.md` — at least 1.
    17. `grep -c "NEEDS_CONTEXT" skills/implement-plan-interactive/SKILL.md` — at least 1.
    18. `grep -c -i "amend" skills/implement-plan-interactive/SKILL.md` — at least 1.
    19. `grep -c -i "force-push\|force push" skills/implement-plan-interactive/SKILL.md` — at least 1.
    20. `grep -c -i "rebase" skills/implement-plan-interactive/SKILL.md` — at least 1.
    21. `grep -c -i "worktree" skills/implement-plan-interactive/SKILL.md` — at least 1.
    22. `grep -c "docs/workflow/v1/immutability.md" skills/implement-plan-interactive/SKILL.md` — at least 1.
    23. `grep -c "docs/workflow/v1/thread-layout.md" skills/implement-plan-interactive/SKILL.md` — at least 1.
    24. `grep -cE -i "ASK.*before committing|before commit.*ASK" skills/implement-plan-interactive/SKILL.md` — at least 1.
    25. `grep -cE "per plan task|per-plan-task" skills/implement-plan-interactive/SKILL.md` — at least 1 (D76 cadence echoes D75).
    26. `grep -cE -i "failed commit|commit fail|commit fails" skills/implement-plan-interactive/SKILL.md` — at least 1.
    27. `grep -c -i "Decision Log\|decision log" skills/implement-plan-interactive/SKILL.md` — at least 1 (D93 section).
    28. `grep -c -i "single-agent\|single agent" skills/implement-plan-interactive/SKILL.md` — at least 1.
    29. `grep -c -i "four-state\|four state" skills/implement-plan-interactive/SKILL.md` — at least 1.
    30. `grep -c "docs/threads/<thread>/plans/" skills/implement-plan-interactive/SKILL.md` — at least 1.
    31. The body MUST NOT contain force-push instructions. `grep -E "git push --force|git push -f|git commit --amend|git rebase " skills/implement-plan-interactive/SKILL.md` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>
      test -f skills/implement-plan-interactive/SKILL.md && \
      grep -q "^name: implement-plan-interactive$" skills/implement-plan-interactive/SKILL.md && \
      grep -qi "Anti-Sycophancy" skills/implement-plan-interactive/SKILL.md && \
      grep -q "Disagree when you disagree" skills/implement-plan-interactive/SKILL.md && \
      grep -qE "Push back on (weak|incomplete)" skills/implement-plan-interactive/SKILL.md && \
      grep -q "Do not treat pushback as correctness" skills/implement-plan-interactive/SKILL.md && \
      grep -qE "Refuse to log" skills/implement-plan-interactive/SKILL.md && \
      grep -qE "expensive to rewind|expensive to undo" skills/implement-plan-interactive/SKILL.md && \
      grep -q "DONE_WITH_CONCERNS" skills/implement-plan-interactive/SKILL.md && \
      grep -q "BLOCKED" skills/implement-plan-interactive/SKILL.md && \
      grep -q "NEEDS_CONTEXT" skills/implement-plan-interactive/SKILL.md && \
      grep -qi "amend" skills/implement-plan-interactive/SKILL.md && \
      grep -qi "force-push\|force push" skills/implement-plan-interactive/SKILL.md && \
      grep -qi "rebase" skills/implement-plan-interactive/SKILL.md && \
      grep -qi "worktree" skills/implement-plan-interactive/SKILL.md && \
      grep -qE -i "ASK.*before committing|before commit.*ASK" skills/implement-plan-interactive/SKILL.md && \
      grep -qE "per plan task|per-plan-task" skills/implement-plan-interactive/SKILL.md && \
      grep -qi "single-agent\|single agent" skills/implement-plan-interactive/SKILL.md && \
      grep -q "docs/threads/<thread>/plans/" skills/implement-plan-interactive/SKILL.md && \
      ! grep -qE "git push --force|git push -f|git commit --amend|git rebase " skills/implement-plan-interactive/SKILL.md && \
      echo "implement-plan-interactive SKILL.md acceptance passes"
    </automated>
  </verify>
  <done>skills/implement-plan-interactive/SKILL.md exists with frontmatter + body covering: opening clarifier with all five sibling references, anti-sycophancy stance with 4 verbatim markers + execution-time stakes amplifier, plan-artifact-path input with ambiguity fallback, four-state status protocol, dirty-worktree check (single-agent), single-agent topology explicit, workflow with present-task / implement / self-review / ASK-before-commit per plan task, commit cadence per D76 (interactive variant of D75), failed-commit → BLOCKED per D77, no-history-rewrite, plan-deviation policy per D80, D93 decision-log opt-in default, scope-drift handling, immutability with adjust-plan-granularity-* revision path.</done>
</task>

<task type="auto">
  <name>Task 3: Register implement-plan-auto and implement-plan-interactive in marketplace, scopes, and README</name>
  <read_first>
    - .claude-plugin/marketplace.json (state after Plan 05-01: JeisKappa-workflow has 15 entries including implement-auto and implement-interactive)
    - .vscode/settings.json (state after Plan 05-01: 23 alphabetically-sorted scopes)
    - README.md (state after Plan 05-01: 23 "Available skills" entries; Retired skills subsection at bottom)
    - AGENTS.md (the 4-touchpoint checklist)
    - .planning/phases/05-implementation-family/05-CONTEXT.md
    - skills/implement-auto/SKILL.md (Plan 05-01 — README description style reference)
    - skills/implement-interactive/SKILL.md (Plan 05-01 — interactive sibling description style)
  </read_first>
  <files>.claude-plugin/marketplace.json, .vscode/settings.json, README.md</files>
  <action>
    Update all three registration files atomically:

    1. **`.claude-plugin/marketplace.json`** — add `"./skills/implement-plan-auto"` and `"./skills/implement-plan-interactive"` to the JeisKappa-workflow `skills` array in alphabetical position (immediately AFTER `./skills/implement-interactive` and BEFORE `./skills/plan-loose-auto`). After this change the JeisKappa-workflow array has exactly 17 entries. JeisKappa-skills stays unchanged at 8.

    2. **`.vscode/settings.json`** — add `"implement-plan-auto"` and `"implement-plan-interactive"` to `conventionalCommits.scopes`. Total length now 25. Keep alphabetical order — they fall between `implement-interactive` and `meta-prompting`. The jq accessor MUST use the quoted form `."conventionalCommits.scopes"` (the dotted key cannot be addressed unquoted).

    3. **`README.md`** — add two new "Available skills" entries (one per new skill), each with the established shape: `### [\`<name>\`](./skills/<name>/SKILL.md)` heading + paragraph description + fenced `npx skills add ...` block. Insert AFTER `implement-interactive` and BEFORE the `## Retired skills` subsection. Position both entries together (auto then interactive). Description sentences MUST state explicit interaction mode per DIST-05.

    Do NOT touch the `## Retired skills` subsection.

    Do NOT commit.
  </action>
  <acceptance_criteria>
    Run these checks and ALL must pass:

    1. `jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length == 17' .claude-plugin/marketplace.json` returns true.
    2. `jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/implement-plan-auto")' .claude-plugin/marketplace.json` returns non-null.
    3. `jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/implement-plan-interactive")' .claude-plugin/marketplace.json` returns non-null.
    4. `jq -e '.plugins[] | select(.name=="JeisKappa-skills") | .skills | length == 8' .claude-plugin/marketplace.json` returns true.
    5. `jq -e '."conventionalCommits.scopes" | length == 25' .vscode/settings.json` returns true.
    6. `jq -e '."conventionalCommits.scopes" | index("implement-plan-auto")' .vscode/settings.json` returns non-null.
    7. `jq -e '."conventionalCommits.scopes" | index("implement-plan-interactive")' .vscode/settings.json` returns non-null.
    8. `jq -e '."conventionalCommits.scopes" == (."conventionalCommits.scopes" | sort)' .vscode/settings.json` returns true.
    9. `grep -c "^### \[\`implement-plan-auto\`\]" README.md` — exactly 1.
    10. `grep -c "^### \[\`implement-plan-interactive\`\]" README.md` — exactly 1.
    11. `grep -c "npx skills add Jei-sKappa/skills --skill implement-plan-auto" README.md` — exactly 1.
    12. `grep -c "npx skills add Jei-sKappa/skills --skill implement-plan-interactive" README.md` — exactly 1.
    13. Both new README headings fall BEFORE `## Retired skills`. Verify:
        ```bash
        bash -c 'A=$(grep -n "^### \[\`implement-plan-auto\`\]" README.md | cut -d: -f1); B=$(grep -n "^### \[\`implement-plan-interactive\`\]" README.md | cut -d: -f1); R=$(grep -n "^## Retired skills" README.md | cut -d: -f1); [[ $A -lt $R && $B -lt $R ]] && echo OK'
        ```
        prints OK.
    14. No new git commit created.
  </acceptance_criteria>
  <verify>
    <automated>
      jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length == 17' .claude-plugin/marketplace.json >/dev/null && \
      jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/implement-plan-auto")' .claude-plugin/marketplace.json >/dev/null && \
      jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/implement-plan-interactive")' .claude-plugin/marketplace.json >/dev/null && \
      jq -e '."conventionalCommits.scopes" | length == 25' .vscode/settings.json >/dev/null && \
      jq -e '."conventionalCommits.scopes" | index("implement-plan-auto")' .vscode/settings.json >/dev/null && \
      jq -e '."conventionalCommits.scopes" | index("implement-plan-interactive")' .vscode/settings.json >/dev/null && \
      jq -e '."conventionalCommits.scopes" == (."conventionalCommits.scopes" | sort)' .vscode/settings.json >/dev/null && \
      [ "$(grep -c '^### \[`implement-plan-auto`\]' README.md)" = "1" ] && \
      [ "$(grep -c '^### \[`implement-plan-interactive`\]' README.md)" = "1" ] && \
      [ "$(grep -c 'npx skills add Jei-sKappa/skills --skill implement-plan-auto' README.md)" = "1" ] && \
      [ "$(grep -c 'npx skills add Jei-sKappa/skills --skill implement-plan-interactive' README.md)" = "1" ] && \
      echo "implement-plan-* pair registration acceptance passes"
    </automated>
  </verify>
  <done>marketplace.json JeisKappa-workflow array has 17 entries, .vscode/settings.json scopes has 25 entries (alphabetically sorted), README.md has 2 new install blocks for the implement-plan-* pair. No git commit created.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

1. **Skill files exist:**
   ```bash
   ls -la skills/implement-plan-auto/SKILL.md skills/implement-plan-interactive/SKILL.md
   ```

2. **D74 four-state status + D77 failed-commit + commit-history sacredness + D79 dirty-worktree + single-agent topology:**
   ```bash
   for SKILL in skills/implement-plan-auto/SKILL.md skills/implement-plan-interactive/SKILL.md; do
     grep -q "DONE_WITH_CONCERNS" "$SKILL" || echo "FAIL D74 in $SKILL"
     grep -q "BLOCKED" "$SKILL" || echo "FAIL D74 (BLOCKED) in $SKILL"
     grep -q "NEEDS_CONTEXT" "$SKILL" || echo "FAIL D74 (NEEDS_CONTEXT) in $SKILL"
     grep -qi "amend" "$SKILL" || echo "FAIL --amend prohibition in $SKILL"
     grep -qi "force-push\|force push" "$SKILL" || echo "FAIL force-push prohibition in $SKILL"
     grep -qi "rebase" "$SKILL" || echo "FAIL rebase prohibition in $SKILL"
     grep -qi "worktree" "$SKILL" || echo "FAIL D79 in $SKILL"
     grep -qi "single-agent\|single agent" "$SKILL" || echo "FAIL single-agent topology in $SKILL"
     grep -qE "per plan task|per-plan-task" "$SKILL" || echo "FAIL D75/D76 cadence in $SKILL"
   done
   ```

3. **Anti-sycophancy markers in interactive skill:**
   ```bash
   grep -q "Disagree when you disagree" skills/implement-plan-interactive/SKILL.md
   grep -qE "Push back on (weak|incomplete)" skills/implement-plan-interactive/SKILL.md
   grep -q "Do not treat pushback as correctness" skills/implement-plan-interactive/SKILL.md
   grep -qE "Refuse to log" skills/implement-plan-interactive/SKILL.md
   grep -qE "expensive to rewind|expensive to undo" skills/implement-plan-interactive/SKILL.md
   ```

4. **Four-touchpoint registration:**
   ```bash
   jq '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length' .claude-plugin/marketplace.json    # expect 17
   jq '."conventionalCommits.scopes" | length' .vscode/settings.json                                              # expect 25
   grep -cE "^### \[\`implement-plan-(auto|interactive)\`\]" README.md                                            # expect 2
   ```
</verification>

<success_criteria>
- skills/implement-plan-auto/SKILL.md and skills/implement-plan-interactive/SKILL.md exist with correct V1 frontmatter.
- Both skills document the four-state status protocol per D74.
- Both skills document the commit cadence per D75/D76 (per plan task; interactive ASKS before each commit).
- Both skills document the failed-commit → BLOCKED rule per D77 and forbid --amend / rebase / force-push.
- Both skills document the dirty-worktree check per D79 (single-agent variant — this skill owns the check).
- Both skills declare single-agent topology explicitly.
- Both skills document the plan-deviation policy per D80.
- Both skills cite docs/workflow/v1/immutability.md and docs/workflow/v1/thread-layout.md by absolute path.
- Both skills cite at least one Phase 4 plan-* skill (input shape source).
- implement-plan-interactive carries all 4 anti-sycophancy markers verbatim + execution-time stakes amplifier.
- implement-plan-interactive enforces D93.
- marketplace.json JeisKappa-workflow array has exactly 17 entries.
- .vscode/settings.json conventionalCommits.scopes has exactly 25 entries, alphabetically sorted.
- README.md has 2 new entries inserted before `## Retired skills`.
- No commit created during this plan.
</success_criteria>

<output>
Create `.planning/phases/05-implementation-family/05-02-SUMMARY.md` when done.
</output>
