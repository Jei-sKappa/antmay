---
phase: 05-implementation-family
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - skills/implement-plan-with-subagents-auto/SKILL.md
  - skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md
  - skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md
  - skills/implement-plan-with-subagents-interactive/SKILL.md
  - skills/implement-plan-with-subagents-interactive/references/spec-compliance-reviewer.md
  - skills/implement-plan-with-subagents-interactive/references/code-quality-reviewer.md
  - .claude-plugin/marketplace.json
  - .vscode/settings.json
  - README.md
autonomous: true
requirements:
  - IMPL-05
  - IMPL-06
  - IMPL-07
  - IMPL-08
  - IMPL-09
  - IMPL-10
  - IMPL-11
  - IMPL-12
  - IMPL-13
  - IMPL-14

must_haves:
  truths:
    - "User can invoke `implement-plan-with-subagents-auto` against a V1 plan artifact path and have the ORCHESTRATOR run the full subagent loop per task: implementer subagent → spec-compliance reviewer subagent (first pass per D70) → on issues, spawn NEW implementer for fix (D71) → re-review until pass (D72) → code-quality reviewer subagent (second pass per D70) → same fix loop → orchestrator commits per orchestration cycle (D75)."
    - "User can invoke `implement-plan-with-subagents-interactive` and have the orchestrator ASK the user before committing each cycle (D76); the rest of the subagent loop is identical."
    - "User reading either subagent skill body can verify: (a) subagent capability is declared as a precondition with NO inline fallback (D69, IMPL-07) — the body MUST contain the literal phrase 'REQUIRES subagent capability' or near-verbatim with 'REQUIRES' + 'subagent capability' as substrings; (b) the 2-pass review loop is documented (spec-compliance FIRST, code-quality SECOND, per D70); (c) the fix loop respawns a NEW implementer (D71); (d) every fix is re-reviewed before advancing (D72, IMPL-09)."
    - "User reading either subagent skill body can verify the four-state status protocol (D74) — orchestrator's task report lists which subagent ran, how many fix iterations occurred, and the final state per task."
    - "User reading either subagent skill body can verify the dirty-worktree handling per D79 IMPL-14: the ORCHESTRATOR (not the implementer subagent) owns the check; the body explicitly says 'the orchestrator runs the dirty-worktree check BEFORE spawning the first implementer subagent' or near-verbatim."
    - "User reading either subagent skill body can verify the no-worktree-isolation rule per D78: subagents run sequentially on the SAME working tree."
    - "User can read both `references/spec-compliance-reviewer.md` and `references/code-quality-reviewer.md` files inside EACH subagent skill folder (4 reference files total) — both contain reviewer prompt text scoped to their respective concern (spec-compliance reviewer: does the diff implement what the task said it would?; code-quality reviewer: is the diff well-structured, safe, idiomatic given the codebase?). D87 / IMPL-08 — reviewer prompts live as embedded supporting files inside the subagent skill folder, NOT as standalone V1 skills."
    - "User reading either subagent skill body can verify the failed-commit handling: orchestrator reports `BLOCKED` and stops (D77); NO --amend / no rebase / no force-push."
    - "User running `npx skills add` finds both new subagent skills installable via the `JeisKappa-workflow` marketplace plugin after registration."
  artifacts:
    - path: "skills/implement-plan-with-subagents-auto/SKILL.md"
      provides: "Autonomous plan-driven implementation skill with subagent loop (orchestrator + implementer + 2 reviewers)"
      contains: "frontmatter with name=implement-plan-with-subagents-auto, version=1.0.0; ## Subagent briefs or equivalent section; literal 'REQUIRES subagent capability'; spec-compliance and code-quality 2-pass loop documented per D70; fix loop respawns NEW implementer per D71; every fix re-reviewed per D72; per-orchestration-cycle commit cadence per D75; failed-commit → BLOCKED; NO --amend / no rebase / no force-push; orchestrator-owned dirty-worktree check per D79; no-worktree-isolation per D78; four-state status protocol per D74"
    - path: "skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md"
      provides: "Embedded reviewer prompt for spec-compliance pass (first review, D70 / D87)"
      contains: "reviewer prompt text scoped to 'does the diff implement what the task said it would?'; covers reading the task / acceptance criteria / observable post-conditions; produces a structured review output the orchestrator can read (pass / issues + actionable findings)"
    - path: "skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md"
      provides: "Embedded reviewer prompt for code-quality pass (second review, D70 / D87)"
      contains: "reviewer prompt text scoped to 'is the diff well-structured, safe, idiomatic given the codebase?'; covers readability / safety / idiomatic patterns / regression risk; produces a structured review output (pass / issues + actionable findings)"
    - path: "skills/implement-plan-with-subagents-interactive/SKILL.md"
      provides: "Collaborative plan-driven implementation skill with subagent loop"
      contains: "frontmatter with name=implement-plan-with-subagents-interactive, version=1.0.0; ## Anti-Sycophancy Stance with 4 verbatim markers from discussion/SKILL.md; literal 'REQUIRES subagent capability'; same 2-pass loop + fix loop discipline; ASK-before-committing at each orchestration cycle per D76; orchestrator-owned dirty-worktree check; no-worktree-isolation; four-state status protocol"
    - path: "skills/implement-plan-with-subagents-interactive/references/spec-compliance-reviewer.md"
      provides: "Embedded reviewer prompt for spec-compliance pass — interactive variant copy (D87 + D28 — DRY refactoring is V2)"
      contains: "reviewer prompt text scoped to spec-compliance; may be identical to the auto sibling's copy (V1 duplicates per D28) or vary slightly at executor discretion to match interactive orchestration behavior"
    - path: "skills/implement-plan-with-subagents-interactive/references/code-quality-reviewer.md"
      provides: "Embedded reviewer prompt for code-quality pass — interactive variant copy"
      contains: "reviewer prompt text scoped to code-quality; may be identical to the auto sibling's copy"
    - path: ".claude-plugin/marketplace.json"
      provides: "Marketplace registration for both new subagent skills"
      contains: "./skills/implement-plan-with-subagents-auto and ./skills/implement-plan-with-subagents-interactive entries under JeisKappa-workflow (total array length now 19)"
    - path: ".vscode/settings.json"
      provides: "Conventional-commit scopes for both new subagent skills"
      contains: "implement-plan-with-subagents-auto and implement-plan-with-subagents-interactive in conventionalCommits.scopes, alphabetically sorted (total length now 27)"
    - path: "README.md"
      provides: "Public install snippets for both new subagent skills"
      contains: "### `implement-plan-with-subagents-auto` and ### `implement-plan-with-subagents-interactive` headings with `npx skills add` install commands"
  key_links:
    - from: "skills/implement-plan-with-subagents-auto/SKILL.md"
      to: "skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md"
      via: "subagent brief references the reviewer prompt by relative path"
      pattern: "references/spec-compliance-reviewer.md"
    - from: "skills/implement-plan-with-subagents-auto/SKILL.md"
      to: "skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md"
      via: "subagent brief references the reviewer prompt by relative path"
      pattern: "references/code-quality-reviewer.md"
    - from: "skills/implement-plan-with-subagents-interactive/SKILL.md"
      to: "skills/implement-plan-with-subagents-interactive/references/spec-compliance-reviewer.md"
      via: "subagent brief references the reviewer prompt by relative path"
      pattern: "references/spec-compliance-reviewer.md"
    - from: "skills/implement-plan-with-subagents-interactive/SKILL.md"
      to: "skills/implement-plan-with-subagents-interactive/references/code-quality-reviewer.md"
      via: "subagent brief references the reviewer prompt by relative path"
      pattern: "references/code-quality-reviewer.md"
    - from: "skills/implement-plan-with-subagents-auto/SKILL.md"
      to: "docs/workflow/v1/immutability.md and docs/workflow/v1/thread-layout.md"
      via: "citation by absolute path"
      pattern: "docs/workflow/v1/immutability.md|docs/workflow/v1/thread-layout.md"
    - from: "skills/implement-plan-with-subagents-interactive/SKILL.md"
      to: "skills/discussion/SKILL.md"
      via: "anti-sycophancy stance carried verbatim"
      pattern: "Disagree when you disagree|Do not treat pushback as correctness"
---

<objective>
Ship the plan-driven subagent-driven implementation pair: `implement-plan-with-subagents-auto` and `implement-plan-with-subagents-interactive`. The orchestrator runs the full subagent loop per plan task per D66/D68/D70/D71/D72: implementer subagent → spec-compliance reviewer subagent (first pass) → on issues spawn NEW implementer subagent for fix → re-review (every fix re-reviewed before advancing) → code-quality reviewer subagent (second pass) → same fix loop → orchestrator commits per orchestration cycle (D75 cadence). Both skills declare subagent capability as a precondition with NO inline fallback (D69, IMPL-07). Both ship with two embedded reviewer prompts under `references/` per D87/IMPL-08: `spec-compliance-reviewer.md` and `code-quality-reviewer.md`. Both honor the V1 four-state status protocol (D74), the failed-commit → BLOCKED rule (D77), the no-history-rewrite prohibition, the dirty-worktree check per D79 (orchestrator-owned), and the no-worktree-isolation rule per D78 (subagents run sequentially on the SAME working tree). Register both skills in the three V1 touchpoints (`marketplace.json`, `.vscode/settings.json`, `README.md`).

Purpose: The subagent-driven pair is the third and last of three pairs in Phase 5. It is the only V1 implementation flow that runs the dual-reviewer pass per task. Per D69 it has no inline fallback — users without subagent capability are pointed at the single-agent siblings (`implement-plan-auto` / `implement-plan-interactive`) from Plan 05-02. Plan 05-03 is the largest plan in Phase 5: 2 skills + 4 reference files + 3 registration touchpoints = 9 modified files total.

Output: Two new skill folders (each with a `references/` subfolder containing 2 reviewer prompt files) + three registration files updated. All four CLAUDE.md touchpoints satisfied for each skill.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-implementation-family/05-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@AGENTS.md

@docs/workflow/v1/README.md
@docs/workflow/v1/thread-layout.md
@docs/workflow/v1/filename-grammar.md
@docs/workflow/v1/immutability.md

<!-- Phase 4 plan family — input shape this pair consumes -->
@skills/plan-loose-auto/SKILL.md
@skills/plan-strict-auto/SKILL.md

<!-- Plan 05-01/05-02 siblings — sibling consistency reference -->
@skills/implement-auto/SKILL.md
@skills/implement-interactive/SKILL.md
@skills/implement-plan-auto/SKILL.md
@skills/implement-plan-interactive/SKILL.md

<!-- Anti-sycophancy stance source -->
@skills/discussion/SKILL.md

<!-- Orchestrator + references/ folder pattern source -->
@skills/afk-exploration/SKILL.md
@skills/afk-exploration/references/pre-mortem-analysis.md

<!-- Second orchestrator example for reference shape variety -->
@skills/the-librarian/SKILL.md

@.claude-plugin/marketplace.json
@.vscode/settings.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author skills/implement-plan-with-subagents-auto/SKILL.md (orchestrator body)</name>
  <read_first>
    - skills/implement-plan-with-subagents-auto/SKILL.md (the file being created — empty / non-existent)
    - .planning/phases/05-implementation-family/05-CONTEXT.md (LOCKED decisions; D66/D68/D69/D70/D71/D72 binding decision summaries)
    - skills/afk-exploration/SKILL.md (orchestrator + subagent + references/ folder pattern source; specifically the `## Subagent briefs` section shape)
    - skills/the-librarian/SKILL.md (second orchestrator example for cross-reference)
    - skills/implement-plan-auto/SKILL.md (Plan 05-02 Task 1 output — sibling consistency for four-state protocol + commit policy + plan-input shape; this skill SUPERSEDES single-agent execution with a subagent loop, but four-state status / commit-cadence-per-cycle / dirty-worktree language stays consistent at the orchestrator level)
    - skills/plan-strict-auto/SKILL.md (Phase 4 — strict-task per-task structure the implementer subagent will execute against)
    - skills/plan-loose-auto/SKILL.md (Phase 4 — loose-task structure)
    - docs/workflow/v1/thread-layout.md
    - docs/workflow/v1/immutability.md
    - docs/workflow/v1/filename-grammar.md
  </read_first>
  <files>skills/implement-plan-with-subagents-auto/SKILL.md</files>
  <action>
    Create the file with YAML frontmatter and a markdown body. Frontmatter required fields:
    - `name: implement-plan-with-subagents-auto`
    - `description:` a single sentence with the "Use when..." trigger AND explicit interaction mode (autonomous; orchestrator runs the full subagent loop per task; commits per orchestration cycle). Mention the sibling `implement-plan-with-subagents-interactive`, the single-agent plan-driven siblings (`implement-plan-auto`, `implement-plan-interactive`), and the less-structured-input siblings. State that this skill REQUIRES subagent capability and has NO inline fallback (per D69 / IMPL-07).
    - `metadata.author: https://github.com/Jei-sKappa`
    - `metadata.version: 1.0.0`

    Body sections (executor discretion on section names; behavioral tokens below MUST appear):

    1. **Opening clarifier paragraph** naming all five siblings (`implement-plan-with-subagents-interactive`, `implement-plan-auto`, `implement-plan-interactive`, `implement-auto`, `implement-interactive`). State the two axes (input-shape × topology) per D64/D65/D66. State that THIS skill is plan-driven + multi-subagent (orchestrator + implementer + 2 reviewers).

    2. **Subagent Capability Precondition** section. MUST contain the LITERAL PHRASE "REQUIRES subagent capability" (or near-verbatim with "REQUIRES" + "subagent capability" as substrings) per D69 / IMPL-07. State explicitly: this skill does NOT fall back to inline execution. If the runtime does not support subagents (e.g., Task tool in Claude Code, equivalent in Codex / Gemini / OpenCode), the user should pick `implement-plan-auto` or `implement-plan-interactive` from Plan 05-02 instead. The skill body MUST forbid inline fallback — there is no "if subagents are unavailable, do it yourself" branch.

    3. **No Worktree Isolation** section or paragraph (D78). MUST state: subagents run sequentially on the SAME working tree. V1 does NOT use `git worktree add` isolation. Each subagent's work is observable to the next via the working tree's mutated state — there is no parallel-worktree topology.

    4. **Inputs** section. Same shape as `implement-plan-auto`: V1 plan artifact path under `docs/threads/<thread>/plans/<UTC>-v<N>[-<descriptor>]-plan.md` (loose or strict). Ambiguity fallback documented (ASK the user per `docs/workflow/v1/immutability.md`).

    5. **Four-State Status Protocol** section (D74, IMPL-10). Same four states. ADDITIONALLY for the subagent flow per the CONTEXT.md `<specifics>` block: the orchestrator's task report MUST list (a) which subagent ran, (b) how many fix iterations occurred, (c) final state per task. This is the audit trail without a separate state file.

    6. **Dirty Worktree Handling** section (D79, IMPL-14). MUST contain the LITERAL PHRASE "the orchestrator runs the dirty-worktree check" (or near-verbatim with "orchestrator" + "dirty" + "worktree" + "check" as substrings) — the orchestrator owns the check, NOT the implementer subagent. The implementer subagent assumes a clean tree per the orchestrator's verification. Body MUST state explicitly the check happens BEFORE spawning the first implementer subagent.

    7. **Workflow** section with numbered steps. MUST describe the full orchestration loop per D66/D68/D70/D71/D72:

       1. Orchestrator resolves the active thread.
       2. Orchestrator resolves the plan artifact path (ASK if multiple plausible plans).
       3. **Orchestrator checks the worktree** (D79). If dirty, ASK "Continue / abort?".
       4. Orchestrator reads the plan READ-ONLY per `docs/workflow/v1/immutability.md`. Parses the numbered task list.
       5. **For each plan task in order** (orchestration cycle):
          a. **Spawn implementer subagent** with a self-contained brief (see `## Subagent Briefs`). Wait for completion. Implementer writes code to the working tree, replies with a 2-3 sentence summary + paths of modified files. (Reply is acknowledgment; the working tree is the completion signal — orchestrator inspects the tree itself.)
          b. **Spawn spec-compliance reviewer subagent** (FIRST review pass per D70) with a self-contained brief that loads `references/spec-compliance-reviewer.md` and points at the plan task's verification + acceptance criteria. Wait for completion. Reviewer writes a structured review (PASS or ISSUES + actionable findings) to a designated path (orchestrator's choice — e.g., a scratch file under `.wip/` per `docs/workflow/v1/immutability.md` since `.wip/` is gitignored) and replies with summary.
          c. **If spec-compliance issues:** spawn a NEW implementer subagent (D71 — no persistent subagent assumption; ALWAYS respawn) with a brief that includes the original task + the review findings. Wait. RE-review (D72, IMPL-09) — spawn a NEW spec-compliance reviewer with a fresh brief. Loop the fix-and-re-review pattern until spec-compliance PASSES. Each iteration costs an implementer + a reviewer subagent.
          d. **Spawn code-quality reviewer subagent** (SECOND review pass per D70) with a brief that loads `references/code-quality-reviewer.md`. Wait. Reviewer writes structured review.
          e. **If code-quality issues:** same fix loop as step c — NEW implementer per fix (D71), NEW code-quality reviewer per fix (D72). Loop until code-quality PASSES.
          f. **Orchestrator commits per the `## Commit Policy`** (per orchestration cycle — D75 cadence). The commit subject SHOULD reference the plan task identifier.
          g. **Write the orchestration cycle task report** (four-state status + which subagents ran + iteration count + final state).
       6. On failed commit: orchestrator reports `BLOCKED`, stops. Do not retry without explicit user instruction.
       7. Final message: list each plan task by its four-state status with iteration counts; cite the commits made.

    8. **Subagent Briefs** section. Mirror the structure of `skills/afk-exploration/SKILL.md` `## Subagent briefs`. Provide ONE brief per subagent ROLE (3 briefs total — implementer, spec-compliance reviewer, code-quality reviewer). Each brief MUST contain:
       - **Scope** — the orchestration-cycle context (current plan task being executed, plan artifact path, optional plan-level context).
       - **Input paths** — for implementer: the plan path + plan task identifier (to be read READ-ONLY). For reviewers: the plan path + task identifier + the working-tree diff (the reviewer must read `git diff` or inspect modified files itself) + the **method reference path** (absolute path of `references/spec-compliance-reviewer.md` for spec-compliance, `references/code-quality-reviewer.md` for code-quality — resolved from this skill's base directory).
       - **Output path** — for implementer: writes source code modifications directly to the working tree (no notes file). For reviewers: writes structured review output to a designated `.wip/` path the orchestrator names.
       - **Return contract** — write the file(s) directly; reply with a 2-3 sentence summary + paths. Do NOT paste content back.
       - **Hard constraints** — implementer: do not commit (orchestrator commits); do not modify the plan artifact (it is immutable); stay within the working tree. Reviewer: do not modify code; only produce findings.

    9. **Commit Policy** section. MUST contain:
       - The cadence rule for `implement-plan-with-subagents-auto` (D75): per ORCHESTRATION CYCLE (one commit per task-after-all-reviews-pass). The literal phrase "per orchestration cycle" (or near-verbatim with "orchestration cycle") MUST appear.
       - Failed-commit → BLOCKED (D77). The literal phrase MUST contain "BLOCKED" + "stop".
       - No history rewriting (NO `--amend`, no rebase, no force-push). All three forbidden git operations named.

    10. **Plan Deviation Policy** section (D80). Same as `implement-plan-auto`. Surface deviations in the orchestrator's task report.

    11. **Immutability** section: plan artifacts are READ-ONLY; revisions go via `adjust-plan-granularity-*`.

    Voice and discipline:
    - Dense, declarative, citation-first.
    - Cite Phase 1 canonical docs by absolute path.
    - The TWO reviewer reference paths MUST appear in the body by RELATIVE PATH from the skill base (i.e., `references/spec-compliance-reviewer.md` and `references/code-quality-reviewer.md` strings MUST appear).
    - Do NOT include a `## Anti-Sycophancy Stance` section (this is the AUTO half).
    - Do NOT embed full reviewer prompt text in the SKILL.md body — the reviewer prompts live in the `references/` files.
    - Do NOT allow an "if subagents unavailable" inline-fallback branch (D69).
  </action>
  <acceptance_criteria>
    Run these checks and ALL must pass:

    1. `test -f skills/implement-plan-with-subagents-auto/SKILL.md`.
    2. `grep -c "^name: implement-plan-with-subagents-auto$" skills/implement-plan-with-subagents-auto/SKILL.md` — exactly 1.
    3. `grep -c "^  version: 1.0.0$" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1.
    4. `grep -c "^  author: https://github.com/Jei-sKappa$" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1.
    5. `grep -c "Use when" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1.
    6. `grep -c "implement-plan-with-subagents-interactive" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1 (sibling).
    7. `grep -cE "implement-plan-(auto|interactive)" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1 (single-agent plan-driven siblings).
    8. `grep -c "REQUIRES subagent capability" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1 (D69 / IMPL-07 precondition).
    9. `grep -cE "spec-compliance" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 2 (at minimum the section reference + the reference file path).
    10. `grep -cE "code-quality" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 2.
    11. `grep -c "references/spec-compliance-reviewer.md" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1 (D87 / IMPL-08 — reference file path appears in subagent brief).
    12. `grep -c "references/code-quality-reviewer.md" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1.
    13. `grep -cE "first pass|FIRST" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1 (D70 — spec-compliance is the FIRST pass).
    14. `grep -cE "second pass|SECOND" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1 (D70 — code-quality is the SECOND pass).
    15. `grep -cE "new implementer|NEW implementer" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1 (D71 — fix loop respawns a NEW implementer).
    16. `grep -cE -i "re-review|reviewed again|re review" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1 (D72 / IMPL-09).
    17. `grep -c "DONE_WITH_CONCERNS" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1.
    18. `grep -c "BLOCKED" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1.
    19. `grep -c "NEEDS_CONTEXT" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1.
    20. `grep -c -i "amend" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1.
    21. `grep -c -i "force-push\|force push" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1.
    22. `grep -c -i "rebase" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1.
    23. `grep -c -i "orchestrator" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 5 (the orchestrator role is named repeatedly).
    24. `grep -cE -i "orchestrator.*dirty.*worktree|orchestrator.*worktree.*check" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1 (D79 — orchestrator owns the check).
    25. `grep -cE "per orchestration cycle|per-orchestration-cycle" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1 (D75 cadence).
    26. `grep -cE -i "failed commit|commit fail|commit fails" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1.
    27. `grep -c "docs/workflow/v1/immutability.md" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1.
    28. `grep -c "docs/workflow/v1/thread-layout.md" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1.
    29. `grep -c "docs/threads/<thread>/plans/" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1.
    30. `grep -cE -i "no inline fallback|no fallback|does not fall back" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1 (D69 — explicit no-fallback language).
    31. `grep -cE -i "same working tree|same tree|sequentially.*tree|tree.*sequentially" skills/implement-plan-with-subagents-auto/SKILL.md` — at least 1 (D78 — no worktree isolation; subagents share the working tree).
    32. The body MUST NOT contain force-push instructions. `grep -E "git push --force|git push -f|git commit --amend|git rebase " skills/implement-plan-with-subagents-auto/SKILL.md` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>
      test -f skills/implement-plan-with-subagents-auto/SKILL.md && \
      grep -q "^name: implement-plan-with-subagents-auto$" skills/implement-plan-with-subagents-auto/SKILL.md && \
      grep -q "REQUIRES subagent capability" skills/implement-plan-with-subagents-auto/SKILL.md && \
      grep -q "references/spec-compliance-reviewer.md" skills/implement-plan-with-subagents-auto/SKILL.md && \
      grep -q "references/code-quality-reviewer.md" skills/implement-plan-with-subagents-auto/SKILL.md && \
      grep -qE "first pass|FIRST" skills/implement-plan-with-subagents-auto/SKILL.md && \
      grep -qE "second pass|SECOND" skills/implement-plan-with-subagents-auto/SKILL.md && \
      grep -qE "new implementer|NEW implementer" skills/implement-plan-with-subagents-auto/SKILL.md && \
      grep -qE -i "re-review|reviewed again|re review" skills/implement-plan-with-subagents-auto/SKILL.md && \
      grep -q "DONE_WITH_CONCERNS" skills/implement-plan-with-subagents-auto/SKILL.md && \
      grep -q "BLOCKED" skills/implement-plan-with-subagents-auto/SKILL.md && \
      grep -q "NEEDS_CONTEXT" skills/implement-plan-with-subagents-auto/SKILL.md && \
      grep -qi "amend" skills/implement-plan-with-subagents-auto/SKILL.md && \
      grep -qi "force-push\|force push" skills/implement-plan-with-subagents-auto/SKILL.md && \
      grep -qi "rebase" skills/implement-plan-with-subagents-auto/SKILL.md && \
      grep -qE -i "orchestrator.*dirty.*worktree|orchestrator.*worktree.*check" skills/implement-plan-with-subagents-auto/SKILL.md && \
      grep -qE "per orchestration cycle|per-orchestration-cycle" skills/implement-plan-with-subagents-auto/SKILL.md && \
      grep -qE -i "no inline fallback|no fallback|does not fall back" skills/implement-plan-with-subagents-auto/SKILL.md && \
      grep -qE -i "same working tree|same tree|sequentially.*tree|tree.*sequentially" skills/implement-plan-with-subagents-auto/SKILL.md && \
      ! grep -qE "git push --force|git push -f|git commit --amend|git rebase " skills/implement-plan-with-subagents-auto/SKILL.md && \
      echo "implement-plan-with-subagents-auto SKILL.md acceptance passes"
    </automated>
  </verify>
  <done>skills/implement-plan-with-subagents-auto/SKILL.md exists with frontmatter + body covering: opening clarifier with all five sibling references, REQUIRES subagent capability + no-inline-fallback per D69, no-worktree-isolation per D78, plan-artifact-path input with ambiguity fallback, four-state status protocol per D74 with which-subagent-ran + iteration-count audit, orchestrator-owned dirty-worktree check per D79, full workflow with implementer → spec-compliance reviewer (FIRST pass per D70) → fix loop spawning NEW implementer (D71) + re-review (D72/IMPL-09) → code-quality reviewer (SECOND pass per D70) → same fix loop → commit per orchestration cycle (D75), subagent briefs for all 3 roles citing references/ files by RELATIVE PATH, failed-commit → BLOCKED per D77, NO --amend / no rebase / no force-push, plan-deviation policy per D80, immutability with adjust-plan-granularity-* revision path. NO anti-sycophancy section. NO reviewer-prompt body embedded inline.</done>
</task>

<task type="auto">
  <name>Task 2: Author skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md + code-quality-reviewer.md</name>
  <read_first>
    - skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md (the file being created — empty / non-existent)
    - skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md (the file being created — empty / non-existent)
    - skills/implement-plan-with-subagents-auto/SKILL.md (Task 1 output — establishes the subagent brief shape and the reviewer roles)
    - .planning/phases/05-implementation-family/05-CONTEXT.md (LOCKED — D70 names the two review concerns; CONTEXT.md `<specifics>` block specifies ~50-100 lines per reviewer file with the focus areas)
    - skills/afk-exploration/references/pre-mortem-analysis.md (references/ folder shape source — what a reference file looks like; structured prompt with Core Principle / Process / Templates / Output Template)
    - skills/plan-strict-auto/SKILL.md (the verification + acceptance criteria fields the spec-compliance reviewer checks against)
    - skills/plan-loose-auto/SKILL.md (the lighter verification language a spec-compliance reviewer adapts to)
  </read_first>
  <files>skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md, skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md</files>
  <action>
    Create both reference files in the same task (paired — they exist together or not at all). Both files are PLAIN MARKDOWN with NO YAML frontmatter (reference docs do not carry frontmatter — see `.planning/codebase/CONVENTIONS.md` and the Phase 1 decision that reference docs are docs, not skills). Both target ~50-100 lines.

    **File A: `skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md`**

    Sections (executor discretion on exact names; content-shape requirements below MUST appear):

    1. **Top-of-file framing line** stating this reference is loaded by the spec-compliance reviewer subagent dispatched by `implement-plan-with-subagents-auto` (and by its interactive sibling). The orchestrator itself does not read this file.

    2. **Focus area** — the SINGLE question this reviewer answers: "Does the diff implement what the task said it would?" State this question (or near-verbatim) verbatim near the top.

    3. **What spec-compliance is** (and is NOT) — short list of what counts: did the implementer touch the files the task said it would (files-modified field for strict tasks; inferred for loose); does the diff satisfy the task's verification block (e.g., the named test passes, the grep returns the expected result); does the diff produce the post-conditions the acceptance criteria list. What spec-compliance is NOT: code style, naming, architectural taste, refactor opportunities — those are code-quality's territory (cite `code-quality-reviewer.md` by name).

    4. **Process / Steps** — a numbered list the reviewer follows:
       a. Read the plan task READ-ONLY (the implementer's brief tells you which task identifier).
       b. Inspect the working tree's modified files (e.g., `git status --porcelain`, `git diff`, file-by-file reads).
       c. Run the task's verification block if present (e.g., the named test, the grep, the jq check) and record PASS / FAIL with output snippets.
       d. Compare against the task's acceptance criteria. Each criterion: SATISFIED / MISSING / PARTIAL.
       e. Identify any spec-compliance gaps as ACTIONABLE FINDINGS (concrete, not vague).
       f. Write the structured review output (see template below).

    5. **Output template** — a markdown shape the reviewer's structured review file uses. MUST include:
       - A verdict line: `Verdict: PASS` or `Verdict: ISSUES`.
       - A findings list (one per gap), each with: what was expected (per the task), what was observed (per the diff), suggested fix (concrete).
       - References: paths to the plan task + modified files.

    6. **Hard constraints** — the reviewer MUST NOT modify code; MUST NOT modify the plan artifact (it is immutable per `docs/workflow/v1/immutability.md`); reads `git diff` and source code, writes only its review output file.

    **File B: `skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md`**

    Sections (executor discretion; content-shape requirements MUST appear):

    1. **Top-of-file framing line** — same orientation note as file A.

    2. **Focus area** — the SINGLE question: "Is the diff well-structured, safe, idiomatic given the codebase?" State this question (or near-verbatim) near the top.

    3. **What code-quality is** (and is NOT) — the reviewer checks:
       - Readability (variable names, function decomposition, dead code).
       - Safety (input validation, error handling for the conditions the diff introduces, no obvious null-deref / type-cast hazards).
       - Idiomatic patterns given the codebase (consistent with how similar functionality is implemented elsewhere; uses the project's existing helpers/types).
       - Regression risk (does the diff break anything that was passing before; are there unrelated files modified that shouldn't be).
       Code-quality is NOT: whether the diff implements the task (that is spec-compliance's territory — cite `spec-compliance-reviewer.md` by name).

    4. **Process / Steps** — numbered list. Same shape as file A but oriented to the code-quality concern.

    5. **Output template** — same shape as file A (Verdict + findings + references).

    6. **Hard constraints** — same as file A (no code modification; reviewer writes only its review output).

    Voice and discipline:
    - Plain prose; numbered lists for processes; tables OK but not required.
    - NO YAML frontmatter on either file (reference docs do not carry frontmatter).
    - Each file MUST reference its sibling reviewer by name (so a reader knows the two-pass topology and the division of concerns).
    - Each file MUST cite `docs/workflow/v1/immutability.md` for the "plan artifact is read-only" constraint.
    - File length target ~50-100 lines per CONTEXT.md `<specifics>` block.
  </action>
  <acceptance_criteria>
    Run these checks and ALL must pass:

    1. `test -f skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md`.
    2. `test -f skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md`.
    3. Both files have NO YAML frontmatter — `head -1 skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md` returns a line that is NOT `---` AND `head -1 skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md` returns a line that is NOT `---`.
    4. spec-compliance-reviewer.md focus question present — `grep -cE -i "does the diff implement what the task" skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md` — at least 1.
    5. spec-compliance-reviewer.md references the sibling reviewer — `grep -c "code-quality-reviewer.md" skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md` — at least 1.
    6. spec-compliance-reviewer.md cites immutability — `grep -c "docs/workflow/v1/immutability.md" skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md` — at least 1.
    7. spec-compliance-reviewer.md contains the verdict structure — `grep -cE -i "Verdict.*PASS|Verdict.*ISSUES|PASS|ISSUES" skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md` — at least 1.
    8. spec-compliance-reviewer.md has a process / steps section — `grep -cE -i "^## (Process|Steps|Workflow)" skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md` — at least 1.
    9. code-quality-reviewer.md focus question present — `grep -cE -i "is the diff (well-structured|well structured)" skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md` — at least 1.
    10. code-quality-reviewer.md references the sibling reviewer — `grep -c "spec-compliance-reviewer.md" skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md` — at least 1.
    11. code-quality-reviewer.md cites immutability — `grep -c "docs/workflow/v1/immutability.md" skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md` — at least 1.
    12. code-quality-reviewer.md contains the verdict structure — `grep -cE -i "Verdict.*PASS|Verdict.*ISSUES|PASS|ISSUES" skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md` — at least 1.
    13. code-quality-reviewer.md has a process / steps section — `grep -cE -i "^## (Process|Steps|Workflow)" skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md` — at least 1.
    14. Both files MUST NOT instruct the reviewer to modify code — `grep -cE -i "modify the code|edit the source|change the file" skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md` returns zero — OR if present, only inside a "MUST NOT" / "do not" / "never" / "forbidden" prohibition (negative grep tolerated: `grep -cE -i "(MUST NOT|do not|never|forbidden).*(modify|edit|change).{0,40}code|code.{0,40}(MUST NOT|do not|never|forbidden).{0,40}(modify|edit|change)" skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md` returns at least 2 — one per file).
    15. Length sanity — `wc -l skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md` returns at least 30 lines AND at most 200 lines (target ~50-100; the bounds are lenient).
    16. Length sanity — `wc -l skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md` returns at least 30 lines AND at most 200 lines.
  </acceptance_criteria>
  <verify>
    <automated>
      test -f skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md && \
      test -f skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md && \
      [ "$(head -1 skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md)" != "---" ] && \
      [ "$(head -1 skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md)" != "---" ] && \
      grep -qE -i "does the diff implement what the task" skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md && \
      grep -q "code-quality-reviewer.md" skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md && \
      grep -q "docs/workflow/v1/immutability.md" skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md && \
      grep -qE -i "is the diff (well-structured|well structured)" skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md && \
      grep -q "spec-compliance-reviewer.md" skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md && \
      grep -q "docs/workflow/v1/immutability.md" skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md && \
      LC1=$(wc -l < skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md) && \
      LC2=$(wc -l < skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md) && \
      [ "$LC1" -ge 30 ] && [ "$LC1" -le 200 ] && \
      [ "$LC2" -ge 30 ] && [ "$LC2" -le 200 ] && \
      echo "subagents-auto references acceptance passes"
    </automated>
  </verify>
  <done>Both reference files exist under skills/implement-plan-with-subagents-auto/references/: spec-compliance-reviewer.md scoped to "does the diff implement what the task said it would?" + code-quality-reviewer.md scoped to "is the diff well-structured, safe, idiomatic given the codebase?". Each ~50-100 lines, no YAML frontmatter (per CONVENTIONS.md), cite immutability.md, reference the sibling reviewer by name, contain a verdict structure (PASS / ISSUES) and a process / steps section. Each enforces "reviewer does not modify code" via explicit prohibition language. D87 / IMPL-08 satisfied for the auto skill.</done>
</task>

<task type="auto">
  <name>Task 3: Author skills/implement-plan-with-subagents-interactive/SKILL.md (orchestrator body)</name>
  <read_first>
    - skills/implement-plan-with-subagents-interactive/SKILL.md (the file being created — empty / non-existent)
    - skills/implement-plan-with-subagents-auto/SKILL.md (Task 1 output — sibling consistency)
    - .planning/phases/05-implementation-family/05-CONTEXT.md
    - skills/plan-loose-interactive/SKILL.md (Phase 4 — interactive pattern; 4 anti-sycophancy markers verbatim)
    - skills/plan-strict-interactive/SKILL.md (Phase 4 — interactive pattern with stakes amplifier)
    - skills/discussion/SKILL.md (anti-sycophancy stance source)
    - skills/implement-plan-interactive/SKILL.md (Plan 05-02 Task 2 output — interactive sibling reference for ASK-before-commit + execution-time stakes amplifier wording)
    - skills/afk-exploration/SKILL.md (orchestrator pattern)
    - docs/workflow/v1/thread-layout.md
    - docs/workflow/v1/immutability.md
    - docs/workflow/v1/filename-grammar.md
  </read_first>
  <files>skills/implement-plan-with-subagents-interactive/SKILL.md</files>
  <action>
    Create the file with YAML frontmatter and a markdown body. Frontmatter required fields:
    - `name: implement-plan-with-subagents-interactive`
    - `description:` a single sentence with the "Use when..." trigger AND explicit interaction mode (collaborative; orchestrator runs the subagent loop per task but ASKS the user before committing each cycle). Mention the sibling `implement-plan-with-subagents-auto`, the single-agent plan-driven siblings, and the less-structured-input siblings. State that this skill REQUIRES subagent capability and has NO inline fallback (D69 / IMPL-07).
    - `metadata.author: https://github.com/Jei-sKappa`
    - `metadata.version: 1.0.0`

    Body sections (executor discretion on names; behavioral tokens below MUST appear):

    1. **Opening clarifier paragraph** naming all five siblings + the two axes per D64/D65/D66.

    2. **Subagent Capability Precondition** section. MUST contain "REQUIRES subagent capability" verbatim or near-verbatim per D69 / IMPL-07. NO inline fallback. Point users without subagent capability at `implement-plan-auto` / `implement-plan-interactive` (Plan 05-02).

    3. **Anti-Sycophancy Stance** section. Carry the FOUR markers from `skills/discussion/SKILL.md` VERBATIM:
       - "Disagree when you disagree"
       - "Push back on weak or incomplete reasoning"
       - "Do not treat pushback as correctness"
       - "Refuse to log a <task / commit> you believe is wrong without flagging it"
       Plus the EXECUTION-TIME stakes amplifier ("Bad commits become expensive to rewind" or near-verbatim).

    4. **No Worktree Isolation** section (D78). Same as the auto sibling: subagents run sequentially on the SAME working tree.

    5. **Inputs** section. Same as the auto sibling: V1 plan artifact path with ambiguity fallback.

    6. **Four-State Status Protocol** section (D74, IMPL-10). Same four states with the subagent audit (which subagent ran + iteration count + final state per task).

    7. **Dirty Worktree Handling** section (D79, IMPL-14). Orchestrator-owned check — same language as the auto sibling.

    8. **Workflow** section. Same orchestration loop as the auto sibling (D66/D68/D70/D71/D72) BUT with the user-confirmation checkpoints inserted at the appropriate places (per IMPL-06 + D68):
       - BEFORE the orchestration cycle for each task, OPTIONALLY surface the task to the user and push back per the `## Anti-Sycophancy Stance` if it appears contradictory or under-specified for the current code state (the orchestrator sees only the plan task and the working tree — the user may have additional context).
       - AFTER all reviews pass (spec-compliance + code-quality both PASS), the orchestrator ASKS the user "Commit task <N> now? (yes / no / show diff first)" before committing the orchestration cycle.
       - On confirm, commit per `## Commit Policy`.
       - Write the orchestration cycle task report.
       - On failed commit: report `BLOCKED`, stop.

    9. **Subagent Briefs** section. Same 3 briefs (implementer, spec-compliance reviewer, code-quality reviewer) — same shape as the auto sibling. The reference paths MUST point at THIS skill's `references/` folder (not the auto sibling's), i.e., `references/spec-compliance-reviewer.md` and `references/code-quality-reviewer.md` resolved from THIS skill's base directory (executor's `references/` folder in Task 4).

    10. **Commit Policy** section. MUST contain:
        - Cadence rule for the interactive variant (D76): per orchestration cycle, ASKS before each commit. Literal phrase "ASK the user before committing" (or near-verbatim with "ASK" + "before committing").
        - Failed-commit → BLOCKED (D77).
        - No history rewriting (NO `--amend`, no rebase, no force-push).

    11. **Plan Deviation Policy** section (D80). Same as the auto sibling. Add: during the orchestration cycle, the orchestrator surfaces deviations LIVE to the user as they emerge from reviewer findings — the user may choose to skip a fix iteration (orchestrator MAY honor this but reports `DONE_WITH_CONCERNS` for the task).

    12. **Decision Log** section enforcing D93. Default: no auto-write. Write a log ONLY when durable trade-offs / rejected alternatives emerge.

    13. **Scope Drift** section. PARK as Inbox via `capture-inbox`, SPLIT into own plan / discussion, or DEFER.

    14. **Immutability** section: plan artifacts are READ-ONLY; revisions via `adjust-plan-granularity-*`.

    Voice and discipline:
    - Dense, declarative, citation-first.
    - All 4 anti-sycophancy markers verbatim; execution-time stakes amplifier present.
    - References `references/spec-compliance-reviewer.md` and `references/code-quality-reviewer.md` by relative path.
    - Do NOT embed full reviewer prompt body inline.
    - Do NOT permit inline fallback.
  </action>
  <acceptance_criteria>
    Run these checks and ALL must pass:

    1. `test -f skills/implement-plan-with-subagents-interactive/SKILL.md`.
    2. `grep -c "^name: implement-plan-with-subagents-interactive$" skills/implement-plan-with-subagents-interactive/SKILL.md` — exactly 1.
    3. `grep -c "^  version: 1.0.0$" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    4. `grep -c "^  author: https://github.com/Jei-sKappa$" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    5. `grep -c "Use when" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    6. `grep -c "implement-plan-with-subagents-auto" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    7. `grep -cE "implement-plan-(auto|interactive)" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    8. `grep -c "REQUIRES subagent capability" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    9. `grep -c -i "Anti-Sycophancy" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    10. `grep -c "Disagree when you disagree" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    11. `grep -cE "Push back on (weak|incomplete)" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    12. `grep -c "Do not treat pushback as correctness" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    13. `grep -cE "Refuse to log" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    14. `grep -cE "expensive to rewind|expensive to undo" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    15. `grep -c "references/spec-compliance-reviewer.md" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    16. `grep -c "references/code-quality-reviewer.md" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    17. `grep -cE "first pass|FIRST" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    18. `grep -cE "second pass|SECOND" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    19. `grep -cE "new implementer|NEW implementer" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    20. `grep -cE -i "re-review|reviewed again|re review" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    21. `grep -c "DONE_WITH_CONCERNS" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    22. `grep -c "BLOCKED" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    23. `grep -c "NEEDS_CONTEXT" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    24. `grep -c -i "amend" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    25. `grep -c -i "force-push\|force push" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    26. `grep -c -i "rebase" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    27. `grep -cE -i "orchestrator.*dirty.*worktree|orchestrator.*worktree.*check" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    28. `grep -cE "per orchestration cycle|per-orchestration-cycle" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    29. `grep -cE -i "ASK.*before committing|before commit.*ASK" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    30. `grep -cE -i "no inline fallback|no fallback|does not fall back" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    31. `grep -cE -i "same working tree|same tree|sequentially.*tree|tree.*sequentially" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    32. `grep -c -i "Decision Log\|decision log" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1 (D93 section).
    33. `grep -c "docs/workflow/v1/immutability.md" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    34. `grep -c "docs/threads/<thread>/plans/" skills/implement-plan-with-subagents-interactive/SKILL.md` — at least 1.
    35. The body MUST NOT contain force-push instructions. `grep -E "git push --force|git push -f|git commit --amend|git rebase " skills/implement-plan-with-subagents-interactive/SKILL.md` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>
      test -f skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -q "^name: implement-plan-with-subagents-interactive$" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -q "REQUIRES subagent capability" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -qi "Anti-Sycophancy" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -q "Disagree when you disagree" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -qE "Push back on (weak|incomplete)" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -q "Do not treat pushback as correctness" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -qE "Refuse to log" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -qE "expensive to rewind|expensive to undo" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -q "references/spec-compliance-reviewer.md" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -q "references/code-quality-reviewer.md" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -qE "first pass|FIRST" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -qE "second pass|SECOND" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -qE "new implementer|NEW implementer" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -q "DONE_WITH_CONCERNS" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -q "BLOCKED" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -q "NEEDS_CONTEXT" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -qi "amend" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -qi "force-push\|force push" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -qi "rebase" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -qE -i "orchestrator.*dirty.*worktree|orchestrator.*worktree.*check" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -qE "per orchestration cycle|per-orchestration-cycle" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -qE -i "ASK.*before committing|before commit.*ASK" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -qE -i "no inline fallback|no fallback|does not fall back" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      grep -qE -i "same working tree|same tree|sequentially.*tree|tree.*sequentially" skills/implement-plan-with-subagents-interactive/SKILL.md && \
      ! grep -qE "git push --force|git push -f|git commit --amend|git rebase " skills/implement-plan-with-subagents-interactive/SKILL.md && \
      echo "implement-plan-with-subagents-interactive SKILL.md acceptance passes"
    </automated>
  </verify>
  <done>skills/implement-plan-with-subagents-interactive/SKILL.md exists with frontmatter + body covering: opening clarifier with all five siblings, REQUIRES subagent capability per D69, anti-sycophancy stance with 4 verbatim markers + execution-time stakes amplifier, no-worktree-isolation per D78, plan-artifact-path input, four-state status protocol per D74 with subagent audit, orchestrator-owned dirty-worktree check per D79, full workflow with implementer → spec-compliance reviewer FIRST pass → fix loop spawning NEW implementer + re-review → code-quality reviewer SECOND pass → same fix loop → ASK-before-commit per cycle per D76, subagent briefs for all 3 roles citing references/ files by RELATIVE PATH, failed-commit → BLOCKED per D77, NO --amend / no rebase / no force-push, plan-deviation policy with live push-back per D80, D93 decision-log opt-in default, scope-drift handling, immutability with adjust-plan-granularity-* revision path.</done>
</task>

<task type="auto">
  <name>Task 4: Author skills/implement-plan-with-subagents-interactive/references/spec-compliance-reviewer.md + code-quality-reviewer.md</name>
  <read_first>
    - skills/implement-plan-with-subagents-interactive/references/spec-compliance-reviewer.md (the file being created — empty / non-existent)
    - skills/implement-plan-with-subagents-interactive/references/code-quality-reviewer.md (the file being created — empty / non-existent)
    - skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md (Task 2 output — the auto sibling's reviewer prompt; per D28 + CONTEXT.md `<decisions>` "Reviewer content reuse", the interactive copy MAY be identical or vary slightly to match interactive orchestration behavior — executor discretion)
    - skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md (Task 2 output — same)
    - skills/implement-plan-with-subagents-interactive/SKILL.md (Task 3 output — this skill's orchestrator body references these reference files by relative path)
    - .planning/phases/05-implementation-family/05-CONTEXT.md (LOCKED — D87 / IMPL-08; "Reviewer content reuse" + D28 DRY-deferred)
    - skills/afk-exploration/references/pre-mortem-analysis.md (references/ folder shape source)
  </read_first>
  <files>skills/implement-plan-with-subagents-interactive/references/spec-compliance-reviewer.md, skills/implement-plan-with-subagents-interactive/references/code-quality-reviewer.md</files>
  <action>
    Create both reference files under `skills/implement-plan-with-subagents-interactive/references/`. Both files MUST satisfy the same content-shape requirements as the auto sibling's reference files (Task 2). Per D28 + CONTEXT.md "Reviewer content reuse", V1 duplicates per-skill self-contained discipline — DRY abstraction is deferred to V2. The two options for the executor are:

    A. **Duplicate exactly** the contents of the auto sibling's reference files into this skill's `references/` folder. This is the simplest path and satisfies D87 / IMPL-08 for this skill independently.

    B. **Vary slightly** to match interactive orchestration behavior — e.g., add a one-line note that this reviewer is invoked from the interactive orchestrator and its findings will be surfaced LIVE to the user via the orchestrator's anti-sycophancy stance. The reviewer prompt body itself does not change behavior (reviewers don't see the user) — the note is for the human reading the reference file.

    The same content-shape requirements as Task 2 MUST hold for both files:

    **File A: `skills/implement-plan-with-subagents-interactive/references/spec-compliance-reviewer.md`**
    - Top-of-file framing line.
    - Focus area: "Does the diff implement what the task said it would?" (verbatim or near-verbatim).
    - What spec-compliance is and is NOT (cite `code-quality-reviewer.md`).
    - Process / Steps numbered list (read task → inspect diff → run verification → compare acceptance → identify gaps → write structured review).
    - Output template (Verdict + findings + references).
    - Hard constraints (reviewer does not modify code; reads `git diff` only; plan artifact is immutable per `docs/workflow/v1/immutability.md`).
    - No YAML frontmatter.

    **File B: `skills/implement-plan-with-subagents-interactive/references/code-quality-reviewer.md`**
    - Top-of-file framing line.
    - Focus area: "Is the diff well-structured, safe, idiomatic given the codebase?" (verbatim or near-verbatim).
    - What code-quality is and is NOT (cite `spec-compliance-reviewer.md`).
    - Process / Steps numbered list.
    - Output template.
    - Hard constraints.
    - No YAML frontmatter.

    Length target: ~50-100 lines per file (bounds 30-200 as in Task 2).
  </action>
  <acceptance_criteria>
    Run these checks and ALL must pass:

    1. `test -f skills/implement-plan-with-subagents-interactive/references/spec-compliance-reviewer.md`.
    2. `test -f skills/implement-plan-with-subagents-interactive/references/code-quality-reviewer.md`.
    3. Both files have NO YAML frontmatter — head -1 returns a line that is NOT `---` for both.
    4. spec-compliance-reviewer.md focus question — `grep -cE -i "does the diff implement what the task" skills/implement-plan-with-subagents-interactive/references/spec-compliance-reviewer.md` — at least 1.
    5. spec-compliance-reviewer.md references sibling reviewer — `grep -c "code-quality-reviewer.md" skills/implement-plan-with-subagents-interactive/references/spec-compliance-reviewer.md` — at least 1.
    6. spec-compliance-reviewer.md cites immutability — `grep -c "docs/workflow/v1/immutability.md" skills/implement-plan-with-subagents-interactive/references/spec-compliance-reviewer.md` — at least 1.
    7. spec-compliance-reviewer.md verdict structure — `grep -cE -i "Verdict.*PASS|Verdict.*ISSUES|PASS|ISSUES" skills/implement-plan-with-subagents-interactive/references/spec-compliance-reviewer.md` — at least 1.
    8. spec-compliance-reviewer.md process section — `grep -cE -i "^## (Process|Steps|Workflow)" skills/implement-plan-with-subagents-interactive/references/spec-compliance-reviewer.md` — at least 1.
    9. code-quality-reviewer.md focus question — `grep -cE -i "is the diff (well-structured|well structured)" skills/implement-plan-with-subagents-interactive/references/code-quality-reviewer.md` — at least 1.
    10. code-quality-reviewer.md references sibling reviewer — `grep -c "spec-compliance-reviewer.md" skills/implement-plan-with-subagents-interactive/references/code-quality-reviewer.md` — at least 1.
    11. code-quality-reviewer.md cites immutability — `grep -c "docs/workflow/v1/immutability.md" skills/implement-plan-with-subagents-interactive/references/code-quality-reviewer.md` — at least 1.
    12. code-quality-reviewer.md verdict structure — `grep -cE -i "Verdict.*PASS|Verdict.*ISSUES|PASS|ISSUES" skills/implement-plan-with-subagents-interactive/references/code-quality-reviewer.md` — at least 1.
    13. code-quality-reviewer.md process section — `grep -cE -i "^## (Process|Steps|Workflow)" skills/implement-plan-with-subagents-interactive/references/code-quality-reviewer.md` — at least 1.
    14. Length sanity for both files — `wc -l skills/implement-plan-with-subagents-interactive/references/spec-compliance-reviewer.md` returns at least 30 and at most 200; same for code-quality-reviewer.md.
  </acceptance_criteria>
  <verify>
    <automated>
      test -f skills/implement-plan-with-subagents-interactive/references/spec-compliance-reviewer.md && \
      test -f skills/implement-plan-with-subagents-interactive/references/code-quality-reviewer.md && \
      [ "$(head -1 skills/implement-plan-with-subagents-interactive/references/spec-compliance-reviewer.md)" != "---" ] && \
      [ "$(head -1 skills/implement-plan-with-subagents-interactive/references/code-quality-reviewer.md)" != "---" ] && \
      grep -qE -i "does the diff implement what the task" skills/implement-plan-with-subagents-interactive/references/spec-compliance-reviewer.md && \
      grep -q "code-quality-reviewer.md" skills/implement-plan-with-subagents-interactive/references/spec-compliance-reviewer.md && \
      grep -q "docs/workflow/v1/immutability.md" skills/implement-plan-with-subagents-interactive/references/spec-compliance-reviewer.md && \
      grep -qE -i "is the diff (well-structured|well structured)" skills/implement-plan-with-subagents-interactive/references/code-quality-reviewer.md && \
      grep -q "spec-compliance-reviewer.md" skills/implement-plan-with-subagents-interactive/references/code-quality-reviewer.md && \
      grep -q "docs/workflow/v1/immutability.md" skills/implement-plan-with-subagents-interactive/references/code-quality-reviewer.md && \
      LC1=$(wc -l < skills/implement-plan-with-subagents-interactive/references/spec-compliance-reviewer.md) && \
      LC2=$(wc -l < skills/implement-plan-with-subagents-interactive/references/code-quality-reviewer.md) && \
      [ "$LC1" -ge 30 ] && [ "$LC1" -le 200 ] && \
      [ "$LC2" -ge 30 ] && [ "$LC2" -le 200 ] && \
      echo "subagents-interactive references acceptance passes"
    </automated>
  </verify>
  <done>Both reference files exist under skills/implement-plan-with-subagents-interactive/references/: spec-compliance-reviewer.md + code-quality-reviewer.md. Per D87 / IMPL-08, the reviewer prompts live inside the skill folder, NOT as standalone V1 skills. Per D28 + CONTEXT.md "Reviewer content reuse", contents MAY be identical to the auto sibling's reviewer files OR vary slightly at executor discretion. Both ~50-100 lines, no frontmatter, cite immutability, reference sibling reviewer by name, contain verdict + process structure. D87 / IMPL-08 satisfied for the interactive skill.</done>
</task>

<task type="auto">
  <name>Task 5: Register implement-plan-with-subagents-auto and implement-plan-with-subagents-interactive in marketplace, scopes, and README</name>
  <read_first>
    - .claude-plugin/marketplace.json (state after Plan 05-02: JeisKappa-workflow has 17 entries including implement-plan-auto and implement-plan-interactive)
    - .vscode/settings.json (state after Plan 05-02: 25 alphabetically-sorted scopes)
    - README.md (state after Plan 05-02: 25 "Available skills" entries; Retired skills subsection at bottom)
    - AGENTS.md (the 4-touchpoint checklist)
    - .planning/phases/05-implementation-family/05-CONTEXT.md
    - skills/implement-auto/SKILL.md, skills/implement-interactive/SKILL.md, skills/implement-plan-auto/SKILL.md, skills/implement-plan-interactive/SKILL.md (Plan 05-01/05-02 outputs — README description style references)
  </read_first>
  <files>.claude-plugin/marketplace.json, .vscode/settings.json, README.md</files>
  <action>
    Update all three registration files atomically:

    1. **`.claude-plugin/marketplace.json`** — add `"./skills/implement-plan-with-subagents-auto"` and `"./skills/implement-plan-with-subagents-interactive"` to the JeisKappa-workflow `skills` array in alphabetical position (immediately AFTER `./skills/implement-plan-interactive` and BEFORE `./skills/plan-loose-auto`). After this change the JeisKappa-workflow array has exactly 19 entries. JeisKappa-skills stays unchanged at 8.

    2. **`.vscode/settings.json`** — add `"implement-plan-with-subagents-auto"` and `"implement-plan-with-subagents-interactive"` to `conventionalCommits.scopes`. Total length now 27. Keep alphabetical order — they fall between `implement-plan-interactive` and `meta-prompting`. The jq accessor MUST use the quoted form `."conventionalCommits.scopes"` for verification — the dotted key requires the quoted form. (NOTE: This is a regression check from Phase 4 lessons — DO NOT use the unquoted form which silently fails on the dotted key.)

    3. **`README.md`** — add two new "Available skills" entries (one per new skill), each with the established shape: `### [\`<name>\`](./skills/<name>/SKILL.md)` heading + paragraph description + fenced `npx skills add ...` block. Insert AFTER `implement-plan-interactive` and BEFORE `## Retired skills`. Description sentences MUST state explicit interaction mode AND the subagent-capability precondition (per DIST-05 + D69 / IMPL-07).

    Do NOT touch the `## Retired skills` subsection.

    Do NOT commit.
  </action>
  <acceptance_criteria>
    Run these checks and ALL must pass:

    1. `jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length == 19' .claude-plugin/marketplace.json` returns true.
    2. `jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/implement-plan-with-subagents-auto")' .claude-plugin/marketplace.json` returns non-null.
    3. `jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/implement-plan-with-subagents-interactive")' .claude-plugin/marketplace.json` returns non-null.
    4. `jq -e '.plugins[] | select(.name=="JeisKappa-skills") | .skills | length == 8' .claude-plugin/marketplace.json` returns true.
    5. `jq -e '."conventionalCommits.scopes" | length == 27' .vscode/settings.json` returns true.
    6. `jq -e '."conventionalCommits.scopes" | index("implement-plan-with-subagents-auto")' .vscode/settings.json` returns non-null.
    7. `jq -e '."conventionalCommits.scopes" | index("implement-plan-with-subagents-interactive")' .vscode/settings.json` returns non-null.
    8. `jq -e '."conventionalCommits.scopes" == (."conventionalCommits.scopes" | sort)' .vscode/settings.json` returns true (alphabetical).
    9. `grep -c "^### \[\`implement-plan-with-subagents-auto\`\]" README.md` — exactly 1.
    10. `grep -c "^### \[\`implement-plan-with-subagents-interactive\`\]" README.md` — exactly 1.
    11. `grep -c "npx skills add Jei-sKappa/skills --skill implement-plan-with-subagents-auto" README.md` — exactly 1.
    12. `grep -c "npx skills add Jei-sKappa/skills --skill implement-plan-with-subagents-interactive" README.md` — exactly 1.
    13. Both new README descriptions MUST mention subagent capability — `grep -cE -i "subagent capability|REQUIRES subagent" README.md` — at least 2 (one per new entry; the description sentence states the precondition).
    14. Both new README headings fall BEFORE `## Retired skills`. Verify:
        ```bash
        bash -c 'A=$(grep -n "^### \[\`implement-plan-with-subagents-auto\`\]" README.md | cut -d: -f1); B=$(grep -n "^### \[\`implement-plan-with-subagents-interactive\`\]" README.md | cut -d: -f1); R=$(grep -n "^## Retired skills" README.md | cut -d: -f1); [[ $A -lt $R && $B -lt $R ]] && echo OK'
        ```
        prints OK.
    15. No new git commit created.
  </acceptance_criteria>
  <verify>
    <automated>
      jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length == 19' .claude-plugin/marketplace.json >/dev/null && \
      jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/implement-plan-with-subagents-auto")' .claude-plugin/marketplace.json >/dev/null && \
      jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/implement-plan-with-subagents-interactive")' .claude-plugin/marketplace.json >/dev/null && \
      jq -e '."conventionalCommits.scopes" | length == 27' .vscode/settings.json >/dev/null && \
      jq -e '."conventionalCommits.scopes" | index("implement-plan-with-subagents-auto")' .vscode/settings.json >/dev/null && \
      jq -e '."conventionalCommits.scopes" | index("implement-plan-with-subagents-interactive")' .vscode/settings.json >/dev/null && \
      jq -e '."conventionalCommits.scopes" == (."conventionalCommits.scopes" | sort)' .vscode/settings.json >/dev/null && \
      [ "$(grep -c '^### \[`implement-plan-with-subagents-auto`\]' README.md)" = "1" ] && \
      [ "$(grep -c '^### \[`implement-plan-with-subagents-interactive`\]' README.md)" = "1" ] && \
      [ "$(grep -c 'npx skills add Jei-sKappa/skills --skill implement-plan-with-subagents-auto' README.md)" = "1" ] && \
      [ "$(grep -c 'npx skills add Jei-sKappa/skills --skill implement-plan-with-subagents-interactive' README.md)" = "1" ] && \
      echo "implement-plan-with-subagents-* pair registration acceptance passes"
    </automated>
  </verify>
  <done>marketplace.json JeisKappa-workflow array has 19 entries (Phase 5 final target reached), .vscode/settings.json scopes has 27 entries (alphabetically sorted), README.md has 2 new install blocks for the implement-plan-with-subagents-* pair with subagent-capability precondition language in each description. No git commit created.</done>
</task>

</tasks>

<verification>
After all 5 tasks complete:

1. **All skill files + reference files exist:**
   ```bash
   ls -la skills/implement-plan-with-subagents-auto/SKILL.md \
           skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md \
           skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md \
           skills/implement-plan-with-subagents-interactive/SKILL.md \
           skills/implement-plan-with-subagents-interactive/references/spec-compliance-reviewer.md \
           skills/implement-plan-with-subagents-interactive/references/code-quality-reviewer.md
   ```

2. **D69 (subagent capability precondition) + D70 (2-pass loop) + D71 (NEW implementer per fix) + D72/IMPL-09 (re-review every fix) + D74 (four-state status) + D75/D76 (per-orchestration-cycle commits) + D77 (failed-commit → BLOCKED) + D78 (no worktree isolation) + D79 (orchestrator-owned dirty-worktree check):**
   ```bash
   for SKILL in skills/implement-plan-with-subagents-auto/SKILL.md skills/implement-plan-with-subagents-interactive/SKILL.md; do
     grep -q "REQUIRES subagent capability" "$SKILL" || echo "FAIL D69 in $SKILL"
     grep -qE "first pass|FIRST" "$SKILL" || echo "FAIL D70 first-pass in $SKILL"
     grep -qE "second pass|SECOND" "$SKILL" || echo "FAIL D70 second-pass in $SKILL"
     grep -qE "new implementer|NEW implementer" "$SKILL" || echo "FAIL D71 in $SKILL"
     grep -qE -i "re-review|reviewed again" "$SKILL" || echo "FAIL D72/IMPL-09 in $SKILL"
     grep -q "DONE_WITH_CONCERNS" "$SKILL" || echo "FAIL D74 in $SKILL"
     grep -q "BLOCKED" "$SKILL" || echo "FAIL D74/D77 in $SKILL"
     grep -q "NEEDS_CONTEXT" "$SKILL" || echo "FAIL D74 in $SKILL"
     grep -qE "per orchestration cycle|per-orchestration-cycle" "$SKILL" || echo "FAIL D75/D76 cadence in $SKILL"
     grep -qE -i "same working tree|same tree|sequentially.*tree" "$SKILL" || echo "FAIL D78 in $SKILL"
     grep -qE -i "orchestrator.*dirty.*worktree|orchestrator.*worktree.*check" "$SKILL" || echo "FAIL D79 in $SKILL"
     grep -qi "amend" "$SKILL" || echo "FAIL --amend prohibition in $SKILL"
     grep -qi "force-push\|force push" "$SKILL" || echo "FAIL force-push prohibition in $SKILL"
     grep -qi "rebase" "$SKILL" || echo "FAIL rebase prohibition in $SKILL"
   done
   ```

3. **D87 / IMPL-08 — embedded reviewer prompts inside both skill folders, NOT standalone skills:**
   ```bash
   test -f skills/implement-plan-with-subagents-auto/references/spec-compliance-reviewer.md && \
   test -f skills/implement-plan-with-subagents-auto/references/code-quality-reviewer.md && \
   test -f skills/implement-plan-with-subagents-interactive/references/spec-compliance-reviewer.md && \
   test -f skills/implement-plan-with-subagents-interactive/references/code-quality-reviewer.md && \
   # Reference files are NOT registered as standalone skills in marketplace.json
   ! jq -e '.plugins[].skills | index("./skills/spec-compliance-reviewer")' .claude-plugin/marketplace.json >/dev/null && \
   ! jq -e '.plugins[].skills | index("./skills/code-quality-reviewer")' .claude-plugin/marketplace.json >/dev/null && \
   echo "D87/IMPL-08 reviewer-as-supporting-files OK"
   ```

4. **Anti-sycophancy markers in interactive skill:**
   ```bash
   grep -q "Disagree when you disagree" skills/implement-plan-with-subagents-interactive/SKILL.md
   grep -qE "Push back on (weak|incomplete)" skills/implement-plan-with-subagents-interactive/SKILL.md
   grep -q "Do not treat pushback as correctness" skills/implement-plan-with-subagents-interactive/SKILL.md
   grep -qE "Refuse to log" skills/implement-plan-with-subagents-interactive/SKILL.md
   grep -qE "expensive to rewind|expensive to undo" skills/implement-plan-with-subagents-interactive/SKILL.md
   ```

5. **Four-touchpoint registration (final Phase 5 targets):**
   ```bash
   jq '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length' .claude-plugin/marketplace.json    # expect 19
   jq '."conventionalCommits.scopes" | length' .vscode/settings.json                                              # expect 27
   grep -cE "^### \[\`implement-plan-with-subagents-(auto|interactive)\`\]" README.md                              # expect 2
   ```
</verification>

<success_criteria>
- skills/implement-plan-with-subagents-auto/SKILL.md and skills/implement-plan-with-subagents-interactive/SKILL.md exist with correct V1 frontmatter.
- Both subagent skills declare subagent capability as a precondition with no inline fallback per D69 / IMPL-07 ("REQUIRES subagent capability").
- Both subagent skills document the 2-pass review loop per D70 (spec-compliance FIRST, code-quality SECOND).
- Both subagent skills document the fix-loop respawning a NEW implementer per D71.
- Both subagent skills document re-review of every fix per D72 / IMPL-09.
- Both subagent skills document the four-state status protocol per D74 with the subagent audit (which subagent ran + iteration count + final state).
- Both subagent skills document the per-orchestration-cycle commit cadence per D75 / D76.
- Both subagent skills document the failed-commit → BLOCKED rule per D77 and forbid --amend / rebase / force-push.
- Both subagent skills document the no-worktree-isolation rule per D78 (subagents run sequentially on the same working tree).
- Both subagent skills document the orchestrator-owned dirty-worktree check per D79.
- Both subagent skills document the plan-deviation policy per D80.
- Both subagent skills' bodies cite their `references/spec-compliance-reviewer.md` and `references/code-quality-reviewer.md` by relative path.
- Both subagent skill folders have a `references/` subfolder containing `spec-compliance-reviewer.md` and `code-quality-reviewer.md` per D87 / IMPL-08 (4 reference files total, NOT registered as standalone skills).
- Both reference files in each folder satisfy: no frontmatter, focus-area question stated, sibling reviewer cited by name, immutability.md cited, verdict + process structure present, ~30-200 lines.
- implement-plan-with-subagents-interactive carries all 4 anti-sycophancy markers verbatim + execution-time stakes amplifier.
- implement-plan-with-subagents-interactive enforces D93.
- marketplace.json JeisKappa-workflow array has exactly 19 entries (Phase 5 final).
- .vscode/settings.json conventionalCommits.scopes has exactly 27 entries, alphabetically sorted (jq accessor uses the quoted form `."conventionalCommits.scopes"`).
- README.md has 2 new entries inserted before `## Retired skills`, each mentioning the subagent-capability precondition.
- No commit created during this plan.
</success_criteria>

<output>
Create `.planning/phases/05-implementation-family/05-03-SUMMARY.md` when done.
</output>
