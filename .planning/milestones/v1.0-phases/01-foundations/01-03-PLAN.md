---
phase: 01-foundations
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - .gitignore
  - .claude-plugin/marketplace.json
  - AGENTS.md
autonomous: true
requirements:
  - THRD-03
  - DIST-04

must_haves:
  truths:
    - "User's `.gitignore` excludes `docs/threads/**/.wip/` recursively, so any thread's WIP scratch is excluded by default"
    - "`.claude-plugin/marketplace.json` contains TWO plugin entries: the existing `JeisKappa-skills` (unchanged) and a new `JeisKappa-workflow` with an empty `skills` array"
    - "`AGENTS.md` contains a section that points agents to `docs/workflow/v1/README.md` as the canonical V1 workflow reference"
    - "`AGENTS.md` does NOT duplicate rules from the canonical doc set — it only points"
  artifacts:
    - path: ".gitignore"
      provides: "Recursive ignore for thread-local WIP scratch"
      contains: "docs/threads/**/.wip/"
    - path: ".claude-plugin/marketplace.json"
      provides: "Second plugin entry (`JeisKappa-workflow`) ready to receive V1 spine skill folders"
      contains: "JeisKappa-workflow"
    - path: "AGENTS.md"
      provides: "Pointer section to canonical V1 workflow doc set"
      contains: "docs/workflow/v1/README.md"
  key_links:
    - from: ".gitignore"
      to: "docs/threads/<thread>/.wip/ folders defined in docs/workflow/v1/thread-layout.md"
      via: "recursive glob"
      pattern: "docs/threads/\\*\\*/\\.wip/"
    - from: ".claude-plugin/marketplace.json"
      to: "future V1 spine skills (added per-phase from Phase 2 onward)"
      via: "plugins[].skills array"
      pattern: "JeisKappa-workflow"
    - from: "AGENTS.md"
      to: "docs/workflow/v1/README.md (created by Plan 01)"
      via: "relative path reference"
      pattern: "docs/workflow/v1/README\\.md"
---

<objective>
Lay the registration baseline that subsequent phases will append to: gitignore the thread-local WIP scratch, register the second marketplace plugin (`JeisKappa-workflow`) with an empty skills array, and add a short pointer section to `AGENTS.md` so agents discover the canonical V1 workflow reference doc set created in Plans 01 and 02.

Purpose: Without the `.gitignore` rule, any future thread's `.wip/` drafts (which `immutability.md` says are editable and expected to live there) would be committed by default — silently violating the V1 "drafts not on mainline" intent. Without the second plugin entry, Phase 2 (`capture-inbox`, `discussion`, `seeded-discussion`) has no place to register V1 workflow skills under the `JeisKappa Workflow` display group. Without the `AGENTS.md` pointer, agents starting a session see the existing repo rules but have no signpost to the new V1 workflow doc set — discoverability collapses.

Output: Three edited files. None of these edits create new directories or new content categories; they wire what Plans 01 and 02 produced into the existing repo registration surface.

Depends on Plan 01 because the `AGENTS.md` pointer must reference `docs/workflow/v1/README.md`, which Plan 01 creates. (Plan 02's docs are also linked transitively from that README.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundations/01-CONTEXT.md
@.planning/codebase/CONVENTIONS.md

# Files being edited — executor MUST read each before editing
@.gitignore
@.claude-plugin/marketplace.json
@AGENTS.md

# Plan 01 output (target of the AGENTS.md pointer)
@docs/workflow/v1/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add recursive `.wip/` ignore rule to `.gitignore`</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (current contents — verify the rule is not already present)
    - .planning/phases/01-foundations/01-CONTEXT.md (Registration Baseline decision: rule lives at the repo root)
  </read_first>
  <action>
    Edit `.gitignore` at the repo root. Append the exact rule `docs/threads/**/.wip/` on its own line, preserving the existing entries (`temp/`, `.library/`, `.claude/`, `.codex/`, `.cursor/`) unchanged. Place the new rule under a one-line comment `# Thread-local WIP scratch (per docs/workflow/v1/thread-layout.md)` so future readers know what it codifies.

    Use the Edit tool — do NOT rewrite the file. Concretely: append two new lines at the end of the file (blank line first if the existing file does not already end with a blank line), the comment line, then the rule line.

    The literal string to add (the trailing newline is required so the rule is its own line):
    ```
    # Thread-local WIP scratch (per docs/workflow/v1/thread-layout.md)
    docs/threads/**/.wip/
    ```

    Do NOT delete or reorder existing rules. Do NOT use `**/.wip/` (less precise — could accidentally ignore unrelated `.wip/` folders elsewhere); use the full `docs/threads/**/.wip/` form per THRD-03.
  </action>
  <verify>
    <automated>grep -qxF 'docs/threads/**/.wip/' .gitignore &amp;&amp; grep -qF 'temp/' .gitignore &amp;&amp; grep -qF '.library/' .gitignore &amp;&amp; grep -qF '.claude/' .gitignore &amp;&amp; grep -qF '.codex/' .gitignore &amp;&amp; grep -qF '.cursor/' .gitignore</automated>
  </verify>
  <done>
    `.gitignore` contains the literal line `docs/threads/**/.wip/` exactly once, on its own line, with the explanatory comment `# Thread-local WIP scratch (per docs/workflow/v1/thread-layout.md)` on the immediately preceding line. All existing ignore rules are preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add `JeisKappa-workflow` plugin entry to `.claude-plugin/marketplace.json`</name>
  <files>.claude-plugin/marketplace.json</files>
  <read_first>
    - .claude-plugin/marketplace.json (current contents — single plugin `JeisKappa-skills`)
    - .planning/phases/01-foundations/01-CONTEXT.md (Registration Baseline decision: second plugin, empty `skills`, preserve existing)
    - AGENTS.md (CLI display rule: `JeisKappa-workflow` renders as `JeisKappa Workflow` — dash-separated segments become the display title)
  </read_first>
  <action>
    Edit `.claude-plugin/marketplace.json` to add a SECOND plugin entry alongside the existing `JeisKappa-skills` entry. The existing `JeisKappa-skills` plugin (and its `skills` array of nine entries) MUST remain unchanged.

    Use the Edit tool — do NOT rewrite the whole file. The new plugin entry to append after `JeisKappa-skills` inside the `plugins` array:
    ```json
        {
          "name": "JeisKappa-workflow",
          "source": "./",
          "skills": []
        }
    ```

    Final file shape (for orientation — DO NOT just overwrite, use Edit to splice the new entry in):
    ```json
    {
      "plugins": [
        {
          "name": "JeisKappa-skills",
          "source": "./",
          "skills": [
            "./skills/afk-exploration",
            "./skills/brief-the-implementer",
            "./skills/consult-the-expert",
            "./skills/derive-spec",
            "./skills/discussion-loop",
            "./skills/meta-prompting",
            "./skills/report-to-the-owner",
            "./skills/review-decision-document",
            "./skills/the-librarian"
          ]
        },
        {
          "name": "JeisKappa-workflow",
          "source": "./",
          "skills": []
        }
      ]
    }
    ```

    Critical requirements:
    - The plugin name is exactly `JeisKappa-workflow` (kebab-case, capital `J` and `K` per the existing convention; the CLI display rule splits on `-` and uppercases each segment's first char, producing `JeisKappa Workflow`).
    - The `skills` array MUST be empty (`[]`). Subsequent phases will append entries as each V1 spine skill folder ships.
    - The comma between the two plugin objects MUST be valid JSON (no trailing comma after the second plugin).
    - Preserve two-space indentation matching the existing file.
  </action>
  <verify>
    <automated>node -e "const j = JSON.parse(require('fs').readFileSync('.claude-plugin/marketplace.json', 'utf8')); if (j.plugins.length !== 2) { console.error('expected 2 plugins, got', j.plugins.length); process.exit(1); } if (j.plugins[0].name !== 'JeisKappa-skills') { console.error('first plugin must remain JeisKappa-skills'); process.exit(1); } if (j.plugins[0].skills.length !== 9) { console.error('JeisKappa-skills must still have 9 skills, got', j.plugins[0].skills.length); process.exit(1); } const wf = j.plugins.find(p =&gt; p.name === 'JeisKappa-workflow'); if (!wf) { console.error('JeisKappa-workflow plugin missing'); process.exit(1); } if (!Array.isArray(wf.skills) || wf.skills.length !== 0) { console.error('JeisKappa-workflow.skills must be empty array'); process.exit(1); } if (wf.source !== './') { console.error('JeisKappa-workflow.source must be ./'); process.exit(1); } console.log('OK');"</automated>
  </verify>
  <done>
    `.claude-plugin/marketplace.json` parses as valid JSON, contains exactly two plugin entries, the first is the existing `JeisKappa-skills` entry with all nine skill paths preserved, the second is `JeisKappa-workflow` with `source: "./"` and `skills: []`. The verification node script prints `OK`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add V1 Workflow Conventions pointer section to `AGENTS.md`</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md (current full file — locate insertion point and confirm voice match)
    - .planning/phases/01-foundations/01-CONTEXT.md (Agent Discoverability decision: short pointer section, no rule duplication)
    - docs/workflow/v1/README.md (Plan 01's index — confirm path and title for accurate linking)
  </read_first>
  <action>
    Edit `AGENTS.md` to insert a new section titled `## V1 Workflow Conventions` between the existing `## Commits` section (which ends near line 66 of the current file) and the GSD-managed `<!-- GSD:project-start ... -->` marker (which begins on the immediately following line). This placement keeps the section inside the human-authored portion of `AGENTS.md` and OUTSIDE every `<!-- GSD:*-start ... -->` / `<!-- GSD:*-end -->` block (those are auto-managed and may be regenerated).

    Use the Edit tool. The exact section to insert (preserve a blank line above and below for spacing):

    ```markdown
    ## V1 Workflow Conventions

    This repo is the home of the Modular Agentic Workflow V1. The canonical reference for V1 workflow rules — thread layout, filename grammars, immutability — lives at `docs/workflow/v1/README.md`. Read it before writing or editing any workflow artifact under `docs/threads/<thread>/`.

    Three things follow from V1:

    1. **Thread storage** — All V1 workflow artifacts live under `docs/threads/<YYMMDDHHMMSSZ-slug>/` using the folder set defined in `docs/workflow/v1/thread-layout.md`. Nothing else writes there.
    2. **Filename grammar** — Every artifact filename uses the record or versioned grammar in `docs/workflow/v1/filename-grammar.md`, including a mandatory artifact-type suffix.
    3. **Immutability** — Emitted versioned and record artifacts are not edited; produce a new version or new record instead. See `docs/workflow/v1/immutability.md` for the ambiguous-reference resolution rule (ask the user — never silently pick "latest").

    Drafts under any thread's `.wip/` folder are gitignored and editable until the owning skill emits them.

    This section is a POINTER — it intentionally does NOT duplicate the rules. Edit the canonical docs under `docs/workflow/v1/` for any rule change; this section only changes if the V1 reference doc set itself moves or splits.
    ```

    Critical requirements:
    - Heading is exactly `## V1 Workflow Conventions` (level 2).
    - Section text MUST contain the literal string `docs/workflow/v1/README.md`.
    - Section text MUST contain the literal string `docs/workflow/v1/thread-layout.md`.
    - Section text MUST contain the literal string `docs/workflow/v1/filename-grammar.md`.
    - Section text MUST contain the literal string `docs/workflow/v1/immutability.md`.
    - Section does NOT duplicate any concrete rule (folder names, UTC pattern, version syntax, frontmatter forbidden list) — it only summarizes and links. The CONTEXT.md "no duplication" constraint is the binding one.
    - Section is OUTSIDE every `<!-- GSD:*-start ... -->` / `<!-- GSD:*-end -->` block. Verify by checking the line immediately after your insertion is `<!-- GSD:project-start source:PROJECT.md -->` (or whichever GSD marker currently starts the auto-managed section).

    Do NOT modify any other section of `AGENTS.md`. Do NOT touch the GSD-managed blocks. Do NOT edit `CLAUDE.md` directly (it is a symlink to `AGENTS.md` and updates automatically).
  </action>
  <verify>
    <automated>grep -qF '## V1 Workflow Conventions' AGENTS.md &amp;&amp; grep -qF 'docs/workflow/v1/README.md' AGENTS.md &amp;&amp; grep -qF 'docs/workflow/v1/thread-layout.md' AGENTS.md &amp;&amp; grep -qF 'docs/workflow/v1/filename-grammar.md' AGENTS.md &amp;&amp; grep -qF 'docs/workflow/v1/immutability.md' AGENTS.md &amp;&amp; awk '/^## V1 Workflow Conventions$/{found=NR} /GSD:project-start/{if(found &amp;&amp; NR &gt; found) print "OK"; else print "FAIL"; exit}' AGENTS.md | grep -qF 'OK' &amp;&amp; awk '/<!-- GSD:[a-z]+-start/{in_block=1} /<!-- GSD:[a-z]+-end/{in_block=0; next} /^## V1 Workflow Conventions$/{if(in_block){print "FAIL: heading inside GSD block"; exit 1}} END{print "OK"}' AGENTS.md | grep -qF 'OK'</automated>
  </verify>
  <done>
    `AGENTS.md` contains a level-2 section `## V1 Workflow Conventions` positioned between the existing `## Commits` section and the first `<!-- GSD:project-start ... -->` marker. The section text contains relative-path references to all four canonical docs (`README.md`, `thread-layout.md`, `filename-grammar.md`, `immutability.md`) and explicitly states it is a pointer (does not duplicate the rules). The section is OUTSIDE every `<!-- GSD:*-start ... -->` / `<!-- GSD:*-end -->` block. `CLAUDE.md` reflects the change automatically via the symlink.
  </done>
</task>

</tasks>

<verification>
- `.gitignore` contains `docs/threads/**/.wip/` as a literal line; all existing rules preserved.
- `.claude-plugin/marketplace.json` is valid JSON with exactly two plugin entries: `JeisKappa-skills` (unchanged, 9 skills) + `JeisKappa-workflow` (empty `skills` array, `source: "./"`).
- `AGENTS.md` contains `## V1 Workflow Conventions` section positioned between `## Commits` and the first GSD-managed block; section references all four canonical doc paths; section is outside every `<!-- GSD:*-start/-end -->` block.
- `CLAUDE.md` reflects the `AGENTS.md` change via its symlink (no separate edit needed).

Run all three verification commands; all three must pass.
</verification>

<success_criteria>
Phase 1's success criterion #2 (`.gitignore` recursive rule), #4 (registration baseline correct — `JeisKappa-workflow` plugin entry exists, `.vscode/settings.json` *ready to receive* per CLAUDE.md rules) are satisfied. THRD-03 (recursive `.wip/` ignore) is addressed by Task 1. DIST-04 (registration baseline: marketplace plugin entry + scopes file ready) is addressed by Task 2 (marketplace) and is consistent with the locked CONTEXT decision that `.vscode/settings.json` does NOT change in Phase 1 (its readiness is the existing alphabetical-sort convention documented in CLAUDE.md — no Phase 1 edit required for "readiness").

The full Phase 1 deliverable set is now on disk: four reference docs (Plans 01 + 02), one gitignore rule (this plan), one marketplace plugin entry (this plan), one AGENTS.md pointer section (this plan). Phase 2 can now begin authoring `capture-inbox`, `discussion`, and `seeded-discussion` against the codified contracts.
</success_criteria>

<output>
Create `.planning/phases/01-foundations/01-03-SUMMARY.md` when done. Record:
- Files edited: `.gitignore`, `.claude-plugin/marketplace.json`, `AGENTS.md`
- Requirements addressed: THRD-03, DIST-04
- Registration baseline state: `JeisKappa-workflow` plugin has empty `skills` array (Phase 2 will append).
- `.vscode/settings.json` was intentionally NOT touched (per CONTEXT.md — entries land when new skill folders ship in later phases; the existing alphabetical-sort convention is documented in CLAUDE.md so the file is already "ready to receive" per DIST-04).
- `CLAUDE.md` reflects the `AGENTS.md` change automatically via symlink.
</output>
