---
phase: 03-forward-spine-propose-and-spec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/propose-auto/SKILL.md
  - skills/propose-interactive/SKILL.md
  - .claude-plugin/marketplace.json
  - .vscode/settings.json
  - README.md
autonomous: true
requirements:
  - PROP-01
  - PROP-02
  - PROP-03

must_haves:
  truths:
    - "User can install propose-auto via `npx skills add Jei-sKappa/skills --skill propose-auto`"
    - "User can install propose-interactive via `npx skills add Jei-sKappa/skills --skill propose-interactive`"
    - "Both skills appear under the `JeisKappa-workflow` plugin grouping in `npx skills list`"
    - "Invoking propose-auto from a rough prompt produces a proposal artifact at `docs/threads/<thread>/proposals/<UTC>-<kebab-desc>-proposal.md`"
    - "Invoking propose-interactive walks the user through 4 suggested elements (intent / context / rough shape / open questions) one at a time"
    - "Both skill bodies suggest the 4-element structure but do not enforce a rigid template"
    - "Neither skill auto-commits the proposal artifact"
    - "propose-interactive carries the anti-sycophancy stance with all 4 verbatim marker phrases from discussion/SKILL.md"
  artifacts:
    - path: skills/propose-auto/SKILL.md
      provides: propose-auto skill body + frontmatter
      contains: "name: propose-auto"
    - path: skills/propose-interactive/SKILL.md
      provides: propose-interactive skill body + frontmatter
      contains: "name: propose-interactive"
    - path: .claude-plugin/marketplace.json
      provides: marketplace registration under JeisKappa-workflow
      contains: "./skills/propose-auto"
    - path: .vscode/settings.json
      provides: conventional commit scopes
      contains: "propose-auto"
    - path: README.md
      provides: human-readable index entries
      contains: "propose-auto"
  key_links:
    - from: skills/propose-auto/SKILL.md
      to: docs/workflow/v1/filename-grammar.md
      via: absolute-path citation in body
      pattern: "docs/workflow/v1/filename-grammar\\.md"
    - from: skills/propose-auto/SKILL.md
      to: docs/workflow/v1/thread-layout.md
      via: absolute-path citation in body
      pattern: "docs/workflow/v1/thread-layout\\.md"
    - from: skills/propose-interactive/SKILL.md
      to: docs/workflow/v1/filename-grammar.md
      via: absolute-path citation in body
      pattern: "docs/workflow/v1/filename-grammar\\.md"
    - from: .claude-plugin/marketplace.json
      to: skills/propose-auto
      via: JeisKappa-workflow.skills array entry
      pattern: "\\./skills/propose-auto"
    - from: .claude-plugin/marketplace.json
      to: skills/propose-interactive
      via: JeisKappa-workflow.skills array entry
      pattern: "\\./skills/propose-interactive"
---

<objective>
Ship the `propose-*` half of the V1 forward spine: `propose-auto` (pure generator from rough prompt) and `propose-interactive` (collaborative walk through 4 suggested elements). Both skills emit proposal artifacts under the active thread's `proposals/` folder using the Phase 1 record-form filename grammar with the `proposal` artifact-type token. Register both skills across the three registration files (marketplace.json under `JeisKappa-workflow`, `.vscode/settings.json` conventional-commit scopes, README.md "Available skills").

Purpose: After this plan, a V1 user can drive "rough idea → freeform proposal" entirely with V1 spine skills, matching the upstream half of the propose → spec → plan spine.

Output: Two new SKILL.md files under `skills/propose-auto/` and `skills/propose-interactive/`, plus the four registration touchpoints required for installability.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-forward-spine-propose-and-spec/03-CONTEXT.md

# Phase 1 canonical V1 workflow reference docs — every spine skill MUST cite these by absolute path
@docs/workflow/v1/README.md
@docs/workflow/v1/thread-layout.md
@docs/workflow/v1/filename-grammar.md
@docs/workflow/v1/immutability.md

# Phase 2 pattern source skills — propose-* must mirror this body shape (Workflow, scope drift, thread resolution)
@skills/discussion/SKILL.md
@skills/seeded-discussion/SKILL.md
@skills/capture-inbox/SKILL.md

# Reference for the "spec" concept and the directional split (do NOT touch this file)
@skills/derive-spec/SKILL.md

# Existing registration state (this plan extends, never rewrites prior entries)
@.claude-plugin/marketplace.json
@.vscode/settings.json
@README.md

# Repo conventions — every new skill body must comply
@AGENTS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

<interfaces>
<!-- The 4 anti-sycophancy marker phrases (verbatim) that propose-interactive MUST carry per CONTEXT decision -->
<!-- Source: skills/discussion/SKILL.md lines 13–26 (## Anti-Sycophancy Stance section header + 8 bullets) -->

From skills/discussion/SKILL.md (verbatim phrases — propose-interactive must contain ALL FOUR):

1. "make them feel good"   (line 15, in the rationale sentence)
2. "Disagree when you disagree" (line 19, bullet label)
3. "pushback as correctness" (line 23, bullet label fragment "Do not treat pushback as correctness")
4. "refuse to log"          (line 73, dissent-handling rule)

The canonical block to mirror is the entire `## Anti-Sycophancy Stance` section header
followed by the prefatory paragraph and 8 bullets. propose-interactive may rephrase the
prefatory sentence to reference "proposal authoring" instead of "discussion", but the four
verbatim marker phrases above MUST appear unchanged so a grep against the new file finds them.

From docs/workflow/v1/filename-grammar.md — record form (what propose-* emits):

```text
<YYMMDDHHMMSSZ>-<kebab-desc>-<artifact-type>.md
```

For `propose-*` skills, `<artifact-type>` is the literal token `proposal`.
Example: `260520120000Z-auth-flow-proposal.md`

From docs/workflow/v1/thread-layout.md — target folder:

```text
docs/threads/<YYMMDDHHMMSSZ-slug>/proposals/
```

The `proposals/` folder is created on-demand on the first proposal written.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author skills/propose-auto/SKILL.md</name>
  <files>skills/propose-auto/SKILL.md</files>

  <read_first>
    - .planning/phases/03-forward-spine-propose-and-spec/03-CONTEXT.md (binding decisions; the "Proposal artifact format" + "propose-auto vs propose-interactive" + "Skill body voice and structure" subsections drive every requirement below)
    - skills/discussion/SKILL.md (Phase 2 pattern source — mirror the body shape: top heading, Workflow numbered steps, scope-drift handling, thread resolution language, Phase 1 doc citations)
    - skills/seeded-discussion/SKILL.md (Phase 2 pattern source — secondary reference for tone, "## Resumption" not applicable here)
    - skills/capture-inbox/SKILL.md (Phase 2 pattern source for single-artifact-type writer with mandatory artifact-type suffix and on-demand folder creation)
    - docs/workflow/v1/filename-grammar.md (record form; `proposal` artifact-type token; UTC stamp grammar)
    - docs/workflow/v1/thread-layout.md (thread root resolution + `proposals/` folder + on-demand creation rule)
    - docs/workflow/v1/immutability.md (emitted artifacts immutable; ambiguous reference resolution by asking)
    - skills/consult-the-expert/SKILL.md (one-sentence frontmatter description style reference — "Use when…" pattern)
    - AGENTS.md (V1 Workflow Conventions section — citations to canonical docs)
  </read_first>

  <action>
Create `skills/propose-auto/SKILL.md` with valid YAML frontmatter followed by a markdown body that turns a rough prompt into a freeform proposal artifact under the active thread's `proposals/` folder. Follow these requirements verbatim — wording within each section is at executor discretion EXCEPT where a literal string is named.

REQUIRED FRONTMATTER FIELDS (exact values):
- `name: propose-auto`
- `description:` one sentence ending with a concrete "Use when…" trigger that explicitly identifies this as the AUTO (non-interactive) mode — see skills/discussion/SKILL.md frontmatter for the trigger pattern. The description must say "freeform" or "free-form" proposal somewhere.
- `metadata.author: https://github.com/Jei-sKappa`
- `metadata.version: 1.0.0`

REQUIRED BODY SECTIONS (top-level `##` headings — names are at executor discretion but each section must EXIST and cover the listed content):

1. Top heading `# Propose Auto` (or equivalent H1 derived from the skill name).
2. A `## Workflow` section with numbered steps covering, in order:
   - Resolve the active thread per `docs/workflow/v1/thread-layout.md`. Apply the standard D18-style thread resolution rule: if cwd sits inside a thread root, use it; if multiple roots exist and active is ambiguous, ASK per `docs/workflow/v1/immutability.md` ("Ambiguous Reference Resolution"); if no thread exists, ASK the user OR auto-create when context makes the slug obvious.
   - Derive the slug from the user's rough prompt (kebab-case description, short).
   - Capture the 12-character `YYMMDDHHMMSSZ` UTC stamp at write time per `docs/workflow/v1/filename-grammar.md`; stamp once and reuse — never re-derive.
   - Draft the proposal body using the SUGGESTED 4-element structure (intent / context / rough shape / open questions), explicitly framed as suggested-not-enforced.
   - Write the artifact to `docs/threads/<thread>/proposals/<UTC>-<kebab-desc>-proposal.md`. The `proposal` artifact-type suffix is MANDATORY per `docs/workflow/v1/filename-grammar.md`. The `proposals/` folder is created on-demand per `docs/workflow/v1/thread-layout.md`.
   - Confirm to the user with the artifact path. No closing remark, no preamble.
3. A `## Suggested Structure` (or equivalent) section that names the 4 elements (intent, context, rough shape, open questions) and EXPLICITLY states this structure is SUGGESTED — adapt as needed; not enforced. This is the PROP-03 obligation.
4. A `## Filename and Folder` (or equivalent) section pointing the reader to `docs/workflow/v1/filename-grammar.md` (record form, `proposal` token) and `docs/workflow/v1/thread-layout.md` (the `proposals/` folder + on-demand creation), citing both by absolute path on first invocation.
5. A `## Commit Policy` (or equivalent — may also be inlined in Workflow) section stating: this skill NEVER auto-commits. Any commit is the surrounding session's decision. Use the literal phrase "NEVER auto-commit" or "never auto-commit" (lower or upper case acceptable; "auto-commit" hyphenated as one token).
6. A `## Immutability` (or equivalent — may be a short bullet inside Workflow or Filename) section stating: emitted proposal artifacts are immutable per `docs/workflow/v1/immutability.md`. To revise an emitted proposal, write a new artifact (this is record-form, so a new record with a different slug or a follow-up artifact).
7. An example block somewhere in the body showing a realistic filename of the form `260521120000Z-<descriptive-slug>-proposal.md` (or similar with a real-looking 12-character UTC stamp and the `-proposal.md` suffix) — per CONTEXT specifics: examples should be realistic.

REQUIRED ABSOLUTE-PATH CITATIONS in the body (each path must appear at least once as a literal token):
- `docs/workflow/v1/thread-layout.md`
- `docs/workflow/v1/filename-grammar.md`
- `docs/workflow/v1/immutability.md`

PROHIBITED CONTENT:
- Do NOT include a `## Anti-Sycophancy Stance` section — this is a pure generator (auto) skill; no decision-influencing interaction. (The interactive sibling owns anti-sycophancy.)
- Do NOT include lineage / source-relation frontmatter (`Supersedes:`, `Alternative to:`, `Forked from:`) — forbidden by `docs/workflow/v1/immutability.md`.
- Do NOT instruct the agent to auto-commit, to add YAML frontmatter to the proposal body, or to write the proposal to any folder other than `proposals/`.
- Do NOT mention `derive-spec` or any reverse-engineering concept — propose is forward-only.

VOICE: Dense, declarative, action-first. Match the voice of `skills/discussion/SKILL.md`. No preamble, no closing pleasantries inside the skill body itself.
  </action>

  <acceptance_criteria>
Run from the repo root. Every check must pass.

1. File exists:
   `test -f skills/propose-auto/SKILL.md`

2. Frontmatter has all required fields with exact values:
   `grep -c "^name: propose-auto$" skills/propose-auto/SKILL.md` returns `1`
   `grep -c "^  author: https://github.com/Jei-sKappa$" skills/propose-auto/SKILL.md` returns `1`
   `grep -c "^  version: 1.0.0$" skills/propose-auto/SKILL.md` returns `1`
   `grep -ci "use when" skills/propose-auto/SKILL.md` returns `>= 1`

3. Body cites all three Phase 1 canonical docs by absolute path (each at least once):
   `grep -c "docs/workflow/v1/thread-layout.md" skills/propose-auto/SKILL.md` returns `>= 1`
   `grep -c "docs/workflow/v1/filename-grammar.md" skills/propose-auto/SKILL.md` returns `>= 1`
   `grep -c "docs/workflow/v1/immutability.md" skills/propose-auto/SKILL.md` returns `>= 1`

4. Body documents the `proposal` artifact-type token explicitly:
   `grep -cw "proposal" skills/propose-auto/SKILL.md` returns `>= 3` (token appears in workflow text, filename example, and at least one other reference)

5. Body has a `## Workflow` section:
   `grep -c "^## Workflow" skills/propose-auto/SKILL.md` returns `>= 1`

6. PROP-03 suggested-not-enforced language is present:
   `grep -iE "suggested|suggests" skills/propose-auto/SKILL.md | grep -iE "not enforce|adapt as needed|not a template|not mandatory|free.?form" | wc -l` returns `>= 1`
   AND the 4 suggested element names all appear: `grep -ciE "intent" skills/propose-auto/SKILL.md` returns `>= 1`; `grep -ci "context" skills/propose-auto/SKILL.md` returns `>= 1`; `grep -ciE "rough shape|shape" skills/propose-auto/SKILL.md` returns `>= 1`; `grep -ciE "open questions" skills/propose-auto/SKILL.md` returns `>= 1`

7. Commit policy marker present:
   `grep -iE "(never|do not|don't).{0,20}auto.commit|NEVER auto-commit" skills/propose-auto/SKILL.md | wc -l` returns `>= 1`

8. Realistic filename example present (12-char UTC stamp followed by kebab desc and `-proposal.md`):
   `grep -oE "[0-9]{12}Z-[a-z0-9-]+-proposal\.md" skills/propose-auto/SKILL.md | wc -l` returns `>= 1`

9. Folder routing to `proposals/` documented:
   `grep -c "proposals/" skills/propose-auto/SKILL.md` returns `>= 1`

10. PROHIBITED content NOT present:
    `grep -c "derive-spec" skills/propose-auto/SKILL.md` returns `0`
    `grep -iE "Supersedes:|Alternative to:|Forked from:" skills/propose-auto/SKILL.md | wc -l` returns `0`
    `grep -i "Anti-Sycophancy" skills/propose-auto/SKILL.md | wc -l` returns `0` (this is auto, not interactive)

11. UTC stamp pattern documented (the literal pattern token):
    `grep -c "YYMMDDHHMMSSZ" skills/propose-auto/SKILL.md` returns `>= 1`

<automated>
bash -c '
set -e
f=skills/propose-auto/SKILL.md
test -f "$f"
[ "$(grep -c "^name: propose-auto$" "$f")" -eq 1 ]
[ "$(grep -c "^  author: https://github.com/Jei-sKappa$" "$f")" -eq 1 ]
[ "$(grep -c "^  version: 1.0.0$" "$f")" -eq 1 ]
[ "$(grep -ci "use when" "$f")" -ge 1 ]
[ "$(grep -c "docs/workflow/v1/thread-layout.md" "$f")" -ge 1 ]
[ "$(grep -c "docs/workflow/v1/filename-grammar.md" "$f")" -ge 1 ]
[ "$(grep -c "docs/workflow/v1/immutability.md" "$f")" -ge 1 ]
[ "$(grep -cw "proposal" "$f")" -ge 3 ]
[ "$(grep -c "^## Workflow" "$f")" -ge 1 ]
[ "$(grep -ciE "intent" "$f")" -ge 1 ]
[ "$(grep -ci "context" "$f")" -ge 1 ]
[ "$(grep -ciE "rough shape|shape" "$f")" -ge 1 ]
[ "$(grep -ciE "open questions" "$f")" -ge 1 ]
[ "$(grep -iE "(never|do not|don'"'"'t).{0,20}auto.commit|NEVER auto-commit" "$f" | wc -l | tr -d " ")" -ge 1 ]
[ "$(grep -oE "[0-9]{12}Z-[a-z0-9-]+-proposal\.md" "$f" | wc -l | tr -d " ")" -ge 1 ]
[ "$(grep -c "proposals/" "$f")" -ge 1 ]
[ "$(grep -c "derive-spec" "$f")" -eq 0 ]
[ "$(grep -iE "Supersedes:|Alternative to:|Forked from:" "$f" | wc -l | tr -d " ")" -eq 0 ]
[ "$(grep -i "Anti-Sycophancy" "$f" | wc -l | tr -d " ")" -eq 0 ]
[ "$(grep -c "YYMMDDHHMMSSZ" "$f")" -ge 1 ]
echo "propose-auto: PASS"
'
</automated>
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'cd /Users/jacopo/Developer/projects/personal/tools/skills && set -e; f=skills/propose-auto/SKILL.md; test -f "$f" && [ "$(grep -c "^name: propose-auto$" "$f")" -eq 1 ] && [ "$(grep -c "docs/workflow/v1/thread-layout.md" "$f")" -ge 1 ] && [ "$(grep -c "docs/workflow/v1/filename-grammar.md" "$f")" -ge 1 ] && [ "$(grep -c "docs/workflow/v1/immutability.md" "$f")" -ge 1 ] && [ "$(grep -cw "proposal" "$f")" -ge 3 ] && [ "$(grep -iE "(never|do not|don.t).{0,20}auto.commit" "$f" | wc -l | tr -d " ")" -ge 1 ] && [ "$(grep -oE "[0-9]{12}Z-[a-z0-9-]+-proposal\.md" "$f" | wc -l | tr -d " ")" -ge 1 ] && [ "$(grep -i "Anti-Sycophancy" "$f" | wc -l | tr -d " ")" -eq 0 ] && echo PASS'</automated>
  </verify>

  <done>
skills/propose-auto/SKILL.md exists with valid YAML frontmatter (`name: propose-auto`, author Jei-sKappa, version 1.0.0, "Use when…" description naming free-form / freeform proposal), a `## Workflow` section walking thread resolution → slug → UTC stamp → draft → write to `proposals/<UTC>-<kebab-desc>-proposal.md` → confirm, a suggested-not-enforced 4-element structure (intent / context / rough shape / open questions), explicit "never auto-commit" language, citations to all three Phase 1 canonical docs by absolute path, a realistic 12-char-UTC-stamped filename example with `-proposal.md` suffix, and NO anti-sycophancy section, NO lineage frontmatter mentions, NO references to derive-spec. All acceptance grep checks pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author skills/propose-interactive/SKILL.md</name>
  <files>skills/propose-interactive/SKILL.md</files>

  <read_first>
    - .planning/phases/03-forward-spine-propose-and-spec/03-CONTEXT.md (binding decisions — "propose-auto vs propose-interactive" + "Skill body voice and structure" subsections drive the interactive walk + anti-sycophancy requirement)
    - skills/discussion/SKILL.md (canonical Anti-Sycophancy Stance source — copy the section structure and the 4 verbatim marker phrases; mirror the Workflow shape and scope-drift section)
    - skills/seeded-discussion/SKILL.md (Phase 2 pattern source — interactive walk-one-point-at-a-time pattern, but propose-interactive walks ELEMENTS not predetermined points)
    - skills/capture-inbox/SKILL.md (Phase 2 pattern source for interactive-vs-autonomous trigger language; propose-interactive is interactive-only but capture-inbox shows the voice for "ask the user")
    - docs/workflow/v1/filename-grammar.md (record form; `proposal` artifact-type token)
    - docs/workflow/v1/thread-layout.md (thread root resolution + `proposals/` folder)
    - docs/workflow/v1/immutability.md (emitted artifacts immutable; ambiguous reference resolution; D93 "interactive variants do NOT auto-write a decision log unless durable trade-offs emerge")
  </read_first>

  <action>
Create `skills/propose-interactive/SKILL.md` with valid YAML frontmatter followed by a markdown body that walks the user through the 4 suggested proposal elements (intent / context / rough shape / open questions) one at a time, accepts freeform answers, assembles the proposal body, writes the artifact to the active thread's `proposals/` folder using the Phase 1 record-form grammar, and carries the V1 anti-sycophancy stance verbatim from `skills/discussion/SKILL.md`.

REQUIRED FRONTMATTER FIELDS (exact values):
- `name: propose-interactive`
- `description:` one sentence ending with a concrete "Use when…" trigger that EXPLICITLY identifies this as the INTERACTIVE mode (the user walks through the 4 elements collaboratively).
- `metadata.author: https://github.com/Jei-sKappa`
- `metadata.version: 1.0.0`

REQUIRED BODY SECTIONS:

1. Top heading `# Propose Interactive` (or equivalent H1 derived from the skill name).

2. A `## Anti-Sycophancy Stance` section (literal section header — must be present verbatim) carrying ALL FOUR of these marker phrases as substrings of the section text:
   - The phrase "make them feel good" (verbatim, lowercase, exactly as in skills/discussion/SKILL.md line 15)
   - The phrase "Disagree when you disagree" (verbatim, capital D start, exactly as in skills/discussion/SKILL.md line 19 bullet label)
   - The phrase "pushback as correctness" (verbatim — the bullet "Do not treat pushback as correctness" from line 23 satisfies this)
   - The phrase "refuse to log" (verbatim — appears in either a bullet "Refuse to log a decision…" or the dissent-handling rule)

   The section may be paraphrased AROUND the four marker phrases (the prefatory paragraph may reference "proposal authoring" instead of "discussion"), but each marker must appear unmodified so a grep finds it. The full 8-bullet stance from discussion/SKILL.md is the canonical structure — reproduce it adapted for proposal authoring (bullets MAY be rephrased provided all 4 marker phrases survive verbatim; the spirit of the 8 bullets must remain).

3. A `## Workflow` section with numbered steps covering, in order:
   - Resolve the active thread per `docs/workflow/v1/thread-layout.md`. Apply the same D18 thread resolution rule as the discussion skills (cwd inside thread; multiple ambiguous → ASK per immutability.md; none → ask or auto-create when obvious).
   - Confirm or derive the slug with the user (the skill MAY ask the user for a slug, or propose one for confirmation — at executor's discretion; the action MUST involve the user).
   - Walk the 4 suggested elements one at a time, accepting freeform answers per element:
     1. **Intent** — what this proposal is trying to do.
     2. **Context** — why it's being raised now / what came before.
     3. **Rough shape** — early sketch of what the change might look like.
     4. **Open questions** — what's unresolved or worth flagging upfront.
   - Frame the 4-element structure explicitly as SUGGESTED, not enforced — the user may add, drop, reorder, or reshape elements (PROP-03 obligation).
   - Capture the 12-character `YYMMDDHHMMSSZ` UTC stamp at write time per `docs/workflow/v1/filename-grammar.md`.
   - Assemble the body, then write to `docs/threads/<thread>/proposals/<UTC>-<kebab-desc>-proposal.md`. The `proposal` artifact-type suffix is MANDATORY. The `proposals/` folder is created on-demand.
   - Confirm to the user with the artifact path. No closing remark.

4. A `## Decision Log` (or equivalent name — at executor's discretion, but the section MUST exist) section that captures the D93 rule: this skill does NOT auto-write a separate decision log; it ONLY writes one if durable trade-offs or rejected alternatives emerge during authoring that cannot be captured in the proposal body itself. When such a log is warranted, write it to `docs/threads/<thread>/discussions/<UTC>-<kebab-desc>-decision-log.md` per `docs/workflow/v1/filename-grammar.md` (record form, `decision-log` artifact-type token). Reference the source decision-log skill set (`discussion` / `seeded-discussion`) for the decision-log shape.

5. A `## Scope Drift` (or equivalent) section instructing the agent to propose parking off-topic branches as Inbox items via `capture-inbox`, splitting into a separate proposal or discussion thread when warranted, or deferring — and to ASK the user rather than silently following the branch. This mirrors the Scope Drift sections in `skills/discussion/SKILL.md` and `skills/seeded-discussion/SKILL.md`.

6. A `## Filename and Folder` (or equivalent) section pointing at `docs/workflow/v1/filename-grammar.md` (record form, `proposal` token) and `docs/workflow/v1/thread-layout.md` (the `proposals/` folder + on-demand creation), citing both by absolute path on first invocation.

7. A `## Commit Policy` (or equivalent — may be a short bullet within Workflow) section stating: this skill NEVER auto-commits the proposal artifact. Any commit is the surrounding session's decision. Use the literal phrase "never auto-commit" (any case).

8. A `## Immutability` (or equivalent — may be a short bullet) section stating: emitted proposal artifacts are immutable per `docs/workflow/v1/immutability.md`; revisions emit a new artifact.

9. A realistic example filename appearing somewhere in the body, of the form `260521120000Z-<descriptive-slug>-proposal.md`.

REQUIRED ABSOLUTE-PATH CITATIONS (each at least once):
- `docs/workflow/v1/thread-layout.md`
- `docs/workflow/v1/filename-grammar.md`
- `docs/workflow/v1/immutability.md`

PROHIBITED CONTENT:
- Do NOT include lineage / source-relation frontmatter discussion (`Supersedes:`, `Alternative to:`, `Forked from:`) in any positive sense.
- Do NOT instruct the agent to auto-commit or to auto-write a decision log (D93 forbids the latter as a default).
- Do NOT mention `derive-spec` or reverse-engineering.

VOICE: Match `skills/discussion/SKILL.md`. Dense, declarative, action-first. The interactive nature means the body addresses the agent driving the user-facing conversation — same as discussion/SKILL.md does.
  </action>

  <acceptance_criteria>
Run from the repo root. Every check must pass.

1. File exists:
   `test -f skills/propose-interactive/SKILL.md`

2. Frontmatter:
   `grep -c "^name: propose-interactive$" skills/propose-interactive/SKILL.md` returns `1`
   `grep -c "^  author: https://github.com/Jei-sKappa$" skills/propose-interactive/SKILL.md` returns `1`
   `grep -c "^  version: 1.0.0$" skills/propose-interactive/SKILL.md` returns `1`
   `grep -ci "use when" skills/propose-interactive/SKILL.md` returns `>= 1`

3. Anti-Sycophancy section heading present:
   `grep -c "^## Anti-Sycophancy Stance" skills/propose-interactive/SKILL.md` returns `>= 1`

4. All FOUR anti-sycophancy marker phrases present VERBATIM (case-sensitive, exact substrings):
   `grep -c "make them feel good" skills/propose-interactive/SKILL.md` returns `>= 1`
   `grep -c "Disagree when you disagree" skills/propose-interactive/SKILL.md` returns `>= 1`
   `grep -c "pushback as correctness" skills/propose-interactive/SKILL.md` returns `>= 1`
   `grep -c "refuse to log" skills/propose-interactive/SKILL.md` returns `>= 1`

5. Three Phase 1 canonical docs cited by absolute path:
   `grep -c "docs/workflow/v1/thread-layout.md" skills/propose-interactive/SKILL.md` returns `>= 1`
   `grep -c "docs/workflow/v1/filename-grammar.md" skills/propose-interactive/SKILL.md` returns `>= 1`
   `grep -c "docs/workflow/v1/immutability.md" skills/propose-interactive/SKILL.md` returns `>= 1`

6. Workflow section present:
   `grep -c "^## Workflow" skills/propose-interactive/SKILL.md` returns `>= 1`

7. The 4 suggested elements all named in the body:
   `grep -ciE "\\bintent\\b" skills/propose-interactive/SKILL.md` returns `>= 1`
   `grep -ciE "\\bcontext\\b" skills/propose-interactive/SKILL.md` returns `>= 1`
   `grep -ciE "rough shape" skills/propose-interactive/SKILL.md` returns `>= 1`
   `grep -ciE "open questions" skills/propose-interactive/SKILL.md` returns `>= 1`

8. D93 no-auto-decision-log rule present:
   `grep -iE "(do not|don't|does not|doesn't|never).{0,40}decision.log|only.{0,20}if.{0,30}(durable|trade.off|rejected)" skills/propose-interactive/SKILL.md | wc -l` returns `>= 1`

9. PROP-03 suggested-not-enforced language present:
   `grep -iE "suggested" skills/propose-interactive/SKILL.md | wc -l` returns `>= 1`
   AND a phrase like "not enforce", "adapt", "free-?form", "not mandatory", or "not a template" appears:
   `grep -iE "not enforce|adapt|free.?form|not mandatory|not a template" skills/propose-interactive/SKILL.md | wc -l` returns `>= 1`

10. Commit policy marker present:
    `grep -iE "(never|do not|don't).{0,20}auto.commit" skills/propose-interactive/SKILL.md | wc -l` returns `>= 1`

11. Scope drift section present (mirrors discussion sibling):
    `grep -ciE "^## (Scope Drift|Off.topic|Branches)" skills/propose-interactive/SKILL.md` returns `>= 1`
    AND references capture-inbox:
    `grep -c "capture-inbox" skills/propose-interactive/SKILL.md` returns `>= 1`

12. Realistic filename example present:
    `grep -oE "[0-9]{12}Z-[a-z0-9-]+-proposal\.md" skills/propose-interactive/SKILL.md | wc -l` returns `>= 1`

13. Folder routing to `proposals/`:
    `grep -c "proposals/" skills/propose-interactive/SKILL.md` returns `>= 1`

14. PROHIBITED content not present:
    `grep -c "derive-spec" skills/propose-interactive/SKILL.md` returns `0`
    `grep -iE "Supersedes:|Alternative to:|Forked from:" skills/propose-interactive/SKILL.md | wc -l` returns `0`

15. UTC stamp pattern documented:
    `grep -c "YYMMDDHHMMSSZ" skills/propose-interactive/SKILL.md` returns `>= 1`

<automated>
bash -c '
set -e
f=skills/propose-interactive/SKILL.md
test -f "$f"
[ "$(grep -c "^name: propose-interactive$" "$f")" -eq 1 ]
[ "$(grep -c "^  author: https://github.com/Jei-sKappa$" "$f")" -eq 1 ]
[ "$(grep -c "^  version: 1.0.0$" "$f")" -eq 1 ]
[ "$(grep -ci "use when" "$f")" -ge 1 ]
[ "$(grep -c "^## Anti-Sycophancy Stance" "$f")" -ge 1 ]
[ "$(grep -c "make them feel good" "$f")" -ge 1 ]
[ "$(grep -c "Disagree when you disagree" "$f")" -ge 1 ]
[ "$(grep -c "pushback as correctness" "$f")" -ge 1 ]
[ "$(grep -c "refuse to log" "$f")" -ge 1 ]
[ "$(grep -c "docs/workflow/v1/thread-layout.md" "$f")" -ge 1 ]
[ "$(grep -c "docs/workflow/v1/filename-grammar.md" "$f")" -ge 1 ]
[ "$(grep -c "docs/workflow/v1/immutability.md" "$f")" -ge 1 ]
[ "$(grep -c "^## Workflow" "$f")" -ge 1 ]
[ "$(grep -ciE "\\bintent\\b" "$f")" -ge 1 ]
[ "$(grep -ciE "\\bcontext\\b" "$f")" -ge 1 ]
[ "$(grep -ciE "rough shape" "$f")" -ge 1 ]
[ "$(grep -ciE "open questions" "$f")" -ge 1 ]
[ "$(grep -iE "suggested" "$f" | wc -l | tr -d " ")" -ge 1 ]
[ "$(grep -iE "(never|do not|don'"'"'t).{0,20}auto.commit" "$f" | wc -l | tr -d " ")" -ge 1 ]
[ "$(grep -c "capture-inbox" "$f")" -ge 1 ]
[ "$(grep -oE "[0-9]{12}Z-[a-z0-9-]+-proposal\.md" "$f" | wc -l | tr -d " ")" -ge 1 ]
[ "$(grep -c "proposals/" "$f")" -ge 1 ]
[ "$(grep -c "derive-spec" "$f")" -eq 0 ]
[ "$(grep -iE "Supersedes:|Alternative to:|Forked from:" "$f" | wc -l | tr -d " ")" -eq 0 ]
[ "$(grep -c "YYMMDDHHMMSSZ" "$f")" -ge 1 ]
echo "propose-interactive: PASS"
'
</automated>
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'cd /Users/jacopo/Developer/projects/personal/tools/skills && set -e; f=skills/propose-interactive/SKILL.md; test -f "$f" && [ "$(grep -c "^name: propose-interactive$" "$f")" -eq 1 ] && [ "$(grep -c "^## Anti-Sycophancy Stance" "$f")" -ge 1 ] && [ "$(grep -c "make them feel good" "$f")" -ge 1 ] && [ "$(grep -c "Disagree when you disagree" "$f")" -ge 1 ] && [ "$(grep -c "pushback as correctness" "$f")" -ge 1 ] && [ "$(grep -c "refuse to log" "$f")" -ge 1 ] && [ "$(grep -c "docs/workflow/v1/thread-layout.md" "$f")" -ge 1 ] && [ "$(grep -c "docs/workflow/v1/filename-grammar.md" "$f")" -ge 1 ] && [ "$(grep -c "docs/workflow/v1/immutability.md" "$f")" -ge 1 ] && [ "$(grep -ciE "rough shape" "$f")" -ge 1 ] && [ "$(grep -oE "[0-9]{12}Z-[a-z0-9-]+-proposal\.md" "$f" | wc -l | tr -d " ")" -ge 1 ] && echo PASS'</automated>
  </verify>

  <done>
skills/propose-interactive/SKILL.md exists with valid YAML frontmatter (`name: propose-interactive`, author Jei-sKappa, version 1.0.0, "Use when…" description naming interactive mode), a `## Anti-Sycophancy Stance` section carrying all 4 verbatim marker phrases ("make them feel good", "Disagree when you disagree", "pushback as correctness", "refuse to log"), a `## Workflow` section walking thread resolution → slug → 4-element interactive walk (intent / context / rough shape / open questions, framed as suggested-not-enforced) → UTC stamp → write to `proposals/<UTC>-<kebab-desc>-proposal.md` → confirm, a section codifying D93 (no auto decision log unless durable trade-offs / rejected alternatives emerge), a Scope Drift section referencing capture-inbox, "never auto-commit" language, citations to all three Phase 1 canonical docs by absolute path, a realistic UTC-stamped `-proposal.md` filename example, and NO mentions of derive-spec or lineage frontmatter. All acceptance grep checks pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register propose-auto + propose-interactive in marketplace.json, .vscode/settings.json, and README.md</name>
  <files>.claude-plugin/marketplace.json, .vscode/settings.json, README.md</files>

  <read_first>
    - .claude-plugin/marketplace.json (current state — `JeisKappa-workflow.skills` array has 3 entries: capture-inbox, discussion, seeded-discussion)
    - .vscode/settings.json (current state — 11 entries, alphabetically sorted)
    - README.md (current state — 11 "Available skills" entries; preserve existing entries and the trailing "## Retired skills" subsection)
    - AGENTS.md (the "When adding a new skill" checklist + the 4-touchpoint rule)
    - skills/discussion/README.md description pattern (the README entry for `discussion` is the canonical model — single sentence trigger, install snippet on next line in a `sh` fenced block)
    - skills/propose-auto/SKILL.md (just authored — use its `description:` field as the README entry source-of-truth)
    - skills/propose-interactive/SKILL.md (just authored — use its `description:` field as the README entry source-of-truth)
  </read_first>

  <action>
Update the three registration files to add `propose-auto` and `propose-interactive` per the four-touchpoint rule (`AGENTS.md` checklist). Preserve all existing entries and ordering. The marketplace and scopes are alphabetically sorted within their respective sections.

A) `.claude-plugin/marketplace.json`:
   - In the `JeisKappa-workflow` plugin's `skills` array, ADD `"./skills/propose-auto"` and `"./skills/propose-interactive"` in alphabetical order relative to the existing 3 entries.
   - The final array order (alphabetical) is:
     1. `./skills/capture-inbox`
     2. `./skills/discussion`
     3. `./skills/propose-auto`
     4. `./skills/propose-interactive`
     5. `./skills/seeded-discussion`
   - Do NOT modify the `JeisKappa-skills` plugin or its 8 entries.
   - Preserve JSON formatting (2-space indent, trailing newline, no trailing commas).

B) `.vscode/settings.json`:
   - In the `conventionalCommits.scopes` array, ADD `"propose-auto"` and `"propose-interactive"` and re-sort the array alphabetically. Final array (13 entries — 11 existing + 2 new):
     1. `afk-exploration`
     2. `brief-the-implementer`
     3. `capture-inbox`
     4. `consult-the-expert`
     5. `derive-spec`
     6. `discussion`
     7. `meta-prompting`
     8. `propose-auto`
     9. `propose-interactive`
     10. `report-to-the-owner`
     11. `review-decision-document`
     12. `seeded-discussion`
     13. `the-librarian`
   - Preserve top-level keys `conventionalCommits.gitmoji` and `conventionalCommits.promptFooter` as-is.
   - Preserve JSON formatting (2-space indent, trailing newline, no trailing commas).

C) `README.md`:
   - Under "## Available skills", APPEND two new entries — one for `propose-auto` and one for `propose-interactive` — following the EXACT structural pattern of the existing entries (header link, description paragraph drawn from the skill's `description:` frontmatter field, install snippet in a fenced ```sh block):

     ```markdown
     ### [`propose-auto`](./skills/propose-auto/SKILL.md)

     <one-paragraph description; pull from the `description:` field of skills/propose-auto/SKILL.md verbatim or near-verbatim; may expand the trigger into a full sentence ending with "Useful when…" matching the voice of the existing 11 entries>

     ```sh
     npx skills add Jei-sKappa/skills --skill propose-auto
     ```

     ### [`propose-interactive`](./skills/propose-interactive/SKILL.md)

     <same pattern for propose-interactive — paragraph derived from its description: field>

     ```sh
     npx skills add Jei-sKappa/skills --skill propose-interactive
     ```
     ```

   - Placement: APPEND these two entries AFTER the last existing "Available skills" entry (which is currently `seeded-discussion`) and BEFORE the `## Retired skills` subsection. Insert order: propose-auto first, then propose-interactive.
   - Do NOT modify any existing skill entries.
   - Do NOT modify the "## Retired skills" subsection.
   - Do NOT introduce a "Forward vs reverse" subsection or any reference to `derive-spec` here — that note lands in Plan 03-02 (which owns the spec entries and the SPEC-04 clarifier).
  </action>

  <acceptance_criteria>
Run from the repo root. Every check must pass.

1. marketplace.json — both propose-* entries are present in JeisKappa-workflow:
   `jq -r '.plugins[] | select(.name == "JeisKappa-workflow") | .skills[]' .claude-plugin/marketplace.json | grep -c '^\./skills/propose-auto$'` returns `1`
   `jq -r '.plugins[] | select(.name == "JeisKappa-workflow") | .skills[]' .claude-plugin/marketplace.json | grep -c '^\./skills/propose-interactive$'` returns `1`

2. marketplace.json — JeisKappa-workflow.skills has exactly 5 entries (3 existing + 2 new):
   `jq -r '.plugins[] | select(.name == "JeisKappa-workflow") | .skills | length' .claude-plugin/marketplace.json` returns `5`

3. marketplace.json — alphabetical order of JeisKappa-workflow.skills:
   `jq -r '.plugins[] | select(.name == "JeisKappa-workflow") | .skills[]' .claude-plugin/marketplace.json | diff - <(jq -r '.plugins[] | select(.name == "JeisKappa-workflow") | .skills[]' .claude-plugin/marketplace.json | sort)` produces no output (exit 0)

4. marketplace.json — JeisKappa-skills is unchanged at 8 entries:
   `jq -r '.plugins[] | select(.name == "JeisKappa-skills") | .skills | length' .claude-plugin/marketplace.json` returns `8`

5. marketplace.json — valid JSON:
   `jq -e . .claude-plugin/marketplace.json > /dev/null` exit code is `0`

6. settings.json — both new scopes present:
   `jq -r '."conventionalCommits.scopes"[]' .vscode/settings.json | grep -c '^propose-auto$'` returns `1`
   `jq -r '."conventionalCommits.scopes"[]' .vscode/settings.json | grep -c '^propose-interactive$'` returns `1`

7. settings.json — scopes array length is 13:
   `jq -r '."conventionalCommits.scopes" | length' .vscode/settings.json` returns `13`

8. settings.json — scopes array alphabetically sorted:
   `jq -r '."conventionalCommits.scopes"[]' .vscode/settings.json | diff - <(jq -r '."conventionalCommits.scopes"[]' .vscode/settings.json | sort)` produces no output (exit 0)

9. settings.json — valid JSON:
   `jq -e . .vscode/settings.json > /dev/null` exit code is `0`

10. README.md — both new entries present with proper header and install snippet:
    `grep -c '^### \[\`propose-auto\`\](./skills/propose-auto/SKILL.md)$' README.md` returns `1`
    `grep -c '^### \[\`propose-interactive\`\](./skills/propose-interactive/SKILL.md)$' README.md` returns `1`
    `grep -c '^npx skills add Jei-sKappa/skills --skill propose-auto$' README.md` returns `1`
    `grep -c '^npx skills add Jei-sKappa/skills --skill propose-interactive$' README.md` returns `1`

11. README.md — propose-* entries appear BEFORE the Retired skills subsection:
    `awk '/^### \[`propose-auto`\]/ {p=NR}; /^## Retired skills/ {r=NR}; END {exit (p>0 && r>0 && p<r ? 0 : 1)}' README.md` exit code is `0`
    `awk '/^### \[`propose-interactive`\]/ {p=NR}; /^## Retired skills/ {r=NR}; END {exit (p>0 && r>0 && p<r ? 0 : 1)}' README.md` exit code is `0`

12. README.md — the "## Retired skills" subsection and its discussion-loop bullet are still present:
    `grep -c '^## Retired skills$' README.md` returns `1`
    `grep -c "discussion-loop" README.md` returns `>= 1`

13. README.md — the original 11 "Available skills" entries are all still present (spot check via headers count >= 13: 11 prior + 2 new):
    `grep -cE "^### \[\`[a-z0-9-]+\`\]" README.md` returns `>= 13`

<automated>
bash -c '
set -e
mp=.claude-plugin/marketplace.json
vs=.vscode/settings.json
rm=README.md
jq -e . "$mp" > /dev/null
jq -e . "$vs" > /dev/null
[ "$(jq -r ".plugins[] | select(.name == \"JeisKappa-workflow\") | .skills[]" "$mp" | grep -c "^\./skills/propose-auto$")" -eq 1 ]
[ "$(jq -r ".plugins[] | select(.name == \"JeisKappa-workflow\") | .skills[]" "$mp" | grep -c "^\./skills/propose-interactive$")" -eq 1 ]
[ "$(jq -r ".plugins[] | select(.name == \"JeisKappa-workflow\") | .skills | length" "$mp")" -eq 5 ]
[ "$(jq -r ".plugins[] | select(.name == \"JeisKappa-skills\") | .skills | length" "$mp")" -eq 8 ]
diff <(jq -r ".plugins[] | select(.name == \"JeisKappa-workflow\") | .skills[]" "$mp") <(jq -r ".plugins[] | select(.name == \"JeisKappa-workflow\") | .skills[]" "$mp" | sort) > /dev/null
[ "$(jq -r ".\"conventionalCommits.scopes\"[]" "$vs" | grep -c "^propose-auto$")" -eq 1 ]
[ "$(jq -r ".\"conventionalCommits.scopes\"[]" "$vs" | grep -c "^propose-interactive$")" -eq 1 ]
[ "$(jq -r ".\"conventionalCommits.scopes\" | length" "$vs")" -eq 13 ]
diff <(jq -r ".\"conventionalCommits.scopes\"[]" "$vs") <(jq -r ".\"conventionalCommits.scopes\"[]" "$vs" | sort) > /dev/null
[ "$(grep -c "^### \[\`propose-auto\`\](./skills/propose-auto/SKILL.md)$" "$rm")" -eq 1 ]
[ "$(grep -c "^### \[\`propose-interactive\`\](./skills/propose-interactive/SKILL.md)$" "$rm")" -eq 1 ]
[ "$(grep -c "^npx skills add Jei-sKappa/skills --skill propose-auto$" "$rm")" -eq 1 ]
[ "$(grep -c "^npx skills add Jei-sKappa/skills --skill propose-interactive$" "$rm")" -eq 1 ]
[ "$(grep -c "^## Retired skills$" "$rm")" -eq 1 ]
[ "$(grep -cE "^### \[\`[a-z0-9-]+\`\]" "$rm")" -ge 13 ]
echo "registration: PASS"
'
</automated>
  </acceptance_criteria>

  <verify>
    <automated>bash -c 'cd /Users/jacopo/Developer/projects/personal/tools/skills && set -e; jq -e . .claude-plugin/marketplace.json > /dev/null && jq -e . .vscode/settings.json > /dev/null && [ "$(jq -r ".plugins[] | select(.name == \"JeisKappa-workflow\") | .skills | length" .claude-plugin/marketplace.json)" -eq 5 ] && [ "$(jq -r ".\"conventionalCommits.scopes\" | length" .vscode/settings.json)" -eq 13 ] && [ "$(grep -c "^### \[\`propose-auto\`\]" README.md)" -eq 1 ] && [ "$(grep -c "^### \[\`propose-interactive\`\]" README.md)" -eq 1 ] && echo PASS'</automated>
  </verify>

  <done>
.claude-plugin/marketplace.json has `./skills/propose-auto` and `./skills/propose-interactive` inserted in alphabetical order in the JeisKappa-workflow plugin's skills array (final length = 5; JeisKappa-skills unchanged at 8). .vscode/settings.json has `propose-auto` and `propose-interactive` added to `conventionalCommits.scopes` in alphabetical position (final length = 13, fully sorted). README.md has two new "Available skills" entries (propose-auto, propose-interactive) placed AFTER seeded-discussion and BEFORE the "## Retired skills" subsection, each with header link, description paragraph drawn from the skill's frontmatter, and install snippet. All existing entries and the Retired skills subsection are untouched. Both JSON files remain valid. All acceptance checks pass.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. Both new skill files exist and pass their per-task grep gates:
   `for s in propose-auto propose-interactive; do test -f "skills/$s/SKILL.md" || (echo "MISSING: $s"; exit 1); done`

2. Marketplace registers both new skills under JeisKappa-workflow (5 entries total) and JeisKappa-skills is unchanged (8 entries):
   `jq -r '.plugins[] | select(.name == "JeisKappa-workflow") | .skills | length' .claude-plugin/marketplace.json` == `5`
   `jq -r '.plugins[] | select(.name == "JeisKappa-skills") | .skills | length' .claude-plugin/marketplace.json` == `8`

3. `.vscode/settings.json` has 13 alphabetically sorted scopes including both new ones.

4. README.md has propose-auto + propose-interactive entries placed before the Retired skills subsection.

5. Anti-sycophancy markers (4 verbatim phrases) exist in propose-interactive but NOT in propose-auto:
   - propose-interactive: all 4 markers grep `>=1`
   - propose-auto: `grep -i "Anti-Sycophancy" skills/propose-auto/SKILL.md` returns `0` matches

6. Both new skills cite all three Phase 1 canonical docs by absolute path.

7. Both new skills use the record-form filename grammar (`-proposal.md` suffix) and write to `proposals/` folder.
</verification>

<success_criteria>
1. User can run `npx skills add Jei-sKappa/skills --skill propose-auto` and install propose-auto.
2. User can run `npx skills add Jei-sKappa/skills --skill propose-interactive` and install propose-interactive.
3. User running `npx skills list` sees propose-auto and propose-interactive grouped under the "JeisKappa Workflow" plugin heading (rendered from `JeisKappa-workflow`).
4. Invoking propose-auto from a rough prompt produces `docs/threads/<thread>/proposals/<UTC>-<kebab-desc>-proposal.md` with body content that suggests (does not enforce) the 4-element structure.
5. Invoking propose-interactive walks the user through the 4 suggested elements one at a time, asks before doing surprising things (anti-sycophancy applied), writes the same shape of artifact, and does NOT auto-write a decision log unless durable trade-offs / rejected alternatives emerge.
6. Neither skill auto-commits the proposal artifact — both bodies state this explicitly.
7. Both skills cite Phase 1 canonical docs (`docs/workflow/v1/thread-layout.md`, `filename-grammar.md`, `immutability.md`) by absolute path on first use.
8. The four-touchpoint registration is complete: skill folder exists, marketplace.json entry under JeisKappa-workflow, .vscode/settings.json conventional-commit scope, README.md "Available skills" entry.
</success_criteria>

<output>
Create `.planning/phases/03-forward-spine-propose-and-spec/03-01-SUMMARY.md` when done, capturing:
- Frontmatter (phase: 03-forward-spine-propose-and-spec, plan: 01, status: complete, requirements: [PROP-01, PROP-02, PROP-03])
- Skills shipped (propose-auto, propose-interactive) with absolute paths
- Anti-sycophancy preservation confirmation for propose-interactive (4 markers present)
- Registration touchpoints updated (marketplace.json, .vscode/settings.json, README.md)
- One-paragraph patterns-established field describing what propose-* plans look like for downstream phases (Plan 4, 5, 6, 7) — single-artifact-type forward generator pair, paired skill emission, freeform-with-suggested-structure pattern
- Note that `derive-spec` was NOT touched and that the SPEC-04 README clarifier note distinguishing `spec-*` (forward) from `derive-spec` (reverse) lands in Plan 03-02 (the next plan)
</output>
</content>
</invoke>