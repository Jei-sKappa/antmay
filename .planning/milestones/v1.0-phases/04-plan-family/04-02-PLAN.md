---
phase: 04-plan-family
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - skills/plan-strict-auto/SKILL.md
  - skills/plan-strict-interactive/SKILL.md
  - .claude-plugin/marketplace.json
  - .vscode/settings.json
  - README.md
autonomous: true
requirements:
  - PLAN-03
  - PLAN-04
  - PLAN-06
  - PLAN-07
  - PLAN-08
  - PLAN-09

must_haves:
  truths:
    - "User can invoke `plan-strict-auto` from a spec/proposal/decision-log/issue/raw prompt and receive an emitted plan artifact under `docs/threads/<thread>/plans/<UTC>-v1-plan.md` whose tasks are sequential, isolated, independently implementable + reviewable, AND carry explicit substeps + verification notes + files-modified + acceptance criteria per task."
    - "User can invoke `plan-strict-interactive` and walk the input collaboratively, with the agent preserving the four anti-sycophancy markers from `discussion/SKILL.md`."
    - "User reading either skill body can verify: (a) the D59 sequential-isolated-independent contract is enforced; (b) the D60 prohibition on parallelization markers / wave numbers / dependency arrays is explicit; (c) the D61 self-review section exists; (d) the D62 NEVER-auto-commit phrase is present; (e) the strict-granularity per-task fields (objective / input / output / verification) are documented."
    - "User running `npx skills add` finds both strict-granularity skills installable via the `JeisKappa-workflow` marketplace plugin after registration."
  artifacts:
    - path: "skills/plan-strict-auto/SKILL.md"
      provides: "Autonomous strict-granularity plan generator skill"
      contains: "frontmatter with name=plan-strict-auto, version=1.0.0; ## Workflow section; ## Self-Review section; ## Strict Plan Body Shape (or equivalent) section documenting per-task fields; literal 'NEVER commits' phrase; literal 'sequential, isolated, independently' phrase"
    - path: "skills/plan-strict-interactive/SKILL.md"
      provides: "Collaborative strict-granularity plan skill"
      contains: "frontmatter with name=plan-strict-interactive, version=1.0.0; ## Anti-Sycophancy Stance section preserving all 4 markers verbatim; ## Workflow section; ## Self-Review section; literal 'NEVER commits' phrase"
    - path: ".claude-plugin/marketplace.json"
      provides: "Marketplace registration for both new strict skills"
      contains: "./skills/plan-strict-auto and ./skills/plan-strict-interactive entries under JeisKappa-workflow (total array length now 11)"
    - path: ".vscode/settings.json"
      provides: "Conventional-commit scopes for both new strict skills"
      contains: "plan-strict-auto and plan-strict-interactive in conventionalCommits.scopes, alphabetically sorted (total length now 19)"
    - path: "README.md"
      provides: "Public install snippets for both new strict skills"
      contains: "### `plan-strict-auto` and ### `plan-strict-interactive` headings with `npx skills add` install commands, inserted alphabetically near the plan-loose pair"
  key_links:
    - from: "skills/plan-strict-auto/SKILL.md"
      to: "docs/workflow/v1/filename-grammar.md"
      via: "citation by absolute path"
      pattern: "docs/workflow/v1/filename-grammar.md"
    - from: "skills/plan-strict-auto/SKILL.md"
      to: "docs/workflow/v1/thread-layout.md"
      via: "citation by absolute path"
      pattern: "docs/workflow/v1/thread-layout.md"
    - from: "skills/plan-strict-auto/SKILL.md"
      to: "docs/workflow/v1/immutability.md"
      via: "citation by absolute path"
      pattern: "docs/workflow/v1/immutability.md"
    - from: "skills/plan-strict-interactive/SKILL.md"
      to: "skills/discussion/SKILL.md"
      via: "anti-sycophancy stance carried verbatim"
      pattern: "Disagree when you disagree|Do not treat pushback as correctness"
    - from: ".claude-plugin/marketplace.json"
      to: "skills/plan-strict-auto/ and skills/plan-strict-interactive/"
      via: "JeisKappa-workflow.skills array entries"
      pattern: "./skills/plan-strict-auto|./skills/plan-strict-interactive"
---

<objective>
Ship the strict-granularity plan-authoring pair: `plan-strict-auto` (autonomous from input → strict plan artifact) and `plan-strict-interactive` (collaborative walk → strict plan artifact). Both skills emit a versioned plan at `docs/threads/<thread>/plans/<UTC>-v<N>[-<descriptor>]-plan.md`. Strict plans carry explicit substeps, verification notes, files-modified per task, and acceptance criteria per task — optimized for autonomous execution by an agent that needs unambiguous instructions. Both skills enforce the D59 sequential-isolated-independent contract, the D60 strict prohibition on parallelization markers, the D61 self-review pass before emission, and the D62 never-auto-commit rule. Register both skills in the three V1 touchpoints (`marketplace.json`, `.vscode/settings.json`, `README.md`).

Purpose: The strict pair is the second of three pairs in Phase 4. Strict plans complement loose plans (Plan 04-01) — per D58 neither is "better"; the user picks based on whether the implementer is human-leaning (loose works) or agent-leaning (strict reduces ambiguity).

Output: Two new skill folders + three registration files updated. All four CLAUDE.md touchpoints satisfied for each skill.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-plan-family/04-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@AGENTS.md

@docs/workflow/v1/README.md
@docs/workflow/v1/thread-layout.md
@docs/workflow/v1/filename-grammar.md
@docs/workflow/v1/immutability.md

@skills/propose-auto/SKILL.md
@skills/propose-interactive/SKILL.md
@skills/spec-auto/SKILL.md
@skills/spec-interactive/SKILL.md
@skills/discussion/SKILL.md

@.claude-plugin/marketplace.json
@.vscode/settings.json
@README.md

<!-- Reference plan-loose siblings for cross-pair consistency. After 04-01 ships these exist. -->
@skills/plan-loose-auto/SKILL.md
@skills/plan-loose-interactive/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author skills/plan-strict-auto/SKILL.md</name>
  <read_first>
    - skills/spec-auto/SKILL.md (Phase 3 closest structural analog — handoff-grade multi-section authoring parallels strict-plan task structure)
    - skills/propose-auto/SKILL.md (pair-skill auto half tone)
    - skills/plan-loose-auto/SKILL.md (Plan 04-01 sibling — opening clarifier, citations, no-parallelization language should be consistent across the strict/loose pair)
    - .planning/phases/04-plan-family/04-CONTEXT.md
    - docs/workflow/v1/filename-grammar.md
    - docs/workflow/v1/thread-layout.md
    - docs/workflow/v1/immutability.md
  </read_first>
  <files>skills/plan-strict-auto/SKILL.md</files>
  <action>
    Create the file with YAML frontmatter and a markdown body. Frontmatter required fields:
    - `name: plan-strict-auto`
    - `description:` a single sentence including the "Use when…" trigger AND explicit interaction mode (autonomous, no clarifying questions). Mention the sibling `plan-strict-interactive` and the loose-granularity siblings (`plan-loose-auto`, `plan-loose-interactive`). Per D58, do NOT recommend strict over loose — name the trade-off (strict reduces ambiguity for agent-leaning implementers and tighter handoff; loose suits human-leaning implementers).
    - `metadata.author: https://github.com/Jei-sKappa`
    - `metadata.version: 1.0.0`

    Body sections (executor discretion on names; behavioral tokens below MUST appear):

    1. Opening clarifier paragraph naming the sibling `plan-strict-interactive` and the loose-granularity siblings. Restate the D58 stance (loose vs strict is a user/context choice, not "strict is better").

    2. **Inputs** section listing the FOUR input forms (same shape as plan-loose-auto and spec-auto): spec path, proposal path, decision-log path, GitHub issue URL/identifier, raw prompt. Document the ambiguity fallback (ASK the user per `docs/workflow/v1/immutability.md`).

    3. **Plan Artifact Contract** section enforcing D59. MUST contain the phrase "sequential, isolated, independently".

    4. **No Parallelization** section enforcing D60 — same prohibition language as plan-loose-auto. MUST name forbidden constructs (wave numbers, dependency arrays, task graph notation, parallelization markers) with explicit MUST NOT / NEVER / do not language. Cite D60.

    5. **Strict Plan Body Shape** section describing the per-task fields that distinguish strict from loose granularity. Each task in a strict plan MUST carry at minimum:
       - **Objective** — one sentence stating what this task accomplishes.
       - **Input / context** — the artifacts, files, or upstream state the task depends on.
       - **Steps / substeps** — the explicit sub-actions the implementer takes. Numbered list.
       - **Files modified** — the exact files this task touches.
       - **Verification** — how the implementer (or a reviewer) confirms the task succeeded. Concrete command, grep, jq, or test-f check preferred over "looks correct".
       - **Acceptance criteria** — the observable state of completion (what must be true after the task is done).
       Add a SHORT (≤ 10 lines) worked example of a tiny strict-plan task that exhibits these fields without going overboard on prescriptiveness. The example MUST NOT contain wave numbers, depends_on arrays, [W1] markers, or any parallelization notation (D60 internal compliance).

    6. **Self-Review** section (D61, PLAN-08) describing the four checks the agent runs in-session before emitting:
       - Coherence: does the plan actually achieve the input artifact's goal?
       - Granularity fit: is strict appropriate for this input? (e.g., is the input rich enough to warrant per-task substeps + verification?)
       - No under-splitting: each task is independently implementable?
       - No over-splitting: no trivial 1-line tasks bloating the plan?
       - State explicitly: self-review is a quality discipline; the emitted plan body does NOT contain a "self-review notes" section. The artifact is clean.

    7. **Workflow** section with numbered steps mirroring spec-auto's sequence: resolve thread → resolve and read the input → derive descriptor (first emission defaults to NO descriptor; `v1-plan.md` canonical first form) → capture UTC stamp at write time → draft body with per-task fields → run self-review → write the artifact → confirm path.

    8. **Filename and Folder** section restating the versioned-form grammar (one-sentence summary) and citing `docs/workflow/v1/filename-grammar.md`. Canonical first-emission example: `260521120000Z-v1-plan.md`. Output folder: `docs/threads/<thread>/plans/`.

    9. **Immutability** section: emitted plans are immutable per `docs/workflow/v1/immutability.md`; revisions go to v2+; no source-relation YAML frontmatter; drafts under `.wip/` are editable until emission.

    10. **Commit Policy** section containing the LITERAL PHRASE "This skill NEVER commits the emitted plan automatically. Commits happen only if the surrounding session explicitly requests one." (or near-verbatim equivalent containing "NEVER commits" as a substring).

    Voice and discipline:
    - Dense, declarative, citation-first. Match Phase 2/3 tone.
    - Cite Phase 1 canonical docs by ABSOLUTE PATH on first invocation.
    - Do NOT include parallelization markers in any example.
    - Do NOT include a `## Anti-Sycophancy Stance` section (this is the AUTO half).
  </action>
  <acceptance_criteria>
    Run these checks and ALL must pass:

    1. `test -f skills/plan-strict-auto/SKILL.md`.
    2. `grep -c "^name: plan-strict-auto$" skills/plan-strict-auto/SKILL.md` — exactly 1.
    3. `grep -c "^  version: 1.0.0$" skills/plan-strict-auto/SKILL.md` — at least 1.
    4. `grep -c "^  author: https://github.com/Jei-sKappa$" skills/plan-strict-auto/SKILL.md` — at least 1.
    5. `grep -c "Use when" skills/plan-strict-auto/SKILL.md` — at least 1.
    6. `grep -c "plan-strict-interactive" skills/plan-strict-auto/SKILL.md` — at least 1.
    7. `grep -c "plan-loose" skills/plan-strict-auto/SKILL.md` — at least 1 (loose sibling referenced).
    8. `grep -c "docs/workflow/v1/filename-grammar.md" skills/plan-strict-auto/SKILL.md` — at least 1.
    9. `grep -c "docs/workflow/v1/thread-layout.md" skills/plan-strict-auto/SKILL.md` — at least 1.
    10. `grep -c "docs/workflow/v1/immutability.md" skills/plan-strict-auto/SKILL.md` — at least 1.
    11. `grep -c "sequential, isolated, independently" skills/plan-strict-auto/SKILL.md` — at least 1.
    12. `grep -c "NEVER commits" skills/plan-strict-auto/SKILL.md` — at least 1.
    13. `grep -c "D60" skills/plan-strict-auto/SKILL.md` — at least 1.
    14. `grep -c -E "wave numbers?|wave markers?" skills/plan-strict-auto/SKILL.md` — at least 1.
    15. `grep -c -E "dependency arrays?|depends_on|task graph" skills/plan-strict-auto/SKILL.md` — at least 1.
    16. `grep -c -i "self-review" skills/plan-strict-auto/SKILL.md` — at least 1.
    17. `grep -c -i "verification\|acceptance criteria\|files modified" skills/plan-strict-auto/SKILL.md` — at least 1 (strict per-task fields named).
    18. `grep -c -i "substeps\|sub-steps" skills/plan-strict-auto/SKILL.md` — at least 1 (strict-granularity differentiator).
    19. `grep -c "v1-plan.md\|-plan.md" skills/plan-strict-auto/SKILL.md` — at least 1.
    20. `grep -c "docs/threads/<thread>/plans/" skills/plan-strict-auto/SKILL.md` — at least 1.
    21. The worked example MUST NOT contain parallelization notation. `grep -E "\[W[0-9]+\]|^wave:|^depends_on:" skills/plan-strict-auto/SKILL.md` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>
      test -f skills/plan-strict-auto/SKILL.md && \
      grep -q "^name: plan-strict-auto$" skills/plan-strict-auto/SKILL.md && \
      grep -q "sequential, isolated, independently" skills/plan-strict-auto/SKILL.md && \
      grep -q "NEVER commits" skills/plan-strict-auto/SKILL.md && \
      grep -q "D60" skills/plan-strict-auto/SKILL.md && \
      grep -qE "wave numbers?|wave markers?" skills/plan-strict-auto/SKILL.md && \
      grep -qi "self-review" skills/plan-strict-auto/SKILL.md && \
      grep -qi "substeps\|sub-steps" skills/plan-strict-auto/SKILL.md && \
      grep -qi "verification\|acceptance criteria\|files modified" skills/plan-strict-auto/SKILL.md && \
      grep -q "docs/threads/<thread>/plans/" skills/plan-strict-auto/SKILL.md && \
      ! grep -qE "\[W[0-9]+\]|^wave:|^depends_on:" skills/plan-strict-auto/SKILL.md && \
      echo "plan-strict-auto SKILL.md acceptance passes"
    </automated>
  </verify>
  <done>skills/plan-strict-auto/SKILL.md exists with frontmatter + body covering: opening clarifier with sibling references, 4-input acceptance, D59 contract, D60 explicit prohibition, D61 self-review section, D62 NEVER-commit phrase, strict per-task fields (objective/input/steps/files-modified/verification/acceptance), V1 filename grammar citation, plans/ folder routing, parallelization-free worked example.</done>
</task>

<task type="auto">
  <name>Task 2: Author skills/plan-strict-interactive/SKILL.md</name>
  <read_first>
    - skills/spec-interactive/SKILL.md (Phase 3 closest analog — interactive walk for handoff-grade multi-section artifact)
    - skills/propose-interactive/SKILL.md (pair-skill interactive half tone)
    - skills/discussion/SKILL.md (anti-sycophancy stance source)
    - skills/plan-strict-auto/SKILL.md (Task 1 output — sibling consistency)
    - skills/plan-loose-interactive/SKILL.md (Plan 04-01 sibling — cross-pair anti-sycophancy stance consistency)
    - .planning/phases/04-plan-family/04-CONTEXT.md
    - docs/workflow/v1/filename-grammar.md
    - docs/workflow/v1/thread-layout.md
    - docs/workflow/v1/immutability.md
  </read_first>
  <files>skills/plan-strict-interactive/SKILL.md</files>
  <action>
    Create the file with YAML frontmatter and a markdown body. Frontmatter required fields:
    - `name: plan-strict-interactive`
    - `description:` a single sentence with the "Use when…" trigger AND explicit interaction mode (collaborative, walks tasks one at a time with substeps/verification fleshed out per task). Mention the sibling `plan-strict-auto` and the loose-granularity siblings.
    - `metadata.author: https://github.com/Jei-sKappa`
    - `metadata.version: 1.0.0`

    Body sections (executor discretion on names; behavioral tokens below MUST appear):

    1. Opening clarifier paragraph naming the sibling `plan-strict-auto` and the loose-granularity siblings, restating D58.

    2. **Anti-Sycophancy Stance** section. Carry the FOUR anti-sycophancy markers from `skills/discussion/SKILL.md` verbatim (same set Plan 04-01 used):
       - "Disagree when you disagree" clause
       - "Push back on weak or incomplete reasoning" clause
       - "Do not treat pushback as correctness" clause
       - "Refuse to log a [plan task] you believe is wrong without flagging it" clause
       Plus the framing line about bad plan decisions becoming expensive in implementation (mirror spec-interactive's heightened framing — the strict plan body is downstream-consumed by an implementer who will not have the agent to ask follow-ups, so unflagged design errors compound).

    3. **Inputs** section listing the FOUR input forms.

    4. **Plan Artifact Contract** section enforcing D59. MUST contain "sequential, isolated, independently".

    5. **No Parallelization** section enforcing D60. MUST cite D60 and name forbidden constructs with explicit prohibition language.

    6. **Strict Plan Body Shape** section documenting the per-task fields (objective / input / steps-substeps / files-modified / verification / acceptance criteria). May share content with plan-strict-auto Task 1 — restate or summarize as the executor prefers. The point is that the WALK collects these fields per task, then the agent assembles them into the body.

    7. **Self-Review** section (D61, PLAN-08) describing the same 4 checks (coherence / granularity fit / no under-splitting / no over-splitting). Note the walk closes WITH the self-review pass BEFORE emission; drafts under `.wip/` are editable; lock applies at write into `plans/`.

    8. **Workflow** section with numbered steps: resolve thread → resolve and read the input → walk the input task-by-task with the user (present each task's objective, then the substeps, then the files/verification/acceptance — push back per the anti-sycophancy stance throughout) → capture UTC stamp at write time → run self-review → write the artifact → confirm path.

    9. **Decision Log** section enforcing D93: default is no separate decision log. Write a log ONLY when durable trade-offs / rejected alternatives emerge that cannot reasonably be captured in the plan body. When written, use `docs/threads/<thread>/discussions/<UTC>-<kebab-desc>-decision-log.md` with `## D<N>:` sequential headings. Mirror spec-interactive's pattern.

    10. **Scope Drift** section: PARK as Inbox item via `capture-inbox`, SPLIT into own plan or discussion, or DEFER. Ask the user which.

    11. **Filename and Folder** section restating the versioned-form grammar and citing `docs/workflow/v1/filename-grammar.md`. Canonical first-emission example: `260521120000Z-v1-plan.md`. Output folder: `docs/threads/<thread>/plans/`.

    12. **Immutability** section: emitted plans are immutable; revisions go to v2+; drafts under `.wip/` are editable until emission.

    13. **Commit Policy** section containing the LITERAL PHRASE "This skill NEVER commits the emitted plan automatically. Commits happen only if the surrounding session explicitly requests one." (or near-verbatim equivalent with "NEVER commits" as a substring).

    Voice and discipline:
    - Dense, declarative, citation-first.
    - All 4 anti-sycophancy marker phrases must be present verbatim or near-verbatim (substring matches required by acceptance criteria).
    - Do NOT include parallelization markers in any example.
  </action>
  <acceptance_criteria>
    Run these checks and ALL must pass:

    1. `test -f skills/plan-strict-interactive/SKILL.md`.
    2. `grep -c "^name: plan-strict-interactive$" skills/plan-strict-interactive/SKILL.md` — exactly 1.
    3. `grep -c "^  version: 1.0.0$" skills/plan-strict-interactive/SKILL.md` — at least 1.
    4. `grep -c "^  author: https://github.com/Jei-sKappa$" skills/plan-strict-interactive/SKILL.md` — at least 1.
    5. `grep -c "Use when" skills/plan-strict-interactive/SKILL.md` — at least 1.
    6. `grep -c "plan-strict-auto" skills/plan-strict-interactive/SKILL.md` — at least 1.
    7. `grep -c "plan-loose" skills/plan-strict-interactive/SKILL.md` — at least 1.
    8. `grep -c "Anti-Sycophancy" skills/plan-strict-interactive/SKILL.md` — at least 1.
    9. `grep -c "Disagree when you disagree" skills/plan-strict-interactive/SKILL.md` — at least 1.
    10. `grep -c -E "Push back on (weak|incomplete)" skills/plan-strict-interactive/SKILL.md` — at least 1.
    11. `grep -c "Do not treat pushback as correctness" skills/plan-strict-interactive/SKILL.md` — at least 1.
    12. `grep -c -E "Refuse to log" skills/plan-strict-interactive/SKILL.md` — at least 1.
    13. `grep -c "docs/workflow/v1/filename-grammar.md" skills/plan-strict-interactive/SKILL.md` — at least 1.
    14. `grep -c "docs/workflow/v1/thread-layout.md" skills/plan-strict-interactive/SKILL.md` — at least 1.
    15. `grep -c "docs/workflow/v1/immutability.md" skills/plan-strict-interactive/SKILL.md` — at least 1.
    16. `grep -c "sequential, isolated, independently" skills/plan-strict-interactive/SKILL.md` — at least 1.
    17. `grep -c "NEVER commits" skills/plan-strict-interactive/SKILL.md` — at least 1.
    18. `grep -c "D60" skills/plan-strict-interactive/SKILL.md` — at least 1.
    19. `grep -c -E "wave numbers?|wave markers?" skills/plan-strict-interactive/SKILL.md` — at least 1.
    20. `grep -c -E "dependency arrays?|depends_on|task graph" skills/plan-strict-interactive/SKILL.md` — at least 1.
    21. `grep -c -i "self-review" skills/plan-strict-interactive/SKILL.md` — at least 1.
    22. `grep -c -i "verification\|acceptance criteria\|files modified" skills/plan-strict-interactive/SKILL.md` — at least 1.
    23. `grep -c -i "substeps\|sub-steps" skills/plan-strict-interactive/SKILL.md` — at least 1.
    24. `grep -c -i "Decision Log\|decision log" skills/plan-strict-interactive/SKILL.md` — at least 1 (D93 section present).
    25. `grep -c "docs/threads/<thread>/plans/" skills/plan-strict-interactive/SKILL.md` — at least 1.
    26. The worked example MUST NOT contain parallelization notation. `grep -E "\[W[0-9]+\]|^wave:|^depends_on:" skills/plan-strict-interactive/SKILL.md` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>
      test -f skills/plan-strict-interactive/SKILL.md && \
      grep -q "^name: plan-strict-interactive$" skills/plan-strict-interactive/SKILL.md && \
      grep -q "Anti-Sycophancy" skills/plan-strict-interactive/SKILL.md && \
      grep -q "Disagree when you disagree" skills/plan-strict-interactive/SKILL.md && \
      grep -qE "Push back on (weak|incomplete)" skills/plan-strict-interactive/SKILL.md && \
      grep -q "Do not treat pushback as correctness" skills/plan-strict-interactive/SKILL.md && \
      grep -qE "Refuse to log" skills/plan-strict-interactive/SKILL.md && \
      grep -q "sequential, isolated, independently" skills/plan-strict-interactive/SKILL.md && \
      grep -q "NEVER commits" skills/plan-strict-interactive/SKILL.md && \
      grep -q "D60" skills/plan-strict-interactive/SKILL.md && \
      grep -qi "self-review" skills/plan-strict-interactive/SKILL.md && \
      grep -qi "substeps\|sub-steps" skills/plan-strict-interactive/SKILL.md && \
      grep -qi "verification\|acceptance criteria\|files modified" skills/plan-strict-interactive/SKILL.md && \
      grep -q "docs/threads/<thread>/plans/" skills/plan-strict-interactive/SKILL.md && \
      ! grep -qE "\[W[0-9]+\]|^wave:|^depends_on:" skills/plan-strict-interactive/SKILL.md && \
      echo "plan-strict-interactive SKILL.md acceptance passes"
    </automated>
  </verify>
  <done>skills/plan-strict-interactive/SKILL.md exists with frontmatter + body covering: opening clarifier with sibling references, 4 anti-sycophancy markers verbatim, 4-input acceptance, D59 contract, D60 explicit prohibition with citation, D61 self-review section, D62 NEVER-commit phrase, D93 decision-log opt-in default, scope-drift handling, strict per-task fields documented, V1 filename grammar citation, plans/ folder routing.</done>
</task>

<task type="auto">
  <name>Task 3: Register plan-strict-auto and plan-strict-interactive in marketplace, scopes, and README</name>
  <read_first>
    - .claude-plugin/marketplace.json (state after Plan 04-01 — JeisKappa-workflow array has 9 entries including plan-loose-auto and plan-loose-interactive)
    - .vscode/settings.json (state after Plan 04-01 — 17 alphabetically-sorted scopes including plan-loose-auto and plan-loose-interactive)
    - README.md (state after Plan 04-01 — 17 "Available skills" entries; Retired skills subsection at bottom)
    - AGENTS.md (the 4-touchpoint checklist)
    - .planning/phases/04-plan-family/04-CONTEXT.md
    - skills/plan-loose-auto/SKILL.md (sibling pair README description style)
    - skills/plan-loose-interactive/SKILL.md
  </read_first>
  <files>.claude-plugin/marketplace.json, .vscode/settings.json, README.md</files>
  <action>
    Update all three registration files atomically:

    1. **`.claude-plugin/marketplace.json`** — add `"./skills/plan-strict-auto"` and `"./skills/plan-strict-interactive"` to the JeisKappa-workflow `skills` array in alphabetical position (they fall between `plan-loose-interactive` and `propose-auto`). After this change the JeisKappa-workflow array has exactly 11 entries. The JeisKappa-skills array stays unchanged at 8 entries.

    2. **`.vscode/settings.json`** — add `"plan-strict-auto"` and `"plan-strict-interactive"` to `conventionalCommits.scopes`. Total length now 19. Keep alphabetical order — the new entries fall between `plan-loose-interactive` and `propose-auto`.

    3. **`README.md`** — add two new "Available skills" entries (one per new skill), placed AFTER `plan-loose-interactive` and BEFORE `spec-auto` (alphabetical position is `plan-strict-*` between `plan-loose-*` and `spec-*` — however, the current README ordering is presentation order rather than strict alphabetical; mirror the order established by Plan 04-01: insert `plan-strict-auto` then `plan-strict-interactive` directly after the `plan-loose-interactive` block, BEFORE the `## Retired skills` subsection). Each entry follows the established shape: `### [\`<name>\`](./skills/<name>/SKILL.md)` heading + paragraph description + fenced `npx skills add ...` block.

    Do NOT touch the `## Retired skills` subsection. Do NOT touch entries above `plan-loose-interactive`. Preserve trailing newlines.

    Do NOT commit.
  </action>
  <acceptance_criteria>
    Run these checks and ALL must pass:

    1. `jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length == 11' .claude-plugin/marketplace.json` returns true.
    2. `jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/plan-strict-auto")' .claude-plugin/marketplace.json` returns non-null.
    3. `jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/plan-strict-interactive")' .claude-plugin/marketplace.json` returns non-null.
    4. `jq -e '.plugins[] | select(.name=="JeisKappa-skills") | .skills | length == 8' .claude-plugin/marketplace.json` returns true.
    5. `jq -e '."conventionalCommits.scopes" | length == 19' .vscode/settings.json` returns true.
    6. `jq -e '."conventionalCommits.scopes" | index("plan-strict-auto")' .vscode/settings.json` returns non-null.
    7. `jq -e '."conventionalCommits.scopes" | index("plan-strict-interactive")' .vscode/settings.json` returns non-null.
    8. `jq -e '."conventionalCommits.scopes" == (."conventionalCommits.scopes" | sort)' .vscode/settings.json` returns true.
    9. `grep -c "^### \[\`plan-strict-auto\`\]" README.md` — exactly 1.
    10. `grep -c "^### \[\`plan-strict-interactive\`\]" README.md` — exactly 1.
    11. `grep -c "npx skills add Jei-sKappa/skills --skill plan-strict-auto" README.md` — exactly 1.
    12. `grep -c "npx skills add Jei-sKappa/skills --skill plan-strict-interactive" README.md` — exactly 1.
    13. Both new README headings fall BETWEEN `plan-loose-interactive` and `## Retired skills`. Verify: `bash -c 'A=$(grep -n "^### \\[\`plan-loose-interactive\`\\]" README.md | cut -d: -f1); B=$(grep -n "^## Retired skills" README.md | cut -d: -f1); C=$(grep -n "^### \\[\`plan-strict-auto\`\\]" README.md | cut -d: -f1); D=$(grep -n "^### \\[\`plan-strict-interactive\`\\]" README.md | cut -d: -f1); [[ $C -gt $A && $C -lt $B && $D -gt $A && $D -lt $B ]] && echo OK'` prints OK.
    14. No new git commit created.
  </acceptance_criteria>
  <verify>
    <automated>
      jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length == 11' .claude-plugin/marketplace.json >/dev/null && \
      jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/plan-strict-auto")' .claude-plugin/marketplace.json >/dev/null && \
      jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/plan-strict-interactive")' .claude-plugin/marketplace.json >/dev/null && \
      jq -e '."conventionalCommits.scopes" | length == 19' .vscode/settings.json >/dev/null && \
      jq -e '."conventionalCommits.scopes" | index("plan-strict-auto")' .vscode/settings.json >/dev/null && \
      jq -e '."conventionalCommits.scopes" | index("plan-strict-interactive")' .vscode/settings.json >/dev/null && \
      jq -e '."conventionalCommits.scopes" == (."conventionalCommits.scopes" | sort)' .vscode/settings.json >/dev/null && \
      [ "$(grep -c '^### \[`plan-strict-auto`\]' README.md)" = "1" ] && \
      [ "$(grep -c '^### \[`plan-strict-interactive`\]' README.md)" = "1" ] && \
      [ "$(grep -c 'npx skills add Jei-sKappa/skills --skill plan-strict-auto' README.md)" = "1" ] && \
      [ "$(grep -c 'npx skills add Jei-sKappa/skills --skill plan-strict-interactive' README.md)" = "1" ] && \
      echo "Strict pair registration acceptance passes"
    </automated>
  </verify>
  <done>marketplace.json JeisKappa-workflow array has 11 entries, .vscode/settings.json scopes has 19 entries (alphabetically sorted), README.md has 2 new install blocks for the strict pair. No git commit created.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

1. **Skill files exist with correct frontmatter:**
   ```
   ls -la skills/plan-strict-auto/SKILL.md skills/plan-strict-interactive/SKILL.md
   ```

2. **D59/D60/D61/D62 contracts enforced + strict per-task fields documented:**
   ```bash
   for SKILL in skills/plan-strict-auto/SKILL.md skills/plan-strict-interactive/SKILL.md; do
     grep -q "sequential, isolated, independently" "$SKILL" || echo "FAIL D59 in $SKILL"
     grep -q "D60" "$SKILL" || echo "FAIL D60 citation in $SKILL"
     grep -qi "self-review" "$SKILL" || echo "FAIL D61 in $SKILL"
     grep -q "NEVER commits" "$SKILL" || echo "FAIL D62 in $SKILL"
     grep -qi "substeps\|sub-steps" "$SKILL" || echo "FAIL strict-granularity differentiator in $SKILL"
     grep -qi "verification\|acceptance criteria\|files modified" "$SKILL" || echo "FAIL strict per-task fields in $SKILL"
   done
   ```

3. **Anti-sycophancy markers in interactive skill:**
   ```bash
   grep -q "Disagree when you disagree" skills/plan-strict-interactive/SKILL.md
   grep -qE "Push back on (weak|incomplete)" skills/plan-strict-interactive/SKILL.md
   grep -q "Do not treat pushback as correctness" skills/plan-strict-interactive/SKILL.md
   grep -qE "Refuse to log" skills/plan-strict-interactive/SKILL.md
   ```

4. **Four-touchpoint registration:**
   ```bash
   jq '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length' .claude-plugin/marketplace.json    # expect 11
   jq '."conventionalCommits.scopes" | length' .vscode/settings.json                                              # expect 19
   grep -c "plan-strict-" README.md                                                                             # expect >= 4
   ```
</verification>

<success_criteria>
- skills/plan-strict-auto/SKILL.md and skills/plan-strict-interactive/SKILL.md exist with correct V1 frontmatter.
- Both skills enforce D59 (sequential-isolated-independent) and D60 (no parallelization, with explicit prohibition language + D60 citation).
- Both skills have a `## Self-Review` (or equivalent) section per D61.
- Both skills contain "NEVER commits" per D62.
- Both skills document strict per-task fields (objective / input / steps-substeps / files-modified / verification / acceptance).
- plan-strict-interactive carries all 4 anti-sycophancy markers verbatim and enforces D93 (no auto-write decision log default).
- marketplace.json JeisKappa-workflow array has exactly 11 entries.
- .vscode/settings.json conventionalCommits.scopes has exactly 19 entries, alphabetically sorted.
- README.md has 2 new entries inserted after the plan-loose pair and before `## Retired skills`.
- No commit created during this plan.
</success_criteria>

<output>
Create `.planning/phases/04-plan-family/04-02-SUMMARY.md` when done.
</output>
