---
phase: 01-foundations
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - docs/workflow/v1/filename-grammar.md
  - docs/workflow/v1/immutability.md
autonomous: true
requirements:
  - THRD-04
  - THRD-05
  - THRD-06
  - THRD-07
  - THRD-08

must_haves:
  truths:
    - "User can read `docs/workflow/v1/filename-grammar.md` and learn the UTC stamp pattern `YYMMDDHHMMSSZ` (12 chars, no separators)"
    - "User can read `docs/workflow/v1/filename-grammar.md` and learn the record form `<UTC>-<kebab-desc>-<artifact-type>.md` with mandatory artifact-type suffix"
    - "User can read `docs/workflow/v1/filename-grammar.md` and learn the versioned form `<UTC>-v<N>[-<kebab-descriptor>]-<artifact-type>.md` with N starting at 1 and target-version semantics"
    - "User can read `docs/workflow/v1/filename-grammar.md` and see the recognized V1 artifact-type tokens list"
    - "User can read `docs/workflow/v1/immutability.md` and learn that emitted versioned and record artifacts are NEVER edited after emission"
    - "User can read `docs/workflow/v1/immutability.md` and learn that drafts under `.wip/` remain editable until emission"
    - "User can read `docs/workflow/v1/immutability.md` and learn that source-relation frontmatter (`Supersedes:`, `Alternative to:`, `Forked from:`) is forbidden"
    - "User can read `docs/workflow/v1/immutability.md` and learn that ambiguous artifact references are resolved by asking the user, not by a global latest-artifact algorithm"
  artifacts:
    - path: "docs/workflow/v1/filename-grammar.md"
      provides: "Record + versioned filename grammars and recognized artifact-type tokens"
      contains: "YYMMDDHHMMSSZ"
    - path: "docs/workflow/v1/immutability.md"
      provides: "Emitted-artifact immutability + reference-resolution rules"
      contains: "immutable"
  key_links:
    - from: "docs/workflow/v1/filename-grammar.md"
      to: "D11, D12, D42, D43, D46, D47 source decisions"
      via: "Codifies header"
      pattern: "Codifies.*D11.*D12.*D42.*D43.*D46.*D47"
    - from: "docs/workflow/v1/immutability.md"
      to: "D39, D40, D41, D44, D49 source decisions"
      via: "Codifies header"
      pattern: "Codifies.*D39.*D40.*D41.*D44.*D49"
    - from: "docs/workflow/v1/README.md"
      to: "filename-grammar.md and immutability.md"
      via: "Plan 01's README index"
      pattern: "filename-grammar\\.md|immutability\\.md"
---

<objective>
Complete the V1 workflow reference doc tree by adding the two remaining rules docs: `filename-grammar.md` (record + versioned filename forms with mandatory artifact-type suffix and target-version semantics) and `immutability.md` (emitted-artifact immutability + no source-relation frontmatter + ambiguous-reference resolution by asking).

Purpose: These two rules are foundational for every V1 skill that emits artifacts. Without `filename-grammar.md`, every emitting skill would have to re-derive the UTC stamp pattern, the kebab-desc placement, and the artifact-type suffix from the source decision log. Without `immutability.md`, downstream skills could violate the no-edit-after-emit rule or invent lineage frontmatter that's explicitly forbidden by D44.

Output: Two markdown files at `docs/workflow/v1/filename-grammar.md` and `docs/workflow/v1/immutability.md`. Together with Plan 01's `thread-layout.md`, they complete the canonical doc set indexed by `docs/workflow/v1/README.md`.

Depends on Plan 01 because Plan 01 created the parent directory and the index that already names these two files as forward references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundations/01-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@AGENTS.md

# Plan 01 output (forward-references this plan resolves)
@docs/workflow/v1/README.md
@docs/workflow/v1/thread-layout.md

# Source decision log (read for D11, D12, D39, D40, D41, D42, D43, D44, D46, D47, D49)
@docs/threads/260520095223Z-agentic-workflow/discussions/260518200115Z-agentic-workflow-design-discussion.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create `docs/workflow/v1/filename-grammar.md` codifying D11, D12, D42, D43, D46, D47</name>
  <files>docs/workflow/v1/filename-grammar.md</files>
  <read_first>
    - .planning/phases/01-foundations/01-CONTEXT.md (Filename Grammar Concrete Specification section — UTC pattern, record form, versioned form, recognized artifact-type tokens list)
    - docs/workflow/v1/thread-layout.md (already on disk — match section heading style and `**Codifies:**` line format)
    - .planning/codebase/CONVENTIONS.md (voice match)
    - docs/threads/260520095223Z-agentic-workflow/discussions/260518200115Z-agentic-workflow-design-discussion.md (locate D11, D12, D42, D43, D46, D47 for citation accuracy)
  </read_first>
  <action>
    Create `docs/workflow/v1/filename-grammar.md`.

    Open with `# V1 Filename Grammar` then immediately `**Codifies:** D11, D12, D42, D43, D46, D47` on its own line.

    Required section headings in this order (use `##`):

    1. `## UTC Stamp` — state the literal pattern `YYMMDDHHMMSSZ` (12 characters, no separators, trailing `Z` denotes UTC). Cite D11 inline. Give one realistic example using the existing thread stamp: `260518200115Z` (parsed as 2026-05-18 20:01:15 UTC). Note that all V1 artifact filenames start with this stamp as the first 12 characters.

    2. `## Record Form` — state the exact pattern `<YYMMDDHHMMSSZ>-<kebab-desc>-<artifact-type>.md`. Break the grammar into a fenced ```text block and label each segment:
       ```
       260518200115Z-agentic-workflow-design-discussion.md
       ^^^^^^^^^^^^ ^^^^^^^^^^^^^^^^^^^^^^^^ ^^^^^^^^^^
       UTC stamp    kebab description        artifact type
       ```
       State that the artifact-type suffix is MANDATORY — no V1 artifact filename may omit it. Cite D12, D43 inline. Add one or two more realistic examples per artifact type (e.g. `260520120000Z-auth-flow-proposal.md`, `260521094500Z-onboarding-inbox-item.md`).

    3. `## Versioned Form` — state the exact pattern `<YYMMDDHHMMSSZ>-v<N>[-<kebab-descriptor>]-<artifact-type>.md`. Break the grammar into a fenced ```text block showing both the mainline-integer case and the descriptor case:
       ```
       260520120000Z-v1-auth-spec.md                 (mainline v1)
       260520120000Z-v2-auth-spec.md                 (mainline v2)
       260520120000Z-v2-no-oauth-auth-spec.md        (v2 variant with descriptor)
       ```
       State explicitly: `N` starts at 1 (NOT 0). `<descriptor>` distinguishes a candidate/variant from the mainline integer-only file at the same version. Artifact-type token is MANDATORY in versioned form too. Cite D42, D46, D47 inline.

    4. `## Target-Version Semantics` — codify D47 in plain language: the `v<N>` in a versioned filename names the TARGET version this artifact represents (the version the author is producing), NOT the predecessor it derives from. State that there is no lineage encoded in the filename beyond the version number — the relationship to prior versions lives in the filenames' shared kebab description, not in metadata. Reference D44 by name as the forbidden alternative.

    5. `## Recognized V1 Artifact-Type Tokens` — list as a markdown bullet list the V1 tokens from 01-CONTEXT.md (Filename Grammar Concrete Specification — Recognized artifact-type tokens for V1): `proposal`, `spec`, `plan`, `discussion`, `decision-log`, `inbox-item`, `review-finding`. State that this list is documented but not exhaustive — future V1 skills may register additional tokens; the recognized set will be extended in their owning skill bodies and re-listed here when stabilized.

    6. `## Examples` — give a short table or bulleted list mapping each recognized artifact-type token to (a) the folder it lands in (per `thread-layout.md`) and (b) one realistic filename example in record form. Use the canonical thread `docs/threads/260520095223Z-agentic-workflow/` for context where applicable.

    Target length: ~70-110 lines (this doc is the most rule-dense of the three). Do NOT add YAML frontmatter. Do NOT inline rules from `immutability.md` or `thread-layout.md` — link to them only if necessary.
  </action>
  <verify>
    <automated>test -f docs/workflow/v1/filename-grammar.md &amp;&amp; grep -F 'Codifies:' docs/workflow/v1/filename-grammar.md | grep -q 'D11' &amp;&amp; grep -F 'Codifies:' docs/workflow/v1/filename-grammar.md | grep -q 'D12' &amp;&amp; grep -F 'Codifies:' docs/workflow/v1/filename-grammar.md | grep -q 'D42' &amp;&amp; grep -F 'Codifies:' docs/workflow/v1/filename-grammar.md | grep -q 'D43' &amp;&amp; grep -F 'Codifies:' docs/workflow/v1/filename-grammar.md | grep -q 'D46' &amp;&amp; grep -F 'Codifies:' docs/workflow/v1/filename-grammar.md | grep -q 'D47' &amp;&amp; grep -qF 'YYMMDDHHMMSSZ' docs/workflow/v1/filename-grammar.md &amp;&amp; grep -qE 'v<N>|v1|v2' docs/workflow/v1/filename-grammar.md &amp;&amp; grep -qiE 'mandatory|MUST' docs/workflow/v1/filename-grammar.md &amp;&amp; grep -qF 'proposal' docs/workflow/v1/filename-grammar.md &amp;&amp; grep -qF 'spec' docs/workflow/v1/filename-grammar.md &amp;&amp; grep -qF 'discussion' docs/workflow/v1/filename-grammar.md &amp;&amp; grep -qF 'inbox-item' docs/workflow/v1/filename-grammar.md &amp;&amp; grep -qF 'review-finding' docs/workflow/v1/filename-grammar.md &amp;&amp; grep -qF 'decision-log' docs/workflow/v1/filename-grammar.md</automated>
  </verify>
  <done>
    File `docs/workflow/v1/filename-grammar.md` exists. It opens with `# V1 Filename Grammar` and `**Codifies:** D11, D12, D42, D43, D46, D47`. It contains the literal `YYMMDDHHMMSSZ` pattern, a record-form example, a versioned-form example with both mainline and descriptor variants, an explicit "MANDATORY" artifact-type-suffix statement, and the recognized V1 artifact-type tokens list (proposal, spec, plan, discussion, decision-log, inbox-item, review-finding). Length is approximately 70-110 lines. No YAML frontmatter.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create `docs/workflow/v1/immutability.md` codifying D39, D40, D41, D44, D49</name>
  <files>docs/workflow/v1/immutability.md</files>
  <read_first>
    - .planning/phases/01-foundations/01-CONTEXT.md (Immutability + Resolution Concrete Specification section)
    - docs/workflow/v1/thread-layout.md (match section heading style)
    - docs/workflow/v1/filename-grammar.md (just created — reference correctly for the "new version means new file" rule)
    - .planning/codebase/CONVENTIONS.md (voice match)
    - docs/threads/260520095223Z-agentic-workflow/discussions/260518200115Z-agentic-workflow-design-discussion.md (locate D39, D40, D41, D44, D49 for citation accuracy)
  </read_first>
  <action>
    Create `docs/workflow/v1/immutability.md`.

    Open with `# V1 Immutability and Reference Resolution` then immediately `**Codifies:** D39, D40, D41, D44, D49` on its own line.

    Required section headings in this order (use `##`):

    1. `## Emitted Artifacts Are Immutable` — state the rule plainly: once a versioned artifact (`-v<N>-...-<artifact-type>.md`) or a record artifact (`-<kebab-desc>-<artifact-type>.md`) has been written to its target folder under `docs/threads/<thread>/<folder>/`, it is NEVER edited. To change content, produce a new version (next mainline integer or a new descriptor variant per `./filename-grammar.md`) or a new record artifact. Cite D39, D40, D41 inline.

    2. `## Drafts Are Editable` — state the exception: in-session drafts under the thread's `.wip/` folder are EDITABLE until the skill chooses to emit. Once emitted (copy or move from `.wip/` into the target type's folder under the canonical filename grammar), the artifact is locked. Cite D40 inline.

    3. `## No Source-Relation Frontmatter` — state the rule plainly: V1 artifacts do NOT carry YAML frontmatter fields like `Supersedes:`, `Alternative to:`, `Forked from:`, or any other source-relation/lineage metadata. Cite D44 inline. State the rationale in one sentence: lineage lives in filenames only (shared kebab-description + version number), not in metadata.

    4. `## Ambiguous Reference Resolution` — state the rule plainly: when a downstream skill must resolve a reference like "the spec" or "the latest plan" and multiple candidates exist, the skill MUST ASK THE USER which artifact is intended. There is NO global "latest artifact" algorithm. There is no fallback to "most recent UTC stamp" or "highest version number". Cite D49 inline. State the rationale in one sentence: ambiguity often signals a real decision (which variant won?) — silently picking would hide it.

    5. `## What This Means In Practice` — bulleted list of 4-6 concrete consequences for skill authors:
       - "A typo discovered in an emitted spec means a new version (`v2`), not an in-place edit."
       - "A `spec-*` skill referencing 'the proposal' when multiple proposal versions exist asks the user which one."
       - "A `merge-artifacts-*` skill produces the next mainline integer version of the target type — it does not rewrite predecessors."
       - "Skill bodies MUST NOT instruct agents to add lineage frontmatter even as a comment or 'optional' field."
       - "Reference docs in `docs/workflow/v1/` are themselves immutable by convention — a V2 ruleset lives at `docs/workflow/v2/`, not as in-place edits here."

    6. `## Related` — link `./thread-layout.md` (where the `.wip/` folder is defined) and `./filename-grammar.md` (where the version-number grammar is defined) with one-line descriptions.

    Target length: ~60-90 lines. Do NOT add YAML frontmatter. Tone: direct, declarative, no hedging. Keep examples concrete.
  </action>
  <verify>
    <automated>test -f docs/workflow/v1/immutability.md &amp;&amp; grep -F 'Codifies:' docs/workflow/v1/immutability.md | grep -q 'D39' &amp;&amp; grep -F 'Codifies:' docs/workflow/v1/immutability.md | grep -q 'D40' &amp;&amp; grep -F 'Codifies:' docs/workflow/v1/immutability.md | grep -q 'D41' &amp;&amp; grep -F 'Codifies:' docs/workflow/v1/immutability.md | grep -q 'D44' &amp;&amp; grep -F 'Codifies:' docs/workflow/v1/immutability.md | grep -q 'D49' &amp;&amp; grep -qiE 'immutable|NEVER edited' docs/workflow/v1/immutability.md &amp;&amp; grep -qF '.wip/' docs/workflow/v1/immutability.md &amp;&amp; grep -qiE 'Supersedes:|Alternative to:|Forked from:' docs/workflow/v1/immutability.md &amp;&amp; grep -qiE 'ask the user|ASK THE USER' docs/workflow/v1/immutability.md &amp;&amp; grep -qiE 'no.*latest.*algorithm|NO global.*latest' docs/workflow/v1/immutability.md</automated>
  </verify>
  <done>
    File `docs/workflow/v1/immutability.md` exists. It opens with `# V1 Immutability and Reference Resolution` and `**Codifies:** D39, D40, D41, D44, D49`. It contains: an explicit "NEVER edited" / "immutable" rule for emitted artifacts; an explicit `.wip/` editable-until-emission exception; an explicit forbidden list (`Supersedes:`, `Alternative to:`, `Forked from:`); an explicit "ask the user" rule for ambiguous references; an explicit "no global latest-artifact algorithm" statement. Length is approximately 60-90 lines. No YAML frontmatter.
  </done>
</task>

</tasks>

<verification>
- `docs/workflow/v1/filename-grammar.md` exists with all six required D-IDs cited in the header.
- `docs/workflow/v1/immutability.md` exists with all five required D-IDs cited in the header.
- Both files match the section-heading style established by `thread-layout.md` (Plan 01).
- Plan 01's `README.md` forward references to `./filename-grammar.md` and `./immutability.md` now resolve (both files exist on disk).
- The recognized V1 artifact-type tokens (proposal, spec, plan, discussion, decision-log, inbox-item, review-finding) appear in `filename-grammar.md`.
- The forbidden frontmatter fields (`Supersedes:`, `Alternative to:`, `Forked from:`) are explicitly named in `immutability.md`.
</verification>

<success_criteria>
Phase 1's success criterion #3 (filename grammar legibility from name alone) and #5 (explicit immutability + lineage-frontmatter-forbidden + ask-not-latest statements) are satisfied by these two docs together with Plan 01. THRD-04 (record form), THRD-05 (versioned form + target-version semantics), THRD-06 (emitted immutable + drafts editable), THRD-07 (no source-relation frontmatter), THRD-08 (ambiguous → ask) are all addressed.

The canonical V1 workflow reference doc tree is now complete: `README.md` + `thread-layout.md` + `filename-grammar.md` + `immutability.md`. Plan 03 will wire `.gitignore`, `marketplace.json`, and `AGENTS.md` to reference this tree.
</success_criteria>

<output>
Create `.planning/phases/01-foundations/01-02-SUMMARY.md` when done. Record:
- Files created: `docs/workflow/v1/filename-grammar.md`, `docs/workflow/v1/immutability.md`
- Decision IDs codified: D11, D12, D39, D40, D41, D42, D43, D44, D46, D47, D49
- Requirements addressed: THRD-04, THRD-05, THRD-06, THRD-07, THRD-08
- Reference doc tree now complete: Plan 01's `README.md` forward references both files; both targets exist.
</output>
