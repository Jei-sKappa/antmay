---
phase: 01-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/workflow/v1/README.md
  - docs/workflow/v1/thread-layout.md
autonomous: true
requirements:
  - THRD-01
  - THRD-02

must_haves:
  truths:
    - "User can open `docs/workflow/v1/README.md` and find links to each rules doc plus a one-line description of each"
    - "User can read `docs/workflow/v1/thread-layout.md` and learn the canonical V1 thread root path `docs/threads/<YYMMDDHHMMSSZ-slug>/`"
    - "User can read `docs/workflow/v1/thread-layout.md` and learn the exact V1 folder set: `proposals/`, `specs/`, `plans/`, `discussions/`, `inbox/{open,processed,dropped}/`, `.wip/` — no others"
    - "Reader understands V1 thread folders are created on-demand when an artifact lands, not pre-created"
  artifacts:
    - path: "docs/workflow/v1/README.md"
      provides: "Index of V1 workflow reference docs"
      contains: "thread-layout.md"
    - path: "docs/workflow/v1/thread-layout.md"
      provides: "V1 thread root path + V1 folder set codification"
      contains: "YYMMDDHHMMSSZ"
  key_links:
    - from: "docs/workflow/v1/README.md"
      to: "docs/workflow/v1/thread-layout.md"
      via: "relative markdown link"
      pattern: "thread-layout\\.md"
    - from: "docs/workflow/v1/thread-layout.md"
      to: "D7, D107 source decisions"
      via: "Codifies header"
      pattern: "Codifies.*D7.*D107"
---

<objective>
Create the V1 workflow reference doc tree at `docs/workflow/v1/` with two seed files: an index (`README.md`) and the thread layout codification (`thread-layout.md`). This is Wave 1 of Phase 1 — it establishes the canonical doc location that all subsequent rules docs and the AGENTS.md pointer (Plan 03) will reference.

Purpose: Without the thread layout doc, no downstream V1 spine skill can cite where to put proposal/spec/plan/discussion/inbox artifacts without re-deriving the folder set from D107. Without the index, the AGENTS.md pointer in Plan 03 has nothing concrete to link to.

Output: Two markdown files. The `README.md` is a discoverability surface (links + one-line per doc). The `thread-layout.md` is the single source of truth for the V1 thread root path and folder set, codifying D7 and D107.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundations/01-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@AGENTS.md

# Source decision log (read by executor — D7 and D107 anchor this plan)
@docs/threads/260520095223Z-agentic-workflow/discussions/260518200115Z-agentic-workflow-design-discussion.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create `docs/workflow/v1/thread-layout.md` codifying D7 and D107</name>
  <files>docs/workflow/v1/thread-layout.md</files>
  <read_first>
    - .planning/phases/01-foundations/01-CONTEXT.md (locked decisions: doc location, doc organization, on-demand folder rule)
    - .planning/codebase/CONVENTIONS.md (match voice: short, declarative, example-heavy)
    - AGENTS.md (section heading and prose style)
    - docs/threads/260520095223Z-agentic-workflow/discussions/260518200115Z-agentic-workflow-design-discussion.md (locate D7 and D107 for direct citation)
  </read_first>
  <action>
    Create `docs/workflow/v1/thread-layout.md` (creating the parent directory `docs/workflow/v1/` in the process). The file MUST:

    Open with a level-1 title `# V1 Thread Layout` followed immediately by a `**Codifies:** D7, D107` line on its own line. No other prefix.

    Include these section headings in this order (use `##` for each):
    1. `## Thread Root` — state the exact path pattern `docs/threads/<YYMMDDHHMMSSZ-slug>/` as the durable artifact root for every V1 thread. Give one realistic example (e.g. the literal path `docs/threads/260520095223Z-agentic-workflow/` from this repo). Cite D7 inline as `[D7]`.
    2. `## Folder Set` — list the exact V1 folders. Render as a fenced ```text block (NOT a markdown bullet list — the tree is a single artifact). Tree MUST contain exactly these entries (no extras, no aliases, no `reviews/` / `verifications/` / `merges/` / `adrs/` folders — D107 explicitly excludes them):
       ```
       docs/threads/<YYMMDDHHMMSSZ-slug>/
       |-- proposals/
       |-- specs/
       |-- plans/
       |-- discussions/
       |-- inbox/
       |   |-- open/
       |   |-- processed/
       |   `-- dropped/
       `-- .wip/
       ```
       After the tree, add a one-sentence description per folder explaining what artifact type lands there (e.g. "`proposals/` — emitted proposal artifacts written by the `propose-*` skills."). Cite D107 inline as `[D107]`.
    3. `## On-Demand Creation` — explicitly state that V1 thread folders are NOT pre-created proactively. A folder appears only when a skill writes its first artifact there. Reference D107 by citing the on-demand discussion explicitly: "Folders are created on-demand when an artifact lands; an empty thread is just a thread root + whatever folders the workflow has touched."
    4. `## Excluded Folder Names` — name the explicitly-rejected folders from D107 and route them to their V1 destinations: `reviews/` → `inbox/open/`; `verifications/` → `inbox/open/`; `merges/` → target artifact type's folder; `adrs/` → `discussions/`. State that no V1 skill writes into excluded folder names.
    5. `## Companion Docs` — link `./filename-grammar.md` and `./immutability.md` with one-line descriptions of each (these files are created by Plan 02 — the links target their final paths).

    Target length: ~50-90 lines. Tone: dense, declarative, example-heavy, matching `.planning/codebase/CONVENTIONS.md`. Use realistic UTC stamps (`260520095223Z`, `260518200115Z`) in examples — do NOT invent fictional ones; reuse the ones already present in `docs/threads/`.

    Do NOT include rules from `filename-grammar.md` or `immutability.md` here — keep concerns separated per the locked Doc Organization decision.

    Do NOT add YAML frontmatter — this is a reference doc, not a skill.
  </action>
  <verify>
    <automated>test -f docs/workflow/v1/thread-layout.md &amp;&amp; grep -F 'Codifies:' docs/workflow/v1/thread-layout.md | grep -q 'D7' &amp;&amp; grep -F 'Codifies:' docs/workflow/v1/thread-layout.md | grep -q 'D107' &amp;&amp; grep -qF 'YYMMDDHHMMSSZ' docs/workflow/v1/thread-layout.md &amp;&amp; grep -qF 'proposals/' docs/workflow/v1/thread-layout.md &amp;&amp; grep -qF 'specs/' docs/workflow/v1/thread-layout.md &amp;&amp; grep -qF 'plans/' docs/workflow/v1/thread-layout.md &amp;&amp; grep -qF 'discussions/' docs/workflow/v1/thread-layout.md &amp;&amp; grep -qF 'inbox/' docs/workflow/v1/thread-layout.md &amp;&amp; grep -qF '.wip/' docs/workflow/v1/thread-layout.md &amp;&amp; grep -qF 'open/' docs/workflow/v1/thread-layout.md &amp;&amp; grep -qF 'processed/' docs/workflow/v1/thread-layout.md &amp;&amp; grep -qF 'dropped/' docs/workflow/v1/thread-layout.md &amp;&amp; grep -qF 'on-demand' docs/workflow/v1/thread-layout.md &amp;&amp; grep -qF '## Excluded Folder Names' docs/workflow/v1/thread-layout.md &amp;&amp; grep -A10 '## Excluded Folder Names' docs/workflow/v1/thread-layout.md | grep -qF 'reviews/'</automated>
  </verify>
  <done>
    File `docs/workflow/v1/thread-layout.md` exists. It opens with `# V1 Thread Layout` and a `**Codifies:** D7, D107` line. It contains every required folder name (proposals/specs/plans/discussions/inbox/{open,processed,dropped}/.wip), the literal token `YYMMDDHHMMSSZ`, an "on-demand" creation statement, and a section that lists explicitly-rejected folder names from D107. Length is approximately 50-90 lines. No YAML frontmatter.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create `docs/workflow/v1/README.md` as the index of V1 workflow reference docs</name>
  <files>docs/workflow/v1/README.md</files>
  <read_first>
    - docs/workflow/v1/thread-layout.md (just created — confirm path + title for accurate linking)
    - .planning/phases/01-foundations/01-CONTEXT.md (Doc Organization decision: index links each rules doc + names entry points)
    - .planning/codebase/CONVENTIONS.md (voice match)
  </read_first>
  <action>
    Create `docs/workflow/v1/README.md` as the index entry point for the V1 workflow reference doc set.

    Required structure:
    1. Level-1 title: `# V1 Workflow Reference`.
    2. A short opening paragraph (1-3 sentences) stating: this directory is the single source of truth for the V1 modular agentic workflow's thread storage contract; every V1 spine skill cites these docs rather than re-defining rules; this set is versioned at `v1` so a future `v2/` set can coexist without disturbing V1 readers.
    3. A section `## Docs` containing a markdown bulleted list with one bullet per file, in this exact order:
       - `[thread-layout.md](./thread-layout.md)` — V1 thread root path and folder set (`proposals/`, `specs/`, `plans/`, `discussions/`, `inbox/{open,processed,dropped}/`, `.wip/`). Created by Plan 01.
       - `[filename-grammar.md](./filename-grammar.md)` — Record-form and versioned-form filename grammars, UTC stamp pattern, mandatory artifact-type suffix. Created by Plan 02.
       - `[immutability.md](./immutability.md)` — Emitted-artifact immutability rule, no source-relation frontmatter, ambiguous reference resolution by asking. Created by Plan 02.
    4. A section `## Entry Points` (1-2 sentences) stating: agents looking for V1 rules should start here; downstream V1 skill bodies inline a one-sentence summary of any rule they invoke AND link to the canonical doc (per the V1 Skill Rule Reuse Convention in 01-CONTEXT.md).
    5. A section `## Versioning` (1-2 sentences) stating: this set is V1; a future V2 ruleset lives at `docs/workflow/v2/` and does not replace V1 in place (immutability applies to rules docs as well — by convention, not by enforcement).

    Target length: ~30-50 lines. The links to `filename-grammar.md` and `immutability.md` are intentionally forward references — Plan 02 creates those files. Mark each bullet's "Created by Plan NN" suffix so a reader running through Phase 1 incrementally understands what's already on disk.

    Do NOT add YAML frontmatter.
  </action>
  <verify>
    <automated>test -f docs/workflow/v1/README.md &amp;&amp; grep -qF 'thread-layout.md' docs/workflow/v1/README.md &amp;&amp; grep -qF 'filename-grammar.md' docs/workflow/v1/README.md &amp;&amp; grep -qF 'immutability.md' docs/workflow/v1/README.md &amp;&amp; grep -qF '# V1 Workflow Reference' docs/workflow/v1/README.md &amp;&amp; grep -qF '## Docs' docs/workflow/v1/README.md</automated>
  </verify>
  <done>
    File `docs/workflow/v1/README.md` exists. It contains a title `# V1 Workflow Reference`, a `## Docs` section listing all three rules docs (`thread-layout.md`, `filename-grammar.md`, `immutability.md`) as relative markdown links, plus `## Entry Points` and `## Versioning` sections. Length is approximately 30-50 lines. No YAML frontmatter.
  </done>
</task>

</tasks>

<verification>
- Directory `docs/workflow/v1/` exists.
- `docs/workflow/v1/thread-layout.md` opens with `# V1 Thread Layout` and `**Codifies:** D7, D107`.
- `docs/workflow/v1/thread-layout.md` contains every V1 folder name in a fenced tree block and an on-demand creation statement.
- `docs/workflow/v1/README.md` links to `./thread-layout.md`, `./filename-grammar.md`, and `./immutability.md` (the last two are forward references resolved by Plan 02).
- No YAML frontmatter in either file (these are reference docs, not skills).
</verification>

<success_criteria>
Phase 1's success criterion #1 ("canonical V1 folder set referenced from a single source of truth") is partially satisfied: the thread layout is now documented at the canonical location. THRD-01 (thread root path) and THRD-02 (V1 folder set) are addressed by `thread-layout.md`.

Downstream readers (Plan 02, Plan 03, future V1 spine skills) can now cite `docs/workflow/v1/thread-layout.md` without re-deriving the folder set.
</success_criteria>

<output>
Create `.planning/phases/01-foundations/01-01-SUMMARY.md` when done. Record:
- Files created: `docs/workflow/v1/README.md`, `docs/workflow/v1/thread-layout.md`
- Decision IDs codified: D7, D107
- Requirements addressed: THRD-01, THRD-02
- Forward references in README.md (`filename-grammar.md`, `immutability.md`) — both resolved by Plan 02
</output>
