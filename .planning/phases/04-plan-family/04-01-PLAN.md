---
phase: 04-plan-family
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/plan-loose-auto/SKILL.md
  - skills/plan-loose-interactive/SKILL.md
  - .claude-plugin/marketplace.json
  - .vscode/settings.json
  - README.md
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-06
  - PLAN-07
  - PLAN-08
  - PLAN-09

must_haves:
  truths:
    - "User can invoke `plan-loose-auto` from a spec/proposal/decision-log/issue/raw prompt and receive an emitted plan artifact under `docs/threads/<thread>/plans/<UTC>-v1-plan.md` whose tasks are sequential, isolated, and independently implementable + reviewable."
    - "User can invoke `plan-loose-interactive` and walk the input collaboratively, with the agent preserving the four anti-sycophancy markers from `discussion/SKILL.md`."
    - "User reading either skill body can verify: (a) the D59 sequential-isolated-independent contract is enforced; (b) the D60 prohibition on parallelization markers / wave numbers / dependency arrays is explicit; (c) the D61 self-review section exists; (d) the D62 NEVER-auto-commit phrase is present."
    - "User running `npx skills add Jei-sKappa/skills --skill plan-loose-auto` and `--skill plan-loose-interactive` after this plan ships finds both skills installable via the `JeisKappa-workflow` marketplace plugin."
  artifacts:
    - path: "skills/plan-loose-auto/SKILL.md"
      provides: "Autonomous loose-granularity plan generator skill"
      contains: "frontmatter with name=plan-loose-auto, version=1.0.0; ## Workflow section; ## Self-Review section; literal 'NEVER commits' phrase; literal 'sequential, isolated, independently' phrase"
    - path: "skills/plan-loose-interactive/SKILL.md"
      provides: "Collaborative loose-granularity plan skill"
      contains: "frontmatter with name=plan-loose-interactive, version=1.0.0; ## Anti-Sycophancy Stance section preserving all 4 markers verbatim; ## Workflow section; ## Self-Review section; literal 'NEVER commits' phrase"
    - path: ".claude-plugin/marketplace.json"
      provides: "Marketplace registration for both new skills"
      contains: "./skills/plan-loose-auto and ./skills/plan-loose-interactive entries under JeisKappa-workflow"
    - path: ".vscode/settings.json"
      provides: "Conventional-commit scopes for both new skills"
      contains: "plan-loose-auto and plan-loose-interactive entries in conventionalCommits.scopes, alphabetically sorted"
    - path: "README.md"
      provides: "Public install snippets for both new skills"
      contains: "### `plan-loose-auto` and ### `plan-loose-interactive` headings with `npx skills add` install commands"
  key_links:
    - from: "skills/plan-loose-auto/SKILL.md"
      to: "docs/workflow/v1/filename-grammar.md"
      via: "citation by absolute path in the skill body"
      pattern: "docs/workflow/v1/filename-grammar.md"
    - from: "skills/plan-loose-auto/SKILL.md"
      to: "docs/workflow/v1/thread-layout.md"
      via: "citation by absolute path in the skill body"
      pattern: "docs/workflow/v1/thread-layout.md"
    - from: "skills/plan-loose-auto/SKILL.md"
      to: "docs/workflow/v1/immutability.md"
      via: "citation by absolute path in the skill body"
      pattern: "docs/workflow/v1/immutability.md"
    - from: "skills/plan-loose-interactive/SKILL.md"
      to: "skills/discussion/SKILL.md"
      via: "anti-sycophancy stance carried verbatim (4 marker phrases)"
      pattern: "Disagree when you disagree|Do not treat pushback as correctness"
    - from: ".claude-plugin/marketplace.json"
      to: "skills/plan-loose-auto/ and skills/plan-loose-interactive/"
      via: "JeisKappa-workflow.skills array entries"
      pattern: "./skills/plan-loose-auto|./skills/plan-loose-interactive"
---

<objective>
Ship the loose-granularity plan-authoring pair: `plan-loose-auto` (autonomous from input → loose plan artifact) and `plan-loose-interactive` (collaborative walk → loose plan artifact). Both skills emit a versioned plan at `docs/threads/<thread>/plans/<UTC>-v<N>[-<descriptor>]-plan.md`. Loose plans use numbered tasks with brief 1–3 sentence descriptions per task. Both skills enforce the D59 sequential-isolated-independent contract, the D60 strict prohibition on parallelization markers, the D61 self-review pass before emission, and the D62 never-auto-commit rule. Register both skills in the three V1 touchpoints (`marketplace.json`, `.vscode/settings.json`, `README.md`).

Purpose: Phase 4 ships the V1 planning catalog. The loose-granularity pair is the first of three pairs; the strict pair (Plan 04-02) and the granularity-shifting pair (Plan 04-03) follow in later waves. Loose plans are NOT the "default" — per D58 loose vs strict is a user/context choice; each skill description states the trade-off rather than recommending one over the other.

Output: Two new skill folders + three registration files updated. All four CLAUDE.md touchpoints satisfied for each skill.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-plan-family/04-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@AGENTS.md

@docs/workflow/v1/README.md
@docs/workflow/v1/thread-layout.md
@docs/workflow/v1/filename-grammar.md
@docs/workflow/v1/immutability.md

@skills/propose-auto/SKILL.md
@skills/propose-interactive/SKILL.md
@skills/spec-auto/SKILL.md
@skills/spec-interactive/SKILL.md
@skills/discussion/SKILL.md

@.claude-plugin/marketplace.json
@.vscode/settings.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author skills/plan-loose-auto/SKILL.md</name>
  <read_first>
    - skills/propose-auto/SKILL.md (closest Phase 3 structural analog — pair-skill auto half; reuse its section ordering and tone)
    - skills/spec-auto/SKILL.md (Phase 3 versioned-form analog — reuse its 4-input-form pattern and filename grammar discussion)
    - .planning/phases/04-plan-family/04-CONTEXT.md (binding CONTEXT — every locked decision)
    - docs/workflow/v1/filename-grammar.md (versioned-form rules; plans use this grammar)
    - docs/workflow/v1/thread-layout.md (plans/ folder + on-demand creation)
    - docs/workflow/v1/immutability.md (emitted-plan immutability + ambiguous-reference resolution)
  </read_first>
  <files>skills/plan-loose-auto/SKILL.md</files>
  <action>
    Create the file with YAML frontmatter and a markdown body. Frontmatter required fields:
    - `name: plan-loose-auto` (MUST equal directory name)
    - `description:` a single sentence including BOTH (a) the "Use when…" trigger phrase and (b) explicit interaction mode (autonomous, no clarifying questions). Per DIST-05, every V1 skill description states its mode. Mention the sibling `plan-loose-interactive` as the alternative for collaborative authoring. Mention this is the LOOSE granularity (per D58, no "better" framing — name `plan-strict-auto` as the alternative if the user wants tighter handoff).
    - `metadata.author: https://github.com/Jei-sKappa`
    - `metadata.version: 1.0.0`

    Body sections (names at executor discretion; the BEHAVIORAL TOKENS below must appear somewhere in the body — section grouping is flexible):

    1. Opening clarifier paragraph naming the sibling `plan-loose-interactive` and the strict-granularity siblings (`plan-strict-auto`, `plan-strict-interactive`). State plainly that loose vs strict is a user/context choice per D58 — loose plans suit human-leaning implementers and exploratory work; strict plans suit agent-leaning implementers and tighter handoff. Do NOT recommend one over the other.

    2. **Inputs** section listing the FOUR accepted input forms (mirroring spec-auto): spec artifact path under `docs/threads/<thread>/specs/`, proposal artifact path under `docs/threads/<thread>/proposals/`, decision-log path under `docs/threads/<thread>/discussions/`, GitHub issue URL/identifier, raw user prompt. Document the ambiguity fallback: ASK the user per `docs/workflow/v1/immutability.md` Ambiguous Reference Resolution — no global "latest" algorithm.

    3. **Plan Artifact Contract** section enforcing D59 verbatim or near-verbatim:
       - Every task is SEQUENTIAL (numbered in execution order), ISOLATED (does not read/write state from other in-progress tasks in the same plan beyond what is explicitly captured), INDEPENDENTLY IMPLEMENTABLE (a single implementer can complete it in one sitting given the task's stated input), and INDEPENDENTLY REVIEWABLE (a reviewer can verify the task succeeded from observable evidence).
       - The literal phrase "sequential, isolated, independently implementable" (or `sequential, isolated, independently` as substring) MUST appear in this section.

    4. **No Parallelization** section (D60, PLAN-07) explicitly stating:
       - "V1 plans are sequential. Do not emit wave numbers, dependency arrays, or any parallelization markers — the implementation skills (Phase 5) execute tasks in plan order."
       - The literal phrase "NEVER" or "MUST NOT" prefixed to "wave numbers", "dependency arrays", "task graph", "parallelization markers" — at minimum one of each forbidden construct named with explicit prohibition language.
       - Cite D60 by name (e.g., "per D60") so readers know the constraint is project-level, not personal taste.

    5. **Loose Plan Body Shape** section describing the per-task fields for a LOOSE plan:
       - Numbered tasks with brief 1–3 sentence descriptions per task.
       - Each task has at minimum: an objective sentence and observable verification.
       - The executor MAY suggest a section heading scaffold (e.g., `## Goal`, `## Tasks`, `## Notes`) but it is NOT required — freeform markdown is acceptable provided every task satisfies the D59 contract.
       - Include a SHORT (≤ 10 lines) worked example of a tiny loose plan body. The example MUST contain only sequential numbered tasks; it MUST NOT contain wave numbers, depends_on arrays, [W1] markers, or any parallelization notation. This is the negative test of D60 — the example demonstrates the absence of forbidden constructs.

    6. **Self-Review** section (D61, PLAN-08) describing the four checks the agent runs IN-SESSION before writing the plan to disk:
       - Coherence: does the plan actually achieve the input artifact's goal?
       - Granularity fit: is loose appropriate for this input?
       - No under-splitting: each task is independently implementable?
       - No over-splitting: no trivial 1-line tasks bloating the plan?
       - State explicitly: self-review is a quality discipline; the emitted plan body does NOT contain a "self-review notes" section. The artifact is clean.

    7. **Workflow** section with numbered steps that mirror the spec-auto sequence: resolve thread → resolve and read the input → derive descriptor (first emission defaults to NO descriptor; `v1-plan.md` is the canonical first form) → capture UTC stamp at write time → draft body → run self-review → write the artifact → confirm path.

    8. **Filename and Folder** section restating the versioned-form grammar inline (one-sentence summary) and linking to `docs/workflow/v1/filename-grammar.md`. Canonical first-emission example: `260521120000Z-v1-plan.md`. Output folder: `docs/threads/<thread>/plans/`. The `plan` artifact-type suffix is MANDATORY.

    9. **Immutability** section (one paragraph) restating: emitted plans are immutable per `docs/workflow/v1/immutability.md`; revisions go to v2+; no source-relation YAML frontmatter; drafts under `.wip/` are editable until emission.

    10. **Commit Policy** section containing the LITERAL PHRASE "This skill NEVER commits the emitted plan automatically. Commits happen only if the surrounding session explicitly requests one." (or near-verbatim equivalent containing "NEVER commits" as a substring). Same pattern as `propose-auto` and `spec-auto`.

    Voice and discipline:
    - Dense, declarative, citation-first. Match Phase 2/3 tone.
    - Cite Phase 1 canonical docs by ABSOLUTE PATH (`docs/workflow/v1/<doc>.md`) on first invocation of any V1 rule.
    - Do NOT add a `## Tone` / `## Structure` / `## Output format` template — this is a generator skill (like spec-auto), not a deliverable skill.
    - Do NOT add a `## Anti-Sycophancy Stance` section — `plan-loose-auto` is the AUTO half (no clarifying questions, no interactive walk). The interactive sibling carries the stance.
    - Do NOT include parallelization markers, wave numbers, or dependency arrays in any example inside the body (D60 internal compliance).
  </action>
  <acceptance_criteria>
    Run these checks and ALL must pass:

    1. `test -f skills/plan-loose-auto/SKILL.md` — file exists.
    2. `grep -c "^name: plan-loose-auto$" skills/plan-loose-auto/SKILL.md` — exactly 1 match (frontmatter name).
    3. `grep -c "^  version: 1.0.0$" skills/plan-loose-auto/SKILL.md` — at least 1 match (frontmatter version under metadata).
    4. `grep -c "^  author: https://github.com/Jei-sKappa$" skills/plan-loose-auto/SKILL.md` — at least 1 match.
    5. `grep -c "Use when" skills/plan-loose-auto/SKILL.md` — at least 1 match (DIST-05 trigger phrase in description).
    6. `grep -c "plan-loose-interactive" skills/plan-loose-auto/SKILL.md` — at least 1 match (sibling reference).
    7. `grep -c "plan-strict" skills/plan-loose-auto/SKILL.md` — at least 1 match (strict-granularity reference per D58).
    8. `grep -c "docs/workflow/v1/filename-grammar.md" skills/plan-loose-auto/SKILL.md` — at least 1 match (Phase 1 citation).
    9. `grep -c "docs/workflow/v1/thread-layout.md" skills/plan-loose-auto/SKILL.md` — at least 1 match.
    10. `grep -c "docs/workflow/v1/immutability.md" skills/plan-loose-auto/SKILL.md` — at least 1 match.
    11. `grep -c "sequential, isolated, independently" skills/plan-loose-auto/SKILL.md` — at least 1 match (D59 contract phrase).
    12. `grep -c "NEVER commits" skills/plan-loose-auto/SKILL.md` — at least 1 match (D62 commit phrase).
    13. `grep -c -i "wave" skills/plan-loose-auto/SKILL.md` — at least 1 match (D60 prohibition must be discussed) AND `grep -E -i "(wave numbers?|wave markers?).*(MUST NOT|do not|never|forbid|prohibit)" skills/plan-loose-auto/SKILL.md` — at least 1 match (explicit prohibition language).
    14. `grep -c -i "dependency arrays\|depends_on\|task graph\|parallelization" skills/plan-loose-auto/SKILL.md` — at least 1 match (D60 forbidden constructs named).
    15. `grep -c "D60" skills/plan-loose-auto/SKILL.md` — at least 1 match (decision ID cited per CONTEXT specifics).
    16. `grep -c "Self-Review\|self-review" skills/plan-loose-auto/SKILL.md` — at least 1 match (D61 section).
    17. `grep -c "v1-plan.md\|-plan.md" skills/plan-loose-auto/SKILL.md` — at least 1 match (artifact-type suffix and first-version filename example).
    18. `grep -c "docs/threads/<thread>/plans/" skills/plan-loose-auto/SKILL.md` — at least 1 match (output folder path).
    19. The body must NOT contain a YAML frontmatter field beyond `name`, `description`, `metadata.author`, `metadata.version`. Verify: `awk '/^---$/{c++;next} c==1' skills/plan-loose-auto/SKILL.md | grep -v -E "^(name|description|metadata|  author|  version):" | grep -v "^$"` returns nothing.
    20. The body section that contains a worked example MUST NOT contain `[W1]`, `[W2]`, `wave:`, `depends_on:`, or any explicit parallelization notation. Verify: `grep -E "\[W[0-9]+\]|wave:|depends_on:" skills/plan-loose-auto/SKILL.md` returns nothing. (The body MAY contain the WORDS "wave" or "dependency" when describing what is FORBIDDEN; this check looks only for STRUCTURAL notation.)
  </acceptance_criteria>
  <verify>
    <automated>
      test -f skills/plan-loose-auto/SKILL.md && \
      grep -q "^name: plan-loose-auto$" skills/plan-loose-auto/SKILL.md && \
      grep -q "^  version: 1.0.0$" skills/plan-loose-auto/SKILL.md && \
      grep -q "sequential, isolated, independently" skills/plan-loose-auto/SKILL.md && \
      grep -q "NEVER commits" skills/plan-loose-auto/SKILL.md && \
      grep -q "D60" skills/plan-loose-auto/SKILL.md && \
      grep -qi "self-review" skills/plan-loose-auto/SKILL.md && \
      grep -q "docs/workflow/v1/filename-grammar.md" skills/plan-loose-auto/SKILL.md && \
      grep -q "docs/threads/<thread>/plans/" skills/plan-loose-auto/SKILL.md && \
      ! grep -qE "\[W[0-9]+\]|^wave:|^depends_on:" skills/plan-loose-auto/SKILL.md && \
      echo "plan-loose-auto SKILL.md acceptance passes"
    </automated>
  </verify>
  <done>skills/plan-loose-auto/SKILL.md exists with frontmatter + body covering: 4-input acceptance, D59 contract, D60 explicit prohibition, D61 self-review section, D62 NEVER-commit phrase, V1 filename grammar citation, plans/ folder routing, loose-granularity body shape with a parallelization-free worked example.</done>
</task>

<task type="auto">
  <name>Task 2: Author skills/plan-loose-interactive/SKILL.md</name>
  <read_first>
    - skills/propose-interactive/SKILL.md (closest Phase 3 structural analog — pair-skill interactive half; reuse anti-sycophancy stance carrying)
    - skills/spec-interactive/SKILL.md (Phase 3 versioned-form interactive analog — reuse 4-input pattern, walk structure, decision-log opt-in default)
    - skills/discussion/SKILL.md (anti-sycophancy stance source — 4 markers carried verbatim)
    - .planning/phases/04-plan-family/04-CONTEXT.md
    - docs/workflow/v1/filename-grammar.md
    - docs/workflow/v1/thread-layout.md
    - docs/workflow/v1/immutability.md
    - skills/plan-loose-auto/SKILL.md (Task 1 output — opening clarifier and section structure must align with the sibling)
  </read_first>
  <files>skills/plan-loose-interactive/SKILL.md</files>
  <action>
    Create the file with YAML frontmatter and a markdown body. Frontmatter required fields:
    - `name: plan-loose-interactive`
    - `description:` a single sentence with the "Use when…" trigger AND explicit interaction mode (collaborative, walks tasks one at a time). Mention the sibling `plan-loose-auto` and the strict-granularity siblings.
    - `metadata.author: https://github.com/Jei-sKappa`
    - `metadata.version: 1.0.0`

    Body sections (section names at executor discretion; behavior tokens below MUST appear):

    1. Opening clarifier paragraph naming the sibling `plan-loose-auto` and the strict-granularity siblings, restating that loose vs strict is a user/context choice per D58.

    2. **Anti-Sycophancy Stance** section. Carry the FOUR anti-sycophancy markers from `skills/discussion/SKILL.md` verbatim:
       - "Disagree when you disagree" clause (the user's leaning conflicts with evidence → say so plainly).
       - "Push back on weak or incomplete reasoning" clause.
       - "Take the user's input seriously" / "Do not treat pushback as correctness" pair — both clauses MUST appear.
       - "Refuse to log a [plan task] you believe is wrong without flagging it" clause — the implementer-phase-becomes-expensive framing applies (same as spec-interactive).
       The stance MAY be adapted to the planning context (e.g., "Refuse to log a plan task you believe is wrong without flagging it"), but the FOUR MARKER PHRASES must remain recognizable as substring matches against the discussion-skill original. Add a framing line: bad plan decisions become expensive in implementation — the V1 stance applies. The stance should also reference Phase 3's heightened framing for forward-direction artifacts (plans are downstream-consumed by an implementer who will not have the agent to ask follow-ups).

    3. **Inputs** section listing the FOUR input forms (same as plan-loose-auto Task 1) plus the ambiguity fallback.

    4. **Plan Artifact Contract** section enforcing D59 (sequential, isolated, independently implementable + reviewable). MUST contain the phrase "sequential, isolated, independently".

    5. **No Parallelization** section enforcing D60 — same prohibition language as plan-loose-auto. MUST name wave numbers, dependency arrays, parallelization markers as forbidden constructs, with explicit MUST NOT / NEVER / do not language. Cite D60.

    6. **Loose Plan Body Shape** section (per CONTEXT specifics, may share content with the auto skill or restate). Same 1–3 sentence per-task descriptions, same optional section scaffold, same SHORT worked example free of parallelization markers.

    7. **Self-Review** section (D61, PLAN-08) documenting the same 4 checks the auto skill runs (coherence, granularity fit, no under-splitting, no over-splitting), plus a clause stating the interactive walk closes WITH the self-review pass BEFORE emission. The walk produces drafts under `.wip/` (editable); the lock applies at write into `plans/`.

    8. **Workflow** section with numbered steps: resolve thread → resolve and read the input → walk the input task-by-task with the user (this is the COLLABORATIVE delta from the auto skill — present tasks as they emerge, push back per the anti-sycophancy stance, adjust granularity in conversation) → capture UTC stamp at write time → run self-review → write the artifact → confirm path.

    9. **Decision Log** section enforcing D93: this skill does NOT auto-write a separate decision log. A log is written ONLY if durable trade-offs / rejected alternatives emerge during the walk that cannot reasonably be captured in the plan body itself. When such a log IS warranted, write it to `docs/threads/<thread>/discussions/<UTC>-<kebab-desc>-decision-log.md` per the V1 record-form grammar, with sequential `## D<N>: <Title>` headings. Mirror spec-interactive's `## Decision Log` section. Default: ASK the user when in doubt.

    10. **Scope Drift** section mirroring spec-interactive's pattern: PARK as an Inbox item (via `capture-inbox`), SPLIT into its own plan or discussion thread, or DEFER. Ask the user which.

    11. **Filename and Folder** section restating the versioned-form grammar (one-paragraph summary) and citing `docs/workflow/v1/filename-grammar.md` by absolute path. Canonical first-emission example: `260521120000Z-v1-plan.md`. Output folder: `docs/threads/<thread>/plans/`.

    12. **Immutability** section restating: emitted plans are immutable per `docs/workflow/v1/immutability.md`; revisions go to v2+; drafts under `.wip/` are editable until emission.

    13. **Commit Policy** section containing the LITERAL PHRASE "This skill NEVER commits the emitted plan automatically. Commits happen only if the surrounding session explicitly requests one." (or near-verbatim equivalent with "NEVER commits" as a substring).

    Voice and discipline:
    - Dense, declarative, citation-first.
    - The four anti-sycophancy marker phrases MUST be present verbatim or near-verbatim (substring matches required by acceptance criteria below).
    - Do NOT include parallelization markers in any example inside the body.
  </action>
  <acceptance_criteria>
    Run these checks and ALL must pass:

    1. `test -f skills/plan-loose-interactive/SKILL.md`.
    2. `grep -c "^name: plan-loose-interactive$" skills/plan-loose-interactive/SKILL.md` — exactly 1.
    3. `grep -c "^  version: 1.0.0$" skills/plan-loose-interactive/SKILL.md` — at least 1.
    4. `grep -c "^  author: https://github.com/Jei-sKappa$" skills/plan-loose-interactive/SKILL.md` — at least 1.
    5. `grep -c "Use when" skills/plan-loose-interactive/SKILL.md` — at least 1.
    6. `grep -c "plan-loose-auto" skills/plan-loose-interactive/SKILL.md` — at least 1 (sibling ref).
    7. `grep -c "plan-strict" skills/plan-loose-interactive/SKILL.md` — at least 1 (strict-granularity ref).
    8. `grep -c "Anti-Sycophancy" skills/plan-loose-interactive/SKILL.md` — at least 1 (section heading).
    9. `grep -c "Disagree when you disagree" skills/plan-loose-interactive/SKILL.md` — at least 1 (marker 1).
    10. `grep -c -E "Push back on weak|Push back on (weak|incomplete)" skills/plan-loose-interactive/SKILL.md` — at least 1 (marker 2).
    11. `grep -c "Do not treat pushback as correctness" skills/plan-loose-interactive/SKILL.md` — at least 1 (marker 3).
    12. `grep -c -E "Refuse to log .* (you believe is wrong|without flagging)" skills/plan-loose-interactive/SKILL.md` — at least 1 (marker 4).
    13. `grep -c "docs/workflow/v1/filename-grammar.md" skills/plan-loose-interactive/SKILL.md` — at least 1.
    14. `grep -c "docs/workflow/v1/thread-layout.md" skills/plan-loose-interactive/SKILL.md` — at least 1.
    15. `grep -c "docs/workflow/v1/immutability.md" skills/plan-loose-interactive/SKILL.md` — at least 1.
    16. `grep -c "sequential, isolated, independently" skills/plan-loose-interactive/SKILL.md` — at least 1.
    17. `grep -c "NEVER commits" skills/plan-loose-interactive/SKILL.md` — at least 1.
    18. `grep -c "D60" skills/plan-loose-interactive/SKILL.md` — at least 1.
    19. `grep -c -E "wave numbers?|wave markers?" skills/plan-loose-interactive/SKILL.md` — at least 1 (forbidden construct named).
    20. `grep -c -E "dependency arrays?|depends_on|task graph" skills/plan-loose-interactive/SKILL.md` — at least 1.
    21. `grep -c -i "self-review" skills/plan-loose-interactive/SKILL.md` — at least 1.
    22. `grep -c -i "Decision Log\|decision log" skills/plan-loose-interactive/SKILL.md` — at least 1 (D93 section present).
    23. `grep -c "v1-plan.md\|-plan.md" skills/plan-loose-interactive/SKILL.md` — at least 1.
    24. `grep -c "docs/threads/<thread>/plans/" skills/plan-loose-interactive/SKILL.md` — at least 1.
    25. The worked example MUST NOT contain parallelization notation. `grep -E "\[W[0-9]+\]|^wave:|^depends_on:" skills/plan-loose-interactive/SKILL.md` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>
      test -f skills/plan-loose-interactive/SKILL.md && \
      grep -q "^name: plan-loose-interactive$" skills/plan-loose-interactive/SKILL.md && \
      grep -q "Anti-Sycophancy" skills/plan-loose-interactive/SKILL.md && \
      grep -q "Disagree when you disagree" skills/plan-loose-interactive/SKILL.md && \
      grep -qE "Push back on (weak|incomplete)" skills/plan-loose-interactive/SKILL.md && \
      grep -q "Do not treat pushback as correctness" skills/plan-loose-interactive/SKILL.md && \
      grep -qE "Refuse to log" skills/plan-loose-interactive/SKILL.md && \
      grep -q "sequential, isolated, independently" skills/plan-loose-interactive/SKILL.md && \
      grep -q "NEVER commits" skills/plan-loose-interactive/SKILL.md && \
      grep -q "D60" skills/plan-loose-interactive/SKILL.md && \
      grep -qi "self-review" skills/plan-loose-interactive/SKILL.md && \
      grep -q "docs/threads/<thread>/plans/" skills/plan-loose-interactive/SKILL.md && \
      ! grep -qE "\[W[0-9]+\]|^wave:|^depends_on:" skills/plan-loose-interactive/SKILL.md && \
      echo "plan-loose-interactive SKILL.md acceptance passes"
    </automated>
  </verify>
  <done>skills/plan-loose-interactive/SKILL.md exists with frontmatter + body covering: opening clarifier with sibling references, 4 anti-sycophancy markers verbatim, 4-input acceptance, D59 contract, D60 explicit prohibition with D60 citation, D61 self-review section, D62 NEVER-commit phrase, D93 decision-log opt-in default, scope-drift handling, V1 filename grammar citation, plans/ folder routing.</done>
</task>

<task type="auto">
  <name>Task 3: Register plan-loose-auto and plan-loose-interactive in marketplace, scopes, and README</name>
  <read_first>
    - .claude-plugin/marketplace.json (current state — 7 entries under JeisKappa-workflow)
    - .vscode/settings.json (current state — 15 alphabetically-sorted entries in conventionalCommits.scopes)
    - README.md (current state — 15 "Available skills" entries; Retired skills subsection at the bottom)
    - AGENTS.md (the 4-touchpoint checklist under "When adding a new skill")
    - .planning/phases/04-plan-family/04-CONTEXT.md
    - skills/propose-auto/SKILL.md (Phase 3 README description style — copy the trigger-sentence shape)
    - skills/propose-interactive/SKILL.md (Phase 3 README description style)
  </read_first>
  <files>.claude-plugin/marketplace.json, .vscode/settings.json, README.md</files>
  <action>
    Update all three registration files atomically (a single coherent change set covering both new skills):

    1. **`.claude-plugin/marketplace.json`** — under the `JeisKappa-workflow` plugin's `skills` array, ADD `"./skills/plan-loose-auto"` and `"./skills/plan-loose-interactive"`. Preserve alphabetical order within the array — entries currently run `capture-inbox`, `discussion`, `propose-auto`, `propose-interactive`, `seeded-discussion`, `spec-auto`, `spec-interactive`. The two new entries should be inserted in alphabetical position. After this change the JeisKappa-workflow array MUST contain exactly 9 entries (7 existing + 2 new). The `JeisKappa-skills` plugin array stays UNCHANGED at 8 entries. Preserve the existing JSON formatting (2-space indent, trailing newline, no trailing comma).

    2. **`.vscode/settings.json`** — add `"plan-loose-auto"` and `"plan-loose-interactive"` to the `conventionalCommits.scopes` array. Keep the array alphabetically sorted (currently 15 entries; new total = 17). The two new entries fall alphabetically between `meta-prompting` and `propose-auto`. Preserve JSON formatting and trailing newline.

    3. **`README.md`** — add two new "Available skills" entries (one per new skill), inserted AFTER the `spec-interactive` block and BEFORE the `## Retired skills` subsection. Each entry follows the established pattern (used by `propose-auto`, `propose-interactive`, `spec-auto`, `spec-interactive`):
       - `### [\`plan-loose-auto\`](./skills/plan-loose-auto/SKILL.md)` heading
       - A blank line, then a one-paragraph description mirroring the SKILL.md frontmatter `description` field (may be paraphrased slightly for README readability — same trigger phrase, same interaction mode, same sibling references).
       - A blank line, then a fenced code block with `npx skills add Jei-sKappa/skills --skill plan-loose-auto`.
       - Same shape for `plan-loose-interactive` directly below.

    Do NOT touch the `## Retired skills` subsection — it stays as is. Do NOT touch any entry above `spec-interactive`. Preserve the existing trailing newline at end of README.md.

    Do NOT commit. Per D62 / PLAN-09 and the project rule "Never commit unless explicitly asked to do so" — this plan emits files but does not commit.
  </action>
  <acceptance_criteria>
    Run these checks and ALL must pass:

    1. `jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length == 9' .claude-plugin/marketplace.json` returns true.
    2. `jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/plan-loose-auto")' .claude-plugin/marketplace.json` returns a non-null integer.
    3. `jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/plan-loose-interactive")' .claude-plugin/marketplace.json` returns a non-null integer.
    4. `jq -e '.plugins[] | select(.name=="JeisKappa-skills") | .skills | length == 8' .claude-plugin/marketplace.json` returns true (untouched).
    5. `jq -e '.conventionalCommits.scopes | length == 17' .vscode/settings.json` returns true.
    6. `jq -e '.conventionalCommits.scopes | index("plan-loose-auto")' .vscode/settings.json` returns a non-null integer.
    7. `jq -e '.conventionalCommits.scopes | index("plan-loose-interactive")' .vscode/settings.json` returns a non-null integer.
    8. `jq -e '.conventionalCommits.scopes == (.conventionalCommits.scopes | sort)' .vscode/settings.json` returns true (alphabetically sorted).
    9. `grep -c "^### \[\`plan-loose-auto\`\]" README.md` — exactly 1.
    10. `grep -c "^### \[\`plan-loose-interactive\`\]" README.md` — exactly 1.
    11. `grep -c "npx skills add Jei-sKappa/skills --skill plan-loose-auto" README.md` — exactly 1.
    12. `grep -c "npx skills add Jei-sKappa/skills --skill plan-loose-interactive" README.md` — exactly 1.
    13. `awk '/^### \[`plan-loose-(auto|interactive)`\]/{print NR}' README.md` — both line numbers fall BETWEEN the `spec-interactive` heading line and the `## Retired skills` heading line. Verify by: `bash -c 'A=$(grep -n "^### \\[\`spec-interactive\`\\]" README.md | cut -d: -f1); B=$(grep -n "^## Retired skills" README.md | cut -d: -f1); C=$(grep -n "^### \\[\`plan-loose-auto\`\\]" README.md | cut -d: -f1); D=$(grep -n "^### \\[\`plan-loose-interactive\`\\]" README.md | cut -d: -f1); [[ $C -gt $A && $C -lt $B && $D -gt $A && $D -lt $B ]] && echo OK'` prints OK.
    14. `git status --porcelain | wc -l` shows ≥ 3 modified files but no commit was created. `git log -1 --pretty=%s` should show the SAME most-recent commit message as before this task ran (Task 3 must not commit).
  </acceptance_criteria>
  <verify>
    <automated>
      jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length == 9' .claude-plugin/marketplace.json >/dev/null && \
      jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/plan-loose-auto")' .claude-plugin/marketplace.json >/dev/null && \
      jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/plan-loose-interactive")' .claude-plugin/marketplace.json >/dev/null && \
      jq -e '.plugins[] | select(.name=="JeisKappa-skills") | .skills | length == 8' .claude-plugin/marketplace.json >/dev/null && \
      jq -e '.conventionalCommits.scopes | length == 17' .vscode/settings.json >/dev/null && \
      jq -e '.conventionalCommits.scopes | index("plan-loose-auto")' .vscode/settings.json >/dev/null && \
      jq -e '.conventionalCommits.scopes | index("plan-loose-interactive")' .vscode/settings.json >/dev/null && \
      jq -e '.conventionalCommits.scopes == (.conventionalCommits.scopes | sort)' .vscode/settings.json >/dev/null && \
      [ "$(grep -c '^### \[`plan-loose-auto`\]' README.md)" = "1" ] && \
      [ "$(grep -c '^### \[`plan-loose-interactive`\]' README.md)" = "1" ] && \
      [ "$(grep -c 'npx skills add Jei-sKappa/skills --skill plan-loose-auto' README.md)" = "1" ] && \
      [ "$(grep -c 'npx skills add Jei-sKappa/skills --skill plan-loose-interactive' README.md)" = "1" ] && \
      echo "Registration acceptance passes"
    </automated>
  </verify>
  <done>marketplace.json JeisKappa-workflow array has 9 entries (alphabetically positioned), .vscode/settings.json scopes has 17 entries (alphabetically sorted), README.md has new install blocks inserted between spec-interactive and Retired skills sections. No git commit created.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

1. **Skill files exist with correct frontmatter:**
   ```
   ls -la skills/plan-loose-auto/SKILL.md skills/plan-loose-interactive/SKILL.md
   ```

2. **D59/D60/D61/D62 contracts enforced in both new skills:**
   ```bash
   for SKILL in skills/plan-loose-auto/SKILL.md skills/plan-loose-interactive/SKILL.md; do
     grep -q "sequential, isolated, independently" "$SKILL" || echo "FAIL D59 in $SKILL"
     grep -q "D60" "$SKILL" || echo "FAIL D60 citation in $SKILL"
     grep -qi "self-review" "$SKILL" || echo "FAIL D61 in $SKILL"
     grep -q "NEVER commits" "$SKILL" || echo "FAIL D62 in $SKILL"
   done
   ```

3. **Anti-sycophancy markers in interactive skill:**
   ```bash
   for MARK in "Disagree when you disagree" "Do not treat pushback as correctness" "Refuse to log"; do
     grep -q "$MARK" skills/plan-loose-interactive/SKILL.md || echo "FAIL marker: $MARK"
   done
   grep -qE "Push back on (weak|incomplete)" skills/plan-loose-interactive/SKILL.md || echo "FAIL marker: Push back"
   ```

4. **Four-touchpoint registration complete:**
   ```bash
   jq '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length' .claude-plugin/marketplace.json    # expect 9
   jq '.conventionalCommits.scopes | length' .vscode/settings.json                                              # expect 17
   grep -c "plan-loose-" README.md                                                                              # expect >= 4 (2 headings + 2 install commands)
   ```

5. **No parallelization markers in plan-loose skill bodies (D60 self-compliance in examples):**
   ```bash
   ! grep -qE "\[W[0-9]+\]|^wave:|^depends_on:" skills/plan-loose-auto/SKILL.md
   ! grep -qE "\[W[0-9]+\]|^wave:|^depends_on:" skills/plan-loose-interactive/SKILL.md
   ```
</verification>

<success_criteria>
- skills/plan-loose-auto/SKILL.md and skills/plan-loose-interactive/SKILL.md exist with correct V1 frontmatter.
- Both skills enforce the D59 sequential-isolated-independent contract (substring "sequential, isolated, independently" present).
- Both skills explicitly cite D60 and prohibit wave numbers + dependency arrays + parallelization markers.
- Both skills have a `## Self-Review` (or equivalent) section documenting the 4 D61 checks.
- Both skills contain the literal phrase "NEVER commits" per D62.
- plan-loose-interactive carries all 4 anti-sycophancy markers from discussion/SKILL.md verbatim.
- plan-loose-interactive enforces D93 (no auto-write of separate decision log by default).
- marketplace.json JeisKappa-workflow array has exactly 9 entries with both new skills added in alphabetical order.
- .vscode/settings.json conventionalCommits.scopes has exactly 17 entries, alphabetically sorted.
- README.md has 2 new entries inserted between `spec-interactive` and `## Retired skills`.
- No commit created during this plan (per D62 and the project commit rule).
</success_criteria>

<output>
Create `.planning/phases/04-plan-family/04-01-SUMMARY.md` when done.
</output>
