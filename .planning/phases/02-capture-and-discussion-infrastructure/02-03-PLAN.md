---
phase: 02-capture-and-discussion-infrastructure
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - skills/discussion-loop/SKILL.md
  - .claude-plugin/marketplace.json
  - .vscode/settings.json
  - README.md
autonomous: true
requirements:
  - DISC-09

must_haves:
  truths:
    - "User reading `skills/discussion-loop/SKILL.md` sees a short deprecation notice (no Setup / Loop / Logging body) naming `discussion` and `seeded-discussion` as replacements with install snippets, and noting that pre-existing `docs/discussions/*-discussion.md` logs from the legacy skill remain valid and require no migration"
    - "User running `npx skills list` sees NO `discussion-loop` entry under `JeisKappa Skills` — the legacy plugin entry is removed"
    - "User checking `.vscode/settings.json` finds `discussion-loop` removed from `conventionalCommits.scopes` — no future commit should scope to this retired skill"
    - "User reading `README.md` sees the `discussion-loop` entry removed from `## Available skills` and a new short `## Retired skills` subsection naming `discussion-loop`, pointing to `discussion` and `seeded-discussion`, and dated 2026-05-21"
    - "User running `ls skills/discussion-loop/` finds the folder still exists (soft retire) with `SKILL.md` present — the directory is not deleted, just the skill body is rewritten and the marketplace advertisement is dropped"
    - "User reading the retirement notice in `skills/discussion-loop/SKILL.md` understands the legacy log shape (`docs/discussions/<YYYY-MM-DD>-<topic>-<purpose>-discussion.md`) is unchanged; the notice does NOT instruct a migration of those files"
  artifacts:
    - path: "skills/discussion-loop/SKILL.md"
      provides: "Short deprecation notice pointing legacy users to `discussion` and `seeded-discussion`"
      contains: "retired"
    - path: ".claude-plugin/marketplace.json"
      provides: "`./skills/discussion-loop` REMOVED from `JeisKappa-skills.skills`"
      contains: "JeisKappa-skills"
    - path: ".vscode/settings.json"
      provides: "`discussion-loop` REMOVED from `conventionalCommits.scopes`"
      contains: "conventionalCommits.scopes"
    - path: "README.md"
      provides: "`discussion-loop` REMOVED from `## Available skills`; new `## Retired skills` subsection added"
      contains: "## Retired skills"
  key_links:
    - from: "skills/discussion-loop/SKILL.md (retirement notice)"
      to: "skills/discussion/SKILL.md"
      via: "named reference + install snippet"
      pattern: "discussion"
    - from: "skills/discussion-loop/SKILL.md (retirement notice)"
      to: "skills/seeded-discussion/SKILL.md"
      via: "named reference + install snippet"
      pattern: "seeded-discussion"
    - from: "README.md `## Retired skills`"
      to: "skills/discussion/SKILL.md and skills/seeded-discussion/SKILL.md"
      via: "named pointer in the retired-entry's description"
      pattern: "(discussion|seeded-discussion)"
---

<objective>
Retire the legacy `discussion-loop` skill cleanly: rewrite its `SKILL.md` body to a short deprecation notice pointing users at the two replacements (`discussion` for open-ended interviews; `seeded-discussion` for predetermined point lists), and pull its registration from all three places that previously advertised it — `marketplace.json`, `.vscode/settings.json`, and the `## Available skills` section of `README.md`. Add a one-line `## Retired skills` subsection to `README.md` that names `discussion-loop`, dates the retirement (2026-05-21), and points at the replacements. This is a SOFT retire — the skill folder stays on disk so any consumer that has previously installed it does not see a hard 404; only the advertisement and the active behavior are removed.

Purpose: Phase 2's CONTEXT.md and ROADMAP.md both lock the migration: `discussion-loop` is REPLACED by `discussion` + `seeded-discussion`. Leaving the legacy skill advertised in the marketplace would cause new installs of the deprecated behavior and split V1 discussion outputs across two non-isomorphic shapes (legacy `docs/discussions/<YYYY-MM-DD>-<topic>-<purpose>-discussion.md` vs. V1 `docs/threads/<thread>/discussions/<UTC>-<kebab-desc>-decision-log.md`). Removing the scope entry prevents accidental `feat(discussion-loop):` commits in the new world. The retirement notice itself does the migration handoff at the moment a curious user opens the legacy skill body.

Output: One rewritten file (`skills/discussion-loop/SKILL.md` — body replaced; frontmatter version bumped per CONVENTIONS.md "bump on any meaningful behavioral change"); three edits to registration files (one removal each, plus one new subsection in README.md). No new files; no folder deletion.

Depends on Plan 02-01 (`capture-inbox`) and Plan 02-02 (`discussion` + `seeded-discussion`) because (a) the retirement notice references `discussion` and `seeded-discussion` by name and assumes both exist on disk, (b) shares the three registration files those plans also edited (`marketplace.json`, `.vscode/settings.json`, `README.md`) — sequential serialization required.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-capture-and-discussion-infrastructure/02-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@AGENTS.md

# Legacy skill body being rewritten
@skills/discussion-loop/SKILL.md

# Replacements referenced from the retirement notice (must exist on disk per Plans 02-01/02-02)
@skills/capture-inbox/SKILL.md
@skills/discussion/SKILL.md
@skills/seeded-discussion/SKILL.md

# Registration files being edited
@.claude-plugin/marketplace.json
@.vscode/settings.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite `skills/discussion-loop/SKILL.md` as a short deprecation notice</name>
  <files>skills/discussion-loop/SKILL.md</files>
  <read_first>
    - skills/discussion-loop/SKILL.md (current full contents — captures the legacy frontmatter so the version bump is correct)
    - .planning/phases/02-capture-and-discussion-infrastructure/02-CONTEXT.md (LOCKED decisions under `### Legacy `discussion-loop` retirement` AND `<specifics>` section: notice should be SHORT (~10–20 lines))
    - skills/discussion/SKILL.md (PLAN 02-02 OUTPUT — verify `name: discussion` matches the reference)
    - skills/seeded-discussion/SKILL.md (PLAN 02-02 OUTPUT — verify `name: seeded-discussion` matches the reference)
    - .planning/codebase/CONVENTIONS.md (`## Version Bumping`: bump on any meaningful behavioral change — retirement qualifies)
  </read_first>
  <action>
    Rewrite `skills/discussion-loop/SKILL.md` ENTIRELY (overwrite with the Write tool — the new body replaces the old Setup / Loop / Logging / Finish structure). Preserve the directory; only the file contents change.

    REQUIRED FRONTMATTER (modified from the existing 1.1.0):
    - `name: discussion-loop` (UNCHANGED — folder name must still match)
    - `description:` REWRITTEN to a single sentence stating the skill is RETIRED and naming the replacements. Example shape: "RETIRED — replaced by the `discussion` skill (open-ended interviews) and the `seeded-discussion` skill (predetermined point walks). Use when you find this skill in a legacy install and need to know what to install instead."
    - `metadata.author: https://github.com/Jei-sKappa` (UNCHANGED)
    - `metadata.version: 2.0.0` (MAJOR bump — behavior fundamentally changed from "interactive discussion loop" to "deprecation notice"; per CONVENTIONS.md "bump on any meaningful behavioral change" and SemVer convention major bump for breaking behavior change)

    REQUIRED BODY (target length: 10–20 lines per CONTEXT.md `<specifics>`):

    The body MUST contain (in any order, executor chooses the prose flow):

    1. A `#` heading or top paragraph explicitly stating the skill is RETIRED. The literal word `retired` MUST appear at least once.

    2. A short statement (1–3 sentences) explaining WHY: this skill was the V1 source of `discussion` and `seeded-discussion`; the behavior was split into two named skills aligned with the V1 thread layout.

    3. A pointer to BOTH replacements. The literal skill names `discussion` and `seeded-discussion` MUST appear. Each replacement MUST include its install snippet inside an `sh` code fence:
       - One snippet for `npx skills add Jei-sKappa/skills --skill discussion`
       - One snippet for `npx skills add Jei-sKappa/skills --skill seeded-discussion`
       Each pointer MUST include a one-line description of when to pick that replacement: `discussion` for open-ended interviews where questions are discovered live; `seeded-discussion` for walking a predetermined point list with options + recommendation default-on.

    4. A short note on existing legacy logs. MUST state (in agent-readable prose): pre-existing decision logs written by the legacy `discussion-loop` at `docs/discussions/*-discussion.md` remain valid; they require NO migration; do not move them, do not rewrite them. The new skills produce different artifacts at `docs/threads/<thread>/discussions/<UTC>-<kebab-desc>-decision-log.md` and the two log shapes coexist.

    5. A short pointer at the END to the V1 thread reference (e.g., "For V1 thread layout and filename grammar see `docs/workflow/v1/README.md`.") — this is optional but recommended; if included, the literal path `docs/workflow/v1/README.md` MUST be accurate.

    PROHIBITED CONTENT:
    - NO Setup / Loop / Logging / Finish sections (those were the legacy active-skill body; the file is now a notice).
    - NO instruction to follow the legacy workflow (e.g., do NOT say "first create a `docs/discussions/...` file" — that workflow is retired).
    - NO instruction to MIGRATE pre-existing legacy logs to the new thread layout (CONTEXT.md `<deferred>`: "no migration; pre-existing logs stay where they are").
    - NO frontmatter `Supersedes:` / `Alternative to:` / `Deprecated by:` field — per D44 / THRD-07 V1 forbids source-relation frontmatter on artifacts; SKILL.md frontmatter has its own schema (name/description/metadata) and adding new fields is prohibited per D4. The retirement information lives in the body and in the `description:`.
  </action>
  <verify>
    <automated>test -f skills/discussion-loop/SKILL.md &amp;&amp; grep -qxF 'name: discussion-loop' skills/discussion-loop/SKILL.md &amp;&amp; grep -qxF '  version: 2.0.0' skills/discussion-loop/SKILL.md &amp;&amp; grep -qxF '  author: https://github.com/Jei-sKappa' skills/discussion-loop/SKILL.md &amp;&amp; grep -qiF 'retired' skills/discussion-loop/SKILL.md &amp;&amp; grep -qF 'npx skills add Jei-sKappa/skills --skill discussion' skills/discussion-loop/SKILL.md &amp;&amp; grep -qF 'npx skills add Jei-sKappa/skills --skill seeded-discussion' skills/discussion-loop/SKILL.md &amp;&amp; grep -qF 'docs/discussions/' skills/discussion-loop/SKILL.md &amp;&amp; grep -qF 'docs/threads/' skills/discussion-loop/SKILL.md &amp;&amp; ! grep -qxF '## Setup' skills/discussion-loop/SKILL.md &amp;&amp; ! grep -qxF '## Loop' skills/discussion-loop/SKILL.md &amp;&amp; ! grep -qiE '(Supersedes:|Alternative to:|Deprecated by:|Forked from:)' skills/discussion-loop/SKILL.md &amp;&amp; [ "$(wc -l &lt; skills/discussion-loop/SKILL.md)" -le 60 ]</automated>
  </verify>
  <done>
    `skills/discussion-loop/SKILL.md` has the rewritten frontmatter (`name: discussion-loop` unchanged, `description:` rewritten to a retirement sentence, `metadata.version: 2.0.0`). The body explicitly states the skill is retired, names both replacements (`discussion`, `seeded-discussion`) with install snippets, references both the legacy log location (`docs/discussions/`) and the new thread log location (`docs/threads/`), and is short (≤ 60 total lines including frontmatter). The file no longer contains the legacy `## Setup` / `## Loop` sections, and contains no prohibited source-relation frontmatter. Verification grep checks all pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove `./skills/discussion-loop` from `.claude-plugin/marketplace.json` `JeisKappa-skills` plugin</name>
  <files>.claude-plugin/marketplace.json</files>
  <read_first>
    - .claude-plugin/marketplace.json (current contents AFTER Plans 02-01/02-02 — `JeisKappa-skills` still has 9 entries including `./skills/discussion-loop`; `JeisKappa-workflow.skills` has 3 entries)
    - .planning/phases/02-capture-and-discussion-infrastructure/02-CONTEXT.md (`### Legacy `discussion-loop` retirement` → Marketplace plugin decision: REMOVE the `./skills/discussion-loop` entry from `JeisKappa-skills`)
  </read_first>
  <action>
    Edit `.claude-plugin/marketplace.json` with the Edit tool. REMOVE the string `"./skills/discussion-loop"` (and the trailing comma on its line, if present) from the `JeisKappa-skills.skills` array. Leave the JeisKappa-skills entry's other 8 skill paths unchanged in their existing order. Leave `JeisKappa-workflow` completely unchanged (still has 3 entries: capture-inbox + discussion + seeded-discussion).

    Target final shape of the `JeisKappa-skills` plugin entry (8 entries; `./skills/discussion-loop` removed):

    ```json
        {
          "name": "JeisKappa-skills",
          "source": "./",
          "skills": [
            "./skills/afk-exploration",
            "./skills/brief-the-implementer",
            "./skills/consult-the-expert",
            "./skills/derive-spec",
            "./skills/meta-prompting",
            "./skills/report-to-the-owner",
            "./skills/review-decision-document",
            "./skills/the-librarian"
          ]
        }
    ```

    Critical requirements:
    - Output JSON MUST be valid (no dangling comma after removal).
    - JeisKappa-workflow plugin entry stays completely unchanged (3 entries: `./skills/capture-inbox`, `./skills/discussion`, `./skills/seeded-discussion`).
    - Do NOT delete `skills/discussion-loop/` from disk — soft retire keeps the folder.
    - Do NOT add `./skills/discussion-loop` to `JeisKappa-workflow` either — the skill is retired everywhere in the marketplace.
  </action>
  <verify>
    <automated>node -e "const j = JSON.parse(require('fs').readFileSync('.claude-plugin/marketplace.json', 'utf8')); if (j.plugins.length !== 2) { console.error('expected 2 plugins, got', j.plugins.length); process.exit(1); } const sk = j.plugins.find(p =&gt; p.name === 'JeisKappa-skills'); const wf = j.plugins.find(p =&gt; p.name === 'JeisKappa-workflow'); if (!sk || !wf) { console.error('both plugins required'); process.exit(1); } if (sk.skills.length !== 8) { console.error('JeisKappa-skills must now have 8 skills, got', sk.skills.length); process.exit(1); } if (sk.skills.includes('./skills/discussion-loop')) { console.error('discussion-loop must be REMOVED from JeisKappa-skills'); process.exit(1); } if (wf.skills.includes('./skills/discussion-loop')) { console.error('discussion-loop must NOT be added to JeisKappa-workflow either'); process.exit(1); } if (wf.skills.length !== 3) { console.error('JeisKappa-workflow must still have 3 skills, got', wf.skills.length); process.exit(1); } for (const p of ['./skills/capture-inbox', './skills/discussion', './skills/seeded-discussion']) { if (!wf.skills.includes(p)) { console.error('missing from JeisKappa-workflow:', p); process.exit(1); } } const expectedSk = ['./skills/afk-exploration', './skills/brief-the-implementer', './skills/consult-the-expert', './skills/derive-spec', './skills/meta-prompting', './skills/report-to-the-owner', './skills/review-decision-document', './skills/the-librarian']; for (const p of expectedSk) { if (!sk.skills.includes(p)) { console.error('JeisKappa-skills missing expected entry:', p); process.exit(1); } } console.log('OK');"</automated>
  </verify>
  <done>
    `.claude-plugin/marketplace.json` parses as valid JSON. `JeisKappa-skills.skills` has 8 entries and no longer includes `./skills/discussion-loop`. `JeisKappa-workflow.skills` is unchanged (3 entries). The folder `skills/discussion-loop/` is still present on disk (soft retire — verified by Task 1's existence check). Verification node script prints `OK`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove `discussion-loop` from `.vscode/settings.json` `conventionalCommits.scopes`</name>
  <files>.vscode/settings.json</files>
  <read_first>
    - .vscode/settings.json (current contents AFTER Plans 02-01/02-02 — 12 entries: afk-exploration → brief-the-implementer → capture-inbox → consult-the-expert → derive-spec → discussion → discussion-loop → meta-prompting → report-to-the-owner → review-decision-document → seeded-discussion → the-librarian)
    - .planning/phases/02-capture-and-discussion-infrastructure/02-CONTEXT.md (`### Legacy `discussion-loop` retirement` → Conventional commit scopes decision: REMOVE `discussion-loop`)
  </read_first>
  <action>
    Edit `.vscode/settings.json` with the Edit tool. REMOVE `"discussion-loop"` (and its trailing comma on that line, if present) from the `conventionalCommits.scopes` array. The remaining 11 entries must stay in their existing positions (alphabetical order preserved automatically since we are removing the entry between `discussion` and `meta-prompting`).

    Target shape of the array after the edit (11 entries):

    ```json
      "conventionalCommits.scopes": [
        "afk-exploration",
        "brief-the-implementer",
        "capture-inbox",
        "consult-the-expert",
        "derive-spec",
        "discussion",
        "meta-prompting",
        "report-to-the-owner",
        "review-decision-document",
        "seeded-discussion",
        "the-librarian"
      ]
    ```

    Critical requirements:
    - Output JSON MUST be valid.
    - `discussion-loop` MUST NOT be present in the array.
    - The other 11 scopes (including `discussion`, `seeded-discussion`, `capture-inbox`) MUST still be present and alphabetical.
    - Top-level keys (`conventionalCommits.gitmoji: false`, `conventionalCommits.promptFooter: false`) unchanged.
  </action>
  <verify>
    <automated>node -e "const j = JSON.parse(require('fs').readFileSync('.vscode/settings.json', 'utf8')); const scopes = j['conventionalCommits.scopes']; if (!Array.isArray(scopes)) { console.error('scopes must be array'); process.exit(1); } if (scopes.length !== 11) { console.error('expected 11 scopes, got', scopes.length); process.exit(1); } if (scopes.includes('discussion-loop')) { console.error('discussion-loop must be REMOVED'); process.exit(1); } for (const s of ['capture-inbox', 'discussion', 'seeded-discussion']) { if (!scopes.includes(s)) { console.error('missing scope:', s); process.exit(1); } } const sorted = [...scopes].sort((a,b) =&gt; a.localeCompare(b)); if (JSON.stringify(sorted) !== JSON.stringify(scopes)) { console.error('scopes NOT alphabetical:', scopes); process.exit(1); } if (j['conventionalCommits.gitmoji'] !== false || j['conventionalCommits.promptFooter'] !== false) { console.error('top-level keys disturbed'); process.exit(1); } console.log('OK');"</automated>
  </verify>
  <done>
    `.vscode/settings.json` parses as valid JSON. The `conventionalCommits.scopes` array has exactly 11 entries, no longer contains `discussion-loop`, still contains `capture-inbox` + `discussion` + `seeded-discussion`, and remains alphabetical. Top-level keys preserved. Verification node script prints `OK`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Remove `discussion-loop` entry from `## Available skills` and add `## Retired skills` subsection in `README.md`</name>
  <files>README.md</files>
  <read_first>
    - README.md (current contents AFTER Plans 02-01/02-02 — `## Available skills` has 12 entries; the `discussion-loop` block is the second entry; no `## Retired skills` section exists yet)
    - .planning/phases/02-capture-and-discussion-infrastructure/02-CONTEXT.md (`### Legacy `discussion-loop` retirement` → README.md decision: remove entry from Available skills; add short `## Retired skills` subsection naming `discussion-loop` and pointing to replacements; `<specifics>` "README.md `Retired skills` subsection format: one bullet per retired skill, with one-sentence description, replacement skill name(s), and the date of retirement")
    - skills/discussion-loop/SKILL.md (TASK 1 OUTPUT — confirm the retirement is in place)
  </read_first>
  <action>
    Edit `README.md` with the Edit tool. Two changes in a single file:

    CHANGE 1 — REMOVE the existing `### [`discussion-loop`](./skills/discussion-loop/SKILL.md)` block from `## Available skills`. Remove the full block: heading line, description paragraph, the `sh` code fence with `npx skills add Jei-sKappa/skills --skill discussion-loop`, and any surrounding blank lines that would leave a double-gap (collapse to a single blank line between the now-adjacent entries — the entry that previously came BEFORE discussion-loop is `consult-the-expert`; the entry that previously came AFTER is `the-librarian`).

    CHANGE 2 — APPEND a new top-level subsection `## Retired skills` at the END of `README.md` (after the last entry in `## Available skills`). Insert exactly this block (preserve a blank line above and below for spacing):

    ```markdown
    ## Retired skills

    - **`discussion-loop`** — retired 2026-05-21. Split into `discussion` (open-ended interviews) and `seeded-discussion` (predetermined point walks) when V1's thread layout shipped. The legacy folder remains on disk so existing installs do not break; new installs should pick the relevant replacement skill. Pre-existing logs at `docs/discussions/*-discussion.md` are valid as-is and require no migration.
    ```

    Critical requirements:
    - The `### [`discussion-loop`](./skills/discussion-loop/SKILL.md)` heading line MUST NOT appear anywhere in the file after the edit. (The `## Retired skills` bullet uses inline-code `` `discussion-loop` `` for the name — NOT a level-3 markdown heading.)
    - The `npx skills add Jei-sKappa/skills --skill discussion-loop` install snippet MUST NOT appear anywhere in the file after the edit — the retired skill should not be installable from README guidance.
    - The new `## Retired skills` heading MUST be a level-2 heading (two `#`).
    - The retired entry MUST contain the literal date `2026-05-21`, the literal skill name `discussion-loop` (in inline backticks), the literal replacement names `discussion` and `seeded-discussion`, and the literal phrase referencing legacy log location `docs/discussions/`.
    - Do NOT touch any other entry in `## Available skills`. The remaining 11 entries (consult-the-expert, the-librarian, brief-the-implementer, report-to-the-owner, meta-prompting, afk-exploration, review-decision-document, derive-spec, capture-inbox, discussion, seeded-discussion) must remain present.
    - This is NOT the Phase 7 README hybrid — keep the rest of the README structure intact. Do not introduce toolbox model framing, layered workflow map, or per-module catalog (Phase 7 owns those).
  </action>
  <verify>
    <automated>! grep -qF '### [`discussion-loop`](./skills/discussion-loop/SKILL.md)' README.md &amp;&amp; ! grep -qF 'npx skills add Jei-sKappa/skills --skill discussion-loop' README.md &amp;&amp; grep -qxF '## Retired skills' README.md &amp;&amp; grep -qF '`discussion-loop`' README.md &amp;&amp; grep -qF '2026-05-21' README.md &amp;&amp; grep -qF '`discussion`' README.md &amp;&amp; grep -qF '`seeded-discussion`' README.md &amp;&amp; grep -qF 'docs/discussions/' README.md &amp;&amp; grep -qF '### [`capture-inbox`](./skills/capture-inbox/SKILL.md)' README.md &amp;&amp; grep -qF '### [`discussion`](./skills/discussion/SKILL.md)' README.md &amp;&amp; grep -qF '### [`seeded-discussion`](./skills/seeded-discussion/SKILL.md)' README.md &amp;&amp; grep -qF '### [`consult-the-expert`](./skills/consult-the-expert/SKILL.md)' README.md &amp;&amp; grep -qF '### [`derive-spec`](./skills/derive-spec/SKILL.md)' README.md</automated>
  </verify>
  <done>
    `README.md` has the `### [`discussion-loop`](./skills/discussion-loop/SKILL.md)` block removed from `## Available skills`, the `npx skills add Jei-sKappa/skills --skill discussion-loop` snippet no longer present anywhere in the file, a new level-2 `## Retired skills` section appended at the end containing a bullet that names `discussion-loop`, dates the retirement `2026-05-21`, points at `discussion` and `seeded-discussion`, and references `docs/discussions/` for the legacy log path. The other 11 entries remain in `## Available skills`. Verification grep checks all pass.
  </done>
</task>

</tasks>

<verification>
- `skills/discussion-loop/SKILL.md` is rewritten: frontmatter version bumped to `2.0.0`, description rewritten to a retirement sentence, body is a short deprecation notice (≤ 60 lines) naming both replacements with install snippets, referencing the legacy log path (`docs/discussions/`) and the new thread log path (`docs/threads/`), and contains no legacy `## Setup`/`## Loop` sections.
- `skills/discussion-loop/` folder is still present on disk (soft retire).
- `.claude-plugin/marketplace.json` is valid JSON: `JeisKappa-skills.skills` has 8 entries (no `./skills/discussion-loop`); `JeisKappa-workflow.skills` is unchanged with 3 entries.
- `.vscode/settings.json` `conventionalCommits.scopes` has 11 entries (no `discussion-loop`), still alphabetical, still includes `capture-inbox` / `discussion` / `seeded-discussion`.
- `README.md` no longer has the `discussion-loop` entry in `## Available skills` nor anywhere the install snippet. A new level-2 `## Retired skills` section exists, dating the retirement (2026-05-21), naming both replacements, and referencing the legacy log location.
- All four CLAUDE.md "When adding a new skill" touchpoints have their corresponding REMOVAL applied for `discussion-loop`.

Run all four verification commands; all four must pass to complete the phase.
</verification>

<success_criteria>
Phase 2 success-criterion #6 (`discussion-loop` explicitly retired with a note documenting the migration; `name`, folder, `marketplace.json` entry, `.vscode/settings.json` scope, and `README.md` index all reflect the new state) is fully satisfied. Requirement DISC-09 (the durable output is named "decision log" — and corollary: legacy skill stops claiming the discussion-loop shape) is addressed by the migration note + the marketplace removal preventing further legacy installs.

The end-of-phase state is now: three new V1 skills installable under `JeisKappa Workflow` (capture-inbox, discussion, seeded-discussion); legacy `discussion-loop` retired with a documented migration; all four registration files (`skills/`, `marketplace.json`, `.vscode/settings.json`, `README.md`) consistent. Phase 2 success criteria 1–6 all satisfied across Plans 02-01/02-02/02-03 collectively. Phase 3 (forward-spine: `propose-*`, `spec-*`) can now begin against a foundation that includes both thread-layout primitives (Phase 1) and the capture/discussion infrastructure (Phase 2).
</success_criteria>

<output>
Create `.planning/phases/02-capture-and-discussion-infrastructure/02-03-SUMMARY.md` when done. Record:
- Files rewritten: `skills/discussion-loop/SKILL.md` (body replaced with deprecation notice; version bumped 1.1.0 → 2.0.0)
- Files edited: `.claude-plugin/marketplace.json` (removed `./skills/discussion-loop` from `JeisKappa-skills`), `.vscode/settings.json` (removed `discussion-loop` from scopes), `README.md` (removed entry from `## Available skills`; added new `## Retired skills` subsection)
- Files unchanged on disk: `skills/discussion-loop/` folder still exists (soft retire — folder NOT deleted).
- Requirements addressed: DISC-09 (migration note + retirement coordination)
- `JeisKappa-skills.skills` count: 9 → 8.
- `JeisKappa-workflow.skills` count: 3 (unchanged).
- `.vscode/settings.json` scopes count: 12 → 11.
- `README.md` `## Available skills` entry count: 12 → 11. New `## Retired skills` subsection: 1 entry (`discussion-loop`, dated 2026-05-21).
- Migration of pre-existing logs: NONE performed (per CONTEXT.md `<deferred>` — pre-existing logs at `docs/discussions/*-discussion.md` stay where they are).
- Phase 2 ROADMAP entry can now flip to complete after verification runs against all three plans.
</output>
