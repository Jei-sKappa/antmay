---
phase: 06-review-family
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/review-proposal-auto/SKILL.md
  - skills/review-proposal-interactive/SKILL.md
  - .claude-plugin/marketplace.json
  - .vscode/settings.json
  - README.md
autonomous: true
requirements:
  - REVW-01
  - REVW-06
  - REVW-07
  - REVW-08

must_haves:
  truths:
    - "User can invoke `review-proposal-auto` against an emitted V1 proposal artifact path under `docs/threads/<thread>/proposals/` and receive a findings-first report written to `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md` covering Verdict / Findings (severity-tagged) / Evidence / References / Open Questions / Next Actions."
    - "User can invoke `review-proposal-interactive` against an emitted V1 proposal artifact path and walk one finding/topic at a time; the skill ASKS the user for their view AND TESTS that view against the artifact per D89; resolved/rejected findings stay in the decision log; unresolved actionable findings dump to `inbox/open/<UTC>-<kebab-desc>-review-finding.md` at the END of the session (no Inbox file if nothing remains) per D92/D95."
    - "User reading `review-proposal-interactive` finds the 4 anti-sycophancy markers from `skills/discussion/SKILL.md` carried verbatim (Disagree / Push back / Do not treat pushback as correctness / Refuse-to-log) plus a review-stance amplifier stating that a review is most valuable when it disagrees with the author."
    - "User reading either skill body sees the optional `the-fool` delegation note per D88 — adversarial pressure on a proposal is delegated to the external `the-fool` skill; no native V1 adversarial pass."
    - "User running `npx skills add` finds both new skills installable via the `JeisKappa-workflow` marketplace plugin after registration (workflow plugin grows 19 -> 21)."
  artifacts:
    - path: "skills/review-proposal-auto/SKILL.md"
      provides: "Autonomous proposal review skill emitting a findings-first report to inbox/open/"
      contains: "frontmatter with name=review-proposal-auto, version=1.0.0; ## Inputs naming proposals/ path; ## Output Artifact naming inbox/open/<UTC>-<kebab-desc>-review-finding.md; ## Findings Report Shape covering Verdict / Findings (severity tags) / Evidence / References / Open Questions / Next Actions; optional the-fool delegation note (D88); citations to docs/workflow/v1/filename-grammar.md + docs/workflow/v1/thread-layout.md"
    - path: "skills/review-proposal-interactive/SKILL.md"
      provides: "Collaborative proposal review skill walking one finding at a time with anti-sycophancy + decision log + opt-in inbox dump"
      contains: "frontmatter with name=review-proposal-interactive, version=1.0.0; ## Anti-Sycophancy Stance with 4 verbatim markers + review-stance amplifier; ## Workflow with ASK-and-TEST-user's-view-against-artifact phrasing (D89); ## Output Artifacts naming discussions/<UTC>-<kebab-desc>-decision-log.md AND conditional inbox/open/<UTC>-<kebab-desc>-review-finding.md for unresolved findings only; optional the-fool delegation note (D88); citations to Phase 1 docs"
    - path: ".claude-plugin/marketplace.json"
      provides: "Marketplace registration for both new skills"
      contains: "./skills/review-proposal-auto and ./skills/review-proposal-interactive added to JeisKappa-workflow.skills (length 19 -> 21)"
    - path: ".vscode/settings.json"
      provides: "Conventional-commit scopes for both new skills"
      contains: "review-proposal-auto and review-proposal-interactive added to conventionalCommits.scopes alphabetically (length 27 -> 29)"
    - path: "README.md"
      provides: "Public install snippets for both new skills"
      contains: "### [`review-proposal-auto`](./skills/review-proposal-auto/SKILL.md) and ### [`review-proposal-interactive`](./skills/review-proposal-interactive/SKILL.md) headings with `npx skills add` blocks"
  key_links:
    - from: "skills/review-proposal-auto/SKILL.md"
      to: "docs/workflow/v1/filename-grammar.md"
      via: "citation by absolute path (review-finding artifact-type token + record-form grammar)"
      pattern: "docs/workflow/v1/filename-grammar.md"
    - from: "skills/review-proposal-auto/SKILL.md"
      to: "docs/workflow/v1/thread-layout.md"
      via: "citation by absolute path (inbox/open/ folder routing)"
      pattern: "docs/workflow/v1/thread-layout.md"
    - from: "skills/review-proposal-interactive/SKILL.md"
      to: "skills/discussion/SKILL.md"
      via: "anti-sycophancy stance carried verbatim (4 markers)"
      pattern: "Disagree when you disagree|Do not treat pushback as correctness"
    - from: "skills/review-proposal-interactive/SKILL.md"
      to: "skills/seeded-discussion/SKILL.md"
      via: "per-finding walk pattern source"
      pattern: "seeded-discussion"
    - from: ".claude-plugin/marketplace.json"
      to: "skills/review-proposal-auto/ and skills/review-proposal-interactive/"
      via: "JeisKappa-workflow.skills entries"
      pattern: "./skills/review-proposal-auto|./skills/review-proposal-interactive"
---

<objective>
Ship the first review pair of Phase 6: `review-proposal-auto` (autonomous; emits a findings-first report at `inbox/open/<UTC>-<kebab-desc>-review-finding.md`) and `review-proposal-interactive` (collaborative; walks one finding at a time, writes a decision log at `discussions/<UTC>-<kebab-desc>-decision-log.md`, dumps only unresolved actionable findings to `inbox/open/` at the END of the session — no Inbox file when nothing remains). Both target the same review target per D81: a V1 proposal artifact (lightweight check — gaps, risks, ambiguities). Both carry the optional `the-fool` delegation hint per D88: native V1 has no adversarial-review skill; adversarial pressure on a proposal is delegated to the external `the-fool` skill. The interactive sibling carries the 4 anti-sycophancy markers from `skills/discussion/SKILL.md` verbatim plus a review-stance amplifier. Register both skills in the three V1 touchpoints (marketplace.json, .vscode/settings.json, README.md).

Purpose: This is the first of five paired review plans for Phase 6. It establishes the V1 paired review skill body shape — auto emits to `inbox/open/` per REVW-06; interactive emits to `discussions/` per REVW-07/REVW-08 with conditional inbox-dump per D92/D95 — that the four subsequent plans (06-02 spec, 06-03 plan, 06-04 implementation, 06-05 code) will reuse. The proposal target is the lightest of the five (D81: gaps/risks/ambiguity) so it is the cleanest plan to anchor the shape on.

Output: Two new skill folders + three registration files updated. All four CLAUDE.md touchpoints satisfied for each skill. After this plan: workflow plugin 19 -> 21; .vscode scopes 27 -> 29; README "Available skills" 27 -> 29.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-review-family/06-CONTEXT.md
@AGENTS.md

@docs/workflow/v1/README.md
@docs/workflow/v1/thread-layout.md
@docs/workflow/v1/filename-grammar.md
@docs/workflow/v1/immutability.md

<!-- Phase 3 propose-* skills — produces the input artifacts this pair reviews -->
@skills/propose-auto/SKILL.md
@skills/propose-interactive/SKILL.md

<!-- Anti-sycophancy + walk-pattern source -->
@skills/discussion/SKILL.md
@skills/seeded-discussion/SKILL.md

<!-- Legacy prior art for the review-X-X body shape (handoff-grade-bar logic lives in review-spec-*, not here, but the prose density / "what to look for" framing transfers) -->
@skills/review-decision-document/SKILL.md

<!-- Registration files being edited -->
@.claude-plugin/marketplace.json
@.vscode/settings.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author skills/review-proposal-auto/SKILL.md</name>
  <files>skills/review-proposal-auto/SKILL.md</files>
  <read_first>
    - skills/review-proposal-auto/SKILL.md (the file being created — empty / non-existent)
    - .planning/phases/06-review-family/06-CONTEXT.md (LOCKED decisions: D81 target-specific logic for proposals; D88 the-fool delegation; D90/D91 findings-first report shape; D94 review-finding artifact-type token)
    - skills/propose-auto/SKILL.md (Phase 3 — defines the proposal artifact this skill consumes; cite this as the source-artifact shape)
    - skills/propose-interactive/SKILL.md (Phase 3 — also produces proposal artifacts)
    - skills/review-decision-document/SKILL.md (legacy prior art for review skill prose density — but NOT for handoff-grade-bar logic; that is review-spec-*'s territory in Plan 06-02)
    - docs/workflow/v1/filename-grammar.md (record-form grammar; review-finding artifact-type token)
    - docs/workflow/v1/thread-layout.md (inbox/open/ folder routing)
    - docs/workflow/v1/immutability.md (emitted-artifact immutability + ambiguous-reference resolution rule)
  </read_first>
  <action>
    Create the file with YAML frontmatter and a markdown body. Frontmatter required fields:
    - `name: review-proposal-auto`
    - `description:` ONE sentence including the "Use when..." trigger AND explicit interaction mode (autonomous; emits a findings-first report; no clarifying questions; no chat walk). Mention the sibling `review-proposal-interactive`. Mention that adversarial pressure on a proposal is delegated to the external `the-fool` skill (D88).
    - `metadata.author: https://github.com/Jei-sKappa`
    - `metadata.version: 1.0.0`

    Body sections (executor discretion on exact section names; behavioral tokens below MUST appear in the body):

    1. **Opening clarifier paragraph** naming the sibling `review-proposal-interactive` and stating the two axes for the proposal review pair: AUTONOMY (this skill is autonomous; sibling is collaborative). Reference the broader Phase 6 family by naming at least ONE other review target (e.g., `review-spec-*` for the handoff-grade bar, or `review-implementation-*` for code-vs-original-intent). State that this skill performs a LIGHTWEIGHT proposal review per D81 — gaps, risks, ambiguities — NOT a handoff-grade bar (that is `review-spec-*`'s job).

    2. **Inputs** section. MUST state the input is a V1 proposal artifact path under `docs/threads/<thread>/proposals/<UTC>-<kebab-desc>-proposal.md` (record-form per Phase 1 grammar). Document the ambiguity fallback: if multiple plausible proposal artifacts exist, ASK the user per `docs/workflow/v1/immutability.md` ("Ambiguous Reference Resolution"). Do not pick by recency. The literal phrase `docs/threads/<thread>/proposals/` MUST appear.

    3. **What This Skill Reviews** section. MUST enumerate the 3 things the review checks against (per D81 proposal target): gaps (missing intent / context / rough shape), risks (named or unnamed downstream consequences), ambiguities (statements that admit more than one reasonable reading). This is the LIGHTWEIGHT review — do not promise a handoff-grade bar; that lives in `review-spec-*`.

    4. **Findings Report Shape** section. MUST document the six required output sections in the emitted review artifact (D90, D91, REVW-06):
       - `## Verdict` — overall judgment (e.g., ready / partially ready / not ready; executor discretion on the verdict vocabulary).
       - `## Findings` — each finding carries a severity tag. Suggested vocabulary (executor MAY adopt or refine): `blocker` / `issue` / `nit` (lowercase, simple).
       - `## Evidence` — cite the proposal's section heading or short quote for each finding. Reference, do not recite.
       - `## References` — the proposal path being reviewed + any related decision logs by absolute path + `D<N>`.
       - `## Open Questions` — clarifications worth confirming.
       - `## Next Actions` — what to do next (typically: re-version the proposal, open a discussion, escalate to `the-fool` for adversarial pressure).
       The 6 sections MUST be listed by literal name in the skill body.

    5. **Output Artifact** section. MUST state the emission path: `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md` per the record-form filename grammar (`review-finding` artifact-type token is MANDATORY per `docs/workflow/v1/filename-grammar.md`). ONE record per review run — multiple findings live in ONE file. Cite the filename grammar doc by absolute path AND the thread layout doc by absolute path AND name the `inbox/open/` folder.

    6. **The Fool Delegation** subsection or short paragraph (D88, REVW-09). MUST state: adversarial pressure on a proposal is delegated to the external `the-fool` skill. There is no native V1 adversarial-review skill. If the user wants an adversarial pass against the proposal — pre-mortem, devil's advocate, "what's missing that would kill this" — invoke `the-fool` separately against the proposal. This skill (review-proposal-auto) performs the standard gaps/risks/ambiguities pass, not the adversarial pass.

    7. **Workflow** section with numbered steps. MUST include:
       - Resolve the active thread per `docs/workflow/v1/thread-layout.md` (ASK if multiple thread roots ambiguous).
       - Resolve the proposal artifact path (ASK if multiple plausible proposals).
       - Read the proposal READ-ONLY per `docs/workflow/v1/immutability.md`.
       - Identify gaps, risks, ambiguities per the `## What This Skill Reviews` section.
       - Draft the findings-first report covering all 6 sections.
       - Capture the UTC stamp per `docs/workflow/v1/filename-grammar.md` (12-character `YYMMDDHHMMSSZ` — stamp once, never re-derive).
       - Write the review artifact to `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md` (inbox/open/ folder created on-demand per `docs/workflow/v1/thread-layout.md`).
       - Confirm: `Review written: <relative-path-to-the-file>`. No closing remark.

    8. **Commit Policy** section. MUST state: this skill NEVER auto-commits the review artifact. Writing the file is where the skill stops.

    9. **Immutability** section. MUST state: emitted review-finding artifacts are immutable per `docs/workflow/v1/immutability.md`. A revision to a review-finding is a NEW review-finding artifact (new UTC stamp), not an in-place edit. The proposal under review is also IMMUTABLE — the reviewer reads READ-ONLY and does not edit the proposal.

    Voice and discipline:
    - Dense, declarative, citation-first. Match Phase 3/4/5 spine skill density.
    - Cite Phase 1 canonical docs (filename-grammar.md, thread-layout.md, immutability.md) by ABSOLUTE PATH.
    - Cite at least one Phase 3 propose-* skill by absolute path to anchor "expected input shape".
    - Do NOT include a `## Anti-Sycophancy Stance` section (this is the AUTO half).
    - Do NOT include a decision-log mention as output (decision logs are the INTERACTIVE half's territory — see Plan 06-01 Task 2).
    - Do NOT reimplement the-fool's adversarial logic — delegate via short note only.
    - Do NOT promise handoff-grade-bar coverage (that is review-spec-*'s explicit job in Plan 06-02; this is the LIGHTWEIGHT proposal review).
  </action>
  <verify>
    <automated>test -f skills/review-proposal-auto/SKILL.md &amp;&amp; grep -qxF 'name: review-proposal-auto' skills/review-proposal-auto/SKILL.md &amp;&amp; grep -qxF '  version: 1.0.0' skills/review-proposal-auto/SKILL.md &amp;&amp; grep -qxF '  author: https://github.com/Jei-sKappa' skills/review-proposal-auto/SKILL.md &amp;&amp; grep -qF 'Use when' skills/review-proposal-auto/SKILL.md &amp;&amp; grep -qF 'review-proposal-interactive' skills/review-proposal-auto/SKILL.md &amp;&amp; grep -qF 'the-fool' skills/review-proposal-auto/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/proposals/' skills/review-proposal-auto/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/inbox/open/' skills/review-proposal-auto/SKILL.md &amp;&amp; grep -qF 'review-finding' skills/review-proposal-auto/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/filename-grammar.md' skills/review-proposal-auto/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/thread-layout.md' skills/review-proposal-auto/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/immutability.md' skills/review-proposal-auto/SKILL.md &amp;&amp; grep -qiF 'verdict' skills/review-proposal-auto/SKILL.md &amp;&amp; grep -qiF 'findings' skills/review-proposal-auto/SKILL.md &amp;&amp; grep -qiF 'evidence' skills/review-proposal-auto/SKILL.md &amp;&amp; grep -qiF 'references' skills/review-proposal-auto/SKILL.md &amp;&amp; grep -qiF 'open questions' skills/review-proposal-auto/SKILL.md &amp;&amp; grep -qiF 'next actions' skills/review-proposal-auto/SKILL.md &amp;&amp; grep -qiE 'gap|gaps' skills/review-proposal-auto/SKILL.md &amp;&amp; grep -qiE 'risk|risks' skills/review-proposal-auto/SKILL.md &amp;&amp; grep -qiE 'ambigu' skills/review-proposal-auto/SKILL.md &amp;&amp; grep -qiE 'NEVER (commit|auto-commit)|never auto-commit|never commit' skills/review-proposal-auto/SKILL.md &amp;&amp; ! grep -qxF '## Anti-Sycophancy Stance' skills/review-proposal-auto/SKILL.md &amp;&amp; ! grep -qiE 'handoff-grade' skills/review-proposal-auto/SKILL.md</automated>
  </verify>
  <done>skills/review-proposal-auto/SKILL.md exists with frontmatter (name=review-proposal-auto, version=1.0.0, author=https://github.com/Jei-sKappa) and a body covering: opening clarifier with sibling reference + lightweight-vs-handoff-grade-bar framing; Inputs naming proposals/ path with ambiguity fallback; What This Skill Reviews enumerating gaps/risks/ambiguities (D81); Findings Report Shape with all 6 required sections (Verdict / Findings / Evidence / References / Open Questions / Next Actions); Output Artifact naming inbox/open/<UTC>-<kebab-desc>-review-finding.md and citing filename-grammar.md + thread-layout.md by absolute path; The Fool delegation note (D88); Workflow with read-only-input + write-review steps; Commit Policy NEVER auto-commits; Immutability. NO Anti-Sycophancy section, NO handoff-grade-bar promise (that is review-spec-*'s job).</done>
</task>

<task type="auto">
  <name>Task 2: Author skills/review-proposal-interactive/SKILL.md</name>
  <files>skills/review-proposal-interactive/SKILL.md</files>
  <read_first>
    - skills/review-proposal-interactive/SKILL.md (the file being created — empty / non-existent)
    - skills/review-proposal-auto/SKILL.md (Task 1 output — sibling consistency on Inputs / What This Skill Reviews / Output framing)
    - .planning/phases/06-review-family/06-CONTEXT.md (LOCKED decisions: D81 / D88 / D89 ASK-AND-TEST / D92 / D95 / D93 / D94 decision-log artifact-type token; interactive-skill shape locks)
    - skills/discussion/SKILL.md (anti-sycophancy stance source — 4 marker phrases for verbatim carry-over)
    - skills/seeded-discussion/SKILL.md (per-point walk pattern source — informs per-finding walk format; ## Loop heading source)
    - skills/propose-interactive/SKILL.md (Phase 3 interactive sibling pattern — Anti-Sycophancy + Workflow + Scope Drift section shapes)
    - skills/plan-loose-interactive/SKILL.md (Phase 4 interactive — 4 anti-sycophancy markers carrying pattern)
    - skills/implement-plan-interactive/SKILL.md (Phase 5 interactive — execution-time stakes amplifier reference for stakes-amplifier construction)
    - docs/workflow/v1/filename-grammar.md (decision-log + review-finding artifact-type tokens)
    - docs/workflow/v1/thread-layout.md (discussions/ + inbox/open/ folder routing)
    - docs/workflow/v1/immutability.md (emitted-artifact immutability + ambiguous-reference resolution)
  </read_first>
  <action>
    Create the file with YAML frontmatter and a markdown body. Frontmatter required fields:
    - `name: review-proposal-interactive`
    - `description:` ONE sentence including the "Use when..." trigger AND explicit interaction mode (collaborative; walks one finding/topic at a time; ASKS the user for their view AND tests that view against the artifact; writes a decision log; opt-in inbox dump). Mention the sibling `review-proposal-auto`. Mention the `the-fool` delegation for adversarial pressure (D88).
    - `metadata.author: https://github.com/Jei-sKappa`
    - `metadata.version: 1.0.0`

    Body sections (executor discretion on exact section names; behavioral tokens below MUST appear):

    1. **Opening clarifier paragraph** naming the sibling `review-proposal-auto` and reference at least ONE other review target (e.g., `review-spec-*` for handoff-grade-bar). State LIGHTWEIGHT proposal review per D81 — gaps, risks, ambiguities — NOT handoff-grade-bar.

    2. **Anti-Sycophancy Stance** section. Carry the FOUR anti-sycophancy markers from `skills/discussion/SKILL.md` VERBATIM. Each of these substrings MUST appear in the body:
       - "Disagree when you disagree"
       - "Push back on weak or incomplete reasoning"
       - "Do not treat pushback as correctness"
       - "Refuse to log" (followed by appropriate clause — e.g., "Refuse to log a finding you believe is wrong without flagging it")

       PLUS a REVIEW-STANCE AMPLIFIER (executor discretion on exact wording but MUST be present): "A review is most valuable when it disagrees with the author" (or near-verbatim — push back on weak reasoning or hidden assumptions; do not soften findings just because the user pushes back). The amplifier MUST be present in the body — it is the review-skill equivalent of plan-strict-interactive's strict-stakes amplifier and implement-interactive's execution-time amplifier.

    3. **Inputs** section. Same as `review-proposal-auto`: a V1 proposal artifact path under `docs/threads/<thread>/proposals/`. Ambiguity fallback per `docs/workflow/v1/immutability.md`.

    4. **What This Skill Reviews** section. Same as `review-proposal-auto`: gaps, risks, ambiguities per D81.

    5. **Walk Format** section (per-finding walk). MUST document the per-finding loop modeled on `seeded-discussion`'s four-element Decision / What you need to know / Options / Recommendation pattern. For each finding (or topic, or component — executor discretion on grain):
       1. Surface the finding with severity tag (e.g., blocker / issue / nit).
       2. Cite the evidence (section heading / short quote from the proposal).
       3. ASK the user for their view on the finding.
       4. TEST the user's explanation against the proposal artifact — re-read the cited section, check whether the user's framing actually resolves the finding or merely defends it. Do NOT just accept the user's view. (D89: "ASK the user for their view when useful AND TEST the user's explanation against the artifact — do not just accept" — literal phrase containing both `ASK` and `TEST` and `do not just accept` (or near-verbatim) MUST appear in the body.)
       5. Settle each finding as: resolved (the artifact already covers it, or the user's clarification suffices), rejected (not a real finding), accepted (genuine — to be addressed), deferred (genuine but parked), or parked (Inbox).
       6. Append a record to the decision log with the per-finding outcome.

    6. **Output Artifacts** section (TWO conditional outputs per D92/D95):
       - **Decision log** (ALWAYS written when the walk produces at least one finding settlement): `docs/threads/<thread>/discussions/<UTC>-<kebab-desc>-decision-log.md` per `docs/workflow/v1/filename-grammar.md`. The `decision-log` artifact-type suffix is MANDATORY. Use the append-only single-record shape from the `discussion` / `seeded-discussion` skill bodies — sequential per-log `## D<N>: <Finding title>` headings with `Decision:` and `Rationale:` lines. Resolved AND rejected findings remain in the decision log only. (D92, D95.)
       - **Unresolved-findings inbox dump** (CONDITIONAL — written ONLY when at least one accepted/deferred/parked finding remains at the END of the session): `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md` per the record-form grammar. The literal phrase "no Inbox file when nothing remains" or near-verbatim equivalent (e.g., "no Inbox dump if nothing remains", "no Inbox file if all findings resolved") MUST appear in the body. The inbox dump (when written) carries ONLY the unresolved actionable findings — NOT the resolved/rejected ones.

    7. **The Fool Delegation** subsection or short paragraph (D88, REVW-09). Same as `review-proposal-auto`: adversarial pressure delegated to `the-fool`. The skill MAY suggest delegation during the walk if the user wants to surface adversarial findings; do NOT reimplement adversarial logic here.

    8. **Workflow** section with numbered steps. MUST include:
       - Resolve the active thread + the proposal artifact path (ASK if multiple plausible).
       - Read the proposal READ-ONLY.
       - Identify the candidate findings list (gaps / risks / ambiguities).
       - Confirm the findings list with the user before walking.
       - For each finding IN ORDER, run the per-finding loop from `## Walk Format` (surface + cite + ASK + TEST + settle + log).
       - At the END of the walk: if at least one unresolved actionable finding remains, write the inbox-dump review-finding artifact. If all findings were resolved/rejected, do NOT write an Inbox file.
       - Final message: cite the decision log path; cite the inbox-dump path IF written, OR state explicitly "no unresolved findings — no inbox file written".

    9. **Decision Log** lazy-creation rule (cross-reference for clarity). MUST state: the decision log is created at the FIRST settled finding, not proactively. An interrupted walk with no settled findings leaves no artifact. (Matches `discussion` / `seeded-discussion` lazy-creation rule.)

    10. **Scope Drift** section. PARK as Inbox item via `capture-inbox`, SPLIT into its own decision log, or DEFER. ASK the user. (Modeled on `discussion` / `seeded-discussion` / `propose-interactive` scope-drift sections.)

    11. **Commit Policy** section. MUST state: this skill NEVER auto-commits any emitted artifact.

    12. **Immutability** section. MUST state: emitted decision logs are append-only (no in-place edits after emission); emitted review-finding artifacts are immutable; the proposal under review is IMMUTABLE — the reviewer reads READ-ONLY. Cite `docs/workflow/v1/immutability.md` by absolute path.

    Voice and discipline:
    - Dense, declarative, citation-first.
    - All 4 anti-sycophancy markers MUST be present verbatim or near-verbatim substrings.
    - The review-stance amplifier MUST be present.
    - The D89 ASK-AND-TEST phrasing MUST be present.
    - The D92/D95 conditional-inbox-dump phrasing MUST be present ("no Inbox file when nothing remains" or equivalent).
    - Cite Phase 1 canonical docs by absolute path.
    - Cite at least one Phase 3 propose-* skill by absolute path (input shape source).
    - Do NOT reimplement the-fool's adversarial logic.
    - Do NOT promise handoff-grade-bar coverage.
  </action>
  <verify>
    <automated>test -f skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qxF 'name: review-proposal-interactive' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qxF '  version: 1.0.0' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qxF '  author: https://github.com/Jei-sKappa' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qF 'Use when' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qF 'review-proposal-auto' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qF 'the-fool' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qiF 'anti-sycophancy' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qF 'Disagree when you disagree' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qF 'Push back on weak or incomplete reasoning' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qF 'Do not treat pushback as correctness' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qF 'Refuse to log' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qiE 'review is most valuable when it disagrees|disagrees with the author|push back hard' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/proposals/' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/discussions/' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/inbox/open/' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qF 'decision-log' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qF 'review-finding' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/filename-grammar.md' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/thread-layout.md' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/immutability.md' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qiE 'ASK.*TEST|TEST.*ASK' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qiE 'do not just accept|don.t just accept' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qiE 'no Inbox file|no inbox dump|no inbox file when nothing remains|no inbox dump if nothing|nothing remains|all findings resolved' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qiE 'gap|gaps' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qiE 'risk|risks' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qiE 'ambigu' skills/review-proposal-interactive/SKILL.md &amp;&amp; grep -qiE 'NEVER (commit|auto-commit)|never auto-commit|never commit' skills/review-proposal-interactive/SKILL.md</automated>
  </verify>
  <done>skills/review-proposal-interactive/SKILL.md exists with frontmatter (name=review-proposal-interactive, version=1.0.0) and a body covering: opening clarifier; Anti-Sycophancy Stance with 4 verbatim markers + review-stance amplifier; Inputs (proposal artifact path); What This Skill Reviews (gaps/risks/ambiguities per D81); Walk Format with per-finding loop including the D89 ASK-AND-TEST-do-not-just-accept phrasing; Output Artifacts naming BOTH the always-written decision-log at discussions/ AND the conditional review-finding dump at inbox/open/ with the no-Inbox-when-nothing-remains phrasing (D92/D95); The Fool delegation note (D88); Workflow; lazy decision-log creation; Scope Drift; Commit Policy NEVER auto-commits; Immutability citing Phase 1 docs by absolute path. NO handoff-grade-bar promise (review-spec-*'s territory).</done>
</task>

<task type="auto">
  <name>Task 3: Register review-proposal-auto + review-proposal-interactive in marketplace.json, .vscode/settings.json, and README.md</name>
  <files>.claude-plugin/marketplace.json, .vscode/settings.json, README.md</files>
  <read_first>
    - .claude-plugin/marketplace.json (current state after Phase 5: JeisKappa-workflow has 19 entries; JeisKappa-skills has 8)
    - .vscode/settings.json (current state after Phase 5: 27 alphabetically-sorted scopes)
    - README.md (current state after Phase 5: 27 "Available skills" entries + Retired skills subsection at bottom containing discussion-loop)
    - AGENTS.md (the 4-touchpoint checklist; jq accessor convention)
    - .planning/phases/06-review-family/06-CONTEXT.md (registration deltas)
    - skills/propose-auto/SKILL.md (description-style reference; Phase 3 paired-skill register style)
    - skills/propose-interactive/SKILL.md (description-style reference for interactive sibling)
  </read_first>
  <action>
    Update all three registration files atomically:

    1. **`.claude-plugin/marketplace.json`** — add `"./skills/review-proposal-auto"` and `"./skills/review-proposal-interactive"` to the `JeisKappa-workflow.skills` array. Place in alphabetical position: after `./skills/propose-interactive` and before `./skills/seeded-discussion`. After this change the JeisKappa-workflow array has exactly 21 entries (19 + 2). JeisKappa-skills stays unchanged at 8.

    2. **`.vscode/settings.json`** — add `"review-proposal-auto"` and `"review-proposal-interactive"` to `."conventionalCommits.scopes"`. They fall alphabetically between `propose-interactive` and `report-to-the-owner`. Total length now 29 (27 + 2). Keep alphabetical order. The jq accessor MUST use the quoted form `."conventionalCommits.scopes"` (the dotted key cannot be addressed unquoted — Phase 4 regression).

    3. **`README.md`** — add TWO new "Available skills" entries (one per new skill), each with the established shape: `### [\`<name>\`](./skills/<name>/SKILL.md)` heading + description paragraph + fenced `npx skills add Jei-sKappa/skills --skill <name>` block. Insert AFTER the `propose-interactive` entry and BEFORE the `spec-auto` entry. Position both entries together (auto then interactive). Description sentences MUST state explicit interaction mode per DIST-05 (auto: autonomous, no clarifying questions; interactive: walks one finding at a time, ASKS user's view AND TESTS it against the artifact). The README descriptions MAY paraphrase the SKILL.md descriptions but must be self-contained one-paragraph entries.

    Do NOT touch the `## Retired skills` subsection (Plan 06-02 will add a new entry there for `review-decision-document`).
    Do NOT add a documentation note about the-fool delegation here — Plan 06-05 owns the README the-fool delegation note.
    Do NOT add a documentation note about verification — Plan 06-04 owns the README verification note.
    Do NOT commit.
  </action>
  <verify>
    <automated>jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length == 21' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/review-proposal-auto")' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/review-proposal-interactive")' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '.plugins[] | select(.name=="JeisKappa-skills") | .skills | length == 8' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" | length == 29' .vscode/settings.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" | index("review-proposal-auto")' .vscode/settings.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" | index("review-proposal-interactive")' .vscode/settings.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" == (."conventionalCommits.scopes" | sort)' .vscode/settings.json &gt;/dev/null &amp;&amp; [ "$(grep -c '^### \[`review-proposal-auto`\]' README.md)" = "1" ] &amp;&amp; [ "$(grep -c '^### \[`review-proposal-interactive`\]' README.md)" = "1" ] &amp;&amp; [ "$(grep -c 'npx skills add Jei-sKappa/skills --skill review-proposal-auto' README.md)" = "1" ] &amp;&amp; [ "$(grep -c 'npx skills add Jei-sKappa/skills --skill review-proposal-interactive' README.md)" = "1" ] &amp;&amp; A=$(grep -n '^### \[`review-proposal-auto`\]' README.md | cut -d: -f1) &amp;&amp; B=$(grep -n '^### \[`review-proposal-interactive`\]' README.md | cut -d: -f1) &amp;&amp; R=$(grep -n '^## Retired skills' README.md | cut -d: -f1) &amp;&amp; [ "$A" -lt "$R" ] &amp;&amp; [ "$B" -lt "$R" ] &amp;&amp; echo OK</automated>
  </verify>
  <done>.claude-plugin/marketplace.json JeisKappa-workflow array has 21 entries (was 19, +2); JeisKappa-skills unchanged at 8. .vscode/settings.json conventionalCommits.scopes has 29 entries (was 27, +2), alphabetically sorted. README.md has 2 new "Available skills" entries (review-proposal-auto, review-proposal-interactive) BEFORE the `## Retired skills` subsection. No git commit created.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run:

1. **Skill files exist:**
   ```bash
   ls -la skills/review-proposal-auto/SKILL.md skills/review-proposal-interactive/SKILL.md
   ```

2. **Auto skill structural tokens:**
   ```bash
   for TOKEN in "review-finding" "docs/threads/<thread>/inbox/open/" "docs/workflow/v1/filename-grammar.md" "docs/workflow/v1/thread-layout.md" "the-fool"; do
     grep -qF "$TOKEN" skills/review-proposal-auto/SKILL.md || echo "FAIL auto: $TOKEN"
   done
   ! grep -qxF '## Anti-Sycophancy Stance' skills/review-proposal-auto/SKILL.md || echo "FAIL auto must NOT contain Anti-Sycophancy section"
   ```

3. **Interactive skill anti-sycophancy markers + D89 + D92/D95:**
   ```bash
   for TOKEN in "Disagree when you disagree" "Push back on weak or incomplete reasoning" "Do not treat pushback as correctness" "Refuse to log"; do
     grep -qF "$TOKEN" skills/review-proposal-interactive/SKILL.md || echo "FAIL interactive: $TOKEN"
   done
   grep -qiE 'ASK.*TEST|TEST.*ASK' skills/review-proposal-interactive/SKILL.md || echo "FAIL D89 ASK-AND-TEST"
   grep -qiE 'no Inbox file|no inbox dump|nothing remains' skills/review-proposal-interactive/SKILL.md || echo "FAIL D92/D95 conditional inbox"
   ```

4. **Four-touchpoint registration:**
   ```bash
   jq '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length' .claude-plugin/marketplace.json    # expect 21
   jq '."conventionalCommits.scopes" | length' .vscode/settings.json                                              # expect 29
   grep -cE '^### \[`review-proposal-(auto|interactive)`\]' README.md                                             # expect 2
   ```
</verification>

<success_criteria>
- skills/review-proposal-auto/SKILL.md and skills/review-proposal-interactive/SKILL.md exist with correct V1 frontmatter and bodies covering all the locked behavioral tokens.
- review-proposal-auto emits to inbox/open/<UTC>-<kebab-desc>-review-finding.md with 6-section findings-first report (REVW-06, D90/D91).
- review-proposal-interactive walks one finding at a time, ASKS the user's view AND TESTS it against the artifact (D89, REVW-07), writes a decision log at discussions/<UTC>-<kebab-desc>-decision-log.md with resolved+rejected findings remaining there, and conditionally dumps only unresolved actionable findings to inbox/open/ (REVW-08, D92, D95).
- review-proposal-interactive carries all 4 anti-sycophancy markers verbatim plus the review-stance amplifier.
- Both skills include the optional the-fool delegation note (D88, REVW-09).
- marketplace.json JeisKappa-workflow array has exactly 21 entries (19 -> 21).
- .vscode/settings.json conventionalCommits.scopes has exactly 29 entries (27 -> 29), alphabetically sorted.
- README.md has 2 new entries inserted before `## Retired skills`.
- No commit created during this plan.
- Requirements REVW-01, REVW-06, REVW-07, REVW-08 partially or fully addressed (REVW-06/07/08 will continue to be addressed by Plans 06-02..06-05 across the other review targets).
</success_criteria>

<output>
Create `.planning/phases/06-review-family/06-01-SUMMARY.md` when done. Record:
- Files created: skills/review-proposal-auto/SKILL.md, skills/review-proposal-interactive/SKILL.md
- Files modified: .claude-plugin/marketplace.json, .vscode/settings.json, README.md
- Registration counts: workflow plugin 19 -> 21; scopes 27 -> 29; README Available skills 27 -> 29
- Requirements addressed: REVW-01 (review-proposal-* pair), REVW-06 (findings-first report shape — established here for subsequent plans), REVW-07 (ASK-and-TEST in interactive — established here), REVW-08 (decision-log + conditional inbox dump — established here)
- Pattern established for Plans 06-02..06-05: paired auto/interactive review skills emitting to inbox/open/ (auto) and discussions/ + conditional inbox/open/ (interactive); 6-section findings-first report; per-finding walk loop; 4-marker anti-sycophancy stance + review-stance amplifier in interactive variants
</output>
