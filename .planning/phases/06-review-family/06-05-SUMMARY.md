---
phase: 06-review-family
plan: 05
subsystem: skills
tags: [review-family, code-review, general-purpose-code-review, spec-independent, no-mandatory-source-artifact, anti-sycophancy, v1-skill-pair, decision-log, review-finding, inbox-open, the-fool-delegation, d86-general-purpose, d88-no-native-adversarial, d89-ask-and-test, d92-d95-conditional-inbox, revw-09-final, phase-6-complete]

requires:
  - phase: 01-foundations
    provides: V1 thread layout, record-form filename grammar, immutability + ambiguous-reference rules
  - phase: 02-capture-discussion
    provides: discussion + seeded-discussion anti-sycophancy stance (4 markers verbatim), capture-inbox for scope-drift parking, lazy decision-log creation pattern
  - phase: 05-implement-family
    provides: implement-auto + implement-interactive + implement-plan-auto + implement-plan-interactive — referenced in Next Actions for re-implementation paths when review surfaces actionable code-change findings
  - plan: 06-01
    provides: V1 review-pair body shape (auto + interactive); 6-section findings-first report shape (Verdict / Findings / Evidence / References / Open Questions / Next Actions); per-finding walk loop with D89 ASK-AND-TEST do-not-just-accept phrasing; 4-marker anti-sycophancy carry-over + review-stance amplifier pattern; D92/D95 conditional inbox-dump rule ("no Inbox file when nothing remains")
  - plan: 06-02
    provides: Per-element walk grain choice in interactive review siblings; severity mapping (`blocker | issue | nit`) reusable shape
  - plan: 06-03
    provides: Multi-axis review skill body shape with per-axis severity heuristics tethered to downstream impact; reuse pattern for the code-target axis enumeration
  - plan: 06-04
    provides: TWO-input + TWO-citation pattern (this plan REVERTS to the one-input pattern per D86); Read-only-against-implementation discipline (carries forward — this skill is READ-ONLY against code); SHORT README documentation-note pattern for V1 review-family decisions that subsume what users might expect (D85 verification-coverage note; D88 the-fool adversarial-delegation note in this plan); V1 review-family body shape codified by Plans 06-01..06-04 reusable verbatim

provides:
  - skills/review-code-auto/SKILL.md (autonomous V1 general-purpose code review per D86 — four primary axes: quality / safety / idioms / testability — plus optional additional axes; ONE-input contract (code reference only, NO source artifact); 6-section findings-first report emitted to inbox/open/<UTC>-<kebab-desc>-review-finding.md; explicit redirection note to review-implementation-* when user HAS a source artifact)
  - skills/review-code-interactive/SKILL.md (collaborative V1 general-purpose code review with per-finding OR per-file OR per-hunk walk against the four axes; 4 anti-sycophancy markers verbatim + review-stance amplifier; D89 ASK-AND-TEST against the code (do-not-just-accept); decision log at discussions/<UTC>-<kebab-desc>-decision-log.md + conditional inbox dump at inbox/open/<UTC>-<kebab-desc>-review-finding.md per D92/D95)
  - SHORT (1-line) the-fool adversarial-delegation blockquote in README.md inserted near the review-code-* entries naming the-fool as the V1 adversarial-pressure answer (D88, REVW-09)
  - Phase 6 complete: 5 plans landed; 10 new V1 review skills shipped; 1 legacy skill (review-decision-document) retired; 2 SHORT README documentation notes added (verification + the-fool)

affects: [phase-07-merge-finish-navigation]

tech-stack:
  added: []
  patterns:
    - "Four-axis general-purpose code review per D86 — quality / safety / idioms / testability — with per-axis severity heuristics tethered to downstream impact (what does the next reader / next maintainer / next caller find broken, fragile, surprising, or untestable?). Plus optional additional axes (performance / dependency hygiene / API design / accessibility / documentation drift) when warranted by the code under review. The four primary axes are the V1 standard for review-code-*; additional axes are executor's discretion."
    - "ONE-input contract reverting from Plan 06-04's TWO-input contract — the distinguishing trait of the code review target per D86 is that NO source artifact is required. Input forms: git ref / commit range / diff text / saved diff path / file path / directory path. The user MAY provide freeform focus areas (e.g., 'look for race conditions', 'check error handling in the new endpoints') as additional input — but the skill does NOT block on their absence. If the user provides no focus, the default general-purpose pass runs across all axes."
    - "Explicit redirection note to review-implementation-* when source artifact exists — both skill bodies state plainly: if the user HAS a source artifact (spec / proposal / plan / GitHub issue / Inbox item) AND wants to check whether the code delivers what the source promised, they should use review-implementation-auto / review-implementation-interactive instead. The redirection appears in the opening clarifier, in the What This Skill Reviews section, and in Next Actions as a recommended escalation. This is the key contract-line dividing review-code-* (general-purpose, no source) from review-implementation-* (fidelity-to-source, two-input)."
    - "SHORT (≤3 lines) README the-fool adversarial-delegation blockquote — `> **Note:** V1 adversarial review is delegated to the external the-fool skill — no native V1 adversarial-review skill. Use the-fool against a proposal or spec draft to surface adversarial risks before the spec / plan / implementation phases (per D88, REVW-09).` Placed directly above the new review-code-auto entry; complements Plan 06-04's SHORT verification-coverage blockquote (which sits directly above review-implementation-auto). Two distinct documentation notes coexist in Phase 6's final README state — they target different REVW-09 facts (verification vs adversarial). Pattern reusable for any future V1 decision that subsumes what users might expect to find as a separate skill."
    - "NO the-fool reference inside review-code-* skill bodies — per CONTEXT.md, the the-fool delegation hint is recommended only for review-proposal-* (Plan 06-01) and review-spec-* (Plan 06-02) where adversarial pressure is most useful at the early shape-defining stages. review-code-* does NOT carry the delegation note in the skill body — the README-level note in this plan is the SOLE in-Phase-6 the-fool reference for code-target review territory. The skill bodies redirect to review-implementation-* (for source-artifact-fidelity) and to review-proposal-* / review-spec-* / review-plan-* (for upstream-artifact-review) instead."
    - "Walk grain reverts to TWO grains for review-code-interactive — per-finding (closer to seeded-discussion's per-point walk) OR per-file / per-hunk (linear traversal of the code). Plan 06-04's third grain (per-source-acceptance-item / per-plan-task) does NOT apply here because there is no source artifact to walk. Per-finding is the recommended default when findings cluster across the four axes (e.g., quality nits PLUS a safety issue PLUS an idiom concern); per-file / per-hunk is the recommended default for small localized diffs."
    - "Read-only-against-code discipline — both skill bodies state explicitly: this skill does NOT check out branches, does NOT run tests, does NOT modify the working tree, and does NOT mutate any git state. Code modifications are an implement-* skill's job per Phase 5. The review is read-only against the code text. Carries forward from Plan 06-04's read-only-against-implementation discipline; the same reasoning applies — review is review, code is implement-*'s job."
    - "Phase 6 completion pattern — the V1 review-pair body shape codified by Plans 06-01, 06-02, 06-03, and 06-04 transfers verbatim. The deltas for this plan (the FIFTH and FINAL paired review) are: (a) the four-axis general-purpose enumeration replaces the five-axis fidelity enumeration of Plan 06-04, the four-axis plan review of Plan 06-03, the 8 D50 elements of Plan 06-02, and the lightweight gaps/risks/ambiguities of Plan 06-01, (b) the ONE-input contract reverts from Plan 06-04's TWO-input contract because there is no source artifact, (c) the ONE-citation Evidence requirement reverts from Plan 06-04's TWO-citation requirement, (d) the SHORT README the-fool adversarial-delegation note replaces Plan 06-04's verification-coverage note (different content; same SHORT-blockquote placement pattern), and (e) this plan completes REVW-09 — the SECOND of the two REVW-09 README notes is now in place alongside Plan 06-04's verification-coverage note."

key-files:
  created:
    - skills/review-code-auto/SKILL.md
    - skills/review-code-interactive/SKILL.md
  modified:
    - .claude-plugin/marketplace.json (JeisKappa-workflow.skills 27 -> 29 — Phase 6 target plugin count REACHED; both new skills inserted alphabetically between propose-interactive and review-implementation-auto; JeisKappa-skills unchanged at 7)
    - .vscode/settings.json (conventionalCommits.scopes 34 -> 36 — Phase 6 final scope count REACHED; review-code-auto + review-code-interactive inserted alphabetically between report-to-the-owner and review-implementation-auto; array remains alphabetically sorted)
    - README.md (Available skills 34 -> 36 — Phase 6 final README state REACHED; both new entries inserted between propose-interactive and the verification-coverage note, above ## Retired skills; SHORT 1-line > **Note:** the-fool adversarial-delegation blockquote ADDED directly above the new review-code-auto entry — Phase 6's SECOND short README documentation note alongside Plan 06-04's verification-coverage note)

key-decisions:
  - "Four-axis general-purpose code review per D86 — quality / safety / idioms / testability. Each axis carries explicit severity heuristics (e.g., race conditions = blocker because they manifest non-deterministically; missing tests for new behavior = nit-to-issue depending on project test convention; unreadable code = issue-or-nit depending on centrality). The four-axis enumeration is the structurally tightest review target in Phase 6 — appropriate because the code-review target is the most general and least source-dependent of the five review targets. Both skill bodies enumerate the four primary axes by name in dedicated ## What This Skill Reviews sections with per-axis severity heuristics, plus an optional-additional-axes subsection for executor's discretion (performance / dependency hygiene / API design / accessibility / documentation drift)."
  - "ONE mandatory input per D86 — a code reference (git ref / commit range / diff text / saved diff path / file path / directory path). NO source artifact is required. This is the distinguishing trait from Plan 06-04's TWO-input contract (review-implementation-* requires BOTH a code reference AND a source artifact). When the user HAS a source artifact AND wants code-vs-original-intent fidelity, the skill redirects to review-implementation-*. The skill MAY accept freeform user focus areas as additional input but does NOT block on their absence — the general-purpose pass runs without any prior structure."
  - "Explicit redirection to review-implementation-* when source artifact exists — both skill bodies state plainly: if you have a source artifact AND want fidelity, use review-implementation-* instead. The redirection appears in the opening clarifier, in the What This Skill Reviews section (under 'What this skill does NOT review'), and in Next Actions as a recommended escalation. This is the key contract-line dividing the two complementary review targets — they answer different questions: review-implementation-* answers 'does the code do what the source said?', review-code-* answers 'is the code any good on its own terms?'."
  - "SHORT README the-fool adversarial-delegation blockquote per D88, REVW-09 — 1-line > **Note:** stating V1 adversarial review is delegated to the external the-fool skill (no native V1 adversarial-review skill). Placed directly above the new review-code-auto entry, satisfying REVW-09's visibility requirement (a user scanning the README for 'adversarial' or 'the-fool' will find the keyword in the blockquote text). Per CONTEXT.md the note MUST be ≤3 lines and findable by a user scanning for the keyword — both requirements satisfied. The placement is executor's discretion per CONTEXT.md (options were: near the review-* entries as a small subsection, inline with the proposal/spec entries, or as a single italic / blockquote line near any review-* entry); chose the SHORT blockquote pattern established by Plan 06-04 for the verification-coverage note. Two distinct documentation notes coexist in Phase 6's final README state."
  - "NO the-fool reference inside review-code-* skill bodies — per CONTEXT.md (lines 96-97), the the-fool delegation hint is recommended only for review-proposal-* (Plan 06-01) and review-spec-* (Plan 06-02) where adversarial pressure is most useful at early shape-defining stages. review-code-* does NOT carry the delegation note in the skill body — the README-level note is the SOLE in-Phase-6 the-fool reference for code-target review territory. The skill bodies redirect to review-implementation-* (for source-artifact-fidelity) and to review-proposal-* / review-spec-* / review-plan-* (for upstream-artifact-review) instead. Verified by automated negative grep on both new SKILL.md files."
  - "Walk grain TWO grains (per-finding OR per-file / per-hunk) for review-code-interactive — Plan 06-04's third grain (per-source-acceptance-item / per-plan-task) does NOT apply here because there is no source artifact to walk. Per-finding is the recommended default when findings cluster across the four axes; per-file / per-hunk is the recommended default for small localized diffs. Same flexible-grain pattern as Plans 06-02 and 06-03; only the third grain is dropped, consistent with the ONE-input contract reversion."
  - "Severity vocabulary `blocker | issue | nit` — adopted verbatim from Plans 06-01 / 06-02 / 06-03 / 06-04 (V1 review-family standard). Per-target meaning of severity differs (e.g., blocker here is 'race conditions / security issues / safety violations / unhandled critical edge cases'; in 06-04 it was 'the source's contract is unmet'; in 06-03 it was 'plan cannot reasonably reach implementation'; in 06-02 it was 'spec cannot be handed downstream')."
  - "Settlement vocabulary `resolved | rejected | accepted | deferred | parked` — adopted verbatim from Plans 06-01 / 06-02 / 06-03 / 06-04. Resolved+rejected stay in the decision log; accepted+deferred+parked dump to inbox/open/ at end-of-session per D92/D95."
  - "Anti-sycophancy stance — 4 markers from skills/discussion/SKILL.md verbatim + review-stance amplifier ('a review is most valuable when it disagrees with the author'). No implementation-stage stakes amplifier here (this is the code-review target, not the implementation-review target — although the review-stance amplifier's 'cheap moment to push back is during the walk, before the code is merged or released' captures the same intuition for general-purpose code review)."
  - "Read-only-against-code discipline — explicit in both skill bodies. The skill does NOT check out branches, does NOT run tests, does NOT modify the working tree, and does NOT mutate any git state. Code modifications are an implement-* skill's job per Phase 5. The review is read-only against the code text — if the review surfaces findings that require code changes, the surrounding session invokes the appropriate implement-* skill on a fresh invocation. Carries forward Plan 06-04's read-only-against-implementation discipline verbatim."
  - "README the-fool blockquote placement — directly above the new review-code-auto entry as a 1-line `> **Note:**` blockquote (same SHORT-blockquote pattern Plan 06-04 established for the verification-coverage note). Executor discretion per CONTEXT.md was {italicized line above, blockquote above, inline parenthetical}; chose blockquote for visibility (a user scanning the README for 'adversarial' or 'the-fool' will find both keywords in the blockquote text). Per CONTEXT.md the note MUST be ≤3 lines and findable by a user scanning for the keyword — both requirements satisfied."
  - "`<kebab-desc>` slug for decision log + review-finding — recommended `<code-reference-summary>-code-review` (e.g., `auth-module-code-review`, `pr-42-code-review`, `caching-layer-code-review`). The interactive sibling confirms slug with the user before first settlement; the auto sibling treats the code-reference summary as authoritative."
  - "Phase 6 completion claim — this plan ships the LAST of the 5 paired review plans (06-01 proposal, 06-02 spec, 06-03 plan, 06-04 implementation, 06-05 code). Phase 6 ROADMAP entry is ready to flip to COMPLETE after verification. The 10 new V1 review skills are now installable via JeisKappa-workflow (29 entries); the legacy review-decision-document is retired (Plan 06-02); two SHORT README documentation notes (verification + the-fool) sit alongside the Available skills entries."

patterns-established:
  - "review-code-* body shape: opening clarifier naming sibling + review-implementation-* redirection (the comparator with a source-artifact requirement) + Phase 6 review siblings (review-proposal-* upstream, review-spec-* upstream, review-plan-* upstream, review-implementation-* parallel-with-source); ## Inputs with ONE input (code reference: git ref / commit range / diff text / saved diff path / file path / directory path) plus optional freeform focus areas; ambiguity fallback per docs/workflow/v1/immutability.md; ## What This Skill Reviews enumerating the four D86 general-purpose axes with per-axis severity heuristics (quality / safety / idioms / testability) + optional additional axes (performance / dependency / API design / accessibility / documentation drift); explicit 'What this skill does NOT review' subsection with redirection to review-implementation-* (when source artifact exists), review-proposal-* / review-spec-* / review-plan-* (when findings trace back to upstream artifact ambiguity), and implement-* (for code modifications); ## Findings Report Shape with 6 required sections (Verdict / Findings / Evidence / References / Open Questions / Next Actions); ## Output Artifact naming inbox/open/<UTC>-<kebab-desc>-review-finding.md per Phase 1 record-form grammar; ## Workflow with READ-ONLY-code-read + 4-axis sequence + optional-additional-axis discretion + UTC-stamp + write; ## Commit Policy NEVER auto-commits + does-not-modify-code; ## Immutability citing docs/workflow/v1/immutability.md by absolute path + naming code under review as read-only. Interactive sibling adds Anti-Sycophancy Stance (4 markers verbatim + review-stance amplifier) + Walk Format (per-finding OR per-file / per-hunk, executor's discretion; per-iteration loop with single-citation + D89 ASK-AND-TEST against the code) + Output Artifacts (always decision-log; conditional review-finding per D92/D95) + Decision Log Lazy Creation + Scope Drift section. NO the-fool reference inside SKILL bodies (README-level note covers V1 adversarial-delegation globally). NO ## V1 Verification Role subsection (Plan 06-04's territory)."
  - "ONE-input + ONE-citation pattern in the code review target (REVERTING from Plan 06-04's TWO-input + TWO-citation pattern) — every other review target except review-implementation-* takes ONE primary input. The code review takes ONE (the code reference) because the question is general-purpose quality, not fidelity. The one-input pattern matches Plans 06-01/06-02/06-03 (proposal/spec/plan reviews) and reverts from Plan 06-04 (implementation review's two-input contract). Plan 06-05 closes the symmetry: review-code-* sits parallel to review-implementation-* in the source-artifact axis — one mandatory source vs zero mandatory sources — sharing the rest of the review-family body shape."
  - "Documentation-note pattern for V1 decisions that subsume what users might expect — V1 ships TWO short README documentation notes in Phase 6 because two V1 decisions subsume what users might expect to find as separate skills: D85 (review-implementation-* covers verification; no separate verify-*) and D88 (review-* delegates adversarial pressure to the external the-fool skill; no native V1 adversarial review). Each decision gets ONE SHORT (≤3 lines) `> **Note:**` blockquote placed directly above the relevant skill entries. The pattern is reusable for any future V1 decision that subsumes user expectation: a SHORT in-skill statement (when applicable) PLUS a SHORT README note findable by scanning for the keyword the user would search for ('verify' for D85; 'adversarial' or 'the-fool' for D88). Two distinct blockquotes coexist — Phase 7 may add a third when the README hybrid lands."
  - "Phase 6 plugin / scope / README final-state pattern — after the FIFTH and FINAL paired review plan, JeisKappa-workflow sits at exactly 29 entries (Phase 6 target REACHED from initial 19; Phase 6 added 10), JeisKappa-skills sits at 7 (Phase 6 target REACHED from initial 8 with -1 retirement for review-decision-document via Plan 06-02), .vscode scopes sits at exactly 36 (Phase 6 final REACHED from initial 27; +10 new -1 retired), README Available skills sits at exactly 36 (Phase 6 final REACHED with same arithmetic), README Retired skills subsection has 2 entries (discussion-loop from Phase 2 + review-decision-document from Plan 06-02), and README documentation notes has 2 entries (verification-coverage from Plan 06-04 + the-fool delegation from this plan). All four registration files reach their Phase 6 target state simultaneously."

requirements-completed: [REVW-05, REVW-06, REVW-07, REVW-08, REVW-09]

duration: 8min
completed: 2026-05-21
---

# Phase 6 Plan 05: Review-Code Pair Summary

**`review-code-auto` (autonomous four-axis general-purpose code review per D86 — quality / safety / idioms / testability — with optional additional axes performance / dependency hygiene / API design / accessibility / documentation drift; 6-section findings-first report to inbox/open/<UTC>-<kebab-desc>-review-finding.md; ONE-input contract — code reference only, NO source artifact; explicit redirection to review-implementation-* when source artifact exists) + `review-code-interactive` (per-finding OR per-file / per-hunk ASK-and-TEST walk with 4 anti-sycophancy markers verbatim + review-stance amplifier; decision log + conditional inbox dump per D92/D95) + SHORT README the-fool adversarial-delegation blockquote (per D88, REVW-09) shipped as the FIFTH and FINAL paired-review plan in Phase 6; pattern continuity with Plans 06-01/06-02/06-03/06-04 codified; the ONE-input contract reverts from Plan 06-04's TWO-input contract because review-code-* is general-purpose review without a source artifact (D86); Phase 6 reaches its target state: 29 workflow plugin entries, 36 scopes, 36 README Available skills entries, 2 README documentation notes (verification + the-fool). REVW-05 marked complete; Phase 6 ROADMAP entry ready to flip to COMPLETE.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-05-21T13:59:33Z
- **Completed:** 2026-05-21T14:07:37Z
- **Tasks:** 3
- **Files modified:** 5 (2 created, 3 modified)

## Accomplishments

- `review-code-auto/SKILL.md` ships the autonomous V1 general-purpose code review per D86: runs the four primary general-purpose code-review axes (quality / safety / idioms / testability) against the ONE mandatory input (a code reference — git ref / commit range / diff text / saved diff path / file path / directory path). NO source artifact is required — this is the distinguishing trait of review-code-* vs review-implementation-*. The skill MAY accept freeform user focus areas as additional input but does NOT block on their absence — the default general-purpose pass runs across all axes. Emits a 6-section findings-first report (Verdict / Findings with severity tags `blocker | issue | nit` / Evidence with file:line citations / References / Open Questions / Next Actions) to `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md`. Carries the optional-additional-axes subsection (performance / dependency hygiene / API design / accessibility / documentation drift) as executor's discretion for code under review that warrants it. Explicit redirection note to `review-implementation-auto` / `review-implementation-interactive` when the user HAS a source artifact and wants code-vs-original-intent fidelity. NO `## Anti-Sycophancy Stance` section (auto half). NO `the-fool` reference (README-level note covers V1 adversarial-delegation globally).
- `review-code-interactive/SKILL.md` ships the collaborative V1 general-purpose code review: per-finding OR per-file / per-hunk walk (executor's discretion — two grains because there is no source artifact to walk, reverting Plan 06-04's third per-source-acceptance-item / per-plan-task grain); ASKs the user for their view AND TESTs the user's explanation against the code (D89; explicit "do not just accept" phrasing); five-way settlement (resolved / rejected / accepted / deferred / parked); 4 anti-sycophancy markers carried verbatim from `skills/discussion/SKILL.md` + review-stance amplifier ("a review is most valuable when it disagrees with the author"); decision log at `docs/threads/<thread>/discussions/<UTC>-<kebab-desc>-decision-log.md` (lazy creation at first settlement); conditional inbox dump at `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md` per D92/D95 ("no Inbox file when nothing remains"). Carries the same explicit redirection note to `review-implementation-*` as the auto sibling. NO `the-fool` reference inside the skill body (README-level note covers V1 adversarial-delegation globally).
- Both new skills enumerate the four D86 general-purpose axes by name with per-axis severity heuristics:
  - **Quality** — readability, maintainability, naming, structure, code smells, dead code, duplication
  - **Safety** — bug risks, edge cases, error handling, race conditions (typically blocker because non-deterministic), security issues (typically blocker), resource leaks
  - **Idioms** — language idioms, framework idioms, project convention violations, API ergonomics
  - **Testability** — untestable code (tight coupling to globals, hidden side effects), missing tests for new behavior, weak tests, integration vs unit balance
- Both new skills include the optional-additional-axes subsection listing performance / dependency hygiene / API design / accessibility / documentation drift as executor's discretion. The four primary axes are the V1 standard for review-code-*; everything else is review territory the executor uses judgment to include or omit.
- Both new skills are explicitly READ-ONLY against the code — they do NOT check out branches, do NOT run tests, do NOT modify the working tree, and do NOT mutate any git state. Code modifications are an `implement-*` skill's job per Phase 5; the review's role is review only. This discipline is stated in both the `## Inputs` section AND the `## Workflow` section AND the `## Commit Policy` section AND the `## Immutability` section.
- All three V1 registration touchpoints updated atomically with Phase 6 target state REACHED:
  - `marketplace.json`: `JeisKappa-workflow.skills` 27 → 29 (`review-code-auto` + `review-code-interactive` inserted alphabetically between `./skills/propose-interactive` and `./skills/review-implementation-auto`); `JeisKappa-skills` unchanged at 7. **Phase 6 target plugin count REACHED (29).** JSON validity preserved.
  - `.vscode/settings.json`: `conventionalCommits.scopes` 34 → 36 (`review-code-auto` + `review-code-interactive` added between `report-to-the-owner` and `review-implementation-auto`; full array remains alphabetically sorted). **Phase 6 final scope count REACHED (36).**
  - `README.md`: Available skills 34 → 36 (both new entries inserted between `propose-interactive` and the verification-coverage note, above the `## Retired skills` subsection); each entry has heading + description paragraph (with explicit interaction mode per DIST-05; mentions general-purpose code review with no source artifact; mentions the four D86 axes; explicit redirection to review-implementation-* when source artifact exists) + fenced `npx skills add` install snippet. **Plus** a SHORT (1-line) `> **Note:**` the-fool adversarial-delegation blockquote inserted directly above the new entries stating: "V1 adversarial review is delegated to the external `the-fool` skill — no native V1 adversarial-review skill. Use `the-fool` against a proposal or spec draft to surface adversarial risks before the spec / plan / implementation phases (per D88, REVW-09)." Placed as a blockquote so a user scanning the README for "adversarial" or "the-fool" will find both keywords. **Phase 6 final README state REACHED (36 Available skills entries + 2 documentation notes).** Plan 06-04's verification-coverage note remains untouched directly above `review-implementation-auto`.
- Pattern continuity: the V1 review-pair body shape codified by Plans 06-01, 06-02, 06-03, and 06-04 is reused. The deltas for this plan are: (a) the four-axis general-purpose enumeration (quality / safety / idioms / testability) replaces the five-axis fidelity enumeration of Plan 06-04, the four-axis plan review of Plan 06-03, the 8 D50 elements of spec review (06-02), and the lightweight gaps/risks/ambiguities of proposal review (06-01); (b) the ONE-input contract reverts from Plan 06-04's TWO-input contract because there is no source artifact (D86); (c) the ONE-citation Evidence requirement reverts from Plan 06-04's TWO-citation requirement; (d) the SHORT README the-fool adversarial-delegation note replaces Plan 06-04's verification-coverage note (different content; same SHORT-blockquote placement pattern as established by Plan 06-04); and (e) this plan completes REVW-09 — the SECOND of the two REVW-09 README notes is now in place alongside Plan 06-04's verification-coverage note.

## Task Commits

Each task was committed atomically:

1. **Task 1: Author skills/review-code-auto/SKILL.md** — `be6b024` (feat)
2. **Task 2: Author skills/review-code-interactive/SKILL.md** — `4f613a8` (feat)
3. **Task 3: Register review-code-* in 3 touchpoints + SHORT README the-fool delegation note** — `4eef181` (chore)

## Files Created/Modified

- `skills/review-code-auto/SKILL.md` — autonomous V1 general-purpose code review skill; runs four D86 general-purpose code-review axes (quality / safety / idioms / testability) plus optional additional axes (performance / dependency / API design / accessibility / documentation drift); ONE-input contract (code reference only, NO source artifact); emits 6-section findings-first report to `inbox/open/<UTC>-<kebab-desc>-review-finding.md`; explicit redirection note to review-implementation-* when source artifact exists.
- `skills/review-code-interactive/SKILL.md` — collaborative V1 general-purpose code review skill; per-finding OR per-file / per-hunk ASK-and-TEST walk; emits decision log at `discussions/` + conditional inbox dump at `inbox/open/`; carries the 4 anti-sycophancy markers verbatim from `discussion` + review-stance amplifier; explicit redirection note to review-implementation-* when source artifact exists.
- `.claude-plugin/marketplace.json` — JeisKappa-workflow.skills 27 → 29 (review-code-* added alphabetically). **Phase 6 target plugin count REACHED.** JeisKappa-skills unchanged at 7.
- `.vscode/settings.json` — conventionalCommits.scopes 34 → 36 (review-code-* added alphabetically; full array remains alphabetical). **Phase 6 final scope count REACHED.**
- `README.md` — Available skills 34 → 36 (review-code-* inserted between `propose-interactive` and the verification-coverage note, above the `## Retired skills` subsection). **Phase 6 final README state REACHED.** SHORT (1-line) `> **Note:**` the-fool adversarial-delegation blockquote ADDED directly above the new review-code-auto entry — Phase 6's SECOND short README documentation note alongside Plan 06-04's verification-coverage note (D88, REVW-09).

## Decisions Made

- **Four-axis general-purpose code review per D86** — quality / safety / idioms / testability. Each axis carries explicit severity heuristics tethered to downstream impact. The four-axis enumeration is the structurally tightest review target in Phase 6 — appropriate because review-code-* is the most general and least source-dependent of the five review targets. Both skill bodies enumerate the four primary axes by name in dedicated `## What This Skill Reviews` sections with per-axis severity heuristics, plus an optional-additional-axes subsection for executor's discretion (performance / dependency hygiene / API design / accessibility / documentation drift).
- **ONE mandatory input per D86** — a code reference (git ref / commit range / diff text / saved diff path / file path / directory path). NO source artifact is required. This is the distinguishing trait from Plan 06-04's TWO-input contract. When the user HAS a source artifact AND wants fidelity, the skill redirects to review-implementation-*. The skill MAY accept freeform user focus areas as additional input but does NOT block on their absence.
- **Explicit redirection to review-implementation-* when source artifact exists** — both skill bodies state plainly: if you have a source artifact AND want fidelity, use review-implementation-* instead. The redirection appears in the opening clarifier, in the What This Skill Reviews section, and in Next Actions as a recommended escalation. This is the key contract-line dividing the two complementary review targets.
- **SHORT README the-fool adversarial-delegation blockquote per D88, REVW-09** — 1-line `> **Note:**` stating V1 adversarial review is delegated to the external the-fool skill (no native V1 adversarial-review skill). Placed directly above the new review-code-auto entry. Satisfies REVW-09's visibility requirement (a user scanning for "adversarial" or "the-fool" will find both keywords). Per CONTEXT.md the note MUST be ≤3 lines and findable by scanning for the keyword — both requirements satisfied. The placement is executor's discretion per CONTEXT.md (options were: near review-* entries as a small subsection, inline with proposal/spec entries, or as a single italic / blockquote line near any review-* entry); chose the SHORT blockquote pattern established by Plan 06-04. Two distinct documentation notes coexist in Phase 6's final README state.
- **NO the-fool reference inside review-code-* skill bodies** — per CONTEXT.md (lines 96-97), the the-fool delegation hint is recommended only for review-proposal-* (Plan 06-01) and review-spec-* (Plan 06-02). review-code-* does NOT carry the delegation note in the skill body — the README-level note is the SOLE in-Phase-6 the-fool reference for code-target review territory. Verified by automated negative grep on both new SKILL.md files (`! grep -qF 'the-fool' skills/review-code-*/SKILL.md`).
- **Walk grain TWO grains (per-finding OR per-file / per-hunk)** — Plan 06-04's third grain (per-source-acceptance-item / per-plan-task) does NOT apply because there is no source artifact to walk. Per-finding is the recommended default when findings cluster across the four axes; per-file / per-hunk is the recommended default for small localized diffs.
- **Severity vocabulary `blocker | issue | nit`** — adopted verbatim from Plans 06-01 / 06-02 / 06-03 / 06-04. Per-target meaning of severity differs (here blocker = race conditions / security issues / safety violations / unhandled critical edge cases).
- **Settlement vocabulary `resolved | rejected | accepted | deferred | parked`** — adopted verbatim from Plans 06-01 / 06-02 / 06-03 / 06-04. Resolved+rejected stay in the decision log; accepted+deferred+parked dump to inbox/open/ at end-of-session per D92/D95.
- **Anti-sycophancy stance** — 4 markers from `skills/discussion/SKILL.md` verbatim + review-stance amplifier ("a review is most valuable when it disagrees with the author"). No implementation-stage stakes amplifier (this is code-review, not implementation-review; the review-stance amplifier's "cheap moment to push back is during the walk, before the code is merged or released" captures the general-purpose intuition).
- **Read-only-against-code discipline** — explicit in both skill bodies. The skill does NOT check out branches, does NOT run tests, does NOT modify the working tree, and does NOT mutate any git state. Code modifications are an `implement-*` skill's job per Phase 5. The review is read-only against the code text. Carries forward Plan 06-04's read-only-against-implementation discipline verbatim.
- **README the-fool blockquote placement** — directly above the new review-code-auto entry as a 1-line `> **Note:**` blockquote (same SHORT-blockquote pattern Plan 06-04 established). Chose blockquote for visibility (a user scanning for "adversarial" or "the-fool" will find both keywords). Per CONTEXT.md ≤3 lines and findable — both requirements satisfied.
- **`<kebab-desc>` slug for decision log + review-finding** — recommended `<code-reference-summary>-code-review` (e.g., `auth-module-code-review`, `pr-42-code-review`, `caching-layer-code-review`). The interactive sibling confirms slug with the user before first settlement; the auto sibling treats the code-reference summary as authoritative.
- **Phase 6 completion** — this plan ships the LAST of the 5 paired review plans (06-01 proposal, 06-02 spec, 06-03 plan, 06-04 implementation, 06-05 code). The Phase 6 ROADMAP entry is ready to flip to COMPLETE after verification of all 5 plans.

## Deviations from Plan

None — both new skill authoring tasks landed cleanly on the first verify pass; the registration task's three atomic edits across `marketplace.json` + `.vscode/settings.json` + `README.md` all passed every automated verify check (workflow plugin count = 29, skills plugin count = 7, scopes count = 36, scopes alphabetically sorted, README heading counts, install-snippet counts, the-fool-note presence, verification-note-preservation, position-before-Retired ordering) on the first run.

The ONE-input pattern reverts cleanly from Plan 06-04's TWO-input contract — both skill bodies carry the one-input contract in `## Inputs`, the one-citation requirement in `## Evidence`, the explicit redirection to review-implementation-* in the opening clarifier + `## What This Skill Reviews` + `## Next Actions`, and the READ-ONLY-against-code discipline in `## Workflow` + `## Commit Policy` + `## Immutability`. The interactive sibling carries the ASK-AND-TEST-against-the-code discipline in `## Walk Format` step 4.

The the-fool delegation note landed in the README as a SHORT `> **Note:**` blockquote per the executor-discretion options in CONTEXT.md, sitting directly above the new review-code-auto entry. Plan 06-04's verification-coverage note remains untouched directly above review-implementation-auto. Two distinct documentation notes coexist.

The NO-the-fool-in-skill-body invariant landed cleanly — both new SKILL.md files pass the `! grep -qF 'the-fool'` negative check. The README-level note is the SOLE the-fool reference in Phase 6's review-code-* territory (consistent with CONTEXT.md lines 96-97 which recommend the in-skill the-fool delegation hint ONLY for review-proposal-* and review-spec-*).

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required. The README the-fool adversarial-delegation note points users at the external `the-fool` skill (V2 / out-of-V1-scope per D88); installation of `the-fool` is the user's choice and out of scope for Phase 6.

## Next Phase Readiness

- **Phase 6 is COMPLETE.** All 5 plans landed: 06-01 (review-proposal-*), 06-02 (review-spec-* + review-decision-document retirement), 06-03 (review-plan-*), 06-04 (review-implementation-* + README verification-coverage note), 06-05 (review-code-* + README the-fool adversarial-delegation note). 10 new V1 review skills shipped. 1 legacy skill retired. 2 SHORT README documentation notes added. Phase 6 ROADMAP entry ready to flip to COMPLETE.
- After this plan: JeisKappa-workflow plugin sits at exactly 29 entries (Phase 6 target REACHED from initial 19; Phase 6 added 10); JeisKappa-skills plugin sits at exactly 7 entries (Phase 6 target REACHED from initial 8 with -1 retirement for review-decision-document via Plan 06-02); .vscode scopes at exactly 36 (Phase 6 final REACHED from initial 27; +10 new -1 retired); README Available skills at exactly 36 (Phase 6 final REACHED with same arithmetic); README Retired skills subsection unchanged with 2 entries (discussion-loop from Phase 2 + review-decision-document from Plan 06-02); README documentation notes now has 2 entries (verification-coverage from Plan 06-04 + the-fool delegation from this plan; Phase 6 final state).
- **REVW-09 is COMPLETE** — both REVW-09 README notes are now in place (verification-coverage from Plan 06-04 + the-fool delegation from this plan).
- **REVW-05 is COMPLETE** — the review-code-* pair ships the general-purpose / no-mandatory-source contract per D86.
- Phase 7 (Merge / Finish / Navigation + Distribution surface + README hybrid) is now unblocked. The two SHORT README documentation notes added in Phase 6 (verification + the-fool) are minimal and non-disruptive — Phase 7's README hybrid (toolbox model, layered map, recommended paths) can fold them into its richer structure or leave them in place; that is Phase 7's design call. The V1 review-family body shape codified across Plans 06-01..06-05 is now solidly reusable for any future V1 review additions (V2 territory per D88: native adversarial-review, multi-angle subagent reviewers, cross-thread aggregation, auto-fix-from-findings chains).

---
*Phase: 06-review-family*
*Completed: 2026-05-21*

## Self-Check: PASSED

- Files created: skills/review-code-auto/SKILL.md, skills/review-code-interactive/SKILL.md — both FOUND on disk.
- Files modified: .claude-plugin/marketplace.json, .vscode/settings.json, README.md, .planning/phases/06-review-family/06-05-SUMMARY.md — all FOUND on disk.
- Commits be6b024 (feat: review-code-auto), 4f613a8 (feat: review-code-interactive), 4eef181 (chore: register 3 touchpoints + README the-fool delegation note) — all FOUND in git log.
- Final registration counts verified: marketplace.json JeisKappa-workflow=29 (Phase 6 target REACHED), JeisKappa-skills=7; .vscode/settings.json scopes=36 (Phase 6 final, alphabetical); README.md Available skills=36 (Phase 6 final), 2 documentation notes (verification + the-fool), Retired skills unchanged with 2 entries.
- Behavioral invariants verified: both review-code-* SKILL.md files contain the 4 anti-sycophancy markers (interactive only), the no-source-required statement, the redirection to review-implementation-*, D89 ASK-AND-TEST phrasing (interactive only), D92/D95 conditional-inbox phrasing (interactive only), and NO `the-fool` reference inside the skill bodies (verified by automated negative grep).
