---
phase: 06-review-family
plan: 05
type: execute
wave: 5
depends_on:
  - 06-04
files_modified:
  - skills/review-code-auto/SKILL.md
  - skills/review-code-interactive/SKILL.md
  - .claude-plugin/marketplace.json
  - .vscode/settings.json
  - README.md
autonomous: true
requirements:
  - REVW-05
  - REVW-06
  - REVW-07
  - REVW-08
  - REVW-09

must_haves:
  truths:
    - "User can invoke `review-code-auto` against a code artifact reference (git ref / diff / file path / directory) WITHOUT providing a spec or plan and receive a findings-first report at `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md` covering general-purpose code review axes — quality, safety, idioms, testability per D86 (REVW-05). The review does NOT require a source artifact — this is the distinguishing trait from `review-implementation-*`."
    - "User can invoke `review-code-interactive` against the same kinds of inputs and walk one finding / one file / one hunk at a time with ASK-and-TEST against the code per D89; decision log + opt-in inbox dump per D92/D95."
    - "User reading either review-code skill body sees the explicit statement that NO mandatory source artifact is required (D86) — this is the differentiator from review-implementation-*. The literal phrase 'no mandatory source artifact' or 'general-purpose code review' or 'spec-independent' (or near-verbatim equivalent making the no-source-required nature explicit) MUST appear."
    - "User reading either review-code skill body sees a clear redirection note: if the user has a source artifact AND wants to check code-vs-original-intent, point them at `review-implementation-auto` / `review-implementation-interactive` instead."
    - "User reading `review-code-interactive` finds the 4 anti-sycophancy markers verbatim + review-stance amplifier."
    - "User reading `README.md` finds a SHORT (≤3 lines) documentation note near the review-* entries (or near the proposal/spec entries — executor discretion per CONTEXT.md) stating that V1 adversarial review is delegated to the external `the-fool` skill — no native V1 adversarial-review skill (per D88, REVW-09). The literal phrase 'the-fool' MUST appear in the note."
    - "User running `npx skills add` finds both new skills installable via JeisKappa-workflow (27 -> 29) — the Phase 6 target plugin count is reached."
  artifacts:
    - path: "skills/review-code-auto/SKILL.md"
      provides: "Autonomous general-purpose code review (no mandatory source artifact)"
      contains: "frontmatter with name=review-code-auto, version=1.0.0; ## Inputs naming code references (git ref / diff / path / directory) WITHOUT a mandatory source artifact; ## What This Skill Reviews enumerating general-purpose code-review axes (quality / safety / idioms / testability per D86); explicit no-source-required statement; redirection note to review-implementation-* when a source artifact exists; ## Findings Report Shape with 6 sections; ## Output Artifact at inbox/open/<UTC>-<kebab-desc>-review-finding.md; citations to Phase 1 docs"
    - path: "skills/review-code-interactive/SKILL.md"
      provides: "Collaborative general-purpose code review — per-finding walk + 4-marker anti-sycophancy + decision log + opt-in inbox dump"
      contains: "frontmatter with name=review-code-interactive, version=1.0.0; ## Anti-Sycophancy Stance with 4 verbatim markers + review-stance amplifier; ## Inputs (code references; no mandatory source); ## What This Skill Reviews; explicit no-source-required statement; redirection note; ## Walk Format; ## Output Artifacts (decision log + conditional inbox dump); D89 / D92 / D95 phrasings"
    - path: ".claude-plugin/marketplace.json"
      provides: "JeisKappa-workflow grows by 2 — Phase 6 target plugin count (29) reached"
      contains: "./skills/review-code-auto + ./skills/review-code-interactive ADDED (length 27 -> 29). JeisKappa-skills unchanged at 7."
    - path: ".vscode/settings.json"
      provides: "Scopes grow by 2; alphabetical — Phase 6 final scope count (36) reached"
      contains: "review-code-auto + review-code-interactive added to conventionalCommits.scopes (length 34 -> 36)"
    - path: "README.md"
      provides: "Available skills grows by 2 (final 36) + new SHORT the-fool delegation note added near the review-* entries (or proposal/spec entries)"
      contains: "### [`review-code-auto`](./skills/review-code-auto/SKILL.md) + ### [`review-code-interactive`](./skills/review-code-interactive/SKILL.md) headings ADDED; SHORT (≤3 lines) the-fool-delegation note stating V1 adversarial review is delegated to the external the-fool skill (D88, REVW-09)"
  key_links:
    - from: "skills/review-code-auto/SKILL.md"
      to: "skills/review-implementation-auto/SKILL.md"
      via: "redirection note — when user HAS a source artifact, point to review-implementation-* instead"
      pattern: "review-implementation"
    - from: "skills/review-code-interactive/SKILL.md"
      to: "skills/discussion/SKILL.md"
      via: "anti-sycophancy stance carried verbatim"
      pattern: "Disagree when you disagree|Do not treat pushback as correctness"
    - from: "README.md"
      to: "the-fool skill (external)"
      via: "SHORT delegation note stating V1 adversarial review is delegated to the-fool"
      pattern: "the-fool"
---

<objective>
Ship the FIFTH and FINAL review pair of Phase 6: `review-code-auto` and `review-code-interactive`, both targeting code artifacts (git ref / diff / file path / directory) for GENERAL-PURPOSE code review per D86 (REVW-05). The distinguishing trait of this pair vs `review-implementation-*` (Plan 06-04) is that NO source artifact is required — `review-code-*` performs spec-independent code review focusing on quality, safety, idioms, and testability. Both skills include a redirection note pointing users at `review-implementation-*` when a source artifact exists and the user wants to check code-vs-original-intent fidelity. Add a SHORT documentation note to README.md stating V1 adversarial review is delegated to the external `the-fool` skill (per D88, REVW-09). This is the final REVW-09 contribution that completes Phase 6.

Purpose: This is the last paired review plan. The code target is the most general — no upstream artifact required — and complements `review-implementation-*` which DOES require a source artifact. Together the two cover the "with source" and "without source" code-review cases. The README the-fool delegation note completes the REVW-09 documentation surface alongside the verification-coverage note added in Plan 06-04.

Output: Two new skill folders + three registration files updated + one SHORT README the-fool delegation note. After this plan: workflow plugin 27 -> 29 (Phase 6 target reached); .vscode scopes 34 -> 36; README "Available skills" 34 -> 36 with a new the-fool delegation note added. Phase 6 ROADMAP entry can flip to COMPLETE after verification.

Depends on Plan 06-04 because shared marketplace/scopes/README files (sequential serialization) and the paired-review body shape established in Plans 06-01..06-04 is reused.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-review-family/06-CONTEXT.md
@AGENTS.md

@docs/workflow/v1/README.md
@docs/workflow/v1/thread-layout.md
@docs/workflow/v1/filename-grammar.md
@docs/workflow/v1/immutability.md

<!-- Anti-sycophancy + walk-pattern source -->
@skills/discussion/SKILL.md
@skills/seeded-discussion/SKILL.md

<!-- Plans 06-01..06-04 — established the review-skill body shape this plan reuses; review-implementation-* is the sibling that DOES require a source artifact (this plan's distinguishing trait) -->
@skills/review-proposal-auto/SKILL.md
@skills/review-spec-auto/SKILL.md
@skills/review-plan-auto/SKILL.md
@skills/review-implementation-auto/SKILL.md
@skills/review-proposal-interactive/SKILL.md
@skills/review-spec-interactive/SKILL.md
@skills/review-plan-interactive/SKILL.md
@skills/review-implementation-interactive/SKILL.md

<!-- Registration files being edited -->
@.claude-plugin/marketplace.json
@.vscode/settings.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author skills/review-code-auto/SKILL.md</name>
  <files>skills/review-code-auto/SKILL.md</files>
  <read_first>
    - skills/review-code-auto/SKILL.md (the file being created — empty / non-existent)
    - .planning/phases/06-review-family/06-CONTEXT.md (LOCKED: D81 / D86 review-code-* general-purpose, NO mandatory source artifact)
    - skills/review-implementation-auto/SKILL.md (Plan 06-04 — sibling that REQUIRES a source artifact; this skill explicitly differentiates from that one)
    - skills/review-proposal-auto/SKILL.md (Plan 06-01 — first established the review-skill body shape)
    - skills/review-spec-auto/SKILL.md (Plan 06-02 — handoff-grade-bar variant; prose density reference)
    - skills/review-plan-auto/SKILL.md (Plan 06-03 — plan-target variant; loose-vs-strict detection reference)
    - docs/workflow/v1/filename-grammar.md
    - docs/workflow/v1/thread-layout.md
    - docs/workflow/v1/immutability.md
  </read_first>
  <action>
    Create the file with YAML frontmatter and a markdown body. Frontmatter required fields:
    - `name: review-code-auto`
    - `description:` ONE sentence with "Use when..." trigger AND explicit interaction mode (autonomous; general-purpose code review; NO mandatory source artifact; emits findings-first report; no clarifying questions). Mention sibling `review-code-interactive`. Mention `review-implementation-auto` / `review-implementation-interactive` as the alternative when the user HAS a source artifact and wants code-vs-original-intent fidelity instead.
    - `metadata.author: https://github.com/Jei-sKappa`
    - `metadata.version: 1.0.0`

    Body sections (executor discretion on names; behavioral tokens below MUST appear):

    1. **Opening clarifier paragraph** naming the sibling `review-code-interactive` AND the `review-implementation-*` skills (the comparator with a source-artifact requirement). State that this skill is GENERAL-PURPOSE code review per D86 — NO source artifact required. If the user has a spec / proposal / plan / issue / Inbox item the implementation was supposed to deliver AND they want code-vs-original-intent fidelity, redirect them to `review-implementation-auto` / `review-implementation-interactive` instead.

    2. **Inputs** section. MUST accept ONE input form (code reference):
       - A git ref (commit SHA, branch name, or commit range like `main..feature-branch`)
       - A diff (unified diff text passed inline or via a path to a saved diff file)
       - A file path (single file under review)
       - A directory path (review the directory's contents)

       MUST EXPLICITLY state that NO source artifact (spec / proposal / plan / issue / Inbox item) is required. The skill MAY accept the user's freeform concerns or focus areas as additional input (e.g., "look for race conditions", "review the new caching layer", "check for missing error handling") — but does NOT block on missing concerns. If the user provides no focus, the skill runs the default general-purpose pass.

       Ambiguity fallback per `docs/workflow/v1/immutability.md` ("Ambiguous Reference Resolution") for the code reference: ASK if the input is ambiguous (e.g., "review my changes" without naming a ref). Do not pick by recency.

    3. **What This Skill Reviews** section. MUST enumerate the general-purpose code-review axes per D86:
       - **Quality** — readability, maintainability, naming, structure, code smells.
       - **Safety** — bug risks, edge cases, error handling, race conditions, security issues.
       - **Idioms** — language / framework idioms; project conventions if observable from surrounding code.
       - **Testability** — test coverage, test quality, edge cases, integration vs unit balance.

       MUST acknowledge that without a source artifact, this skill cannot check "does the code implement what was intended" — that is `review-implementation-*`'s job. This skill's findings are about code QUALITY, not code FIDELITY.

       Executor MAY add other general-purpose axes (performance characteristics, dependency hygiene, API design, accessibility, etc.) as appropriate.

    4. **Findings Report Shape** section. Same 6 sections as Plans 06-01..06-04: `## Verdict` / `## Findings` (severity tags) / `## Evidence` (cite file:line in the code under review) / `## References` (the code reference + any external references the reviewer used) / `## Open Questions` / `## Next Actions`. List all 6 by literal name.

    5. **Output Artifact** section. Emit to `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md`; `review-finding` artifact-type token MANDATORY per `docs/workflow/v1/filename-grammar.md`. ONE record per review run. Thread resolution: if `cwd` sits inside a thread root, use it; if not, ASK the user where to write the review OR auto-create a thread when the input's slug is obvious (some general-purpose code reviews may not have a natural thread — in that case the skill may suggest writing the review to a fresh thread root or, with user confirmation, ANSWER without writing if no thread context exists). Default: prefer to write the artifact to a thread.

    6. **Workflow** section with numbered steps:
       - Resolve thread (ASK if ambiguous; ASK if no thread exists for review storage).
       - Resolve the code input (ASK if ambiguous reference).
       - Capture any user-provided focus areas/concerns.
       - Read the code READ-ONLY per `docs/workflow/v1/immutability.md` (READ-ONLY applies here too — this skill does NOT modify code).
       - Run the general-purpose review axes from `## What This Skill Reviews`.
       - Draft the findings-first report covering all 6 sections.
       - Capture the UTC stamp per `docs/workflow/v1/filename-grammar.md`.
       - Write the artifact to `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md`.
       - Confirm: `Review written: <relative-path-to-the-file>`. No closing remark.

    7. **Commit Policy** section. NEVER auto-commits.

    8. **Immutability** section. Emitted review-finding immutable; code under review READ-ONLY (this skill does NOT modify source code). Cite `docs/workflow/v1/immutability.md` by absolute path.

    Voice and discipline:
    - Dense, declarative, citation-first.
    - Cite Phase 1 docs by absolute path.
    - Cite `skills/review-implementation-auto/SKILL.md` by absolute path as the redirection target when a source artifact exists.
    - NO `## Anti-Sycophancy Stance` section (this is the AUTO half).
    - NO decision-log output.
    - The "no mandatory source artifact" statement MUST be unambiguous (the literal phrase or near-verbatim equivalent like "general-purpose code review" + "no source artifact required" / "spec-independent" MUST appear).
    - NO the-fool delegation note inside the SKILL body (CONTEXT.md specifics: the the-fool hint is recommended for review-proposal-* and review-spec-* SKILL bodies; review-code-* does NOT carry it — only the README-level note in this same plan covers V1 adversarial-delegation globally).
  </action>
  <verify>
    <automated>test -f skills/review-code-auto/SKILL.md &amp;&amp; grep -qxF 'name: review-code-auto' skills/review-code-auto/SKILL.md &amp;&amp; grep -qxF '  version: 1.0.0' skills/review-code-auto/SKILL.md &amp;&amp; grep -qxF '  author: https://github.com/Jei-sKappa' skills/review-code-auto/SKILL.md &amp;&amp; grep -qF 'Use when' skills/review-code-auto/SKILL.md &amp;&amp; grep -qF 'review-code-interactive' skills/review-code-auto/SKILL.md &amp;&amp; grep -qF 'review-implementation' skills/review-code-auto/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/inbox/open/' skills/review-code-auto/SKILL.md &amp;&amp; grep -qF 'review-finding' skills/review-code-auto/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/filename-grammar.md' skills/review-code-auto/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/thread-layout.md' skills/review-code-auto/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/immutability.md' skills/review-code-auto/SKILL.md &amp;&amp; grep -qF 'skills/review-implementation-auto/SKILL.md' skills/review-code-auto/SKILL.md &amp;&amp; grep -qiE 'no mandatory source|no source artifact|general-purpose code review|spec-independent|without a source' skills/review-code-auto/SKILL.md &amp;&amp; grep -qiF 'quality' skills/review-code-auto/SKILL.md &amp;&amp; grep -qiF 'safety' skills/review-code-auto/SKILL.md &amp;&amp; grep -qiF 'idioms' skills/review-code-auto/SKILL.md &amp;&amp; grep -qiF 'testability' skills/review-code-auto/SKILL.md &amp;&amp; grep -qiF 'verdict' skills/review-code-auto/SKILL.md &amp;&amp; grep -qiF 'findings' skills/review-code-auto/SKILL.md &amp;&amp; grep -qiF 'evidence' skills/review-code-auto/SKILL.md &amp;&amp; grep -qiF 'references' skills/review-code-auto/SKILL.md &amp;&amp; grep -qiF 'open questions' skills/review-code-auto/SKILL.md &amp;&amp; grep -qiF 'next actions' skills/review-code-auto/SKILL.md &amp;&amp; grep -qiE 'NEVER (commit|auto-commit)|never auto-commit|never commit' skills/review-code-auto/SKILL.md &amp;&amp; ! grep -qxF '## Anti-Sycophancy Stance' skills/review-code-auto/SKILL.md &amp;&amp; ! grep -qF 'the-fool' skills/review-code-auto/SKILL.md</automated>
  </verify>
  <done>skills/review-code-auto/SKILL.md exists with frontmatter (name=review-code-auto, version=1.0.0) and body covering: opening clarifier naming sibling + review-implementation-* redirection; Inputs (code reference; no mandatory source artifact); What This Skill Reviews enumerating quality / safety / idioms / testability; explicit no-source-required statement; Findings Report Shape with 6 sections; Output Artifact at inbox/open/<UTC>-<kebab-desc>-review-finding.md; Workflow; Commit Policy NEVER auto-commits; Immutability. Cites review-implementation-auto by absolute path. NO Anti-Sycophancy section. NO the-fool reference in the body.</done>
</task>

<task type="auto">
  <name>Task 2: Author skills/review-code-interactive/SKILL.md</name>
  <files>skills/review-code-interactive/SKILL.md</files>
  <read_first>
    - skills/review-code-interactive/SKILL.md (the file being created — empty / non-existent)
    - skills/review-code-auto/SKILL.md (Task 1 output — sibling consistency)
    - .planning/phases/06-review-family/06-CONTEXT.md
    - skills/discussion/SKILL.md (anti-sycophancy 4 markers verbatim)
    - skills/seeded-discussion/SKILL.md (per-finding walk shape)
    - skills/review-implementation-interactive/SKILL.md (Plan 06-04 — sibling that REQUIRES a source artifact; the redirection note differentiates this skill)
    - skills/review-proposal-interactive/SKILL.md (Plan 06-01 — first established the interactive review-skill body shape; D89/D92/D95 phrasings)
    - skills/review-spec-interactive/SKILL.md (Plan 06-02)
    - skills/review-plan-interactive/SKILL.md (Plan 06-03)
    - docs/workflow/v1/filename-grammar.md
    - docs/workflow/v1/thread-layout.md
    - docs/workflow/v1/immutability.md
  </read_first>
  <action>
    Create the file with YAML frontmatter and a markdown body. Frontmatter required fields:
    - `name: review-code-interactive`
    - `description:` ONE sentence with "Use when..." trigger + explicit interaction mode (collaborative; walks one finding / file / hunk at a time; ASKS user's view AND TESTS against the code; writes decision log; opt-in inbox dump; NO mandatory source artifact). Mention sibling `review-code-auto`. Mention `review-implementation-*` for code-vs-source-artifact-fidelity alternative.
    - `metadata.author: https://github.com/Jei-sKappa`
    - `metadata.version: 1.0.0`

    Body sections (executor discretion on names; behavioral tokens below MUST appear):

    1. **Opening clarifier paragraph** naming the sibling `review-code-auto` + the `review-implementation-*` redirection target. State GENERAL-PURPOSE code review per D86; NO source artifact required.

    2. **Anti-Sycophancy Stance** section. Carry the FOUR markers from `skills/discussion/SKILL.md` VERBATIM:
       - "Disagree when you disagree"
       - "Push back on weak or incomplete reasoning"
       - "Do not treat pushback as correctness"
       - "Refuse to log" (clause)

       PLUS the REVIEW-STANCE AMPLIFIER: "A review is most valuable when it disagrees with the author" or near-verbatim.

    3. **Inputs** section. Same as `review-code-auto`: code reference (git ref / diff / path / directory) WITHOUT mandatory source. Explicit no-source-required statement. Ambiguity fallback.

    4. **What This Skill Reviews** section. Same general-purpose code-review axes as `review-code-auto`: quality / safety / idioms / testability. Same redirection-to-review-implementation note for code-vs-source-fidelity cases.

    5. **Walk Format** section. MUST document the per-finding OR per-file/per-hunk walk:
       - Either walk per finding (impact-ordered) or per file / hunk (linear). Executor discretion.
       - For each step:
         1. Surface the finding (or the file/hunk with the issues observed) with severity tag.
         2. Cite evidence — file:line in the code.
         3. ASK the user for their view.
         4. TEST the user's explanation against the code. Do NOT just accept. (D89 phrase with `ASK` + `TEST` + `do not just accept` MUST appear.)
         5. Settle: resolved / rejected / accepted / deferred / parked.
         6. Append a record to the decision log with the outcome.

    6. **Output Artifacts** section (TWO conditional outputs per D92/D95):
       - **Decision log** (ALWAYS when ≥1 finding settled): `docs/threads/<thread>/discussions/<UTC>-<kebab-desc>-decision-log.md`; `decision-log` artifact-type MANDATORY.
       - **Unresolved-findings inbox dump** (CONDITIONAL): `docs/threads/<thread>/inbox/open/<UTC>-<kebab-desc>-review-finding.md`. Literal "no Inbox file when nothing remains" phrasing MUST appear.

    7. **Workflow** section with numbered steps:
       - Resolve thread (ASK if ambiguous).
       - Resolve the code input.
       - Capture user-provided focus areas.
       - Read code READ-ONLY.
       - Identify candidate findings (or file/hunk walk order).
       - Confirm with user before starting.
       - Per-finding loop from `## Walk Format`.
       - At end: write inbox-dump IF unresolved actionable findings remain; else NO inbox file.
       - Final message.

    8. **Decision Log** lazy-creation rule.

    9. **Scope Drift** section. PARK / SPLIT / DEFER.

    10. **Commit Policy** section. NEVER auto-commits.

    11. **Immutability** section. Code under review READ-ONLY; emitted artifacts immutable; cite `docs/workflow/v1/immutability.md` by absolute path. Code modifications go through implement-* skills, not this skill.

    Voice and discipline:
    - 4 anti-sycophancy markers verbatim; review-stance amplifier present.
    - D89 ASK-AND-TEST phrasing present.
    - D92/D95 conditional-inbox phrasing present.
    - No-source-required statement present.
    - Cite Phase 1 docs + `skills/review-implementation-interactive/SKILL.md` by absolute path.
    - NO the-fool reference inside this SKILL body (README-level note in Task 3 covers V1 adversarial-delegation globally).
  </action>
  <verify>
    <automated>test -f skills/review-code-interactive/SKILL.md &amp;&amp; grep -qxF 'name: review-code-interactive' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qxF '  version: 1.0.0' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qxF '  author: https://github.com/Jei-sKappa' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qF 'Use when' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qF 'review-code-auto' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qF 'review-implementation' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qiF 'anti-sycophancy' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qF 'Disagree when you disagree' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qF 'Push back on weak or incomplete reasoning' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qF 'Do not treat pushback as correctness' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qF 'Refuse to log' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qiE 'review is most valuable when it disagrees|disagrees with the author|push back hard' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/discussions/' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qF 'docs/threads/<thread>/inbox/open/' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qF 'decision-log' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qF 'review-finding' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/filename-grammar.md' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/thread-layout.md' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qF 'docs/workflow/v1/immutability.md' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qF 'skills/review-implementation-interactive/SKILL.md' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qiE 'no mandatory source|no source artifact|general-purpose code review|spec-independent|without a source' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qiF 'quality' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qiF 'safety' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qiF 'idioms' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qiF 'testability' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qiE 'ASK.*TEST|TEST.*ASK' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qiE 'do not just accept|don.t just accept' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qiE 'no Inbox file|no inbox dump|nothing remains|no inbox file when nothing remains|all findings resolved' skills/review-code-interactive/SKILL.md &amp;&amp; grep -qiE 'NEVER (commit|auto-commit)|never auto-commit|never commit' skills/review-code-interactive/SKILL.md &amp;&amp; ! grep -qF 'the-fool' skills/review-code-interactive/SKILL.md</automated>
  </verify>
  <done>skills/review-code-interactive/SKILL.md exists with frontmatter (name=review-code-interactive, version=1.0.0) and body covering: opening clarifier naming sibling + review-implementation-* redirect; Anti-Sycophancy Stance with 4 verbatim markers + review-stance amplifier; Inputs (code reference; no mandatory source); What This Skill Reviews enumerating quality/safety/idioms/testability; explicit no-source-required statement; Walk Format with per-finding loop + D89 phrasing; Output Artifacts (decision log + conditional inbox dump with D92/D95 phrasing); Workflow; lazy decision-log; Scope Drift; Commit Policy NEVER auto-commits; Immutability. Cites review-implementation-interactive by absolute path. NO the-fool reference in the body.</done>
</task>

<task type="auto">
  <name>Task 3: Register review-code-* in marketplace.json, .vscode/settings.json, README.md + add SHORT the-fool delegation README note</name>
  <files>.claude-plugin/marketplace.json, .vscode/settings.json, README.md</files>
  <read_first>
    - .claude-plugin/marketplace.json (state AFTER Plan 06-04: JeisKappa-workflow has 27 entries; JeisKappa-skills has 7)
    - .vscode/settings.json (state AFTER Plan 06-04: 34 alphabetically-sorted scopes)
    - README.md (state AFTER Plan 06-04: 34 "Available skills" entries + verification-coverage note; Retired skills subsection has discussion-loop + review-decision-document bullets)
    - AGENTS.md (4-touchpoint rule)
    - .planning/phases/06-review-family/06-CONTEXT.md (LOCKED: README documentation note for adversarial-delegation per REVW-09, D88; SHORT note ≤3 lines)
    - skills/review-code-auto/SKILL.md (Task 1 output)
    - skills/review-code-interactive/SKILL.md (Task 2 output)
  </read_first>
  <action>
    Update all three registration files atomically:

    1. **`.claude-plugin/marketplace.json`** — add `"./skills/review-code-auto"` and `"./skills/review-code-interactive"` to `JeisKappa-workflow.skills` in alphabetical position. `review-code-auto` and `review-code-interactive` come alphabetically BEFORE `review-implementation-auto` (since `review-co...` &lt; `review-im...`). Insert between whatever precedes `review-implementation-auto` and `./skills/review-implementation-auto`. After: JeisKappa-workflow array has exactly 29 entries (27 + 2). This is the **Phase 6 target plugin count** (CONTEXT.md: 19 + 10 = 29). JeisKappa-skills unchanged at 7.

    2. **`.vscode/settings.json`** — add `"review-code-auto"` and `"review-code-interactive"` to `."conventionalCommits.scopes"` alphabetically. They fall before `review-implementation-auto`. Net length 36 (34 + 2). Keep alphabetical. The jq accessor MUST use the quoted form `."conventionalCommits.scopes"`. **Phase 6 final scope count is 36** (CONTEXT.md: 27 + 10 - 1 = 36).

    3. **`README.md`** — TWO changes:
       a. ADD two new "Available skills" entries (`### [\`review-code-auto\`](./skills/review-code-auto/SKILL.md)` + `### [\`review-code-interactive\`](./skills/review-code-interactive/SKILL.md)`), each with description paragraph + fenced `npx skills add Jei-sKappa/skills --skill <name>` block. Insert in alphabetical position — BEFORE the `review-implementation-auto` entry (added by Plan 06-04). Position both new entries together (auto then interactive). Description sentences MUST state explicit interaction mode per DIST-05 AND mention that this is general-purpose code review (no source artifact required) — the differentiator from review-implementation-*.

       b. ADD a SHORT (≤3 lines) documentation note about the-fool adversarial delegation. Per CONTEXT.md the executor MAY place this note (1) near the review-* entries as a small subsection, (2) inline with the proposal/spec entries (since adversarial pressure is most useful there), or (3) as a single italic / blockquote line near any review-* entry. Executor discretion provided REVW-09 visibility is satisfied (a user scanning the README for `adversarial` or `the-fool` MUST find the note).

          Suggested text (executor may rephrase; the literal phrase `the-fool` MUST appear and the note MUST be ≤3 lines):

          > **Note:** V1 adversarial review is delegated to the external `the-fool` skill — there is no native V1 adversarial-review skill. Use `the-fool` against a proposal or spec draft to surface adversarial risks before the spec / plan / implementation phases.

       The note from Plan 06-04 (verification-coverage near review-implementation-*) MUST remain untouched. The new note (the-fool delegation) MUST be ADDITIONAL — NOT replacing or merging with the verification-coverage note. Two distinct short documentation notes can coexist; they target different REVW-09 facts (verification vs adversarial).

    Final state after this plan:
    - .claude-plugin/marketplace.json `JeisKappa-workflow.skills` length: **29** (Phase 6 target reached)
    - .vscode/settings.json `."conventionalCommits.scopes"` length: **36** (Phase 6 final)
    - README.md "Available skills" entries: **36** (Phase 6 final) — count via `grep -c '^### \[`'` README.md (counts all `### [\`...\`]` headings)
    - README.md gains TWO short notes (verification + the-fool) from Plans 06-04 + 06-05

    Do NOT touch the `## Retired skills` subsection.
    Do NOT touch the verification-coverage note from Plan 06-04 — leave it as-is.
    Do NOT commit.
  </action>
  <verify>
    <automated>jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length == 29' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/review-code-auto")' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index("./skills/review-code-interactive")' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '.plugins[] | select(.name=="JeisKappa-skills") | .skills | length == 7' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" | length == 36' .vscode/settings.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" | index("review-code-auto")' .vscode/settings.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" | index("review-code-interactive")' .vscode/settings.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" == (."conventionalCommits.scopes" | sort)' .vscode/settings.json &gt;/dev/null &amp;&amp; [ "$(grep -c '^### \[`review-code-auto`\]' README.md)" = "1" ] &amp;&amp; [ "$(grep -c '^### \[`review-code-interactive`\]' README.md)" = "1" ] &amp;&amp; [ "$(grep -c 'npx skills add Jei-sKappa/skills --skill review-code-auto' README.md)" = "1" ] &amp;&amp; [ "$(grep -c 'npx skills add Jei-sKappa/skills --skill review-code-interactive' README.md)" = "1" ] &amp;&amp; grep -qF 'the-fool' README.md &amp;&amp; grep -qiE 'adversarial' README.md &amp;&amp; grep -qiE 'no separate.*verify|verification.{0,30}review-implementation|review-implementation.{0,30}verification' README.md &amp;&amp; A=$(grep -n '^### \[`review-code-auto`\]' README.md | cut -d: -f1) &amp;&amp; B=$(grep -n '^### \[`review-code-interactive`\]' README.md | cut -d: -f1) &amp;&amp; R=$(grep -n '^## Retired skills' README.md | cut -d: -f1) &amp;&amp; [ "$A" -lt "$R" ] &amp;&amp; [ "$B" -lt "$R" ] &amp;&amp; [ "$(grep -c '^### \[`' README.md)" = "36" ] &amp;&amp; echo OK</automated>
  </verify>
  <done>marketplace.json JeisKappa-workflow array has 29 entries (was 27, +2; Phase 6 target reached). .vscode/settings.json scopes has 36 entries (Phase 6 final), alphabetical. README.md has 2 new "Available skills" entries inserted before `## Retired skills` AND a new SHORT the-fool adversarial-delegation note (Plan 06-04's verification-coverage note remains untouched). Available skills count is 36 (Phase 6 final). No git commit created.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

1. **Skill files exist:**
   ```bash
   ls -la skills/review-code-auto/SKILL.md skills/review-code-interactive/SKILL.md
   ```

2. **Auto skill structural tokens:**
   ```bash
   for TOKEN in "skills/review-implementation-auto/SKILL.md" "review-finding" "docs/threads/<thread>/inbox/open/"; do
     grep -qF "$TOKEN" skills/review-code-auto/SKILL.md || echo "FAIL auto: $TOKEN"
   done
   grep -qiE 'no mandatory source|no source artifact|general-purpose code review|spec-independent|without a source' skills/review-code-auto/SKILL.md || echo "FAIL no-source-required statement"
   ! grep -qxF '## Anti-Sycophancy Stance' skills/review-code-auto/SKILL.md || echo "FAIL auto must NOT contain Anti-Sycophancy section"
   ! grep -qF 'the-fool' skills/review-code-auto/SKILL.md || echo "FAIL the-fool must NOT appear in review-code-* body (README-level note covers it)"
   ```

3. **Interactive skill anti-sycophancy + D89 + D92/D95 + no-source-required:**
   ```bash
   for TOKEN in "Disagree when you disagree" "Push back on weak or incomplete reasoning" "Do not treat pushback as correctness" "Refuse to log"; do
     grep -qF "$TOKEN" skills/review-code-interactive/SKILL.md || echo "FAIL interactive: $TOKEN"
   done
   grep -qiE 'ASK.*TEST|TEST.*ASK' skills/review-code-interactive/SKILL.md || echo "FAIL D89"
   grep -qiE 'no Inbox file|no inbox dump|nothing remains' skills/review-code-interactive/SKILL.md || echo "FAIL D92/D95"
   grep -qiE 'no mandatory source|no source artifact|general-purpose code review|spec-independent|without a source' skills/review-code-interactive/SKILL.md || echo "FAIL no-source-required statement"
   ```

4. **Phase 6 final registration counts + README documentation notes:**
   ```bash
   jq '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length' .claude-plugin/marketplace.json    # expect 29 (Phase 6 target reached)
   jq '.plugins[] | select(.name=="JeisKappa-skills") | .skills | length' .claude-plugin/marketplace.json      # expect 7
   jq '."conventionalCommits.scopes" | length' .vscode/settings.json                                              # expect 36 (Phase 6 final)
   grep -cE '^### \[`review-code-(auto|interactive)`\]' README.md                                                # expect 2
   grep -cF '`the-fool`' README.md                                                                                 # expect ≥ 1 (the-fool delegation note)
   grep -qiE 'adversarial' README.md                                                                              # the-fool note
   grep -qiE 'no separate.*verify|verification.{0,30}review-implementation' README.md                            # Plan 06-04 verification note still present
   grep -c '^### \[`' README.md                                                                                    # expect 36 (Phase 6 final Available skills)
   ```

5. **Sanity: no review-* SKILL file in this plan contains the-fool reference (it belongs in the README only for this plan):**
   ```bash
   ! grep -qF 'the-fool' skills/review-code-auto/SKILL.md
   ! grep -qF 'the-fool' skills/review-code-interactive/SKILL.md
   ```
</verification>

<success_criteria>
- skills/review-code-auto/SKILL.md and skills/review-code-interactive/SKILL.md exist with correct V1 frontmatter and bodies.
- Both skills enumerate code-only inputs (git ref / diff / path / directory) with NO mandatory source artifact (per D86).
- Both skills enumerate general-purpose code review axes (quality / safety / idioms / testability).
- Both skills include the redirection note pointing at review-implementation-* when a source artifact exists.
- Both skills cite review-implementation-* by absolute path.
- review-code-interactive carries all 4 anti-sycophancy markers verbatim + review-stance amplifier.
- README.md gains a SHORT (≤3 lines) the-fool delegation note per D88 / REVW-09 — the literal phrase `the-fool` appears.
- Plan 06-04's verification-coverage note remains present and untouched.
- marketplace.json JeisKappa-workflow array has exactly 29 entries (Phase 6 target reached; 27 -> 29).
- .vscode/settings.json conventionalCommits.scopes has exactly 36 entries (Phase 6 final; 34 -> 36), alphabetically sorted.
- README.md has 2 new entries inserted before `## Retired skills`; Available skills count is 36 (Phase 6 final).
- No commit created.
- Phase 6 ROADMAP entry can flip to COMPLETE after verification of all 5 plans.
</success_criteria>

<output>
Create `.planning/phases/06-review-family/06-05-SUMMARY.md` when done. Record:
- Files created: skills/review-code-auto/SKILL.md, skills/review-code-interactive/SKILL.md
- Files modified: .claude-plugin/marketplace.json (workflow 27 -> 29; Phase 6 plugin target reached), .vscode/settings.json (scopes 34 -> 36; Phase 6 final), README.md (Available skills 34 -> 36 + new the-fool delegation note added near review entries per REVW-09; Phase 6 README final state)
- Requirements addressed: REVW-05 (review-code-* pair with general-purpose / no-mandatory-source per D86), REVW-06/07/08 (review-shape contract applied to the code target), REVW-09 fully completed (the-fool delegation note ADDED in this plan + verification-coverage note from Plan 06-04 = both REVW-09 README notes now in place)
- Phase 6 final state: 5 plans completed; 10 new V1 review skills shipped (review-proposal-{auto,interactive}, review-spec-{auto,interactive}, review-plan-{auto,interactive}, review-implementation-{auto,interactive}, review-code-{auto,interactive}); 1 legacy skill retired (review-decision-document); workflow plugin grew 19 -> 29; .vscode scopes grew 27 -> 36; README Available skills grew 27 -> 36; README Retired skills gained 1 new bullet; 2 new short README documentation notes added (verification + the-fool delegation)
- Phase 6 ROADMAP entry ready to flip to COMPLETE after verification.
</output>
