---
phase: 07-merge-finish-navigation-distribution
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - README.md
autonomous: true
requirements:
  - DIST-01
  - DIST-02
  - DIST-03
  - DIST-05

must_haves:
  truths:
    - "User opening `README.md` sees a HYBRID layout per DIST-01, D34, D109 — REPLACING the simple-list 'Available skills' structure — with the following sections in order: Title + 1-sentence value statement; Toolbox Model; Layered Workflow Map; Recommended Common Paths (3–5 paths); Per-Module Catalog; Retired Skills; Installation."
    - "User reading the Toolbox Model section finds a short narrative (3–6 sentences) explaining V1 is composable, every spine phase is optional, users can pick a single skill or compose the whole spine — sourced from PROJECT.md 'Core Value' statement."
    - "User reading the Layered Workflow Map sees a simple ASCII or table representation of the spine layer (`propose → spec → plan → implementation → finish`) and overlay layers (discussion / review / merge / inbox / navigation). NO NFA/state-machine notation (V2 per D34)."
    - "User reading the Recommended Common Paths section finds 3–5 onboarding paths for typical use cases (e.g., 'Implementing a feature from rough idea', 'Fixing a bug', 'Exploring an idea', 'Refining a plan'). Each path shows a sequence of skills tied together with arrows."
    - "User reading the Per-Module Catalog finds the full skill list grouped by module/family with one-line description + install command per skill. Module families MUST cover: Capture & Discussion / Propose / Spec / Plan / Implement / Review / Merge / Finish & Navigate / Other (non-V1)."
    - "User can confirm DIST-03 by reading: every V1 skill entry has an `npx skills add Jei-sKappa/skills --skill <name>` command (no nested directories, no name prefixes — flat skill folder names only)."
    - "User can confirm DIST-05 by reading: every V1 skill description in the catalog explicitly states its interaction mode (auto / interactive / single-skill-V1-exception for finish / advisory for whats-next) — no unsuffixed convenience aliases. Finish is the lone exception per D97."
    - "User can confirm DIST-02 by reading the Installation note: plugin grouping under `JeisKappa-workflow` renders as 'JeisKappa Workflow' in `npx skills list` (dashes split, segments capitalized)."
    - "User finds all 33 V1 workflow skills present in the catalog: capture-inbox, discussion, seeded-discussion, propose-{auto,interactive}, spec-{auto,interactive}, plan-{loose,strict}-{auto,interactive}, adjust-plan-granularity-{auto,interactive}, implement-{auto,interactive}, implement-plan-{auto,interactive}, implement-plan-with-subagents-{auto,interactive}, review-{proposal,spec,plan,implementation,code}-{auto,interactive}, merge-artifacts-{auto,interactive}, finish, whats-next."
    - "User finds the existing non-V1 skills preserved in an 'Other' module family (or equivalent named subsection): the-librarian, derive-spec, afk-exploration, consult-the-expert, meta-prompting, report-to-the-owner, brief-the-implementer."
    - "User finds the Retired Skills subsection preserved with both existing bullets (discussion-loop + review-decision-document) untouched."
    - "User finds the verification-coverage documentation note (Plan 06-04, D85) AND the the-fool delegation documentation note (Plan 06-05, D88) integrated into the new hybrid layout — typically as one-line callouts inside the Review module family or as a separate documentation-notes subsection (executor discretion per CONTEXT.md)."
  artifacts:
    - path: "README.md"
      provides: "Full hybrid redesign — simple-list 'Available skills' REPLACED with title + value statement + toolbox model + layered workflow map + recommended common paths + per-module catalog (covering all 33 V1 skills + 7 non-V1 skills) + retired skills subsection + installation note. Skill-block format preserved (each catalog entry has a markdown heading + description paragraph + fenced `npx skills add` command) for DIST-03 + DIST-05 verifiability."
      contains: "Hybrid layout sections in order: (1) title + skills.sh badge; (2) 1-sentence value statement; (3) Toolbox Model section; (4) Layered Workflow Map section; (5) Recommended Common Paths section (3–5 paths); (6) Per-Module Catalog section with module families (Capture & Discussion / Propose / Spec / Plan / Implement / Review / Merge / Finish & Navigate / Other); (7) Retired Skills subsection (preserved 2 bullets); (8) Installation section with plugin-grouping note. All 33 V1 + 7 non-V1 skill names present. All V1 skill descriptions state interaction mode."
  key_links:
    - from: "README.md"
      to: ".planning/PROJECT.md"
      via: "Toolbox Model narrative drawn from PROJECT.md 'Core Value' statement"
      pattern: "composable|every spine phase is optional|every artifact is reviewable"
    - from: "README.md"
      to: ".claude-plugin/marketplace.json"
      via: "Installation note documents JeisKappa-workflow plugin name rendering as 'JeisKappa Workflow' in npx skills list"
      pattern: "JeisKappa Workflow|JeisKappa-workflow"
    - from: "README.md"
      to: "skills/finish/SKILL.md"
      via: "Finish & Navigate module family entry for finish (single-skill V1 exception per D97)"
      pattern: "finish"
    - from: "README.md"
      to: "skills/whats-next/SKILL.md"
      via: "Finish & Navigate module family entry for whats-next (advisory chat-first)"
      pattern: "whats-next"
---

<objective>
Ship the FINAL plan of Phase 7 (and of the V1 milestone): REPLACE the simple-list "Available skills" structure of `README.md` with the full hybrid layout per DIST-01, D34, D109. The hybrid layout has SEVEN sections in order:

1. **Title + 1-sentence value statement** — what this repo is.
2. **Toolbox Model** — short narrative (3–6 sentences) explaining V1 is composable, every spine phase is optional, users can pick a single skill or compose the whole spine. Sourced from PROJECT.md "Core Value" statement.
3. **Layered Workflow Map** — visual + textual representation: spine layer (`propose → spec → plan → implementation → finish`) plus overlay layers (discussion / review / merge / inbox / navigation). ASCII art or table — NO NFA/state-machine notation (V2 per D34).
4. **Recommended Common Paths** — 3–5 onboarding paths for typical use cases (canonical full workflow + simpler entry points).
5. **Per-Module Catalog** — full skill list grouped by module/family with one-line description + install command per skill. Module families cover all 33 V1 workflow skills + 7 non-V1 skills.
6. **Retired Skills** subsection — preserve the 2 existing bullets (discussion-loop + review-decision-document).
7. **Installation** note — generic `npx skills add Jei-sKappa/skills --skill <name>` pattern with one example, plus 1-line note about plugin grouping rendering as `JeisKappa Workflow`.

This redesign verifies four V1 distribution requirements simultaneously:
- **DIST-01** — Hybrid README (toolbox model + layered map + recommended paths + per-module catalog).
- **DIST-02** — Plugin grouping renders `JeisKappa Workflow` (confirmed by the Installation note).
- **DIST-03** — Per-skill installability via `npx skills add Jei-sKappa/skills --skill <name>` (no nested directories, no name prefixes — confirmed by every catalog entry).
- **DIST-05** — Every V1 skill description explicitly states its interaction mode; no unsuffixed convenience aliases (confirmed by reading the catalog).

After this plan ships, the Phase 7 milestone is COMPLETE — the V1 catalog is fully shipped and discoverable.

Purpose: This is the FINAL plan of the V1 milestone. After this plan, the user can open `README.md` and discover the entire workflow — the toolbox model orients them, the layered map shows what attaches where, the recommended paths give 3–5 onboarding starting points, and the per-module catalog lists every installable skill with its `npx skills add` command. Phase 6's two documentation notes (verification-coverage + the-fool delegation) are integrated into the new layout.

Output: ONE file modified — `README.md` — replacing the simple-list "Available skills" section with the hybrid layout. The skills.sh badge at the top stays. The Retired Skills subsection is preserved (already 2 bullets) and migrated into the new hybrid layout as section 6. After this plan: README is final V1 state. Phase 7 ROADMAP entry can flip to COMPLETE.

Depends on Plan 07-02 because the new V1 skills (finish + whats-next) MUST exist in the catalog, and Plan 07-02's README modifications (Available skills 38 -> 40) are about to be REPLACED by the hybrid layout — the dependency is sequential serialization so the hybrid layout reflects the complete set of 33 V1 workflow skills.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-merge-finish-navigation-distribution/07-CONTEXT.md
@.planning/PROJECT.md
@AGENTS.md

@docs/workflow/v1/README.md
@docs/workflow/v1/thread-layout.md

<!-- Existing README — being redesigned -->
@README.md

<!-- All 33 V1 workflow skills (their descriptions are the source for the catalog one-liners) -->
@skills/capture-inbox/SKILL.md
@skills/discussion/SKILL.md
@skills/seeded-discussion/SKILL.md
@skills/propose-auto/SKILL.md
@skills/propose-interactive/SKILL.md
@skills/spec-auto/SKILL.md
@skills/spec-interactive/SKILL.md
@skills/plan-loose-auto/SKILL.md
@skills/plan-loose-interactive/SKILL.md
@skills/plan-strict-auto/SKILL.md
@skills/plan-strict-interactive/SKILL.md
@skills/adjust-plan-granularity-auto/SKILL.md
@skills/adjust-plan-granularity-interactive/SKILL.md
@skills/implement-auto/SKILL.md
@skills/implement-interactive/SKILL.md
@skills/implement-plan-auto/SKILL.md
@skills/implement-plan-interactive/SKILL.md
@skills/implement-plan-with-subagents-auto/SKILL.md
@skills/implement-plan-with-subagents-interactive/SKILL.md
@skills/review-proposal-auto/SKILL.md
@skills/review-proposal-interactive/SKILL.md
@skills/review-spec-auto/SKILL.md
@skills/review-spec-interactive/SKILL.md
@skills/review-plan-auto/SKILL.md
@skills/review-plan-interactive/SKILL.md
@skills/review-implementation-auto/SKILL.md
@skills/review-implementation-interactive/SKILL.md
@skills/review-code-auto/SKILL.md
@skills/review-code-interactive/SKILL.md
@skills/merge-artifacts-auto/SKILL.md
@skills/merge-artifacts-interactive/SKILL.md
@skills/finish/SKILL.md
@skills/whats-next/SKILL.md

<!-- Non-V1 skills preserved in 'Other' module family -->
@skills/the-librarian/SKILL.md
@skills/derive-spec/SKILL.md
@skills/afk-exploration/SKILL.md
@skills/consult-the-expert/SKILL.md
@skills/meta-prompting/SKILL.md
@skills/report-to-the-owner/SKILL.md
@skills/brief-the-implementer/SKILL.md

<!-- Marketplace plugin source (Installation note references this) -->
@.claude-plugin/marketplace.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign README.md with the hybrid layout</name>
  <files>README.md</files>
  <read_first>
    - README.md (the current file — state after Plan 07-02: skills.sh badge + 40 "Available skills" entries + 2 doc notes + 2 Retired entries)
    - .planning/phases/07-merge-finish-navigation-distribution/07-CONTEXT.md (LOCKED: DIST-01..03 + DIST-05; D34 toolbox model + layered map + recommended paths + per-module catalog; D109 hybrid layout; Phase 6 documentation notes preservation; non-V1 skill family preservation)
    - .planning/PROJECT.md ("Core Value" statement is the source for the Toolbox Model narrative)
    - AGENTS.md ("When adding a new skill" rules — `npx skills add Jei-sKappa/skills --skill <name>` pattern + marketplace plugin display rule)
    - .claude-plugin/marketplace.json (Installation note documents JeisKappa-workflow plugin = "JeisKappa Workflow" rendering)
    - docs/workflow/v1/README.md (V1 reference doc entry point — Installation section may link to it)
    - All 33 V1 SKILL.md files (frontmatter descriptions are the source for one-liner descriptions in the Per-Module Catalog)
    - All 7 non-V1 SKILL.md files (frontmatter descriptions are the source for one-liner descriptions in the 'Other' module family)
  </read_first>
  <action>
    REPLACE the existing `## Available skills` section structure of `README.md` with a hybrid layout. The skills.sh badge at the top of the file stays. The Retired Skills subsection (currently 2 bullets — discussion-loop + review-decision-document) MUST be preserved and migrated into the new hybrid layout as section 6.

    **Required sections in this order:**

    1. **Title + value statement.** Keep the skills.sh badge line at the top (unchanged). Keep `# Jei-sKappa's Skills` as the H1 title (or refine the title if a tighter V1-aware title is preferred — executor discretion; the existing title is acceptable). Below the H1, write ONE sentence summarizing what this repo is (refined V1-aware version of the existing "A personal collection of refined and custom SKILL.md files for software engineering and everyday AI-assisted work." — executor discretion; the existing sentence is acceptable; the value statement MAY draw from PROJECT.md's "What This Is" + "Core Value" combined).

    2. **`## Toolbox Model` section.** 3–6 sentences explaining V1 is composable. Draw from PROJECT.md "Core Value": "A user picking up any single skill or composing the whole spine can drive a feature end-to-end without depending on a CLI, runtime, or project-local state file — every artifact is reviewable Markdown on disk under `docs/threads/<thread>/`." The narrative MUST cover:
       - V1 is a TOOLBOX of installable skills — not a CLI or runtime.
       - Every spine phase is OPTIONAL — users may pick a single skill OR compose the whole spine (propose → spec → plan → implementation → finish).
       - Every artifact is reviewable Markdown on disk under `docs/threads/<thread>/`.
       - Skills are harness-agnostic — they work inside Claude Code, Codex, Gemini CLI, OpenCode, etc.
       - Cross-cutting modules (discussion, review, merge, inbox, navigation) attach anywhere on the spine.

       Executor discretion on exact wording. The 5 bullet-content concepts above MUST all be expressed (whether as a paragraph or bulleted list). No NFA/state-machine framing.

    3. **`## Layered Workflow Map` section.** Visual + textual representation of the workflow as layers. Two layers MUST be shown:
       - **Spine layer**: `propose → spec → plan → implementation → finish` (with the arrow direction representing the typical forward order).
       - **Overlay layers**: discussion / review / merge / inbox / navigation — each one attaches anywhere on the spine.

       Use ASCII art OR a markdown table — NOT an external image. Executor discretion on exact shape. A simple table is sufficient (07-CONTEXT.md "specifics" shows a 5-row table as an example). The map MUST be readable in plain text. No NFA notation, no state-machine diagram, no graphviz syntax (V2 per D34).

    4. **`## Recommended Common Paths` section.** 3–5 onboarding paths for typical use cases. Each path is a one-line description + a sequence of skills tied together with arrows. Suggested paths (executor MAY refine, but the canonical full-workflow path MUST appear as one of them):
       - "Implementing a feature from rough idea" — `discussion → propose → spec → plan-strict → implement-plan-with-subagents → review-implementation → finish` (canonical full workflow).
       - "Fixing a bug" — `implement-auto + review-code → finish` (simpler entry).
       - "Exploring an idea" — `discussion → propose` (no further commitment).
       - "Refining a plan" — `plan-strict-auto → review-plan-interactive → adjust-plan-granularity → implement-plan-auto`.

       Executor discretion on the final 3–5 paths and exact phrasing. Each path is a one-liner — not a detailed walkthrough.

    5. **`## Per-Module Catalog` section** (or equivalent heading like `## Skills by Module`). This is the LARGEST section and REPLACES the existing simple-list `## Available skills`. Each module family has its own H3 subsection (e.g., `### Capture & Discussion`, `### Propose`, etc.) followed by skill entries. The module families MUST cover ALL of these (in any order — executor discretion; ideally the order matches the spine flow + cross-cutting overlays + Other at the end):

       - **Capture & Discussion** (3 skills): `capture-inbox`, `discussion`, `seeded-discussion`
       - **Propose** (2 skills): `propose-auto`, `propose-interactive`
       - **Spec** (2 skills): `spec-auto`, `spec-interactive`
       - **Plan** (6 skills): `plan-loose-auto`, `plan-loose-interactive`, `plan-strict-auto`, `plan-strict-interactive`, `adjust-plan-granularity-auto`, `adjust-plan-granularity-interactive`
       - **Implement** (6 skills): `implement-auto`, `implement-interactive`, `implement-plan-auto`, `implement-plan-interactive`, `implement-plan-with-subagents-auto`, `implement-plan-with-subagents-interactive`
       - **Review** (10 skills): `review-proposal-auto`, `review-proposal-interactive`, `review-spec-auto`, `review-spec-interactive`, `review-plan-auto`, `review-plan-interactive`, `review-implementation-auto`, `review-implementation-interactive`, `review-code-auto`, `review-code-interactive`
       - **Merge** (2 skills): `merge-artifacts-auto`, `merge-artifacts-interactive`
       - **Finish & Navigate** (2 skills): `finish`, `whats-next`
       - **Other (non-V1)** (7 skills): `the-librarian`, `derive-spec`, `afk-exploration`, `consult-the-expert`, `meta-prompting`, `report-to-the-owner`, `brief-the-implementer`

       Per-skill entry format (each entry MUST):
       - Have a markdown heading naming the skill — e.g., `#### [\`<skill-name>\`](./skills/<skill-name>/SKILL.md)` (H4 inside an H3 module subsection) — OR `### [\`<skill-name>\`](./skills/<skill-name>/SKILL.md)` if the executor chooses to keep H3 entries underneath an H2 module heading (executor discretion on heading levels, as long as the structure is navigable and EVERY skill name appears in a heading line that links to its SKILL.md).
       - Include a 1–3 sentence description that EXPLICITLY states the interaction mode (auto / interactive / single-skill-V1-exception for finish / advisory for whats-next) per DIST-05. The description MAY be drawn from the SKILL.md's frontmatter description (which already follows the DIST-05 contract). One-line entries are acceptable if the SKILL.md's description supports it; longer descriptions for complex skills are also acceptable.
       - Include a fenced `sh` code block with `npx skills add Jei-sKappa/skills --skill <skill-name>` — exactly one install snippet per skill, per DIST-03.

       **Documentation notes integration** (executor discretion per CONTEXT.md):
       - The verification-coverage note (Plan 06-04, D85) — currently a blockquote `> **Note:** V1 verification of implementations is covered by review-implementation-auto and review-implementation-interactive — there is no separate verify-* skill in V1 (per D85, REVW-09).` — MUST be preserved in the new layout. Recommended placement: inside the Review module family, as a one-line callout near the review-implementation-* entries.
       - The the-fool delegation note (Plan 06-05, D88) — currently a blockquote `> **Note:** V1 adversarial review is delegated to the external the-fool skill — no native V1 adversarial-review skill. Use the-fool against a proposal or spec draft to surface adversarial risks before the spec / plan / implementation phases (per D88, REVW-09).` — MUST be preserved. Recommended placement: inside the Review module family OR near the Propose / Spec module family entries (executor discretion).
       - Both notes MUST be findable by searching for `the-fool` (the-fool note) and `verification` or `review-implementation` (verification-coverage note).

    6. **`## Retired skills` subsection.** PRESERVE the 2 existing bullets EXACTLY AS-IS:
       - discussion-loop (retired 2026-05-21; split into discussion + seeded-discussion).
       - review-decision-document (retired 2026-05-21; evolved into review-spec-auto + review-spec-interactive).

       The existing bullet text from the current README is the canonical phrasing — do NOT rewrite or shorten. Copy verbatim from the current README's `## Retired skills` section.

    7. **`## Installation` section.** A short final section explaining how to install. MUST include:
       - The generic install command pattern: `npx skills add Jei-sKappa/skills --skill <skill-name>`.
       - ONE example using a real skill (e.g., `npx skills add Jei-sKappa/skills --skill spec-auto`).
       - A ONE-LINE note that workflow skills are grouped under the `JeisKappa-workflow` marketplace plugin, which renders as `JeisKappa Workflow` in `npx skills list` (per DIST-02 — the dash gets split into a space and segments are capitalized; non-V1 skills stay under `JeisKappa-skills` rendering as `JeisKappa Skills`).
       - OPTIONAL: a pointer to `docs/workflow/v1/README.md` for the V1 reference docs (thread layout, filename grammar, immutability).

    **Hard constraints:**

    - REPLACE the existing `## Available skills` simple-list section ENTIRELY. The new Per-Module Catalog takes its place.
    - PRESERVE the skills.sh badge at the top of the file.
    - PRESERVE the `## Retired skills` subsection content (2 bullets) verbatim.
    - PRESERVE both Phase 6 documentation notes (verification-coverage + the-fool delegation) — integrate them into the new layout (recommended: inside the Review module family).
    - EVERY V1 workflow skill (33 total) MUST appear in the Per-Module Catalog with its `npx skills add` command.
    - EVERY non-V1 skill (7 total) MUST appear in the Per-Module Catalog under the "Other" family (or equivalent named subsection) with its `npx skills add` command.
    - EVERY V1 skill description MUST state its interaction mode per DIST-05 (auto / interactive / single-skill / advisory).
    - NO NFA / state-machine / graphviz notation (V2 per D34).
    - NO external image references in the Layered Workflow Map — ASCII art or markdown table only.
    - Recommended Common Paths section has 3–5 paths (not fewer than 3, not more than 5).
    - Do NOT commit.
  </action>
  <verify>
    <automated>test -f README.md &amp;&amp; grep -qiE '^# Jei-sKappa.s Skills|^# Modular Agentic Workflow|^# .{1,80}Skills' README.md &amp;&amp; grep -qiE 'skills\.sh' README.md &amp;&amp; grep -qiE '^## Toolbox|^### Toolbox' README.md &amp;&amp; grep -qiE 'composable|optional' README.md &amp;&amp; grep -qiE '^## Layered|^### Layered|workflow map' README.md &amp;&amp; grep -qiE 'propose.*spec.*plan|spine' README.md &amp;&amp; grep -qiE '^## Recommended|^### Recommended|common paths' README.md &amp;&amp; grep -qiE '^## (Per.Module|Skills by Module|Module Catalog|Catalog)' README.md &amp;&amp; grep -qiE '^### Capture|^#### Capture|Capture.*Discussion' README.md &amp;&amp; grep -qiE '^### Propose|^#### Propose|Propose' README.md &amp;&amp; grep -qiE '^### Spec|^#### Spec' README.md &amp;&amp; grep -qiE '^### Plan|^#### Plan' README.md &amp;&amp; grep -qiE '^### Implement|^#### Implement' README.md &amp;&amp; grep -qiE '^### Review|^#### Review' README.md &amp;&amp; grep -qiE '^### Merge|^#### Merge' README.md &amp;&amp; grep -qiE '^### Finish|^#### Finish|Finish.*Navigate' README.md &amp;&amp; grep -qiE '^### Other|^#### Other|non-V1' README.md &amp;&amp; for SKILL in capture-inbox discussion seeded-discussion propose-auto propose-interactive spec-auto spec-interactive plan-loose-auto plan-loose-interactive plan-strict-auto plan-strict-interactive adjust-plan-granularity-auto adjust-plan-granularity-interactive implement-auto implement-interactive implement-plan-auto implement-plan-interactive implement-plan-with-subagents-auto implement-plan-with-subagents-interactive review-proposal-auto review-proposal-interactive review-spec-auto review-spec-interactive review-plan-auto review-plan-interactive review-implementation-auto review-implementation-interactive review-code-auto review-code-interactive merge-artifacts-auto merge-artifacts-interactive finish whats-next the-librarian derive-spec afk-exploration consult-the-expert meta-prompting report-to-the-owner brief-the-implementer; do grep -qF "$SKILL" README.md || { echo "FAIL: $SKILL missing"; exit 1; }; done &amp;&amp; for SKILL in capture-inbox discussion seeded-discussion propose-auto propose-interactive spec-auto spec-interactive plan-loose-auto plan-loose-interactive plan-strict-auto plan-strict-interactive adjust-plan-granularity-auto adjust-plan-granularity-interactive implement-auto implement-interactive implement-plan-auto implement-plan-interactive implement-plan-with-subagents-auto implement-plan-with-subagents-interactive review-proposal-auto review-proposal-interactive review-spec-auto review-spec-interactive review-plan-auto review-plan-interactive review-implementation-auto review-implementation-interactive review-code-auto review-code-interactive merge-artifacts-auto merge-artifacts-interactive finish whats-next the-librarian derive-spec afk-exploration consult-the-expert meta-prompting report-to-the-owner brief-the-implementer; do grep -qF "npx skills add Jei-sKappa/skills --skill $SKILL" README.md || { echo "FAIL: install command for $SKILL missing"; exit 1; }; done &amp;&amp; grep -qF 'the-fool' README.md &amp;&amp; grep -qiE 'adversarial' README.md &amp;&amp; grep -qiE 'no separate.*verify|verification.{0,30}review-implementation|review-implementation.{0,30}verification' README.md &amp;&amp; grep -qF 'discussion-loop' README.md &amp;&amp; grep -qF 'review-decision-document' README.md &amp;&amp; grep -qF 'JeisKappa Workflow' README.md &amp;&amp; grep -qiE '^## Retired skills' README.md &amp;&amp; grep -qiE '^## Installation|^### Installation' README.md &amp;&amp; ! grep -qiE 'NFA|state.machine|state machine|graphviz|finite automaton' README.md &amp;&amp; echo OK</automated>
  </verify>
  <done>README.md fully redesigned with the hybrid layout. Sections present in order: title + value statement; Toolbox Model (composable, every spine phase optional); Layered Workflow Map (spine + overlays, NO NFA/state-machine notation); Recommended Common Paths (3–5 paths); Per-Module Catalog (Capture & Discussion / Propose / Spec / Plan / Implement / Review / Merge / Finish & Navigate / Other module families); Retired skills subsection (2 preserved bullets); Installation note (with JeisKappa Workflow plugin grouping note). All 33 V1 workflow skill names + 7 non-V1 skill names present with `npx skills add` commands. Verification-coverage + the-fool delegation notes preserved. No commit created.</done>
</task>

<task type="auto">
  <name>Task 2: Verify DIST-01/02/03/05 satisfaction + Phase 7 completion sanity check</name>
  <files></files>
  <read_first>
    - README.md (Task 1 output — the redesigned README)
    - .planning/phases/07-merge-finish-navigation-distribution/07-CONTEXT.md (LOCKED: DIST-01..03 + DIST-05 acceptance criteria)
    - .planning/REQUIREMENTS.md (Phase 7 requirement list — DIST family)
    - .claude-plugin/marketplace.json (verify JeisKappa-workflow = 33 entries, JeisKappa-skills = 7)
    - .vscode/settings.json (verify scopes = 40)
  </read_first>
  <action>
    Perform a comprehensive sanity check that ties Phase 7's final state to DIST-01..03 + DIST-05. This task does NOT modify any file — it runs verification commands and ensures the Phase 7 deliverables are coherent. The verification block below is exhaustive; the task is complete when every check passes.

    **DIST-01 — Hybrid README layout present:**
    - All 7 hybrid sections present in order (Title + value statement / Toolbox Model / Layered Workflow Map / Recommended Common Paths / Per-Module Catalog / Retired Skills / Installation).
    - All 9 module families present in the Per-Module Catalog (Capture & Discussion / Propose / Spec / Plan / Implement / Review / Merge / Finish & Navigate / Other).

    **DIST-02 — Plugin grouping renders 'JeisKappa Workflow':**
    - Installation note documents the plugin-grouping rendering rule (literal phrase 'JeisKappa Workflow' present).
    - marketplace.json has JeisKappa-workflow plugin name (verified by jq).

    **DIST-03 — Per-skill installability:**
    - Every V1 workflow skill (33) has an `npx skills add Jei-sKappa/skills --skill <name>` snippet (NOT `--skill ./skills/<name>`, NOT `--skill JeisKappa-workflow/<name>` — flat name only, no nested path).
    - Every non-V1 skill (7) has the same install snippet pattern.
    - No skill name in the README catalog contains a `/` character in the install command (proves no nested directories / no name prefixes per D3).

    **DIST-05 — Interaction mode in every V1 description:**
    - The description text for each V1 skill mentions one of: 'auto', 'interactive', 'single skill' (for finish), or 'advisory' (for whats-next).
    - No 'unsuffixed convenience alias' entries in the catalog (every V1 skill name carries its `-auto`, `-interactive` suffix, except `finish` and `whats-next` per D97/D106 exceptions, and except `capture-inbox`, `discussion`, `seeded-discussion` which are cross-cutting single skills established in Phase 2 per the original V1 contract — these are NOT mode variants).

    **Phase 7 final-state sanity:**
    - marketplace.json `JeisKappa-workflow.skills` length = 33.
    - marketplace.json `JeisKappa-skills.skills` length = 7.
    - .vscode/settings.json `."conventionalCommits.scopes"` length = 40.
    - All 4 new V1 skills (merge-artifacts-auto, merge-artifacts-interactive, finish, whats-next) appear in marketplace.json + .vscode scopes + README catalog.
    - Phase 6 documentation notes (verification-coverage + the-fool delegation) preserved in README.
    - Retired Skills subsection has discussion-loop + review-decision-document bullets preserved.
    - No file modifications in this task — verification only.

    If ANY check fails, the task is BLOCKED and the executor MUST report which specific check failed before proceeding. If all checks pass, report final V1 catalog status.
  </action>
  <verify>
    <automated>jq -e '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length == 33' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '.plugins[] | select(.name=="JeisKappa-skills") | .skills | length == 7' .claude-plugin/marketplace.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" | length == 40' .vscode/settings.json &gt;/dev/null &amp;&amp; jq -e '."conventionalCommits.scopes" == (."conventionalCommits.scopes" | sort)' .vscode/settings.json &gt;/dev/null &amp;&amp; for SKILL in capture-inbox discussion seeded-discussion propose-auto propose-interactive spec-auto spec-interactive plan-loose-auto plan-loose-interactive plan-strict-auto plan-strict-interactive adjust-plan-granularity-auto adjust-plan-granularity-interactive implement-auto implement-interactive implement-plan-auto implement-plan-interactive implement-plan-with-subagents-auto implement-plan-with-subagents-interactive review-proposal-auto review-proposal-interactive review-spec-auto review-spec-interactive review-plan-auto review-plan-interactive review-implementation-auto review-implementation-interactive review-code-auto review-code-interactive merge-artifacts-auto merge-artifacts-interactive finish whats-next; do jq -e --arg s "./skills/$SKILL" '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | index($s)' .claude-plugin/marketplace.json &gt;/dev/null || { echo "FAIL: $SKILL missing from JeisKappa-workflow plugin"; exit 1; }; done &amp;&amp; for SKILL in capture-inbox discussion seeded-discussion propose-auto propose-interactive spec-auto spec-interactive plan-loose-auto plan-loose-interactive plan-strict-auto plan-strict-interactive adjust-plan-granularity-auto adjust-plan-granularity-interactive implement-auto implement-interactive implement-plan-auto implement-plan-interactive implement-plan-with-subagents-auto implement-plan-with-subagents-interactive review-proposal-auto review-proposal-interactive review-spec-auto review-spec-interactive review-plan-auto review-plan-interactive review-implementation-auto review-implementation-interactive review-code-auto review-code-interactive merge-artifacts-auto merge-artifacts-interactive finish whats-next; do jq -e --arg s "$SKILL" '."conventionalCommits.scopes" | index($s)' .vscode/settings.json &gt;/dev/null || { echo "FAIL: $SKILL missing from .vscode scopes"; exit 1; }; done &amp;&amp; grep -qF 'JeisKappa Workflow' README.md &amp;&amp; grep -qF 'the-fool' README.md &amp;&amp; grep -qiE 'no separate.*verify|verification.{0,30}review-implementation|review-implementation.{0,30}verification' README.md &amp;&amp; grep -qF 'discussion-loop' README.md &amp;&amp; grep -qF 'review-decision-document' README.md &amp;&amp; grep -qiE '^## Retired skills' README.md &amp;&amp; ! grep -qE 'npx skills add Jei-sKappa/skills --skill \./skills/' README.md &amp;&amp; ! grep -qE 'npx skills add Jei-sKappa/skills --skill JeisKappa-workflow/' README.md &amp;&amp; ! grep -qiE 'NFA|state.machine|graphviz|finite automaton' README.md &amp;&amp; echo PHASE_7_OK</automated>
  </verify>
  <done>All Phase 7 DIST-01/02/03/05 acceptance criteria verified by automated checks. JeisKappa-workflow plugin has exactly 33 skills (all 33 verified by name). .vscode scopes has exactly 40 entries alphabetical (all 33 V1 verified by name). README.md has the hybrid layout with all 7 sections, all 33 V1 + 7 non-V1 skills in the Per-Module Catalog, every install command flat (no nested path), JeisKappa Workflow rendering documented in Installation note, both Phase 6 documentation notes preserved, Retired Skills subsection preserved with 2 bullets, NO NFA/state-machine notation. Phase 7 milestone is COMPLETE. No file modifications were made in this verification task.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Hybrid layout sections present in README.md:**
   ```bash
   grep -qiE '^## Toolbox' README.md || echo "FAIL: Toolbox Model section missing"
   grep -qiE '^## Layered' README.md || echo "FAIL: Layered Workflow Map section missing"
   grep -qiE '^## Recommended' README.md || echo "FAIL: Recommended Common Paths section missing"
   grep -qiE '^## (Per.Module|Skills by Module|Module Catalog|Catalog)' README.md || echo "FAIL: Per-Module Catalog section missing"
   grep -qiE '^## Retired skills' README.md || echo "FAIL: Retired skills subsection missing"
   grep -qiE '^## Installation|^### Installation' README.md || echo "FAIL: Installation section missing"
   ```

2. **All 33 V1 workflow skills present in catalog with install commands:**
   ```bash
   for SKILL in capture-inbox discussion seeded-discussion propose-auto propose-interactive spec-auto spec-interactive \
                plan-loose-auto plan-loose-interactive plan-strict-auto plan-strict-interactive \
                adjust-plan-granularity-auto adjust-plan-granularity-interactive \
                implement-auto implement-interactive implement-plan-auto implement-plan-interactive \
                implement-plan-with-subagents-auto implement-plan-with-subagents-interactive \
                review-proposal-auto review-proposal-interactive review-spec-auto review-spec-interactive \
                review-plan-auto review-plan-interactive review-implementation-auto review-implementation-interactive \
                review-code-auto review-code-interactive merge-artifacts-auto merge-artifacts-interactive \
                finish whats-next; do
     grep -qF "npx skills add Jei-sKappa/skills --skill $SKILL" README.md || echo "FAIL: install command for $SKILL missing"
   done
   ```

3. **All 7 non-V1 skills present:**
   ```bash
   for SKILL in the-librarian derive-spec afk-exploration consult-the-expert meta-prompting report-to-the-owner brief-the-implementer; do
     grep -qF "npx skills add Jei-sKappa/skills --skill $SKILL" README.md || echo "FAIL: non-V1 install command for $SKILL missing"
   done
   ```

4. **Phase 6 documentation notes preserved:**
   ```bash
   grep -qF 'the-fool' README.md || echo "FAIL: the-fool note must remain"
   grep -qiE 'adversarial' README.md || echo "FAIL: adversarial term must appear in the-fool note"
   grep -qiE 'no separate.*verify|verification.{0,30}review-implementation' README.md || echo "FAIL: verification-coverage note must remain"
   ```

5. **Retired skills subsection preserved (2 bullets):**
   ```bash
   grep -qF 'discussion-loop' README.md || echo "FAIL: discussion-loop retired entry must remain"
   grep -qF 'review-decision-document' README.md || echo "FAIL: review-decision-document retired entry must remain"
   ```

6. **No prohibited notations (V2 deferral):**
   ```bash
   ! grep -qiE 'NFA|state.machine|graphviz|finite automaton' README.md || echo "FAIL: prohibited V2 notation in V1 README"
   ```

7. **Installation note documents plugin grouping rendering (DIST-02):**
   ```bash
   grep -qF 'JeisKappa Workflow' README.md || echo "FAIL: JeisKappa Workflow plugin rendering missing from Installation note"
   ```

8. **Flat install command pattern (DIST-03 — no nested paths):**
   ```bash
   ! grep -qE 'npx skills add Jei-sKappa/skills --skill \./skills/' README.md || echo "FAIL: nested install path detected (should be flat name)"
   ! grep -qE 'npx skills add Jei-sKappa/skills --skill JeisKappa-workflow/' README.md || echo "FAIL: prefixed install path detected (should be flat name)"
   ```

9. **Phase 7 final registration state:**
   ```bash
   jq '.plugins[] | select(.name=="JeisKappa-workflow") | .skills | length' .claude-plugin/marketplace.json    # expect 33
   jq '.plugins[] | select(.name=="JeisKappa-skills") | .skills | length' .claude-plugin/marketplace.json      # expect 7
   jq '."conventionalCommits.scopes" | length' .vscode/settings.json                                              # expect 40
   ```
</verification>

<success_criteria>
- README.md fully redesigned with the hybrid layout per DIST-01, D34, D109.
- Hybrid sections present in order: title + value statement; Toolbox Model; Layered Workflow Map; Recommended Common Paths (3–5 paths); Per-Module Catalog; Retired Skills; Installation.
- Per-Module Catalog covers 9 module families (Capture & Discussion / Propose / Spec / Plan / Implement / Review / Merge / Finish & Navigate / Other).
- All 33 V1 workflow skill names present in the catalog with `npx skills add Jei-sKappa/skills --skill <name>` commands (DIST-03).
- All 7 non-V1 skill names present in the catalog with the same install command pattern.
- Every V1 skill description explicitly states its interaction mode (auto / interactive / single-skill V1 exception for finish / advisory for whats-next) per DIST-05.
- Toolbox Model narrative draws from PROJECT.md Core Value statement (composable, every spine phase optional, every artifact reviewable markdown, harness-agnostic, cross-cutting overlays).
- Layered Workflow Map shows spine + overlay layers as ASCII / table — NO NFA/state-machine notation (V2 per D34).
- Recommended Common Paths section has 3–5 onboarding paths including the canonical full-workflow path.
- Installation note documents JeisKappa Workflow plugin-grouping rendering (DIST-02).
- Phase 6 documentation notes preserved: verification-coverage (D85) + the-fool delegation (D88) — integrated into the new hybrid layout (recommended placement: Review module family).
- Retired Skills subsection preserved with both existing bullets (discussion-loop + review-decision-document) verbatim.
- No git commit created.
- Phase 7 milestone COMPLETE — V1 catalog fully shipped and discoverable.
</success_criteria>

<output>
Create `.planning/phases/07-merge-finish-navigation-distribution/07-03-SUMMARY.md` when done. Record:
- Files modified: README.md (full hybrid redesign — Available skills simple-list REPLACED with 7-section hybrid layout: title + value statement; Toolbox Model; Layered Workflow Map; Recommended Common Paths; Per-Module Catalog (9 module families covering all 33 V1 workflow skills + 7 non-V1 skills); Retired Skills; Installation note)
- Files NOT modified by this task: skills/, .claude-plugin/marketplace.json (already at 33), .vscode/settings.json (already at 40) — Plan 07-02 brought all of these to their Phase 7 V1 final state.
- Requirements addressed: DIST-01 (hybrid README layout per D34/D109), DIST-02 (JeisKappa Workflow plugin grouping rendering documented in Installation note), DIST-03 (per-skill installability via flat `npx skills add` commands), DIST-05 (every V1 skill description states its interaction mode).
- Phase 7 V1 FINAL STATE: 4 new V1 skills shipped (merge-artifacts-auto, merge-artifacts-interactive, finish, whats-next); JeisKappa-workflow plugin at 33 (Phase 7 V1 target reached); .vscode scopes at 40 (Phase 7 V1 scopes target reached); README fully redesigned with hybrid layout (per-module catalog of 33 V1 + 7 non-V1 skills, 9 module families); Phase 6 documentation notes preserved (verification-coverage + the-fool delegation); Retired Skills subsection preserved (2 bullets).
- V1 MILESTONE COMPLETE: All 78 V1 requirements satisfied. ROADMAP can flip Phase 7 to Complete; the milestone overall flips to Complete.
</output>
</content>
</invoke>